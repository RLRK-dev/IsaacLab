# 工程説明付き動画だけを生成・納品する

2026-09-11、ユーザーから「Downloadsにおく動画は0_split_process_*_review.mp4一種類のみでよい」「今後はその1種類のみの動画生成にして」と指示を受けた。

指示時の再確認ではv06の4動画版は10:21:53 JSTに描画・全編デコード・ブラウザー・コピー・ZIP検査を完了していた。元ZIP SHA256は `96cf056ef52a0b8057b897d837d40688100f1e0389d9a2ce3943f87e162cf533`。したがって完成済みの工程説明付きMP4を再生成せずに配布内容を1動画へ変更する。旧v06のステージと検査記録を保持し、Downloadsの旧v06一式は検査後に作業領域へ移して保全する。

今後の生成方針は `data/video_delivery_policy.json`。工程PNGへ工程表示を直接重ねてreview MP4だけを符号化する新エンコーダを使い、raw MP4の中間ファイルや全景動画は生成しない。モデル、動作、画質条件はこの指示で変更しない。元の19個の再ベイク入力スクリプトも保存時のSHAで保全し、新しい出力エンコーダを別に追加する。

既存の工程・全景向けCLIは過去の再現資料であり、新規動画作成の既定には用いない。新しい配布確認では工程映像1本、160章、相対リンク、狭い画面と広い画面を検査する。正式な物理妥当性判定を新たに行ったとは扱わない。

prior-art `process_review / single_video` は0件・blocker0。旧失敗経路の再実行ではなく、ユーザーが明示した出力種類の制限への変更である。
