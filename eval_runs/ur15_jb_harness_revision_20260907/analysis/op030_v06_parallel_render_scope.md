# v06最終動画：残りのフレームを分担して描画

2026-09-11の再開時、工程映像と全景の2プロセスは正常に描画を継続していた。最新の実測は各フレーム約1.6〜1.7秒、GPU占有メモリーは工程2652 MiB・全景2452 MiB。既存の2プロセスを同じ120フレーム単位の協調停止点で止め、完了済みのPNG・manifest・ログを保全した上で、残りを各3区間、計6プロセスに分担する。

再利用先は変更しない `scripts/render_op030_v02.py` の既存 `--first_frame`・`--last_frame`・`--frame_step 2`・`--resume`。保存native、モデル、動作、カメラ、19個の再ベイク用スクリプトは一切編集しない。最終native SHA256は `e65bef607c1d29671fc982d14d72d4c2694420edd284378cb2f1445f18f354ed`。Cycles/OPTIX、16 samples、工程1280×720、全景960×540、15 fps、各7031サンプルを保持する。

prior-art `render_op030_v02 / first_frame / last_frame` は0件・blocker0。先に広い `OP030 / v06 / 納品` 検索で現れた古い失敗の再実行ではない。既知のZIP一覧の除外ミスは修正済みのv06梱包コードを使い、旧bankや旧nativeの動画を合格の代替にしない。今回の具体差分は同じ保存済みフレームを重複しない区間に割り当てる描画スケジューリングだけである。

各プロセスは専用フォルダーへ書き込み、同じmanifestを同時更新しない。区間ごとの全PNG SHA、native/renderer/settings/shot-plan SHA、フレーム集合、カメラを確認後、元のフォルダーへ統合する。既存レンダラーのresumeで全7031フレームのSHAと欠落なしを再確認し、既存の動画化処理へ渡す。意図した協調停止と各区間の完了は別々に記録する。描画の分担を新しい物理判定として扱わない。
