# 再出発（2026-09-12）— v07 の設計は破棄。目標から見直す

**利用者の指示**: 「いままで検討を破棄して、また新たに検討をスタートさせたい」（2026-09-12）。
確認した範囲は 3 点。

| 問い | 回答 |
|---|---|
| 破棄の範囲 | **設計の結論だけ。** モデルの実測と検査スクリプトは残す |
| 新しい検討の出発点 | **目標から見直す。** 軸1／軸2 という枠組み自体も前提にしない |
| ブランチと PR | **`claude/op030-v07-renewal-proposal` と PR #3 はそのまま残す。** 新ブランチで始める |

⛔ **「破棄」は「使わない」であって「消す」ではない。** v07 の文書は残してあり、
`snapshot/op030-v07-before-restart-20260912`（`d3ad8d12ee`）にも固定してある。

---

## 1. まず、この文書が答えないこと

⛔ **新しい目標は、ここには書かない。**

設計は Rs 専権であり、**完了条件は受入基準そのもの**である（`HANDOFF_v07.md` §10）。
「目標から見直す」は、目標を**この席が書き直してよい**という意味ではない。

**v07 は、目標が一度も書かれないまま作業が 4 段階進んだ**（旧 §11.5-k）。
再出発でそれを繰り返さないために、**目標が定まるまで設計に着手しない。**

→ **最初に要るのは §4 の問いへの回答である。**

---

## 2. 破棄するもの（設計の結論）

いずれもファイルは残し、冒頭に破棄の印を付けた。**参照はしてよいが、前提にしない。**

| 文書 | 内容 |
|---|---|
| `analysis/OP030_v07_刷新仕様案.md` | 2 軸の枠組み、案①〜④の試算、実施段階 v07a/b/c/d |
| `analysis/OP030_v07a_異常系詳細仕様.md` | 異常系 4 事象（E1〜E4）、WMSO としての表現 |
| `analysis/OP030_v07b_再分割詳細仕様.md` | 工程順序制約、再分割の全探索 |
| `analysis/OP030_v07c_千鳥両側実装仕様.md` | 6 セル実装、作業 0〜8 |
| `analysis/OP030_v07_Rs問い合わせ.md` | 破棄された設計についての設問。**送られていない** |
| `HANDOFF_v07.md` の §1〜§9・§12 | 現在地、作業一覧、段階表 |

**あわせて破棄される数値** — タクト試算（×1.37 / ×2.05 / ×2.76〜3.44）、
律速と段取りの配分、作業 0〜8 の工程、6 セルの配置。

---

## 3. 持ち越すもの

### 3.1 事実（シーンや設計に依存しない）

| 内容 | 出典 |
|---|---|
| 床は一様。上面 Z = **−0.033**、面積 1575.6 m² | `audit/op030_v07c_floor_contact_probe.json`（source = v06 native）|
| オブジェクト名 → `equipment_id` → 外接寸法（5,581 件） | `audit/op030_v06_equipment_ids.json`（v06 inventory `9bc2e8f5…` に pin）|
| ⭐ `p04` は**表示用の概念経路**。生成元が「ケーブル・曲げ仕様は未選定」と記録 | `scripts/controller_layout.py:41` `:158`（v01_source）|
| ⭐ `CONDUIT_RADIUS = 0.028`。メッシュのリング半径 0.027999999848 と **1.5e-10 m** 差 | 同 `:18` と `audit/op030_v07c_cable_mesh_check.json` |
| ⭐ 床下 8.085 m の部品は**消灯中の橙色信号灯**。異常ではない | `render_ur15_line.py:3852-3858` `:6377-6378` `:3813` |
| セルごとに工具構成が違う（フィンガ 1 手分 21 個、ドライバ 1 本 27 個） | `analysis/op030_v07c_cell_selection.json` の `missing` と `closure_brings_extra` |

### 3.2 道具（そのまま使える）

`scripts/` の検査・計測スクリプト一式。特に:

- `check_co_run_calibration.py` — 11 件の校正。**Blender 不要、この席で走る**
- `check_documents_against_artifacts.py` — 文書と artifact の対応
- `check_fixed_set_mapped.py` — 集合の照合。**構築した集合と同一かを自分で確かめる**
- 計測 5 本（メッシュ／局所軸／光線／掃引／刻み）と抽出 1 本

### 3.3 記録（作法と、過去の誤り）

- **`HANDOFF_v07.md` §11 訂正表 58 件** — 2 席がどう間違えてきたかの記録。**破棄しない。**
  設計の結論ではなく、**同じ形の誤りを繰り返さないための記録**である
- **`HANDOFF_v07.md` §11.5** — 未解決の一覧。設計に依存しない項目（器械の限界）は生きている
- **`HANDOFF_v07_設計側.md`** — この席の動かし方。**そのまま有効**
  （宣言してから走らせる／既知の形で校正する／実行側・pane3 との関係と経路）

### 3.4 持ち越せない測定（シーンに pin されている）

**六セル候補（`4e7c5b0d…`）か修復後（`1091683a…`）の sha に紐づいており、数値は持ち越せない。**
新しい検討が別のシーンを作るなら、**スクリプトを再実行する**（実行側の申告、この席でも `source` を確認済み）。

`op030_v07c_mesh_contact` / `containment_rays` / `cable_room_sweep` / `cable_room_step_check` /
`routing_survey` / `cable_mesh_check` / `crossing_local_axis` / `cell_completeness` /
`mirror_clearance` / `controller_placement`

⚠ **`cable_mesh_check` と `cable_room_sweep` の "cable" という語は、表示用導管を指す。**
実物のケーブルではない（訂正表 53・58）。持ち越し・破棄のどちらでも、この注記は要る。

---

## 4. 再出発の前に決まっていないといけないこと

**この席では書けない。Rs へ。**

1. **何を達成したいのか。** v07 は「タクト短縮（軸1）＋異常系の WMSO 実証（軸2）」と置き、
   軸2を主としていた。⛔ **その枠組みごと前提にしない**のが今回の指示である。
2. **何をもって達成とみなすのか。** v07 では、この条件は**異常系（v07a）にしか書かれていなかった**。
   再出発では**最初に**決める。
3. **対象の範囲。** v07 は OP030 単体を対象にしていた。ライン全体（OP010〜OP040）か、
   OP030 か、別工程か。

**この 3 つが決まるまで、設計・試算・工程の検討に着手しない。**
測ることと道具を作ることは、目標が決まる前でもできる（この席の裁量）。

---

## 5. 変わらない制約

- **v06 を上書きしない。** native `e65bef60…`、B bank `41b353e2…`、ZIP `1c66b66a…`。
  **ZIP は Downloads に戻してある**（2026-09-12、SHA256 再計算で一致）
- **設計は Rs 専権。acceptance を発明しない。**
- **物理妥当性の判定をしない。** VaultProtocol V12 の独立レビュー経路による
- 検査スクリプトは読取専用。シーンを保存せず、world 行列の前後一致を確認する
- push 前に `pre_commit run --files <変更ファイル>`。⛔ 共有ツリーで `isaaclab.sh -f` を走らせない

---

## 6. 体制（変わらない）

| 役割 | 担当 |
|---|---|
| 設計案の作成、試算、スクリプト、記録 | この席（`w2:p1C`）|
| 実行、計測、結果の報告 | 実行側（`w2:p1A` / `isaaclab-70`）|
| ライン・元データ・生成元コードの照会 | **pane3**（`w2:p1B`、codex）※利用者指示 2026-09-12 |
| 検査 | 検査役 |
| **採否の決定** | **Rs** |
| **物理妥当性の判定** | **VaultProtocol V12 の独立レビュー経路** |

経路と作法は `HANDOFF_v07_設計側.md` §4。
