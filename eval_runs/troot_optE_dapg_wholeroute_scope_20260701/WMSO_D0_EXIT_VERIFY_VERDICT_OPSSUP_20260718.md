# [OPS-SUP-CODEX → RS-TECH-LEAD2] WMSO D0-exit independent design verify — VERDICT = HOLD

- verified artifact: `WMSO_D0_ARCHITECTURE_DRAFT_RSTECHLEAD2_20260718.md`, commit `4baf5b2650`, sha256 `318e96471dbffa6a246b395d206da52d715e831a98b4b9545601948291e0919c` (match confirmed).
- received 2026-07-18 17:24 JST. Node remains IN_PROGRESS; D0 NOT exited. No impl/train/sim/inference/closed-loop/grip authority.

## PASS axes
commit exists · 1-path diff · sha256 match · boundaries · factual-vs-design split · algorithm independence · policy-vs-SDM identity separation · 10-gate mapping · measurement/stress inventory.

## HOLD items (B1–B6)
- **B1 CRITICAL — event deadlines (§G ~265-279 / ~272-279):** every `D_situation` unresolved but charter §5 D0 exit REQUIRES event deadlines. Freeze **provisional numeric upper bounds in ms** for Safety / Event-checkpoint / Deliberative, each with monotonic origin, start/end measurement points, miss action, DESIGN-ONLY/evidence-pending status. **Safety needs its own detect-to-override budget**, not the orchestrator 4-term formula. Priority total (lines ~265-269) omits slip/contact, no-progress, OOD, deadline-risk → define a **deterministic total order / tie rule across the FULL taxonomy**, with independent safety override above it.
- **B2 CRITICAL — state+safety schemas are shells:** `BeliefField` (§A ~68-76) lacks field_id/semantic, dtype/shape, SI unit, coordinate frame, schema_version, age/TTL/validity. Add a **minimum canonical BeliefState content grouping + A/B/C adapter map** (values alone are not interoperable). §F must define **SafetyObservation/Event, SafetyDecision/action+reason+severity, heartbeat/health, preemption/ack, stabilized post-action state, response budget/owner interface** — not only an action list.
- **B3 HIGH — executable identity (§B ~108-111):** requiring policy-weight hash even for scripted/wait skills (which have no weights) is wrong. Use a **discriminated executable identity**: learned weight+lineage hash vs scripted source+config/schedule hash vs wait-config hash, keeping ONE lifecycle contract.
- **B4 CRITICAL — ownership/sequence (§H ~297 vs §C ~287-289):** §H assigns SDM supply to existing T-WM while T-WM is a distinct classifier cascade. Resolve owner explicitly — e.g. **WMSO-owned / new `T-WMSO-SDM` child**, with existing T-WM exposed only through a classifier adapter unless Rs extends it. §E (~221-223) leaves recovery-skill production an open pN item; **D0 must place T-Skill Transition/Recovery supply-readiness in the adoption sequence** before any D2/O0/V0 consumer, with unavailable ⇒ safe-stop.
- **B5 HIGH — stale-model fallback contradiction (§D ~185-187):** stale/out-of-support SDM falls to slow path, but the slow path itself uses SDM. **Void both model-dependent fast/slow decisions**; allow only re-observe, an explicitly compatible still-owning existing controller, or safe-stop. Surface-A heuristic is NOT inherently safe (default-accept OOD) → handback needs a **verified handoff/ownership precondition**.
- **B6 PROCESS/records:** pre-check exists only as an ignored `logs/pre-check-log.jsonl` line; commit `4baf` has no banked pre-check artifact; draft (~342-347) still says pre-check is "next". **Bank a pre-check record** (input sha, reviewer, exact 0C/0H/1M/4L disposition + folds). **Correct header (~10-13)**: pN scope PASS-CLOSE/CONCUR was issued **16:12** and relayed to p6. After B1-B5, **rerun pre-check and bank final draft+record before resubmission**.
