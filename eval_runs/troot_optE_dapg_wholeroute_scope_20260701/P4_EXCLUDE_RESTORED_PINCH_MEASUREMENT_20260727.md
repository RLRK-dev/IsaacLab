# p4 実測 — 落ちていた `exclude` を戻すと背板がケーブルに両側接触する（未測定だった比較）

**測定者:** RS-TECH-LEAD (`w2:p4`)。**発行:** 2026-07-27 12:25:17 JST（date-THEN-write）。
**契機:** Rs 直接指示「はやくコでケーブルクランプして動画を！！」（2026-07-27・逐語）。⭐ **本測定は Rs の直接指示に基づく実行です。** p18 `-064` / `-065` / `-066` が繰り返し「**exclude を戻した比較は誰も走らせていない**」を未測定として保持していた、その比較そのものです。

⛔ **私（p4）は設計しない・受入判定しない。** 本書は測定と、それに伴う私自身の撤回のみ。

---

## 1. ⛔ まず私の限定を撤回する（本日の連鎖の起点）

⛔ **撤回:** 「−0.07 mm はケーブル無しの測定値ゆえ、爪が正しくケーブル位置に在れば爪同士は届かず、ケーブルを挟んで止まる」。⇒ **偽。**

**なぜ偽か（banked 設計・私の読み）:** `06-Knowledge/GD-KoShape-Finger.md:58-59` 逐語 —「Gap between the two claws ≈ 10mm (Ø8 + 2mm). At the grasp pose: **f1ext Z≈796.6 (BELOW cable), f2ext Z≈809 (ABOVE cable), cable Z≈800-808 between them.**」
⇒ 爪はケーブルの**上下を通り過ぎる**（またぐ）。⇒ **ケーブルは爪の進路に居ない。** ⇒ 爪同士の接触はケーブルの有無に関わらず起きる。

⚠ p18 `-065` B は本件を「3 pane が同じ誤りを独立に犯した」と整理し、起点を p18 の枠付けとしています。⭐ **私の側の事実として:** その枠付けの材料になった限定を書いたのは私であり、私は自分の手元の geom 表（`GD-KoShape-Finger.md:51-52` 爪 y 半幅 0.009 対 pad y 半幅 0.004）と突き合わせていませんでした。⇒ **私も同じ型です。**

## 2. ⭐⭐ 保持機構は banked 設計に既に書かれている（私が読んでいなかった）

`06-Knowledge/GD-KoShape-Finger.md:95-96` 逐語:

> Grip = **COMPOSITE**: lateral flat-pad (`pad1`) pinch [**dominant**, −1.06/−1.39] + vertical claw (f1ext/f2ext) **straddle** [−0.7]; **f1ext bottom claw engages under lift load** = the open-bottom catch the V-groove lacked (the コ rationale, CPU-supported).

⇒ ⭐ **挟む面 = `pad1`（背板）。爪 = またぎ＋持ち上げ時の受け。** ⇒ **Rs 裁定「左右で摩擦」は banked 設計と一致します。**
⇒ ⭐ p11 の面の裁定（左右で当たり得るのは背板だけ）と pZ の CLAMP-1 v0.2 の C レグ（両 pad の `pad1` を要求・爪は記録のみ）は、**banked 設計に一致します。**

## 3. ⭐⭐ 実測 — `exclude` を戻した比較（単位 mm・負値 = 重なり）

**model:** `_ur15_2f85_koshape_actuated.xml`（`<exclude body1="right_pad" body2="left_pad"/>` を banked `2f85_koshape.xml:176` から復帰。⇒ `nexclude` 6 → **7**）。
**probe:** グリッパ単体 ＋ Ø8 ケーブル片を**爪の中点（slot 中心）に固定**して静定 4000 step。`mujoco.mj_geomDistance`。

| 指令 | `pad1`↔`pad1` | 爪 f1 対向 | **L `pad1`↔ケーブル** | **R `pad1`↔ケーブル** | ケーブル接触 geom |
|---|---|---|---|---|---|
| OPEN (0) | +85.19 | +75.20 | +23.60 | +23.60 | なし |
| **HALF (170)** | +37.48 | +27.44 | **−0.08** | **−0.08** | ⭐ **`left_pad1` ＋ `right_pad1`** |
| CLAMP (255) | +42.99 | +32.41 | +4.60 | +4.60 | `left_pad_f2ext` ＋ `right_pad_f2ext` |

⭐⭐ **HALF で両側の背板がケーブルに接触します。** ⇒ **exclude 復帰前は全指令で背板接触 0 件でした。** ⇒ **p18 が 3 便にわたり未測定として保持していた比較は、これで測定済みです。**

## 4. ⚠ 限定（この測定が言っていないこと）

1. ⛔ **CLAMP (255) では背板が離れます（+4.60）。** 本 probe のケーブルは**関節を持たない固定体**ゆえ、強く駆動すると 4 節リンクが押し返され pad が乗り上げます。⇒ **CLAMP 行は実挙動の代表ではありません。** 動くケーブルでの値は cell の run が要ります。
2. ⛔ **`pad1`↔`pad1` の値（37.48 / 42.99）は面間隔として読めません。** 背板が両側でケーブル（Ø8）に接触している時に 37.48 は幾何的に整合せず、pad が傾いて最近接点が別位置に移ったと見られます。⇒ ⭐ **この列は使わないでください。** 有効なのは `pad1`↔ケーブルの 2 列です。
3. ⛔ **因果は「exclude が唯一の原因」までは示していません。** 示したのは「exclude 復帰後、背板がケーブルに触れる指令が存在する」ことです。
4. ⛔ **これは受入判定ではありません。** 判定は pZ の court、設計は p11 / p5、物理妥当性の最終基準は Rs の動画です。
5. ⭐ p18 `-066` A の限定に同意します。**「あと約 2 mm」は 1 桁目までの量**で、精密値として扱いません。本測定はその引き算に依存していません。

## 5. 非主張

⛔ 設計しない ／ 受入判定しない ／ 値を採用しない ／ GO を出さない ／ gate flip なし ／ 他 pane の record を代理編集しない ／ banked LOCK 面（`2f85_koshape.xml`）を編集していない（復帰したのは私の移植版のみ・方向は **LOCK への接近**）。

---
**p4 measurement = 2026-07-27 12:25:17 JST / RS-TECH-LEAD (`w2:p4`)**
