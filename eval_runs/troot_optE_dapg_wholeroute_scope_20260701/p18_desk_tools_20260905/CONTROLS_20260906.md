# hub_send.py — build controls and gate outputs (2026-09-06)

Landing commit `2f154e9e21` (hooked bytes sha256 3294a04d…170a); post-landing fix (fused_with counts head-shaped tokens `MSG m-p18-N /` at any position — in every fused record the next head is glued to the previous footer with no newline) = the commit that adds this file. Written 2026-09-06 09:25:28 JST.

## Code gate A (detached worktree /home/rlrk/wt_hub_send @ HEAD)
- pre-commit 4.5.1 file-limited on hub_send.py: pass 1 = ruff 16 findings auto-fixed + ruff-format reformatted; pass 2 and 3 = all hooks Passed. Copy-back sha equality printed and equal.
- validate.sh --staged-only with the working-tree validator (validate.sh blob e7aa2636a31c, check_control_method.sh 971231d15dda) in the worktree: RESULT FAIL (18 failures) — all about the tree baseline (untracked audit/guard scripts absent in a HEAD checkout; Layer 8 joint-position writes in envs/); no layer reads eval_runs/*.py. Output: validate_staged_out.txt (this directory).

## init
- floor = 321 by the closed query (max m-p18-N over ~/.claude/projects/-home-rlrk-IsaacLab/*.jsonl ∪ git grep ∪ every /tmp/claude-1000/-home-rlrk-IsaacLab/*/scratchpad). The true last sent id was 315; 316–321 occur only as hypothetical ids inside the cycle-2 challenger reports in the scratchpad — the query counts any token, which is the conservative side (ids need only be unique; gaps are harmless).

## Controls (node ③ letters)
- (a) composer-only → NOT delivered; Enter → DELIVERED: by hand 2026-09-05 11:19 on w2:pB (by_hand_20260905/CONTROL_A_20260905.md).
- (b) queued → QUEUED/ABSORBED, not DELIVERED — **a classifier replay** (scan() over the destinations' transcripts, read-only) of the 15 by-hand Tab events; the tool's own Tab branch (send-keys Tab → 0.5 s viewport read) has 0 live executions. Label mapping: the pre-registration said "4 DELIVERED(fused), 1 DELIVERED"; the tool labels position 0 of a record as DELIVERED even when the record holds other hub messages (fused_with non-empty), so the measured labels are 4 DELIVERED + 1 DELIVERED(fused) while the record-level count is 4 multi-message records + 1 standalone (m-p18-300 → w2:p4) — the same fact under two label schemes, stated here because the acceptance text had called it an exact match:
```
m-p18-290 -> w2:p0: ABSORBED(unacked) queued_command@27925501 head_found=False fused_with=[]
m-p18-290 -> w2:pZ: ABSORBED(unacked) queued_command@16007554 head_found=False fused_with=[]
m-p18-291 -> w2:pZ: DELIVERED user@16382141+0 head_found=True fused_with=['m-p18-292', 'm-p18-293']
m-p18-292 -> w2:p4: DELIVERED user@5823590+0 head_found=True fused_with=['m-p18-293']
m-p18-292 -> w2:pZ: DELIVERED(fused) user@16382141+920 head_found=True fused_with=['m-p18-291', 'm-p18-293']
m-p18-294 -> w2:p0: DELIVERED user@28290420+0 head_found=True fused_with=['m-p18-295']
m-p18-296 -> w2:pZ: ABSORBED(unacked) queued_command@16662649 head_found=False fused_with=[]
m-p18-297 -> w2:pZ: ABSORBED(unacked) queued_command@16733390 head_found=False fused_with=[]
m-p18-298 -> w2:pZ: ABSORBED(unacked) queued_command@16817917 head_found=False fused_with=[]
m-p18-298 -> w2:p0: ABSORBED(unacked) queued_command@28461743 head_found=False fused_with=[]
m-p18-299 -> w2:pZ: ABSORBED(unacked) queued_command@16819299 head_found=False fused_with=[]
m-p18-300 -> w2:pZ: ABSORBED(unacked) queued_command@16873507 head_found=False fused_with=[]
m-p18-300 -> w2:p4: DELIVERED user@6008770+0 head_found=True fused_with=[]
m-p18-301 -> w2:pZ: ABSORBED(unacked) queued_command@16927198 head_found=False fused_with=[]
m-p18-304 -> w2:p4: DELIVERED(turn_end) user@6250029+0 head_found=True fused_with=[]
summary: {'ABSORBED(unacked)': 9, 'DELIVERED': 4, 'DELIVERED(fused)': 1, 'DELIVERED(turn_end)': 1}
```
- (c) working destination without --queue → HELD(working), nothing sent: real run against the hub's own pane (--control) while working → held row id m-p18-322 (sent_records.jsonl row 2); transcript untouched. ⚠ The hub's own pane is `working` for as long as it runs the command, so this run could not have come out differently; it exercises the branch, not the reading of a foreign desk's status. To be re-run in dry-run against a genuinely working other desk when one is working, and banked here.
- (d) HELD → resend → DELIVERED: to be measured on the next real checkpoint message that meets a working desk (resend --id).
- (g) send --to COORD → refused(retired) before any read; no row written.
- (h) ghost half: the two raw ANSI composer lines of 2026-09-05 15:37 (the cycle-2 record holds them only as prose; the raw reads lived on the scratchpad), banked here escaped — w2:pZ line 62 `\u276f\u00a0\x1b[0m\x1b[2mMSG m-p18-312 / w2:p18 → w2:pZ IMPL-VERIFIER\x1b[0m` (sha256 of the raw line 2b061dd2da62805013bb89cc3dfae626ac57a757642f68756a6ea8cc25d966be), w2:p4 line 63 `\u276f\u00a0\x1b[0m\x1b[2mpush 認可\x1b[0m` (sha256 d7c80075a44d448ac62ef17d2be96a72ca5b06fc84aacc2b7e54f0eaddb64d9e); dim_only() = True on both after 6e91c3295d (SGR tokens 0, 2, 0). Draft half: awaits Rs1's two characters on w2:pB.

## Post-landing fixes from 層5 view 1 (SSOT compliance, 2026-09-06 09:31) — applied 2026-09-06 17:08 JST
- `dim_only()` now parses SGR parameters as a list: only a standalone `2` means dim; `38;5;n`, `38;2;r;g;b`, `48;…` consume their arguments (measured: truecolour/256-colour drafts were read as dim before; now False).
- `verify --always_write` removed (a CLI argument absent from the authorization; CLAUDE.md:46). `verify` writes a row only when the state changes.
- The held row's state is no longer double-wrapped (`HELD(working)`, not `HELD(HELD(working))`); the existing row 2 keeps its historical string.
- `init` counts only delivered head lines (transcripts: content starting with `MSG m-p18-N /`; files: line-start heads), not bare mentions. Re-measured floor by the refined query (transcripts, delivered heads only) = 322 at 17:08 JST, 323 after m-p18-323 was allocated; `bodies/.floor` is a lower bound and gaps are harmless.
- Open per view 1: ② needs a real delivery by the tool (the first real message closes it); ③(b)'s node wording ("queued → QUEUED, not DELIVERED") — p6 was asked by m-p18-323 (17:10) and reconciled it at 358a1d72ad (17:15); ③(d) awaits the first HELD on real traffic; ④ awaits the post-build records.

## First real message through the tool (m-p18-323 → w2:p6 PLAN-KEEPER, 2026-09-06 17:11 JST) — text placed by the tool, Enter pressed by the desk, record read by the tool
- `send` composed and placed the 5-line text; the post-send gate read the composer as `[Pasted text #2 +4 lines]` (Claude Code folds a multi-line paste) and, not seeing the head line, held before any keypress (`HELD(foreign_text_in_composer)`, row 3). The desk read the composer (marker = 1 + 4 lines = the sent text; pane idle), pressed Enter once by hand, and ran `verify --id m-p18-323` (row 4 = the verify result). Fix: the gate now accepts a folded-paste marker whose line count equals the sent text's line count as "landed" (a pre-existing paste before the send still holds).

## Post-build 層2/層5 cycle 1 (2026-09-06 17:12–17:40 JST) → fixes landed at ee24742ec6 (hub_send.py sha256 c19dd27c264d2b44657f54e33cd35531193aa2ac290836922e8624e5ebc5dc34, 903 lines; three hook passes all Passed, bytes unchanged)
Bodies: CC2 premise 12 (H2) · CC3 rule 17 (H4) · CC4 numeric 13 (H2) · CC5 side-effects 15 (H4) · CC6 NHA CHANGE_JUSTIFIED · 層5 view 2 PARTIAL · view 3 PARTIAL. Union + dispositions = the ledger entry §1447 (28 rows, U1–U28). DECIDE = FAIL → this fix cycle (cycle 2 of 2 follows on the landed state).
- U1 gate: the folded-paste marker is accepted only when it is the whole composer line (`re.fullmatch`); the marker text is stored as `landed_as`. Residual, declared: a foreign paste of the same line count arriving between the pre-send read and the post-send read (no byte identity is visible in a fold).
- U2 verify: verify rows copy `sent_at`, `via`, `enter_at`, `session_id`, `to`, `cc`, `control`, `resend_of`, `to_pane`, `resolved_by`; a per-row error prints `verify_error(...)` and the loop continues (measured on a synthetic 4-pass history: QUEUED → QUEUED (no row) → DELIVERED(absorbed,acked) → skipped).
- U3 `send --id`: re-sends the exact bytes of `bodies/<id>.txt` (no recompose; sha equals the held row's), refuses `refused(unknown_id)` / `refused(no_body)` / `refused(body_mismatch)` / `refused(member_not_in_fanout)` (dry runs printed all four).
- U4 `--to_pane`: consulted before role resolution; head role = the pane's live label; `resolved_by` and `to_pane` in every row; `resend` reuses the pane. Dry run: `--to NOSUCH --to_pane w2:pB` → head `→ w2:pB LOG-ANALYST`, `resolved_by: pane`; `--to_pane w2:pV` → `refused(retired)`.
- U5 verify refuses a row without a body (`refused(no_body)`, rc 1, nothing scanned); `scan()` raises on an empty needle.
- U6/U7 dry-run is write-free (resend passes the body in memory; `init` creates nothing before its write branch); `--dry_run` before the subcommand is an argparse error.
- U8 own-message set from the latest state per (id, pane); a dim ghost of our own head is kind `ghost`. U9 guard on verify/init. U10 agent status re-read before the keypress; a working/idle flip → `HELD(status_changed:a->b)`, no key. U11 held rows carry per-member status/kind/sha/reason; send rows carry `status` and `status_at_keypress`. U12 `DELIVERED(fused_with_unknown_prefix)` + the prefix's first 200 chars printed (never stored); the agent-type leg of T/C stays unimplemented: `herdr agent list` reports `agent_type` None for all 16 agents (declared deviation). U13 the queue hint is kind `queue_hint`. U14 a post-send HELD stops the fan-out (`HELD(fanout_stopped)` rows for the members not yet sent). U15 `row_type: resend` + `resend_of`; control rows are not resent; footers stripped by pattern. U16 init: transcripts read line by line (the transcript leg: 0.82 s, 23 MB RSS; was 5.4 s, 1.1 GB — the whole `init` is ≈8 s because its `git grep --untracked` child walks the shared tree at ≈1.87 GB peak; one-shot, and `init` refuses to write while `.floor` exists), id-shaped files of the hub's own scratchpad (⚠ cycle 2 showed this leg still counted verifier fixtures inside the hub's own scratchpad — the measured 323→900 fixture had lived there, not in another session — so the closing commit narrows it to the retired by-hand directories `ids_retired_*/` and `desk_msgs_retired_*/` only), `git grep --untracked` (head-shaped tokens at any position, not only delivered heads), fields `transcripts` (28) and `paths_scanned` separately, `hub_session_id`; refined query in READONLY on the landed state = 323 at 17:47, 324 after m-p18-324. U27 `REMOVED(reason=absent)`; unmapped queue ops only on our bytes. DELIVERED rows carry `line` (m-p18-323 → 25733 = p6's own reading).
- Size: 903 lines against the spec's target ≤250 — declared deviation (a split into a module would be a new file → Rs1's word).
- Not built (behaviour additions, CLAUDE.md:46 → proposals to Rs1): an in-tool completion that re-reads a held member's composer and presses once; a hand-written `keypress` row. Until then a keypress outside the tool is recorded as a verify row after a `via: none` send row (docstring), plus a ledger line.
- Also caught by the dry runs, not by a body: the heredoc that produced this version had turned the composer prompt's U+00A0 into a space (every pane read as no_composer/ambiguous_composer); the prompt is now written as escapes. Sha pins of the two untracked scripts (verification_log_append.py 30509c34…, check_thread_vault_prior_art.sh b8d85cdb…, 2026-09-06 17:38) are in the docstring.
- Operating rules while the tool carries all hub traffic (CC6 post-build): a HELD on real traffic → read the composer → at most one Enter by hand after the read → `verify --id`; `--queue` only with the same-matter assertion, every QUEUED/ABSORBED row closed by `verify` before the next checkpoint report; the first tool-alone delivery is banked as the ② scope closure; subagents never without `HUB_SEND_READONLY=1 … --dry_run`.
Written 2026-09-06 17:49:24 JST.

## Post-build 層2/層5 cycle 2 of 2 (2026-09-06 17:52–22:29 JST; 8 bodies, 6 relaunched after the 22:00 limit reset) → closing commit 5b60b1556d (hub_send.py sha256 a0a099145bf2e97cd08188180d138d0897c468ae643389859c9e0c4274d89c4d, 1016 lines; three hook passes 14 Passed each, bytes unchanged) — **the code is frozen at this blob**
Bodies: CC2 9 (H0 M5 L4) · CC3 8 (H0 M3 L5) · CC4 10 (H0 M1 L9) · CC5 7 (H0 M2 L5) · CC6 CHANGE_JUSTIFIED (freeze recommended) · 層5 view 1 PARTIAL (FAIL only the two unratified files) · view 2 PARTIAL (docstring numbers) · view 3 PARTIAL (S3 declared residual; new S22). Union + dispositions = ledger §1450 (V1–V25). DECIDE = PASS with accepted MEDIUM/LOW → this closing commit, then freeze; unfreeze only on a real-traffic failure that is not fail-closed (a keypress on foreign text, a DELIVERED row without a matching destination record, a duplicate id) — a fail-closed failure gets the documented recovery and a ledger line, no code.
- V1 `send --id` completes only members never handed to `herdr agent send` (a pre-send held row or `HELD(fanout_stopped)`); every other member prints `skipped(<state>)` before any read (dry runs: `--id m-p18-323` / `--id m-p18-324` → `skipped(DELIVERED)`, `nothing to complete`). The own-message set is built from pane-keyed rows (a held row counts only while its id has no send row).
- V2 `verify` keeps a HELD state unless it observes something (`-> kept (nothing observed)`), skips `HELD(fanout_stopped)` rows, computes `overdue` only for rows with a keypress, and treats `QUEUED(observed:viewport)`/`QUEUED(observed)` as one state.
- V3 any exception after the text was placed still appends the row (`HELD(error:<Type>)` before the keypress, `UNKNOWN(error:<Type>)` after) and then propagates (`send_one` → `_after_send`).
- V4 an agent whose `agent` kind is not `claude` is refused pre-send (`refused(agent_type_not_banked: <kind>)`) — `herdr agent list` carries `agent: 'claude'` on all 16 (the earlier "cannot be implemented: agent_type is None" looked at the wrong key); stricter than the spec's `UNKNOWN(table-not-banked)`, fail-closed, declared.
- V5 `--to_pane`: a `--to` that resolves uniquely to another pane → `refused(contradiction)` (dry run: `--to PLAN-KEEPER --to_pane w2:pB` → `refused(contradiction): --to PLAN-KEEPER resolves to w2:p6, not w2:pB`); an unlabelled pane → `refused(unlabelled_pane)`; a live label outside the roster → `refused(unregistered_label)` unless `--control`; rows carry `to_requested`.
- V6 init's scratch leg = `ids_retired_*/` + `desk_msgs_retired_*/` of the hub's own scratchpad only (dry run: own id files 64, was 67). V10 a status outside idle/done/working at the keypress holds. V11 `init --dry_run` prints the query beside the live floor (`dry_run floor: 324 (live .floor = 324)`). V12 `strip_body` pops at most one resend line then one footer (a quoted footer inside a body survives). V13 `HELD(paste_count_mismatch:K+1!=n)`. V14 rows carry `send_rc`/`keypress_rc`; a refused keypress → `HELD(keypress_refused:rc=N)`, via none. V15 ABSORBED rows carry `absorbed_at` = the remove record's timestamp.
- V16 docstring: the fold/identity note, the ≈0.8 s flip window, `+k` = character index, `origin.kind` never discriminates, `HUB_SEND_REPO` declared, the repo leg counts head-shaped tokens, "record 18-23 ms (n=2; m-p18-324 = 40 ms, n=3)", "~98% of removes: 2600/2634 on 09-05, 2561/2603 on 09-06", one leg of each n=2 pair has no banked keypress stamp.
- Records: by-hand `sent.jsonl` = 59 rows = 15 Tab-queue + 43 Enter-path (35 `via=Enter` + 8 old-schema) + 1 verify (the PROPOSE's "42 Enter" was 43). Row-schema extras beyond spec §4, declared: `head`, `role`, `status`, `status_at_keypress`, `landed_as`, `line`, `to_pane`, `to_requested`, `resolved_by`, `send_rc`, `keypress_rc`, `absorbed_at`, `child_session`, `resend_of`. The `validate.sh --staged-only` leg was re-run on the closing blob in the worktree with the validations sub-scripts copied in: `RESULT: FAIL (2 failures, 13 warnings)` = the audit and cable-model guard scripts absent from a HEAD checkout + the known kinematic-arm-drive sites (`validate_staged_out.txt`); the landing's "18 failures" came from the sub-scripts not having been copied and Layer 8 failing instead of warning; no layer reads this file.
- Operating rules added: the row, body and `.floor` of a send are committed (pathspec) in the same turn as the send (the tracked runtime files otherwise sit modified in the shared tree, measured 74–97 s per send); a message body cites only artifacts that exist at send time, with full shas or none.
- Not built (Rs1's word): a regression-test file for `hub_send.py` (new file; today the unit/dry-run outputs below are the test); the module split (size 1016 vs ≤250); the in-tool completion keypress; the hand-keypress row; ratification of `CONTROLS_20260906.md` + `validate_staged_out.txt` (+ the record-file form of ④ if preferred over the ledger's `<details>`).
- ③(c) stays "branch exercised on the hub's own pane (cannot fail); foreign-desk dry-run pending" — no foreign w2 desk was working at 22:11 (CC3) or 22:13 (CC2); the dry-run runs at the first moment one is.

### Outputs of the closing blob's checks (banked verbatim; `HUB_SEND_READONLY=1`, `--dry_run` after the subcommand)
```
### unit_v4.py on the closing blob
PASS marker exact
PASS marker prefix rejected
PASS marker suffix rejected
PASS two markers rejected
PASS marker no count
PASS marker with spaces
PASS queue hint + queue → proceed
PASS queue hint no queue → HELD(working) HELD(working)
PASS ghost of own head → proceed ghost
PASS own head non-dim → HELD(own) HELD(composer_holds_own_message id=m-p18-323)
PASS draft → HELD(draft) HELD(draft)
PASS paste → HELD(paste) HELD(paste_in_composer)
PASS empty idle → proceed
PASS strip_body 323 = 3 lines '(1) 何をしたか: hub 送信計器 hub_send.py が着地（2f15'
PASS strip_body resend-of-resend '(1) a\n(2) b'
PASS norm_body
PASS scan 323 DELIVERED line 25733 {"state": "DELIVERED", "evidence": "user@91049910+0", "fused_with": [], "head_found": true, "delivered_at": "2026-09-06T08:11:41.053Z", "line": 25733}
PASS empty needle refused
unknown_prefix (first 200 chars, not stored): 'ab'
PASS unknown prefix state {"state": "DELIVERED(fused_with_unknown_prefix)", "evidence": "user@258+2", "fused_with": [], "head_found": true, "delivered_at": "t1", "line": 3, "unknown_prefix_len": 2}
PASS REMOVED(reason=absent) REMOVED(reason=absent)
PASS unmapped op on our bytes UNKNOWN(unmapped_op=weird)
m-p18-950 w2:p6 UNKNOWN(no-observation) -> QUEUED(observed) enqueue@0
PASS verify pass1 wrote QUEUED row with sent_at/via {"id": "m-p18-950", "pane": "w2:p6", "role": "PLAN-KEEPER", "to": "PLAN-KEEPER", "cc": [], "session_id": "s", "transcript_path": "/tmp/claude-1000/-home-rlrk-IsaacLab/1c3d805c-2a9a-4b6d-bba2-ae7d47986
m-p18-950 w2:p6 QUEUED(observed) -> QUEUED(observed) enqueue@0
PASS verify pass2 no crash, no new row
m-p18-950 w2:p6 QUEUED(observed) -> DELIVERED(absorbed,acked) assistant@563
PASS verify pass3 acked → DELIVERED(absorbed,acked) {"id": "m-p18-950", "pane": "w2:p6", "role": "PLAN-KEEPER", "to": "PLAN-KEEPER", "cc": [], "session_id": "s", "transcript_path": "/tmp/claude-1000/-home-rlrk-IsaacLab/1c3d805c-2a9a-4b6d-bba2-ae7d47986
PASS verify pass4 final skipped
m-p18-951 w2:p6 refused(no_body): row skipped, nothing scanned
PASS verify missing body refused rc1 no row
PASS recorded_members mixed {'w2:p0', 'w2:p4', 'w2:pZ'}
ALL PASS

### unit_v5.py on the closing blob
PASS strip_body keeps a quoted footer line '(1) a\n2026-09-06 17:20:00 JST (hub_send.py)'
PASS strip_body plain
PASS strip_body real m-p18-322
PASS strip_body real m-p18-323
PASS strip_body real m-p18-324
PASS absorbed_at = remove ts {"state": "ABSORBED(unacked)", "evidence": "queued_command@405", "fused_with": [], "head_found": false, "delivered_at": "", "absorbed_at": "t1"}
m-pXX-950 w2:pA HELD(foreign_text_in_composer) -> kept (nothing observed)
m-pXX-950 w2:pC QUEUED(observed:viewport) -> ABSORBED(unacked) queued_command@405
PASS verify: HELD kept, fanout_stopped skipped, QUEUED viewport→ABSORBED is a change [["w2:pC", "ABSORBED(unacked)", true]]
m-pXX-950 w2:pC QUEUED(observed:viewport) -> QUEUED(observed) enqueue@0
PASS verify: QUEUED label change writes no row
m-pXX-950 w2:pA HELD(foreign_text_in_composer) -> DELIVERED user@0+0
PASS verify: HELD → DELIVERED after hand Enter, overdue None (no keypress) {"state": "DELIVERED", "overdue": null, "via": "none"}
PASS own set rule ['m-pXX-1', 'm-pXX-3']
PASS to_pane contradiction
PASS to_pane same label ok
PASS to_pane NOSUCH ok (no contradiction)
PASS to_pane unlabelled refused
PASS to_pane label not in roster refused
PASS to_pane label not in roster ok with control
PASS agent kind codex refused (resolve)
PASS agent kind codex refused (to_pane)
ALL PASS
```
```
$ hub_send.py send --to PLAN-KEEPER --body_file b_test.txt --dry_run
dry_run row: {"id": "dry-run", "body_sha256": "04c0aa800fa6624b24ed4bb9f01126a97b757e7cf5ecbe2be97624a7f08dc118", "head": "MSG dry-run / w2:p18 / OPS-SUPERVISOR → w2:p6 PLAN-KEEPER", "to": "PLAN-KEEPER", "to_requested": "PLAN-KEEPER", "cc": [], "to_pane": "", "resolved_by": "label", "hub_session_id": "1c3d805c-2a9a-4b6d-bba2-ae7d479862e7", "child_session": "1", "herdr_version": "herdr 0.7.1", "c
$ hub_send.py send --to PLAN-KEEPER --to_pane w2:pB --body_file b_test.txt --dry_run
refused(contradiction): --to PLAN-KEEPER resolves to w2:p6, not w2:pB
$ hub_send.py send --to NOSUCH --to_pane w2:pB --body_file b_test.txt --dry_run
resolved_by=pane w2:pB label= LOG-ANALYST
dry_run row: {"id": "dry-run", "body_sha256": "2e3ea07a82a4cd76e8f8fe7e67ffba49f258b45e084244d82c6a728c51acedc6", "head": "MSG dry-run / w2:p18 / OPS-SUPERVISOR → w2:pB LOG-ANALYST", "to": "LOG-ANALYST", "to_requested": "NOSUCH", "cc": [], "to_pane": "w2:pB", "resolved_by": "pane", "hub_session_id": "1c3d805c-2a9a-4b6d-bba2-ae7d479862e7", "child_session": "1", "herdr_version": "herdr 0.7.1", "co
$ hub_send.py send --to X --to_pane w2:pV --body_file b_test.txt --dry_run
refused(retired): w2:pV carries the retired label COORD
$ hub_send.py send --to PLAN-KEEPER --id m-p18-323 --dry_run
w2:p6 PLAN-KEEPER: skipped(DELIVERED)
m-p18-323: nothing to complete
$ hub_send.py send --to LOG-ANALYST --id m-p18-323 --dry_run
refused(member_not_in_fanout): ['w2:pB'] not in ['w2:p6']
$ hub_send.py send --to PLAN-KEEPER --id m-p18-323 --body_file b_diff.txt --dry_run
refused(body_mismatch): --body_file differs from the stored body of m-p18-323
$ hub_send.py send --to OPS-SUPERVISOR --control --id m-p18-322 --dry_run
w2:p18 OPS-SUPERVISOR: HELD(working) status=working composer=empty
dry_run row: {"id": "m-p18-322", "body_sha256": "f38367484c6f3f37231c6226d229ced81e5ce1ec06fc5893af95e4e4dd48a72f", "head": "MSG m-p18-322 / w2:p18 / OPS-SUPERVISOR → w2:p18 OPS-SUPERVISOR", "to": "OPS-SUPERVISOR", "to_requested": "OPS-SUPERVISOR", "cc": [], "to_pane": "", "resolved_by": "label", "hub_session_id": "1c3d805c-2a9a-4b6d-bba2-ae7d479862e7", "child_session": "1", "herdr_version": "he
$ hub_send.py resend --id m-p18-324 --dry_run
dry_run row: {"id": "dry-run", "body_sha256": "d710760c782c17b2bf7e25eb54c752b8673c4132fff559bd0c62787afa57b1fe", "head": "MSG dry-run / w2:p18 / OPS-SUPERVISOR → w2:p6 PLAN-KEEPER", "to": "PLAN-KEEPER", "to_requested": "PLAN-KEEPER", "cc": [], "to_pane": "", "resolved_by": "label", "hub_session_id": "1c3d805c-2a9a-4b6d-bba2-ae7d479862e7", "child_session": "1", "herdr_version": "herdr 0.7.1", "c
$ hub_send.py resend --id m-p18-322 --dry_run
refused(control_row): m-p18-322 was a control send to the hub's own pane; not resent
$ hub_send.py verify --dry_run
$ hub_send.py init --dry_run
dry_run floor: 324 (live .floor = 324) | transcripts 28 | own id files 64
$ hub_send.py --dry_run verify
usage: hub_send.py [-h] {send,verify,resend,init} ...
hub_send.py: error: unrecognized arguments: --dry_run
```
Written 2026-09-06 22:37:38 JST.
