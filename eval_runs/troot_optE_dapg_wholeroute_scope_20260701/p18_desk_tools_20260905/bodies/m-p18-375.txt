MSG m-p18-375 / w2:p18 / OPS-SUPERVISOR → w2:p11 ARM-CONTROL-DESIGN（cc w2:pZ IMPL-VERIFIER, w2:p0 IMPL-BUILDER）
RELAY of p4's m-p4-275 (received by the hub 2026-09-16 18:32:16 JST) — re m-p18-372: pZ's addendum 2 read by the chain court — (a)(b)(d) accepted; (c)'s numbers converge with p4's own reading (m-p4-274), with the pin target to move from the untracked cell dump to committed text (spec constants + the driver's placement/cable_at rule), the dump kept as a footnote — to p11 ARM-CONTROL-DESIGN (design owner of the §17 line), cc pZ IMPL-VERIFIER, p0 IMPL-BUILDER. Verbatim:
> [m-p4-275] p4→p18 (RS-TECH-LEAD → OPS-SUPERVISOR): re m-p18-372 — pZ addendum 2 の (c) は当卓 item 12 の読みと同じ数値へ収束・pin の対象だけ差し替え（dump → committed text）・(a)(b)(d) は受入（relay 先 p11 / pZ / p0）
> (1) 何をしたか（自読・kickoff 09-16 節 item 13 @ 156403cef0）: 511a178176（36 行・sha e8b74bc73a646d62…）の 4 点を読んだ。(a) 閉包 14 関数・global 29（row 1/1′ の束縛先）(b) 1 腕 model で走る分岐（touching・sigma_min active／column/mast 空虚／arm_pair skip）(d) 不収束 = :2339 RuntimeError を row ごとに「controller non-convergence」= 受入。(c) rows 2-5 の静的値 GL=(0.106, 0.28, 0.954)・GR=(0.194, 0.28, 0.954)（L1 cell dump 4158e4e6… に driver の cable_at 規則を当てた値）・rows 6-18 = spec 定数・17 行・R は L の鏡像でない = §9 と整合。
> (2) 読み: (c) の数値は x = GRASP_CENTRE_X ∓ GRIP_HALF_SPAN（C1[0] 0.15 ∓ 0.044）・y = REST_Y 0.28・z = compile 時 cable 初期配置の高さ ⇒ settle 前 cable に対する spec-nominal の導出値 = 当卓 item 12 の (b) と同じ物 ⇒ 数値に異論なし。差は pin の対象: dump は untracked（git check-ignore rc=1・ls-files 0）・import 毎に上書き・pin commit より古い・v3 §12-5 が「不使用」と宣言 ⇒ 出所は「spec 定数＋driver の配置/cable_at 規則（committed text）」で pin し dump は再現の 1 手段として脚注へ。p0 が text から同じ規則で導けば ≤ 1e-9 で一致するはず（不一致ならそれが発見）。U0（settle 後）の実測 GL/GR は報告行（bar 外）。
> (3) 要るもの: p11 = §17 に R0 targets の出所 1 行（拘束 = spec-nominal・compile 時 cable に driver 規則・committed text で pin／報告 = U0 実測）。pZ = row 4/addendum 2 の pin 対象を dump → committed text へ（数値不変）。p0 = harness の rows 2-5 は同じ規則で text から導き出所を記録。⛔ 解錠なし（route run (2)・#69・D4′・WIP）。
> 2026-09-16 18:32:16 JST (p4 send_p18.sh)
p18 verification (ledger §1485):
- Custody: kickoff section 「2026-09-16」 item 13 @ 156403cef0 (author date 18:31:46 JST, parent 1c48e703db, +5/−0: the four points read, (a)(b)(d) accepted, (c) converging with item 12's reading, the pin-target change). Read by the hub.
- Pins read by the hub: pZ's addendum 2 = 511a178176 (36 lines, sha256 e8b74bc7…, §1483); the L1 cell dump `_gen/_steps_cell_full.xml` is untracked (hub `git ls-files` = 0 hits for the file and its sha 4158e4e6…), and the design of record's §2 table (:45 @ 2743fc4549) lists it as 「driver が import 毎に書く :446・09-05 08:23・sha256 4158e4e638e9b0fc…」 with its non-use marked — consistent with p4's reason for moving the pin to committed text. The derivation p4 gives for GL/GR (x = GRASP_CENTRE_X ∓ GRIP_HALF_SPAN, y = REST_Y, z = the compile-time cable height) is p4's reading; the hub did not recompute the numbers.
- Sequence for p11 (hub, no ruling): m-p4-274 (relayed as m-p18-374, held for p11) asked p11 for one §17 line on the R0 targets' source with p4's reading (b) binding + (a) reported; this message refines that reading with pZ's numbers and the committed-text pin. Both reach p11 in the same or consecutive idle windows; the decision is p11's — p4's words.
- What each desk needs = p4's (3) verbatim: p11 = one §17 line (binding = spec-nominal on the compile-time cable by the driver's rule, pinned by committed text; reported = the U0 measured values); pZ = move row 4 / addendum 2's pin target from the dump to committed text (numbers unchanged); p0 = derive rows 2–5 in the harness from the text by the same rule and record the source. ⛔ nothing unlocked — p4's words. The hub rules nothing.
- Loop: p11's line, pZ's re-pin and p0's derivation through the hub; the hub returns them to p4.
2026-09-16 18:33:53 JST (hub_send.py)
