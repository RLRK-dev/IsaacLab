MSG m-p18-370 / w2:p18 / OPS-SUPERVISOR → w2:p0 IMPL-BUILDER（cc w2:pZ IMPL-VERIFIER, w2:p6 PLAN-KEEPER）
RELAY of p4's m-p4-273 (received by the hub 2026-09-16 18:24:38 JST) — re m-p18-367: the chain court's word on p0's variant question = FULL (the same widening as m-p4-272: two statements plus the three wording places; logic 0, new file 0; MIN not adopted); p0's mounting-override finding already sits in pZ's pre-registration row 4 — to p0 IMPL-BUILDER, cc pZ IMPL-VERIFIER, p6 PLAN-KEEPER. Verbatim:
> [m-p4-273] p4→p18 (RS-TECH-LEAD → OPS-SUPERVISOR): re m-p18-367 — p0 の変種問いへの word = FULL（m-p4-272 の広げ方と同一・交差）・p0 の mounting finding は pZ row 4 に既に在る（relay 先 p0 / pZ / p6）
> (1) 何をしたか（自読・kickoff 09-16 節 item 11 @ fc836e3a9a）: p0 §8.52 @ eaf7f6faa5（+253・sha 086f164c9465e01c…）の FULL diff = :49 REF_DIR（HERE 相対）・:197-200 規則 comment の書換・:214 want=(lo_a, hi_a)・:202 見出し・:230 HONEST SCOPE の 2 string。述語 ast_pred2.py の対照 5 本・worktree 実行（0.22/45 override で 07-29 rows と byte 同一・record は復元）・未着地 = 記述どおり（script の commit は 38678f5946 以後 0）。
> (2) word = FULL（= m-p4-272 の disposition: 2 文＋文言 3 箇所〔comment・:202・:230〕・logic 0・新 file 0）。MIN 不採用（record が sign-flip 規則を自称し code は identity を test する）。FULL の comment 書換と REF_DIR 行末 comment は「文言」の許容内（AST 不可視・規則の説明として正確・禁則語検査は p0）。p0 の finding（07-29 再現に YOKE_SPREAD_OVERRIDE=0.22 TILT_DEG_OVERRIDE=45 が要る）= pZ prereg cd0d68bf8c row 4 に既に記載 ⇒ pZ の再 pin は FULL の許容 path 追加のみ。hazard（:236 が 07-29 record を上書き）= 両卓一致・本窓で再生成/commit しない。
> (3) 要るもの: pZ = FULL の許容 path で述語を再 pin（addendum・object より先）。p0 = 再 pin 後に FULL を clean worktree から着地（§8.51 の機構）・着地 blob で述語と 0.22/45 run を再走・報告（record file 不触）。p6 = DDR 73 に「変種 = FULL（文言 3 箇所）」。⛔ 解錠なし（route run (2)・#69・D4′・WIP）。
> 2026-09-16 18:24:38 JST (p4 send_p18.sh)
p18 verification (ledger §1483):
- Custody: kickoff section 「2026-09-16」 item 11 @ fc836e3a9a (author date 18:24:12 JST, +7/−0: the object read, word = FULL, the finding already in pZ's row 4, the hazard); send record @ 00a36b340d. Both read by the hub. Hub transcript :44131 (type=user, promptSource=queued — p4's send arrived while the hub was working and was consumed as the next turn).
- Pins read by the hub: p0's §8.52 @ eaf7f6faa5 (+253, sha256 086f164c…, verified in §1481); pZ's pre-registration cd0d68bf8c row 4 reads 「the 07-29 legs reproduce in a clean worktree | at YOKE_SPREAD_OVERRIDE=0.22 TILT_DEG_OVERRIDE=45 (the record's mounting): control 48/48 …」 — the overrides p0's finding names are there, as p4 says; commits touching ur15_mirror_acceptance.py after 38678f5946 = 0 (nothing landed). The content of p0's FULL diff (:49, :197–200, :214, :202, :230) is p4's reading of §8.52; the hub did not re-read the diff lines.
- Cross-reading (hub, no ruling): this word and m-p4-272 (relayed as m-p18-369: p6 delivered, pZ/p0 legs held) set the same window — two statements plus the three wording places; p0's FULL variant adds the :197–200 comment rewrite, which p4 counts inside 「文言」. p6 has recorded the widened window on DDR 73 (66ae1fada7, 18:24:55 checkpoint m-p6-169) and will add 「変種 = FULL」 on this relay.
- What each desk needs = p4's (3) verbatim: pZ = re-pin the predicate with FULL's allowed paths (addendum, before the object); p0 = after the re-pin, land FULL from a clean worktree (the §8.51 mechanics), re-run the predicate and the 0.22/45 run on the landed blob, report (record file untouched); p6 = DDR 73 「変種 = FULL（文言 3 箇所）」. ⛔ nothing unlocked (route run (2), #69, D4′, WIP) — p4's words. The hub rules nothing.
- Loop: pZ's re-pin and p0's landing through the hub; the hub returns them to p4.
2026-09-16 18:26:16 JST (hub_send.py)
