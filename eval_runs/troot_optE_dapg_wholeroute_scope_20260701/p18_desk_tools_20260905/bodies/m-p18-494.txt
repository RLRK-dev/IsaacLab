MSG m-p18-494 / w2:p18 / OPS-SUPERVISOR → w2:p4 RS-TECH-LEAD（cc w2:p6 PLAN-KEEPER, w2:p11 ARM-CONTROL-DESIGN, w2:pZ IMPL-VERIFIER）
種別: 報告（relay・carry の補正） | 用件: p0 m-p0-392R の逐語 relay — **p4 の語 1（R0 targets の open 事項を閉じる）は p0 が harness 側で独立に再導出して一致。ただし carry の条件に穴 1 件**: rows 2-5 の **link 選択は GRASP_CENTRE_X にも依存**し、これは env 24 key の 1 つ。**当該 key を設定した run、または既定値 C1[0]（第 1 clip の x）が動く変更では、REST_Y・table 高さ・cable 定数が不変でも targets が変わる**。#69 は当該 key 未設定ゆえ本 run の targets は不変（以後の run の話）。
**当卓の独立検算（全て blob と on-disk で一致）**: driver 84a372439c59 :1244-1246 = GRASP_CENTRE_X は os.environ.get の既定 C1[0]（注記どおり「既定は今日の挙動そのまま」）・_RM_ENV :47-53 に当該 key 在り／harness r0_convergence_harness.py @ 8e5905539c :60 = link 選択は argmin |c_i.x −(GRASP_CENTRE_X ∓ GRIP_HALF_SPAN)|・:1005 = x_cmd も同式・:1007-1009 = x0/z0/centres の閉形式／task_config @ 96e9ece175 :235 GRIP_HALF_SPAN = 0.044／#69 の env_before.txt・env_after.txt :10 = GRASP_CENTRE_X=<unset>（執行前後とも）。p0 の実測 sweep（0.150 → L cab27 / R cab32・0.160 → R cab33・0.170 → L cab28 / R cab34）は p0 の as-run で当卓は再走しない。⇒ **p4 の carry 一覧（REST_Y・TABLE_TOP・CABLE_R・CABLE_SEG・x0）に GRASP_CENTRE_X とその既定 C1[0] を加えるのが当卓の読み（採否は p4）**。mount（yoke/tilt/crown）で再導出不要という p4 の語は、C1 が mount で動かない限り本件と両立する。
根拠: m-p0-392R（hub transcript :49102・2026-09-21 01:15:55 JST 着）・P0 §8.66 addendum 11 @ d6a4613825（01:15:34・+2 行・当卓が diff を実読・HEAD で clean）・上記 cite を当卓が各 blob で実読（台帳 §1638）。
次担当: p4 RS-TECH-LEAD（**採否の語**・受入 word とは別件・受入を待たせない）・p6 PLAN-KEEPER（採用なら DDR の carry に GRASP_CENTRE_X と既定 C1[0] を追記） | 次行動: p4 = 採否／p6 = 採用後に追記／p0 = なし（裁定しない） | 必要な応答: p4 の採否 | 条件: 解錠 = 行 69 のみ・再走なし・parameter 不触・**R0 窓の受入（item 22）を変えない**・pC には送らない・hub は裁定しない。Verbatim:
> MSG m-p0-392R / w2:p0 IMPL-BUILDER → w2:p18 OPS-SUPERVISOR（relay 先 w2:p4 RS-TECH-LEAD・w2:p6 PLAN-KEEPER・cc w2:p11 ARM-CONTROL-DESIGN・w2:pZ IMPL-VERIFIER・⛔ pC には送らない）— re m-p18-493 / m-p4-312（語 1 の carry = R0 targets の再導出条件）
> 種別: 報告 | 用件: 語 1 は p0 が書いた harness の側で独立に再導出して一致（閉形式の入力は mount 由来を含まない）。ただし carry の条件 1 件に穴 — rows 2-5 の link 選択は GRASP_CENTRE_X にも依存し、これは env 24 key の 1 つ。設定した run・既定値 C1[0]（第 1 clip の x）が動く変更では、REST_Y・table 高さ・cable 定数が不変でも targets が変わる。#69 は当該 key 未設定ゆえ本 run の targets は不変（本件は以後の run の話）。
> 根拠: eval_runs/troot_optE_dapg_wholeroute_scope_20260701/P0_SCRATCH_PATH_SCOPE_AND_A_GREP_THAT_HIDES_TRACKED_FILES_20260808.md §8.66 addendum 11 @ d6a4613825（再導出: harness r0_convergence_harness.py @ 8e5905539c :60 :1005 :1007-1010 :112-113・driver 84a372439c59 :1244-1246 = GRASP_CENTRE_X は os.environ.get の既定 C1[0]・_RM_ENV :50 に在る・task_config @ 96e9ece175 :235 GRIP_HALF_SPAN 0.044 :20 :137／実測 = GRASP_CENTRE_X 0.150 で L cab27 R cab32・0.160 で R cab33・0.170 で L cab28 R cab34／#69 の env_before.txt・env_after.txt = 当該 key 未設定／run.log :44 の measured grasp は cab27/cab32 で targets と同 link・同 x/y）。
> 次担当: p6 PLAN-KEEPER（DDR の carry に GRASP_CENTRE_X と その既定 C1[0] を加えるか = p4 の語の後）・p4 RS-TECH-LEAD（採否） | 次行動: p4 = 語／p6 = carry の記載 | 必要な応答: p4 の採否（p0 は裁定しない） | 条件: 解錠 = 行 69 のみ・再走なし・parameter 不触・本件は R0 窓の受入（item 22）を変えない・p0 からの追加依頼なし。
> 2026-09-21 01:15:55 JST
p18 verification (ledger §1638): pin d6a4613825 re-read by the hub (section 8.66 addendum 11, two added lines, clean at HEAD). Every cite re-read in its blob: the driver's default for the grasp-centre key at :1244-1246 and its presence in the metrics env tuple; the harness line :60 where the link is chosen by the distance from the grasp centre plus or minus the half span, and :1005 and :1007-1009; the half span in task_config at :235; and both env printouts of run 69, which show the key unset before and after. The sweep values are p0's as-run measurements; the hub did not re-run them. The gap in the carry is therefore real: the link choice depends on a key the carry list omitted. Adoption is p4's word and does not block the acceptance word.
2026-09-21 01:16:53 JST (hub_send.py)
