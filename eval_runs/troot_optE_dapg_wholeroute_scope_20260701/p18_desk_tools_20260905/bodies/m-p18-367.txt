MSG m-p18-367 / w2:p18 / OPS-SUPERVISOR → w2:p4 RS-TECH-LEAD（cc w2:pZ IMPL-VERIFIER, w2:p6 PLAN-KEEPER）
RELAY of p0's m-p0-362R (received by the hub 2026-09-16 18:18:07 JST) — ANNOUNCE-FIRST for the acceptance-instrument window (Rs1 Q9 / m-p4-269): two candidates MIN and FULL built and run statically in a clean worktree, NOT landed; one scope question for p4 (MIN = the two statements, or FULL = also the two strings that print the rule); one finding for pZ's pre-registration (the 07-29 reproduction needs the 0.22/45 mounting overrides) — to p4 RS-TECH-LEAD (chain court), cc pZ IMPL-VERIFIER, p6 PLAN-KEEPER. Verbatim:
> p0 -> p18 (route: p4 chain court / cc pZ p6). m-p0-362R. ANNOUNCE-FIRST for the acceptance-instrument window (Rs1 Q9 / m-p4-269): two candidates built and run statically, NOT landed; one scope question for p4 before pZ's prereg binds; one finding pZ's prereg needs.
> (1) What I did: on the HEAD blob 0803ea391298 of ur15_mirror_acceptance.py (shared copy = 09-07 WIP, not read) in a clean worktree: (a) :49 REF_DIR = HERE / "reference" / "ur15-dual-arm-cell" (HERE-relative; JSON there = the 08-10 pin 20ac0935c707757c); (b) :214 want = (lo_a, hi_a). Two variants: MIN = exactly those two statements (+ the rule comment :197-200 corrected, AST-invisible); FULL = the same + the two strings that PRINT the rule (:202 leg heading "mirrored [lo,hi] must equal the stock [-hi,-lo]" and the :230 HONEST SCOPE line) -- under MIN the record would announce the sign-flip rule while the code tests identity. Recursive DoD predicate ast_pred2.py (allowed set = statement paths): MIN/min-allowed PASS; FULL/full-allowed PASS; FULL/min-allowed FAIL (labels seen); PASS_MM 1.0->1.5 FAIL; stray statement in the limit loop FAIL. Instrument run in the worktree (mj_kinematics only, mj_step 0, driver not imported; the run rewrites the worktree copy of the 07-29 record, restored, sha c5229911315f57b5 before/after): at the cell_spec default (C-2 0.28/20) control 0/48 (worst 241.08 mm), test/formula 0/48, negative 0/48 -- with YOKE_SPREAD_OVERRIDE=0.22 TILT_DEG_OVERRIDE=45 control/test/formula 48/48 (worst 0.0076 mm at main_route_high), negative 0/48 (1357.5480 at connector_insert), limit leg all 6 consistent = byte-equal to the banked 07-29 rows :37/:65/:93/:121; the record prints its mounting (:4). FINDING for pZ's row "07-29 legs reproduce in a clean worktree": the reference publishes positions for 0.22/45 only (DDR 68 / pZ F4) and cell_spec has defaulted to C-2 since 0f6b4a733e, so the reproduction needs the two overrides. HAZARD: :236 writes the banked 07-29 record file in place -- do not run this script in the shared tree; I will not regenerate/commit that record in this window. Not claimed: the asymmetric-range mock negative control for the new rule (pZ's row); any UR15-B grade.
> (2) Where: section 8.52 of P0_SCRATCH_PATH_SCOPE_AND_A_GREP_THAT_HIDES_TRACKED_FILES_20260808.md @ eaf7f6faa5 (sha256 086f164c9465e01c4608c1078b75500a8c46c80d600011b5d2929ab054a4e25c): both diffs (13 / 24 lines), ast_pred2.py, the predicate legs, the two-mounting table, the allowed-set paths verbatim.
> (3) Needed: p4 = the variant -- MIN (the window's letter, 2 statements) or FULL (4 statements; my recommendation: the leg's printed rule must match the rule it tests). pZ = prereg carrying the mounting overrides and the chosen variant's allowed paths. Then I land from the clean worktree (as section 8.51: worktree commit, CAS ref move, pathspec index reset), re-run the predicate and the 0.22/45 run on the landed blob, and report. Nothing landed; nothing unlocked (route run (2), #69, D4', WIP). gate 不変・self-start しません。
> 2026-09-16 18:18:07 JST
p18 verification (ledger §1481):
- Pins read by the hub: eaf7f6faa5 (author date 18:17:39 JST, parent 1d42a6f198, +253/−0 to the P0 scratch-path file; §8.52 heading at :2977; file sha256 086f164c9465e01c4608c1078b75500a8c46c80d600011b5d2929ab054a4e25c — equal to p0's figure; file clean at HEAD); commits touching ur15_mirror_acceptance.py after 38678f5946 = 0 (nothing landed, as p0 states); 0f6b4a733e = 「Make C-2 the mounting defaults: spread 0.280, tilt 20.0」 (2026-08-10), the commit p0 names for the cell_spec default; the HEAD blob of the script is 0803ea391298 with :202 and :230 carrying the old-rule strings (§1481, pZ's observation). The MIN/FULL diffs, the predicate legs, the instrument runs at the two mountings and the record sha are p0's as-run statements from a clean worktree; the hub did not re-run them. p0 states the run was mj_kinematics only (mj_step 0, driver not imported) and the shared tree untouched; the hub records the statement and rules nothing on it.
- Cross-reading (hub, no ruling): p0's scope question (MIN vs FULL) and pZ's PZ-226 observation (the record's wording still states the old rule; changing it is a third statement) are the same question from two desks; pZ's pre-registration cd0d68bf8c binds the two-statement window as written, and p0's finding (the 07-29 reproduction needs YOKE_SPREAD_OVERRIDE=0.22 TILT_DEG_OVERRIDE=45 since the C-2 default) bears on pZ's row (iv). Both are for p4's disposition and pZ's pre-registration; the hub decides neither.
- Hazard restated by both desks in their own words: the script's :236 rewrites the tracked 07-29 record file when run — not to be run in the shared tree.
- What each desk needs = p0's (3) verbatim: p4 = the variant (MIN or FULL); pZ = the pre-registration carrying the mounting overrides and the chosen variant's allowed paths; then p0 lands from the clean worktree and reports. ⛔ nothing landed, nothing unlocked — p0's words.
- Loop: p4's word and pZ's pre-registration update through the hub; the hub returns them to p0.
2026-09-16 18:19:41 JST (hub_send.py)
