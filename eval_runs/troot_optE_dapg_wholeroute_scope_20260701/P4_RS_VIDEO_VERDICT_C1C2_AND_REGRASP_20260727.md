# Rs の動画判定 (2026-07-27 21:3x) — C1・C2 貫通 / 「左ハンド」の再把持失敗

記録 2026-07-27 21:39:16 JST (w2:p4 RS-TECH-LEAD)
判定者 = **Rs (human ground truth・本レグは Rs 専権)**
対象動画 = `~/Downloads/ur15_wide14.mp4` sha256 `8b499aa4f38a3956938f8a715905b9ce9cab9b66ceb35fa4f2ae6608bac77046`
(Rs はスマホで視聴 ⇒ 私が圧縮した同内容の複製 sha256 `13f160b6c4f78c8f1ba818e8e33426d3bd63f9d41cbb78922d9fed35d0017f54` を送付。960px・無音・38.6 秒・編集なし)

---

## 1. Rs 逐語

> C1、C2ケーブル貫通、左ハンドの再把持失敗

---

## 2. ログ側の照合 (私・数値レグ) — ⭐ **2 点とも一致します**

⛔ 以下は Rs の判定を検証し直すものではありません (映像の物理妥当性は Rs 専権)。**同じ事象がログにも出ているか**を確認したものです。

### 2-1 C1・C2 とも貫通

ログ行の意味 = `ur15_steps_reaim.py:935` — `c1[y… z…]` は **座リンクの位置、z は台面からの高さ**。

| STEP | 行 | c1 の z | c2 の z |
| --- | --- | --- | --- |
| 9 「C1がcable固定」 | `ur15_wide14.log:56` | **+0.004** | +0.013 |
| 15 「C2へ押し込み」 | `:68` | +0.004 | **+0.006** |
| 16 「C2固定」 | `:70` | +0.004 | **+0.004** |
| 17 「解放」 | `:72` | +0.004 | +0.004 |

⇒ ⭐ **どちらのクリップでも 座リンクは台面 +4.0 mm** = **半径 4 mm のケーブルが台に接している**高さです。
⇒ ⛔ 当時のクリップの溝は **+70 mm** ⇒ ケーブルは **66 mm 下**、つまり**クリップの実体の中を通って台まで落ちています。**
⇒ ⭐⭐ **Rs の「C1、C2」は両方ともログに在ります。**

⛔⛔ **私の報告漏れ**: 私が本日出した貫通の報告 (`P4_CLIP_PENETRATION_DETECTED_20260727.md`) は **C1 だけ**でした。⭐ **同じログの同じ行に C2 も出ています。** ⇒ 私は片方を測って片方を報告し、**もう片方を見ていませんでした。**

### 2-2 「左ハンド」の再把持失敗

- ⭐ **この工程で再把持するのは 1 本だけ**です — `:64` STEP13「右がcable再把持へ」。
- 把持状態 `grip=` は `ur15_steps_reaim.py:937` で `L`/`R`/`-`。⇒ `:62` STEP12 = `grip=L-` ・`:64` STEP13 = `L-` ・`:66` **STEP14「両手クランプ」= `L-`**。
⇒ ⭐⭐ **「両手クランプ」の時点でも 2 本目は掴んでいません** = **その再把持は成立していません。**

**⚠ 画面の左右と、私の呼び名の対応** (⭐ 私の計算・pC の視覚レグではありません):
本動画の 2 面はどちらも `ur15_steps_reaim.py:763-767` のカメラで、方位角 108° と 250°。画像の「右」方向へ **arm L → arm R** の向きを投影すると **両面とも負** ⇒ ⭐ **arm R が画面の左に写ります (2 面とも)。**
⇒ ⭐ **Rs の「左ハンド」= 私の呼ぶ arm R** ⇒ **再把持するただ 1 本と一致** ⇒ **食い違いはありません。**
⚠ ⛔ 本項は幾何計算です。**視覚レグとしては pC の判定が要ります。**

### 2-3 ⛔ 計器の穴 — **再把持のところだけ何も測っていません**

GRASP 診断ブロックは `ur15_wide14.log:31-44` の **14 行のみ**で、すべて **STEP 4 (最初の把持) の前**です (`:45` が STEP 4)。
⇒ ⛔ **STEP13-14 の再把持には診断が 1 行も在りません** ⇒ 失敗しても `grip=L-` としか出ません。
⇒ ⭐ **Rs は動画で見えるのに、私の計器はそこで黙っています。** ⇒ 次の走行前に塞ぐべき穴です (最初の把持と同じブロックを再把持でも出す)。

⚠ 参考 = 最初の把持の診断には既に兆候が在りました: `:38` 「seat vs NEAREST cable link … |20.2| mm (containment wants ±1.0 mm)」・`:39` 「cable vs where it was aimed |21.5| mm」。⛔ ただしこれは **STEP 4 の数値で、再把持の数値ではありません** — 流用しません。

---

## 3. 射程 — ⭐ **本判定は「作り直す前」の cell についてです**

| Rs の指摘 | 当時の候補原因 | 現行 cell で在るか |
| --- | --- | --- |
| C1・C2 貫通 | ①クリップが自作で **64 mm の中実**だった ②積分刻みが producer の **9.6 倍粗い** | ⭐ ①は権威実装のクリップへ差し替え済 ・②は producer 値へ変更済 ⇒ ⛔ **どちらも「直したが未検証」** (走行認可待ちで 1 度も走っていません) |
| 再把持の失敗 | ⛔ **上の 2 つでは説明されません** | ⛔ **そのまま残ります** ⇒ 現行 cell でも起きる想定 |

⇒ ⭐⭐ **走らせれば ①②の当否は分かります。再把持は別に手当てが要ります。**
⇒ ⛔ 走行認可は Rs のものです。私は走らせていません。
