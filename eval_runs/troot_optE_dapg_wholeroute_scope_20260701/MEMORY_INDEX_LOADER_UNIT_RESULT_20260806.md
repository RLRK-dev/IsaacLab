# MEMORY.md の単位問題 — 決着 + 項目 2 の三つの根拠を実測

desk: p4 RS-TECH-LEAD (w2:p4) / 測定 2026-08-06 16:15:44–16:16 JST
subject file: `/home/rlrk/.claude/projects/-home-rlrk-IsaacLab/memory/MEMORY.md`
版: mtime `2026-08-05 22:53:51`（測定中 不変・handoff.md は 22:53:51 / MEMORY.md は 22:42:32）
規則の物: `CLAUDE.md` §運用31 @ commit `9213a675b0` + `f98aa82425` + `49717bddc6`（全て `658d511e5f` の祖先）

---

## 1. 実測（同一ターン・同一 file）

```
wc -c (BYTES)      : 29207
wc -m (CHARACTERS) : 21160
wc -l (lines)      : 104
```

banked 閾値（§運用31）= hard **24,986 chars** / 90% トリガ **22,487 chars**

| 単位で読むと | 値 | hard に対して | 90% トリガに対して |
|---|---|---|---|
| chars（規則が言う単位） | 21,160 | **84.7%** — 下回る | **下回る**（起票 不要） |
| bytes | 29,207 | **116.9%** — 4,221 超過 | 超過 |

⇒ 同じ file・同じ瞬間で、**単位を変えるだけで「余裕あり」と「17% 超過」が入れ替わる。**

## 2. ⭐ 決着させた検査（positive control・この会話自身が計器）

⚠ 先行して 2 卓が「loader がどちらの単位を使うかは **未検証**」と明記して止めていた
（私の dispatch + p18 台帳 §942 の単位訂正）。以下でその未検証を閉じた。

**設計**: 24,986 **バイト**で切る loader なら、byte offset 24,986 より後ろは届かない。
届いたかどうかは、**この会話に注入された MEMORY.md 本文**を見れば分かる。

**byte offset（実測）**:

| offset (bytes) | 見出し |
|---|---|
| 22,739 | `## Output Preferences` |
| 23,476 | `## Standing Operational Directives` |
| **28,727** | `## Feedback (6 categories, consolidated)` |
| **29,038** | `## Background pointers` |

**cut は 24,986** ⇒ 下 2 節は**丸ごと** cut より後ろ（かつ 24,986 は行の途中に落ちる）。

**結果**: 下 2 節は**どちらも本セッションの注入本文に在る**（末尾行まで disk と逐語一致）:

```
## Background pointers
- ⭐[pC 動画 leg 手法](reference-pC-video-analyst-route-video-method-2026-07-11.md): 7-cam montage / zoom+brighten / 世界系 R-L 帰属。
```

⇒ ⭐ **loader は 24,986 バイトで切っていない。** 29,207 バイトの file が丸ごと届いた。

### honest scope（この検査が言っていないこと）
- 照合したのは **末尾 2 節と最終行の在否**。注入本文と disk の**全文 byte 比較はしていない** ⇒
  「24,986 バイトでの末尾切断が無い」ことは言えるが、あらゆる truncation を排除してはいない。
- ⛔ **loader の実際の上限は不明のまま。**「24,986 バイトではない」≠「24,986 chars である」。
  規則の数（chars）が loader の数だと確かめたのではなく、**「17% 超過」に見せていた byte 読みが偽**だと確かめた。
- 版限定: mtime `2026-08-05 22:53:51` の内容について。

### ⭐ なぜ 2 日見落とされたか（型）
この検査は**最初から誰の手元にもあった** — 注入本文は loader の出力そのもの。
2 卓は backup file の履歴と hook の label から論じ、**loader の納品物を読みながら、それを納品物として見なかった**。
⇒ ⭐ **「切れたか」を答える artifact は、loader 自身の配達物であり、私はそれを最初から読んでいた。**
（型 = 計器が結論の側でなく、context の側に置かれていた。近縁 = 述語が識別しない検査／measured-but-out-of-scope）

## 3. 項目 2（私の未解除書き込み 8 件）の三つの根拠を実測

§運用31 が凍結を置き換えたあと、**選択肢 A（復元）を支える根拠が残っているか**を面ごとに測った。

| 面 | §運用31 の規定 | 実測 | A の根拠は残るか |
|---|---|---|---|
| topic file ×2 | **解放**（通常どおり書いてよい） | — | ⛔ **残らない**（現行規則が許す内容） |
| `MEMORY.md` | 成長条件・90% 超で coordinated 圧縮 | **84.7%（21,160 chars）** = トリガ未満 | ⛔ **残らない**（大きさの根拠なし） |
| `handoff.md` | 自分の節のみ Edit・⛔全書き換え禁止 | 印を付けた 6 箇所は**全て p4 節**（`:11`/`:20`/`:56`/`:98`/`:118`/`:191`/`:197` の見出しは全て `(p4 …)`）＋共有 frontmatter `:3`。全書き換えなし | ⛔ **残らない**（制約は充足） |

⇒ **A（復元）を支える根拠は三面とも残らない。**

⚠ **ただしこれは「Rs が B を決めた」ではない。** 規則の解除は**以後**について書かれており、
**解除の 1 時間前に書かれた分を遡って免じる**とは書いていない（境界の問いと同一性の問いは別）。
私が動かしたのは**根拠の測定**であって処分ではない。⇒ 残るのは Rs の一言のみ。

## 4. 規則そのものへの含意（⛔ 私は CLAUDE.md を編集しない）

§運用31 は **chars** で書かれており、本測定は**その読みを支持する**（矛盾しない）。
⇒ ⭐ **規則の訂正は要らない。** 閉じたのは規則の外に在った「単位が未検証」という穴のみ。
`CLAUDE.md` は L3・Rs 専権ゆえ、注記を足すかは Rs 判断。私は報告のみ。

---

---

# 追記 2026-08-06 16:22:30 JST — 残った未知（loader の実上限）を history から測ろうとして、循環中の主張が偽と判明

⭐ **supersede でなく追記**（§2 の結論は不変・以下は「未測」の側を掘った結果）。

## 5. 索引の全 backup を chars で実測（20 版）

| chars | bytes | file |
|---|---|---|
| **455,561** | **483,341** | **`MEMORY.md.bak_20260625_220401`** |
| 24,020 | 32,088 | `…bak_20260715_0010_p5_precompact` |
| 24,008 | 32,100 | `…bak_20260715_p6_compact_pass7` |
| 23,638 | 30,679 | `…bak_20260715_2146_p5_reward_gate2` |
| 23,390 | 30,488 | `…bak_20260715_0842_p6_pass8_precompact` |
| 22,703 | 28,416 | `…bak_20260714_0742_prepass7` |
| （以下 14 版はすべて 22,554 chars 以下） | | |
| 21,160 | 29,207 | `MEMORY.md`（現行） |

**06-25 版は本物の索引**（実測: 625 行・先頭 `# Memory Index`・`## PANE ROLES` / `## Feedback / lessons` の同一構造・最長行 11,358 chars・2,000 chars 超の行が 43 本）。
⇒ **hard limit 24,986 chars の 18.2 倍。**

## 6. ⛔ これが偽にする、循環中の主張

**出所 = `w2:p12 RS-TECH-LEAD2` → p18**（`~/.claude/paste-cache/80bba259bb057416.txt` 冒頭の宛名行で実測。⚠ **私の主張ではない**）。逐語:

> 「chars で見るかぎり、私が引いた backup 履歴の**全期間**で一度も 24985 を超えていません**（最大でも 24008）**」

⛔ **偽。** 06-25 版が 455,561 chars。p12 が引いたのは **3 版**（07-14 / 07-15 / 07-20）= 6 日窓であって、backup 群の期間（06-25 → 08-06）ではない。

### ⭐ 形（今日 3 度目の同族だが、これは新しい変種）
**射程を示す語は文中に在った** — 「私が引いた」。⛔ **ところがそれが係るのは「どの file を」であって、「どの期間を」ではない。**
同じ文の「全期間」が期間を主張し、**限定語は標本に、結論は期間に**掛かった。
⇒ ⭐ **限定語を書いたことは、結論が主張する側の量を限定したことにならない。**
（近縁 = [但し書きを 2 つ付けても束縛している 1 つを見つけたことにならない]。今回は但し書きが **1 つ在って、掛かる先が隣の名詞だった**）
⚠ そして除外された 1 版こそ、答えを **18 倍** 変える版だった。

## 7. p12 の結論はどうなるか（⚠ 反証ではない）

p12 の帰結は「⇒ **圧縮を load 完全性で正当化できない**」。
- 支えていた前提（「一度も超えたことがない」）は **偽**
- ⛔ **しかし結論は反証されていない** — 455K 版が丸ごと配達されていたなら load 完全性はいっそう正当化にならず、**逆に**切られていたなら切断が誰にも記録されずに起きていたことになる
⇒ ⭐ **崩れたのは結論でなく、結論を支えていた根拠。** 帰結は **未決** であって偽ではない。

## 8. ⛔ これでも loader の実上限は測れない（そして、なぜ測れないか）

**私は 06-25 の *file* を持っているが、06-25 の *配達物* を持っていない。**
§2 で決着したのは「配達物を見れば切断が分かる」であって、過去の配達物は残っていない。
⇒ 二分岐は**どちらも on-disk からは選べない**:
- (A) 455K 版が丸ごと配達されていた ⇒ loader の上限は **24,986 の 18 倍以上**、§運用31 の帰結節「これを越えると索引が読めない」は大幅に外れる
- (B) 切られていた ⇒ 索引は当時**黙って切られていた**（どの記録にも無い）

⚠ **観測された欠落**: 06-25（455,561）と次の backup 07-14（22,703）の間は **20 日・18 倍の落差**で、その間の backup が本 dir に無い。⇒ 大規模圧縮が backup を残さず行われた。

⛔ **測る唯一の方法は共有 file を意図的に育てること** ⇒ **やらない**（他卓が live で書く file・かつ項目 2 が未決）。
⇒ ⭐ **「未測」を「測れない」に格上げして返す** — 手元の物では閉じない、という**境界つきの事実**として。

## 9. §運用31 への含意（⛔ 私は L3 を編集しない）

- 数値（24,986 **chars**）= §2 で支持され、**訂正不要**
- 帰結節（「これを越えると索引が読めない」）= ⚠ **両方向とも未検証**。24,986 は hook の表示由来であって、loader の実測ではない
⇒ **Rs 判断事項**。私は報告のみ。

---

# 追記 2026-08-06 16:29:27 JST — ⛔ §5 の版数と §8 の「空白 20 日」を訂正（p18 が捕捉・私の実測で確定）

⭐ **削除でなく挿入**。⛔ **§8 の「大規模圧縮が backup を残さず行われた」は偽。** 以下が正。

## 10. ⛔ 訂正 1 — 「全 backup 20 版」は母集団の取り違え

| 数 | 母集団 | 実測 query |
|---|---|---|
| **18** | `MEMORY.md.bak_*`（**アンダースコア必須**） | p18 の query |
| **19** | **backup 全部**（無印の `MEMORY.md.bak` を含む） | `MEMORY.md.bak*` |
| **20** | 19 + **live `MEMORY.md`** | 私が §5 で表にした集合 |

⇒ **18 も 20 も、それぞれの母集団では正しい。⛔ どちらも「backup の数」ではない（＝19）。**
私は 20 行の表に「**全 backup**（20 版）」と見出しを付けた ⇒ **live file を backup に数えた。**
⚠ p18 の 18 が落とすのは **無印 `MEMORY.md.bak` の 1 本**（glob の `_` が要求される）。

⚠ **これは自分の memory に既に在る教訓の再発**（「私の 19 と相手の 29 はどちらも正しく、母集団が違っただけ」）— **今回また 19 だった。**

## 11. ⛔ 訂正 2 — 「20 日・その間 backup 無し」は偽（3 重に）

**実測（mtime 順・chars）**:

```
2026-06-25 22:04    455561  MEMORY.md.bak_20260625_220401
2026-06-30 10:01     20912  MEMORY.md.bak          <-- 直後の backup
2026-07-03 04:54     20840  MEMORY.md.bak_20260703_cpe
2026-07-06 02:06     20008  MEMORY.md.bak_20260706_p9merge
2026-07-12 11:30     20329  MEMORY.md.bak_20260712_claudemd_opt
2026-07-14 07:32     22703  MEMORY.md.bak_20260714_0742_prepass7
```

- ⛔ 「20 日」→ **落差の窓は 06-25 22:04 〜 06-30 10:01 = 約 4.5 日**
- ⛔ 「その間 backup 無し」→ **06-25 と 07-14 の間に 4 本**在る
- ⛔ 「backup を残さず」→ **残している**。正しい弱い形（p18 の言い方が正確）= **結果の backup は在り、中間段階の backup が無い**（06-30 の 20,912 は既に落差の後）

### ⭐ 私の誤りの機構（新しい変種・名指しできる）
§5 の表は **`sort -rn`＝大きさ順**。私はそれを**時間順として読んだ**。
06-25 の外れ値の次に大きいのが 07-14（22,703）なので、上から辿ると 07-14 が「次」に見える。
07-03 / 07-06 / 07-12 は**小さいので表の下方**に落ちていた — ⛔ **私が自分で印字した表の中に在った。**
⇒ ⭐ **並べ替えは、その順序で読まれる。大きさで並べた表から時間の主張を読むと、材料は目の前に在るまま見えない。**
（今日の系列: disk に在った → 配達物に在った → **自分の出力に在った**。距離はさらに縮み、点検はさらに後になった）

### ⚠ そして p18 の訂正も 1 本ぶん狭かった（同じ file が原因）
p18 は「間に 3 本・落差は 8 日以内」とした。**4 本・4.5 日**が正。
落とされた 1 本は **無印 `MEMORY.md.bak`（06-30）= 落差の直後で最も近い境界**であり、
⛔ **p18 の glob `MEMORY.md.bak_*` が `_` ゆえに除外する、まさにその file。**
⇒ ⭐ **二人の訂正は、同じ 1 本の除外によって、別々に狭められていた。**

## 12. ⚠ 行数の食い違い（未解決・私の実測を出す）

`MEMORY.md.bak_20260625_220401` の行数 = **625**（`wc -l` / `awk END{NR}` / `grep -c ''` の **3 手法一致**、末尾改行 **在り**を `od -c` で確認）。
p18 は **626**。⇒ **私の値は 625**。差の出所は p18 側の手法にあり、私からは同定できない。

## 13. 上記で **変わらないもの**（訂正の射程を明示）

- 06-25 版 = **455,561 chars / 483,341 bytes = hard limit の 18.2 倍** — 不変
- **p12 の前提は偽のまま**（「一度も超えていない」は 18 倍の反例で崩れる。母集団の数え違いはこれに影響しない）
- **p12 の結論は未決**（反証していない）— 不変
- **二分岐は on-disk から選べない・loader 実上限は手元の物では測れない** — 不変
- **§運用31: 数値は支持・帰結節は両方向とも未検証** — 不変

⇒ ⭐ **崩れたのは §5 の見出しの数と §8 の空白主張のみ。§2 の核心と §6-9 の帰結は立つ。**

---

## 再現コマンド

```bash
M=/home/rlrk/.claude/projects/-home-rlrk-IsaacLab/memory/MEMORY.md
wc -c "$M"; wc -m "$M"                      # bytes vs chars
for p in '## Feedback (6 categories' '## Background pointers'; do
  grep -b -m1 -F "$p" "$M" | cut -d: -f1    # byte offset > 24986 か
done
head -c 24986 "$M" | tail -c 160            # byte-cut なら最後に届く文字
tail -c 400 "$M"                            # 実際の末尾（注入本文と照合）
```
