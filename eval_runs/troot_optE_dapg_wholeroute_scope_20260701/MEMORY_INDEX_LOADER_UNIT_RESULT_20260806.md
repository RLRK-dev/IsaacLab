# MEMORY.md の単位問題 — 決着 + 項目 2 の三つの根拠を実測

desk: p4 RS-TECH-LEAD (w2:p4) / 測定 2026-08-06 16:15:44–16:16 JST
subject file: `/home/rlrk/.claude/projects/-home-rlrk-IsaacLab/memory/MEMORY.md`
版: mtime `2026-08-05 22:53:51`（測定中 不変・handoff.md は 22:53:51 / MEMORY.md は 22:42:32）
規則の物: `CLAUDE.md` §運用31 @ commit `9213a675b0` + `f98aa82425` + `49717bddc6`（全て `658d511e5f` の祖先）

---

## 1. 実測（同一ターン・同一 file）

```
wc -c (BYTES)      : 29207
wc -m (CHARACTERS) : 21160
wc -l (lines)      : 104
```

banked 閾値（§運用31）= hard **24,986 chars** / 90% トリガ **22,487 chars**

| 単位で読むと | 値 | hard に対して | 90% トリガに対して |
|---|---|---|---|
| chars（規則が言う単位） | 21,160 | **84.7%** — 下回る | **下回る**（起票 不要） |
| bytes | 29,207 | **116.9%** — 4,221 超過 | 超過 |

⇒ 同じ file・同じ瞬間で、**単位を変えるだけで「余裕あり」と「17% 超過」が入れ替わる。**

## 2. ⭐ 決着させた検査（positive control・この会話自身が計器）

⚠ 先行して 2 卓が「loader がどちらの単位を使うかは **未検証**」と明記して止めていた
（私の dispatch + p18 台帳 §942 の単位訂正）。以下でその未検証を閉じた。

**設計**: 24,986 **バイト**で切る loader なら、byte offset 24,986 より後ろは届かない。
届いたかどうかは、**この会話に注入された MEMORY.md 本文**を見れば分かる。

**byte offset（実測）**:

| offset (bytes) | 見出し |
|---|---|
| 22,739 | `## Output Preferences` |
| 23,476 | `## Standing Operational Directives` |
| **28,727** | `## Feedback (6 categories, consolidated)` |
| **29,038** | `## Background pointers` |

**cut は 24,986** ⇒ 下 2 節は**丸ごと** cut より後ろ（かつ 24,986 は行の途中に落ちる）。

**結果**: 下 2 節は**どちらも本セッションの注入本文に在る**（末尾行まで disk と逐語一致）:

```
## Background pointers
- ⭐[pC 動画 leg 手法](reference-pC-video-analyst-route-video-method-2026-07-11.md): 7-cam montage / zoom+brighten / 世界系 R-L 帰属。
```

⇒ ⭐ **loader は 24,986 バイトで切っていない。** 29,207 バイトの file が丸ごと届いた。

### honest scope（この検査が言っていないこと）
- 照合したのは **末尾 2 節と最終行の在否**。注入本文と disk の**全文 byte 比較はしていない** ⇒
  「24,986 バイトでの末尾切断が無い」ことは言えるが、あらゆる truncation を排除してはいない。
- ⛔ **loader の実際の上限は不明のまま。**「24,986 バイトではない」≠「24,986 chars である」。
  規則の数（chars）が loader の数だと確かめたのではなく、**「17% 超過」に見せていた byte 読みが偽**だと確かめた。
- 版限定: mtime `2026-08-05 22:53:51` の内容について。

### ⭐ なぜ 2 日見落とされたか（型）
この検査は**最初から誰の手元にもあった** — 注入本文は loader の出力そのもの。
2 卓は backup file の履歴と hook の label から論じ、**loader の納品物を読みながら、それを納品物として見なかった**。
⇒ ⭐ **「切れたか」を答える artifact は、loader 自身の配達物であり、私はそれを最初から読んでいた。**
（型 = 計器が結論の側でなく、context の側に置かれていた。近縁 = 述語が識別しない検査／measured-but-out-of-scope）

## 3. 項目 2（私の未解除書き込み 8 件）の三つの根拠を実測

§運用31 が凍結を置き換えたあと、**選択肢 A（復元）を支える根拠が残っているか**を面ごとに測った。

| 面 | §運用31 の規定 | 実測 | A の根拠は残るか |
|---|---|---|---|
| topic file ×2 | **解放**（通常どおり書いてよい） | — | ⛔ **残らない**（現行規則が許す内容） |
| `MEMORY.md` | 成長条件・90% 超で coordinated 圧縮 | **84.7%（21,160 chars）** = トリガ未満 | ⛔ **残らない**（大きさの根拠なし） |
| `handoff.md` | 自分の節のみ Edit・⛔全書き換え禁止 | 印を付けた 6 箇所は**全て p4 節**（`:11`/`:20`/`:56`/`:98`/`:118`/`:191`/`:197` の見出しは全て `(p4 …)`）＋共有 frontmatter `:3`。全書き換えなし | ⛔ **残らない**（制約は充足） |

⇒ **A（復元）を支える根拠は三面とも残らない。**

⚠ **ただしこれは「Rs が B を決めた」ではない。** 規則の解除は**以後**について書かれており、
**解除の 1 時間前に書かれた分を遡って免じる**とは書いていない（境界の問いと同一性の問いは別）。
私が動かしたのは**根拠の測定**であって処分ではない。⇒ 残るのは Rs の一言のみ。

## 4. 規則そのものへの含意（⛔ 私は CLAUDE.md を編集しない）

§運用31 は **chars** で書かれており、本測定は**その読みを支持する**（矛盾しない）。
⇒ ⭐ **規則の訂正は要らない。** 閉じたのは規則の外に在った「単位が未検証」という穴のみ。
`CLAUDE.md` は L3・Rs 専権ゆえ、注記を足すかは Rs 判断。私は報告のみ。

---

## 再現コマンド

```bash
M=/home/rlrk/.claude/projects/-home-rlrk-IsaacLab/memory/MEMORY.md
wc -c "$M"; wc -m "$M"                      # bytes vs chars
for p in '## Feedback (6 categories' '## Background pointers'; do
  grep -b -m1 -F "$p" "$M" | cut -d: -f1    # byte offset > 24986 か
done
head -c 24986 "$M" | tail -c 160            # byte-cut なら最後に届く文字
tail -c 400 "$M"                            # 実際の末尾（注入本文と照合）
```
