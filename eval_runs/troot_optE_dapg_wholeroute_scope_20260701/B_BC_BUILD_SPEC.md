# B (BC-imitation) BUILD SPEC — converter + trainer + evaluator for the committed C1→C2 route

**v2.1 (post-5体-debate + pre-check errata §10)** — Author: RS-TECH-LEAD (%12). Date: 2026-07-02.
⚠ v2.1: `/pre-check` first pass = BLOCK (1 CRIT / 2 HIGH / 6 MED, `B_EVALUATOR_PRECHECK.md`) → ALL resolved in **§10 ERRATA** (binding; §10 overrides earlier sections where they conflict). Recorder now COMMITTED `fd005ab83f` (Rs rider-2 承認).
v1→v2: folded ALL accepted challenges from the 5体 pre-debate (CC2:8 / CC3:6 incl 2 CRITICAL / CC4:7 / CC5:7 / CC6 NHA CHANGE_JUSTIFIED+1). Adjudication record: `B_BC_BUILD_SPEC_DEBATE_R1.md` (same dir). **Binding contract**: the builder builds THE SPEC; deviations = STOP → %12.
**Decisions of record:**
- **DQ1 = B** — Rs verbatim「B」2026-07-02 ~07:37 (node `state.md:38`). A/C = future options, not killed.
- **DQ4 = DR range** — Rs verbatim**「XY ±20mm で開始」**2026-07-02 ~08:01 (node `state.md:41`). Matches `CABLE_XY_DR_AMPLITUDE=(0.020,0.020)` (`task_config.py:264`).「開始」= start value, later widening possible.
- Scoping adopted: %10 `B_BC_IMITATION_SCOPING_COORD2.md` fork (i) + lean obs + scripted gripper + `bc_pretrain.py` reuse + separate evaluator (rationale there; this file binds).
- **0-commit** until Rs approves. INVARIANTS #1-5 untouched **in B0/B1** (locked route file NOT edited). ⚠ **B2 wiring (§5.1) = a known locked-file edit = L3 + its own Rs approval (§8-6b)** — headline corrected per debate (CC6/CC4).

## §0. INPUT ARTIFACT (on disk, verified + %9 GT cross-PV CONCUR-PASS 2026-07-02)

`p3_dod_cuda_demo_raw/route_demo_raw.npz` (12.0MB, cuda:0, fingerprint == authoritative baseline EXACT) + `route_demo_raw_meta.json`; full DoD evidence durable at `p3_dod_evidence/`. Actual schema (np.load-verified): T=7707; 22 arrays — `ee_pos_l/r [T,3]`, `ee_quat_l/r [T,4]` xyzw, `ee_tgt_pos_l/r [T,3]`, `ee_tgt_quat_effective_l/r [T,4]`, `arm_q [T,74]` (= FULL physics joint_q incl ~46 cable coords — misnomer, %9 flag), `grip_cmd [T,2]`, `cable_xyz [T,40,3]`, `cable_quat [T,40,4]`, `phase_id [T]` (−1×10 initial + 13 phases), `nearest_seg_l/r`, `held_seg_l` (⚠ unconditional Y-argmin, NEVER a validity signal — CC2-1), `pin_active/pinned_body/pin_eqid`, `ik_rot_override_active`, `frame_idx`, `sim_time`. Meta: `phase_names` 13, `resolved_clip_c1_xy/c2_xy`, `n_grip_events=26` (= note_grip CALL count, ≠ the 37 per-arm value transitions — CC2-5), `env_gates` (⚠ omits CLIP2_X/Y — do NOT treat as complete; assert against `resolved_clip_*` instead — CC5-7b), `as_run_sha256`, `sim_substeps=10`, `quat_convention=xyzw`, `verdict`.
Recorder follow-ups (%9 §2, before any B2 re-record): meta C2 hardcode `recorder:217`, CLIP2 env_gates omission, "(CPU)" banner `test:3816`.

## §1. PIPELINE + MILESTONES

```
route_demo_raw.npz ─(converter)→ bc_dataset.npz + schedule.json ─(bc_pretrain)→ policy.pt ─(policy_route_runner, cuda:0)→ runner_verdict.json + video leg
```

| Phase | What runs | Pass bar (pre-declared) | Cost/gate |
|---|---|---|---|
| **B0** | evaluator replays CONVERTED actions open-loop (no policy; same code path, policy⇄lookup) | fingerprint **categories** (`regrasp_ok=True`, verdict ∈ SUCCESS_*, C2 seat, C1 held) + **per-step tracking-error series delivered** (§9-B0) + video leg | ~40min/run + smoke runs; `/pre-check` + scoped `/reward-design` first (§7) |
| **B1** | `bc_pretrain.py` on the 1 converted demo → **deterministic actor-MEAN** rollouts ×N=5 | category-match RATE + **per-rollout failure-taxonomy table** (desync/drift/GPU-non-det/converter) + video-analyst leg on EVERY rollout counted SUCCESS (L-claw crop; rate reported split video-verified/numeric-only — CC4-6) | ~3.3h GPU total; honest framing = pipeline shakedown |
| **B2** | DR demo-SET (±20mm) → record → convert → BC → held-out rollouts | §5; **fresh Rs GO with B1 rate+taxonomy in hand (NOT pre-approved)** — CC5-5 | **≥16 route-runs ≈ ≥10.7h ⇒ `/production-launch-gate` REQUIRED** (CC5-2/CC4-5) |

**Aggregate GPU budget (whole-B, honest):** B0 ~0.7h + B0-smokes ~0.5h + B1 ~3.3h + B2 record 8.7h + held-out ≥2h + retry budget ~2h ≈ **~17h cuda:0 total** — stated here so Rs approves the aggregate, not slices (CC4-5).

## §2. CONVERTER — `thread_isaac_lab/scripts/route_demo_to_bc.py` (NEW, ~150-250 LOC)

Pure offline transform (numpy only; no solver/mujoco/env import).
**2.1 Cadence:** every 10th physics frame (`PHYSICS_STEPS_PER_RL=10`, AC env `:238`) → T_ctrl=770 (frame 7700 last; 6-frame tail has zero motion/events — verified CC2).
**2.2 Actions [770,6]:** `a[t] = concat(Δee_pos_r, Δee_pos_l)/0.015` (`POS_ACTION_SCALE`, AC env `:230`; R-then-L per AC layout `:39-42`). Measured (both arms — CC2-4): max ‖Δ‖ R=8.53mm / **L=11.39mm**, max per-component 8.8mm ⇒ max |a|=**0.59** < 1, no clipping.
**2.3 Obs [770,25]:** R EE (0:3) + L EE (3:6) + active-target seg pos (6:9) + next-clip target (9:12) + phase one-hot 13 (12:25; −1→GRASP_HOVER, 10 frames zero-motion).
**seg(t) rule (CONCRETE — CC2-1/2):** per-phase table finalized at build time from THIS npz and **echoed into dataset meta** (indices + windows auditable): GRASP..C1_SEAT-entry → `nearest_seg_r[10t]`; C1_SEAT..C1_PIN → **seated seg := `pinned_body − cable_body_start`** (ground truth IN the npz = seg 27; do NOT use argmin-at-phase-entry which gives 28 — off by one full 15mm seg); L_HALF_UNCLAMP.. → **grip-predicated held seg**: `held_seg_l[10t]` iff `grip_cmd[10t,L] ≥ 0.6` (closed) else `nearest_seg_r[10t]`; GUIDE_C2.. → nearest-to-C2 seg (recomputed from `cable_xyz`, NOT the recorder convenience indices — %9 §2.3a).
**next-clip flip (CC2-6):** C1 through C1_PIN; **C2 from L_HALF_UNCLAMP entry**; z_top = clip-top z derived from task_config CLIP_FLOAT_Z+clip height, numeric echoed in meta.
**2.4 `schedule.json` (evaluator replay track):** `grip_events` = the **37 per-arm value transitions** (frame, arm, target_rad; init state `grip_cmd[0]=[0,0]` asserted — CC2-5); `pin_event` = (frame≈2544, `pinned_body`, `pin_eqid`) + **anchor semantics = live-derived at fire time (§4.5)** — the npz has NO anchor (CC3-4); `phase_transitions`; `ik_rot_override` window (5000..6180) — **converter ASSERTS `ee_tgt_quat_effective_{l,r} == default Rx(−90) xyzw on ALL frames** (true for this npz) **and exactly ONE override window; any violation = STOP** (unsupported in v1 — CC2-8/CC3-5); **verdict landmark frames (CC3-6):** C1_SEAT landmark (claw-partition freeze), C2 hover-end (reach target = recorded `ee_tgt_pos_r` there), post-close settle end (grip-force read frame).
**2.5 Output `bc_dataset.npz`:** `obs [770,25] f32` + `actions [770,6] f32` (= `bc_pretrain.py:67-68` contract) + meta (source sha256, cadence, seg table, layouts, scale).
**2.6 Self-checks:** shapes; |a|≤1; finite; one-hot sum=1; cumulative reconstruction `<1e-5` (measured 2.7e-7 — CC2 verified); seg table covers 13 phases; grip init/transition asserts; quat-default assert (2.4).

## §3. TRAINER — REUSE `bc_pretrain.py` (:28/:49/:67-68/:138) + wrapper ≤60 LOC

`build_actor_critic(25, 6, (128,128), "cuda:0")` → `train_bc(..., epochs, batch=256, lr=1e-3)` → `save_bc_checkpoint` (RSL-RL format = future PPO-fine-tune compatible). MSE on actor mean. **Inference = deterministic actor MEAN ONLY (`act_inference`/mean; NEVER `act()` sampling — `init_noise_std=0.1` ⇒ 1.5mm/axis/step noise on a 0.9mm-margin route; CC2-7/CC5-6). Inference mode recorded in runner_verdict.json.**

## §4. EVALUATOR — `thread_isaac_lab/scripts/policy_route_runner.py` (NEW, ~250-400 LOC)

**4.1 Startup preamble (BINDING checklist — CC3-1/2 CRITICAL fixes; mirrors route `main()` :6480-6615):**
1. Set canonical env-gates via `os.environ` BEFORE importing the route module (module reads gates at import; `PERCLIP_PIN=1` must precede `build_scene` or zero eqs pre-allocated `:1386-1397`). Gate VALUES from meta `env_gates` + **assert scene C1/C2 == meta `resolved_clip_c1_xy/c2_xy` after build** (env_gates echo incomplete — CC5-7b).
2. `sys.path.insert(0, <envs dir>)`; `from newton_skill_env_base import make_solver, _wire_s6_grasp_solref` (**NOT from the route module** — `make_solver` is main()-local there; CC3-1). `from test_newton_clip_routing import build_scene, physics_step, solve_ik_dual, build_fk_model, _set_gripper_target` (module-level, `__main__`-guarded :6898). **Assert route-file sha256 == meta `as_run_sha256`** at start (CC3-LOW).
3. FK: `build_fk_model()` + home-config init + `FINGER_OPEN_POS` on +7/+8 + `newton.eval_fk` (mirror :6531-6548).
4. `build_scene(use_cable=True, fk_model, fk_state, solver_backend="mujoco", grasp_actuation=True, grasp_y=0.15)` (args from meta env_gates S6_ENGAGE_YC — CC3-2.6).
5. Wire `scene_info["fk_model"/"fk_state"]` (:6577-6578); `vbd_control = model.control()` + `scene_info["vbd_control"]` (:6584-6585); `model.rigid_contact_max = NJMAX`; `contacts = model.contacts()` (:6588-6589).
6. **`scene_info["gripper_dynamic"] = True`** (:3525 pattern; WITHOUT it physics_step FK-overwrites all 28 gripper coords and the servo is inert ⇒ B0 cannot grasp — CC3-2.1). **B0 DoD asserts this flag before the loop.**
7. `make_solver(model, backend="mujoco", enable_cable_contacts=True)` → **`_wire_s6_grasp_solref(solver, scene_info)`** (:6607-6608; negative PAD_SOLREF poke — omitting it = demo-mismatched grasp contact physics; CC3-1b/CC5-1). **Startup parity assert: solref readback == demo-known values.**
8. Initial open servo `_set_gripper_target(control, driver_joints, GRIPPER_DRIVER_OPEN_RAD)` (:3526 mirror); assert `scene_info["perclip_pin_n"] > 0`.
**4.2 Control loop (×770):** (1) build obs (§2.3 mirror, live state); (2) `a` = policy mean (B0: dataset lookup — same code path); (3) `tgt = ee + clip(a,±1)×0.015`; (4) **arm apply = KINEMATIC FK-assign (the actual route mechanism — CC3-3):** IK once per control step → **sub-interpolate the joint-space delta across the 10 frames (gripper coords preserved, :1988 pattern)** → per frame `fk_state.joint_q.assign` + `newton.eval_fk` + `physics_step` (internal SIM_SUBSTEPS=10, `task_config:101`). Integrator identical to demo; **pose-update cadence ≈ demo via sub-interpolation** (NOT claimed "EXACT" — CC3-3); sub-interp mode recorded in verdict json. (5) scheduled events fire at their EXACT physics-frame indices INSIDE the 10-frame window (≤9-frame quantization eliminated — CC3-LOW).
**4.3 Phase clock (B0/B1):** fixed recorded schedule. Rotation: NO monkeypatch replay — plain `solve_ik_dual` via module attribute; the §2.4 quat-default assert guarantees value-equivalence for this demo (CC3-5).
**4.4 Verdict re-implementation** (locked file untouched): recompute at the schedule.json landmark frames — claw partition frozen at the C1_SEAT landmark (`_arm_split` GRASP_YC semantics :3677-3682/:4085), `_reach_R_mm` vs recorded `ee_tgt_pos_r` at hover-end, grip normals via CPU `mj_forward`+`mj_contactForce` geom-ID filtering (:4021-4033 pattern), 4-way verdict split + `regrasp_ok` (:4495-4502), spans, **C1-held = the FULL multi-criterion seat incl z (NOT XY-only — CC4-3)**, C2 seat. Emit `runner_verdict.json` (fields mirror `_c2_regrasp_rec` :4505-4518 + `source` tag + inference mode + sub-interp mode + `seat_state_at_pin_fire`). **Cross-validation (anti-moving-goalpost — CC5-4):** categories = judged, PRE-DECLARED (equality at the fingerprint's defined 1-2dp rounding); numerics = REPORTED in a delta table, not judged.
**4.5 Pin replay (authorization-scoped — CC4-3/CC3-4):** at the scheduled frame, **verify the canonical multi-criterion seat FIRST; unseated ⇒ DO NOT FIRE ⇒ rollout = FAIL/INVALID** (pinning an unseated cable = outside the Rs pin authorization). If seated: full 3-part poke with **anchor = live seat-body world pos** (`wp.synchronize` + `mj_forward` → `eq_data[0:3]=[0,0,0]`, `eq_data[3:6]=live pos` → `eq_active=1` → 40 settle frames; :4105-4126 mirror). B2 rollouts likewise derive pin body LIVE (seg-identity shifts under DR — CC2-3).
**4.6 Smoke mode (CC5-4):** `--max-control-steps K` (e.g. GRASP..LIFT) — mandatory before any full 40-min run during B0 debug.
**4.7 Video leg:** offscreen post-hoc render on every VERDICT rollout (B0 final, every B1-SUCCESS, B2 held-outs) → skill-path video-analyst. Debug rollouts exempt (loud note).

## §5. B2 — DR demo-SET at cable XY ±20mm (design-only until its own gates)

**5.1 Wiring = a LOCKED-FILE EDIT (honest — CC6/CC4-1):** `build_scene` has no cable-position param (sig :1031-1032; `cable_start` hardcoded :1365). ANY wiring (env-gate read / new param / monkeypatch) edits the Rs-LOCKED route file ⇒ **L3 + DESIGN-GATE + its own Rs approval (§8-6b) + fingerprint-equality proof that default-unset = byte-identical behavior.** Preferred form: additive `cable_xy_offset=None` param (grasp_y precedent). Explicit per-run offsets, no in-harness RNG.
**5.2 EE-target follow gate:** verify the GRASP-phase targeting follows the ACTUAL cable (later phases argmin-follow) — grasp_y-sweep proxy banned (`SPEC_77:32-34`; v1's ":56-58" cite was false — CC5-7a; ⚠ same bad cite in `P3_DEMO_RECORDER_SPEC.md:25` + node `state.md:41`, flagged for fix).
**5.3 Validity filter + honesty (CC5-3):** only fingerprint-SUCCESS runs train; **success-rate over the design = the primary range-coverage number**; **held-out ⊂ scripted-SUCCESS draws only** (else policy-fail vs route-infeasible is unattributable); **minimum training-set floor = 6 demos** — fewer ⇒ report BLOCKED + widen/redesign (no silent B1-redux); pre-declared held-out semantics BEFORE runs.
**5.4 Cost (full accounting — CC5-2):** 13 recordings + ≥3 held-out evals + retry budget ≈ **≥16 runs ≈ ≥10.7h ⇒ `/production-launch-gate` REQUIRED before B2 execution** (no boundary-slicing).
**5.5 What a B2 PASS banks AS (CC5-3d):** feasible-region, sim-perfect-state, scripted-events, pin-ON, cuda:0 generalization ONLY — **NON-CONSERVATIVE for full-range/deployment/real** (survivorship + perfect obs). Recorded as a §6 row.

## §6. RISKS / CONSERVATISM (delta over %10 §5, incorporated by reference)

| Item | Direction | Note |
|---|---|---|
| B0 apply-path gap | honest-unknown → **B0 DELIVERS the per-step ‖ee_replay−ee_demo‖ series** (§9-B0) | on FAIL: attribution protocol first (converter bug / wiring / gap / non-det) incl **1 scripted-route control run BEFORE any fork-(ii) talk** (CC5-4) |
| Fingerprint re-impl drift | mitigated | §4.4 categories pre-declared; numerics reported-not-judged |
| Phase-schedule desync under policy | NON-CONSERVATIVE for B1 | taxonomy table per rollout (§9-B1); pin fire is seat-verified (§4.5) so desync cannot launder success |
| B2 near 0.9mm knife-edge | conservative info | off-center failures = real feasibility data |
| **B2 held-out PASS** | **NON-CONSERVATIVE for full-range/deploy/real** | survivorship + perfect-state + scripted events + pin-ON + cuda:0 (§5.5) |
| Import side-effects | build-time check | env-gates before import; sha assert; parity asserts (§4.1) |

## §7. L-TRIAGE + GATES + BUILD ORDER (re-classified per CC4-2 + recorder precedent)

- **The B build = ONE L3-managed change-set**: evaluator carries `solver/phase/success` as CORE logic (§0 keyword rule has no new-file exemption) + likely >200 LOC + recorder precedent (274>200 → L3). So: **pre-gates (this 5体 debate = done) + post-build 層2 debate + 層5 multi-view** on the landed code. Converter/wrapper ride the same chain (batch).
- **DESIGN-GATE (CC4-4):** scoped `/reward-design` on the verdict re-implementation + B1/B2 pass bars (ground-truth = canonical json; reachability = B0 replay; trace = the demo) + `/pre-check` on the evaluator — BOTH before the build charter.
- Build order: converter (+§2.6 on real npz) → evaluator skeleton → **smoke (§4.6)** → B0 → B0 report → B1 → B1 report (rate + taxonomy) → **fresh Rs GO** → B2 (its own L3 + production-launch-gate + §5.1 approval).
- Charter to %11 **after** the recorder verification chain (%9 ✅ done → 層2/層5 in flight) completes. 層3 on every code drop.

## §8. Rs CONFIRMATION ITEMS — ✅ **Rs 承認 2026-07-02「§8 推奨どおりで良い」= items 1-7 全て推奨デフォルトで RATIFIED**
(item 6b の織込み: B2 kickoff は承認済み framing 自体により、B1 結果を見た fresh GO + `/production-launch-gate` + §5.1 locked-file L3 承認を別途要する — 本 ratification はその手順を確定したもの)

1. **B1 pass bar** = category-match rate over N=5 (deterministic-mean inference) + per-rollout taxonomy + video-analyst on every counted SUCCESS — OK?
2. **Gripper scripted** in evaluator (B0/B1) — OK?
3. **pin-ON demos** kept; runner pin fire is **seat-verified** (unseated ⇒ no fire ⇒ FAIL) = stays inside the pin authorization — OK?
4. **Device** cuda:0-only eval; reach knife-edge carried as known gap — OK?
5. **Verdict re-implementation** (locked file untouched) — OK?
6. **B2**: (a) design = 13 recordings + ≥3 held-out (min-training floor 6; §5.3 semantics), **aggregate ~17h whole-B GPU budget acknowledged**, B2 runs `/production-launch-gate`; (b) **§5.1 locked-file wiring = L3 + Rs approval at B2 kickoff** (fresh GO with B1 results in hand) — acknowledge now, decide then?
7. **INVARIANT check**: no change (dual-arm 6D position-Δ through solve_ik_dual; ori constant; コ/span/no-trick untouched) — confirm.

## §9. DoD (per phase)

- **Converter:** §2.6 all-pass on real npz + 5-row human dump + meta complete (seg table echoed).
- **Evaluator+B0:** startup parity asserts log (solref/sha/clip/gripper_dynamic) + smoke evidence + open-loop verdict json + categories PASS + **per-control-step tracking-error series (both arms) + max/terminal drift + drift at each event frame** + §4.4 delta table + video leg + 0-diff on locked files + 層3.
- **B1:** loss curve + N=5 verdict jsons + rate (split video-verified/numeric-only) + per-rollout taxonomy table + video-analyst legs + honest shakedown framing.
- **B2:** per-run manifest (offset/seed/verdict/sha) + success-rate map + held-out results + §5.2 follow-gate evidence + production-launch-gate record.

## §10. ERRATA (binding; resolves the pre-check BLOCK — B_EVALUATOR_PRECHECK.md ISSUE 1-9)

- **E1 (CRIT→resolved) sha assert:** §4.1-2's route-file sha assert accepts the SET {as-run `9bdf8f63…` , committed `fd005ab83f` state `ac0e3f53…`}. Equivalence chain (recorded evidence, not assumption): batch-fix delta = recorder-gated hooks/banner/label lines ONLY (`p3_dod_evidence/wt_hook_diff_*.patch` + `P3_RECORDER_BATCHFIX_REPORT.md` DEMO_RECORD=0 byte-identity + physics fns content-unchanged); runner FORCES `DEMO_RECORD=0` (E9) so the delta is dead code in its process. Runner records the actual sha it saw in `runner_verdict.json`.
- **E2 (HIGH→resolved) event/frame ledger:** events fire at their recorded PHYSICS frame index, INSIDE the normal frame stream — **no extra settle steps injected** (the demo timeline already contains the pin's 40 settle frames; §4.5's "40 settle frames" mirror wording is void). Convention: event at frame f fires BEFORE that frame's physics_step, at loop position f−10t of control step t=⌊f/10⌋ (matches hook semantics: grip recorded at assign; pin recorded post-eq_active pre-settle).
- **E3 (HIGH→resolved) phase-schema versioning:** B0/B1 consume the FROZEN 13-phase canonical npz → one-hot 13 / obs 25D stands. The batch-fixed recorder emits **15 phases** on any NEW recording → **schema v2 (one-hot 15 / obs 27D) for ALL B2 data**, fixed at B2 kickoff. Converter asserts `len(meta.phase_names)` matches its schema version; mixed-version datasets = STOP.
- **E4 (MED→resolved) cable_body_start:** pinned constant **28** (derivation: `pinned_body` 55 − seated seg 27, verified on the npz). Converter hardcodes 28 WITH a self-validating assert: `‖cable_xyz[t_pin, 55−28] − (pin-frame pinned-body pos)‖ < 15mm`.
- **E5 (MED→resolved) anchors:** all §4.1/§4.4 line anchors are indicative (batch-fix drift +3..+14; e.g. verdict block no longer at :4495-4502 exactly). Builder MUST re-grep every anchor against `fd005ab83f` and record the resolved anchor table in the build report. Spec anchors are NOT to be trusted at build time.
- **E6 (MED→resolved) column orders:** `grip_cmd` columns = **[L, R]** — OPPOSITE of the action layout (R-then-L). Converter/evaluator map explicitly (named indices, no positional reuse). Servo writes go through `scene_info["vbd_control"]` (the object `physics_step` consumes).
- **E7 (MED→resolved) tightest cliff:** the narrowest B0 margin is C2 `settled_in_notch` (**0.892mm / 1.4mm**), not the reach/close cliffs (19.1mm / 104N). B0's per-step tracking additionally records seat-z error at the C2 landmarks and reports margin consumption explicitly.
- **E8 (MED→resolved) video + failure policy:** video leg reuses the route's offscreen frame-capture pattern (every-N frames → mp4 post-hoc). `solve_ik_dual` non-convergence → log + continue (mirror route) + tag the frame in `runner_verdict.json`; hard crash → partial artifacts tagged INVALID, never counted in any rate.
- **E9 (MED→resolved) startup additions:** checklist adds `state = model.state()` (§4.1-5.5); env-gate replay skips null-valued keys and **forces `DEMO_RECORD=0`**; the sha assert extends to `task_config.py` (as-run `1a0851db…`, currently unchanged — verified).

**v2.2 (post-re-check, closes the 2 WARN residuals — `B_EVALUATOR_PRECHECK_R2.md`):**
- **E4′ (replaces E4's assert — R2 NEW-2):** the E4 self-check was circular/±1-blind. Binding assert (numpy-only, exact): at the pin-fire frame `t_pin`, `argmin_j ‖cable_xyz[t_pin, j, :2] − resolved_clip_c1_xy‖ == pinned_body − 28`. Integer equality rejects ±1-seg errors (wrong-constant candidate sits 12.2mm away — inside E4's old 15mm tolerance, outside an argmin).
- **E10 (R2 NEW-1):** `_wire_s6_grasp_solref` lives in `newton_skill_env_base.py` = **WT-modified, UNCOMMITTED (+301/−13, absent at HEAD)** and NOT pinned by the demo meta. Runner startup **sha-pins it: `e6d3cdbc…` (2026-07-02 state)**, asserts + records in `runner_verdict.json`; mismatch = fail-closed STOP. Added to the build-report provenance table. ✅ **Rs-flag RESOLVED 2026-07-02**: Rs「newton_skill_env_base.py も commit 可」→ full hunk audit (all S6-track, gated default-False byte-identical) → **COMMITTED `7eded5368c`** (+301/−13; content sha unchanged ⇒ the `e6d3cdbc…` pin remains exact).
- **E11 (R2 NEW-3, LOW):** extending the E1 sha accept-set beyond {as-run, `fd005ab83f`-state} requires a %12-authorized spec edit; default = fail-closed STOP.
- **E13 (post-B0 fork, %12 決裁 2026-07-02 ~11:5x):** B0-(i) full run = **category FAIL, cleanly attributed** (control-run-first protocol executed: scripted route on the sha-identical env = SUCCESS same-day; converter/wiring/non-det ruled out) → the relative-delta apply accumulates a directional ~14mm EE bias over 770 steps (per-step tracking ≤1.02mm but landing errors are chained, never corrected) → cable 23.1mm off the 0.5mm C1-seat bar → pin correctly NO-FIREs → cascade. **Disposition: (a) the B0-(i) FAIL result STANDS as the characterized apply-path measurement (conservative direction, banked);** **(b) NEW diagnostic leg B0a = absolute-waypoint replay**: targets = cumsum(dataset actions × scale) from the seeded origin (≡ demo EE waypoints by §2.6 recon; consumes ONLY converter outputs) applied as ABSOLUTE per-step targets → landing errors stop accumulating (~1mm bounded) → expected to seat → validates the yet-unexecuted legs (pin fire §4.5, C2 verdict §4.4 cross-validation vs canonical, E7 margins, video-analyst on a seating case, per-dim obs-parity re-read where trajectories align). ⚠ B0a is a PIPELINE-VALIDATION leg, **NOT a substitute pass bar for the (i) semantics** — no goalpost move. **(c) PRE-REGISTERED B1 prediction (anti-hindsight): with (i) relative deltas + 1 demo, a closed-loop MLP has no restoring term in its data → B1 category rate predicted LOW; the §8-1 deliverable (rate + per-rollout taxonomy + video-verified split) is the outcome either way; correction mechanisms live in B2-DR + the future DAPG PPO stage — any action-semantics change = a NEW design fork requiring its own gate (not improvised).**
- **E14 (post-B0a fork, %12 決裁 2026-07-02 ~12:2x):** B0a validated: accumulation FIXED (16.7→1.38mm max), converter action-stream (recon + re-grasp §4.4 cross-val PASS: reach 3.6mm / verdict SUCCESS_*-set match), §4.5 no-fire logic. **Isolated residual = 10-frame linear sub-interp fidelity (~1.4mm EE) vs the 0.5mm C1-seat knife-edge** (cable 1.68mm off → correct no-fire → pin-FIRE / C1-held / C2-seat / E7 legs REMAIN UNVALIDATED). ⚠ note non-conservative nuance: B0a reproduces CATEGORIES, not grip physics (9.34N dual-loaded vs canonical 104.51N R-only at ~1.4mm offset). **Disposition: B0b = fork-(ii)-AS-DIAGNOSTIC** — replay the RECORDED commanded macro-targets through the route's own `ik_move_both` (:1924, byte-identical apply path; macro schedule = `ee_tgt_pos_l/r` transition extraction from the npz; events still fire per schedule.json; ik_rot default asserted) → expected to seat → validates the remaining verdict/pin/E7 legs + full §4.4 cross-validation. **Purpose-scoped: B0b validates the EVENT+VERDICT machinery only (the action stream is already B0a-validated); fork-(ii) is NOT adopted as policy semantics — (i) stands; any semantics change remains a gated design fork (E13c).** After B0b: **B1 GO** (§8 ratified; E13c low-rate prediction stands; obs-parity flag: cable/seg dims diverge with the route — inherent, framed).
- **E12 (smoke-discovered 2026-07-02, spec-gap %12-owned):** the §4.1 checklist mirrored `main()` (:6480-6615) but MISSED the route-FUNCTION's grasp-approach seed (`test:3839-3848`, `seed_l/seed_r` → EE [0.267,0.202,1.560] = the demo's frame-0 arm). Without it the replay starts from the home config = **1.1m absolute origin offset that per-step delta-tracking CANNOT see** (deltas were <1mm throughout). Binding: startup ALSO applies the route-fn seed (override + `eval_fk` + `update_kinematic_bodies`) BEFORE obs[0]; validated smoke3 (t=0 offset 0.004mm; abs drift 12.4mm over GRASP..LIFT). Lesson banked: open-loop replay validation MUST include an ABSOLUTE-pose leg, never delta-only.
- **E15 (A-fork = action-representation fork-(iv), Rs「A で進めて」2026-07-02 15:2x; E13c の gated fork; v2 = 5体 pre-debate R1 反映 [34 challenges 全 ACCEPT、REBUT 0 — `B_E15_DEBATE_R1.md`]。v2.1 = /pre-check R1 **BLOCK** (CRIT 1 + HIGH 1 + MED 3) の 5 fix 反映 — 旧 OG-b スカラー β bar は protocol deadlock (正しい policy も data-forced β≈1) につき置換、OG-a STOP を phase-scope 化、P1 帰属判別、P2 を P2a/P2b 分割。**v2.2 = /pre-check R2 WARN の definition pin 4 件反映で CLOSED** (per-step tangent + dwell 総 gain / OG-a 中間帯 BLOCKED / OG-b′ transverse metric + any-STOP-wins / P1 catch-all + P4 δ 条件) + ISSUE 10 文言訂正 2 件):**
  - **B1 帰結 (E13c CONFIRMED):** 0/5、5/5 BLOCKED_REACH_WALL。機構 = per-step 着地誤差の**積分** (relative-delta apply が誤差を chain; 追従 1.09mm でも resid 95.2mm)。学習は収束 (val 3.35e-4 < train)。⚠ B1 の N=5 は実効 n=1 (5 本 bit-identical、sha 実証 — CC2/CC5)。
  - **fork-(iv) = ABSOLUTE-TARGET actions、per-phase affine (v2 CRIT fix):** `a[t] ∈ [−1,1]^6` が次 waypoint の絶対 EE 位置を **phase 別 (13) × per-axis** affine で符号化: `a = 2(wp − lo_p)/(hi_p − lo_p) − 1`、`lo_p/hi_p` = **その phase の waypoint min/max ± 10% margin**、meta `abs_affine[13][6][2]`。decode 時の phase は schedule clock (runner 既存)。**global affine は棄却** — 実測 range (mm): Rx 133.5 / Ry 82.3 / **Rz 492.9** / Lx 133.9 / **Ly 305.6** / **Lz 492.9**。z の 492.9mm は GRASP_HOVER 降下 sweep (385.7mm) が支配し、**seat 判定 phase の z range は 22-32mm のみ** → global だと z decode 誤差 5.4mm ≈ workspace 級 (便益 ≈ nil)、per-phase なら seat phase box ~30mm → **~0.3mm 級** (5/5 体 concur; CC2-CH1 CRIT)。式 pin: `decode_err_mm = RMSE_a × (hi_p − lo_p)/2 × 1000` (v1 の workspace 11mm は ×2 誤り → 正 5.5mm; 結論不変)。
  - **guard-1 v2 (床):** phase×axis で `hi−lo < 30mm` → **midpoint (min+max)/2 中心**に 30mm へ拡幅 (v1 の median 中心は歪み軸で |a|>1 に割れる — 4体 concur)。適用 (phase,axis) 一覧を meta に記録 (**seat 系 phase の低 range 軸で発火が正常** — C1_SEAT z 21.8mm は発火、C2_DUAL_SEAT z は 31.9×1.2=38.3mm box で非発火、range≈0 の x/y 軸は発火 [re-check ISSUE 10 で「常時」を訂正])。
  - **RMSE 移転の honesty:** B1 の 1.8e-2 は delta-repr の pooled proxy (per-axis 5.7× 差を隠す / 再学習で非不変 [equal-R² 仮説なら z 13.5mm] / interleaved val は楽観バイアス — CC2-CH2) → **decode 誤差は「unknown until measured」、下記 OG gate で実測**してから GPU。
  - **encode/decode 契約:** converter assert `max|a| ≤ 0.95`; **runner は decode 前に a を [−1,1] へ clamp** (actor 終端は無界 Linear — a∈[−1,1] は label の性質で policy の性質でない; v1 guard-2 の「fork-(i) 暗黙 cap」表現は撤回 [CC2-CH3]) + **per-axis clamp 発生数 = 真の off-manifold 診断**として verdict json に記録。
  - **guard-2 v2 (rate-limit):** **per-arm 3D** `‖tgt − ee_now‖ > 15mm` → 方向保存 clamp、**挿入点 = solve_ik_dual 直前** (sub-interp はその後で clamped 制御 delta を 10 分割 = 1.5mm/frame ≤ demo 1.14mm/frame と同階級で無害 — CC3 検証)。ee_err 系列は **APPLIED (clamped) target 比**で測る。発火は (step, phase, magnitude) を記録。**「on-distribution 無発火」claim は撤回** (operand は Δwp + decode 誤差 + tracking を含む; per-arm 実測 max R 8.53 / L 11.39mm → headroom 3.61mm < 2×sweep-decode 誤差 [re-check ISSUE 10 で per-arm 値に訂正; 旧 6D 13.81mm 引用は norm 不一致] — CC2-CH4) → **expected-fire set を OG-a から事前登録**し、fires ≫ 予測 = off-manifold 信号と再定義。**fires>0 の SUCCESS には loud caveat 必須** (success-laundering 封鎖 — CC4-CH7)。位置づけ: Newton substrate (PhysX 制御 API 規則は prohibited.md preamble により非適用 — 転用こそ禁止)、fork-(i) の label-box envelope 保存、route mechanism (IK→sub-interp→FK-assign) 不変。B0a validation の移転は **no-fire step のみ byte-同一**。
  - **runner 分岐 (偽 SUCCESS channel 封鎖 — CC3-CH2):** B0a の `absolute_waypoints` 分岐は **precomputed cumsum LOOKUP** (naive 再利用は policy 出力を捨て demo 再生)。新 ctx `policy_absolute` を**独立分岐**として実装 (`if policy_absolute: tgt=decode(a) elif absolute_waypoints: lookup elif …`)、相互排他 assert、`waypoint_mode="absolute_policy(B1p,E15)"` は is_policy 分岐より上位。「B0a と同一」なのは **IK/sub-interp/physics apply 層のみ**。E12 seed は abs mode でも必須 (obs[0] 整合) + t=0 canary `‖decode(a[0]) − ee‖ < 30mm`。stale cum 配列は abs mode で不計算にガード。
  - **files/provenance (CC3-CH3/4 + CC4-CH3/5):** abs pair = `bc_dataset_abs.npz` (**obs 同梱** + actions + meta) + `bc_dataset_abs_meta.json` (abs_affine / `action_repr:"abs"` / `phase_schema:13` / guard-1 記録 / source_delta sha)。**delta trio (npz/meta/schedule) は上書きしない** (assert 存在のみ; convert() の無条件書出しを分岐)。meta.action_repr は delta 側にも追記。**train wrapper: torch/np/random seed を PIN + 記録** (B1 は unseeded と実証 — CC4-CH2) + sidecar json {ckpt sha, dataset sha, action_repr, seed}。runner は sidecar repr == mode を assert。verdict json に `runner_self_sha256` / converter sha / dataset sha / **GL backend + DISPLAY 状態**を追加。2 version 軸 (phase-schema × action-repr) は直交と宣言、両軸 assert。producer の pre-E15 状態は `e15_producer_snapshots_preE15/` (4 files + SHA256SUMS) に保全済。self_check は repr 分岐 (abs: `decode(labels)==wp ≤0.01mm` + max|a|≤0.95 + guard-1 echo; delta recon 行は abs で無効)。
  - **OG offline gate (学習後・sim 前、必須、GPU 0; v2.1 — pre-check R1 CRIT/HIGH fix。旧 β≪1 bar は構造欠陥: GRASP_HOVER は corr(wp_Rz, ee_Rz)=1.0000 で進行 index が ee しか無く、OG-a を通る policy は必然的に β≈1 → 全健全 policy を偽 STOP + collinear seg channel (corr 0.9996) 経由の偽 GO を選択):**
    - **OG-a**: `decode(policy(obs_demo[t]))` vs `wp[t+1]` 全 770 step → per-axis × per-phase RMSE-in-mm 表 (**C1_SEAT / C2_REGRASP / C2_DUAL_SEAT 行 + 各 cell n 記載** [C1_PIN n=4 / C2_SETTLE n=8 は薄い]) + 12 phase-boundary max output jump 表 + expected guard-2 fire set。**STOP/GO は phase-scoped (ISSUE 2)**: verdict-critical 行 (seat 系 z / reach 窓軸) = **p95 > 5mm STOP・seat-phase z RMSE ≤ 1mm 級 GO** / sweep 行 (HOVER 等 task bar 無し) = 相対 bar `p95 ≤ max(5mm, 2.5%×(hi−lo))` (unscoped 絶対 5mm は HOVER の 471mm box で健全 policy を偽 STOP)。**中間帯 (v2.2 pin、ISSUE 8): GO にも STOP にも該当しない場合 = BLOCKED_FOR_USER** (implementer 裁量で進まない)。
    - **OG-b (co-moving gain probe — 旧 ee-only β を置換、ISSUE 1)**: grip 状態に応じ **ee+seg を共動摂動** (post-grasp; pre-grasp は ee のみ) → 総 gain γ を **tangent (γ∥ = 局所 demo path 方向) / transverse (γ⊥)** に分解。**γ∥ ≈ 1 = EXPECTED-AND-BENIGN** (本 obs layout に独立 progress clock が無い — 反証条件ではない; drift は per-phase box + a-clamp + dwell 再 anchoring が抑える)。**GATE = verdict-critical phase の γ⊥ のみ: GO γ⊥ ≤ 0.5 / STOP γ⊥ ≥ 0.9 / 中間 = BLOCKED_FOR_USER**。β_ee / β_seg 単独値は credit-split 診断 (非 gate)。probe は **two-scale ±2mm (primary、closed-loop 誤差スケール) + ±10mm (secondary)**、deployed decode path (clamp+affine) 経由、**clamp-active cell は「gain 測定不能」と報告** (低 β 証拠に使わない — ISSUE 3)。**tangent 定義 (v2.2 pin、re-check ISSUE 6): `t̂[t] = normalize(wp[t+1] − wp[t])` を probe step ごとに使用** (per-phase 単一方向は C2_REGRASP の実測 80.1° 屈曲で γ∥ が γ⊥ に漏れる)。**`‖Δwp‖ < 0.1mm` の dwell cell (C1_PIN / C2_SETTLE は全 step 該当) は tangent carve-out なし = γ⊥ := 総 gain γ** (dwell は最清浄な integrator 検出器; healthy は γ ≈ 0)。
    - **OG-b′ (closed-loop contraction probe = 真の判別器)**: verdict-critical 各 phase で `x_{k+1} = decode(π(obs(x_k)))` (seg 共動・phase 固定) を `x_0 = wp_demo ± 5mm ×3 直交方向` から反復 → **healthy = ≤5 反復で decode-RMSE ball へ収縮 / integrator = 平行移送 → STOP**。P2 が sim で観測するものの offline 直接測定。**metric (v2.2 pin、ISSUE 9): moving phase は demo path への transverse 距離で収縮判定** (healthy γ∥≈1 は ~1 wp/iter 前進するため中心固定距離は不適)、**dwell は固定点距離。OG-b と OG-b′ の判定不一致は any-STOP-wins (fail-closed)。**
    - **OG-c (regression 化)**: `b0a_full/obs_series.npy` (770×25 実 closed-loop obs、存在検証済) 上の **decode 出力偏差 vs obs 偏差の回帰 = 実現 off-manifold 方向の経験的総 gain** (旧「RMSE 再評価」は γ=1/γ=0 を判別不能)。
  - **B1′ pre-registered 予測表 (B0a-anchored、v1 の MARGINAL 文言を置換 — CC5-C5-1; δ = OG-a 実測 decode 誤差):**
    | P | leg | 予測 | 反証条件 |
    |---|---|---|---|
    | P1 | re-grasp reach | resid ≤ 3.6 + δ_R + 2mm (bar 20mm) & reached_88mm_lane=True → **PASS 予測** | P1 FAIL は**帰属判別つき (ISSUE 4)**: resid ≥ 50mm + 単調 ramp = 積分復活 = **fork 反証 → STOP** / resid < 30mm + 非単調 + IK residual spike が reach 窓内のみ = **wall-class** (cuda:0 reach-wall 0.9mm 余白の knife-edge — fork 反証でない → knife-edge ledger へ)。per-step ‖tgt−ee‖ + IK residual 両系列を verdict json に記録 |
    | P2a | anti-integration (有界性) | abs drift ≤ 当該 phase box 半幅 + margin (全時刻) **かつ** 全 route 単調 ramp なし | 単調 ramp 再出現 = 反証 (γ∥≈1 学習解の最尤形 = sweep 内 chain + dwell reset の sawtooth — 旧 P2 の全時刻 tight bar は最尤解でも FAIL する誤較正だった、ISSUE 5) |
    | P2b | 再 anchoring | dwell-exit checkpoint (GRASP_CLOSE / C1_PIN / L_HALF_UNCLAMP / C2_SETTLE 終端) で abs drift ≤ 1.38 + δ + 1mm | checkpoint 超過 = 反証疑義 |
    | P3 | span | achieved_3d_span ∈ 95.2 ± (δ_R+δ_L) mm | 逸脱 = 機構疑義 |
    | P4 | C1-seat | cable↔C1 ∈ 1.68 ± δ mm → **pin NO-FIRE 予測** (B0a floor 1.68mm > bar 0.5mm at δ=0) | fire = anomaly → 調査が先 (banking 禁止) |
    | P5 | full SUCCESS | **非予測** | observed = anomaly protocol (human-ground-truth rule) |
    | P6 | guard-2 fires | ≈ OG-a 予測数 | ≫ 予測 = off-manifold |
    | P7 | nominal 決定性 | nominal×2 byte-identical | 不一致 = GPU 非決定性再燃 → 調査 |
    **adopt/revert 事前決定表 (CC5-C5-8 + pre-check ISSUE 4/5): P1∧P2a∧P2b∧P3 PASS → fork-(iv) を B2 schema-v2 の表現として採用 (P4/P5 は knife-edge 残余 = B2-DR/DAPG の持ち分)。P1 (integration-class) or P2a FAIL → fork 反証 → BLOCKED_FOR_USER (option: obs ablation / path-progress feature / affine 変種)。P1 (wall-class) FAIL → fork 帰属でない → knife-edge ledger 行き + Rs 報告。**P1 分類不能 FAIL (v2.2 pin、ISSUE 7 — 例: resid 30-50mm 帯 / ≥50mm 非単調 / <30mm 単調 / IK-spike 無し) = BLOCKED_FOR_USER + banking 禁止 + 両系列を Rs 提出。** P4 fire or P5 success → anomaly 調査完了まで banking 禁止。**P4 の anomaly 框付けは δ ≤ 1mm 条件付き (v2.2 pin、ISSUE 8) — OG-a 実測 δ ≥ 1.18mm では fire は EXPECTED 側 (1.68−δ < 0.5) となるため、その場合は P4 予測を実測 δ で再登録してから rollout。**
  - **OG gate 結果 + %12 adjudication (2026-07-02 19:1x、CP3 full 完走後):** COMBINED = **STOP 確定** (fail-closed 遵守; any-STOP-wins)。内訳: OG-a ep2000 = verdict-critical 全 GO (C2 系 <2mm、underfit 説明は ep100→2000 の 50× 改善で棄却) + 残余 sweep-z 3 STOP / seat-z 3 MIDDLE (δ実測 1.586-1.885mm)。**OG-b γ⊥ = STOP** (C2_REGRASP 1.17、他 verdict-critical 0.66-0.83 MIDDLE) = **1-demo abs policy は off-path で integrator (target ≈ ee + delta)** — E13c/honesty 節の「n=1 = replay-in-disguise」が構造計測で確認された。**OG-b′ = seat 系 phase は CONTRACT (restoring)** (C2_DUAL_SEAT 4.5→0.50mm 等) / sweep・reach は発散 (HOVER →372mm)。OG-c 経験 gain 0.63 (部分 integrator、dec_dev 23.7mm ≫ obs_dev 1.94mm)。**判定: (1) 残余は表現の欠陥でなく n=1 data 制約 (off-path coverage ゼロ) — repr 自体は decode 忠実 + seat-restoring を実証 (per-phase box + dwell anchoring の効果 = delta repr には無い性質)。(2) CP5 の pass-bar rollout は禁止のまま。(3) CP5′ = characterization rollout を purpose-scoped で許可** (B0b 前例の診断 run 類型): **N=2 nominal のみ** (P7 byte-identity; jitter×3 は非採用 policy の robustness 測定につき削除)、P-table は characterization として適用 (adopt/revert 判定には使わない — adopt 判断は Rs へ)、目的 = P2a 有界性 (= Rs A 問「絶対表現は閉ループで積分 drift を殺すか」の直接実測) + seat-contraction の sim 発現 + B2-DR の coverage 必要箇所同定。**P4′ (再登録、v2.2 pin 履行、δ=1.586-1.885mm):** cable↔C1 @pin frame ∈ 1.68 ± 1.9mm = 予測帯 [−0.2, +3.6]mm → **fire (≤0.5mm) = EXPECTED-side** (anomaly ではない) — fire 時は C1_held + video で通常検証し characterization として報告 (banking = Rs)。CP4 (runner build + smoke + rule-check stage1) は CP5′ の前提として続行。
  - **convergence-extension 条項 (v2.2 追補、%12 決裁 2026-07-02 18:5x — CP2 実測起点):** pinned 100 epochs は delta-tuned。CP2 実測: abs val は ep100 でも下降継続 (last-10 slope −0.00132、min@ep96、elapsed 1.3s)。**OG-a が verdict-critical 行で STOP し、かつ曲線が下降継続の場合に限り**、seed/batch/lr を pin したまま **epochs=2000 の 1 回限り延長再学習** (`policy_abs_e2000.pt` + sidecar) → OG 全 leg 再実行を許可 (offline・GPU-0、数秒)。**iterative tuning 禁止 (1 回限り)** — ep2000 でも STOP = underfit 説明の棄却 = fork-invalid 側の証拠として **HOLD → %12**。CP5 は OG 通過 ckpt を使用し verdict json に記録。⚠ raw MSE の repr 間比較は無意味 (label 分散が異なる: delta ≈0 中心 vs abs [−1,1] per-phase) — 忠実度判定は **OG-a decode-mm のみ**。
  - **B1′ rollouts:** **N=5 = nominal ×2 (byte-identity assert = 決定性再確認) + EE-seed jitter ±1mm ×3 (乱数 seed 記録)** — §8-1 の N=5 形を維持しつつ 4/5 を情報化 (B1 で nominal は bit-決定的と実証済; rate は nominal/jitter 分離報告 — CC2-CH6/CC5-C5-4 統合)。**MUJOCO_GL=egl + DISPLAY unset を binding** (B1 X11 BadWindow abort 教訓 — CC5-C5-6)。学習 = 同 hyper (epochs 100, batch 256, lr 1e-3) + seed pin。
  - **honesty (over-claim 禁止、v1 継承):** n=1 demo では (iv) も trajectory-replay-in-disguise (waypoint は cable 逸脱に無応答 — data 制約は (i)/(iv) 共通)。fork の目的 = 積分病理の除去 + B2-DR/DAPG が乗れる表現 (cable-conditioned waypoints は B2 multi-demo で初学習)。B1′ を deploy 能力の主張に使わない。**DAPG-on-abs は将来 RL env 側が同 decode/apply を採る gated 変更が条件 (CC6-R6)。**
  - **forced-choice + KNOWN_ALTERNATIVES (CC6-R2/R3):** Rs 承認集合「closed-loop 補正 / absolute 系」のうち **naive corrective は n=1 で fork-(i) と完全同一** (on-demo labels も apply rule も一致 — CC6 導出検証) = 空虚 → **absolute-target が唯一の非自明・非合成データメンバー (必然選択)**。代替: augmented corrective (synthetic restoring labels — post-grasp EE/seg obs 不整合 + 新 perturbation 設計面で棄却; AC-env native 相対性は DAPG 適合で将来再訪可) / (iv-c) anchor+residual (multi-demo で anchor 不定義、policy 装飾化) / (iv-d) ACT-chunks (6D bc_pretrain 契約破壊) / (iv-f) DAPG 直行 (Rs C 非選択; repr は DAPG の上流課題)。
  - **cost/governance:** B1′ ≈ **2-2.5h 級** (B1 実測: train 2s + rollout 15-16min×5) ≪ 10h gate → production-launch-gate 不要。**B 全体 aggregate ~17h → ~19-20h に更新** (§1 anti-slicing 遵守 — CC4-CH6)。**L-TRIAGE: E15 は §7 の既存 L3 chain に乗る** (v1 の L2 label 撤回 — evaluator は solver/phase/success CORE + §0 keyword に new-file 免除なし; 3体 concur)。実 diff に `/rule-check stage1`、実装後 **層2 post-debate + 層5** (3-file 直交トリガー)。**DESIGN-GATE = skip を記録** (reward/success/obs 不変; 本 errata の decode-vs-bar 表 = reachability artifact — CC4-CH9)。`B0_BUILD_REPORT.md:161` の「residual levers = B2 schema-v2 items only」parking を **本 E15 が supersede** (Rs fork-A 承認 — CC4-CH8)。INVARIANT 不変 (dual-arm 6D position / ori 定数 / コ / span / no-kinematic-trick; guard-2 は運動 authority を増やさず減らすのみ)。gates: 5体 pre-debate ✅ (R1) → `/pre-check` (train 前必須) → %11 charter。

*INVARIANTS untouched; ANTI-REVERT markers untouched (content-identical at 4385/4401/4500 in `fd005ab83f`); env7 python. Recorder = COMMITTED `fd005ab83f`; new B files stay 0-commit until Rs approves build results. v1/v2 in this file's edit history; debate record = `B_BC_BUILD_SPEC_DEBATE_R1.md`.*
