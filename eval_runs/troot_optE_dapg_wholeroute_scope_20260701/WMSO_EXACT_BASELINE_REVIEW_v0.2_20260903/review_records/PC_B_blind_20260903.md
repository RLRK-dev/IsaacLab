# PC_B_blind — REVIEWER B（axis B: runtime safety / authority・adversarial）blind review report

- 対象（primary）: `review/pc/05_pc.md`（739 行・全行読了）／（secondary）`review/pc/06_pc.md` §4–§5（:256-277）+ §5 が写す型（:141-146 `RuntimeTimeouts`・:114 `SafetyRestrictionSet`・:169-175 `EscalationPolicy`・:228 / :234 単調性表）
- checklist: `review/briefs/AXIS_B_RUNTIME.md` B1–B10（binding）。共通前提 = `08_INDEPENDENT_REVIEW_PLAN_20260903.md` §1。
- 作成時刻（date-THEN-write）: 2026-09-03 17:04:53 UTC
- `$D` = `/home/user/IsaacLab/eval_runs/troot_optE_dapg_wholeroute_scope_20260701`。`05:` = `review/pc/05_pc.md` の行、`06:` = `review/pc/06_pc.md` の行。全 `file:line` は本 session で `sed -n` / `grep -n` により再確認済み（label でなく内容を引く）。
- 他 reviewer の出力は一切読んでいない（`review/pc/KEY.md` も未読）。

## 0. 前提の再測定

### 0.1 pin 再測定（common premise (1)）

`bash $D/WMSO_EXACT_BASELINE_REVIEW_v0.2_20260903/verify_exact_baseline_pins.sh` → **11/11 PASS**（本 session 実測）:

```
PASS  D1.1-A DESIGN v2.11.2 (worktree)  00192d20ca00b654cf6cfdb9d04b03ca14adfd0b93f2105c0fea28c295ff8aff
PASS  EvidencePolicy v1.9 md (worktree)  c474acea7c58acc22050c2ad9944fd45a18f5c76967964b42d11922e28fa27e7
PASS  EvidencePolicy v1.9 json (worktree)  e63176af9bc3a246b1c32db369ec59f8d09a4c96c381bb03a3a6024bd9811c6e
PASS  D1.1-B DESIGN v13 (worktree)  5a1874d3be8b98b8aeaace73890d8021cbc7f9814e048741f7f58d6746f349a6
PASS  D1.1-A DESIGN git blob id (HEAD)  1353430228a90ad36dd190a9359cd81da52e8242
PASS  D1.1-B DESIGN git blob id (HEAD)  ebe8154abf2461a6b24db05728b20e5cee949fb2
PASS  D1.1-A DESIGN sha256 of blob 135343…  00192d20…
PASS  D1.1-B DESIGN sha256 of blob ebe815…  5a1874d3…
PASS  D1.1-A DESIGN at frozen commit 54f90a7d  00192d20…
PASS  D1.1-B DESIGN at frozen commit 07250f4a  5a1874d3…
PASS  evidence_policy_definition_hash (embedded command)  e7ca43093084c167a209b008533a66d26a1fd3223d2a3c11274d28306c3ff803
policy_semver=1.9.0
HEAD=17f04fd6a8f5742a64ecfa70a0d614bbecd8dd43 branch=claude/v0-2-design-candidates-snqbmy
```

### 0.2 機械検査（common premise (3)）

- `check_review_candidate.py review/pc/05_pc.md --runtime` → `citations=138 FAIL=0 WARN=6`（WARN は全て C2 heuristic: slice prereg `:59` / contracts_v2 `:151` `:380` 近傍に `InterruptOutcome` / `FreshnessPolicy.max_staleness_s = None` が無い、という語彙 miss。05:738 anchor 23 の自己申告と一致）
- `check_review_candidate.py review/pc/06_pc.md` → `citations=55 FAIL=0 WARN=19`（C2 heuristic のみ）

### 0.3 本 review が依拠した凍結側の内容（sed で確認した逐語）

| 凍結 locus | 内容（要点） |
|---|---|
| contracts_v2 `:354` | 「closed-loop 実運転権限は本 API の granted == True を必要条件とする（十分条件ではない）」 |
| contracts_v2 `:381` | `offer.control_epoch == authority_epoch_snapshot`（等値検査のみ・不一致 = `E_HANDOFF_EPOCH_STALE`） |
| contracts_v2 `:382` | 「epoch の CAS 更新・新 epoch 発行・**旧 epoch command 拒否**・使用済み offer 拒否（`handoff_offer_id` 単位）・snapshot 読出し = authority manager（O0 層）の状態所有責務」 |
| contracts_v2 `:498` | `handoff_offer_id` = 「authority manager の使用済み拒否（replay 防止）単位」 |
| D0 `:270-272` | 「one atomic token flip … **exactly one owner at every instant — no double-owner interval and no owner-gap interval**」 |
| D0 `:273-274` | 「fail-closed: reject / abort / timeout → producer retains ownership and **safe-stops (or re-observes)** — control is never released into a vacuum and never duplicated」 |
| D0 `:318-320` | heartbeat missed ⇒ consumers must not assume safety alive; monitor `failed` ⇒ safe-stop ／ preemption acts first, does not wait for ack ／ `stabilized_post_action_state` = lets §D reason about **resumption eligibility** |
| EP md `:155` / `:156` | 「SHADOW は定義上 非 authority」／「closed-loop authority は O0/S0/V0 two-key + 独立安全 gate を必ず conjoin」 |
| charter `:129` | S0 Shadow mode = 「zero control authority」 |
| Rs C3 `:32-34` / `:63` | slice = SHADOW（rank 2・非 authority）／ closed-loop authority を解錠しない |
| D1.1-B `:40` / `:125-127` | `ActionHold = ZERO_ORDER_HOLD \| LINEAR_INTERPOLATE`／`ActionTimingSpec{action_rate_hz, hold}` = 「policy step 間の保持規約」（訓練時束縛・hash-visible） |
| slice prereg `:59` | boundary = TERMINAL outcome のみ・checkpoint 途中切替 = Phase G / 別 gate |

---

## 1. Findings（severity 順・全件に doc:line + frozen file:line + 再現 + minimal fix（schema delta 0））

### PC-B-01 ｜ CRITICAL ｜ B1 / B4 ｜ 旧 epoch 拒否が CAS でなく HealthConfirmation まで遅延 → 二重所有窓が復活

- **candidate**: 05:231 「**旧 epoch 拒否の予約**: CommandGateway の admission は `AuthorityState` を線形化して読むが、旧 epoch の拒否は新 lease の `HealthConfirmation` 受領（`HEALTH_CONFIRMED` 記録）の線形化点から有効化する」／ 05:262 遷移表「HEALTH_CONFIRMED（この線形化点で旧 epoch 拒否が有効化・旧 lease の command は以後 R_EPOCH_STALE_COMMAND）」
- **同一 doc 内の矛盾**: 05:32 「旧 epoch 拒否（同一線形化点）」／ 05:296 証明 B 「旧 lease の command は線形化点以降 admit されない」／ 05:294 証明 A（admission = `(cmd.lease_id, cmd.control_epoch) == (state.active_lease_id, state.control_epoch)` を CAS 線形化点で要求）／ 05:611 INV-03 ／ 05:641 T-06 「CAS 直後に旧 lease の command を投入 → R_EPOCH_STALE_COMMAND」／ 05:701 fold-map 訂正 4 「旧 epoch 拒否は同一 CAS で有効（post-transfer ACK に遅延しない）」
- **frozen / 裁定との矛盾**: handoff 決定 C.4（「Old-epoch rejection takes effect at the same CAS, not deferred until a post-transfer ACK」）／ contracts_v2 `:382`（旧 epoch command 拒否は manager の状態遷移責務 — validator と二層で fail-closed）／ D0 `:270-272`（no double-owner interval）
- **再現（spec の state 名で）**:
  1. `S_LEASE_ACTIVE(L_old, epoch e)`。producer が `SkillOutcome(TERMINAL)` → `S_BOUNDARY_WAIT`。
  2. ReadinessAck(E_new) → CommitPermit → `AuthorityCas(TRANSFER_TO_EXECUTOR)` SUCCESS → state = `(EXECUTOR(E_new), e+1, L_new, …)` → `S_HEALTH_PENDING`。
  3. 区間 `[t_CAS, t_HEALTH_CONFIRMED)`（上限 `health_confirm_timeout_s`）で、E_old（遅延 queue / bug）が `c_old(L_old, e)` を送る。05:231 により拒否は未有効 ⇒ **admit**。
  4. 同区間で E_new が `c_new(L_new, e+1)` を送る。05:209 `first_command_admitted`（CLOSED_LOOP）は「最初の admissible command が gateway で admit された」を HealthConfirmation の前提とするため **admit**。
  5. ⇒ 同一 `AuthorityState` の下で 2 lease の command が admit（P1 05:111 の否定）。HealthConfirmation が timeout すれば `TRANSFER_TO_SAFEHOLD(HEALTH_FAIL)` だが、その前に旧 owner が actuation を駆動済 = 「旧 owner を silent に復活させない」（B4）の否定。
- **minimal fix（runtime 層のみ）**: 05:231 を 「旧 epoch 拒否は CAS の線形化点から有効（INV-03）。線形化点以降 `control_epoch ≠ expected.control_epoch + 1` の command は `R_EPOCH_STALE_COMMAND`、`lease_id ≠ new_lease_id` は `R_LEASE_UNKNOWN_COMMAND`」に置換。05:262 の備考を「HEALTH_CONFIRMED（旧 epoch 拒否は CAS 時点で既に有効。本線形化点は `S_LEASE_ACTIVE` への射影のみ）」に修正。

### PC-B-02 ｜ HIGH ｜ B2 / B10 ｜ CAS 後も「旧 lease の直近 admitted command」を出力し続ける規則 — SafeHold-at-CAS を破り、無効化済 lease の command が actuation を駆動

- **candidate**: 05:232 「**SafeHold 活性**: gateway 出力は新 lease の最初の admissible command が admit されるまで、**旧 lease の直近 admitted command（hold 地平内）があればそれを**、無ければ `SafeHold` とする」
- **同一 doc 内の矛盾**: 05:32 「SafeHold 活性（最初の安全 command）」／ 05:296 証明 B 「CAS の線形化点では (ii) の admitted command 集合が空にリセットされ (iii) に落ちる」／ 05:436 「CAS の線形化点で (ii) の集合は空にされる」／ 05:112 P2 の出力集合 = {活性 lease の admitted command, SafeHold, SafeStop, safety} — 無効化済 lease の command はこの集合に無い／ 05:641 T-06 期待「gateway 出力 = SafeHold（新 lease の最初の command まで）」
- **frozen / 裁定との矛盾**: handoff 決定 C の v0.2 順序 「atomic CAS … └ first safe command/HOLD activation」／ D1.1-B `:127` `hold` = 「policy step 間の保持規約」— `LINEAR_INTERPOLATE`（`:40`）は次 command への補間であり、後続 command を出せない無効化済 lease の command を gateway が「hold 地平内」として再生する意味論は D1.1-B に無い（B10）
- **再現**: `L_old` の最後の admitted command `c_k`（`hold = LINEAR_INTERPOLATE`、目標 `x_k` へ補間中）。CAS 成功後、E_new の最初の command まで最長 `health_confirm_timeout_s`。05:232 により gateway は `c_k` を出力し続ける ⇒ 無効化済 lease の運動要求で robot が動く。SafeHold（05:420: 直近 admitted EE target を保持・速度 0）とは異なり「速度 0」が保証されない。
- **minimal fix**: 05:232 を 「CAS の線形化点で admitted command 集合を空にし、新 lease の最初の admissible command が admit されるまで出力は (iii) `SafeHold`（§5.1: 直近 admitted EE target を目標・速度 0）」に置換（姿勢保持は SafeHold の定義 05:420 で既に担保される）。

### PC-B-03 ｜ CRITICAL ｜ B5 ｜ SHADOW_NON_AUTHORITY lease が「最初の admissible command」を actuation port へ物理出力する

- **candidate**: 05:400 「gateway actuation | **DISABLED（初回 admit を除く）** — 最初の admissible command のみ actuation port へ出力し（`HealthConfirmation.first_command_admitted` を実 readback で確認するため）、以後の command は admission 判定・記録のみ」
- **同一 doc 内の矛盾**: 05:424 「SHADOW_NON_AUTHORITY では actuation port が DISABLED なので … 物理 command は出ない」／ 05:290 「SHADOW lease から actuation 要求 → 記録のみ・出力なし」／ 05:431-432 `out(t)` (ii) は `mode == CLOSED_LOOP_AUTHORITY` を要求（SHADOW は (iii) にしか落ちない）／ 05:617 INV-09 「actuation port から当該 lease 由来の command が **0 件**」／ 05:646 T-11 「actuation port 出力 0 件」／ 05:728 anchor 12 「SHADOW lease は actuation を出さず」／ 05:209 `first_command_admitted` SHADOW = 「記録された」（readback 不要）／ 05:82 「SHADOW lease の actuation port = DISABLED」
- **frozen / 裁定との矛盾**: EP md `:155` 「SHADOW は定義上 非 authority」／ EP md `:156` closed-loop authority = two-key + 独立安全 gate の conjoin／ contracts_v2 `:354` `granted == True` 必要条件（SHADOW lease は 05:401 で `authority_decision_ref` 任意）／ charter `:129` S0 「zero control authority」／ Rs C3 `:32-34`・`:63`（closed-loop authority 解錠なし）／ VOCABULARY 「SHADOW lease cannot emit commands」
- **悪化要因（06）**: 06:114 `requires_safety_layer_health: bool = True # SHADOW_NON_AUTHORITY lease のみ False を許容（actuation port DISABLED ゆえ）` — 05:400 を残すと「safety layer health を要求しない SHADOW lease から物理 command が 1 件出る」構成が profile として受理される（06:235 も同旨）。なお 05:225 CAS 条件 9 は mode を問わず safety health を要求するので 06:114 は 05 より弱く、整合上も要修正（LOW）。
- **再現**: slice（Rs C3 = SHADOW）で `AuthorityDecision` 無しの SHADOW lease → CAS 成功 → policy の最初の action が `AcceptedEnvelope` を通る → actuation port 出力 ⇒ LEARNED policy が authority grant 無しで robot を動かす。INV-09 の計数 = 1 ≠ 0。
- **minimal fix**: 05:400 を 「**DISABLED（例外なし）** — 全 command は admission 判定 + `SHADOW_COMMAND_RECORDED` のみ。`HealthConfirmation.first_command_admitted`（SHADOW）= 最初の admissible command が記録された（05:209 のとおり・readback 不要）」に置換。06:114 の注記を「SHADOW でも 05 CAS 条件 9 は適用（False は health_checks 段のみ緩和）」に揃える。

### PC-B-04 ｜ HIGH ｜ B4 / B7 ｜ safety override の TRANSFER_TO_SAFEHOLD が CONFLICT で着地せず、safety 解除後に executor lease が無審査で再開する経路

- **candidate**: 05:235 「TRANSFER_TO_SAFEHOLD の前提条件 = 条件 1 のみ（`read_current() == cas.expected`）」／ 05:269 「any | IndependentSafetyLayer STOP / HOLD | TRANSFER_TO_SAFEHOLD(SAFETY)」／ 05:288 「in-flight CAS は線形化順で『先に成功 → 直後の safety CAS で無効化』か『後 → CONFLICT』のいずれか」／ 05:281 「CAS conflict → 変化なし・permit VOID → 現状態に従う（… EXECUTOR なら継続）」／ 再試行規則: **無し**（`grep -nE '再試行|retry'` = 05:180 のみ・別件）
- **frozen**: D0 `:319` preemption acts first（出力側は 05:431 (i) で満たす）／ D0 `:320` `stabilized_post_action_state` = 「resumption eligibility」を §D が判断する材料 — 05:448 も「SAFETY の解除 = clearance record + 完全経路（ACK → permit → CAS）」と規定
- **再現**:
  1. `s0 = (EXECUTOR L_old, e)`。t1: transfer CAS(expected = s0) SUCCESS → `s1 = (EXECUTOR L_new, e+1)`。
  2. t1+ε: `SafetyDecision(STOP)` → `out(t)` = (i)。manager が 05:269 に従い `TRANSFER_TO_SAFEHOLD(SAFETY, expected = s0)`（t1 前に読んだ snapshot）を発行 → 条件 1 不成立 → **CONFLICT** → 05:281 「EXECUTOR なら継続」。再試行規則が無いため `AuthorityState` は `s1` のまま（`S_SAFEHOLD(SAFETY)` に入らない）。
  3. IndependentSafetyLayer が自らの決定を解除（`stabilized_post_action_state`）。(i) 消滅 → (ii): `owner == EXECUTOR(L_new) ∧ CLOSED_LOOP ∧ hold 地平内 admitted command`（または E_new の新 command が INV-03 を満たし admit）⇒ **actuation 再開**。05:448 の clearance record・CAS 条件 10（05:226）・§5.4 の「runtime は自ら解除しない」（05:453）が全て bypass。
  4. 同区間で `HealthConfirmation` が全 True になれば `S_LEASE_ACTIVE` へ昇格し、`FAULT(R_SAFETY_OVERRIDE)` は記録されるが状態には何も残らない。
- **minimal fix**: §3.5 に 「TRANSFER_TO_SAFEHOLD は manager が `read_current()` を expected として発行し、CONFLICT なら再読して SUCCESS まで再発行する（safety-class: SAFETY / EXECUTOR_LOST / DEADLINE_MISS / ENVELOPE_VIOLATION / MANAGER_RESTART）」を追加。INV-18（05:626）を 「SafetyDecision 受領の線形化点以降、`TRANSFER_TO_SAFEHOLD(SAFETY)` SUCCESS まで gateway は (ii) を評価しない」に拡張。05:288 の「後 → CONFLICT」を「後 → CONFLICT → 再発行で SUCCESS」に修正。

### PC-B-05 ｜ HIGH ｜ B2 / B6 ｜ `S_BOUNDARY_WAIT` の「gateway = SafeHold」が admission 規則で執行されず、TERMINAL 後の producer command が admit される

- **candidate**: 05:254 「`S_BOUNDARY_WAIT` | `S_LEASE_ACTIVE` のまま producer の `SkillOutcome` 受領済（lease は活性・**gateway = SafeHold**）」／ 05:441 「producer の `SkillOutcome` 受領後も lease は活性のまま、**executor は新しい運動 command を出さず** gateway 出力は (iii) SafeHold」
- **執行規則の不在**: admission = 05:82 「`(lease_id, control_epoch)` 等値 ∧ `AcceptedEnvelope` 連言 ∧ command 種別」／ 05:611 INV-03 ／ 05:431 (ii) 「owner == EXECUTOR ∧ CLOSED_LOOP ∧ hold 地平内の admitted command あり → その command」。outcome 受領後の command を拒否する規則は無い（`grep -nE 'R_POST_OUTCOME|OUTCOME_RECEIVED'` = 0 件）。「executor は … 出さず」は executor への期待であり、「actuation への唯一経路」（05:82）である gateway の規則ではない。
- **frozen**: D0 `:273-274` 「producer retains ownership and **safe-stops** (or re-observes)」／ handoff 決定 D 「re-selection / re-initialization from current state」= §6.2 B2（05:471）の `BeliefRef` が物理状態を代表することが前提
- **再現**: t0: E が `SkillOutcome(TERMINAL SUCCESS)` → `S_BOUNDARY_WAIT`。t1: Orchestrator B0–B2 が `BeliefRef` を取り `validate_invocation_start(…, now)` を通す。t2 > t1: E（遅延 queue / bug）が `c(L, e)` を送る → INV-03 成立・envelope 通過 ⇒ **admit** ⇒ `out` = (ii) ⇒ robot が動く。新 lease の `InitializationRecord.start_belief_ref` はもはや物理状態を代表しない。「gateway = SafeHold」（05:254）は成立しない。
- **minimal fix（runtime 層）**: gateway が `SkillOutcome` 受領（ASSESSMENT 記録）時に `boundary_reached[lease_id] := True` を持ち、admission に `¬boundary_reached[cmd.lease_id]` を連言追加。違反 = 新 runtime code `R_POST_OUTCOME_COMMAND`（拒否のみ・状態不変・`COMMAND_REJECTED` 記録）。`RuntimeFaultCode` は runtime 層 enum ゆえ frozen schema delta 0。T-xx を追加。

### PC-B-06 ｜ HIGH ｜ B10 / B5 ｜ 06 が `action_rate_hz` の「引下げ（p ≤）」を許し、05 R5 / INV-12 / T-14 の等値束縛と正面衝突

- **candidate（06）**: 06:228 「`action_rate_hz` のみ **stricter-or-equal**（`p ≤` TensorBindingSpec の値 — controller が支える周期まで下げてよい）… `P_RATE_MISMATCH`（`action_rate_hz` は `p >` のとき）」／ 06:274（§5）「lease へ写される profile 由来の値 …」— この `BindingExpectation` が 05 `TimingBinding` の入力
- **candidate（05）**: 05:375 「`action_rate_hz` / `hold` は D1.1-B の訓練時束縛事実 … executor は**ちょうどその値**で走る。profile が『narrow』できるのは運動学的限界（速度/加速度/力）と鮮度だけ」／ 05:313-317 `TimingBinding` 各 field 「等値必須」／ 05:620 INV-12 等値 → `R_PROFILE_MISBOUND`／ 05:649 T-14 「action_rate_hz を profile で『narrow』した lease 提案 → R_PROFILE_MISBOUND（等値のみ許容）」／ VOCABULARY `ControllerEnvelope`（「NOT control frequency」）
- **frozen**: D1.1-B `:125-127` `ActionTimingSpec{action_rate_hz, hold}` = 訓練時束縛・hash-visible（`:374` U-1 と同じく binding は静的 artifact）。critic 07 X3 も「R1/R5 govern — equality」と結論。
- **再現**: profile が `BindingExpectation.action_rate_hz = 24`（binding は 48）を宣言 → 06 は受理（`p ≤`）。(a) 05 に従う実装: 全 lease 提案が INV-12 で `R_PROFILE_MISBOUND` → その profile では lease が一つも成立しない（doc 間 deadlock）。(b) 06 に従う実装: certified policy が半分の周期で走り、ZOH で各 action の保持時間が 2 倍 — 認証外の挙動で authority を行使。
- **minimal fix**: 06:228 の `action_rate_hz` 例外句を削除して等値（`P_RATE_MISMATCH` = `p ≠`）に統一、06 版歴に記録。05 側は変更不要。（scope 注: 06 §3 の行だが 05 §4.2 / `TimingBinding`（本 axis）の直接入力ゆえ本 axis で報告。）

### PC-B-07 ｜ MEDIUM ｜ B8 / B7 ｜ 06 `EscalationPolicy` が 05 §7 既定 `SAFE_STOP` を `HOLD` へ緩和できる — 「SAFE_STOP 側へのみ強化可」の否定

- **candidate（06）**: 06:172 「`on_invalid_state_unresolved: EscalationTarget # 05 §7 既定 = SAFE_STOP。clearance_roles が整備された cell では HOLD を選択可`」／ 06:234 「HOLD ↔ SAFE_STOP の選択（05 既定 SAFE_STOP の経路も、`clearance_roles` が整備された cell では HOLD に留めてよい）」
- **candidate（05）**: 05:560 「outcome × disposition（… **profile は SAFE_STOP 側へのみ強化可**）」／ 05:569 「Terminal INVALID_STATE | 解消せず | SAFE_STOP」／ handoff 決定 C.7 「A deployment profile may only NARROW frozen semantics (monotone strengthening)」
- **再現**: profile が `on_invalid_state_unresolved = HOLD` を宣言 → 06 の P_ 検査に該当 code 無し（06 §8.1 に `P_ESCALATION_RELAXED` 相当は無い）→ 受理。runtime で `Terminal INVALID_STATE` 未解消 → 05:569 の `SAFE_STOP`（`SafeStop` = SafeHold + stop-class 要求 05:424）でなく `HOLD` に留まる = 既定より弱い終端。
- **minimal fix**: 06:172 を 「既定 = SAFE_STOP。HOLD への緩和は不可（`P_ESCALATION_RELAXED`）」、06:234 を 「SAFE_STOP 側へのみ」に修正。cell 側で HOLD を許したいなら 05 §7 既定の変更（Rs 裁定）であり profile の権限ではない — open として記録。

### PC-B-08 ｜ MEDIUM ｜ B3 / B9 ｜ permit の `CONSUMED` が「失効時」に設定され、CAS 成功後も `UNUSED` — one-shot 証明・fault 分類・audit と矛盾

- **candidate**: 05:180 「`CONSUMED` は `expires_at` 到達時に設定される（失効前の permit は CAS 成功後も `UNUSED` のまま残り、同一 `expected` への再試行に再提示できる）」／ 05:612 INV-04 「`UNUSED→CONSUMED`（`expires_at` 到達時）」
- **同一 doc 内の矛盾**: 05:230 事後条件 「`permit.state := CONSUMED`」／ 05:300 証明 D 「`permit.state` は `UNUSED → CONSUMED` へ CAS 成功の線形化点でのみ遷移」／ 05:279 「permit 再使用 → `R_PERMIT_REUSED` → `HOLD`（異常経路・audit 必須）」／ 05:637 T-02 ／ 05:581 `PERMIT_CONSUMED` audit kind ／ 05:600 記録義務「全 permit 発行/消費/失効」／ 05:724 anchor 8
- **再現**: permit `P(expected = s0)` → CAS SUCCESS → `s1`。05:180 では `P` は `UNUSED` のまま ⇒ `PERMIT_CONSUMED` 記録が発生しない（INV-17 欠落）。`P` を replay ⇒ 条件 1（`s1 ≠ s0`）が先に偽 ⇒ `CONFLICT` ⇒ `R_CAS_CONFLICT` ⇒ 05:281 「EXECUTOR なら継続」。05:279 の `R_PERMIT_REUSED → HOLD + audit` は到達不能（条件の評価順・fault 優先規則も未定義: `grep 'fault の優先|優先順位.*fault'` = 0 件）。「同一 expected への再試行」は `seq` 単調（05:136）ゆえ SUCCESS 後に再現不能、CONFLICT 後は 05:281 で VOID — 動機自体が空。
- **minimal fix**: 05:180 を 「`UNUSED → CONSUMED`（CAS SUCCESS の線形化点）／ `UNUSED → VOID`（`expires_at` 到達 = VOID(EXPIRED)・CONFLICT・REJECTED・安全介入・TRANSFER_TO_SAFEHOLD）」に、INV-04 を同様に修正。§3.5 に fault 優先を追加: 「条件 2 の permit 状態を条件 1 より先に評価し、CONSUMED = `R_PERMIT_REUSED`・失効 = `R_PERMIT_EXPIRED`」。

### PC-B-09 ｜ MEDIUM ｜ B4 / B2 ｜ CommandGateway 再起動が crash matrix に無い（named disposition 不在・出力未定義区間）

- **candidate**: 05:122 `SafeHoldReason`（GATEWAY_RESTART 無し）／ 05:261-270 遷移表（manager 再起動 :270・executor 喪失 :267 のみ）／ 05:274-290 失敗表 ／ 05:541-551 `RuntimeFaultCode`（`R_GATEWAY_*` 無し）／ `grep -nE 'gateway.*再起動|CommandGateway.*再起動|GATEWAY_RESTART|R_GATEWAY'` = 0 件。gateway の状態 05:82 「`AuthorityState` 読み + 直近 admitted command」は揮発。
- **frozen**: D0 `:272` 「no owner-gap interval」／ checklist B4 が明示列挙するケース
- **再現**: `S_LEASE_ACTIVE(L, CLOSED_LOOP)`。t0 gateway crash、t1 restart。(a) `[t0, t1]`: `SafeHold` は「gateway が**自ら**出す command であって『信号なし』ではない」（05:416）ため、誰も SafeHold を出さない ⇒ P2（05:112）の出力集合のどれでもない区間。(b) t1: gateway が `AuthorityState` を再読（`manager_liveness_s` 内で新鮮）→ `owner == EXECUTOR(L)` ⇒ `L` の command を無審査で再 admit（HealthConfirmation 再実行なし・失われた「直近 admitted command」の代替なし）。
- **minimal fix**: `SafeHoldReason.GATEWAY_RESTART` + `R_GATEWAY_RESTART`（runtime enum・schema delta 0）を追加。§3.7 に行 「CommandGateway 再起動 → 起動直後は (iii) のみ・manager へ通知 → `TRANSFER_TO_SAFEHOLD(GATEWAY_RESTART)` → `HOLD`」。gateway 沈黙時の actuator 側挙動（06:245 `CONTROLLER_LIVENESS` health check / `ControllerEnvelope` stop-class）は解けないので OP-17 として open に列挙（主張しない）。

### PC-B-10 ｜ MEDIUM ｜ B8 ｜ fault → disposition の写像が全域でない（rule 2 「fault は常に disposition を伴う」の自己違反）

- **candidate**: 05:557 「fault は常に disposition を伴う（fault だけで終わる経路は無い）」に対し、05:274-290 失敗表・05:261-270 遷移表のどこにも行を持たない `RuntimeFaultCode` member:
  - `R_COMMAND_KIND_MISMATCH`（05:548; 検査は 05:82 のみ）— 拒否のみか lease 無効化か未定義
  - `R_LEASE_UNKNOWN_COMMAND`（05:544; 05:231 のみ）— 同上
  - `R_AUTHORITY_DECISION_MISBOUND`（05:549; CAS 条件 6 後半 05:222）— disposition 無し
  - `R_PROFILE_MISBOUND`（05:549; INV-12 / INV-22 の「違反時」列のみ）— disposition 無し
  - `R_MANAGER_UNAVAILABLE`（05:550; INV-19 05:627 のみ）— disposition 無し
  - `R_TIMING_VIOLATION`（05:547; 05:378 は `HOLD` のみ）— 05:410 §4.5 は「timing 違反」を lease 無効化 trigger に列挙するが、遷移表 05:268 は DEADLINE_MISS / ENVELOPE_VIOLATION しか無い（`SafeHoldReason` にも TIMING 無し）
  - `R_NO_CANDIDATE`（05:551; 05:532 「候補から除外し記録」）— disposition 無し
- **再現**: E_new が DIFF_IK lease の下で SCRIPTED 種別の command を送る → `R_COMMAND_KIND_MISMATCH`。実装者は「拒否のみ（executor は lease を保持し送り続ける）」か「`TRANSFER_TO_SAFEHOLD`」かを spec から決められない（05:268 では ENVELOPE_VIOLATION は無効化）。fail-closed 既定が文面で決まっていない。
- **minimal fix**: 05:274-290 に上記 7 code の行（検出者 / 状態への効果 / disposition）を追加。gateway 拒否系（`R_EPOCH_STALE_COMMAND` / `R_LEASE_UNKNOWN_COMMAND` / `R_COMMAND_KIND_MISMATCH`）は 「拒否のみ・状態不変・`COMMAND_REJECTED` 記録」と明記。`R_TIMING_VIOLATION` を 05:268 の行に加え `SafeHoldReason.TIMING_VIOLATION` を追加（runtime enum）。

### PC-B-11 ｜ MEDIUM ｜ B10 / B3 / B8 ｜ `profile.ack_validity_s` が 05 `TimingBinding` にも 06 `RuntimeTimeouts` にも無い（INV-22 不成立）＋ CAS 時の ACK 失効に fault code が無い

- **candidate**: 05:156 「`valid_until: float # = t_ack + profile.ack_validity_s`」／ 05:313-325 `TimingBinding`（`ack_timeout_s` / `permit_ttl_s` / `health_confirm_timeout_s` / `command_deadline_s` / `manager_liveness_s` のみ）／ 06:141-146 `RuntimeTimeouts`（同 5 個のみ）／ 06:274 §5 の写し元列挙にも無い／ 05:630 INV-22 「全 timeout（ack / permit / health / command / manager liveness）は `profile_hash` 由来で lease に写され」
- **再現**: `ReadinessAck.valid_until` の算出に必要な `ack_validity_s` が profile に存在しない ⇒ CAS 条件 3（05:219 `now ≤ ack.valid_until`）は「評価不能 = FALSE」（05:216）⇒ 書面どおりでは転送が一件も成功しない。実装者が数値を hard-code すれば INV-22 違反（B10 の「hard-coded numbers 禁止」）。さらに条件 3 が ACK 失効で偽になった場合の fault code が無い: 05:276 `R_ACK_TIMEOUT` = 「ACK が届かない」、05:277 `R_ACK_MISBOUND` = 「束縛不一致（epoch/offer/invocation）」のいずれにも該当せず、INV-07（05:615）は `valid_until ≥ permit.issued_at` しか検査しない（条件 3 より弱い）。
- **minimal fix**: 05 `TimingBinding` と 06 `RuntimeTimeouts` に `ack_validity_s: CanonicalDecimal` を追加（runtime / profile 層・schema delta 0）、06:274 の写し列挙に追加、06:243 `P_TIMEOUT_NONPOSITIVE` の対象に含める。05:277 `R_ACK_MISBOUND` の定義に「ACK 失効（`now > ack.valid_until`）」を含める（または `R_ACK_EXPIRED` を追加）。

### PC-B-12 ｜ LOW ｜ B4 / B3 ｜ 「記録が無い初回のみ epoch 0」が durable store 喪失と区別不能

- **candidate**: 05:140 「`e0` は durable 記録から復元（**記録が無い初回のみ 0**）」 vs 05:287 「durable state 不読 ⇒ 起動拒否」— 「無い」と「不読」の判別規則が無い。
- **再現**: 前世で epoch 0 の executor X が `offer o(control_epoch = 0)` を発行 → store 喪失 → 再起動で `(SAFEHOLD, 0, ∅)`（`consumed_offer_ids` も `invalidated_lease_ids`（INV-11 05:619）も空）→ Orchestrator が `validate_handoff(o, snapshot = 0)` を通す → CAS が `o` を消費 → 死んだ producer の `offer.ownership` で lease が組まれる。ReadinessAck と fresh `BeliefRef` 検証で物理的には bounded なので LOW。
- **minimal fix**: 05:140 を 「durable 記録が無い場合も起動拒否。初回 bootstrap は profile / operator の attested bootstrap record（06 `DeploymentEvidenceRecord`）を要する」に。lease_id / permit_id / ack_id に boot nonce を含め INV-11 を store 喪失越しに保つ。

### PC-B-13 ｜ LOW ｜ B9 ｜ clearance record と CAS CONFLICT/REJECTED の audit kind が無い

- **candidate**: 05:579-582 `AuditKind` に `CLEARANCE_*` 無し（`grep CLEARANCE` = 0 件）。一方 05:226 CAS 条件 10・05:445-451 §5.4 は clearance record の存在を転送の前提にする。05:600 「CAS の SUCCESS/CONFLICT/REJECTED を含む」記録義務に対し、CONFLICT/REJECTED をどの kind で記録するか未指定（`EPOCH_TRANSITION` は SUCCESS のみ）。
- **minimal fix**: `AuditKind.CLEARANCE_RECORDED`（payload_ref = clearance record）を追加し、CAS CONFLICT/REJECTED は `FAULT`（payload_ref = `AuthorityCas` 記録形）で記録すると明記。

### PC-B-14 ｜ LOW ｜ B4 ｜ Orchestrator 喪失（permit 発行後）の行が無い

- **candidate**: `grep -nE 'Orchestrator.*(crash|喪失|再起動)'` = 0 件。05:278 「permit 失効 → `RESELECT`（ACK から再取得）」の actor が Orchestrator 喪失中は不在。状態は不変・permit は失効で VOID なので（PC-B-05 修正後は）安全側だが、B4 の named disposition が無い。
- **minimal fix**: §3.7 に 「Orchestrator 喪失 / 再起動 → 変化なし（現 owner 継続・permit は失効 VOID）→ 再起動後 §6.2 B0 から新規 `RuntimeAssessmentRecord` で再開」を追加。

---

## 2. Checklist ごとの PASS / FAIL（line 付き）

| id | 判定 | 根拠 |
|---|---|---|
| B1 | **FAIL**（PC-B-01） | PASS 部分: ACK が permit の前提（05:160 `CommitPermit.ack_id` 必須・05:219 条件 3）／ CAS = tuple 全体の等値（05:217）／ 旧 lease 無効化は CAS 事後条件で記録（05:233）＝旧 owner には事後通知／ 同一 `expected` の複数 permit で SUCCESS 高々 1（05:181・05:636 T-01）。FAIL: 旧 epoch 拒否が CAS 瞬間でなく HEALTH_CONFIRMED（05:231・:262） |
| B2 | **FAIL**（PC-B-02・PC-B-05・PC-B-09） | PASS 部分: 初期状態 `SAFEHOLD(INITIAL)`（05:140）／ `out(t)` 全域・排他（05:429-432・INV-10 05:618）／ manager 不達で (iii)（05:437・INV-19）。FAIL: CAS 後に旧 lease command を出力（05:232）／ `S_BOUNDARY_WAIT` の SafeHold 未執行（05:254・:441）／ gateway 再起動区間が未定義 |
| B3 | PASS-with-caveats（PC-B-08・PC-B-11） | offer 再利用 = 条件 4 + `consumed_offer_ids` tuple（05:220・05:242-243・証明 C 05:298・INV-05 05:613）／ 失効 permit = 条件 2（05:218）／ 同一 expected の重複 permit（05:181）／ stale ACK = 条件 3（05:219）。caveat: permit CONSUMED 意味論（05:180）と ACK 失効の code 不在 |
| B4 | **FAIL**（PC-B-04・PC-B-09・PC-B-14；LOW PC-B-12） | PASS 部分: 新 executor の CAS 後 crash → `R_EXECUTOR_LOST`/`R_HEALTH_CONFIRM_TIMEOUT` → SAFEHOLD → HOLD（05:285）／ manager 再起動 → `epoch := persisted + 1`・全 lease 無効（05:287・T-09 05:644）・原子性は OP-6（05:679）で honest／ safety 出力は (i) 即時（05:288）。FAIL: safety CAS の着地未保証、gateway 再起動・orchestrator 喪失の行なし |
| B5 | **FAIL**（PC-B-03；PC-B-06 は B10 と共通） | PASS 部分: 活性 lease ちょうど 1（05:133 `active_lease_id`・P1 05:111）／ profile_hash・authority_decision_ref・initialization・accepted_envelope・timing を束縛（05:360-364）／ 変更 = 新 lease（05:370）／ 無効化 trigger 列挙（05:410）／ CLOSED_LOOP は `granted == True` 必要条件 + 外部 gate（05:401・INV-08 05:616・05:659）。FAIL: SHADOW lease が 1 件物理出力（05:400） |
| B6 | PASS（caveat PC-B-05） | mid-skill 切替経路なし・`InterruptOutcome` = 受理 + HOLD（05:265・:462・:464・INV-21 05:629）／ 再選択は fresh `BeliefRef`（05:471）+ 明示 `now`（05:475）／ assessment は非 certified（05:508） |
| B7 | **FAIL**（PC-B-04） | PASS 部分: (i) 最優先・ack 待ちなし（05:431・:435）／ HOLD 解除者の表（05:445-453）／ deadline miss → `TRANSFER_TO_SAFEHOLD(DEADLINE_MISS)`（05:268・:377）、D0 §G `D_event` は OP-14（05:687）。FAIL: safety CAS 未着地 → 解除後の無審査再開 |
| B8 | **FAIL**（PC-B-10・PC-B-11；PC-B-07） | PASS 部分: `ProducerOutcome` 不変（05:556・INV-20 05:628・T-19）／ 既定 fail-closed（05:274 表頭）。FAIL: 写像が全域でない、ACK 失効 code 無し、06 で SAFE_STOP → HOLD 緩和 |
| B9 | PASS-with-caveats（PC-B-08・PC-B-13） | permit 発行/消費/VOID・lease 活性/無効・health・safety・fault/disposition・assessment・manager restart の kind（05:579-582）／ record に `control_epoch` / `lease_id`（05:590-591）／ hash = U14 defer を明記（05:602）。caveat: `PERMIT_CONSUMED` が 05:180 の意味論では発生しない、clearance kind 無し |
| B10 | **FAIL**（PC-B-06・PC-B-11） | PASS 部分: timeout は profile 由来・数値なし（05:321-325・INV-22 05:630・06:141-146）／ rate 等値（05:375・INV-12）／ hold は D1.1-B `:40`/`:127` を再生し deadline を追加するのみ（05:377）。FAIL: 06 が `action_rate_hz` 引下げを許容、`ack_validity_s` 未定義 |

## 3. Verdict

- **HOLD**。CRITICAL 2（PC-B-01・PC-B-03）+ HIGH 4（PC-B-02・PC-B-04・PC-B-05・PC-B-06）。いずれも runtime / profile 層の文面修正で閉じ、frozen 4 file・frozen enum への delta は 0。
- 特記: PC-B-01 / PC-B-02 / PC-B-03 / PC-B-08 は doc 内部で自己矛盾しており（証明 A/B/D・INV・T・fold-map が正しく、§3.4/§3.5 事後条件/§4.4 の 1 文ずつが誤り）、fold は「誤った側の 1 文の置換」で済む。
- 本 review は gate PASS / two-key / Rs 裁定を主張しない。
