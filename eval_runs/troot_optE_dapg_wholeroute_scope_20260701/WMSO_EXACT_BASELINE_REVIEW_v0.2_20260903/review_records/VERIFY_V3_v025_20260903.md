# V3 verification — R3 findings against WMSO v0.2.5 REVIEW CANDIDATE（3 lens・adversarial・refute-first）

- verifier = V3（別 context・authority 無し・凍結物へ非接触・`eval_runs/` 未編集）; 実施 = 2026-09-05 01:5x–02:06 UTC（`date -u` 実測 02:06）; 対象 commit = `50119dc3`（branch `claude/v0-2-design-candidates-snqbmy`）
- 対象 finding = `scratchpad/review3/R3_findings_v025.json`（22 件・全件読了）+ `R3_review_v025.md`（全読）。対象 doc = `$PKG/05_…`（993 行・sha256 `c3c8b5bab864d8bb2…` = SHA256SUMS 一致）／ `$PKG/06_…`（649 行・sha256 `184d71e8fd4097da9…` = 一致）。両 doc とも全行を cat -n で読了し、finding ごとに引用行を sed / grep で再確認した。
- 略号: `$PKG` = `eval_runs/troot_optE_dapg_wholeroute_scope_20260701/WMSO_EXACT_BASELINE_REVIEW_v0.2_20260903`、`$D` = `eval_runs/troot_optE_dapg_wholeroute_scope_20260701`、A = `$D/WMSO_D11A_CONTRACTS_V2_DESIGN_RSTECHLEAD2_20260719.md`、B = `$D/WMSO_D11B_TENSOR_BINDING_DESIGN_RSTECHLEAD2_20260720.md`、D0 = `$D/WMSO_D0_ARCHITECTURE_DRAFT_RSTECHLEAD2_20260718.md`、EP = `$D/WMSO_D11A_EVIDENCE_POLICY_V1_RSTECHLEAD2_20260719.md`、RS71 = `thread_isaac_lab/thread-vault/04-Specs/RS71-System-Spec-SSOT.md`、GPT-A = `$PKG/review_records/rs_consult/A_GPT56sol_20260904.md`、V1 = `$PKG/review_records/VERIFY_V1_v023_20260903.md`。
- 拘束（VERIFY.md）: 各 finding に text / logic / impact の 3 lens を独立に当て、**反証を試みる**。文書のままでは欠陥を再現できなければ `refuted = true`。confirmed ⇔ 2 lens 以上が反証できない。Rs 裁定 4 件（`11_` §8・GPT-A）の**設計方向は欠陥ではない** — 裁定に反対するだけの finding は impact lens で refuted、裁定を本文が実現し損ねている finding は confirmed。

## 0. 手続記録（自己実測）

```text
$ git log -1                                         → 50119dc3 "WMSO v0.2.5: fold the Rs ruling on the four open decision items"
$ bash $PKG/verify_exact_baseline_pins.sh            → 11/11 PASS（D1.1-A 00192d20…8aff / EP md c474acea…27e7 / EP json e63176af…c6e / D1.1-B 5a1874d3…a6 /
                                                       blob 1353430…・ebe8154… / 54f90a7d・07250f4a / definition hash e7ca4309…3803 / policy_semver=1.9.0 / HEAD=50119dc3）
$ python3 $PKG/check_review_candidate.py 05 --runtime → citations=153 FAIL=0 WARN=10（全 C2 heuristic — R3 の自記と同一）
$ sha256sum 05 06 ↔ SHA256SUMS.txt                    → 一致（上記）
```

重複統合: 22 件を claim 単位で突合。R3-01 ↔ R3-16（`GatewayCommand` に arm 単位構造が無い）は R3-01 = 評価意味論、R3-16 = 06 §4 の未定義 symbol で claim が異なる。R3-02 ↔ R3-17（INTER_ARM の結線）は enum 不在 vs hash 列挙 / 再検証文で異なる。R3-03 ↔ R3-14（`effective_expires_at`）は未定義項 vs fault 帰属で異なる。R3-06 / R3-07 / R3-08 / R3-09 / R3-20 は同じ OP-19 / OPP-13 機構の別の穴。**完全重複 0 件・全件 `merged_into = ""`**。別 reviewer R4（06 対象）との重複（R4-01 / R4-06 / R4-07 / R4-18）は各 finding 内に注記し、reviewer をまたぐ merge はしない。

## 1. 集計

| verdict | 件数 | id |
|---|---|---|
| **confirmed** | **21** | R3-01（HIGH）／ R3-02, 03, 04, 05, 06, 07, 08, 09, 10, 11, 13（MEDIUM）／ R3-12, 14, 16, 17, 18, 19, 20, 21, 22（LOW） |
| refuted | 1 | R3-15（NOT_A_DEFECT） |
| merged | 0 | — |

severity 別 = HIGH 1 / MEDIUM 11 / LOW 9 / NOT_A_DEFECT 1。全 confirmed の最小修正は **frozen 4 file の schema delta 0**（runtime / deployment 層の型・散文のみ）。R3 の severity から変えたもの: R3-12（MEDIUM → LOW・R2-11 前例と同格）。他は R3 の付値を維持（理由は各節）。

## 2. Finding 別 verdict

### R3-01（HIGH・CONFIRMED）— INTER_ARM 項が他 arm を静止点として評価し、同時運動する両腕に対して non-conservative
- **sed 実測**: 05:457「評価対象 = 提案 command の arm の線分 × 他 arm の**同一線形化 snapshot**での状態（直近 admitted setpoint または実測）」／ 05:459「INTER_ARM は両 arm の状態を同一 snapshot で連言評価」／ 05:291「両 arm の**提案状態**が pairwise keep-out / min separation / swept-volume predicate に反する」／ 05:738 INV-36「同一線形化 snapshot で満たす（両 arm）」／ 05:780 T-37「左右 arm の**提案線分**が pairwise keep-out を跨ぐ command」／ 06:107「swept_volume_predicate_refs: 提案線分の swept volume に対する拒否述語」／ 06:109「両 arm の同一線形化 snapshot で連言評価」／ 05:496-503 `GatewayCommand{lease_id, control_epoch, executor_id, cmd_seq, kind, payload_ref, t_issue}`（arm field 無し）／ 05:444「後続 command が未 admit の間、gateway は直近 admitted setpoint を保持する（ZOH 相当）」。凍結 / 裁定: RS71:23「BOTH arms … in EVERY motion」／ GPT-A:19 条件 ⑤「CommandGateway は**両 arm の提案状態**を同一 snapshot で連言評価」／ D0:319（ISL は AND）。
- **text lens（未反証）**: 05:457 は「提案 arm の**線分** × 他 arm の**状態（直近 admitted setpoint または実測）**」と明示しており、他 arm は点。同じ doc の 05:291 / T-37 は「両 arm の提案線分・提案状態」の対で書かれ、05:459 / INV-36 の「両 arm の状態」は多義。裁定条件 ⑤ の「両 arm の提案状態」を 05:457 は「片 arm の提案 + 他 arm の静止」に置換している。反証を探した: 「他 arm の同一 snapshot の状態」を「同一 command 内の提案線分」と読める文は 05 / 06 に無い（grep INTER_ARM 全 9 箇所）。
- **logic lens（未反証）**: 反例を数値で構成。L: (0,0)→(2,2)、R: (0,2)→(2,0)、min_separation = 0.5。05:457 で L を評価: 線分 L0L1 × 点 R0=(0,2) → 距離 √2 ≈ 1.41 > 0.5 → admit。R を評価: 線分 R0R1 × 点 L1=(2,2)（L を先に admit した場合）→ 距離 √2 → admit（L0=(0,0) を使っても √2）。2 線分は (1,1) で交差する。command の詰め方（両 arm 同梱 / arm 別 command）にかかわらず、他 arm を点で固定する限り交差は見えない。arm 別 command でも 05:444 の ZOH により L の command 直後に R の command が admit されれば両腕は同時に線分上を進む。DEPLOYMENT_WORKSPACE 項は単項 keep-out（06:137-139）で arm 間を見ない。ISL は AND であって代替ではない（05:457 自身・D0:319）。
- **impact lens（未反証）**: RS71 #1 の regime（両腕が every motion で同時に動く）でこそ本 term が空文化し、裁定条件 ⑤ を本文が実現していない。本 finding は裁定 B+ への反対ではなく実現文の欠陥。INV-36 / T-37 が主張する性質を §4.3 の機構が与えない = 「appearance-only ≠ working」。ISL が backstop として残るため CRITICAL ではないが、admission 層の静的 arm 間検査が主要 regime で無効 = **HIGH**。
- **verdict**: CONFIRMED / **HIGH**。R4-01（同旨・HIGH）と重複するが merge しない。
- **minimal fix（frozen delta 0）**: (1) 05:457 を「評価対象 = 同一 step の**両 arm の線分の対**。command が両 arm の setpoint を持つ場合は両提案線分（直前 admitted setpoint → 新 setpoint・同一時間 parameterization）；片 arm のみの場合、他 arm は**直近 admitted 線分**（直前々 admitted → 直近 admitted setpoint）を線分として扱い、実測 TCP が直近 admitted setpoint に到達済（hold 完了）と線形化 snapshot で確認できる場合に限り点として扱う。判定不能 = FALSE」に改める（gateway は同一 step の対を揃えてから評価し、片 arm だけ先に admit しない）；(2) `GatewayCommand` に runtime 層 field `arm_targets: tuple[tuple[str, str], ...]`（(resource_id ∈ frozen ControlResourceSpec key, setpoint_ref)）を足す（R3-16 と同時）；(3) 05:459 / INV-36 の「両 arm の状態」を「両 arm の提案線分（または hold 中線分）」に揃える。

### R3-02（MEDIUM・CONFIRMED）— INTER_ARM が `EnvelopeTermKind` に無く、06 §4 の連言・両 doc の項数が未更新
- **sed 実測**: 05:364 `class EnvelopeTermKind(Enum): INTRINSIC_D11B | DEPLOYMENT_WORKSPACE | CONTROLLER | SAFETY_RESTRICTION`／ 05:391 `kind: EnvelopeTermKind`／ 05:398 `terms: tuple[EnvelopeTerm, ...]`／ 05:459「全 term T について T.admit(c)」／ 05:234 条件 7 `satisfiable`／ 06:16「(b) AcceptedEnvelope の非 frozen 項（ControllerEnvelope / WorkspaceRestriction / SafetyRestrictionSet）」／ 06:320-323 の連言 = 4 項／ 06:329・05:800「profile 3 項のみ」／ 06:336「`AcceptedEnvelope.terms` の 3 項 ← §4」／ 05:756 T-13「各 term 単独違反 × 4」。
- **text lens（未反証）**: enum に INTER_ARM は無い（grep `INTER_ARM` 05 = 9 箇所、enum 行には無い）。06 §4 の供給式（06:320-323）にも無い。項数 4 箇所が v0.2.5 で未更新。
- **logic lens（未反証）**: `AcceptedEnvelope.terms` の要素は `kind ∈ EnvelopeTermKind` ゆえ INTER_ARM の term object は構成不能。06 §4 が lease へ供給する連言（06:336「3 項 ← §4」）にも無いため、05:459 の「全 term」連言と条件 7 の `satisfiable` に INTER_ARM は現れない。INV-36 / 失敗表 05:341 は散文で存在するが執行 object が無い。
- **impact lens（未反証）**: 裁定 B+ の term が型・供給式に結線されていない = ABSENT-IN-CODE 類。R3-01 の修正も本 enum 無しには置けない。**MEDIUM**。
- **verdict**: CONFIRMED / MEDIUM。
- **minimal fix**: 05:364 に `INTER_ARM` を追加；06:320-323 に `, INTER_ARM(profile.inter_arm_restrictions, profile.cell.kinematic_layout) … workspace（両 arm）` を追加；06:16 に `InterArmRestrictionSet` を追加；05:756「× 5」、05:800 / 06:329「profile 4 項」、06:336「4 項」に更新。

### R3-03（MEDIUM・CONFIRMED）— `effective_expires_at` の decision 項が SHADOW で未定義・05:179/425 の「⇔」が 05:469 と矛盾
- **sed 実測**: 05:187「= min(expires_at, ack.valid_until, decision.t_mono + decision_max_age_s, validity_deadline_mono)」（条件節なし）／ 05:179・05:425「proposed_lease.mode == CLOSED_LOOP_AUTHORITY ⇔ 非 null ∧ granted == True」／ 05:469 SHADOW 列「任意（存在すれば記録）」／ 05:227「前提条件（全て AND・**不能判定 = FALSE**）」／ 05:229 条件 2 `now < permit.effective_expires_at`／ 05:190 (k)「発行時に導出して permit に固定」。凍結: EP:155「SHADOW は定義上 非 authority」／ C3:32-34（slice = SHADOW）。
- **text lens（未反証）**: `min(` は 05 に 2 箇所（183 / 187）のみで「定義される項のみ」の但し書きは無い。05:179 の ⇔ は「SHADOW ⇒ ¬(非 null ∧ granted)」を含意し、05:469「任意（存在すれば記録）」（granted な decision を SHADOW が持ち得る）と両立しない。
- **logic lens（未反証）**: SHADOW・`authority_decision_ref = None` ⇒ `decision.t_mono` 解決不能 ⇒ (k) の導出が不能。permit を float で固定する規則しか無いため、実装者は「導出不能 = 発行不能」（SHADOW lease が一切作れない）か「項を黙って落とす」（本文外の判断）のいずれかを選ぶ。fail-closed 読み（05:227 と同じ規律）は前者。
- **impact lens（未反証）**: slice の mode（Rs C3）が本文だけでは活性化できない。裁定 OPP-11 の実現文の欠落であり裁定への反対ではない。方向は over-closed だが主要 mode の到達不能 = **MEDIUM**。
- **verdict**: CONFIRMED / MEDIUM。R4-18（同旨・LOW）と重複。
- **minimal fix**: 05:187 を「min は**存在する項のみ**（decision 項は `authority_decision_ref ≠ None` のときのみ；`validity_deadline_mono` の各期限も宣言された項のみ）」に；05:179 / 05:425 を「CLOSED_LOOP_AUTHORITY ⇒ 非 null ∧ granted == True；SHADOW_NON_AUTHORITY では None 可（存在すれば記録・granted の値は問わない）」に揃える；05:424 との整合として「SHADOW permit の条件 2 でも `validity_deadline_mono` を保守的に含める」を明記（R4-18 の指摘）。

### R3-04（MEDIUM・CONFIRMED）— `validity_deadline_mono` の `issued_at + lease_max_duration_s` 項が INV-33 / R1-15 を巻き戻し、fault を誤帰属する
- **sed 実測**: 05:183「min(calibration 期限, evidence 期限, acceptance 期限, **issued_at + lease_max_duration_s**)」／ 05:289 from =「`S_LEASE_ACTIVE` / `S_HEALTH_PENDING` / **`S_BOUNDARY_WAIT`**（CLOSED_LOOP_AUTHORITY）」・fault = `R_CALIBRATION_EXPIRED_ACTIVE | R_EVIDENCE_EXPIRED_ACTIVE`・reason VALIDITY_EXPIRED／ 05:288 from = `S_LEASE_ACTIVE` / `S_HEALTH_PENDING` のみ・「LEASE_ACTIVATED からの経過」・TIMING_VIOLATION・`R_LEASE_DURATION_EXCEEDED`／ 05:735 INV-33「総活性時間 ≤ lease_max_duration_s + boundary_dwell_s（boundary 滞留を TIMING_VIOLATION に写さない）」／ 05:775 T-32／ V1:188「:281 に S_BOUNDARY_WAIT を足す修正は R1-01 と同型の害 … 採らない」／ 05:555「SAFEHOLD からの起動では B1 と offer 検証を skip」。裁定: GPT-A:43「lease_valid_until_mono = min(calibration期限, evidence期限, acceptance期限, lease_max_duration)」（起点・帰属は無指定）。
- **text lens（未反証）**: 05:183 の項は起点 = `issued_at`、05:288 / INV-33 の起点 = LEASE_ACTIVATED。同一 duration に 2 起点。05:289 の from は S_BOUNDARY_WAIT を含む。
- **logic lens（未反証）**: issued_at = t0、activated = t0+δ（δ > 0）。行 289 は t0+lease_max で発火、行 288 は t0+δ+lease_max。CLOSED_LOOP lease では**常に**行 289 が先に発火し、fault = R_*_EXPIRED_ACTIVE・reason = VALIDITY_EXPIRED（何も失効していない）。T-32 の期待（TIMING_VIOLATION / R_LEASE_DURATION_EXCEEDED）は CLOSED_LOOP で成立しない（SHADOW では 05:424「記録のみ」ゆえ成立）。TERMINAL を t0+lease_max−ε で受領し S_BOUNDARY_WAIT に居れば行 289 が producer lease を潰し、以後の転送は SAFEHOLD 起動（05:555 = offer 検証 skip）に逸れる — V1:188 が明示的に退けた挙動。
- **impact lens（未反証）**: 裁定の min 式を字義で写した結果、同一事象に 2 行・2 fault・2 reason、かつ INV-33 / T-32（同 doc が保持する検証済 fold）と矛盾。裁定は起点・帰属を定めていないので、本 finding は裁定への反対ではなく実現の不整合。lease_max の有界性は INV-33 / 行 288 が既に担う。**MEDIUM**。
- **verdict**: CONFIRMED / MEDIUM。R4-06（同旨）と重複。
- **minimal fix**: 05:183 の min から `issued_at + lease_max_duration_s` を外し、「裁定の `lease_max_duration` 成分は INV-33 / 行 05:288（LEASE_ACTIVATED 起点・permit 発行時には未知）で実現する」と一文で明記；05:289 の期限成分は calibration / evidence / acceptance に限定（S_BOUNDARY_WAIT を from に残すのはこれら外部期限のみ）；INV-34 も同旨。

### R3-05（MEDIUM・CONFIRMED）— deadline の argmin（何が失効したか）が permit / lease に無く、HOLD / SAFE_STOP の分岐と fault 選択が計算不能
- **sed 実測**: 05:183 `validity_deadline_mono: float`／ 05:424 同（permit から複写）／ 05:289「TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED \| SAFE_STOP if **失効項目が**安全機能・TCP・停止性能に関わる)」「FAULT(R_CALIBRATION_EXPIRED_ACTIVE \| R_EVIDENCE_EXPIRED_ACTIVE)」／ 05:338 同／ 05:776 T-33「TCP なら SAFE_STOP」／ 06:512 OPP-3「permit 発行時に wall-clock で評価し結果を lease に固定」。grep `argmin|失効項目|deadline_source|導出 ref` = 05:289 のみ。裁定: GPT-A:45「`validity_deadline_mono`、`acceptance_record_hash`、**期限導出 ref** を追加」— 本文は導出 ref を実現していない。
- **text lens（未反証）**: float 1 個以外に項目を運ぶ field / 台帳規定が無い。
- **logic lens（未反証）**: deadline 到達時、manager は「どの項目か」を知り得ない。wall-clock を再読して再導出するのは OPP-3 が固定した「発行時評価・lease に固定」と別の clock 読み。acceptance 期限の到達に至っては 05:289 の 2 fault のどちらにも写像が無い。fail-closed 既定（不明 = SAFE_STOP）も書かれていない。
- **impact lens（未反証）**: TCP / 安全機能失効の SAFE_STOP 強化（裁定 GPT-A:43）が実装不能で、実装者の自然な既定は HOLD（弱い側）。裁定が要求した「期限導出 ref」の欠落 = 実現漏れ。**MEDIUM**。
- **verdict**: CONFIRMED / MEDIUM。
- **minimal fix（runtime 層のみ）**: `CommitPermit` / `ActiveAuthorityLease` に `validity_deadline_items: tuple[tuple[str, str, float], ...]`（(kind ∈ {CALIBRATION, EVIDENCE, ACCEPTANCE}, id（calibration_id / record_id / acceptance_record_hash）, deadline_mono)）を足し `validity_deadline_mono = min(items[].deadline_mono)`（発行時確定）；05:289 / 05:338 の分岐を argmin の kind / id で決める規則（CALIBRATION(TCP) / EVIDENCE(SAFETY_LAYER_ACCEPTANCE) / 停止性能に関わる CONTROLLER_ENVELOPE_MEASUREMENT ⇒ SAFE_STOP；fault = CALIBRATION ⇒ `R_CALIBRATION_EXPIRED_ACTIVE`、EVIDENCE ⇒ `R_EVIDENCE_EXPIRED_ACTIVE`、ACCEPTANCE ⇒ `R_PROFILE_SUSPENDED`）；**argmin が解決できない場合 = SAFE_STOP**（fail-closed）を明記。

### R3-06（MEDIUM・CONFIRMED）— VALIDITY_EXPIRED が再試行 scope 分類（lease-scoped / non-lease-scoped）と `pending_invalidate` 列挙に無く、profile event と転送の競合で無効化が捨てられ得る
- **sed 実測**: 05:248「**lease-scoped な理由**（EXECUTOR_LOST / HEALTH_FAIL / DEADLINE_MISS / TIMING_VIOLATION / ENVELOPE_VIOLATION / CHECKPOINT_DISABLED / NO_CHAIN）の再試行は `current.active_lease_id == trigger.lease_id` のときのみ … 不一致なら CAS を捨て」「non-lease-scoped（SAFETY / SAFE_STOP / MANAGER_RESTART / GATEWAY_RESTART）は無条件に再試行」（VALIDITY_EXPIRED はどちらにも無い）／ 同行 pending_invalidate の非 safety 起因 7 件（同じく無し）／ 05:290 event 行「any (EXECUTOR・CLOSED_LOOP_AUTHORITY)」（profile 一致条件の記述なし）／ 05:88（event の内容 = 未規定）／ 05:512。凍結: A:382（状態遷移規則は manager 側で完全定義）。
- **text lens（未反証）**: grep `lease-scoped` = 05:248 / 05:889 のみ。VALIDITY_EXPIRED（v0.2.5 追加）は分類されておらず、event が lease_id を運ぶのか profile_hash を運ぶのかも未規定。
- **logic lens（未反証）**: trace = (EXECUTOR(X1), e, L1, boundary_wait) / π2(L2, profile P gen1) / t0 CAS(π2) SUCCESS（条件 12 は ACCEPTED を読む）/ t1 > t0 P → SUSPENDED / monitor event（lease = L1 を伴う場合）→ manager の TRANSFER_TO_SAFEHOLD が旧 snapshot で CONFLICT → 再読 `active_lease_id = L2 ≠ L1`。lease-scoped 読みでは CAS を捨て L1 に FAULT のみ ⇒ **L2 は SUSPENDED な P の下で actuation を続ける**（deadline / boundary まで）。non-lease-scoped 読みでは L2 を落とす。本文はどちらも定めない（05:290 の行を「任意の活性 CLOSED_LOOP lease」と字義で読む実装者は安全側、B-M1 の類推で lease-scoped と読む実装者は危険側）。deadline 到達の VALIDITY_EXPIRED は lease 固有ゆえ、trigger 種別で分けるしかない。
- **impact lens（未反証）**: SUSPENDED profile の継続 actuation = 裁定 OPP-13「revocation は active closed-loop lease を SafeHold へ」の実現漏れ・fail-closed 穴。**MEDIUM**。
- **verdict**: CONFIRMED / MEDIUM。
- **minimal fix**: 05:248 に「VALIDITY_EXPIRED の scope = trigger 種別: deadline 到達 = lease-scoped（key = lease_id）；registry / monitor event = **profile-scoped**（`current.active_lease.profile_hash == trigger.profile_hash ∧ mode == CLOSED_LOOP_AUTHORITY` の間は再試行）」を追加；05:88 / 05:290 に「event は `profile_hash` + `acceptance_record_hash`（+ 任意で lease_id）を運ぶ」を明記；05:512 の gateway 側列挙に VALIDITY_EXPIRED を加え、manager 検出ゆえ INV-24 の manager 窓が適用される旨を併記。

### R3-07（MEDIUM・CONFIRMED）— `R_REGISTRY_UNAVAILABLE` 行の「actuation 全面拒否」を活性 lease に対して実現する遷移が無い
- **sed 実測**: 05:340「permit 不発行・CAS REJECTED・**actuation 全面拒否**（診断用 SAFEHOLD での起動は可）」／ 遷移表 05:274-294 に `registry` = **0 hit**／ 05:478 §4.5 trigger に無し／ 05:82 admission に registry 読み無し／ 05:779 T-36「registry 不読で**起動**」／ 06:489「registry を復元できない場合、process は診断用 SAFEHOLD で起動してよいが permit 発行・CAS・actuation は全面拒否」／ 05:324（`R_MANAGER_UNAVAILABLE` は「lease は形式上継続」と明記する前例）。裁定: GPT-A:37（起動文脈）。
- **text lens（未反証）**: 失敗表の trigger「registry が読めない」は無限定で、起動 / permit / CAS 以外（活性 lease 中）の効果を実現する行・規則が無い。同表の `R_MANAGER_UNAVAILABLE` 行は「lease は形式上継続」と書き分けており、本行だけ主張 > 機構。
- **logic lens（未反証）**: CLOSED_LOOP lease 活性中に registry 不読 → permit / CAS は拒否 → admission は registry を読まないので lease 継続 → 「全面拒否」は文言のみ。加えて monitor は registry を観測して SUSPENDED を検出するため、不読中は event 層（registry 由来）が盲目 — deadline 層だけが残る。
- **impact lens（未反証）**: 失敗表（INV-27 / T-25 の対象）が機構より強い fail-closed を主張 = records-must-match-fact 違反、かつ mid-lease の不読が event 層を盲目にする点は R3-08 と同じ穴。裁定 GPT-A:37 は起動文脈なので、活性 lease の扱いは本文が決めて明記すべき事項。**MEDIUM**（曝露は deadline 層で有界）。
- **verdict**: CONFIRMED / MEDIUM。
- **minimal fix**: 05:340 を「起動時 / permit 発行時 / CAS 時に拒否。活性 lease は継続（deadline 層のみ有効・registry 由来 event は不読中は届かない）」に scope し、R3-08 の head 検査と結合して「不読が baseline `diagnostic_items` の当該周期を超えて続く場合、活性 CLOSED_LOOP lease を TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED)・fault `R_REGISTRY_UNAVAILABLE`」の行を §3.7 に追加（新 field 不要）。

### R3-08（MEDIUM・CONFIRMED）— `DeploymentValidityMonitor` に生存契約が無く、OP-19 の event 層は monitor が死ぬと黙って消える
- **sed 実測**: 05:88「持つ状態 = 監視対象の直近観測（durable 不要）」（heartbeat / timeout の記述なし）／ grep `heartbeat` 05 = ISL（64, 85, 236, 286, 336, 381, 511, 731, 770）と executor（283, 478, 753）のみ・monitor 0／ 05:290（event 行は monitor 通知にのみ依存）／ 06:174-187 `RuntimeTimeouts`（monitor 用 field なし）／ 06:67 `HealthCheckKind`（monitor 用 kind なし）／ 06:482 `diagnostic_items`（項目・周期・worst-case detection）／ 06:487「自動 monitor が即時 SUSPENDED にできる」。凍結: D0:318「missed heartbeat ⇒ consumers must not assume … alive」。
- **text lens（未反証）**: monitor の生存・遅延上限に関する規定は 05 / 06 に無い。
- **logic lens（未反証）**: monitor crash → registry で P → SUSPENDED（tool 交換）→ 誰も manager に通知しない → 条件 12 は次の CAS でしか registry を読まない → lease は `validity_deadline_mono`（acceptance 期限 = 日〜）まで継続。周期診断（層 3）は baseline が定める drift / proof-test 項目に限られ registry head の再読を要求しない。
- **impact lens（未反証）**: 裁定 OP-19「次 permit まで待たない」の実現が単一 process の生存に無条件依存し、D0:318 の原則が event 層の唯一の検出者に適用されていない。裁定は「一律 polling」を退けたが、baseline `diagnostic_items` の 1 項目としての head 検査は裁定の層 3（根拠付き周期診断）そのものであり裁定に反しない。**MEDIUM**。
- **verdict**: CONFIRMED / MEDIUM。
- **minimal fix（新 field 不要）**: (1) 05 §3.7 / §4.5 に manager 側の**決定論的 head 検査**を追加 — baseline `diagnostic_items` の必須項目 `REGISTRY_HEAD`（周期・worst-case detection は baseline 値）ごとに `head(registry, lease.profile_hash).record_hash == lease.acceptance_record_hash ∧ state == ACCEPTED` を線形化読みで検査、偽 = TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED)・`R_PROFILE_SUSPENDED`、不読 = R3-07 の行；(2) `diagnostic_items` の必須項目 `VALIDITY_MONITOR_LIVENESS`（monitor heartbeat）を 06 §8.3 に規定し、喪失 = 新規 CLOSED_LOOP permit 不発行 + worst-case detection 経過で活性 CLOSED_LOOP lease を VALIDITY_EXPIRED。

### R3-09（MEDIUM・CONFIRMED）— cell 水準 trigger の SUSPENDED が record 単位で、同一 cell の別 ACCEPTED profile への permit で clearance 無しに解除できる
- **sed 実測**: 06:487「incident・ISL 異常・layout / tool / controller / safety-layer identity の変更のいずれでも … 即時 SUSPENDED にできる」（対象 = record）／ 06:462-473 `ProfileAcceptanceRecord{profile_hash, generation, state, …, cell_identity_hash, …}`（profile_hash 単位）／ 06:485「SUSPENDED → ACCEPTED は新 generation の新 record（原因除去 evidence + two-key）でのみ」／ 05:529 VALIDITY_EXPIRED 行「追加前提 = なし」・解除 = 完全経路／ 05:190 (i)（`permit.profile_hash` の record のみ検査）／ 05:237 条件 10 の集合 {SAFETY, SAFE_STOP, MANAGER_RESTART, GATEWAY_RESTART, CHECKPOINT_DISABLED}（VALIDITY_EXPIRED 外）／ 06:432 PT-14（同一 cell に profile A / B）。凍結: D0:318-320（safety 事象後の再開 = stabilized state の宣言）。
- **text lens（未反証）**: cell 水準 trigger を同一 cell の全 record に適用する規則、および (i) で同一 cell の SUSPENDED を見る規則は無い（grep `cell_identity_hash` = 06:470 のみ、`cell_id` = 06:78 / 354）。
- **logic lens（未反証）**: cell C に P / Q が ACCEPTED（PT-14 が想定）。incident → 認可者が P を SUSPENDED → event → SAFEHOLD(VALIDITY_EXPIRED)。Q で permit → (i) は Q の head = ACCEPTED で通過 → 条件 10 は VALIDITY_EXPIRED を要求しない → CAS → actuation。tool / identity 変更なら BEFORE_PERMIT health check（TOOL_IDENTITY 等）が塞ぐが、incident（identity 不変）では塞がらない。ISL 決定が伴えば SAFETY 行の clearance が残るが、registry 経由だけの suspension では残らない。
- **impact lens（未反証）**: 裁定 OPP-13（GPT-A:35）「解除・再受理は原因除去 evidence + 新 generation + two-key」の意図が sibling profile で迂回される = 実現漏れ。**MEDIUM**。
- **verdict**: CONFIRMED / MEDIUM。
- **minimal fix（registry 規則中心）**: 06 §8.3 に「cell 水準 trigger（incident / ISL 異常 / layout・tool・controller・safety-layer identity 変更）による SUSPENDED は、同一 `cell_id`（identity 変更を伴う trigger では同一 `cell_identity_hash`）を持つ**全** ACCEPTED record に適用する」を追加；05:190 (i) と条件 12 に「同一 `cell_id` の record に未解除（新 generation の ACCEPTED record で supersede されていない）の SUSPENDED があれば `R_PROFILE_SUSPENDED`」を追加。

### R3-10（MEDIUM・CONFIRMED）— 06 §8.1 規則 (3) が v0.2.4 の「活性 lease は継続」のままで §5 (b) と矛盾；(2) に `accepted_profiles` が残る
- **sed 実測**: 06:411 (3)「受理後の事実変化（期限切れ・evidence 失効）は profile を変えない。**活性 lease は継続し、次の permit 発行が** … 不成立になる（§5 (b)）。**profile 起因で** lease を無効化するのは health check 失敗（§5 (a)）と `lease_max_duration_s` 超過（CD-10）**のみ**」／ 同行 (2)「`profile_hash` の登録 = `accepted_profiles` への追加」／ 06:337 §5 (b)「CLOSED_LOOP_AUTHORITY lease では次 permit まで待たない … 到達 = 05 TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED)」／ 05:289-290 / 05:478。`fold_v025.py:98` は (2) の registry 文のみ置換し (3) に触れていない（sed 確認）。
- **text lens（未反証）**: 字義どおり矛盾。(3) が参照する §5 (b) は逆の規則。
- **logic lens（未反証）**: (3) だけを読む実装者は活性 lease を継続させる（裁定前の挙動）。
- **impact lens（未反証）**: 同一 doc 内で裁定 OP-19 の中核挙動が 2 通り = 「two records disagreeing」。**MEDIUM**（R1-15 の「のみ」は scope 語の問題で LOW だったが、今回は裁定との正面矛盾）。
- **verdict**: CONFIRMED / MEDIUM。R4-07（同旨・MEDIUM）と重複。
- **minimal fix**: (3) を「受理後の事実変化は profile を変えない。SHADOW lease = 次 permit 評価で不成立；CLOSED_LOOP lease = §5 (b) の三層（deadline / event / 周期診断）で無効化（05 §4.5）。profile 起因の無効化 = §5 (a)・§5 (b)・CD-10」に書き換え；(2) の `accepted_profiles への追加` を `ProfileAcceptanceRecord(state = ACCEPTED) の append（§8.3）` に置換。

### R3-11（MEDIUM・CONFIRMED）— R2-01（timeout 経路 evidence を CLOSED_LOOP 前に要求）× R1-01（計時は CLOSED_LOOP のみ）で CLOSED_LOOP permit が到達不能
- **sed 実測**: 05:443「計時は `owner == EXECUTOR ∧ ¬boundary_wait ∧ **mode == CLOSED_LOOP_AUTHORITY** ∧ 当該 lease で admitted command ≥ 1` のときのみ」／ 06:379-381 MIN_FAULT_INJECTION（CLOSED_LOOP_AUTHORITY 前）∋ `R_DEADLINE_MISS`, `R_TIMING_VIOLATION`・「timeout 駆動 code は subject_ref = '<code>#<timeout_trigger>' で timeout 経路そのものを要求 … 別経路で代替できない」・「同一 profile_hash の下で」／ 06:292（permit 発行時 = 当該 lease mode の集合）／ 06:297 `P_EVIDENCE_POLICY_CIRCULAR`（SHADOW gated の循環のみ）／ 05:468（SHADOW command は admission 判定・記録される）／ 05:441 / 05:444（DEADLINE_MISS は timer 駆動のみ）。V1 の R1-01 最小修正（V1 §R1-01）は「最初の admitted command から開始・boundary_wait で停止」であり **mode 条件を含まない**（sed 確認）— CLOSED_LOOP 限定は fold 時の追加。
- **text lens（未反証）**: 05:443 は mode == CLOSED_LOOP を明示し、06:379-381 は当該 fault の timeout 経路 evidence を CLOSED_LOOP 前に要求する。
- **logic lens（未反証）**: 初回 CLOSED_LOOP permit（profile P）: (b) が `FAULT_INJECTION_RESULT{subject = R_DEADLINE_MISS#…, profile_hash = P}` を要求 → 当該 fault は 05:443 により P の CLOSED_LOOP lease でしか計時されない → record は存在し得ない → `P_EVIDENCE_UNBOUND` → 永久に不発行。`R_TIMING_VIOLATION` の cmd_seq 逆順経路（05:500）は timeout 経路でないため代替不可（06:381）。commissioning mode は存在しない。
- **impact lens（未反証）**: fail-closed 方向だが、定義された mode が到達不能 = 仕様の矛盾であり、実装時にどちらかが黙って緩められる。EP:155-156（CLOSED_LOOP は conjuncts の上に成る）の梯子が登れない。**MEDIUM**。
- **verdict**: CONFIRMED / MEDIUM。
- **minimal fix**: 05:443 の conjunct から `mode == CLOSED_LOOP_AUTHORITY` を外す（V1 の最小修正の範囲に戻す）— SHADOW command は admission 判定される（05:468）ので deadline / jitter は測定でき、結果の TRANSFER_TO_SAFEHOLD(DEADLINE_MISS / TIMING_VIOLATION) は SHADOW では記録上の状態で actuation を伴わない。S_BOUNDARY_WAIT・初回 command 前の除外は保つ。任意: 06:297 を「mode M でしか作れない evidence を M の前提にする循環」の一般形へ拡張。

### R3-12（LOW・CONFIRMED）— `P_SAFETY_BUDGET_EXCEEDED` に評価点 / mode 限定が無く、fail-closed 既定で SHADOW 受理が不能（R2-11 と同型）
- **sed 実測**: 06:285（評価点・mode の記述なし。「measured 値は CONTROLLER_ENVELOPE_MEASUREMENT / FAULT_INJECTION_RESULT の実測 artifact」）／ 06:305 `P_ENVELOPE_EXCEEDS_MEASURED`「評価点 = 第 2 評価点・CLOSED_LOOP_AUTHORITY のとき（登録時 = MIN_SHADOW には測定 record が無い）」／ 06:377-378（MIN_SHADOW に両 kind 無し）／ 06:383（timeout 駆動 FAULT_INJECTION_RESULT が P_SAFETY_BUDGET_EXCEEDED の入力）／ 06:411 (1)「評価不能 = 違反」・(2) の第 1 / 第 2 評価点列挙に本 code 無し。
- **text lens（未反証）**: 06:284 は「受理時 + 第 2 評価点」と書くが 06:285 は無記述。R2-11 で同型を限定した前例と不一致。
- **logic lens（未反証）**: SHADOW 専用 profile の登録（第 1 評価点）で実測 artifact が無い ⇒ 評価不能 ⇒ 違反 ⇒ 受理拒否。
- **impact lens（未反証・LOW）**: 方向は over-closed（availability）。R2-11 と同型で V2 は LOW と判定。slice mode の受理を塞ぐが 1 文で解消。R3 の MEDIUM から **LOW** へ。
- **verdict**: CONFIRMED / LOW。
- **minimal fix**: 06:285 に「評価点 = 第 2 評価点・`proposed_lease.mode == CLOSED_LOOP_AUTHORITY` のとき（登録時 = MIN_SHADOW には実測 artifact が無い）」を 06:305 と同文で追加。

### R3-13（MEDIUM・CONFIRMED）— `safehold_disposition` が TRANSFER_TO_SAFEHOLD の事後条件・初期状態に無く、(iii) の SafeStop 述語が 3 箇所で異なる
- **sed 実測**: 05:139「当該 SAFEHOLD 期間の実効 disposition の max … 同一線形化点で durable 更新され … owner.kind == EXECUTOR では None」／ 05:143 初期状態 `(SAFEHOLD, epoch=e0, None, ∅, INITIAL, False, 0)`（7 要素）／ 05:248 事後条件 `(SAFEHOLD, epoch+1, None, consumed そのまま, reason, False, seq+1)`（7 要素・disposition 無し）・`:=` 更新は「**いずれの no-op でも** `safehold_disposition := max(現値, 実効 disposition)`」のみ・末尾「(iii) は `safehold_reason == SAFE_STOP` で SafeStop を出す」／ 05:350 証明 B「(iii) さもなくば SafeHold（`safehold_reason == SAFE_STOP` なら SafeStop）」／ 05:513 §5.2 (iii)「`state.safehold_disposition == SAFE_STOP` なら SafeStop」。grep `safehold_disposition` = 139 / 248 / 513 / 771 / 941、`safehold_reason == SAFE_STOP` = 248 / 350。凍結: D0:363（miss action = STOP へ escalate）。
- **text lens（未反証）**: 05:139 の**定義**（期間の max）からは成功遷移で値が入ることが推論できるが、**操作的事後条件**（T-16 model check の tuple）と初期状態には無く、(iii) の述語が reason 版（248 / 350）と disposition 版（513）で並存する。
- **logic lens（未反証）**: S_LEASE_ACTIVE（disposition = None）から `on_health_fail = SAFE_STOP` で TRANSFER_TO_SAFEHOLD(SAFE_STOP)。事後条件の 7 要素を字義で書く実装では disposition = None のまま → §5.2 (iii) は `None == SAFE_STOP` 偽 → **SafeHold**。05:248 / 05:350 の述語なら SafeStop。同一状態で出力関数が 2 通り。
- **impact lens（未反証）**: SafeStop → SafeHold の退化は D0:363 に反する non-conservative 方向。R1-03 の fold が完結していない。**MEDIUM**。
- **verdict**: CONFIRMED / MEDIUM。
- **minimal fix**: 05:248 の事後条件に `safehold_disposition := 当該遷移の実効 disposition`（profile `EscalationPolicy` 適用後・SAFE_STOP > HOLD）を明記し、05:143 の初期状態に `HOLD` を加える；05:248 末尾と 05:350 の述語を §5.2 (iii)（`safehold_disposition == SAFE_STOP`）に統一し「reason SAFE_STOP ⇒ disposition SAFE_STOP は構成上成立」を一言添える。

### R3-14（LOW・CONFIRMED）— 条件 2 が条件 3 / 6 を包含し、同一事象の fault / disposition が二義；証明 D が旧述語
- **sed 実測**: 05:229 条件 2 `now < permit.effective_expires_at`／ 05:230 条件 3 `now ≤ ack.valid_until`／ 05:233 条件 6 `now − decision.t_mono ≤ decision_max_age_s`／ 05:227（AND・順序未規定。225-240 に「優先 / 順序」= 0 hit）／ 05:304「permit 失効 → R_PERMIT_EXPIRED → RESELECT」／ 05:301（ACK 失効 → R_ACK_MISBOUND → RESELECT）／ 05:315（decision 不在 → R_AUTHORITY_DECISION_ABSENT → HOLD）／ 05:737 INV-35（age 違反 = R_AUTHORITY_DECISION_ABSENT）／ 05:778 T-35「条件 6 で REJECTED」／ 05:354 証明 D「失効は `now < expires_at` の条件 2 で遮断」。
- **text lens（未反証）**: 条件 2 の min は条件 3 / 6 の失効を包含し、優先順は無い。証明 D は v0.2.4 の述語のまま。
- **logic lens（未反証）**: `now − decision.t_mono > decision_max_age_s` ⇒ `effective_expires_at < now` ⇒ 条件 2 も偽。記録 fault は R_PERMIT_EXPIRED（RESELECT）か R_AUTHORITY_DECISION_ABSENT（HOLD）か実装依存 — disposition が異なる。ACK 失効では両方 RESELECT で実害なし。
- **impact lens（未反証・LOW）**: 状態効果（REJECTED・permit VOID）は同一で、差は fault 帰属と disposition（RESELECT vs HOLD）。audit / T-35 の決定性の問題。**LOW**。
- **verdict**: CONFIRMED / LOW。
- **minimal fix**: 05:229 に「条件 2 不成立時の fault は `effective_expires_at` の argmin で決める: ttl ⇒ `R_PERMIT_EXPIRED` / ack ⇒ `R_ACK_MISBOUND` / decision ⇒ `R_AUTHORITY_DECISION_ABSENT` / validity ⇒ R3-05 の規則」（sub-expiry が優先）を追加；05:354 の `expires_at` を `effective_expires_at` に更新。

### R3-15（NOT_A_DEFECT・REFUTED）— `decision.t_mono` の clock domain が manager と一致する保証が無い
- **sed 実測**: 05:190 (e)「`t_mono` = … 記録した `RuntimeAuditRecord`（kind = ASSESSMENT …）の `t_mono`」／ 05:681 `RuntimeAuditRecord.t_mono: float  # 単調 clock・単一 origin`／ 05:87 AuditRecorder = `RuntimeAuditRecord` の唯一の append-only 記録者／ 05:146 clock domain（manager 単調 clock・D0 §G 単一 origin）／ 05:570「now は単調 clock … D0 CLOCK_MONOTONIC 単一 origin」／ D0:354-355「one declared monotonic source (e.g. CLOCK_MONOTONIC), origin = process/episode start」。
- **text lens（反証）**: 当該 record は AuditRecorder（runtime 構成要素）の `RuntimeAuditRecord` であり、その `t_mono` は 05:681 で「単一 origin」と型注記されている。D0 §G は「one declared monotonic source」を全体要件として宣言し、05:65 / 05:146 / 05:570 がそれを継承する。executor の `t_ack` を参考値とした B2-12 は「executor（process 境界 = OP-13 で open）が stamp する値」への規律であり、recorder が stamp する record には既に単一 origin の規律が掛かっている。
- **logic lens（反証）**: 反例は「O0 process の origin が manager と異なる」ことを前提にするが、これは本 doc と D0 が宣言する単一 source（CLOCK_MONOTONIC は同一 host で process 間共通）に反する状況であり、spec 自身の定義の下では構成できない。
- **impact lens（未反証）**: 仮に前提が破れれば age 過小で期限切れ decision が通る（non-conservative）ため、影響自体は否定しない。
- **verdict**: 2 lens が反証 ⇒ **REFUTED / NOT_A_DEFECT**。任意の hardening（採否自由・delta 0）: (e) に「record の t_mono は AuditRecorder が §3.2 clock domain で stamp する。`t_mono > now(manager)` の record は不在扱い（`R_AUTHORITY_DECISION_ABSENT`）」を添えると、宣言違反の検出が fail-closed になる。

### R3-16（LOW・CONFIRMED）— 06 §4 の `cmd.resource` が 05 `GatewayCommand` に無い未定義 symbol
- **sed 実測**: 06:321「CONTROLLER(profile.cell.arms[**cmd.resource**].controller)」／ 05:496-503 `GatewayCommand` 7 field（`resource` 無し・`payload_ref: str` のみ、payload 構造の定義 = grep `payload_ref` 05:502 / 687 で無し）／ 06:89 `ArmSpec.resource_id`（"ee_left" | "ee_right"）／ A:142 `ControlResourceSpec` の key 語彙。
- **text lens（未反証）**: `cmd.resource` は 06 にのみ現れ 05 に定義が無い。
- **logic lens（未反証）**: CONTROLLER 項は arm 別 envelope（06:92）を command の関節目標に当てる必要があるが、command のどの部分がどの arm かを定める構造が無い（payload は opaque）。
- **impact lens（未反証・LOW）**: 未定義 symbol・cross-doc 不整合。R3-01 の修正と同時に解消。**LOW**。
- **verdict**: CONFIRMED / LOW。
- **minimal fix**: 05 §5.1 `GatewayCommand` に runtime 層 field `arm_targets: tuple[tuple[str, str], ...]`（(resource_id ∈ {ee_left, ee_right}, setpoint_ref)・DIFF_IK_EE_TARGET / SCRIPTED_SEQUENCE で必須・WAIT = ()）を足し、06:321 を「∀ r ∈ {t.resource_id | t ∈ cmd.arm_targets}: CONTROLLER(profile.cell.arms[r].controller)」に書き換える。

### R3-17（LOW・CONFIRMED）— (j) / 06:306 の hash 列挙に v0.2.5 の 3 hash が無く、INTER_ARM 行に gateway の評価前再検証文が無い
- **sed 実測**: 05:190 (j)「profile が宣言する**全ての** content hash（`predicate_sha256` / `evaluator_sha256` / `zones[].geometry_sha256` / `calibration_refs[].artifact_sha256` / `gateway_config.artifact_sha256` / `deployment_evidence_policy_hash` / required `DeploymentEvidenceRecord.artifact_sha256`）」（7 種）／ 06:306（同 7 種・「gateway は predicate と zone geometry を評価前に再検証」）／ 06:100「05 §3.4 (j) の照合対象」／ 06:302「content hash は 05 §3.4 (j) の照合対象」／ 05:454・05:456「gateway は評価前に … sha256 を再検証し、不一致 = FALSE」／ 05:457 INTER_ARM 行（再検証文なし・grep `評価前` 05 = 454 / 456 のみ）。凍結: B:23。
- **text lens（一部反証）**: 規則本体は「**全ての** content hash」なので新 hash は規則上は対象（06:100 / 302 もそう主張）。列挙は stale。ただし gateway の**評価前再検証**は行ごとに個別に書かれており INTER_ARM 行に無い — こちらは実在する欠落。
- **logic lens（未反証）**: 登録後に pairwise keep-out / swept-volume predicate の artifact を差し替えると、(j) は次 permit でしか走らないため、活性 lease 中の gateway は改変内容で INTER_ARM を評価する（PT-25 / PT-27 が DEPLOYMENT_WORKSPACE / SAFETY_RESTRICTION で閉じた穴と同型）。
- **impact lens（未反証・LOW）**: R1-05 の一般化を新 term へ適用し損ねた残差。ISL 監視は残る。**LOW**。
- **verdict**: CONFIRMED / LOW。
- **minimal fix**: 05:190 (j) と 06:306 の列挙に `cell.kinematic_layout.artifact_sha256` / `inter_arm_restrictions.pairwise_keepout_sha256` / `swept_volume_predicate_sha256` を追加；05:457 に「gateway は評価前に pairwise keep-out / swept-volume predicate / kinematic layout の解決内容 sha256 を再検証し、不一致 = 評価不能 = FALSE」を追加；06:306 の gateway 再検証対象にも追記。

### R3-18（LOW・CONFIRMED）— 第 2 評価点の P_* 集合が 05 §3.4 (b) / 06 §8.1 (2) で 4 code のみ
- **sed 実測**: 05:190 (b)「doc 06 §8.1 の第 2 評価点 **=** `P_CALIBRATION_EXPIRED` / `P_EVIDENCE_UNBOUND` / `P_EVIDENCE_EXPIRED` / `P_EXPECTATION_UNCERTIFIED`」／ 06:411 (2)（同 4 code）／ 06:284 `P_TIMEOUT_CEILING`「評価 = 受理時 + 第 2 評価点」／ 06:295 `P_FAULT_EVIDENCE_UNVERIFIED`「（第 2 評価点・v0.2.4 R2-02）」／ 06:305 `P_ENVELOPE_EXCEEDS_MEASURED`「評価点 = 第 2 評価点」。凍結: A:385（strict evaluation）。
- **text lens（未反証）**: 06 §3 が第 2 評価点と宣言する 3 code が両 list に無い。05:190 (b) は「=」で定義形。
- **logic lens（未反証）**: `P_FAULT_EVIDENCE_UNVERIFIED` と `P_ENVELOPE_EXCEEDS_MEASURED` は第 2 評価点**のみ**で評価される検査なので、05 (b) の list どおりに結線した実装では**一度も評価されない**（R2-02 / R2-11 の fold が appearance-only になる）。
- **impact lens（未反証・LOW）**: 06 §3 の各行には評価点が書かれているため、行を実装する側は評価する。矛盾は 2 つの要約 list。ただし放置すれば 2 検査が未結線になり得るので修正は必須。**LOW**。
- **verdict**: CONFIRMED / LOW。
- **minimal fix**: 05:190 (b) と 06:411 (2) の列挙を「06 §3 が第 2 評価点と宣言する全 `P_*`（SSOT = 06 §3 表）」に置換。

### R3-19（LOW・CONFIRMED）— admission の `cmd.executor_id == lease.executor_id` 不一致に fault code 写像が無い
- **sed 実測**: 05:82（admission に `cmd.executor_id == lease.executor_id`・v0.2.4 R1-12）／ 05:705 INV-03 違反列 = `R_EPOCH_STALE_COMMAND / R_LEASE_UNKNOWN_COMMAND / R_TIMING_VIOLATION`／ 05:244（lease_id 不一致 = R_LEASE_UNKNOWN_COMMAND）／ 05:319「lease_id 不明の command」行／ 失敗表 298-344 に executor_id 行なし（grep `executor_id` 05 = 82 / 128 / 154 / 229 / 230 / 242 / 348 / 415 / 499 / 705 / 709 / 949）。
- **text lens（未反証）**: 写像は無い。**logic lens（未反証）**: 拒否は起きるが code が無く、05:296「各 failure に disposition を必ず割り当てる」と INV-17 の記録義務が満たせない。**impact lens（未反証・LOW）**: 拒否自体は起きる（fail-closed）。audit 完全性の問題。
- **verdict**: CONFIRMED / LOW。
- **minimal fix**: INV-03 の違反列と 05:319 行に「executor_id 不一致 = `R_LEASE_UNKNOWN_COMMAND`」を明記。

### R3-20（LOW・CONFIRMED）— 周期診断の失敗が event / fault に写像されず、event 通知遅延に上限が無い
- **sed 実測**: 05:290 event 列挙 = incident / tool 交換 / firmware 更新 / layout revision / ISL build 変更 / profile SUSPENDED・REVOKED／ 05:88「周期診断は timing baseline が定める drift / proof-test 項目のみ」／ 05:816 OP-19 層 3／ 06:482 `diagnostic_items`（項目・周期・worst-case detection）／ 06:337。
- **text lens（未反証）**: 診断失敗の帰結（fault / 遷移）は無く、event 駆動通知の遅延上限も無い（diagnostic_items の worst-case detection は周期項目のみ）。
- **logic lens（未反証）**: proof-test 失敗 → 通知先・fault・disposition が未定義 → 実装者判断。
- **impact lens（未反証・LOW）**: 裁定の層 3 が帰結を欠くが、実装者は 05:290 の `R_EVIDENCE_EXPIRED_ACTIVE` に自然に写せる（列挙漏れ）。**LOW**。
- **verdict**: CONFIRMED / LOW。
- **minimal fix**: 05:290 の event 列挙に「baseline `diagnostic_items` の失敗」を加え fault = `R_EVIDENCE_EXPIRED_ACTIVE`（安全機能なら SAFE_STOP）；06 §8.3 に「event 通知の worst-case 遅延は baseline の 1 項目として bound し、R3-08 の head 検査周期と揃える」を追加。

### R3-21（LOW・CONFIRMED）— manager が head record を state label だけで信頼し、append-only の head 決定（fork）が未定義
- **sed 実測**: 05:190 (i)「最新 `ProfileAcceptanceRecord` が `state == ACCEPTED ∧ mode ∈ allowed_lease_modes ∧ 有効期限内`」／ 05:239 条件 12（同・approvals 非参照）／ 06:471 `approvals`／ 06:486 two-key／ 06:491 `P_ACCEPTANCE_RECORD_INVALID`（受理拒否 = 書込時）／ 06:473 `supersedes_record_hash`・06:485（chain・fork 規則なし。grep `fork` = 0、「最新」の定義なし）。凍結: A:341（label ≠ authority）。
- **text lens（未反証）**: (i) / 12 は approvals を入力にせず、「最新」の決め方も無い。
- **logic lens（未反証）**: 同一 record を supersede する 2 件（例: SUSPENDED record と新 generation ACCEPTED record が同一 predecessor を指す）が並存すると head が二義。approvals 再検証は registry 書込時の検査（06:491）に依存する（registry は ACL・journal 分離 = 06:489）。
- **impact lens（一部反証）**: approvals 再検証は裁定 OPP-13「AuthorityManager は read-only consumer」の下では defense-in-depth（設計選好寄り）。head / fork の未定義は安全関連 lookup の未定義語で実在。**LOW**。
- **verdict**: CONFIRMED / LOW（text / logic 未反証）。
- **minimal fix**: 06 §8.3 に「registry は同一 `supersedes_record_hash` を持つ 2 件目の append を拒否（fork 禁止）；head = chain 末尾の record」を追加；05:190 (i) / 条件 12 に「head record について `P_ACCEPTANCE_RECORD_INVALID = 0`（mode に要する approvals role を含む）を再評価」を追加（任意だが delta 0）。

### R3-22（LOW・CONFIRMED）— `CellSafetyTimingBaseline` の改訂が旧 baseline に束縛された ACCEPTED record に及ばない
- **sed 実測**: 06:469 `timing_baseline_hash`／ 06:476-483／ 06:487 失効 trigger（baseline 改訂なし）／ 06:525 OPP-16「発行主体・改訂手続・ceiling 値の根拠 = RT0 へ carry」／ 05:817 OP-20（同）。裁定: GPT-A:25（baseline = 唯一の ceiling source）。
- **text lens（一部反証）**: 改訂**手続**は OPP-16 / OP-20 で明示的に open。ただし「改訂された baseline に束縛された record がどうなるか」は registry 側の帰結であり、open point の文言に含まれない。
- **logic lens（未反証）**: baseline を締めて新 hash を発行しても、旧 hash を持つ ACCEPTED record は `valid_until_iso8601` まで旧 ceiling で permit を通す。
- **impact lens（未反証・LOW）**: 超えた ceiling が失効まで認可し続ける = non-conservative だが、acceptance 期限で有界・改訂は RT0 手続で、1 行で fail-closed 既定を置ける。**LOW**。
- **verdict**: CONFIRMED / LOW。
- **minimal fix**: 06 §8.3 失効 trigger に「baseline の supersession ⇒ 旧 `timing_baseline_hash` を持つ全 ACCEPTED record を SUSPENDED（one-key fail-safe）」を追加（改訂手続自体は OPP-16 のまま）。

## 3. 反証 trace の要約（lens 別）

| finding | text | logic | impact | verdict | severity |
|---|---|---|---|---|---|
| R3-01 | 未反証 | 未反証（数値反例） | 未反証 | CONFIRMED | HIGH |
| R3-02 | 未反証 | 未反証 | 未反証 | CONFIRMED | MEDIUM |
| R3-03 | 未反証 | 未反証 | 未反証 | CONFIRMED | MEDIUM |
| R3-04 | 未反証 | 未反証 | 未反証（裁定の起点・帰属は無指定） | CONFIRMED | MEDIUM |
| R3-05 | 未反証 | 未反証 | 未反証（裁定の「期限導出 ref」欠落） | CONFIRMED | MEDIUM |
| R3-06 | 未反証 | 未反証 | 未反証 | CONFIRMED | MEDIUM |
| R3-07 | 未反証 | 未反証 | 未反証 | CONFIRMED | MEDIUM |
| R3-08 | 未反証 | 未反証 | 未反証（層 3 と整合） | CONFIRMED | MEDIUM |
| R3-09 | 未反証 | 未反証 | 未反証 | CONFIRMED | MEDIUM |
| R3-10 | 未反証 | 未反証 | 未反証 | CONFIRMED | MEDIUM |
| R3-11 | 未反証 | 未反証 | 未反証 | CONFIRMED | MEDIUM |
| R3-12 | 未反証 | 未反証 | 未反証（LOW） | CONFIRMED | LOW |
| R3-13 | 未反証（定義は示唆・事後条件は欠落） | 未反証 | 未反証 | CONFIRMED | MEDIUM |
| R3-14 | 未反証 | 未反証 | 未反証（LOW） | CONFIRMED | LOW |
| R3-15 | **反証** | **反証** | 未反証 | REFUTED | NOT_A_DEFECT |
| R3-16 | 未反証 | 未反証 | 未反証（LOW） | CONFIRMED | LOW |
| R3-17 | 一部反証（規則は「全て」） | 未反証 | 未反証（LOW） | CONFIRMED | LOW |
| R3-18 | 未反証 | 未反証 | 未反証（LOW） | CONFIRMED | LOW |
| R3-19 | 未反証 | 未反証 | 未反証（LOW） | CONFIRMED | LOW |
| R3-20 | 未反証 | 未反証 | 未反証（LOW） | CONFIRMED | LOW |
| R3-21 | 未反証 | 未反証 | 一部反証 | CONFIRMED | LOW |
| R3-22 | 一部反証（手続は open） | 未反証 | 未反証（LOW） | CONFIRMED | LOW |

## 4. 主張しないこと・境界
- 本報告は AI verifier の判定であり two-key / Rs 裁定 / gate PASS を主張しない。凍結 4 file は不変（11/11 PASS 再測）。`eval_runs/` は未編集。出力は本 file と `V3_results_v025.json`（同 dir）のみ。
- Rs 裁定 4 件の設計方向は検証対象にしていない。confirmed 21 件はいずれも「裁定を本文が正しく・一貫して・fail-closed に実現しているか」の欠陥であり、最小修正は全て frozen schema delta 0（runtime / deployment 層の型 field 追加を含むものは R3-01 / 05 / 16 のみ・いずれも frozen 4 file 外）。
- R4（06 対象）との重複 4 件（R4-01 / 06 / 07 / 18）は各節に注記。reviewer をまたぐ merge は行わない。
- 08 §5 の語彙では HIGH 1 件（R3-01）が残るため、本 round の group verdict は **HOLD**（R3-01 / R3-02 / R3-16 を同時に fold すれば解消可能）。
- 実測 timestamp = 2026-09-05 02:06 UTC（`date -u`）。
