# VERIFY_A_v021 — finding group A（reviewer A・12 件）の 3 lens 反証検証 + v0.2.2 fold 状態

- 検証者 = adversarial verifier（別 context）。実施 = 2026-09-04 15:53 UTC（`date -u` 実測）。
- 対象版: 前提検証 = v0.2.1 候補（`scratchpad/design/fold/backup_v021_pkg/`）／ fold 検証 = v0.2.2（`$R = eval_runs/troot_optE_dapg_wholeroute_scope_20260701/WMSO_EXACT_BASELINE_REVIEW_v0.2_20260903/`）。
- 規則: text / logic / impact の 3 lens を各 finding に独立適用。既定 = refuted。2 lens 以上が未反証のときのみ confirmed。
- 記法: `A:` = frozen D1.1-A `WMSO_D11A_CONTRACTS_V2_DESIGN_RSTECHLEAD2_20260719.md`、`B:` = frozen D1.1-B `WMSO_D11B_TENSOR_BINDING_DESIGN_RSTECHLEAD2_20260720.md`、`EP md/json` = `WMSO_D11A_EVIDENCE_POLICY_V1_RSTECHLEAD2_20260719.md` / `WMSO_EvidencePolicy_v1.9.json`、`D0:` = `WMSO_D0_ARCHITECTURE_DRAFT_RSTECHLEAD2_20260718.md`。`05:`/`06:` 等は特記なければ v0.2.1 backup の行番号、`v0.2.2 05:` は現行 package の行番号。

## 0. 共通前提の実測

- pin 再測: `bash $R/verify_exact_baseline_pins.sh` → **11/11 PASS**（D1.1-A `00192d20…` @ `54f90a7d`・blob `13534302…`／ D1.1-B `5a1874d3…` @ `07250f4a`・blob `ebe8154a…`／ EP md `c474acea…`／ EP JSON `e63176af…`／ definition hash `e7ca4309…`・policy_semver 1.9.0）。HEAD = `297c850bb31a…` の表示は script 出力（worktree HEAD は `f3c6ca35`）。
- 機械検査（v0.2.2 checker・v0.2.2 doc）: 05 `--runtime` FAIL=0 WARN=10 ／ 06 FAIL=0 WARN=23 ／ 07 FAIL=0 WARN=11 ／ 04 FAIL=0 WARN=7 ／ 08 FAIL=0 ／ 02 CSV FAIL=0（citations=83）／ 03 CSV FAIL=0（citations=35）／ 10 FAIL=11（S1 — records-only doc・従来どおり）。
- 機械検査（v0.2.1 backup checker・v0.2.1 doc）: 05 FAIL=0 WARN=6 ／ 06 FAIL=0 WARN=19 ／ 07 FAIL=0 ／ 02 **FAIL=15**（H1×5 / S1×5 / F1×5）／ 03 **FAIL=11**（H1×6 / S1×5）。
- 陽性対照 `10_` の注入 8 欠陥は本群の finding に含まれない（各 finding の前提文を v0.2.1 実体で sed 確認済）。
- 群内の完全重複: 無し（A-07 / A-08 は同じ CSV でも軸が異なる）。merged_into は全件 ""。

## 1. 各 finding

### A-01（MEDIUM・confirmed・fold = PARTIAL）
- v0.2.1 実体: `05:224` 条件 6 = 「…かつ decision の inputs hash/ref 束縛が lease の `skill_action_id` / `profile_hash` に一致」。`05:218` 「前提条件（全て AND・不能判定 = FALSE）」。`05:300` 失敗行 `R_AUTHORITY_DECISION_MISBOUND`。
- frozen: `A:346-351` `evaluate_authority_grant(eligibility_report, acceptance_state, two_key_state, safety_gate_state) -> AuthorityDecision {granted, denials, inputs の hash/ref 束縛}`；`A:329-337` `UsageEligibilityReport` の field に `skill_action_id` 無し；`A:354` gate 実体 = O0/S0/V0 層・本 chunk 外；`profile_hash` / `IndustrialDeploymentProfile` = frozen 4 file で 0 hit（grep -c 実測 0/0/0/0）。
- text: 未反証（引用どおり。frozen 型に referent 無し）。
- logic: 未反証。frozen `AuthorityDecision` の inputs 束縛から `profile_hash` を読む経路が無く、05 の「不能判定 = FALSE」により CLOSED_LOOP_AUTHORITY lease は全て条件 6 で REJECTED。満たす唯一の方法 = `AuthorityDecision` に profile 束縛を足す = schema delta（05 §11 項 3 と矛盾）。`skill_action_id` は certificate-first（`A:323`・`ContractCertificate.skill_action_id` `A:306`）経由で間接的に辿れるが、`profile_hash` は辿れない。
- impact: MEDIUM。fail-closed だが弁別力 0 の必要条件で、anchor「schema delta 0」の下では充足不能。slice では CLOSED_LOOP は定義のみ（Rs C3）ゆえ HIGH ではない。
- v0.2.2 fold: `v0.2.2 05:227` 条件 6 = granted ∧ 「decision の入力 eligibility_report が依拠する certificate（`A:323`・`A:306`）の skill_action_id == proposed_lease.skill_action_id」+ 「frozen AuthorityDecision は profile を知らない（`A:346-350`）ため profile_hash の束縛は permit 側（条件 2）」；`v0.2.2 05:223` 条件 2 に `permit.profile_hash == permit.proposed_lease.profile_hash`；`v0.2.2 05:184` §3.4 (f) 同等値；`v0.2.2 05:777` OP-18（cell 固有 gate は O0/S0/V0 層）；06 `decision_max_age_s`（`v0.2.2 06:153`）。最小修正案と一致。
- **残る gap（PARTIAL の根拠）**: `v0.2.2 06:284` 「`CommitPermit.profile_hash` と等値（**05 §3.5 前提 6 の inputs 束縛**）」が stale — v0.2.2 の条件 6 は profile_hash を束縛せず、束縛点は §3.5 条件 2 / §3.4 (f)。cross-reference を「05 §3.5 条件 2・§3.4 (f)」へ直す 1 箇所。
- 付記（A-01 の採点外・B2-14 由来）: `v0.2.2 05:184` (e) の `age ≤ decision_max_age_s` は `AuthorityDecision` に時刻 field が無い（`A:351`）ため `age` の出所（ref 解決 record の受領時刻等）が 05 に未定義 — 同じ「referent 無き述語」類。B2-14 側で定義を要する。
- minimal fix（残差）: 06 §5 の該当文を「05 §3.5 条件 2・§3.4 (f) の等値束縛」に置換。schema delta 0。

### A-02（MEDIUM・confirmed・fold = RESOLVED）
- v0.2.1 実体: `05:646` INV-16 = 「runtime 型の全 field 名が frozen 型の WCJ payload key 集合に不在（… `A:550` に倣う）」；`05:680` T-18 = 「runtime 型 field 名 ∩ frozen WCJ key 集合 = ∅」。
- frozen: `A:550` 「runtime 型の全 field 名が **static hash の WCJ payload key 集合**に不在」（frozen runtime 型 vs static hash payload）。
- text: 未反証（引用どおり。「static hash の key 集合」→「frozen 型の WCJ payload key 集合」へ一般化されている）。
- logic: 未反証。狭い読み（static hash の key 集合）でも反例が成立: `ActiveAuthorityLease.control_mode`（`05:376`）↔ `ExecutionBundle.control_mode`（`A:70`・SkillDefinitionHash の payload 内）／ `TimingBinding.{policy_rate_hz, obs_sampling_rate_hz, action_rate_hz, hold}`（`05:333-336`）↔ `TimingSpec` `B:97-98`・`ActionTimingSpec` `B:126-127`（tensor_binding_hash の payload 内）／ `AuthorityCas.kind`（`05:194`）↔ `ExecutionBundle.kind` `A:65`・`ProofItem.kind` `A:244` ／ `ExtraInitiationConjunct.{expr_kind, schema_ref, schema_hash, payload_canonical_json, required_belief_fields}`（`06:121-126`）= `InitiationSpec`（`A:151`）と完全一致。よって T-18 は常に非空交差 = 恒常 FAIL。
- impact: MEDIUM。INV-16 は「設計違反（test）」扱いの不変量かつ anchor 1（`05:770`）の根拠 — 自身の型で充足不能な不変量は anchor を空文にする。
- v0.2.2 fold: `v0.2.2 05:690` INV-16 = 「型境界 + hash preimage の断定（名前の非交差ではない）: (a) frozen strict codec が runtime 固有 field を unknown field として拒否（`A:385`）; (b) runtime / profile 型の値は frozen hash の preimage に入らない（03 matrix C01–C07）。`A:550` は static hash の key 集合に対する主張であり、本 INV はその趣旨を継承」；`v0.2.2 05:729` T-18 = negative decode test + preimage test；`v0.2.2 05:871` anchor 1 更新。残る name-disjointness 文は grep で無し。→ RESOLVED。

### A-03（MEDIUM・confirmed・fold = RESOLVED）
- v0.2.1 実体: `05:97` 「凍結 4 file に該当語彙なし（EP md/json で `controller`/`calibrat`/`deploy` = 0 hit — 03 map §6(e)）」；`07:23` 「凍結 4 file に cell / controller / calibration / deploy の語彙は無い（critic 報告 §3.7 grep）」；`02:37` F36 「frozen 4 file に該当語彙なし」。06 は v0.2.1 で EP md/JSON に限定済（`06:330`）。
- 実測: `grep -n -i -E 'controller|calibrat|deploy'` → `A:570`「calibrated uncertainty」／ `B:255`・`B:313`「全 deployment の resolver」／ `B:337`「calibrated uncertainty」= DESIGN 2 file に 4 hit；EP md/json = 0。`cell`（-w）= A 8 行 / B 1 行 / EP md 10 行 / JSON 1 行（3 出現）。critic 報告の §3.7 は「EP v1.9 profile tables — verbatim」で grep ではなく、grep 記述は critic `:215`（「0 hits in EP md/json」= EP 限定）。maps `03_evidence_policy_map.md` §6(e) も EP md + json のみ。
- text: 未反証（3 doc の文言は「4 file に無い」、根拠は EP 2 file のみ。07 の節参照も誤り）。
- logic: 未反証（散文語の hit は schema 語彙ではないため結論「外部 profile」は保つが、主張文は偽）。
- impact: MEDIUM。records-must-match-fact（CLAUDE.md §運用15 (b)・§5）と frozen 前例 `B:458` N-1（over-broad な不在主張は設計の scope 修正対象）。07 §1 の decision ground に使われている。結論不変ゆえ HIGH ではない。
- v0.2.2 fold: `v0.2.2 05:97`（型 level の事実 + 再現 command + DESIGN 2 file の散文 hit `A:570`・`B:255`/`:313`・`:337` を明記）；`v0.2.2 07:23`（`A:127-149`・`B:176-181` の型列挙に根拠を移し、散文 hit を列挙）；`v0.2.2 02:37` F36（型 level・citation `A:127-149; B:176-181`）；`v0.2.2 06:336`（EP md/JSON 限定・再現 command）。再現: 6 pattern × EP md/json = 全て 0/0（本 session 実測）。→ RESOLVED。
- 付記（採点外・微小）: `v0.2.2 05:97` は SkillDefinition を `A:151`（構成型行）で引く（07/02 は `A:127-149`）— いずれも型 level で内容は正。`v0.2.2 06:336` の「json 3 行」は実測 1 行（3 出現）。

### A-04（LOW・confirmed・fold = RESOLVED）
- v0.2.1 実体: `05:425` 「lease は `profile_hash` 経由で EP evidence profile の要求（eligibility report の `requested_profile`）を参照する」。
- frozen / 対 doc: `A:329-331` `requested_profile` は `UsageEligibilityReport` の field；`A:346-347` `eligibility_report` は `evaluate_authority_grant` の入力；`06:194-213` `IndustrialDeploymentProfile` に EP profile / `requested_profile` を運ぶ field 無し；`06:56` 自身が「lease の `authority_decision_ref` 経由（05 §4.4）」と書く → 05:425 は 06:56 と矛盾。
- text / logic: 未反証。impact: LOW（3 軸分離の文言）。
- v0.2.2 fold: `v0.2.2 05:451` 「lease は `authority_decision_ref` 経由で（`A:346-347`）eligibility report の `requested_profile` を参照するのみで再評価しない。`profile_hash`（doc 06）は evidence profile を運ばない」。→ RESOLVED。

### A-05（LOW・confirmed・fold = RESOLVED）
- v0.2.1 実体: `05:19` 「ActionBinding{bounds, action_scale, control_mode}」。frozen `B:128-135` ActionBinding = {features, total_dim, control_mode, action_scale, timing, container_dtype}（bounds 無し）；`B:81` `FeatureBinding.bounds`；`B:138`。05 自身の `05:58` は正しく {control_mode, action_scale, timing}。
- text / logic: 未反証。impact: LOW。
- v0.2.2 fold: `v0.2.2 05:19` 「ActionBinding{control_mode, action_scale, timing} + FeatureBinding.bounds」。→ RESOLVED。

### A-06（LOW・confirmed・fold = RESOLVED）
- v0.2.1 実体: `06:220` 「profile は自身の `profile_hash`・lease id・`control_epoch`・`SkillActionId`・`ExecutionBundleHash` を内包しない…`binding_expectations[].skill_action_id` は他者の id」；`06:183` `BindingExpectation.skill_action_id: str`；`06:212-214` preimage に含む。frozen `B:23` の規則は自己参照 hash（自身の hash・自身の SkillActionId・自身と同一を主張する hash）。
- text: 未反証（字義的自己矛盾）。logic: 未反証（cycle は無い — 03 C03 — が規則文は型と矛盾）。impact: LOW。
- v0.2.2 fold: `v0.2.2 06:226` 構造 pattern（自身から導かれる hash・自身と同一を主張する hash・profile_hash を preimage に持つ runtime 値を置かない；他 artifact の静的 id 参照は可）。→ RESOLVED。付記: 「profile_hash を preimage に持つ runtime 値（lease id・`control_epoch`・permit id）」は `control_epoch`（counter）に対して緩い表現だが型との矛盾は無い。

### A-07（LOW・confirmed・fold = RESOLVED）
- v0.2.1 実体: `02:22` F21 rationale = 「D0『producer retains ownership and safe-stops』」、citation = `D0:319`（実測 `D0:319` = preemption/ack 行、`D0:273-274` = producer retains…）；`02:17` F16 locus/citation = `D0:270-272`（「accept + ack」は `D0:269`、`:270-272` は commit/token flip）；`02:19` F18 = 4 要素 tuple（v0.2.1 `05:130-137` は 7 field、`05:219` は tuple 全体等値）；v0.2.1 fold 表 `05:750-763` に 02/03 再同期行なし。
- text / logic: 未反証。impact: LOW（引用精度・同期）。
- v0.2.2 fold: `v0.2.2 02:17` F16 = `D0:267-272`；`v0.2.2 02:19` F18 = 7 要素 tuple；`v0.2.2 02:22` F21 citation = `D0:273-274`；`v0.2.2 05:862` fold 表行「A-07 / A-08 / A2-07 | 02・03 CSV …」= 再同期行。→ RESOLVED。

### A-08（LOW・confirmed・fold = RESOLVED）
- v0.2.1 実体: v0.2.1 checker で 02 → FAIL=15（H1×5・S1×5・F1×5: 行 6/29/30/40 = F05 removed / F28 not_adopted / F29 removed / F39 removed）、03 → FAIL=11（H1×6・S1×5）；`08:23` 「FAIL があれば読む前に HOLD」、`08:6` 対象に 02/03；CSV に pin header 無し；checker `:44-61` は md 前提（版歴 heading 下のみ免除）。
- text / logic: 未反証（計器の様式不一致で計画前提が CSV に対し充足不能）。impact: LOW（手続）。
- v0.2.2 fold: checker に CSV mode（拡張子判定・K1 header/行数 40/14・F1 免除 = disposition/verdict ∈ {removed, not_adopted}・C1 継続）；実測 v0.2.2 02 FAIL=0 / 03 FAIL=0（v0.2.1 backup CSV でも FAIL=0）；`v0.2.2 08:23` に CSV mode を明記。pin header 行は任意提案で未追加（08 §1 項 3 が CSV の検査様式を定義したため A10 の header 検査は CSV に非適用と読める）。→ RESOLVED。

### A-09（LOW・confirmed・fold = RESOLVED）
- v0.2.1 実体: `grep -n -E '[0-9]{2}:[0-9]x'` → `05:8`（16:4x）・`05:748`（01:5x）・`06:3`・**`06:419`**（finding は 06:420 と記すが実体は 419 — 内容は同一）・`10:3`。frozen `A:4` 「旧『10:5x』表記を確定 — 以後 x-mask 廃止」、`B:2` records-fix（実測 bracket）。
- text / logic: 未反証（1 行のずれは finding の主張を変えない）。impact: LOW（date-THEN-write）。
- v0.2.2 fold: 同 grep → `v0.2.2 06:454`（fold 表の finding 記述）のみ。`v0.2.2 05:8` = 「11:20 UTC 実測 … v0.2（16:44 UTC）… v0.2.1（2026-09-04 01:47 UTC）」、`v0.2.2 05:805` 「01:47 UTC 実測」、`v0.2.2 06:3` 「01:51 UTC（file mtime 実測 01:51:18）」、`v0.2.2 10:3` 「01:49 UTC（file mtime 実測 01:49:59）」。git `297c850b` = 2026-09-04 01:51:43 UTC（v0.2.1 commit）と整合。→ RESOLVED。

### A-10（LOW・confirmed・fold = PARTIAL）
- v0.2.1 実体: `05:269` §3.7 = 「`SkillOutcome`（`InterruptOutcome`）受領 → TRANSFER_TO_SAFEHOLD(CHECKPOINT_DISABLED)・FAULT(R_CHECKPOINT_SWITCH_DISABLED)」（reason 不問）；`05:592` §7 = 「Interrupt SAFETY_STABILIZED → HOLD（`safehold_reason = SAFETY`・clearance 待ち）」；`05:467` SAFETY = ISL clearance record、`05:469` CHECKPOINT_DISABLED = operator clearance + 再観測。frozen `A:151` InterruptReason = {PLANNED_SWITCH, EVENT, SAFETY_STABILIZED}。
- text / logic: 未反証（節間で safehold_reason と解除権者が食い違う）。impact: LOW（executor 発の SAFETY_STABILIZED のみ。ISL 発の safety は §5.2 (i) 経由で SAFEHOLD(SAFETY) に落ちる）。
- v0.2.2 fold: `v0.2.2 05:273` PLANNED_SWITCH / EVENT → CHECKPOINT_DISABLED、`v0.2.2 05:274` SAFETY_STABILIZED → TRANSFER_TO_SAFEHOLD(SAFETY)・解除 = §5.4 SAFETY 行；§7 `v0.2.2 05:633-634` と §5.4 `v0.2.2 05:507/509` は整合；失敗表 `v0.2.2 05:321` は PLANNED_SWITCH / EVENT に限定。
- **残る gap（PARTIAL の根拠）**: `v0.2.2 05:522` §6.1 CHECKPOINT 行は「executor 自身が `InterruptOutcome{checkpoint_id, reason}` を出し…→ TRANSFER_TO_SAFEHOLD(CHECKPOINT_DISABLED)・fault `R_CHECKPOINT_SWITCH_DISABLED`・disposition HOLD」と reason 不問のまま → SAFETY_STABILIZED について §6.1 ↔ §3.7/§7 の矛盾が残存（矛盾の所在が §3.7↔§7 から §6.1↔§3.7 へ移った）。
- minimal fix（残差）: `v0.2.2 05:522` に「reason ∈ {PLANNED_SWITCH, EVENT} のとき。SAFETY_STABILIZED は TRANSFER_TO_SAFEHOLD(SAFETY)・fault なし（§3.7 行・§5.4 SAFETY 行）」を追記。schema delta 0。

### A-11（LOW・confirmed・fold = RESOLVED）
- v0.2.1 実体: `05:245` `validate_handoff(invocation, offer, producer_def, consumer_def, …)` に「invocation = producer の」の明示なし；`05:494` `new_invocation(c, …)` を構築した直後に `05:495` `validate_handoff(invocation, …)`。frozen `A:381` = producer_invocation_id 一致 / `offer.producer_action_id == invocation.skill_action_id` / `offer.producer_definition_hash == invocation.skill_definition_hash` → 引数は producer の invocation。
- text: 未反証（明示文は無い。B1 `05:489` の `invocation` と B4 の `new_invocation(c, …)` の命名で区別はされるが規範文が無い）。logic: 未反証（consumer 側を渡すと全 offer が `R_OFFER_INVALID` = 空文の fail-closed）。impact: LOW。
- v0.2.2 fold: `v0.2.2 05:249` 「`invocation` = producer の `SkillInvocation`・`offer.producer_invocation_id` と一致（`A:381`）」、`v0.2.2 05:536` `validate_handoff(producer_invocation, …)`。→ RESOLVED。

### A-12（LOW・confirmed・fold = RESOLVED）
- v0.2.1 実体: `06:280` 写像文 = 5 field（ack_timeout_s / permit_ttl_s / health_confirm_timeout_s / command_deadline_s / manager_liveness_s）；`06:146` `RuntimeTimeouts.ack_validity_s`；`05:339` `TimingBinding.ack_validity_s`；`05:652` INV-22 は 6 種を列挙。
- text / logic: 未反証。impact: LOW。
- v0.2.2 fold: `v0.2.2 06:286` = 12 field（ack_validity_s を含む）← `RuntimeTimeouts`（`v0.2.2 06:146-157`）、05 `TimingBinding`（`v0.2.2 05:353-368`）と同名対応。→ RESOLVED。
- 付記（採点外・他 fold 由来）: `v0.2.2 05:696` INV-22 の括弧列挙は依然 6 種（ack validity / ack / permit / health / command / manager liveness）で、v0.2.2 で足された `safety_heartbeat_timeout_s` / `decision_max_age_s` / `boundary_dwell_s` / `inter_command_jitter_s` を列挙していない（「全 timeout」の規範自体は保つ）。B2-01 / B-M9 / B2-14 / B-L2 側の同期漏れとして記録。

## 2. 集計

| id | severity | confirmed | fold_status |
|---|---|---|---|
| A-01 | MEDIUM | yes | PARTIAL（06:284 stale cross-ref） |
| A-02 | MEDIUM | yes | RESOLVED |
| A-03 | MEDIUM | yes | RESOLVED |
| A-04 | LOW | yes | RESOLVED |
| A-05 | LOW | yes | RESOLVED |
| A-06 | LOW | yes | RESOLVED |
| A-07 | LOW | yes | RESOLVED |
| A-08 | LOW | yes | RESOLVED |
| A-09 | LOW | yes | RESOLVED |
| A-10 | LOW | yes | PARTIAL（05:522 §6.1 行が reason 不問） |
| A-11 | LOW | yes | RESOLVED |
| A-12 | LOW | yes | RESOLVED |

- 未反証 CRITICAL / HIGH = 0。MEDIUM 3 件はいずれも fold 済（A-01 は cross-ref 1 箇所の残差）。
- 本検証は gate PASS / two-key / Rs 裁定を主張しない。凍結 4 file および `$R` 配下は無編集（本 report のみ scratchpad に書込）。
