# R2 — WMSO v0.2.3 review candidate 独立レビュー（軸 C: deployment / commissioning & functional-safety・adversarial）

- reviewer = R2（別 context・authority 無し・凍結物へ非接触・`eval_runs/` 配下は一切編集していない）
- 対象 = `$PKG/06_WMSO_INDUSTRIAL_PROFILE_v0.2_REVIEW_CANDIDATE_20260903.md`（header v0.2.3・526 行・全行読了）／ 副 = `05_…RUNTIME_SPEC…`（928 行・全行読了）§3.4 / §4 / §5.4 中心／ `04_…`（76 行）／ 前 round = `11_…`（166 行）・`review_records/AXIS_C_review_v022_20260903.md`（209 行）・`review_records/VERIFY_C_v022_20260903.md`（182 行）
- `$PKG` = `eval_runs/troot_optE_dapg_wholeroute_scope_20260701/WMSO_EXACT_BASELINE_REVIEW_v0.2_20260903`、`$D` = `eval_runs/troot_optE_dapg_wholeroute_scope_20260701`。凍結引用の略記: A = `$D/WMSO_D11A_CONTRACTS_V2_DESIGN_RSTECHLEAD2_20260719.md`、B = `$D/WMSO_D11B_TENSOR_BINDING_DESIGN_RSTECHLEAD2_20260720.md`、EP = `$D/WMSO_D11A_EVIDENCE_POLICY_V1_RSTECHLEAD2_20260719.md`、D0 = `$D/WMSO_D0_ARCHITECTURE_DRAFT_RSTECHLEAD2_20260718.md`
- 本 report 作成 = 2026-09-04 22:41:07 UTC（`date -u` 実測）。成果物 = 本 file + `R2_findings_v023.json`（同 dir）+ `R2_pin_recheck.txt` + `R2_checker.txt`

## 0. 共通前提の実施記録（08 §1）

| 項 | 実測 |
|---|---|
| pin 再測 | `bash $PKG/verify_exact_baseline_pins.sh`（素の bash）= **PASS 11 / FAIL 0**（2026-09-04 22:22:09 UTC・HEAD `ddb37db1d3eee2a4fa92331312cfbb4ff22f3af1` branch `claude/v0-2-design-candidates-snqbmy`・`policy_semver=1.9.0`・definition hash `e7ca4309…`）。出力全文 = `R2_pin_recheck.txt` |
| 機械検査（読む前） | `python3 $PKG/check_review_candidate.py 06… --profile` = **citations=60 FAIL=0 WARN=24**（全 WARN = C2 heuristic）。出力全文 = `R2_checker.txt` |
| content sha | `sha256sum -c SHA256SUMS.txt` = **31 / 31 OK**（22:31:39 UTC）；`11_THREE_AXIS_REVIEW_RECORD_20260903.md` 実在（mtime 09-04 20:48・38,558 B） |
| 陽性対照の照合 | 10_ の注入 4 欠陥（PC-06-E bounds field / F rate ≤ / G `requires_safety_layer_health=False` 可 / H SAFE_STOP→HOLD 緩和）が実 06 に無いことを確認: 06:240（bounds field 無し・`P_UNKNOWN_FIELD`）／ 06:242（rate/hold **等値**・PT-01）／ 06:120（True 必須・False = 登録拒否）／ 06:70（`EscalationTarget` に緩和値無し）。本 report の finding はいずれもこれらの再報告ではない |
| checklist C1 の supersede | 「rate ≤ / hold stricter-or-equal」は誤り。rate / hold / control_mode は frozen tensor binding と**等値のみ**（06 §3 `P_RATE_MISMATCH` / `P_HOLD_MISMATCH` / `P_CONTROL_MODE_MISMATCH`）として審査した |
| cross-reference（機械） | 06 本文の `R_*` は全て 05:607-621 の `RuntimeFaultCode`（39 member）に存在；05 が引く `P_*` は全て 06:371 の一覧（35 code）に存在（regex 実測。「MISSING」に出た `R_TO_SAFEHOLD` 等は `TRANSFER_TO_SAFEHOLD` 等の部分一致で false positive）。`DeploymentEvidenceKind` 8 名は EP JSON `policy_definition` の大文字識別子 54 個と衝突なし（python 実測） |
| 独立手続 | 他軸の v0.2.3 finding は存在しない（11_ §4「v0.2.3 の再レビューは未実施」）。前 round（CD-01..18・11_・VERIFY_C）は fold 検証のための文脈としてのみ読み、既 fold 項目は再報告しない |

## 1. 判定

**HOLD** — 未反証の HIGH が 3 件残る（R2-01 / R2-03 / R2-04）。いずれも fold 案は zero frozen-schema delta で明確（R2-04 は Rs 裁定を要する 2 択）。MEDIUM 6 / LOW 10。open = 0 を宣言しない。

- 数え方: HIGH 3 / MEDIUM 6 / LOW 10 = 19 件（`R2_findings_v023.json` と同一）。
- 08 §5 の語彙で、HIGH 3 件が fold されれば PASS-WITH-CONDITIONS へ移行可能。

## 2. PASS 1 — v0.2.3 fold（06 §11 CD-01..CD-18）の行別回帰

各行: (a) 元 finding の反例を新本文に再実行、(b) 05 / 06 横断の矛盾（grep）、(c) 到達不能 code・充足不能な必須集合・循環・05↔06 の未定義参照。

| CD | 変更節（06 §11） | (a) 反例の除去 | (b) 矛盾 | (c) 新規欠陥 | 判定 |
|---|---|---|---|---|---|
| CD-01 | §3 `P_TIMEOUT_ORDER`（06:258）・§6.1 MIN_FAULT_INJECTION（06:344-345）・(x)（06:280）・OPP-11（06:433） | 元反例（全 timeout = 1e9・jitter = 1e9）は `inter_command_jitter_s < 1/action_rate_hz` で拒否される。**しかし** jitter だけ現実値に戻した反例は通る（§4 R2-01 の再現）。「timeout 駆動 fault は必ず発火させる」（06:345）は、同一 fault code に timeout 以外の trigger 経路がある 2 code（05:279 health==failed／05:275 max_reselect_attempts）で bind せず、CD-10 の `R_LEASE_DURATION_EXCEEDED` は集合に無く、`ack_timeout_s` / `health_confirm_timeout_s` / `manager_liveness_s` は順序式にも集合にも無い | 06:345「必ず発火させる」↔ 05:279/05:275 の同一 code 別 trigger | evidence の内容を audit range と照合する検査が無い（R2-02） | **FINDING**（R2-01 HIGH・R2-02 MEDIUM） |
| CD-02 | §2.1 `clearance_roles` 注記（06:190）・§3 `P_CLEARANCE_ROLE_MISSING`（06:261）・必須 kind に GATEWAY_CONFIG_MATCH（06:260）・05 §5.4 record 型（05:516） | `clearance_roles = ()` → `P_CLEARANCE_ROLE_MISSING`（PT-22）。GATEWAY_CONFIG_MATCH 必須化。元反例は除去 | **05:516 は condition-10 の全 reason（SAFETY 含む・05:518）で `record.role ∈ clearance_roles[reason]` を要求**するが、06:261 は SAFETY を必須集合から外す（「ISL clearance が主・role は AND」）。SAFETY role 未宣言の適法 profile で SAFEHOLD(SAFETY) が解除不能 | — | **FINDING**（R2-05 MEDIUM） |
| CD-03 | §3 `P_EVIDENCE_UNBOUND` (kind × subject)（06:264）・`P_EVIDENCE_EXPIRED`（06:265）・`P_FAULT_CODE_UNKNOWN`（06:266）・§8.1 規則 (2)（06:373）・05 §3.4 (b) | (1) 失効 SAFETY_LAYER_ACCEPTANCE → `P_EVIDENCE_EXPIRED`（PT-23）(2) 1 code のみ → subject 単位で `P_EVIDENCE_UNBOUND`（PT-24）(3) 登録時 = MIN_SHADOW / permit 時 = mode 別。元反例 3 件とも除去 | 05:184 (b) の列挙 = 06:373 第 2 評価点の列挙と一致 | subject 規則が 3 kind にしか無い（R2-16 LOW）；record 内容の未照合（R2-02）；artifact_ref の join key が lease 無し record を排除（R2-07・CD-16 と交差） | OK（残差は別 id） |
| CD-04 | §2.1 `predicate_sha256` / `evaluator_sha256`（06:119 / 06:168）・§3 `P_PREDICATE_HASH_MISMATCH` / `P_TCP_REF_UNRESOLVED`（06:271-272）・05 §3.4 (j)・05 §4.3 SAFETY_RESTRICTION 再検証（05:437） | 元反例（`keepout_pred_v1` の差替）→ 次 permit で `R_PROFILE_MISBOUND`・gateway 評価前に FALSE（PT-25）。除去 | — | **verify-on-resolve が名指しの 3 ref に限られ、`geometry_sha256` / `CalibrationRef.artifact_sha256` / `GatewayConfigRef.artifact_sha256` / `deployment_evidence_policy_hash` / `DeploymentEvidenceRecord.artifact_sha256` には照合規則が無い**（05:184 (j)・05:435 vs 05:437）— CD-04 の反例が zone 幾何で逐語再現（R2-03 HIGH）；evaluator の実行時再照合無し（R2-17 LOW） | OK for the 3 refs / **FINDING**（R2-03） |
| CD-05 | §2.1 注記（06:193）・§3（06:242・null expectation の timing = None 必須・`P_INTRINSIC_OVERRIDE`）・05 §3.4 (g) | (a) SCRIPTED expectation に `action_rate_hz = 50` → `P_INTRINSIC_OVERRIDE`（PT-19）(b) profile 未列挙 skill の lease → (g) `R_PROFILE_MISBOUND`。除去 | 05 INV-12 は LEARNED のみ・06:242 も `tensor_binding_hash ≠ null` のみ評価 — 整合 | — | OK |
| CD-06 | §3（06:247）・05 §3.4 (h) | offer が cell に無い `ee_right` を主張 → (h) `R_PROFILE_MISBOUND`。除去（`resource` dict は不検査だが値語彙が凍結に無い旨 06:143 に明示） | — | — | OK |
| CD-07 | §2.1 force/torque = 監視上限（06:91-92）・§3 `P_ENVELOPE_NONPOSITIVE` / `P_WORKSPACE_EMPTY` / `P_ENVELOPE_EXCEEDS_MEASURED`（06:268-270）・§4（06:286）・(xi)・OPP-5 | `joint_velocity_max = 1e6`・`zones = ()` → `P_WORKSPACE_EMPTY` / `P_ENVELOPE_EXCEEDS_MEASURED`（PT-26）。force/torque は CONTROLLER 項外（05:436 と一致）。除去 | — | (xi) が「predicate を空虚にする」に対し `P_PREDICATE_HASH_MISMATCH` を挙げるが `predicate_refs = ()` には空文（R2-10 LOW）；`P_ENVELOPE_EXCEEDS_MEASURED` を登録時に評価すると MIN_SHADOW 登録が規則 (1) で不能（R2-11 LOW） | OK（残差 LOW） |
| CD-08 | §2.1 閉 enum（06:71-73）・§3 `REQUIRED_CALIBRATION_KINDS`（06:263） | typo kind → codec 拒否（PT-18）。required set が決定的。除去 | 05:377 `EnvelopeTerm.representation: str` は runtime 層の自由文字列のまま（06 の field 削除とは別物・矛盾ではない） | `REQUIRED_CALIBRATION_KINDS(control_mode)` の引数語彙が IdentityKind（LEARNED）と ControlMode（SCRIPTED/WAIT）の混在（写像は一意・欠陥にしない） | OK |
| CD-09 | §2.1 `CONTROLLER_IDENTITY` / `ENVELOPE_READBACK` 定義（06:67-68）・§3 必須集合（06:260）・PT-21 | firmware 更新・tool 交換 → CONTROLLER_IDENTITY / TOOL_IDENTITY fail → `R_HEALTHCHECK_FAILED`。除去 **ただし evaluator が正しく弁別する前提** | — | kind は label であり evaluator 意味論の attest が無い（R2-06 MEDIUM）；cell に controller が 2 台（dual-arm）でも識別は 1 台（R2-04 HIGH）；ISL 自身の identity 無し（R2-09 MEDIUM） | OK for the kinds / **FINDING**（R2-04 / R2-06 / R2-09） |
| CD-10 | §2.1 `lease_max_duration_s`（06:161）・§3（06:258）・§5（06:302）・PT-13・05 INV-33 / §3.7 / `R_LEASE_DURATION_EXCEEDED` / T-32 | WAIT lease + 失効 calibration → `lease_max_duration_s` で SAFEHOLD(TIMING_VIOLATION) → 次 permit で `P_CALIBRATION_EXPIRED`。除去 | 05:714 INV-33「全 lease」↔ 05:281 の from 列は S_LEASE_ACTIVE / S_HEALTH_PENDING のみ（S_BOUNDARY_WAIT は lease 活性・05:262）（R2-12 LOW） | `R_LEASE_DURATION_EXCEEDED` が MIN_FAULT_INJECTION に無い（R2-01 (c)） | OK（残差 LOW / R2-01 に包含） |
| CD-11 | header・`11_` 追加・SHA256SUMS 再生成 | `11_` 実在・SHA 31/31 OK（§0）・06:4 が「verdict は AI verifier のもので human two-key ではない」を明記 | — | — | OK |
| CD-12 | 05 §4.2（06 が SSOT） | 05:428「profile None = 強化なし = frozen 値が執行値；`P_FRESHNESS_RELAXED` ⇔ `p > f`」= 06:246。一致 | — | — | OK |
| CD-13 | 05 INV-22・§4.1 comment | 05:703 INV-22 =「`RuntimeTimeouts` の全 field」；05:359 `t_receive`。06 `RuntimeTimeouts` 13 field = 05 `TimingBinding` の profile 由来 13 field（06:301 写像文と一致・checker O8） | — | — | OK |
| CD-14 | §3 `P_INTRINSIC_OVERRIDE` 再定義（06:240・06:242）・§2.1 bool 注記（06:120・06:170） | `P_INTRINSIC_OVERRIDE` は null-binding expectation の非 null timing で到達可能（PT-19）。bool は保持理由を注記 | — | — | OK |
| CD-15 | §2.2 注記（06:209）・OPP-12 | label = identity event と明記・射影の是非は OPP-12 | — | — | OK |
| CD-16 | §6.1 `artifact_ref`（06:320）・§6.3（06:358） | join key は定義された。**しかし** `(lease_id, seq 範囲)` は lease 無しの audit record（05:667 が明示的に許す・BEFORE_PERMIT check の実行・SAFEHOLD からの fault）を解決できず、stage = BEFORE_PERMIT の必須 check を持つ適法 profile は HEALTH_CHECK_RUN を永久に満たせない | 06:320 ↔ 05:667 / 05:663 | 充足不能な必須集合（(c) 型） | **FINDING**（R2-07 MEDIUM） |
| CD-17 | §8.1（06:373）・OPP-13（06:435）・05 §3.4 (i) | `accepted_profiles` 前提は wired。除去 | — | 受理の逆（revocation / retirement）経路が無く、superseded profile が permit 適格のまま（R2-08 MEDIUM・新規） | OK（新規は別 id） |
| CD-18 | §4（06:295）・OPP-14・05 §4.3（05:435） | LINEAR_INTERPOLATE = 線分・ZERO_ORDER_HOLD = 点・tool 形状 = ISL（OPP-14）。05:435 と一致 | — | ZOH でも物理経路は存在する（05:468 の SafeHold が「計測姿勢で凍結」と述べるとおり robot は setpoint 間を動く）ため点評価は物理的に弱い（R2-19 LOW） | OK（残差 LOW） |

- 循環検査（(c) 型）で見つけた事項: R2-07（artifact_ref の join key）・R2-14（policy 型が許す SHADOW 前 fault 要求）・R2-15（genesis の role が未 key）。到達不能 code = 無し（`P_INTRINSIC_OVERRIDE` は CD-14 で到達可能化）。05 が引き 06 が定義しない enum/field = 無し（§0 cross-reference）。

## 3. PASS 2 — commissioning engineer としての新規 adversarial 構成

1. **全 `P_*` を通しつつ D0 §F / 凍結が禁じる挙動へ至る profile**: R2-01（safety window を 1 h に伸ばす profile）・R2-03（zone 幾何の差替）・R2-06（常時 True evaluator）・R2-04（第 2 腕の controller が未識別）。
2. **cell 状況**: calibration 失効 = CD-03/CD-10 で有界（OK）／ tool 交換 = TOOL_IDENTITY（evaluator 意味論に依存 → R2-06）／ controller firmware 更新 = CONTROLLER_IDENTITY（同上・dual-arm は R2-04）／ **ISL firmware 更新 = 検出手段なし（R2-09）**／ evidence 失効 = `P_EVIDENCE_EXPIRED`（OK・ただし `valid_until = None` を許す → R2-18）／ operator error = genesis の role 未定義（R2-15）／ **registry confusion = 撤回不能な superseded profile（R2-08）**。
3. **DeploymentEvidencePolicy（§6）の穴**: record 内容の未照合（R2-02）・lease 無し record の join 不能（R2-07）・型が許す循環 / 非単調 policy（R2-14）・singleton kind の subject 未定義（R2-16）・無期限 evidence（R2-18）。
4. **「広げる」網羅（06:280）の各経路に live check があるか**: (i)-(ix) = あり（§0 陽性対照照合と 06 §3 表で確認）／ (x) = `P_TIMEOUT_ORDER` は相対順序のみで、絶対上限の代替として置いた evidence 機構が bind しない（R2-01）／ (xi) = envelope・zone は live、**predicate 空虚化は空文**（R2-10）。

## 4. Findings（型 = {id, severity, 候補 doc:line, 凍結 file:line, 再現, 最小修正}）

### R2-01 ［C1 / C4］ HIGH — CD-01 fold の「timeout は commissioning で必ず発火させるから自然に有界」が bind しない（safety window を伸ばす profile が全 `P_*` = 0）

- 候補 doc:line: 06:258（`P_TIMEOUT_ORDER` = 相対順序 5 式）・06:280 (x)（「絶対上限 = OPP-11」）・06:344-345（MIN_FAULT_INJECTION ∪ timeout 駆動 7 code「同一 profile_hash の下で commissioning 時に必ず発火させる」）・06:433 OPP-11／ 05:279・05:324（`R_SAFETY_LAYER_LOST` は「heartbeat 欠落 **/ health == failed**」の両 trigger で同一 code）・05:275・05:327（`R_BOUNDARY_DWELL_EXCEEDED` は「`boundary_dwell_s` 超過 **/ `max_reselect_attempts` 超過**」で同一 code）・05:281・05:325（`R_LEASE_DURATION_EXCEEDED` — MIN_FAULT_INJECTION に無い）・05:364・05:498（`manager_liveness_s` — 順序式にも集合にも無い）。
- 凍結 file:line: D0:318「missed heartbeat ⇒ consumers must **not** assume safety alive」。
- 再現（反例 profile）: `safety_heartbeat_timeout_s = command_deadline_s = 3600`・`inter_command_jitter_s = 0.01`・`decision_max_age_s = permit_ttl_s = ack_validity_s = 1`・`boundary_dwell_s = lease_max_duration_s = ack_timeout_s = health_confirm_timeout_s = manager_liveness_s = 1e9`・`max_reselect_attempts = 1`・`action_rate_hz = 50`。`P_TIMEOUT_NONPOSITIVE`: 全 > 0。`P_DEADLINE_BELOW_PERIOD`: 3600 ≥ 0.02。`P_TIMEOUT_ORDER`（06:258 の 5 式）: 3600 ≤ 3600 ／ 0.01 < 0.02 ／ 1 ≤ 1 ≤ 1e9 ／ 1 ≤ 1e9 ／ 1e9 ≥ 1e9 — 全て真。`P_EVIDENCE_UNBOUND`（06:264）の FAULT_INJECTION_RESULT: `R_SAFETY_LAYER_LOST` は ISL に health == failed を注入して**正当に**取得（05:279）、`R_BOUNDARY_DWELL_EXCEEDED` は候補 1 件を失敗させて取得（05:275）、`R_DEADLINE_MISS` は executor を 1 h 止めて取得、`R_LEASE_DURATION_EXCEEDED` は要求されない。⇒ 全 `P_*` = 0 で受理。効果: (a) CLOSED_LOOP 中に ISL の**heartbeat**が途絶（health 報告は来ない = ISL 自身が死んだ場合）しても (i′) は 1 h 発火せず learned command が出続ける（D0:318 違反・B2-01 fold の空文化）(b) AuthorityManager crash 時、gateway は線形化読みを `manager_liveness_s` まで待つ間 (ii)/(iii) のいずれとも決まらず直近 setpoint を保持し得る（05:498・INV-10 の全域性が「待ち」を定義していない）(c) 失効 calibration の下の lease が 1e9 s 生きる（CD-10 fold の空文化）。
- 反証の自己検討: 「絶対上限は OPP-11 で open と宣言済み」は反証にならない — 06:345 は evidence 機構を**上限の代替**として本文に置き、verifier もそれを CD-01 CONFIRMED→fold の根拠にした（VERIFY_C:49「1e9 秒の timeout は commissioning で発火させられない ⇒ 自然に排除」）。その機構が 2 code で bind せず 1 code が欠け 3 field が対象外である事実は本文から再現できる。
- 最小修正（zero frozen delta）: (1) 06 §6.1 MIN_FAULT_INJECTION += `R_LEASE_DURATION_EXCEEDED`, `R_MANAGER_UNAVAILABLE`。(2) 06 §3 `P_TIMEOUT_ORDER` += `manager_liveness_s ≤ command_deadline_s`（LEARNED / SCRIPTED）・`ack_timeout_s ≤ boundary_dwell_s`・`health_confirm_timeout_s ≤ lease_max_duration_s`。(3) 05 §8 FAULT record の payload に trigger 副因（`HEARTBEAT_TIMEOUT | HEALTH_FAILED`、`DWELL_TIMEOUT | ATTEMPTS_EXHAUSTED`）を持たせ、06 §6.1 で timeout 駆動 code の `subject_ref` を `<code>#<timeout_trigger>` とし timeout 経路そのものの発火を要求。(4) OPP-11 を正直化: 「絶対上限が無い間、`safety_heartbeat_timeout_s` を bound するのは `command_deadline_s` だけであり、それは commissioning の忍耐でしか bound されない」。

### R2-02 ［C5］ MEDIUM — FAULT_INJECTION_RESULT / HEALTH_CHECK_RUN の内容が参照先 audit range と照合されない（CD-01 の bound が装飾になる）

- 候補 doc:line: 06:264（`P_EVIDENCE_UNBOUND` = (kind × subject) の**存在**）・06:320（`artifact_ref` は (lease_id, seq 範囲) に「解決する」のみ）・06:353（「deployment evidence は存在・有効期限・束縛だけを持ち」）・06:358／ 05:657-671（`RuntimeAuditRecord` は `kind` / `fault` / `profile_hash` / `lease_id` を持つ = 機械照合可能）。
- 凍結 file:line: A:537（U14 は runtime 記録の **hash 方式**を defer するのであって内容の照合を禁じない）。
- 再現: `DeploymentEvidenceRecord(kind=FAULT_INJECTION_RESULT, subject_ref="R_DEADLINE_MISS", profile_hash=H, artifact_ref=(L1, 10..20))` を登録。audit seq 10..20 に FAULT record が 1 件も無くても 06:264 は満たされる（存在・subject 一致）。HEALTH_CHECK_RUN も同型（range に当該 `check_id` の HEALTH_CONFIRMED / HEALTH_FAILED が無くてよい）。
- 最小修正（zero frozen delta）: 第 2 評価点で `P_EVIDENCE_UNBOUND` を拡張（または `P_FAULT_EVIDENCE_UNVERIFIED`）: 解決した range に `kind = FAULT ∧ fault == subject_ref ∧ profile_hash == record.profile_hash` の record が ≥ 1（HEALTH_CHECK_RUN は `kind ∈ {HEALTH_CONFIRMED, HEALTH_FAILED} ∧ payload.profile_health_checks ∋ check_id`）。

### R2-03 ［C2 / C3］ HIGH — content-hash の verify-on-resolve が CD-04 の 3 ref に限られ、zone 幾何・calibration・gateway config・evidence policy は名前解決のまま（CD-04 の反例が逐語再現）

- 候補 doc:line: 06:106（`ZoneRef.geometry_sha256`）・06:176（`CalibrationRef.artifact_sha256`）・06:182（`GatewayConfigRef.artifact_sha256`）・06:224（`deployment_evidence_policy_hash`）・06:321（`DeploymentEvidenceRecord.artifact_sha256`）・06:271（`P_PREDICATE_HASH_MISMATCH` の対象 = `predicate_refs` / `evaluator_ref` のみ）／ 05:184 (j)（照合対象 = `predicate_refs` / `evaluator_ref` / `tcp_offset_ref` の 3 つ）・05:435（DEPLOYMENT_WORKSPACE 行に評価前再検証の文が**無い**）vs 05:437（SAFETY_RESTRICTION 行には有る）。
- 凍結 file:line: B:255（凍結側は content-addressed store を**要求しない** ⇒ 宣言 hash は解決時に照合して初めて束縛になる）・B:23（identity は preimage が束縛するものが決める）。
- 再現: profile P の `workspace.zones = (ZoneRef(zone_id="ko_1", kind=KEEP_OUT, geometry_sha256=X, frame_ref=base),)` を登録・lease 活性。resolver が `ko_1` に返す幾何 artifact を空多角形に編集。`profile_hash` 不変・lease 不変・05:184 (j) は geometry を列挙せず・05:435 は評価前に照合しない ⇒ gateway は旧 keep-out 内の setpoint を admit。`DeploymentEvidencePolicy` artifact の差替も同型（required 集合が縮み、`P_EVIDENCE_UNBOUND` は差替後の policy に対して評価される）。
- 最小修正（zero frozen delta）: 05 §3.4 (j) と 06 §3 `P_PREDICATE_HASH_MISMATCH` を「profile が宣言する**全** content hash」に一般化（`geometry_sha256` / `CalibrationRef.artifact_sha256` / `GatewayConfigRef.artifact_sha256` / `deployment_evidence_policy_hash == H_WCJ(解決した policy)` / required record の `artifact_sha256`）；05 §4.3 DEPLOYMENT_WORKSPACE 行に SAFETY_RESTRICTION 行と同じ「gateway は評価前に geometry sha256 を再検証・不一致 = FALSE」を追記。

### R2-04 ［C4］ HIGH — profile schema は robot を 1 台しか識別・envelope・tool 束縛・calibration できないのに両腕 cell の宣言を許す（第 2 controller の firmware 更新が検出不能・第 2 腕の envelope 項が未定義）

- 候補 doc:line: 06:76-83（`CellIdentity`: `robot_serial_ref` / `controller_firmware_ref` は各 1 つ）・06:86-93（`ControllerEnvelope` 1 つ・tuple 長 = 1 台分）・06:96-101（`ToolPayloadSpec` / `tcp_offset_ref` 1 つ）・06:267（`P_ENVELOPE_SHAPE` = `robot_model_ref` の関節数）・06:68（CONTROLLER_IDENTITY =「controller が報告する serial / firmware」= 単数）・06:365（`ResourceAvailability.control` で両腕 True は「cell に両腕がある事実の宣言」として**許容**）／ 05:405・05:417（lease.ownership は両腕を持ち得る・1 executor）。
- 凍結 file:line: A:151 `ControlResourceSpec = {ee_left, ee_right, gripper_left, gripper_right: bool}`；project 不変前提 DUAL-ARM（RS71 §0・CLAUDE.md L3 keyword）— 対象 cell は UR15 × 2。
- 再現: `resource_availability.control = {ee_left: T, ee_right: T, gripper_left: T, gripper_right: T}`・`CellIdentity.robot_serial_ref = "UR15-A"`・`controller_firmware_ref = "5.14"`・`controller.joint_velocity_max` = 6-tuple。全 `P_*` = 0（`P_ENVELOPE_SHAPE`: 6 == UR15）。右腕 controller B の firmware を更新: CONTROLLER_IDENTITY は controller A のみを照合 ⇒ pass ⇒ permit ⇒ 両腕 ownership の CLOSED_LOOP lease。右腕 command の CONTROLLER 項: envelope は 1 つしか無く、どの robot に適用するかを 05 / 06 は定めない（05:439「評価不能 = FALSE」なら右腕は運転不能、そうでなければ A の envelope が黙って B に適用）。
- 反証の自己検討: 「合成は OUT（§7）」は反証にならない — 06:365 が両腕宣言を合成でないと明言し、frozen `ControlResourceSpec` が両腕を 1 lease に持たせる。「1 腕 1 profile」は反証にならない — lease は `profile_hash` を 1 つしか束縛しない（05:406）。
- 最小修正（Rs 裁定・いずれも zero frozen delta）: (A) 最小 = profile schema 1.0 を単 robot cell と宣言し `P_MULTI_ROBOT_UNSUPPORTED`（`resource_availability.control.ee_left` / `ee_right` のちょうど 1 つが True）を追加、dual-arm profile を OPP に登録；(B) 06 型変更（deployment 層）= `CellIdentity.robots: tuple[RobotIdentity, ...]`、controller / tool_payload / TCP calibration を frozen 資源 id（ee_left / ee_right）で key、`P_ENVELOPE_SHAPE` を robot ごと、CONTROLLER_IDENTITY / TOOL_IDENTITY / ENVELOPE_READBACK / calibration kinds を True な全腕に要求、CONTROLLER 項を腕ごとに評価。

### R2-05 ［C4］ MEDIUM — 05:516 は SAFEHOLD(SAFETY) の解除に profile role を必須化し、06:261 は SAFETY role の宣言を任意にする（適法 profile で SAFETY hold が解除不能）

- 候補 doc:line: 05:516（「該当」= `record.role ∈ profile.escalation.clearance_roles[reason]` を condition-10 の全 reason に適用）・05:518（集合に SAFETY を含む）／ 06:261（`P_CLEARANCE_ROLE_MISSING` の必須集合 = {SAFE_STOP, CHECKPOINT_DISABLED, MANAGER_RESTART, GATEWAY_RESTART}・「SAFETY は ISL clearance record が主・role は AND」）・06:190。
- 凍結 file:line: D0:273-274（fail-closed は producer retains + safe-stop — 解除者未定義 = 永久 hold）・D0:320（`stabilized_post_action_state` = ISL 側の復帰入力）。
- 再現: `clearance_roles = ((SAFE_STOP,"so"),(CHECKPOINT_DISABLED,"so"),(MANAGER_RESTART,"so"),(GATEWAY_RESTART,"so"))` ⇒ 06:261 通過。ISL STOP ⇒ `S_SAFEHOLD(SAFETY)`。ISL が clearance record を書く。CAS 条件 10（05:516）: `record.role ∈ clearance_roles[SAFETY] = ∅` ⇒ FALSE（05:678 既定）⇒ 当該 profile_hash では TRANSFER_TO_EXECUTOR 不能・解除には新 profile 登録が要る。
- 最小修正（text-only）: どちらかを選び両 doc に写す — 05:516 に「`reason == SAFETY`: role 項は profile が SAFETY の role を ≥ 1 宣言する場合のみ AND、未宣言なら ISL clearance record のみ」を追記、または 06:261 の必須集合に SAFETY を加える。

### R2-06 ［C4］ MEDIUM — `HealthCheckKind` は label であり evaluator の意味論が attest されない（常時 True の evaluator で必須 kind を全て満たせる）

- 候補 doc:line: 06:164-170（kind + `evaluator_ref` / `evaluator_sha256` — 意味は evaluator が持つ）・06:260（`P_HEALTHCHECK_MISSING` = kind label の存在検査）・06:399（PT-21 は evaluator が弁別する前提）・06:431（OPP-9 evaluator registry = open）・06:280（evidence へ委ねる物理事実の列挙に evaluator の正しさが無い）。
- 凍結 file:line: D0:318（失敗し得ない信号から alive を仮定しない — 失敗し得ない check にも同じ原理）。
- 再現: 必須 6 kind の各 `evaluator_ref → "always_true_v1"`（`evaluator_sha256` は正しく一致）。全 `P_*` = 0、HEALTH_CHECK_RUN ∀ check_id 存在（実行され pass した）。firmware 更新 + tool 交換 ⇒ 全 check pass ⇒ permit。PT-21 の期待 `R_HEALTHCHECK_FAILED` は起きない。
- 最小修正（zero frozen delta）: 06 §6.1 に、必須 kind の各 check_id について **negative-control** の HEALTH_CHECK_RUN（誘発した不一致の下で `R_HEALTHCHECK_FAILED` / HEALTH_FAILED を含む audit range を指す record）を CLOSED_LOOP lease 前に要求；§3 / OPP-9 に「kind 認証済 evaluator registry が無い間、`P_HEALTHCHECK_MISSING` は label 検査である」と明記。

### R2-07 ［C5］ MEDIUM — `artifact_ref → (lease_id, seq 範囲)` は lease 無しの audit record を解決できず、stage = BEFORE_PERMIT の必須 check を持つ適法 profile は HEALTH_CHECK_RUN を永久に満たせない

- 候補 doc:line: 06:320（HEALTH_CHECK_RUN / FAULT_INJECTION_RESULT の `artifact_ref` は (lease_id, seq 範囲) に解決）・06:264（HEALTH_CHECK_RUN ∀ `health_checks[].check_id`）・06:260（必須 kind の stage ∈ {BEFORE_PERMIT, BOTH}）／ 05:184 (a)（BEFORE_PERMIT check は permit 発行前 = lease 不在で実行）・05:663（`lease_id: str | None`）・05:667（deployment 起因 record は lease 無し・`profile_hash` で join）。
- 凍結 file:line: —（05:667 という runtime 層の規則と 06:320 の内部矛盾）。
- 再現: `HealthCheckSpec(check_id="ci_1", kind=CONTROLLER_IDENTITY, stage=BEFORE_PERMIT, …)` は 06:260 を満たす。その実行 record は `lease_id = None`（05:667）。HEALTH_CHECK_RUN(subject_ref="ci_1") の `artifact_ref` は 06:320 により (lease_id, seq) を要する ⇒ 整形不能 ⇒ 全 CLOSED_LOOP permit で `P_EVIDENCE_UNBOUND`。SAFEHOLD から発火させる `R_PERMIT_EXPIRED` / `R_ACK_TIMEOUT` / `R_GATEWAY_RESTART` の FAULT_INJECTION_RESULT も同型。
- 最小修正（text-only）: 06:320 を「`(lease_id | None, seq 範囲)` かつ `record.profile_hash == profile_hash`（05:667 の join key を写す）」へ。

### R2-08 ［C3 / C10］ MEDIUM — 受理済 profile の撤回（revocation / retirement）経路が無く、superseded profile が permit 適格のまま残る（registry confusion）

- 候補 doc:line: 06:373 規則 (2)/(3)（受理 = `accepted_profiles` へ追加・受理後の事実変化は profile を変えない）・06:300（in-place 更新なし・新 profile = 新 lease のみ）・06:435（OPP-13 = 所在・受理権限・永続化のみ）／ 05:184 (i)（`profile_hash ∈ accepted_profiles` のみ）。
- 凍結 file:line: D0:273-274（fail-closed の解除規則は reset authority の定義を前提）・A:39（certificate は runtime 事象で無効化されない ⇒ deployment の退役は profile registry にしか置けない）。
- 再現: profile A（`layout_revision_ref="r1"`・keep-out Z1）受理 → layout 変更 → profile B（"r2"・Z2）受理。A は `accepted_profiles` に残る。operator（または stale な orchestrator 設定）が `profile_hash = H(A)` で permit 要求: 05:184 (i) 通過、A の CELL_COMMISSIONING / SAFETY_LAYER_ACCEPTANCE は valid_until 内、layout を感知する health check は存在し得ない ⇒ 旧 keep-out Z1 の下で CLOSED_LOOP lease。
- 最小修正（zero frozen delta）: 06 §8.1 に registry 状態 {ACCEPTED, REVOKED}（撤回 = clearance_roles の role による CLEARANCE_RECORDED 型 record + 撤回 profile_hash）を置き、05 §3.4 (i) を `∈ accepted_profiles ∧ ∉ revoked_profiles` に；撤回権限は OPP-13 へ。

### R2-09 ［C4 / C5］ MEDIUM — IndependentSafetyLayer 自身の identity / 版が profile に無く、ISL firmware 更新が検出不能

- 候補 doc:line: 06:76-83（`CellIdentity` に ISL の識別子が無い）・06:67（SAFETY_LAYER_HEARTBEAT = liveness のみ）・06:311-312・06:319（SAFETY_LAYER_ACCEPTANCE の `subject_ref` が未定義）・06:354。
- 凍結 file:line: D0:310-318（ISL は別 owner の module・独自の health を持つ ⇒ その変更は安全機能の変更）。
- 再現: ISL v1 を受入（SAFETY_LAYER_ACCEPTANCE・valid_until = +1y）。ISL を v2（STOP 閾値変更）に更新。profile は変える field が無く不変、SAFETY_LAYER_HEARTBEAT pass、`P_EVIDENCE_UNBOUND` / `P_EVIDENCE_EXPIRED` pass ⇒ 未受入の安全層の下で CLOSED_LOOP permit。
- 最小修正（06 型変更・zero frozen delta）: `CellIdentity.safety_layer_ref: str`；`HealthCheckKind += SAFETY_LAYER_IDENTITY`（報告版 == `safety_layer_ref`）を必須集合に；`SAFETY_LAYER_ACCEPTANCE.subject_ref == safety_layer_ref`。

### R2-10 ［C1］ LOW — (xi)「predicate を空虚にする」に対する check が空文（`predicate_refs = ()` に `P_PREDICATE_HASH_MISMATCH` は何も比較しない）

- 候補 doc:line: 06:280 (xi)・06:117-119（`predicate_refs` は空を許す）・06:404（PT-26 は `predicate_refs = ()` に predicate 系 code を期待しない）。
- 再現: `SafetyRestrictionSet(restriction_ids=(), predicate_refs=(), predicate_sha256=(), requires_safety_layer_health=True)` ⇒ 全 `P_*` = 0。
- 最小修正: `P_SAFETY_RESTRICTION_EMPTY`（≥ 1 predicate）を足すか、(xi) を「predicate 集合は空を許す（静的 restriction 無し = ISL のみ）；空虚化の検査対象は zone / envelope」と書き直す。

### R2-11 ［C5］ LOW — `P_ENVELOPE_EXCEEDS_MEASURED` を登録時に評価すると MIN_SHADOW だけの profile は規則 (1) で登録不能

- 候補 doc:line: 06:270（登録時に CONTROLLER_ENVELOPE_MEASUREMENT record と照合）・06:264（登録時 evidence 集合 = MIN_SHADOW）・06:342（MIN_SHADOW は当該 kind を含まない）・06:373 規則 (1)（評価不能 = 違反）。
- 再現: evidence = MIN_SHADOW のみの profile ⇒ 測定 record 無し ⇒ 評価不能 ⇒ 違反 ⇒ 登録拒否。
- 最小修正: 「`P_ENVELOPE_EXCEEDS_MEASURED` は第 2 評価点・`proposed_lease.mode == CLOSED_LOOP_AUTHORITY` で評価」と明記（または MIN_SHADOW に kind を追加）。

### R2-12 ［C3］ LOW — INV-33「全 lease」と執行行（S_LEASE_ACTIVE / S_HEALTH_PENDING のみ）の不一致

- 候補 doc:line: 05:714（INV-33）・05:281（from 列に S_BOUNDARY_WAIT が無い）・05:262（S_BOUNDARY_WAIT は lease 活性）。
- 再現: `t0` 活性 → `t0 + lease_max − ε` で TERMINAL → boundary wait ≤ `boundary_dwell_s` ⇒ 活性時間 > `lease_max_duration_s` だが `R_LEASE_DURATION_EXCEEDED` は発火しない。安全側（gateway = SafeHold）だが INV の字面は機械検査と一致しない。
- 最小修正: 05:281 の from 列に S_BOUNDARY_WAIT を足すか INV-33 を限定する。

### R2-13 ［C9］ LOW — `WorkspaceRestriction.frame_ref: str = ""` に default があり strict codec / no-default 規律と矛盾

- 候補 doc:line: 06:113・06:120 / 06:170（「省略時 default を持たせない」）・06:230（B-declared 明示 null）。凍結: B:24。
- 再現: `frame_ref` を省略して encode ⇒ codec は欠落として拒否すべきところ default で `""` に decode され、後段の `P_FRAME_MISMATCH` で拒否される。
- 最小修正: default を外す（`frame_ref: str`）。

### R2-14 ［C5］ LOW — policy 型が許す循環（SHADOW 前に lease 由来 evidence を要求）と非単調（CLOSED_LOOP 集合 ⊉ SHADOW 集合）

- 候補 doc:line: 06:329（`must_be_exercised_before` に SHADOW_NON_AUTHORITY 可）・06:334-335（`required_before_shadow_lease` は MIN_SHADOW 以上なら自由・包含関係の規則なし）・06:264（HEALTH_CHECK_RUN / FAULT_INJECTION_RESULT は lease を要する）。
- 再現: (a) `required_before_shadow_lease = MIN_SHADOW ∪ {HEALTH_CHECK_RUN}` ⇒ `P_EVIDENCE_POLICY_TOO_WEAK` 通過；最初の SHADOW permit は lease 由来の record を要し永久不成立。(b) SHADOW 集合の extra kind が CLOSED_LOOP 集合に無い policy が表現可能。
- 最小修正: `P_EVIDENCE_POLICY_CIRCULAR`（HEALTH_CHECK_RUN / FAULT_INJECTION_RESULT ∉ `required_before_shadow_lease`・`must_be_exercised_before == CLOSED_LOOP_AUTHORITY` のみ）と `P_EVIDENCE_POLICY_NONMONOTONE`（closed_loop ⊇ shadow）。

### R2-15 ［C4 / C10］ LOW — genesis record を書く role が `clearance_roles` に key されていない

- 候補 doc:line: 05:141（genesis = 「profile の clearance 手続（doc 06 `EscalationPolicy.clearance_roles`）」）・06:190（key = SAFETY / SAFE_STOP / CHECKPOINT_DISABLED / MANAGER_RESTART / GATEWAY_RESTART）・06:261。
- 再現: INITIAL / genesis は key に無く、05:516 の `record.profile_hash == permit.profile_hash` にも permit が無い ⇒ 厳密読み = 有効な genesis 無し／緩い読み = 任意 operator。
- 最小修正: 06:190 / 06:261 に GENESIS（または MANAGER_RESTART 流用）を key として追加し、05:141 に genesis record が authorize する profile_hash を持つと明記。

### R2-16 ［C5］ LOW — singleton 5 kind の subject 規則が未定義

- 候補 doc:line: 06:264（subject 規則 = CALIBRATION_RECORD / HEALTH_CHECK_RUN / FAULT_INJECTION_RESULT のみ）・06:319（subject 語彙は comment）・06:373 規則 (1)。
- 再現: TOOL_PAYLOAD_IDENTIFICATION の `subject_ref = "old_tool"`・profile の `tool_id = "new_tool"`・profile_hash 一致 ⇒ (kind × subject) の「存在」は未定義。
- 最小修正: kind ごとに `subject_ref ==` 対応 profile field（cell_id / controller_firmware_ref / tool_id / config_id / safety_layer_ref〔R2-09〕）を定義。

### R2-17 ［C3］ LOW — evaluator の content sha は permit 発行時のみ照合され、AT_HEALTH_CONFIRMATION / BOTH の実行時には再照合されない

- 候補 doc:line: 06:168（第 2 評価点で照合）・06:271・05:184 (j)・06:69（AT_HEALTH_CONFIRMATION / BOTH は CAS 後に実行）。
- 再現: permit 発行（sha 一致）→ resolver の evaluator 内容を差替 → CAS → HealthConfirmation は差替後の evaluator を実行（窓 ≤ `permit_ttl_s + health_confirm_timeout_s`）。predicate 項には評価前再照合（05:437）が付いたが evaluator には無い。
- 最小修正: 06:168 / 05 §3.5 に「各実行の直前に再照合・不一致 = check FAILED（fail_closed）」。

### R2-18 ［C4 / C5］ LOW — calibration / 受入 evidence の無期限（`valid_until = None`）が表現可能で、CD-03 の失効検査が当該 profile では到達不能

- 候補 doc:line: 06:177（`CalibrationRef.valid_until_iso8601: str | None`）・06:324（record 同）・06:263 / 06:265（有限値のみ発火）。
- 再現: TCP calibration と SAFETY_LAYER_ACCEPTANCE の `valid_until = None` ⇒ `P_CALIBRATION_EXPIRED` / `P_EVIDENCE_EXPIRED` は永久に発火しない。
- 最小修正: `P_VALIDITY_UNBOUNDED`（`CalibrationRef` と CALIBRATION_RECORD / SAFETY_LAYER_ACCEPTANCE の required record は非 None 必須）、または理由付きで OPP 登録。

### R2-19 ［C2］ LOW — ZERO_ORDER_HOLD の点評価は物理経路を見ない（robot は setpoint 間を動く）

- 候補 doc:line: 06:295 / 05:435（ZOH = 点・LINEAR_INTERPOLATE = 線分）・05:425（hold は admitted 間）・05:468（SafeHold(DIFF_IK) =「計測現在姿勢で凍結」— robot が setpoint 間を物理的に移動している事実を 05 自身が前提にしている）。凍結: B:40（`ActionHold` は policy step 間の保持規約であり物理運動の記述ではない）。
- 再現: hold = ZOH の LEARNED skill、`p_prev`（外）と `p_new`（外）の間に keep-out 板 ⇒ DEPLOYMENT_WORKSPACE は `p_new` のみ評価 ⇒ admit ⇒ controller が板を貫通して移動。
- 最小修正: 06 §4 / 05 §4.3 を「hold 種別にかかわらず（直前 admitted or 計測 TCP → 新 setpoint）の線分を評価。tool hull は OPP-14 のまま」に。

## 5. 主張しないこと

- gate PASS / two-key / Rs 裁定を主張しない。本 report は 08 §6 の準備レビュー。
- 凍結 4 file への変更を要求しない（全 fix は 05 / 06 の新語層・R2-04 (B) と R2-09 は 06 自身の deployment 層型の変更）。
- impl / training / closed-loop authority は CLOSED のまま。`eval_runs/` 配下は編集していない（`git status --short` = 空・§0）。
- 前 round の既 fold 項目（CD-01..18・A/B 系）は再報告していない。PASS 1 表で FINDING とした行は「fold が反例を除去したか」「fold が新たな矛盾・充足不能・循環を持ち込んだか」の再検証結果である。

— R2・2026-09-04 22:41:07 UTC
