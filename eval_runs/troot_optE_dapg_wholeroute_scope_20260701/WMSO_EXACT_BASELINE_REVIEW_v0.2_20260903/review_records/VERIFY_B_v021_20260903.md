# VERIFY_B_v021 — finding group B（reviewer B・21 件）の 3 lens 反証検証 + v0.2.2 fold 状態

- 検証者 = adversarial verifier（別 context）。実施 = 2026-09-04 16:1x UTC（`date -u` 実測 16:10:32 UTC 以降）。
- 対象版: 前提検証 = v0.2.1 候補（`scratchpad/design/fold/backup_v021_pkg/` の 05 = 792 行・06 = 456 行を全読）／ fold 検証 = v0.2.2（`$R = eval_runs/troot_optE_dapg_wholeroute_scope_20260701/WMSO_EXACT_BASELINE_REVIEW_v0.2_20260903/` の 05 = 893 行・06 = 474 行・v0.2.1 → v0.2.2 の unified diff 505 + 116 行を全読）。
- 規則: text / logic / impact の 3 lens を各 finding に独立適用。既定 = refuted。2 lens 以上が未反証のときのみ confirmed。
- 記法: `A:` = frozen D1.1-A `WMSO_D11A_CONTRACTS_V2_DESIGN_RSTECHLEAD2_20260719.md`、`B:` = frozen D1.1-B `WMSO_D11B_TENSOR_BINDING_DESIGN_RSTECHLEAD2_20260720.md`、`D0:` = `WMSO_D0_ARCHITECTURE_DRAFT_RSTECHLEAD2_20260718.md`、`prereg:` = `WMSO_D11BC_SLICE_SCOPE_PREREG_RSTECHLEAD2_20260720.md`、`05:` / `06:` = v0.2.1 候補、`v0.2.2 05:` / `v0.2.2 06:` = fold 後。

## 0. 共通前提の実測

- pin 再測: `bash $R/verify_exact_baseline_pins.sh` → **11/11 PASS**（D1.1-A `00192d20…` @ `54f90a7d`・blob `13534302…`／ D1.1-B `5a1874d3…` @ `07250f4a`・blob `ebe8154a…`／ EP md `c474acea…`／ EP JSON `e63176af…`／ definition hash `e7ca4309…`・policy_semver 1.9.0／ HEAD `f3c6ca35` branch `claude/v0-2-design-candidates-snqbmy`）。出力 = `scratchpad/review/verifyB_pin_output.txt`。
- 機械検査（v0.2.2 checker）: v0.2.2 05 `--runtime` FAIL=0 WARN=10 ／ v0.2.2 06 FAIL=0 WARN=23 ／ v0.2.1 05 `--runtime` FAIL=0 WARN=6 ／ v0.2.1 06 FAIL=0 WARN=19。
- 陽性対照 `10_` の注入欠陥は本群の finding に含まれない（各 finding の前提文を v0.2.1 実体で sed 確認済 — 下記各節）。
- 群内の完全重複: **無し**（B-H3 と B-L2 は `safety_heartbeat_timeout_s` で接するが、B-H3 = runtime 遷移の欠落・B-L2 = 06 field 欠落で軸が異なる）。merged_into は全件 ""。
- INV-27 機械再現（自作 script）: v0.2.1 = enum 33 member（`05:561-572`）のうち失敗表（`05:278-309`）に fault 列を持たないもの = `R_CHECKPOINT_SWITCH_DISABLED / R_DEADLINE_MISS / R_ENVELOPE_EMPTY / R_ENVELOPE_VIOLATION / R_EPOCH_STALE_COMMAND / R_LEASE_INVALIDATED`（6 件）。v0.2.2 = 37 member・欠落 0。

## 1. 各 finding

### B-H1（HIGH・confirmed・fold = RESOLVED）
- v0.2.1 実体: `05:82` CommandGateway の「持つ状態」= 「admission 用の `AuthorityState` 読み（線形化）」／ `05:233` 「CommandGateway の admission は `AuthorityState` を線形化して読むため」／ `05:312` 証明 A 同旨。一方 `05:344` `manager_liveness_s` = 「gateway が AuthorityState の**鮮度**を要求する上限」／ `05:456` 「`manager_liveness_s` 内に**新鮮な** `AuthorityState` を読めない場合」／ `05:649` INV-19 「`manager_liveness_s` 内の `AuthorityState` を**持たない**とき」。
- frozen: `A:382` 旧 epoch command 拒否 = authority manager の状態所有責務／ `D0:270-272` exactly one owner at every instant。
- text: 未反証。線形化読み（3 箇所）と「鮮度・持つ」（3 箇所）が併存し、後者は age を持つ cache copy を含意する。
- logic: 未反証。cache 読みの下では finding の interleaving（t0 read s0 → t1 CAS → t2 < t0 + manager_liveness_s で X1 の (L1,e) が s0 で admit）が成立し `05:234`「即座に出力から外れる」に反する。証明 A は線形化読みを前提にするが、INV-19 の文言はその前提を壊す実装を許す。
- impact: HIGH。handoff 決定 C.3/C.4（単一 CAS・旧 epoch 拒否は同一 CAS）と P1 の中心に関わる曖昧さで、slice = SHADOW でも設計書としての主張が二義。
- v0.2.2 fold: `v0.2.2 05:82` 「admission = manager 所有 `AuthorityState` の**同期・線形化読み**（cache 不可 — v0.2.2・B-H1）… `manager_liveness_s` は「線形化読みを待つ上限」」／ `v0.2.2 05:362` `manager_liveness_s` comment = 「gateway が線形化読みを待つ上限（超過 = (iii)）」／ `v0.2.2 05:495` 「admission は `AuthorityState` の同期・線形化読みで行う（cache を持たない）。読みが `manager_liveness_s` 内に完了しない場合、(ii) を評価せず (iii)」／ `v0.2.2 05:693` INV-19 同旨。→ RESOLVED（finding の minimal fix と同一内容）。

### B-H2（HIGH・confirmed・fold = RESOLVED）
- v0.2.1 実体: `05:216-228` TRANSFER_TO_EXECUTOR 前提条件 1-10 に `expected.owner.kind` / `expected.boundary_wait` への条件無し（条件 1 は「tuple 等値」のみ）／ `05:181` permit 発行前提 (a)-(c) も同様／ `05:261` 「表外遷移は存在しない」／ `05:265` 遷移表の from 列は `S_SAFEHOLD` / `S_BOUNDARY_WAIT` のみ／ `05:651` INV-21 = Orchestrator 側「設計違反（test）」。
- frozen: `prereg:59` boundary = TERMINAL のみ・checkpoint 途中切替 = 別 gate／ `D0:273-274`。
- text: 未反証。logic: 未反証 — s = (EXECUTOR(X1), e, L1, boundary_wait=False) を expected とする permit は条件 1-10 を全て満たし SUCCESS。遷移表は projection であって enforcement ではない。`05:276` の「新 instance が B0 から再開」は複数 Orchestrator instance の存在を認めている。impact: HIGH（slice prereg の boundary-only を authority 層が保証していない・「表外遷移は存在しない」が偽）。
- v0.2.2 fold: `v0.2.2 05:184` (d) boundary guard（permit 不発行・`R_MIDSKILL_TRANSFER_BLOCKED`）／ `v0.2.2 05:232` 条件 11 同旨（REJECTED）／ `v0.2.2 05:702` INV-28／ `v0.2.2 05:737` T-26／ 失敗表 `v0.2.2 05:322`。S_HEALTH_PENDING（EXECUTOR ∧ boundary_wait=False）も阻止され遷移表と整合。→ RESOLVED。

### B-H3（HIGH・confirmed・fold = RESOLVED）
- v0.2.1 実体: `05:449` (i) は「未解除 SafetyDecision があれば」のみ／ `05:429` §4.5 trigger 列に ISL heartbeat 喪失無し／ `05:227` 条件 9 は転送時のみ／ `05:263-276` に該当行無し／ `grep -n heartbeat` = `05:64/85/227/271/429/672/714/718/721` — `271/429/672` は executor heartbeat、他は D0 引用・OP。`06:67-68` HealthCheckStage ∈ {BEFORE_PERMIT, AT_HEALTH_CONFIRMATION, BOTH}（lease 中の段無し）／ `06:117` `requires_safety_layer_health: True 必須` は静的 flag で runtime 述語の定義無し（`05:408` SAFETY_RESTRICTION term は「静的」・live 決定は (i)）。
- frozen: `D0:318` 「missed heartbeat ⇒ consumers must not assume safety alive; monitor failed ⇒ system fails to safe-stop」。
- text / logic / impact: いずれも未反証。S_LEASE_ACTIVE 中に ISL が停止すると (i) 不在 → (ii) 継続。HIGH（D0 §F の明示要求に反する安全欠落）。
- v0.2.2 fold: `v0.2.2 05:488` (i′)「ISL heartbeat が `safety_heartbeat_timeout_s` を超えて欠落 → SafeStop（(ii) を評価しない）」／ `v0.2.2 05:279` 遷移行 any(EXECUTOR) → TRANSFER_TO_SAFEHOLD(SAFETY)・FAULT(R_SAFETY_LAYER_LOST)・DISPOSITION(SAFE_STOP)／ `v0.2.2 05:323` 失敗行／ `v0.2.2 05:703` INV-29／ `v0.2.2 05:738` T-27／ `v0.2.2 05:363` `TimingBinding.safety_heartbeat_timeout_s`／ `v0.2.2 06:152` `RuntimeTimeouts.safety_heartbeat_timeout_s`。→ RESOLVED。付記（採点外・字句）: `v0.2.2 05:684` INV-10 と `v0.2.2 05:332` 証明 B は依然「(i)(ii)(iii)」と列挙し (i′) を名指ししない — (i′) は (ii) より先に評価される sub-case で機構上の穴ではない。

### B-M1（MEDIUM・confirmed・fold = PARTIAL）
- v0.2.1 実体: `05:237` 「TRANSFER_TO_SAFEHOLD が CONFLICT した場合、AuthorityManager は current を再読して**即座に再試行**する … 再試行中も gateway 出力は §5.2 (i) または (iii) に既に落ちているため … (INV-24)」／ `05:654` INV-24 同旨／ `05:271` EXECUTOR_LOST は any(EXECUTOR) からの lease-scoped trigger。
- text: 未反証。logic: 未反証 — s = (EXECUTOR(X1), e, L1, bw=True) で X1 heartbeat 喪失と TRANSFER_TO_EXECUTOR(X2, L2) が競合 → safehold CAS CONFLICT → 再読・無条件再試行 → (SAFEHOLD(EXECUTOR_LOST), e+2) で健全な L2 を無効化・audit は L2 に R_EXECUTOR_LOST。また EXECUTOR_LOST は manager 検出ゆえ、CAS が landing するまで gateway は (ii)（hold 地平内の admitted command）を出し続け、INV-24 の「CONFLICT 中は (i) または (iii)」は偽。impact: MEDIUM（誤無効化 + 偽 audit + INV-24 の過剰主張）。
- v0.2.2 fold: `v0.2.2 05:241` 「**lease-scoped な理由**（EXECUTOR_LOST / HEALTH_FAIL / DEADLINE_MISS / TIMING_VIOLATION / ENVELOPE_VIOLATION / CHECKPOINT_DISABLED / NO_CHAIN）の再試行は `current.active_lease_id == trigger.lease_id` のときのみ … 不一致なら CAS を捨て元 lease への FAULT のみ記録。non-lease-scoped … は無条件に再試行」→ 誤無効化は解消。
- **残る gap（PARTIAL の根拠）**: `v0.2.2 05:698` INV-24 は不変（「CONFLICT 中の出力は (i) または (iii)」）、`v0.2.2 05:241` の「再試行中も gateway 出力は §5.2 (i) または (iii) に既に落ちている」も不変 — manager 検出 trigger（EXECUTOR_LOST）では CAS landing まで (ii) が残るため依然偽。finding の fix 後半（INV-24 の改訂）が未 fold。
- minimal fix（残差）: INV-24 と `05:241` の該当文を「(i) / (iii)、または manager 検出 trigger では CAS landing まで ≤ `command_deadline_s` の既 admit command」に改訂。schema delta 0。

### B-M2（MEDIUM・confirmed・fold = PARTIAL）
- v0.2.1 実体: `05:237` TRANSFER_TO_SAFEHOLD 事後条件 = 「(SAFEHOLD, epoch+1, None, consumed そのまま, **reason**, False, seq+1)」（reason 置換）／ `05:274` any → TRANSFER_TO_SAFEHOLD(MANAGER_RESTART)／ `05:228` 条件 10／ `05:464-470` §5.4: SAFETY = ISL clearance record、MANAGER_RESTART = durable 整合検査記録のみ。
- frozen: `D0:318`, `D0:320`。
- text / logic: 未反証 — SAFEHOLD(SAFETY) 中の manager 再起動で reason = MANAGER_RESTART となり、§5.4 の追加前提が ISL clearance から整合検査へ降格。条件 9（未処理 STOP/HOLD 無し）は ISL / gateway が決定を保持している場合のみ効く。impact: MEDIUM（安全 clearance の laundering）。
- v0.2.2 fold: `v0.2.2 05:241` 「理由の優先順位: SAFETY / SAFE_STOP > MANAGER_RESTART / GATEWAY_RESTART / CHECKPOINT_DISABLED > その他。current が既に SAFEHOLD のとき … 弱い理由への上書きは記録付き no-op = FAULT + audit・状態不変・epoch 不変 — INV-30。manager 起動時は ISL に未解除決定を照会し、あれば reason = SAFETY で復帰」／ `v0.2.2 05:704` INV-30／ `v0.2.2 05:739` T-28。laundering は解消。
- **残る gap（PARTIAL の根拠）**: `v0.2.2 05:300` 失敗行 R_MANAGER_RESTART = 「durable state を読み `epoch := persisted + 1`・owner := SAFEHOLD・全 lease 無効・全 permit VOID」と `v0.2.2 05:720` T-09「epoch = persisted + 1」は無条件のまま — persisted state が SAFEHOLD(SAFETY / SAFE_STOP) のとき INV-30 / T-28 は「epoch 不変の no-op」を要求し、同一事象に 2 つの期待値が並ぶ（fold が矛盾を導入）。
- minimal fix（残差）: `05:300` と T-09 に「persisted owner が EXECUTOR または INV-30 で弱い reason のときのみ epoch+1；SAFETY / SAFE_STOP なら reason 保持・epoch 不変（T-28）」を付す。schema delta 0。

### B-M3（MEDIUM・confirmed・fold = RESOLVED）
- v0.2.1 実体: `grep -n SAFE_STOP 05` = `102/122/228/270/292/293/294/314/451/468/500/559/581/588/590/595` — CAS 引数として `TRANSFER_TO_SAFEHOLD(SAFE_STOP)` を書く行は 0。`05:270` / `05:500` は `TRANSFER_TO_SAFEHOLD(NO_CHAIN)` のうえで disposition のみ SAFE_STOP、`05:590` は disposition のみ、`05:451` (iii) は `safehold_reason == SAFE_STOP` でのみ SafeStop。
- frozen: `A:151` FailClosedAction.SAFE_STOP／ `D0:363`。
- text / logic / impact: 未反証。profile `on_no_chain = SAFE_STOP`（`06:175`）でも reason = NO_CHAIN → 出力 = SafeHold・stop-class 要求無し。MEDIUM（handoff 決定 7 の SAFE_STOP 側強化が物理効果を持たない）。
- v0.2.2 fold: `v0.2.2 05:241` 「実効 disposition（profile EscalationPolicy 適用後）が SAFE_STOP となる遷移は全て `TRANSFER_TO_SAFEHOLD(SAFE_STOP)` を用い、原因 fault は `RuntimeAuditRecord.fault` に別途記録」／ `v0.2.2 05:271` HEALTH_FAIL \| SAFE_STOP per profile／ `v0.2.2 05:275` NO_CHAIN \| SAFE_STOP／ `v0.2.2 05:541` B5／ `v0.2.2 05:628-632` §7 表。→ RESOLVED。

### B-M4（MEDIUM・confirmed・fold = RESOLVED）
- v0.2.1 実体: `05:160` 「ACK は「候補 invocation + 期待 epoch + 消費予定 offer + lease 提案」に束縛される」／ `05:221` 条件 3 = expected_control_epoch / valid_until / invocation_id / handoff_offer_id / envelope_readback_ok のみ（`executor_id` / `lease_proposal_id` / `skill_action_id` 無し）／ `05:637` INV-07 同様／ `05:154` `lease_proposal_id: runtime-local id`／ `05:312` 証明 A「lease は executor_id を 1 つだけ持つ」。
- frozen: `A:477-486` SkillInvocation に executor 識別子無し／ `D0:269, :272`。
- text / logic: 未反証 — X2 の ACK（invocation I・offer O・proposal P1）を ack_id に持つ permit の proposed_lease{executor_id: X3, envelope E2} は条件 3 を全て満たし SUCCESS → ACK していない X3 が owner。impact: MEDIUM（handoff 決定 C.2「次 executor の ready ACK を転送前に」が実質未執行）。
- v0.2.2 fold: `v0.2.2 05:157` `lease_proposal_id` = 最終 `lease_id`／ `v0.2.2 05:224` 条件 3 += `ack.executor_id == proposed_lease.executor_id ∧ ack.skill_action_id == proposed_lease.skill_action_id ∧ ack.lease_proposal_id == proposed_lease.lease_id`／ `v0.2.2 05:223` 条件 2 で CAS 引数も permit に束縛／ INV-07 同旨。→ RESOLVED。

### B-M5（MEDIUM・confirmed・fold = PARTIAL）
- v0.2.1 実体: `05:224` 条件 6 「… decision の inputs hash/ref 束縛が lease の `skill_action_id` / `profile_hash` に一致」／ `05:218` 「不能判定 = FALSE」／ `06:278` 「`CommitPermit.profile_hash` と等値（05 §3.5 前提 6 の inputs 束縛）」。
- frozen: `A:346-351` `evaluate_authority_grant(eligibility_report, acceptance_state, two_key_state, safety_gate_state) -> AuthorityDecision {granted, denials, inputs の hash/ref 束縛}` — profile を運ぶ入力無し／ `A:385` strict codec。
- text / logic: 未反証 — profile_hash の束縛は frozen 型から読めず「不能 = FALSE」で CLOSED_LOOP lease は全て REJECTED、充足には frozen 型への field 追加（schema delta）が要る。impact: MEDIUM（slice = SHADOW ゆえ HIGH ではないが、schema delta 0 の下で充足不能な必要条件）。
- v0.2.2 fold: `v0.2.2 05:227` 条件 6 = granted ∧ 「decision の入力 `eligibility_report` が依拠する certificate（`A:323`・`A:306`）の `skill_action_id == proposed_lease.skill_action_id`。⚠ frozen `AuthorityDecision` は profile を知らない … `profile_hash` の束縛は permit 側（条件 2）で行う」／ `v0.2.2 05:223` 条件 2 `permit.profile_hash == permit.proposed_lease.profile_hash`／ `v0.2.2 05:184` (f)／ `v0.2.2 05:777` OP-18。05 側は解消。
- **残る gap（PARTIAL の根拠）**: `v0.2.2 06:284` 「`CommitPermit.profile_hash` と等値（**05 §3.5 前提 6 の inputs 束縛**）」が不変 — v0.2.2 の条件 6 は profile_hash を束縛せず、finding が明示した「sync 06」が未実施（VERIFY_A の A-01 と同じ残差）。
- minimal fix（残差）: 06 §5 第 1 bullet の括弧を「05 §3.5 条件 2・§3.4 (f) の等値束縛」に置換。schema delta 0。

### B-M6（MEDIUM・confirmed・fold = RESOLVED）
- v0.2.1 実体: `05:441` WAIT = 「no-op（元々運動 command を出さない）」／ `05:211` `first_command_admitted` = 「CLOSED_LOOP: 最初の admissible command が gateway で admit された」／ `05:266` HealthConfirmation 全 True 必須／ `05:343` `command_deadline_s` は non-null・超過 = R_DEADLINE_MISS／ `05:396` deadline miss → SafeHold。`grep -w WAIT 05` = `54/332/441/707` に免除規則無し。
- frozen: `A:62` ControlMode に WAIT／ `B:125-127`。
- text: 未反証。logic: 未反証（WAIT の「admissible command」は 05 で未定義 → 不能 = FALSE → health_confirm_timeout_s で HEALTH_FAIL；仮に通っても command_deadline_s で DEADLINE_MISS）。impact: MEDIUM（frozen enum member が runtime で実行不能）。
- v0.2.2 fold: `v0.2.2 05:423` 「WAIT lease: `first_command_admitted` と `command_deadline_s` は `control_mode ∈ {DIFF_IK_EE_TARGET, SCRIPTED_SEQUENCE}` にのみ適用し、WAIT では両者を N/A = True として記録する。WAIT の liveness は executor heartbeat（`R_EXECUTOR_LOST`）で担う」。→ RESOLVED（finding の fix と同一）。

### B-M7（MEDIUM・confirmed・fold = PARTIAL）
- v0.2.1 実体: `06:281` 「lease 活性中に profile の前提が崩れた場合（calibration 期限切れ・health check 失敗・evidence 失効）は … **lease 無効化**（05 §4.5 → `R_LEASE_INVALIDATED` / `R_HEALTH_CONFIRM_FAILED`）」／ `06:371` PT-13 「活性 lease は無効化（05 `R_LEASE_INVALIDATED`）」／ `05:429` §4.5 trigger 列に calibration / evidence 失効無し／ `05:263-276` 該当行無し／ `06:68` HealthCheckStage に lease 中の段無し。
- frozen: `D0:318`。
- text / logic / impact: 未反証。05 に検出者・遷移・timeout が無く、06 の主張は空文。MEDIUM（05/06 不整合・安全関連の主張が根拠無し）。
- v0.2.2 fold: `v0.2.2 06:287` 「(b) calibration 期限切れ・deployment evidence 失効は**活性 lease 中には検出されない** … 検出点 = 次の permit 発行時（05 §3.4 (b) → `R_PROFILE_MISBOUND`）。曝露 = 最大 1 skill」／ `v0.2.2 06:377` PT-13 同旨／ `v0.2.2 05:778` OP-19。finding の保守案と同一。
- **残る gap（PARTIAL の根拠）**: `v0.2.2 06:357` §8.1 規則 (3) 「受理後の事実変化（期限切れ・evidence 失効）は profile を変えず **lease を無効化する**（§5）」が不変で、新 §5 (b) と同一 doc 内で矛盾。
- minimal fix（残差）: 規則 (3) を「… profile を変えず、活性 lease は継続・次の permit 発行で `P_CALIBRATION_EXPIRED` / `P_EVIDENCE_UNBOUND` → 05 `R_PROFILE_MISBOUND`（§5 (b)）；health check 失敗のみ lease 無効化（§5 (a)）」に改訂。schema delta 0。

### B-M8（MEDIUM・confirmed・fold = RESOLVED）
- v0.2.1 実体: `05:439` 「`DIFF_IK_EE_TARGET`: 直近 admitted EE target（無ければ計測現在姿勢）を目標として保持・速度 0」／ `05:396` 「次 command が `command_deadline_s` 内に届かなければ hold 継続ではなく `SafeHold`（`R_DEADLINE_MISS`）— 無期限 ZOH を「安全」と見なさない」／ `05:234` 「旧 lease の直近 command は hold 地平にかかわらず即座に出力から外れる」。
- frozen: `B:127` hold／ `D0:273-274`。
- text / logic: 未反証 — 直近 admitted target を目標に保持し続けることは、その target への DiffIK 収束 = 直近 command の ZOH と物理的に同一で、`05:396` の根拠と矛盾。TRANSFER 後は無効化 lease の運動意図が SafeHold target として残り `05:234` は名目上のみ真。impact: MEDIUM（安全側既定の物理意味の自己矛盾。OP-4 は controller 固有実現を外に置くが、種別定義の矛盾は本 doc 内）。
- v0.2.2 fold: `v0.2.2 05:465` 「**計測現在姿勢で凍結・速度 0**（直近 admitted EE target を目標に保持し続けない — 無効化 lease の運動意図を残さない）」。`v0.2.2 05:422` の hold 規則（admitted 間のみ・deadline まで直近 setpoint 保持）は lease 内の窓に限られ SafeHold 定義と整合。→ RESOLVED。

### B-M9（MEDIUM・confirmed・fold = RESOLVED）
- v0.2.1 実体: `05:589` INVALID_STATE → RE_OBSERVE → RESELECT／ `05:292` HEALTH_FAIL → HOLD／ `05:466` HEALTH_FAIL 等の解除 = 完全経路のみ（back-off 無し）／ `05:339-344` TimingBinding に dwell / attempts 無し／ `05:719` OP-14 は ack/permit timeout vs D_event のみ／ `06:145-151` RuntimeTimeouts 6 field のみ。
- frozen: `D0:273`（timeout ⇒ producer retains + safe-stop or re-observe）／ `D0:364` Event-checkpoint miss action = cached-recovery / safe-stop／ `D0:213`。
- text: 未反証。logic: 未反証（RE_OBSERVE↔RESELECT・HEALTH_FAIL→再選択の無限周回に上限無し）。impact: MEDIUM（SafeHold 中ゆえ fail-safe だが、`05:278`「各 timeout / failure に disposition を必ず割り当てる」に反し D0 §G の deadline-miss → safe-stop が写像されない）。
- v0.2.2 fold: `v0.2.2 05:365-366` `boundary_dwell_s` / `max_reselect_attempts`／ `v0.2.2 05:275` 遷移行（超過 → NO_CHAIN \| SAFE_STOP・FAULT(R_BOUNDARY_DWELL_EXCEEDED)）／ `v0.2.2 05:541` B5／ `v0.2.2 05:325` 失敗行／ `v0.2.2 05:774` OP-14 に写像／ `v0.2.2 06:154-155`。→ RESOLVED（finding の fix と同一）。付記（採点外）: 上限は「1 boundary あたり」で、HEALTH_FAIL → SAFEHOLD → 新 episode の周回は episode をまたいで数えられない — finding 自身の fix（NO_CHAIN → HOLD）も同じ性質で、各周回は epoch+1 と FAULT record を伴う。

### B-M10（MEDIUM・confirmed・fold = RESOLVED）
- v0.2.1 実体: `05:275` 「gateway は SafeHold 出力で起動し manager へ再起動を報告。manager が CAS」／ `05:307` 「起動時出力 = SafeHold」／ `05:655` INV-25 「再起動前の lease は新 CAS 無しに再 admit されない」／ `05:450` (ii) は state のみで判定。
- frozen: `D0:272`。
- text / logic: 未反証 — 起動後・manager の GATEWAY_RESTART CAS 前に state = (EXECUTOR(X1), e, L1) を読めば (ii) は成立し X1 の command を admit する。INV-25 を執行する規則が無い。impact: MEDIUM（P1 違反ではないが、宣言された不変量に機構が無い = appearance-only）。
- v0.2.2 fold: `v0.2.2 05:489` (ii) に `¬restart_pending`／ `v0.2.2 05:496` 「起動時に `restart_pending := True` とし、`safehold_reason == GATEWAY_RESTART`（または `seq` > 起動時観測 seq）を観測するまで (i)/(i′)/(iii) のみ」／ `v0.2.2 05:705` INV-31／ `v0.2.2 05:82` 持つ状態に `restart_pending`。seq 代替は「L1 が活性のまま seq が進む唯一の CAS = MARK_BOUNDARY_WAIT（以後 L1 は R_POST_OUTCOME_COMMAND）」ゆえ抜け道にならない。→ RESOLVED。

### B-M11（MEDIUM・confirmed・fold = RESOLVED）
- v0.2.1 実体: `05:657` INV-27 「全 `RuntimeFaultCode` member は §3.7 の失敗表に行を持つ」／ `05:755` fold 記録 B-10「7 つの fault code に失敗表の行が無い」（完全性を主張）。機械再現（§0）: 6 member が失敗表に fault 列を持たない（`R_CHECKPOINT_SWITCH_DISABLED` は遷移表 `05:269` のみ、DEADLINE / ENVELOPE は `05:272` に無名 FAULT）。
- frozen: `A:382`。
- text / logic / impact: 未反証。INV-27 が自己充足していない。MEDIUM（不変量の偽・T-25 が実行不能）。
- v0.2.2 fold: `v0.2.2 05:316-321` 6 行追加（+ 新 member 5 行 `05:322-326`）、機械再現 = 37 member 欠落 0／ `v0.2.2 05:701` INV-27 更新。→ RESOLVED。

### B-M12（MEDIUM・confirmed・fold = RESOLVED）
- v0.2.1 実体: `05:140` 「`AuthorityState` は durable」（CAS SUCCESS と durable commit の順序未定義）／ `05:293` 「`epoch := persisted + 1`」／ `05:711` OP-6 「durable 書込みと CAS の原子性」を open として列挙／ `05:320` 証明 E 「epoch は成功列に沿って厳密増加」。
- frozen: `D0:270-272`（CAS/lock は D1 refinement）。
- text / logic: 未反証 — in-memory SUCCESS(L2, e+1) → 通知・LEASE_ACTIVATED(e+1) → durable 前 crash → restart (SAFEHOLD(MANAGER_RESTART), e+1) で e+1 が 2 状態を指し、audit が manager state と矛盾。impact: MEDIUM（証明 E の前提が open point に依存）。
- v0.2.2 fold: `v0.2.2 05:142` 「線形化点 = durable commit の完了: … 通知・audit・`permit.state := CONSUMED` はその後にのみ行う。… crash した CAS は「起きなかった」ものとして扱われ … `invalidated_lease_ids` は同一 durable store に同一 commit」／ `v0.2.2 05:706` INV-32／ T-30／ `v0.2.2 05:766` OP-6 を primitive 選定に縮小。→ RESOLVED。

### B-L1（LOW・confirmed・fold = RESOLVED）
- v0.2.1 実体: `05:269` 「`SkillOutcome`（`InterruptOutcome`）受領 | TRANSFER_TO_SAFEHOLD(CHECKPOINT_DISABLED)」（reason を問わず）／ `05:592` 「Interrupt SAFETY_STABILIZED | HOLD（`safehold_reason = SAFETY`・clearance 待ち）」／ `05:467` vs `05:469` 解除経路が異なる。
- frozen: `A:151` InterruptReason = {PLANNED_SWITCH, EVENT, SAFETY_STABILIZED}／ `D0:262`。
- text / logic / impact: 未反証。LOW（節間不一致・clearance 経路の二義）。
- v0.2.2 fold: `v0.2.2 05:273` PLANNED_SWITCH / EVENT → CHECKPOINT_DISABLED／ `v0.2.2 05:274` SAFETY_STABILIZED → TRANSFER_TO_SAFEHOLD(SAFETY)・解除 = §5.4 SAFETY 行／ `v0.2.2 05:633-634` §7 表と整合／ `v0.2.2 05:321` 失敗行も PLANNED_SWITCH / EVENT に限定。→ RESOLVED。

### B-L2（LOW・confirmed・fold = RESOLVED）
- v0.2.1 実体: `05:298` 「連続 N 回で TRANSFER_TO_SAFEHOLD(ENVELOPE_VIOLATION) — N は profile」／ `05:397` 「inter-command 間隔が profile 許容を超えて逸脱 ⇒ R_TIMING_VIOLATION」／ `06:145-151` RuntimeTimeouts = {ack_validity_s, ack_timeout_s, permit_ttl_s, health_confirm_timeout_s, command_deadline_s, manager_liveness_s}（`grep -E '連続|tolerance|許容' 06` = 0 hit）／ `06:280` 写像列挙は 5 field で `ack_validity_s` を欠く（`06:146` / `05:339` は定義済）。
- frozen: `D0:354-359`／ `05:652` INV-22。
- text / logic / impact: 未反証。LOW（INV-22「全 timeout は profile 由来」に対し器の無い閾値 2 つ + 列挙漏れ）。
- v0.2.2 fold: `v0.2.2 06:152-157` `safety_heartbeat_timeout_s` / `decision_max_age_s` / `boundary_dwell_s` / `max_reselect_attempts` / `command_kind_mismatch_max` / `inter_command_jitter_s`／ `v0.2.2 06:286` 写像列挙 12 field（`ack_validity_s` を含む）／ `v0.2.2 05:367-368`／ `v0.2.2 05:421` `inter_command_jitter_s`／ `v0.2.2 05:308`（失敗行）`command_kind_mismatch_max`／ `v0.2.2 06:252` `P_TIMEOUT_NONPOSITIVE` を int field に拡張。→ RESOLVED。付記（採点外・字句）: `v0.2.2 05:696` INV-22 の括弧列挙は旧 6 種のまま。

### B-L3（LOW・confirmed・fold = PARTIAL）
- v0.2.1 実体: `05:600-604` AuditKind に COMMAND_REJECTED / SHADOW_COMMAND_RECORDED のみ（COMMAND_ADMITTED 無し）／ `05:633` INV-03 は `(lease_id, control_epoch)` のみ／ `05:622` 記録義務は「SHADOW の全 command 判定」のみ／ gateway が admit する command の型定義無し。
- frozen: `A:498`（offer の replay 単位）／ `A:537`（runtime 記録の hash は defer）。
- text / logic: 未反証 — 同一 lease 内の再配送 / 逆順 DIFF_IK target は admission を通る；CLOSED_LOOP の admitted command は `first_command_admitted` 以外に痕跡を残さない。impact: LOW。
- v0.2.2 fold: `v0.2.2 05:473-480` `GatewayCommand{lease_id, control_epoch, executor_id, cmd_seq, kind, payload_ref, t_issue}`（`cmd_seq` 重複・逆順 = R_TIMING_VIOLATION）／ `v0.2.2 05:677` INV-03 += `cmd_seq` 厳密単調／ `v0.2.2 05:647` `AuditKind.COMMAND_ADMITTED`。
- **残る gap（PARTIAL の根拠）**: `v0.2.2 05:666` 記録義務は依然「SHADOW の全 command 判定」のみで、CLOSED_LOOP の admitted command（`COMMAND_ADMITTED`）の記録が義務化されていない — kind は存在するが任意のままで、finding の「admitted commands leave no audit trace」は義務の面で未解消。加えて `v0.2.2 05:647` の comment が B-13 の旧注記と連結して読みにくい（字句）。
- minimal fix（残差）: `05:666` 記録義務に「CLOSED_LOOP lease の全 admitted command（`COMMAND_ADMITTED`・`GatewayCommand` payload）」を追加。schema delta 0。

### B-L4（LOW・confirmed・fold = RESOLVED）
- v0.2.1 実体: `05:141` genesis record = 「epoch 起点・consumed_offer_ids = ∅・invalidated_lease_ids = ∅」（起点の下限無し）／ `05:656` INV-26 は「0 から自動再開しない」のみ。
- frozen: `A:382`。
- text / logic: 未反証 — store 消失後に低い起点で再発行すると、consumed / invalidated が ∅ に戻るため `control_epoch == e` の旧 offer は等値検査（`A:381`）を再び通り得る。impact: LOW（operator 行為 + audit 必須の経路だが下限規則が無い）。
- v0.2.2 fold: `v0.2.2 05:143` 「genesis record の epoch 起点は、（別 durable の）AuditRecorder に現れる最大 `control_epoch` より**厳密に大きい**こと。両 store が失われた場合は operator が厳密に大きい起点を宣言し、その導出を `GENESIS_RECORDED` に記録する」。→ RESOLVED。

### B-L5（LOW・confirmed・fold = RESOLVED）
- v0.2.1 実体: `05:489` B1 「issues 非空 = R_INVOCATION_INVALID → HOLD」（CAS / reason 無し）／ `05:239` MARK_BOUNDARY_WAIT は「`validate_outcome` の report ref を CAS に添える」のみで issues 空を要求しない／ `05:469` CHECKPOINT_DISABLED 解除後は「§6.2 B0 から」= SAFEHOLD 起動でも B1 を通る手順。
- frozen: `A:374`（validate_outcome）。
- text / logic / impact: 未反証。LOW（手続の未定義）。
- v0.2.2 fold: `v0.2.2 05:530` B1 「issues 非空 = R_OUTCOME_INVALID: MARK_BOUNDARY_WAIT は受領の事実で既に実行済（§3.5）。候補評価を行わず B5（NO_CHAIN）へ。SAFEHOLD からの起動（producer 無し）では B1 と offer 検証を skip し `InitializationRecord.handoff_validation_report_ref = null`」／ `v0.2.2 05:272` 遷移行（issues の有無を問わず）／ `v0.2.2 05:324` 失敗行／ T-29。→ RESOLVED（finding の fix と同一）。

### B-L6（LOW・confirmed・fold = RESOLVED）
- v0.2.1 実体: `05:183` 「同一 `expected` に対し複数の permit が並存してもよい … CAS 成功は高々 1 つ」／ `05:276` Orchestrator 喪失行／ 敗者 executor への通知規則無し（`05:284-287` の permit VOID 行は状態効果のみ）。
- frozen: `D0:272`。
- text / logic / impact: 未反証。LOW（提案状態の残留・availability）。
- v0.2.2 fold: `v0.2.2 05:184` 末尾「permit が CONFLICT / VOID になった場合、manager は ACK 元 executor へ `PERMIT_VOIDED` を通知し executor は提案を破棄する」／ `v0.2.2 05:326` 失敗表行。→ RESOLVED。

## 2. 集計

- confirmed 21 / 21（refuted 0）。severity: HIGH 3（B-H1 / B-H2 / B-H3）・MEDIUM 12・LOW 6（reviewer B の格付けを維持。各 impact lens で下げる根拠を探したが、いずれも handoff 決定（C.2 / C.3 / C.4 / 7）・D0 §F/§G・05 自身の不変量宣言に対する具体的欠陥で、stylistic ではない）。
- fold_status: RESOLVED 16 ／ PARTIAL 5（B-M1: INV-24 と `05:241` の過剰主張が不変／ B-M2: `05:300` / T-09 の無条件 epoch+1 が INV-30 / T-28 と矛盾／ B-M5: `06:284` の stale cross-reference／ B-M7: `06:357` 規則 (3) が新 §5 (b) と矛盾／ B-L3: `05:666` 記録義務に COMMAND_ADMITTED 無し）。NOT_RESOLVED 0。
- 残差はいずれも 1 文の改訂で閉じ、frozen 4 file への schema delta は不要。
