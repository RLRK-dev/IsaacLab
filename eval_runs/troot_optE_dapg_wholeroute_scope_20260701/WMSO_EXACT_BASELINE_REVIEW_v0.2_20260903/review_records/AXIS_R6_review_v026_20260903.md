# R6 review — WMSO v0.2.6 REVIEW CANDIDATE（deployment / commissioning & functional-safety・adversarial）

- reviewer: R6（別 context・deployment 軸・実 cell UR15 × 2 の commissioning / 機能安全担当の立場）
- 対象: `$PKG/06_WMSO_INDUSTRIAL_PROFILE_v0.2_REVIEW_CANDIDATE_20260903.md`（header v0.2.6）primary ／ `05_…` §2 / §3.4 / §3.5 / §3.7 / §4.3 / §5.4 ／ `04_…` secondary。`$PKG` = `eval_runs/troot_optE_dapg_wholeroute_scope_20260701/WMSO_EXACT_BASELINE_REVIEW_v0.2_20260903`、`$D` = `eval_runs/troot_optE_dapg_wholeroute_scope_20260701`、HEAD = `f9fe9757`。
- 実施 = 2026-09-05 06:47 UTC（`date -u` 実測）。凍結物・`eval_runs/` は非接触（本 dir にのみ書込）。
- 読了範囲: 06 全 690 行 ／ 05 全 1022 行 ／ 04・08・10 全文 ／ 11 §1–§9 ／ `check_review_candidate.py` 全文 ／ `review_records/fold/fold_v026.py` 全文 ／ 凍結: contracts_v2 `:36-42, :138-172, :215-220, :350-390, :535-540`、tensor_binding `:20-30, :38-56, :60-100, :120-140, :180-190, :285-292, :330-334`、EP md `:95-110, :145-165`、EP JSON `:1-8, :205-215`、D0 `:310-325, :350-358`、C3 `:1-8, :60-70`、prereg `:55-62`、RS71 `:19-33`（§0 #1–#5 = `:23-27`）、`task_config.py:15-30, :230-240`。

## 0. verdict

**HOLD**（08 §5: CRITICAL / HIGH が残る）。HIGH 3 / MEDIUM 7 / LOW 11 / CRITICAL 0（計 21）。全 finding は凍結 schema delta 0 の最小修正案を持つ。

## 1. 前提の実測

### 1.1 pin 再測（`bash $PKG/verify_exact_baseline_pins.sh`・素の bash）

```text
PASS  D1.1-A DESIGN v2.11.2 (worktree)  00192d20ca00b654cf6cfdb9d04b03ca14adfd0b93f2105c0fea28c295ff8aff
PASS  EvidencePolicy v1.9 md (worktree)  c474acea7c58acc22050c2ad9944fd45a18f5c76967964b42d11922e28fa27e7
PASS  EvidencePolicy v1.9 json (worktree)  e63176af9bc3a246b1c32db369ec59f8d09a4c96c381bb03a3a6024bd9811c6e
PASS  D1.1-B DESIGN v13 (worktree)  5a1874d3be8b98b8aeaace73890d8021cbc7f9814e048741f7f58d6746f349a6
PASS  D1.1-A DESIGN git blob id (HEAD)  1353430228a90ad36dd190a9359cd81da52e8242
PASS  D1.1-B DESIGN git blob id (HEAD)  ebe8154abf2461a6b24db05728b20e5cee949fb2
PASS  D1.1-A DESIGN sha256 of blob 135343…  00192d20ca00b654cf6cfdb9d04b03ca14adfd0b93f2105c0fea28c295ff8aff
PASS  D1.1-B DESIGN sha256 of blob ebe815…  5a1874d3be8b98b8aeaace73890d8021cbc7f9814e048741f7f58d6746f349a6
PASS  D1.1-A DESIGN at frozen commit 54f90a7d  00192d20ca00b654cf6cfdb9d04b03ca14adfd0b93f2105c0fea28c295ff8aff
PASS  D1.1-B DESIGN at frozen commit 07250f4a  5a1874d3be8b98b8aeaace73890d8021cbc7f9814e048741f7f58d6746f349a6
PASS  evidence_policy_definition_hash (embedded command)  e7ca43093084c167a209b008533a66d26a1fd3223d2a3c11274d28306c3ff803
policy_semver=1.9.0
HEAD=f9fe975783aa63f43989c4be7c7bb2ae544ba90f branch=claude/v0-2-design-candidates-snqbmy
EXIT=0
```
→ **11/11 PASS**（原本 = 本 dir `pin_recheck.txt`）。

### 1.2 機械検査（`python3 $PKG/check_review_candidate.py 06_… --profile`）

`== 06_…: citations=63 FAIL=0 WARN=25`（exit 0・原本 = 本 dir `checker_06.txt`）。WARN は全て C2 heuristic。**FAIL = 0 を確認してから読んだ。**

### 1.3 陽性対照（10_）の注入 4 欠陥（PC-06-E/F/G/H）が実体に無いことの確認

- action-space bounds field: 06 §2.1–§2.2 に無し（`P_UNKNOWN_FIELD`・06:267）。
- `action_rate_hz ≤`: 06:269 は等値のみ（`P_RATE_MISMATCH`）、PT-01 が narrow を拒否（06:428）。
- `requires_safety_layer_health` False 可: 06:147 は True 必須（省略 default 無し）。
- Escalation 緩和: 06:212-217 は SAFE_STOP 側のみ（enum に緩和値無し）。
→ 4 件とも実体には存在しない。以下の finding はいずれも実体の本文から再現したもの。

### 1.4 方針（premise 6 / 7）

rate / hold / control_mode は **等値のみ**として審査した（06:269-270 は等値・narrow も欠陥）。v0.2.5 の設計方向（Rs 裁定: B+ / baseline 外部化 / registry two-key / OP-19 三層）は finding にせず、v0.2.6 本文がそれを正しく・整合的に・機械検査可能に・fail-closed に実現しているかだけを見た。

## 2. PASS 1 — 06 §11 v0.2.6 fold 12 行の回帰

| # | fold 行（06:660-671） | 判定 | 根拠（doc:line）・finding |
|---|---|---|---|
| 1 | R3-01 / R4-01 / R3-16 / R3-02（INTER_ARM 評価対象・cmd.resource・term 実体化 → §0 (b)・§2.1・§4） | **finding（LOW）** | §4（06:323-327）= 5 term、05 `EnvelopeTermKind` 5 member（05:367）、05 §4.3 INTER_ARM 行（05:461）、`GatewayCommand.arm_targets`（05:506）と整合。**ただし 06:340「`AcceptedEnvelope.terms` の 3 項 ← §4」が旧文のまま**で 06:333「profile 4 項」と矛盾 → R6-12 |
| 2 | R3-17 / R4-02（hash 列挙 → §3） | OK（LOW 注記） | 06:308 と 05:191 (j) の列挙は一致（11 項目）。checker Q2 は「§2.1 hash field ⊆ (j) 列挙」を**主張ほど機械検査していない**（substring を bullet 全行に当てる）→ R6-13。`BindingExpectation.tensor_binding_hash` は (g) で等値検査されるため (j) 外で可 |
| 3 | R4-03（MIN_NEGATIVE_CONTROLS を執行する P_* → §6.1 `negative_controls`・§3） | **finding（HIGH）** | `negative_controls` field（06:378）と `P_EVIDENCE_POLICY_TOO_WEAK`（06:316）/ `P_EVIDENCE_UNBOUND`（06:294）は結線された。しかし内容照合の `P_FAULT_EVIDENCE_UNVERIFIED` 行（06:297）は NC# subject を評価できず（fault 分岐「fault == subject_ref の code」が未定義）、06:391 の comment「下の期待 audit 内容で照合」と矛盾。同じ行の health 分岐は HEALTH_CONFIRMED も受理するため check_id#NEGATIVE（06:291）は空虚 → R6-02。subject 文法（06:359）に 'NC#<name>' / '<check_id>#NEGATIVE' が無い（同 finding） |
| 4 | R4-04 / R3-21 / R4-16 / R4-19（AcceptanceRole / role_registry_hash / registry 規則 / §9） | **finding（HIGH + MEDIUM + LOW）** | (a) 06:513 の two-key 条件が `state` で限定されず、06:514 の one-key SUSPENDED / REVOKED record が「CLOSED_LOOP ∈ allowed_lease_modes ⇒ two-key」「generation = 直前 + 1」に落ちる → 書込拒否 → 05:291「記録なしの event は無い」により無効化 event が出ない → R6-04（HIGH）。(b) `role_registry_hash` は record 著者が指す artifact で、権威ある head が無い（自己証明）・資格取消が既存 record に伝播しない → R6-05（MEDIUM）。(c) 06:488「role ごとに相異」と 06:513 の相異範囲の不一致、`max_acceptance_validity_s` 超過の基準時刻未定義 → R6-18（LOW）。R4-19 §9 反映（06:515, 06:519）は OK |
| 5 | R4-05 / R3-12（予算検査の artifact と評価点 → §3 `P_SAFETY_BUDGET_EXCEEDED`） | **finding（LOW）** | 06:287 の式・評価点（第 2・CLOSED_LOOP）は §8.1 (2) と整合。「FI = 当該 arm の R_SAFETY_LAYER_LOST#HEARTBEAT_TIMEOUT record」は subject 文法（06:359: fault code のみ・arm 無し）と artifact struct（arm field 無し）から解決不能 → R6-15。FI の数値が引用 audit と照合されない → R6-20 |
| 6 | R3-10 / R4-07 / R3-18 / R4-08（§8.1 (2)(3) の旧文・第 2 評価点の閉じた list） | **finding（MEDIUM + MEDIUM + LOW）** | 旧文撤回（06:422 (3)）と閉じた list は反映。05:191 (b) は 06 §8.1 (2) を SSOT として参照 ✓。残差: (a) `P_TIMEOUT_ORDER` を第 1 評価点に置くが 06:285 の `max_reselect_attempts × worst_case_attempt_s + selector_overhead_s ≤ boundary_dwell_s` は baseline 値を要し、baseline は「全 P_* = 0 の後に書く」acceptance record 経由でしか束縛されない → 循環 → R6-09。(b) `P_BASE_LAYOUT_MISMATCH`（06:311・baseline 許容を要する）が第 1・第 2 どちらの list にも無い → R6-07。(c) `P_VALIDITY_UNBOUNDED` は CALIBRATION_RECORD が required になる第 2 評価点で要るが閉じた list に無い → R6-11 |
| 7 | R4-10 / R3-22（baseline の cell 束縛・supersession → §8.3・§3 `P_TIMEOUT_CEILING`） | OK（LOW 注記） | 06:286 / 06:497-498 の cell_id・layout_revision_ref 等値 ✓。supersession → SUSPENDED(BASELINE_SUPERSEDED)（06:509）✓。検出経路は monitor（05:88「baseline registry」= 未定義語）のみで manager head-check（05:292）は baseline head を見ない → R6-10（LOW） |
| 8 | R4-11 / R3-05（event の記録・disposition 表 → §2.1 `on_validity_expired`・§5 (b)・§8.3 `SuspensionReason`） | OK（LOW 注記 ×3） | 表（06:342）は `SuspensionReason` 8 member を全て被覆、`validity_deadline_source` kind 3 種を被覆、表外 = SAFE_STOP、`on_validity_expired` は HOLD→SAFE_STOP のみ ✓。05:290-292 / 05:534 と整合。残差: 06:341「SHADOW … 次 permit 評価で可」・PT-13（06:442）が 05:290 / 05:427 / INV-34「mode を問わず」と矛盾 → R6-16；FI（停止性能 evidence）失効 → HOLD が 05:340 の「停止性能に関わる失効 = SAFE_STOP」と不整合 → R6-17；05:290 記録列に ACCEPTANCE deadline の `R_PROFILE_SUSPENDED` が無く INV-34 と不一致 → R6-21 |
| 9 | R4-12（arm 単位 evidence の subject → §6.1・§3） | **finding（LOW）** | 06:359 subject ✓・06:307 `P_ENVELOPE_EXCEEDS_MEASURED` ✓。`P_EVIDENCE_UNBOUND` 行（06:294）の明示 ∀ 列挙に CONTROLLER_ENVELOPE_MEASUREMENT / TOOL_PAYLOAD_IDENTIFICATION ∀ arms[] が無い → R6-14 |
| 10 | R4-13（MIN_FAULT_INJECTION の v0.2.5 code → §6.1） | **finding（MEDIUM）** | 06:387 に `R_CALIBRATION_EXPIRED_ACTIVE#DEADLINE` / `R_EVIDENCE_EXPIRED_ACTIVE#DEADLINE` / `R_PROFILE_SUSPENDED#EVENT` を追加したが、`FaultInjectionRequirement`（06:367-369）は trigger を持たず `runtime_fault_code ∉ RuntimeFaultCode = P_FAULT_CODE_UNKNOWN`（06:298）、かつ `P_EVIDENCE_UNBOUND` は `subject_ref == runtime_fault_code`（06:294）— 06:389 の '<code>#<trigger>' 要求と両立しない。05 §8（05:700）の trigger 副因語彙も DEADLINE / EVENT を持たない → 必須集合が表現不能 → R6-03 |
| 11 | R4-14（物理変更の検出 → §2.1 BASE_POSE_VERIFY・tool_id 個体 serial・§3 必須集合） | **finding（MEDIUM ×2 + LOW）** | 必須集合（06:289）✓・subject（06:359）✓。(a) BASE_POSE_VERIFY（06:68）の「baseline 許容内」に対応する field が `CellSafetyTimingBaseline`（06:495-505）に無い → R6-07。(b)「lease 中も周期再検証」（06:68）は baseline `diagnostic_items` に依存するが最小集合を要求する P_* が無く、空の diagnostic_items で周期層（liveness / drift / identity / head-check 周期）が消える → R6-08。(c) serial 自己申告の信頼と tool の serial 報告能力の前提が §9 に無い → R6-19 |
| 12 | R4-17（min_separation の定義と 88 mm・固定 base の照合 → §2.1・§3 `P_BASE_LAYOUT_MISMATCH`） | **finding（MEDIUM）** | TCP 点間距離の定義（06:105）✓、`≥ grasp span ⇒ P_ENVELOPE_EMPTY`（06:310）✓。`P_BASE_LAYOUT_MISMATCH`（06:311）は (i) 許容値の出所無し、(ii) 評価点無し、(iii) RS71:24 は「Y = ∓0.35」で左右割当を書かず（割当は `task_config.py:21-22` ROBOT_LEFT_BASE = −0.35 / ROBOT_RIGHT_BASE = +0.35）、per-arm の集合所属として読むと鏡像 layout（ee_left = +0.35, ee_right = −0.35）が通る → R6-07 |

追加の横断確認（task 指定）:
- 全 `P_*` が §8.1 に列挙され（checker Q1 = FAIL 0）、各 code に §3 / §8.3 の trigger 行がある（`P_ACCEPTANCE_RECORD_INVALID` は §8.3 06:513）。到達不能な code は無い。
- PT-01…PT-42 / OPP-1…OPP-17 は連番・重複無し（Q1）。
- `P_BASE_LAYOUT_MISMATCH` ↔ RS71 §0 #2（`RS71:24`）: 値を書かない方針は妥当だが機械検査可能な SSOT（`task_config.py:21-22`）を名指していない（R6-07 に含む）。
- baseline cell binding ↔ `P_TIMEOUT_CEILING`: 整合（06:286・06:497-498）。
- `AcceptanceRole` / `SuspensionReason` ↔ 05 (i)（05:191）/ 条件 12（05:240）/ 05:291: 語彙は整合。R6-04 / R6-05 参照。

## 3. PASS 2 — commissioning engineer としての反証

### 3.1 「全 P_* = 0 で通るが frozen / D0 §F / RS71 §0 が禁じることが起きる」構成

**S-A（RS71 §0 #4・gripper geometry LOCK）**: ToolPayloadSpec（06:123-128）= `tool_id`（個体 serial）/ 質量 / CoM / payload / TCP ref のみ。finger 設計（コ字形・`_ur15_2f85_koshape_actuated.xml`・RS71:26）を束縛する field も evidence subject も無い（TOOL_PAYLOAD_IDENTIFICATION subject = `resource_id#tool_id`、TOOL_IDENTITY = serial 等値・06:68/06:359）。Robotiq 2F-85 の finger は交換可能なので、**gripper 本体 serial を変えず finger を V-groove（DISCARDED）や別形状に替えた cell は、全 P_*・全 health check・全 evidence を通して CLOSED_LOOP permit に到達する**。→ R6-01（HIGH）。

**S-B（identity を通す arm 入替）**: 同機種 UR15 2 台を物理的に入替え、controller の報告 serial を再書込み → CONTROLLER_IDENTITY（serial 等値）pass、BASE_POSE_VERIFY（実測 base pose vs layout）pass（位置は元通り）、CEM subject `resource_id#robot_serial_ref` pass。TCP calibration（`calibration_id` で束縛・robot serial に束縛されない・06:128/06:359）は別個体のものが適用される。→ 契約層で検出不能（自己申告の限界）。§9 に honest scope として無く、cheap な強化（calibration subject に robot serial を含める・KINEMATIC_SIGNATURE 診断）も無い → R6-19（LOW）。

**S-C（base 再取付の許容内 drift）**: BASE_POSE_VERIFY の許容値が baseline に存在しない（06:495-505）ので、許容の定義自体が無い。fail-closed（評価不能 = FALSE）で読むと BASE_POSE_VERIFY が常時 FAIL = 全 profile が permit 不能、緩く読むと evaluator 任せ（OPP-9）。→ R6-07。参照が layout なので drift の累積は起きない（反例無し）。

**S-D（negative-control evidence の偽装）**: constant-True evaluator の profile でも、HEALTH_CHECK_RUN record（subject `hc#NEGATIVE`）の artifact_ref を「HEALTH_CONFIRMED(check_id = hc)」を含む audit 範囲に向ければ `P_EVIDENCE_UNBOUND`（存在）と `P_FAULT_EVIDENCE_UNVERIFIED`（health 分岐は CONFIRMED / FAILED のどちらでも可・06:297）を通る。NC#ARM_SWAP 等は逆に fault 分岐で評価不能。旧 audit 範囲の再利用は `profile_hash` 一致で縛られるが、1 つの範囲を複数 subject（例: `R_DEADLINE_MISS` と `NC#SCHEDULER_STALL`）が共有することは禁じられていない（同一現象なら妥当・記録として明示すべき）。audit 内容自体の改竄は U14 defer（05 OP-5）で本 review の外。→ R6-02（HIGH）。

**S-E（role registry の編集・自己証明）**: `role_registry_hash` は record 著者が任意の content-addressed artifact を指せる（06:489）。registry へ append できる 1 人が、自作 registry（架空 operator_ref 2 つに SAFETY_APPROVER / CUSTODY_APPROVER）を作り、ACCEPTED(CLOSED_LOOP) record を書ける。05 (i) は record 自身の hash で再評価するので後から資格を取り消しても既存 record に届かない。frozen `two_key_state`（O0/S0/V0）と ISL は AND なので**これだけで actuation には至らない**が、06:508「cell operator 単独では CLOSED_LOOP を許可できない」は本文の規則からは導けない。→ R6-05（MEDIUM）。

**S-F（baseline superseded・registry event 消失）**: monitor が生存しつつ event を落とすと、head record は ACCEPTED のまま、manager head-check（05:292）は baseline head を照合しない、(i) の再検証は次 permit まで無い → 旧 ceiling が lease 継続時間（≤ `lease_max_duration_s + boundary_dwell_s`）残る。「baseline registry」（05:88）は未定義語。→ R6-10（LOW）。

**S-G（(l) を通る clock skew）**: (l) は「後戻り」「evidence / approvals より前」「drift 超過」を捕らえる。**前進 jump** は 3 条件を全て通す（失効が早まる = 保守側で無害）。**速度 drift**（wall が遅れて失効が遅れる）は baseline の drift 項目に依存し、項目が無ければ「評価不能 = FALSE」で permit 全拒否（fail-closed だが baseline 著者に最小項目の指定が無い）。→ R6-08。加えて validity の**上限**が無い（`valid_until = 2999-…` は `P_VALIDITY_UNBOUNDED` を通る・06:296）ため、clock を触らずに calibration を実質無期限にできる → R6-11（MEDIUM）。

**S-H（two-key / one-key の穴）**: (1) R6-05 の自己証明 registry。(2) clearance record（05:541）は `role` 文字列の集合所属だけで資格照合が無い → SAFE_STOP / CHECKPOINT_DISABLED / GATEWAY_RESTART / MANAGER_RESTART の HOLD を 1 人が自己申告 role で解除できる（SAFETY は ISL record と AND）→ R6-06（MEDIUM）。(3) 逆方向: one-key の SUSPENDED 書込が validator に拒否され得る → R6-04（HIGH）。(4) GENESIS は ≥ 1 role（SAFEHOLD 起動のみ・問題なし）。

### 3.2 D0 §F / frozen との整合で反例が作れなかった点

- ISL 優先（D0:319）: `requires_safety_layer_health` True 必須（06:147）・heartbeat ≤ command_deadline_s ≤ ceiling（06:285-286）・(i′) SafeStop（05:516）。反例なし。
- frozen `granted == True` 必要条件（contracts_v2:354）と EP ceiling（EP JSON:213）: profile は条件を足すのみ（06:519）。反例なし。
- rate / hold / control_mode 等値（tensor_binding:125-127 / :132）: `P_RATE_MISMATCH` 等（06:269-270）、PT-01/02、05 INV-12。narrow 経路なし。
- initiation AND のみ（contracts_v2:151 / EP:101）: `InitiationStrengthening`（06:159-160）、05 B4。反例なし。
- 反循環（tensor_binding:23）: 06:258・06:512。`role_registry_hash` / `timing_baseline_hash` は record 側 ✓。
- composition OUT（06 §7）: 合成語彙なし ✓。3 軸分離（06 §1.1）✓。

## 4. Findings（型 = {id, severity, doc:line, frozen file:line, 再現, 最小修正（凍結 schema delta 0）}）

### R6-01 — HIGH — gripper finger 幾何（RS71 §0 #4 LOCK）が profile のどこにも束縛されない
- doc: 06:123-128（`ToolPayloadSpec`）、06:67-68（TOOL_IDENTITY = 個体 serial 等値）、06:359（TOOL_PAYLOAD_IDENTIFICATION subject = `resource_id#tool_payload.tool_id`）、06:13（本 doc が「tool・payload」条件を宣言すると主張）、06:547（OPP-14 は collision hull の workspace 評価のみを open にし、finger 設計の identity には触れない）。
- frozen: `thread_isaac_lab/thread-vault/04-Specs/RS71-System-Spec-SSOT.md:26`（#4 GRIPPER GEOMETRY LOCKED・authoritative asset `_ur15_2f85_koshape_actuated.xml`・◇ V-groove は DISCARDED）。
- 再現: gripper 本体（serial 一致）に別 finger（V-groove 等）を装着した cell。TOOL_IDENTITY（serial 等値）pass、TOOL_PAYLOAD_IDENTIFICATION（subject = serial・内容は質量 / CoM 程度で幾何を要求しない）pass、他の P_* は finger に依存しない → CLOSED_LOOP permit の profile 側条件を全て満たす。RS71 #4 が human-LOCK した幾何と異なる tool で学習 policy が走る。
- 最小修正: `ToolPayloadSpec` に `finger_design_ref: str` + `finger_geometry_sha256: str`（RS71 #4 の authoritative asset の content hash）を追加（deployment 層型・凍結 delta 0）。第 1 評価点で RS71 #4 の pin と等値でなければ `P_TOOL_GEOMETRY_MISMATCH`（`P_ROBOT_MODEL_MISMATCH` と同型）。`finger_geometry_sha256` を §3 hash 列挙と 05 (j) に追加。TOOL_PAYLOAD_IDENTIFICATION artifact に finger 設計の attest（RS71 #4 の 16.00 mm opening 等の実測）を要求し、TOOL_IDENTITY は serial ∧（報告可能なら）finger 設計 id の等値とする。

### R6-02 — HIGH — negative-control evidence の内容照合が空虚（health 分岐）または評価不能（NC 分岐）
- doc: 06:291（check_id#NEGATIVE の要求）、06:297（`P_FAULT_EVIDENCE_UNVERIFIED`: fault 分岐「kind = FAULT ∧ fault == subject_ref の code」／ health 分岐「kind ∈ {HEALTH_CONFIRMED, HEALTH_FAILED} ∧ payload に check_id」）、06:378 / 06:390-393（`negative_controls`・subject 'NC#<name>'・期待 audit 内容は comment）、06:391（「P_FAULT_EVIDENCE_UNVERIFIED は下の期待 audit 内容で照合」— 06:297 の規範行と矛盾）、06:359（subject 文法に 'NC#<name>' / '<check_id>#NEGATIVE' 無し）、06:422 (1)（評価不能 = 違反）。
- frozen: D0 `:318`（fail-closed）/ `:323-324`（acceptance = fault-injection independence proof）— 本文は「検出できたこと」を証拠化する意図だが述語がそれを弁別しない。
- 再現 (a) 空虚: constant-True evaluator の CONTROLLER_IDENTITY check `hc1`。HEALTH_CHECK_RUN record（subject `hc1#NEGATIVE`）の artifact_ref を HEALTH_CONFIRMED(check_id = hc1) を含む audit 範囲に向ける。06:291 は「存在」を `P_EVIDENCE_UNBOUND` で見るだけ、06:297 health 分岐は CONFIRMED でも真 → 両方 pass → arm 入替を検出できない evaluator で CLOSED_LOOP へ。(b) 評価不能: FAULT_INJECTION_RESULT（subject `NC#ARM_SWAP`）は kind により fault 分岐に入り、'NC#ARM_SWAP' は code でないため「fault == subject_ref の code」が評価不能 → 規則 (1) で違反 → `P_FAULT_EVIDENCE_UNVERIFIED` が常に立ち CLOSED_LOOP は不成立（MIN_NEGATIVE_CONTROLS は必須集合）。どちらの読みでも欠陥。
- 最小修正: 06:297 を subject 文法ごとの期待内容表に書き換える — `<check_id>` → HEALTH_CONFIRMED(check_id)；`<check_id>#NEGATIVE` → **HEALTH_FAILED**(check_id) かつ FAULT(R_HEALTHCHECK_FAILED)；`<code>` → FAULT(fault == code)；`<code>#<trigger>` → FAULT(fault == code ∧ payload.trigger == trigger)；`NC#<name>` → 06:392 の表；それ以外 = FALSE。06:359 の subject 文法に 'NC#<name>' と '<check_id>#NEGATIVE' を追加。BASE_POSE_VERIFY の #NEGATIVE は物理的に base を動かせないため、許容される誘発方法（測定入力の摂動 / 別 layout artifact による test profile）を明記する。

### R6-03 — MEDIUM — MIN_FAULT_INJECTION の '#DEADLINE' / '#EVENT' 要件と '<code>#<trigger>' subject が `FaultInjectionRequirement` で表現不能（必須集合が unsatisfiable）
- doc: 06:385-389（MIN_FAULT_INJECTION に `R_CALIBRATION_EXPIRED_ACTIVE#DEADLINE` / `R_EVIDENCE_EXPIRED_ACTIVE#DEADLINE` / `R_PROFILE_SUSPENDED#EVENT`；timeout 駆動は subject '<code>#<trigger>' で「timeout 経路そのものを要求」）、06:367-369（`FaultInjectionRequirement` = `runtime_fault_code` + `must_be_exercised_before` のみ・「05 enum に無い名 = P_FAULT_CODE_UNKNOWN」）、06:298（`P_FAULT_CODE_UNKNOWN`）、06:294（`P_EVIDENCE_UNBOUND`: `subject_ref == runtime_fault_code`）、06:316（`P_EVIDENCE_POLICY_TOO_WEAK` が ⊇ MIN_FAULT_INJECTION を要求）。
- frozen / 05: 05:632-648（`RuntimeFaultCode` に '#' 付き member は無い）、05:700（trigger 副因は `R_SAFETY_LAYER_LOST` / `R_BOUNDARY_DWELL_EXCEEDED` の 2 code のみ定義・DEADLINE / EVENT 無し）。
- 再現: policy が MIN_FAULT_INJECTION を満たすには `runtime_fault_code = "R_PROFILE_SUSPENDED#EVENT"` を書く必要 → `P_FAULT_CODE_UNKNOWN`。`"R_PROFILE_SUSPENDED"` と書けば `P_EVIDENCE_UNBOUND` は subject `R_PROFILE_SUSPENDED` の record を要求し、06:389 が要求する `R_PROFILE_SUSPENDED#EVENT` subject とは一致しない。timeout 駆動 code（`R_SAFETY_LAYER_LOST` 等）も同じ二律背反。
- 最小修正: `FaultInjectionRequirement` に `trigger: str | None` を追加（deployment 層）。`P_FAULT_CODE_UNKNOWN` = `runtime_fault_code ∉ RuntimeFaultCode ∨ trigger ∉ 05 §8 trigger 語彙`。`P_EVIDENCE_UNBOUND` の subject = `code[#trigger]`。05 §8（05:700）の trigger 語彙に DEADLINE / EVENT / HEARTBEAT_TIMEOUT / HEALTH_FAILED / DWELL_TIMEOUT / ATTEMPTS_EXHAUSTED を列挙し、`R_CALIBRATION_EXPIRED_ACTIVE` / `R_EVIDENCE_EXPIRED_ACTIVE` / `R_PROFILE_SUSPENDED` の FAULT payload に副因を義務化する。

### R6-04 — HIGH — one-key の SUSPENDED / REVOKED record が `P_ACCEPTANCE_RECORD_INVALID` に落ち、失効 event が出ない
- doc: 06:513（検査は「record 書込時」に全 record へ適用；条件「CLOSED_LOOP ∈ allowed_lease_modes ⇒ ∃ SAFETY_APPROVER ∧ ∃ CUSTODY_APPROVER…」「generation が同一 profile_hash の直前 record + 1 でない」は `state` で限定されない）、06:514（SUSPENDED / REVOKED は「hash field を複写・approvals = 認可された一者または MONITOR」— `allowed_lease_modes` / `generation` の扱い未記載）、06:481 / 06:507（generation = 受理世代・SUSPENDED → ACCEPTED は新 generation ⇒ SUSPENDED は同 generation と読める）、06:509（記録が先・通知が後）、06:422 (1)（評価不能 = 違反）。
- 05: 05:291（「記録なしの event は無い」— SUSPENDED record の append が event の前提）、05:292（head-check は head の state のみ）。
- frozen: D0 `:318`（monitor failed ⇒ system fails to safe-stop）— safety 側の停止経路が validator に拒否される構造は fail-closed の逆。
- 再現: gen 1 ACCEPTED(allowed = {SHADOW, CLOSED_LOOP}) が head。ISL 異常で monitor が SUSPENDED(ISL_ANOMALY, approvals = ((MONITOR, mon, t),), allowed = 複写 or 未定義, generation = 1 or 2) を append。読み A（allowed 複写・generation 1）: two-key 条件偽 ∧ generation ≠ 直前 + 1 → 拒否。読み B（generation 2）: two-key 条件偽 → 拒否。読み C（allowed 未定義）: 評価不能 → 違反。いずれも append 拒否 → 05:291 の event が発生せず、head は ACCEPTED のまま → 活性 CLOSED_LOOP lease は継続。
- 最小修正: 06:513 の two-key / SHADOW 条件を `state ∈ {PROPOSED, ACCEPTED}` に限定；「generation + 1」規則を ACCEPTED（および PROPOSED）にのみ適用し、SUSPENDED / REVOKED は superseded record と同一 generation + `supersedes_record_hash` 必須と明記；SUSPENDED / REVOKED は `allowed_lease_modes = ()` を持つ；SUSPENDED / REVOKED の append は approvals 不足で拒否しない（fail-safe 方向・拒否理由は fork / supersedes 欠落のみ）と明記。PT に「MONITOR 単独の SUSPENDED append が受理される」試験を追加。

### R6-05 — MEDIUM — `role_registry_hash` が record 著者の自己証明で、権威ある head も資格取消の伝播も無い
- doc: 06:489（record 内の content-addressed registry・「受理時に照合」）、06:513（資格照合は record の registry に対して）、06:508（「cell operator 単独では CLOSED_LOOP を許可できない」）、06:511（registry の ACL は namespace 分離の言及のみ）；05:191 (i)（head 再評価も record の hash で解決）。
- frozen: EP `:156`（closed-loop authority は O0/S0/V0 two-key + 独立安全 gate を conjoin）— frozen 側の two-key は AND のまま残るため actuation 単独到達には至らないが、deployment 層の two-key 主張（06:508）は本文から導けない。
- 再現: registry append 権限を持つ 1 人が、operator_ref `a` = SAFETY_APPROVER・`b` = CUSTODY_APPROVER と書いた registry artifact R を作り `role_registry_hash = H(R)` の ACCEPTED(CLOSED_LOOP) record を書く。06:513 の全条件（存在・相異・資格）が真。後日 `a` の資格を本物の registry から外しても record の hash は R を指し続け、05 (i) は真のまま。
- 最小修正: 権威ある `RoleRegistryRef`（content-addressed・head 付き）を `CellSafetyTimingBaseline` に載せる（`role_registry_hash` field 追加）か AuthorityManager の config artifact とし、record 書込時と 05 (i) / 条件 12 で `record.role_registry_hash == 現 head` を要求；head 更新 = 全 bound record を SUSPENDED(新 reason `ROLE_REGISTRY_SUPERSEDED`・deployment enum)。record の hash は監査用に残す。

### R6-06 — MEDIUM — clearance record の role が自己申告 label で、資格照合が無い
- doc: 05:541（「該当」= `record.role ∈ profile.escalation.clearance_roles[reason]` ∧ profile_hash ∧ seq）、06:218（`clearance_roles: tuple[tuple[str, str], ...]` — role は自由文字列・`AcceptanceRole` でも registry でもない）、05:534-539（SAFE_STOP / CHECKPOINT_DISABLED / MANAGER_RESTART / GATEWAY_RESTART の解除前提 = profile role のみ）。
- frozen: D0 `:319-320`（stabilized_post_action_state による resumption eligibility）— SAFETY 行は ISL record と AND だが他 4 reason は人的 role のみ。
- 再現: SAFE_STOP 後、audit へ書ける 1 人が CLEARANCE_RECORDED{role = "SAFETY_OFFICER", operator_ref = 自分} を append → CAS 条件 10 真 → 完全経路で再 lease（CLOSED_LOOP には acceptance two-key が別途要るが、SHADOW / 同 profile の再起動は 1 人で可能）。
- 最小修正: CLEARANCE_RECORDED payload に `role_registry_hash` を持たせ、条件 10 で R6-05 の権威 registry に対する資格照合を要求；`clearance_roles` の role 語彙を `AcceptanceRole` または registry の role 集合に限定。

### R6-07 — MEDIUM — BASE_POSE_VERIFY / `P_BASE_LAYOUT_MISMATCH` の許容値が baseline に無く、評価点も左右割当も未定義
- doc: 06:68（「baseline 許容内」）、06:311（「baseline 許容内で一致」・評価点無し）、06:495-505（`CellSafetyTimingBaseline` の field に許容値無し）、06:422 (2)（第 1 / 第 2 list のどちらにも `P_BASE_LAYOUT_MISMATCH` 無し）、06:83（kinematic_layout が「固定 base Y = ∓0.35 を含む」）。
- frozen: `RS71-System-Spec-SSOT.md:24`（「bases fixed at Y = ∓0.35」— 左右割当なし・`task_config.py:21-22` を引く）、`thread_isaac_lab/configs/task_config.py:21-22`（ROBOT_LEFT_BASE Y = −0.35 / ROBOT_RIGHT_BASE Y = +0.35）。
- 再現: (i) 許容値が存在しないので規則 (1) では評価不能 = 違反 → 全 profile が `P_BASE_LAYOUT_MISMATCH` / BASE_POSE_VERIFY FAIL（no profile can pass）。(ii) 緩く読み「各 arm の Y ∈ {+0.35, −0.35}」とすると、ee_left = +0.35 / ee_right = −0.35 の鏡像 layout が pass し、BASE_POSE_VERIFY（実測 vs layout）も pass → 学習 policy が左右反転した cell で走る。
- 最小修正: baseline に `base_pose_tolerance: tuple[CanonicalDecimal /*m*/, CanonicalDecimal /*rad*/]` を追加；`P_BASE_LAYOUT_MISMATCH` を record 書込時 + 第 2 評価点の list に入れる；検査を resource_id ごとの符号付き等値（ee_left ↔ ROBOT_LEFT_BASE, ee_right ↔ ROBOT_RIGHT_BASE）と定義し、機械可読 SSOT として `task_config.py:21-22` を名指す（値は引き続き本 doc に書かない）。

### R6-08 — MEDIUM — baseline `diagnostic_items` の最小集合が無く、周期層が空で成立する
- doc: 06:504（diagnostic_items = 自由文字列項目）、06:68（identity 系の周期再検証は「baseline diagnostic item として」）、06:341（周期診断は baseline 項目のみ）；05:88（monitor liveness 上限 = 項目 `VALIDITY_MONITOR_LIVENESS`）、05:191 (l)（drift 許容 = baseline 項目）、05:292（head-check 周期 = baseline 項目）。最小集合を要求する P_* は無い（06:284-316 に該当行なし）。
- frozen: D0 `:318`（missed heartbeat ⇒ safety alive を仮定しない）・D0 `:321`（response budget = worst-case）— 周期層の存在を baseline 著者の裁量に置くのは fail-closed でない。
- 再現: `diagnostic_items = ()` の baseline を acceptance record が指す。`P_TIMEOUT_CEILING` は approved_ceiling だけを見る → 受理。monitor 停止の上限なし（heartbeat 欠落を「超えた」と判定できない）、identity の周期再検証なし、head-check 周期なし。(l) の drift 項だけは評価不能 = FALSE で permit を止める（fail-closed だが baseline 著者に理由が示されない）。
- 最小修正: `MIN_DIAGNOSTIC_ITEMS = {VALIDITY_MONITOR_LIVENESS, CLOCK_DRIFT, REGISTRY_HEAD_CHECK, CONTROLLER_IDENTITY, TOOL_IDENTITY, SAFETY_LAYER_IDENTITY, BASE_POSE_VERIFY}` を §8.3 に規範化し、record 書込時 + 第 2 評価点で `baseline.diagnostic_items ⊇ MIN` を `P_TIMEOUT_CEILING`（baseline 妥当性）か新 `P_BASELINE_INCOMPLETE` で検査；項目 → `health_checks[].check_id` の対応（周期実行する evaluator）を明記。

### R6-09 — MEDIUM — `P_TIMEOUT_ORDER` の baseline 依存項が第 1 評価点で循環
- doc: 06:285（`max_reselect_attempts × worst_case_attempt_s + selector_overhead_s ≤ boundary_dwell_s`「worst_case / overhead は timing baseline の値」）、06:422 (2)（`P_TIMEOUT_ORDER` = 第 1 評価点 = 登録時；受理 record は「全 P_* = 0 を満たした profile_hash について」書く）、06:286（baseline は acceptance record の `timing_baseline_hash` 経由でのみ束縛）。
- frozen: —（内部循環。前提 = 06 §8.1 規則 (1) 評価不能 = 違反）。
- 再現: 登録時に baseline が束縛されていない → 当該不等式は評価不能 → `P_TIMEOUT_ORDER` → 全 P_* = 0 に到達せず record を書けない → baseline を束縛できない。
- 最小修正: 当該不等式を record 書込時（`P_TIMEOUT_CEILING` と同じ評価点）へ移す、または「第 1 評価点は `timing_baseline_hash` を持つ PROPOSED record の下で行う」と §8.3 状態遷移に明記する。

### R6-10 — LOW — baseline supersession の検出が monitor 単独経路で、参照する「baseline registry」が未定義
- doc: 06:509（supersession → SUSPENDED(BASELINE_SUPERSEDED)）、06:549（OPP-16: 発行主体・改訂手続 open）；05:88（monitor 最小入力「baseline registry の supersession」— 本語は 05/06 でここだけ）、05:292（head-check は record の state / hash・registry 可読・monitor heartbeat・diagnostic 失敗のみ）、05:191 (i)（baseline 再検証は permit 発行時のみ）。
- frozen: D0 `:321`（response budget は worst-case）— 改訂された budget が lease 中に反映されない窓。
- 再現: monitor 生存・event 消失（または baseline registry に到達不能）。head は ACCEPTED、head-check は真、lease は `lease_max_duration_s + boundary_dwell_s` まで旧 ceiling で継続。
- 最小修正: head-check に「cell_id の baseline head ≠ record.timing_baseline_hash ⇒ TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED)」を追加し、baseline registry を content-addressed head を持つ artifact として §8.3 に定義（発行主体は OPP-16 のまま carry）。

### R6-11 — MEDIUM — calibration / evidence の validity に上限が無く、`P_VALIDITY_UNBOUNDED` が第 2 評価点の閉じた list に無い
- doc: 06:296（`P_VALIDITY_UNBOUNDED` = None のみ）、06:364（None = 失効なし）、06:499（上限は acceptance record にのみ `max_acceptance_validity_s`）、06:422 (2)（閉じた list に `P_VALIDITY_UNBOUNDED` 無し；CALIBRATION_RECORD は MIN_CLOSED_LOOP でのみ required → 第 2 評価点で初めて「required record」になる）。
- frozen: D0 `:354-355`（monotonic clock・単一 origin）— wall-clock 期限の上限が無いと OP-19 三層の deadline 層が実質無効化できる。
- 再現: `CalibrationRef(kind = TCP, valid_until = "2999-01-01T00:00:00Z")` と同 valid_until の CALIBRATION_RECORD。`P_VALIDITY_UNBOUNDED`（None でない）pass、`P_CALIBRATION_EXPIRED` / `P_EVIDENCE_EXPIRED` pass、`validity_deadline_mono` は acceptance 期限が支配 → TCP calibration は事実上無期限。
- 最小修正: baseline に `max_validity_s: dict[CalibrationKind | DeploymentEvidenceKind → s]` を追加し、`P_VALIDITY_UNBOUNDED` を「None ∨ valid_until − measured_at > max」に拡張；`P_VALIDITY_UNBOUNDED` を第 2 評価点 list に追加（required になった record に対して）。

### R6-12 — LOW — 06:340「`AcceptedEnvelope.terms` の 3 項 ← §4」が旧文
- doc: 06:340 vs 06:333（「profile 4 項」）、06:323-327（5 term）；05:367（`EnvelopeTermKind` 5 member）。
- 再現: 文字どおり読むと INTER_ARM 項が lease へ写されない（R3-02 fold 不完全）。
- 最小修正: 「4 項（DEPLOYMENT_WORKSPACE / CONTROLLER / SAFETY_RESTRICTION / INTER_ARM）」に修正。

### R6-13 — LOW — checker Q2 の主張「06 §2.1 の hash field ⊆ 05 §3.4 (j) 列挙」が実装より強い
- doc: 08:23 / 06:690 anchor 16 / 05:191（Q2 の説明）；`check_review_candidate.py:178-187`（`h not in jl[0]` — (j) を含む bullet **全行**（05:191・4,119 文字・(a)–(l) 全部）への substring）、`:179-180`（走査は §2.1 のみ・§2.2 の `deployment_evidence_policy_hash` は対象外・`artifact_sha256` は 3 型で同名）。
- 再現（本 dir `q2test/` に複製して実施・凍結物非接触）: 05 複製の (j) 列挙から `evaluator_sha256` を削除 → `python3 check_review_candidate.py <06 複製> --profile` = **FAIL=0**（(a) に同語があるため）。`tensor_binding_hash` も (g) の出現で pass。
- 最小修正: Q2 は「(j) profile が宣言する」から「checker Q2 が」までの部分文字列だけを対象にし、qualified 名（`zones[].geometry_sha256` 等）で照合；走査範囲を §2.1–§2.2 に拡張。

### R6-14 — LOW — `P_EVIDENCE_UNBOUND` 行の ∀ 列挙に arm 単位 kind が無い（R4-12 fold 不完全）
- doc: 06:294（明示 ∀ は CALIBRATION_RECORD / HEALTH_CHECK_RUN / FAULT_INJECTION_RESULT のみ）vs 06:359（CEM / TOOL_PAYLOAD_IDENTIFICATION は arm ごと）。
- 再現: 列挙を網羅と読む validator は CEM 1 record（ee_left）で両 arm の存在要件を満たす。
- 最小修正: 06:294 に「CONTROLLER_ENVELOPE_MEASUREMENT / TOOL_PAYLOAD_IDENTIFICATION ∀ `cell.arms[]`（subject = §6.1）」を追加。

### R6-15 — LOW — `P_SAFETY_BUDGET_EXCEEDED` の「当該 arm の FI」が解決不能
- doc: 06:287（FI = 当該 arm の `R_SAFETY_LAYER_LOST#HEARTBEAT_TIMEOUT` record・struct に arm field 無し）vs 06:359（FAULT_INJECTION_RESULT の subject = fault code のみ）。
- 再現: 両 arm で別々の FI record を作ろうとすると subject が同一（kind × subject で 1 record）→ 「当該 arm の」は指定不能 → 評価不能。
- 最小修正: FI を cell 単位（両 arm 負荷条件を `load_condition_ref` に含む worst-case）と定義し式を「∀ arm: … + CEM[arm].stop_time_worst_case_s[arm.stop_class]」に書き換える、または artifact struct に `per_arm: dict[resource_id → …]` を追加。

### R6-16 — LOW — SHADOW lease の validity deadline の扱いが 06 と 05 で矛盾
- doc: 06:341（「SHADOW_NON_AUTHORITY lease は … 次 permit 評価で可」）、06:442 PT-13（「SHADOW lease は次 permit まで継続」）、06:654 vs 05:290 / 05:427 / 05:742 INV-34（「mode を問わず」・R4-18）。
- 再現: PT-13 の期待結果は 05 の遷移表と食い違う（SHADOW でも deadline 到達で TRANSFER_TO_SAFEHOLD）。
- 最小修正: 06:341 / PT-13 を 05 に揃える（SHADOW も deadline 到達で無効化・効果は記録のみ）。

### R6-17 — LOW — 停止性能 evidence（FI）の失効が HOLD に写像され 05:340 の基準と不整合
- doc: 06:342（EVIDENCE(SAFETY_LAYER_ACCEPTANCE | CONTROLLER_ENVELOPE_MEASUREMENT) → SAFE_STOP；その他 → HOLD）vs 05:340（「SAFE_STOP（安全機能・TCP・停止性能に関わる失効）」）、06:287（FI が停止性能予算の入力）。
- 最小修正: FAULT_INJECTION_RESULT（subject `R_SAFETY_LAYER_LOST#…`）を SAFE_STOP 行に追加するか、05:340 の基準文言を 06 表に一致させる。

### R6-18 — LOW — approvals の相異範囲・validity 上限の基準時刻が未定義
- doc: 06:488（「operator_ref は role ごとに相異」）vs 06:513（相異は SAFETY / CUSTODY / OPERATOR 間のみ）；06:513（「baseline `max_acceptance_validity_s` 超過」の起点が write time か approvals[].t か未記載）。
- 最小修正: 06:488 を 06:513 の範囲に合わせる；超過判定の基準 = registry 書込 wall time と明記。

### R6-19 — LOW — identity 検査の前提（自己申告 serial・tool の serial 報告能力）が honest scope に無い
- doc: 06:68-70（CONTROLLER_IDENTITY / TOOL_IDENTITY = 報告値の等値）、06:128 / 06:359（TCP CALIBRATION_RECORD subject = calibration_id・robot serial 非束縛）、06:525（§9 項 7 は機能安全内容のみを外に置く）。
- frozen: RS71 `:23`（DUAL-ARM）— 個体入替は左右の calibration を取り違える。
- 再現: 同機種 2 台を物理入替 + serial 再書込 → CONTROLLER_IDENTITY / BASE_POSE_VERIFY / CEM subject 全て pass、他個体の TCP calibration が適用される。Robotiq 2F-85 が個体 serial を報告しない構成では TOOL_IDENTITY が必須集合ゆえ全 profile が permit 不能。
- 最小修正: §9 に「identity 検査は controller / tool の自己申告を信頼する」を明記；CALIBRATION_RECORD subject を `calibration_id#resource_id#robot_serial_ref` に拡張；diagnostic item `KINEMATIC_SIGNATURE`（関節 offset 較正署名）を任意項目として定義；tool が serial を報告できない場合の代替 attest（TOOL_PAYLOAD_IDENTIFICATION の operator attest + 周期目視）を明示。

### R6-20 — LOW — 予算検査の入力（FI artifact の worst-case 値）が引用 audit と照合されない
- doc: 06:287（FI.detection_to_safehold_worst_case_s 等を式に使う）、06:297（audit 照合は FAULT record の存在のみ）、06:318（物理事実は evidence に委ねる）。
- 再現: `detection_to_safehold_worst_case_s = 0` の FI artifact + 実 FAULT を含む audit 範囲 → 予算 pass。audit には FAULT と DISPOSITION / SafeHold 到達の `t_mono` があり cheap に反証可能。
- 最小修正: `P_FAULT_EVIDENCE_UNVERIFIED` に「FI struct を持つ record は `detection_to_safehold_worst_case_s ≥ max(t_mono(DISPOSITION) − t_mono(FAULT))` over 引用範囲」を追加。

### R6-21 — LOW — 05:290 の記録列が ACCEPTANCE deadline の fault を欠く
- doc: 05:290（記録 = FAULT(R_CALIBRATION_EXPIRED_ACTIVE | R_EVIDENCE_EXPIRED_ACTIVE)）vs 05:742 INV-34（違反時 = … / R_PROFILE_SUSPENDED）、05:184（`validity_deadline_source.kind` に ACCEPTANCE）。
- 最小修正: 05:290 の記録列に `R_PROFILE_SUSPENDED`（ACCEPTANCE 到達）を追加。

## 5. 集計と verdict

| severity | 件数 | id |
|---|---|---|
| CRITICAL | 0 | — |
| HIGH | 3 | R6-01, R6-02, R6-04 |
| MEDIUM | 7 | R6-03, R6-05, R6-06, R6-07, R6-08, R6-09, R6-11 |
| LOW | 11 | R6-10, R6-12, R6-13, R6-14, R6-15, R6-16, R6-17, R6-18, R6-19, R6-20, R6-21 |

**verdict = HOLD**（HIGH 3 件が未反証）。全件が凍結 4 file を触らない最小修正で閉じる（新規 field / enum は全て deployment / runtime 層）。R6-01 は RS71 §0 #4 不変前提の**束縛欠落**であり前提の変更ではない（不変前提に触れる案は出していない）。

## 6. 主張しないこと

- 本 review は AI reviewer の準備レビュー（08 §6）であり two-key / Rs 裁定 / gate PASS を代替しない。
- 凍結 4 file・`eval_runs/` は非接触（本 dir のみ書込・`q2test/` は複製）。
- 先行 round で fold 済の項目は再報告していない（fold の不完全・矛盾のみ）。Rs 裁定の設計方向（B+ / baseline 外部化 / registry / OP-19）は finding にしていない。
