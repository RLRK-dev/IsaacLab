# R5 review — WMSO RUNTIME SPEC v0.2.6 REVIEW CANDIDATE（contract fidelity + runtime safety・adversarial）

- reviewer: R5（別 context・authority 無し・凍結物へ非接触・eval_runs/ 配下へ書込みなし）
- 実施: 2026-09-05 06:35–06:52 UTC（`date -u` 実測。開始 06:35:35 / 最終 grep 06:46:45 / 報告書込 06:52:42）
- 対象: `$PKG/05_WMSO_RUNTIME_SPEC_v0.2_REVIEW_CANDIDATE_20260903.md`（header v0.2.6・1022 行・完読）／ 副次 `$PKG/06_WMSO_INDUSTRIAL_PROFILE_v0.2_REVIEW_CANDIDATE_20260903.md`（v0.2.6・690 行・完読）。`$PKG` = `eval_runs/troot_optE_dapg_wholeroute_scope_20260701/WMSO_EXACT_BASELINE_REVIEW_v0.2_20260903`、`$D` = `eval_runs/troot_optE_dapg_wholeroute_scope_20260701`。HEAD = `f9fe9757`。
- 凍結参照（sed で読んだ範囲）: contracts_v2 `:36-40` / `:60-64` / `:140-152` / `:300-390` / `:470-552` / `:568-572`；tensor_binding `:3-6` / `:22-25` / `:38-42` / `:50-54` / `:63-68` / `:73-84` / `:94-100` / `:123-140` / `:171-186` / `:253-256` / `:287-291` / `:311-315` / `:330-338` / `:372-376`；D0 `:175-182` / `:205-225` / `:240-250` / `:265-285` / `:308-322` / `:352-372`；EP md `:99-103` / `:134-138` / `:146-158` / `:161-165`；EP JSON `:3-7` / `:195-215`；C3 `:3-7` / `:30-36` / `:61-67`；slice prereg `:57-61`；RS71 §0 `:18-32`。
- 前提 (5)(6) 遵守: 陽性対照の注入欠陥（10_ §1 の 8 件）は実本文で再現しない（例: 旧 epoch 拒否は §3.5 事後条件 05:245 で CAS 同一線形化点・permit one-shot 05:244/357・CAS 事後条件に `∪ {consume_offer_id}` 05:243・SHADOW actuation DISABLED 05:472）ため報告しない。v0.2.5 の設計方針（Rs 裁定・11_ §8）は finding にしない — 本文が方針を正しく・整合的に・fail-closed に実現しているかのみを見た。

## 0. 前提の実測

### 0.1 pin 再測（`bash $PKG/verify_exact_baseline_pins.sh`・素の bash）

```text
PASS  D1.1-A DESIGN v2.11.2 (worktree)  00192d20ca00b654cf6cfdb9d04b03ca14adfd0b93f2105c0fea28c295ff8aff
PASS  EvidencePolicy v1.9 md (worktree)  c474acea7c58acc22050c2ad9944fd45a18f5c76967964b42d11922e28fa27e7
PASS  EvidencePolicy v1.9 json (worktree)  e63176af9bc3a246b1c32db369ec59f8d09a4c96c381bb03a3a6024bd9811c6e
PASS  D1.1-B DESIGN v13 (worktree)  5a1874d3be8b98b8aeaace73890d8021cbc7f9814e048741f7f58d6746f349a6
PASS  D1.1-A DESIGN git blob id (HEAD)  1353430228a90ad36dd190a9359cd81da52e8242
PASS  D1.1-B DESIGN git blob id (HEAD)  ebe8154abf2461a6b24db05728b20e5cee949fb2
PASS  D1.1-A DESIGN sha256 of blob 135343…  00192d20ca00b654cf6cfdb9d04b03ca14adfd0b93f2105c0fea28c295ff8aff
PASS  D1.1-B DESIGN sha256 of blob ebe815…  5a1874d3be8b98b8aeaace73890d8021cbc7f9814e048741f7f58d6746f349a6
PASS  D1.1-A DESIGN at frozen commit 54f90a7d  00192d20ca00b654cf6cfdb9d04b03ca14adfd0b93f2105c0fea28c295ff8aff
PASS  D1.1-B DESIGN at frozen commit 07250f4a  5a1874d3be8b98b8aeaace73890d8021cbc7f9814e048741f7f58d6746f349a6
PASS  evidence_policy_definition_hash (embedded command)  e7ca43093084c167a209b008533a66d26a1fd3223d2a3c11274d28306c3ff803
policy_semver=1.9.0
HEAD=f9fe975783aa63f43989c4be7c7bb2ae544ba90f branch=claude/v0-2-design-candidates-snqbmy
```
→ **11/11 PASS**（原本 = `$OUT/pin_recheck.txt`）。

### 0.2 機械検査（読む前に実行）

```text
$ python3 $PKG/check_review_candidate.py $PKG/05_... --runtime
== 05_WMSO_RUNTIME_SPEC_v0.2_REVIEW_CANDIDATE_20260903.md: citations=153 FAIL=0 WARN=11   (exit 0)
$ python3 $PKG/check_review_candidate.py $PKG/06_... --profile
== 06_WMSO_INDUSTRIAL_PROFILE_v0.2_REVIEW_CANDIDATE_20260903.md: citations=63 FAIL=0 WARN=25
```
→ **FAIL = 0**（WARN は全て C2 heuristic。原本 = `$OUT/checker_05.txt`）。O5（fault code の失敗表 total）/ O6（id 連番）/ O7（条件 10 = §5.4 集合）/ O8（06 RuntimeTimeouts ⊆ 05 TimingBinding）/ Q2（06 hash field ⊆ 05 §3.4 (j)）は全て通過。⚠ 機械検査は「fault code → 行」の方向しか見ない。「遷移行 → fault code / disposition が定まる」の逆方向（totality の反対側）は本 review で手動確認し、R5-02 / R5-08 で欠落を検出した。

## 1. 判定

**PASS-WITH-CONDITIONS** — 未反証 CRITICAL / HIGH = 0。MEDIUM 6・LOW 8。全 finding は凍結 4 file への schema delta 0 の fold 案を持つ（runtime / deployment 層の文言・列挙・1 行追加のみ）。P1–P5（§3.1）は v0.2.6 本文で反例を構成できなかった（§4 参照）。

| severity | 件数 | id |
|---|---|---|
| CRITICAL | 0 | — |
| HIGH | 0 | — |
| MEDIUM | 6 | R5-01 / R5-02 / R5-03 / R5-04 / R5-05 / R5-06 |
| LOW | 8 | R5-07 / R5-08 / R5-09 / R5-10 / R5-11 / R5-12 / R5-13 / R5-14 |

## 2. PASS 1 — v0.2.6 fold 表（05 §13・17 行）の回帰

各行について (a) fix が実現されているか、(b) 05 / 06 の他文と矛盾しないか、(c) 到達不能状態・未定義記号・集合不一致が無いか、を本文 grep で確認した。

| # | fold 行（05:979-995） | (a) 実現 | (b) 矛盾 | (c) 未定義 / 集合 | 判定 |
|---|---|---|---|---|---|
| 1 | R3-01 / R4-01 / R3-16 — `GatewayCommand.arm_targets`・§4.3 INTER_ARM・§3.7・INV-36 | ✓ 05:506 `arm_targets: tuple[(resource_id, setpoint_ref)]`、05:461 INTER_ARM = 同一 command の両 arm 線分の対、05:293 §3.7 行、05:744 INV-36 | §2 admission（05:82）は「AcceptedEnvelope 連言」に INTER_ARM を内包（R3-02 で term 化）。06 §4（06:324/327）は `cmd.arm_targets[].resource_id` を index に使用 — 一致。06 §2.1（06:110）comment 一致 | 「静止を確認」の述語（計測 TCP vs 直近 setpoint の許容）は未定義だが「動いていれば評価不能 = FALSE」（05:461）で fail-closed | **OK** |
| 2 | R3-02 — `EnvelopeTermKind.INTER_ARM`・T-13・OP-2 | ✓ 05:367 enum 5 member、05:762 T-13「× 5」、05:806 OP-2「profile 4 term」 | ✗ 06 §5 の lease 写像文（06:340）が「`AcceptedEnvelope.terms` の **3 項** ← §4」のまま。06 §4（06:323-327）と 05 §4.3 は profile 4 項 | — | **R5-09（LOW）** |
| 3 | R3-17 / R4-02 — §3.4 (j) hash 列挙・checker Q2 | ✓ 05:191 (j) の列挙に `kinematic_layout.artifact_sha256` / `pairwise_keepout_sha256[]` / `swept_volume_predicate_sha256[]` / `conjuncts[].schema_hash`。Q2 FAIL 0 | 06 §3（06:308）の列挙と一致 | — | **OK** |
| 4 | R3-03 / R4-18 — `effective_expires_at` は定義される項だけの min・SHADOW で不能判定にならない・validity deadline は mode を問わず | ✓ 05:188 / 05:180 / 05:429 / 05:427 | ✗ 05:427 / 05:290 / 05:742（INV-34）「mode を問わず」 ↔ 05:822（OP-19）「SHADOW は次 permit 評価で可」・05:341（失敗表 R_PROFILE_SUSPENDED）「活性 **CLOSED_LOOP** lease は」・06:341（§5 (b)）「SHADOW_NON_AUTHORITY lease は … 次 permit 評価で可」・06:442（PT-13）「SHADOW lease は次 permit まで継続」・06:510（§8.3）「活性 CLOSED_LOOP lease を SafeHold へ」 | — | **R5-01（MEDIUM）** |
| 5 | R3-04 / R4-06 / R3-05 — `validity_deadline_source`・lease_max 項の除去・INV-34 | ✓ 05:183-184 / 05:428 / 05:290 / 05:742。lease_max は INV-33 / R_LEASE_DURATION_EXCEEDED（05:289 / 05:339）へ分離 | kind = ACCEPTANCE（05:184・06:342「ACCEPTANCE → HOLD」）の fault が §3.7 行（05:290）・失敗表（05:340）に無い（両者は `R_CALIBRATION_EXPIRED_ACTIVE \| R_EVIDENCE_EXPIRED_ACTIVE` のみ）。INV-34（05:742）と条件 2（05:230）は `R_PROFILE_SUSPENDED` を示唆 | source tuple は `(kind, id, deadline)` のみで、06 §5 (b) 表の副 kind（TCP / camera、SAFETY_LAYER_ACCEPTANCE / 他）の解決に遷移時の profile / evidence store 読出しが要る（不読 = 表に無い = SAFE_STOP で fail-closed） | **OK + R5-08（LOW）** |
| 6 | R3-06 — VALIDITY_EXPIRED の再試行 scope・`pending_invalidate` | ✓ 05:249（lease-scoped = deadline 到達 / profile-scoped = event）、05:517 | deadline の検出者は AuthorityManager（05:290）であり、gateway の状態（05:82 = 線形化読み + 直近 admitted command + `restart_pending`）に lease の `validity_deadline_mono` は無い → gateway が「trigger を検出」する経路が deadline 副 case には無い（実効上限 = INV-24 の manager 単独検出窓） | manager 側の deadline 検出遅延（timer 粒度）が未記載 | **OK + R5-11（LOW）** |
| 7 | R3-07 / R3-08 / R3-20 — registry 不読の範囲・monitor liveness・診断失敗 | R3-07 ✓ 05:342（起動 / permit / CAS 拒否・活性 lease は head-check 周期まで）。R3-08 / R3-20 △ 05:88 / 05:292 に head-check 行を追加 | ✗ head-check 行（05:292）の 4 副 case のうち monitor heartbeat 欠落・baseline diagnostic 失敗に **fault code が無い**（列挙は `R_PROFILE_SUSPENDED \| R_REGISTRY_UNAVAILABLE \| R_EVIDENCE_EXPIRED_ACTIVE`）。disposition は「06 §5 (b) 表で引く」だが表の key は `validity_deadline_source.kind` か `SuspensionReason` のみで、manager 単独検出には record が無い（06:511「AuthorityManager は read-only consumer」／ 06:509 append は認可者か monitor）→ 「表に無い = SAFE_STOP」（06:342）に落ち、失敗表 `R_REGISTRY_UNAVAILABLE` → HOLD（05:342）・`SuspensionReason.DIAGNOSTIC_FAILURE` → HOLD（06:342）と矛盾。さらに permit 発行前提 (a)-(l)（05:191）に monitor liveness / diagnostic 通過が無く、§5.4:534「(b)(i) で失効原因の解消を要求」が monitor 欠落 case で偽 | head-check の registry 読みが「線形化読み」と宣言されていない（条件 12・05:240 は宣言） | **R5-02（MEDIUM）・R5-03（MEDIUM）** |
| 8 | R3-09 — 兄弟 profile による cell 停止の迂回 | ✓ 05:191 (i)「同一 `cell_identity_hash` を持つ他 record が原因未解消の SUSPENDED なら本 profile も R_PROFILE_SUSPENDED」 | 条件 12（05:240）と head-check（05:292）は自 profile の head のみ。06:509 は registry が兄弟全 record へ SUSPENDED を append する前提 — 05 (i) と二重化されているが伝播窓は未束縛 | 「原因未解消」を判定する field が無い（評価不能 → FALSE = 拒否側なので fail-closed だが規範として曖昧） | **OK + R5-12（LOW）** |
| 9 | R3-11 — 計時 scope の mode 非依存 | ✓ 05:447「mode は問わない — SHADOW でも admission 判定は行う」 | §4.4（05:472 actuation DISABLED・05:474 他段同一）・HealthConfirmation（05:221 SHADOW = 記録された）・§3.7 deadline 行（05:285 any (EXECUTOR)）と整合。SHADOW lease も DEADLINE_MISS で無効化される（記録上の SafeHold）— 06 MIN_FAULT_INJECTION の timeout 経路 evidence を CLOSED_LOOP 前に作れる（R2-01 と両立） | — | **OK** |
| 10 | R3-13 — `safehold_disposition` の設定 | △ 初期状態（05:143）= HOLD、TRANSFER_TO_SAFEHOLD 事後条件（05:249）で設定、no-op でも max 更新 | ✗ TRANSFER_TO_EXECUTOR の事後条件 tuple（05:243）は 7 要素で `safehold_disposition` を None へ戻さない。§3.2:139「owner.kind == EXECUTOR では None」・§5.2 (iii) 05:518 は線形化読みで参照 | — | **R5-05（MEDIUM）** |
| 11 | R3-14 — effective expiry の fault 優先順・証明 D | ✓ 05:230 優先順（ACK → R_ACK_MISBOUND / age → R_AUTHORITY_DECISION_ABSENT / validity → R_*_EXPIRED_ACTIVE・R_PROFILE_SUSPENDED / ttl → R_PERMIT_EXPIRED）、05:357 証明 D | 条件 2 `now < effective_expires_at`（05:230・strict）vs 条件 3 `now ≤ ack.valid_until`（05:231）・失敗表「`now > ack.valid_until`」（05:303）・条件 6 `≤`（05:234）— 境界 `now == valid_until` で 2 は偽・3 は真。validity 項到達時に付く `R_*_EXPIRED_ACTIVE` の失敗行（05:340）は「TRANSFER_TO_SAFEHOLD・lease 無効」（活性 lease の効果）であり CAS REJECTED の効果 / disposition の行が無い | ledger 側の失効（CAS 未試行）の fault 割当規則が無い | **OK + R5-07（LOW）** |
| 12 | R3-15 — refuted・適用せず | ✓ 本文に変更なし（05:990 に「適用せず」） | — | — | **OK** |
| 13 | R3-18 / R4-08 — 第 2 評価点 list の SSOT = 06 §8.1 (2) | ✓ 05:191 (b) は 06 §8.1 (2) の閉じた list を SSOT として参照 | ✗ `P_VALIDITY_UNBOUNDED`（06:296 = required record kind ∈ {CALIBRATION_RECORD, SAFETY_LAYER_ACCEPTANCE} の `valid_until` None）が 06:422 の第 1 評価点 list にも閉じた第 2 評価点 list にも無い → record 側の無期限検査は評価点を失った。06:364「None = 失効なし（期限検査の対象外）」ゆえ `P_EVIDENCE_EXPIRED` も発火せず、05:184 `validity_deadline_source` に項が入らない（R2-18 の fail-closed 既定が v0.2.6 fold で静かに外れた） | — | **R5-04（MEDIUM）** |
| 14 | R3-19 — `executor_id` 不一致の fault | ✓ 05:711 INV-03・05:321 失敗表・05:82 admission | — | — | **OK** |
| 15 | R3-21 / R4-09 / R4-16 / R4-04 — head record 再評価・hash 再検証・fork | ✓ 05:191 (i)（head 再評価・`evidence_set_hash` 内側・validator hash・baseline 解決）、05:240 条件 12 に `record_hash ==`、06:513-514 | 05:191「追加 evidence = 新 generation」・06:514「新しい ACCEPTED record は必ず generation +1」に対し 06:507 の状態遷移に ACCEPTED → ACCEPTED(gen+1) が無い。head-check（05:292）は良性の新 generation（`record_hash ≠` ∧ state == ACCEPTED）を `R_PROFILE_SUSPENDED` + 表に無い = SAFE_STOP で処理する | — | **OK + R5-13（LOW）** |
| 16 | R4-15 — clock 健全性 (l)・`R_CLOCK_ANOMALY` | ✓ 05:191 (l)、05:343 失敗表、05:647 enum、06:387 MIN_FAULT_INJECTION、06:390-392 negative control、06:476 `SuspensionReason.CLOCK_ANOMALY`、06:342 → SAFE_STOP | manager 単独の周期 drift 検査失敗は registry record を伴わない → 06 §5 (b) の key に無く「表に無い = SAFE_STOP」で結果的に一致（05:343 の「HOLD /」は活性 lease 側では到達不能）。整理は R5-02 に含む | — | **OK（注記）** |
| 17 | R4-11 — event = 記録が先・§5.4 VALIDITY_EXPIRED 行・06 §5 (b) 表・`on_validity_expired` | ✓ 05:291 / 05:534 / 06:342 / 06:217 | 失敗表 05:341「活性 CLOSED_LOOP lease は」 ↔ 遷移行 05:291「any (EXECUTOR)」（R5-01 に含む）。event 行の `R_EVIDENCE_EXPIRED_ACTIVE` に対応する `SuspensionReason` が無い（R5-02 に含む） | — | **OK + R5-01 / R5-02 参照** |

## 3. Findings（id / severity / 候補 doc:line / 凍結 file:line / 再現 / 最小修正）

### R5-01（MEDIUM）SHADOW lease の validity deadline 到達の扱いが 05 内・05 ↔ 06 で 3 通りに分裂している（R4-18 fold の未完）

- 候補: 05:290（§3.7 行「mode を問わず」）・05:427（§4.1）・05:742（INV-34）↔ 05:822（OP-19「SHADOW は次 permit 評価で可」）・05:341（失敗表 `R_PROFILE_SUSPENDED`「活性 CLOSED_LOOP lease は TRANSFER_TO_SAFEHOLD」）・06:341（§5 (b)「SHADOW_NON_AUTHORITY lease は actuation が無いため次 permit 評価で可」）・06:442（PT-13「SHADOW lease は次 permit まで継続」）・06:510（§8.3「活性 CLOSED_LOOP lease を SafeHold へ移す」）。
- 凍結: EP `$D/WMSO_D11A_EVIDENCE_POLICY_V1_RSTECHLEAD2_20260719.md:155`「SHADOW は定義上 非 authority」は actuation の有無を定めるだけで lease の失効時刻を定めない — どちらの読みも凍結からは導けず、候補文書が自分で決めた規範が 2 つ並存している状態。
- 再現: SHADOW lease L（profile P・TCP calibration 期限 t_c）で `now ≥ L.validity_deadline_mono` に到達。05:290 に従えば TRANSFER_TO_SAFEHOLD(SAFE_STOP)（TCP）+ `R_CALIBRATION_EXPIRED_ACTIVE` の CAS が発行され L は無効化される。06:341 / PT-13 に従えば L は TERMINAL まで継続し次 permit で `P_CALIBRATION_EXPIRED`。実装者がどちらを採るかで (i) `LEASE_INVALIDATED` record の有無、(ii) 06 MIN_FAULT_INJECTION の `R_CALIBRATION_EXPIRED_ACTIVE#DEADLINE`（06:387）を SHADOW commissioning で発火できるか、が変わる。06 を採ると当該 evidence が CLOSED_LOOP 前に作れず（R3-11 と同型の衝突）、`P_EVIDENCE_UNBOUND` で CLOSED_LOOP 受理が不能になる。
- 最小修正（schema delta 0）: 05:822 OP-19 の「SHADOW は次 permit 評価で可」→「mode を問わず deadline で無効化（SHADOW は記録上の SafeHold）」、05:341「活性 CLOSED_LOOP lease は」→「活性 lease は（mode を問わず）」、06:341 の SHADOW 文と 06:442 PT-13 の SHADOW 文と 06:510 の「CLOSED_LOOP」を 05:290 に同期。

### R5-02（MEDIUM）manager 側 head-check 行（05:292）の fault / disposition 写像が副 case に対して total でなく、manager 単独検出には 06 §5 (b) 表の key が存在しない

- 候補: 05:292（head-check 行。trigger 4 副 case: head record 不一致 / registry 不読 / monitor heartbeat 欠落 / diagnostic item 失敗。FAULT 列挙 = `R_PROFILE_SUSPENDED | R_REGISTRY_UNAVAILABLE | R_EVIDENCE_EXPIRED_ACTIVE`、disposition = 「06 §5 (b) 表で引く」）・05:342（`R_REGISTRY_UNAVAILABLE` 行 disposition = HOLD）・05:343（`R_CLOCK_ANOMALY` 行「HOLD / SAFE_STOP」）・05:291（event 行の `R_EVIDENCE_EXPIRED_ACTIVE`）・06:342（表の key = `validity_deadline_source` kind と `SuspensionReason`、「表に無い項目 = SAFE_STOP」、DIAGNOSTIC_FAILURE → HOLD）・06:509（SUSPENDED record を append するのは認可者か monitor）・06:511（AuthorityManager は read-only consumer）・05:240（条件 12 は「registry の線形化読み」を宣言、head-check 行は未宣言）。
- 凍結: D0 `$D/WMSO_D0_ARCHITECTURE_DRAFT_RSTECHLEAD2_20260718.md:318`「missed heartbeat ⇒ consumers must not assume safety alive; monitor failed ⇒ system fails to safe-stop」— 欠落を名前付き disposition へ写す要求。写像が未定義 / 矛盾のままでは D0 の要求を「本文どおりに」実装できない。
- 再現: (1) DeploymentValidityMonitor の heartbeat が `VALIDITY_MONITOR_LIVENESS` 上限を超えて欠落。manager は 05:292 により TRANSFER_TO_SAFEHOLD を発行する。fault は 3 候補のどれにも該当しない（`R_PROFILE_SUSPENDED` = registry state、`R_REGISTRY_UNAVAILABLE` = registry 不読、`R_EVIDENCE_EXPIRED_ACTIVE` = evidence 期限）→ FAULT record の `fault` が未定義。disposition: `validity_deadline_source` の到達項目でも `SuspensionReason` でもない（manager は record を append できない）→ 06:342「表に無い = SAFE_STOP」。(2) registry が head-check 周期を超えて不読 → 05:342 は HOLD、05:292 経由の表引きは SAFE_STOP — 同一事象に 2 つの disposition。(3) diagnostic item 失敗 → 06:476 に `DIAGNOSTIC_FAILURE → HOLD` があるが manager 検出には record が無いため key に到達せず SAFE_STOP。(4) event 行 05:291 の `R_EVIDENCE_EXPIRED_ACTIVE` に対応する `SuspensionReason` member は無い（06:476）→ 到達不能な fault 候補。
- 最小修正（schema delta 0）: 05:292 を副 case 別に分割して各々に fault と disposition 源を固定する — head 不一致 → `R_PROFILE_SUSPENDED`・disposition = head record の `suspension_reason` を 06 表で引く（state == ACCEPTED の新 generation は HOLD）；registry 不読 → `R_REGISTRY_UNAVAILABLE` → HOLD（05:342 と同一）；monitor 欠落 → `R_REGISTRY_UNAVAILABLE`（payload trigger = MONITOR_LOST）→ HOLD、または `SuspensionReason` に `MONITOR_LOST` を足し monitor / 認可者の append 経路へ載せる；diagnostic 失敗 → `R_PROFILE_SUSPENDED`（trigger = DIAGNOSTIC）→ DIAGNOSTIC_FAILURE 行（HOLD）、drift → CLOCK_ANOMALY 行（SAFE_STOP）。head-check の registry 読みを「線形化読み」と明記。05:291 から `R_EVIDENCE_EXPIRED_ACTIVE` を外す（または対応 reason を足す）。

### R5-03（MEDIUM）permit 発行前提に monitor liveness / diagnostic 通過が無く、monitor 欠落で落ちた SAFEHOLD(VALIDITY_EXPIRED) が即座に再 lease される（§5.4:534 の「失効原因の解消を要求」が偽になる副 case）

- 候補: 05:191（permit 発行前提 (a)-(l) — grep: monitor / heartbeat / liveness の語は (l) の drift 検査以外に無い）・05:534（§5.4 VALIDITY_EXPIRED 行「新 permit が §3.4 (b)(i) で失効原因の解消を要求する。event 起因なら registry の SUSPENDED record が … 置換されるまで (i) が拒否」）・05:292（head-check 行）・05:88（liveness 契約）・06:511（manager は read-only → SUSPENDED record を書けない）。
- 凍結: D0 `:318`（heartbeat 欠落 ⇒ alive を仮定しない）・D0 `:273-274`（fail-closed: control is never released into a vacuum）。監視が死んだ状態で新たな authority を出すことは D0 §F の「alive を仮定しない」に反する。
- 再現（crash trace）: t0 monitor crash。t0+L（L = VALIDITY_MONITOR_LIVENESS）manager head-check → TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED) SUCCESS・lease L1 無効。Orchestrator は B0 から再開（05:296 と同じく無状態）。新 permit 要求: (a) health checks pass（monitor は health check kind に無い・06:67）、(b) 第 2 評価点 list pass（06:422 に monitor 項目なし）、(i) registry head = ACCEPTED（manager は SUSPENDED を append できない・06:511 — event が無いので registry は無変化）、(l) drift pass → permit 発行 → CAS 条件 1-12 pass（条件 12 も ACCEPTED）→ L2 活性 → command admit。t0+2L 再び head-check で SAFEHOLD。以後 L 周期で「actuation あり窓」が繰り返される。§5.4:534 の「(b)(i) が失効原因の解消を要求する」は monitor 欠落には当てはまらない。同型: baseline `diagnostic_items` のうち health check kind でも drift でもない項目（proof-test 等）の失敗。
- 最小修正（schema delta 0）: 05:191 に前提 (m) を追加 — 「DeploymentValidityMonitor の heartbeat が `VALIDITY_MONITOR_LIVENESS` 内 ∧ baseline `diagnostic_items` の全項目が各周期内に pass（不能 / 欠落 = `R_REGISTRY_UNAVAILABLE`・permit 不発行）」。併せて 05:292 / 06:509: manager 検出（monitor 欠落・diagnostic 失敗・drift）は認可された writer（MONITOR role）経由で `SUSPENDED(DIAGNOSTIC_FAILURE | CLOCK_ANOMALY | MONITOR_LOST)` を append してから CAS（R4-11「記録が先」を manager 検出にも適用）— これにより §5.4:534 の (i) gate が全副 case で成立する。

### R5-04（MEDIUM）`P_VALIDITY_UNBOUNDED`（required record 側）が第 1 / 第 2 評価点のどちらの list にも無く、無期限 evidence が permit を通り `validity_deadline_source` に項を残さない（R2-18 の fail-closed 既定が v0.2.6 の R3-18 / R4-08 fold で静かに外れた）

- 候補: 06:296（`P_VALIDITY_UNBOUNDED` = CalibrationRef と required record kind ∈ {CALIBRATION_RECORD, SAFETY_LAYER_ACCEPTANCE} の `valid_until` None）・06:422（第 1 評価点 list と「閉じた list・SSOT」の第 2 評価点 list — どちらにも `P_VALIDITY_UNBOUNDED` が無い）・05:191 (b)（「doc 06 §8.1 (2) が第 2 評価点として列挙する全 code … SSOT」）・06:364（`valid_until_iso8601: None` = 失効なし・期限検査の対象外）・06:295（`P_EVIDENCE_EXPIRED` は「wall-clock より前」の比較のみ）・05:184（`validity_deadline_source` は deadline を持つ項目のみ）。
- 凍結: contracts_v2 `$D/WMSO_D11A_CONTRACTS_V2_DESIGN_RSTECHLEAD2_20260719.md:354`「gate state の UNKNOWN / 欠落 = fail-closed DENY」— 期限「不明（None）」を「無期限 OK」に読む既定は同じ設計原則の逆。
- 再現: SAFETY_LAYER_ACCEPTANCE record を `valid_until_iso8601 = None` で登録。登録時（第 1 評価点）は profile 構造の検査であり evidence record は対象外（record は profile の外）。permit 発行時（第 2 評価点）は 06:422 の閉じた list を評価 — `P_EVIDENCE_EXPIRED` は None を「期限検査の対象外」として pass、`P_VALIDITY_UNBOUNDED` は list に無いため評価されない → permit 発行。`validity_deadline_source` に ACCEPTANCE / EVIDENCE(SAFETY_LAYER_ACCEPTANCE) の項が無い → 06:342 で SAFE_STOP 級と分類された evidence が lease 中に一切失効しない。PT-32（06:459）は CalibrationRef 側しか試験していない。
- 最小修正（schema delta 0）: 06:422 の第 2 評価点 list に `P_VALIDITY_UNBOUNDED`（required record 側）を追加し、第 1 評価点 list に `P_VALIDITY_UNBOUNDED`（CalibrationRef 側）を明記。05:191 (b) は SSOT 参照のまま変更不要。

### R5-05（MEDIUM）TRANSFER_TO_EXECUTOR の事後条件が `safehold_disposition` を None に戻さず、SAFE_STOP で閉じた前 SAFEHOLD の disposition が新 lease の (iii) 窓に漏れる（R3-13 fold の未完）

- 候補: 05:243（事後条件 tuple = 7 要素、`safehold_disposition` 無し）・05:139（「owner.kind == EXECUTOR では None」・「同一線形化点で durable 更新」は SAFEHOLD 側のみ）・05:249（TRANSFER_TO_SAFEHOLD と no-op では設定）・05:518（(iii) = `state.safehold_disposition == SAFE_STOP` なら SafeStop・線形化読み）・05:528（§5.3 boundary_wait の出力 = (iii) **SafeHold**）・05:246（CAS 直後の出力 = **SafeHold**）。
- 凍結: D0 `:270-274`（no owner-gap・出力は常に定義される）は満たすが、D0 `:273-274`「producer retains ownership and safe-stops (or re-observes)」の runtime 実現（§5.3）が本文の自己記述（SafeHold）と食い違う。
- 再現: SAFEHOLD(SAFE_STOP)（例: TCP calibration 期限・`safehold_disposition = SAFE_STOP`）→ clearance record → TRANSFER_TO_EXECUTOR SUCCESS。事後条件 05:243 は `safehold_disposition` に触れないので durable 値は SAFE_STOP のまま。新 lease L2 の最初の admissible command が admit されるまで out = (iii)（05:246）→ 05:518 により **SafeStop**（stop-class 要求・06:66 `StopClass` は POWER_REMOVED_STOP を含む）。以後 `boundary_wait` / `pending_invalidate` / `restart_pending` の各窓も SafeStop。§5.3:528「gateway 出力は (iii) SafeHold」と矛盾し、L2 の `first_command_admitted`（admission ≠ actuation）は True でも controller は停止要求下にある。
- 最小修正（schema delta 0）: 05:243 の tuple の後に「+ `safehold_disposition := None`（同一線形化点）」を追加し、INV-01（05:709）に `state.owner.kind == SAFEHOLD ⇔ state.safehold_disposition != None` を併記。

### R5-06（MEDIUM）AuditRecorder の不能（append 失敗 / 停止）に fault code・disposition・fail-closed 規則が無く、durable commit 済の CAS が無記録のまま lease を走らせる

- 候補: 05:140（線形化点 = durable commit 完了。「通知・audit・`permit.state := CONSUMED` はその後にのみ行う」）・05:700（記録義務「欠落 = INV-17 違反」）・05:725（INV-17 違反時 = 「記録欠落 = fault」— code 名なし）・05:766（T-17 全単射）・05:87（AuditRecorder = append-only・seq 単調）・05:632-647（`RuntimeFaultCode` に recorder 系 code なし）・05:302-347（失敗表に recorder 行なし）。
- 凍結: contracts_v2 `:537`（runtime 記録の content-hash 化は U14 defer — 記録の**存在義務**は defer されていない）・D0 `:318`（監視系の欠落 = alive を仮定しない）。
- 再現（crash trace）: TRANSFER_TO_EXECUTOR の durable write 完了（= SUCCESS・05:140）→ AuditRecorder.append(EPOCH_TRANSITION) が store 障害で失敗。本文に「recorder 不能 ⇒ SAFEHOLD」「permit を CONSUMED にしない」等の規則は無く、状態は EXECUTOR(L2) のまま、gateway は線形化読みで (ii) を admit する。L2 は EPOCH_TRANSITION / LEASE_ACTIVATED / PERMIT_CONSUMED 無しで走る。INV-17 / T-17 の全単射は事後にしか検出できず、検出時の fault code が enum に無い（INV-27 の逆方向: 名前の無い fault）。06 §6.1 の FAULT_INJECTION_RESULT / HEALTH_CHECK_RUN は audit 範囲へ束縛される（06:360）ため、commissioning 中に起きれば `P_FAULT_EVIDENCE_UNVERIFIED` で evidence 側が拾うが、production lease の無記録は拾えない。
- 最小修正（schema delta 0）: `RuntimeFaultCode` に `R_AUDIT_UNAVAILABLE`（runtime 層 enum・frozen 外）を追加し、失敗表に 1 行（検出者 = AuthorityManager / CommandGateway；効果 = 記録待ちの transition は完了扱いとせず gateway は (iii)、`audit_liveness_s`（`RuntimeTimeouts` へ 1 field）超過で TRANSFER_TO_SAFEHOLD(MANAGER_RESTART tier)；disposition = HOLD）。05:140 に「CAS SUCCESS の通知は record の durable append 後」を追記。06 MIN_FAULT_INJECTION に `R_AUDIT_UNAVAILABLE` を追加。

### R5-07（LOW）条件 2 の失効境界と fault 優先順が条件 3 / 6・失敗表と 1 点ずれ、CAS 時の validity / age 到達に効果・disposition の行が無い

- 候補: 05:230（`now < permit.effective_expires_at`・strict）・05:231（`now ≤ ack.valid_until`）・05:303（失敗表「`now > ack.valid_until`」）・05:234（`now − decision.t_mono ≤ decision_max_age_s`）・05:340（`R_*_EXPIRED_ACTIVE` 行の効果 = TRANSFER_TO_SAFEHOLD・lease 無効 — 活性 lease の効果）・05:317（`R_AUTHORITY_DECISION_ABSENT` 行 = HOLD）・05:191 (e) / 05:784 T-35（age 超過 = 再評価要求）。
- 凍結: contracts_v2 `:354`（UNKNOWN / 欠落 = DENY）— 方向は保守的で違反ではないが、同一事象に対する code / disposition の一意性（05:653 規則 2「fault は常に disposition を伴う」）が崩れる。
- 再現: `effective_expires_at == ack.valid_until` の permit で `now == ack.valid_until` に CAS。条件 2 偽（strict）→ 優先順により `R_ACK_MISBOUND`。条件 3 は真。失敗表 05:303 は `now > valid_until` を条件にしており本 case を含まない。別 case: TCP calibration 期限が permit 発行後・CAS 前に到達（`S_BOUNDARY_WAIT`・活性 L1 は別 generation で期限が違う）→ 条件 2 で `R_CALIBRATION_EXPIRED_ACTIVE`・REJECTED — 05:340 の効果列「TRANSFER_TO_SAFEHOLD・lease 無効」は L1 に適用されず、permit VOID 後の disposition（RESELECT か HOLD か）が無い。
- 最小修正（schema delta 0）: 条件 3 / 6 を strict（`<`）に揃える（または条件 2 を `≤`）；失敗表に「CAS 条件 2 の validity / age 項到達（REJECTED）→ permit VOID・状態不変 → RESELECT（新 permit は (b)(e) で原因解消を要求）」の 1 行；05:303 の条件を `now ≥` に。

### R5-08（LOW）`validity_deadline_source.kind = ACCEPTANCE` の到達に fault code が割り当てられていない

- 候補: 05:184（kind ∈ {CALIBRATION, EVIDENCE, ACCEPTANCE}）・06:342（「ACCEPTANCE → HOLD」）・05:290（§3.7 行 FAULT = `R_CALIBRATION_EXPIRED_ACTIVE | R_EVIDENCE_EXPIRED_ACTIVE` のみ）・05:340（失敗表も同 2 code）・05:742（INV-34 は `R_PROFILE_SUSPENDED` を含む）・05:230（条件 2 は validity → `R_*_EXPIRED_ACTIVE / R_PROFILE_SUSPENDED`）。
- 凍結: 該当なし（候補内部の totality）。D0 `:363-365`「miss action」は名前付き disposition を要求する（同型）。
- 再現: acceptance record の `valid_until` に活性 lease 中に到達 → 05:290 で TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED)。FAULT record の `fault` は 05:290 の 2 候補のどちらでもない。
- 最小修正: 05:290 / 05:340 の FAULT 列挙に `R_PROFILE_SUSPENDED`（ACCEPTANCE 到達）を追加。併記: 副 kind（TCP / camera、SAFETY_LAYER_ACCEPTANCE / 他）は `id` から lease.profile_hash の profile / evidence store で解決し、不読 = 表に無い = SAFE_STOP（05:290 の既定を明文化）。

### R5-09（LOW）06 §5 の lease 写像文が「`AcceptedEnvelope.terms` の 3 項」のまま（R3-02 fold の未完）

- 候補: 06:340「`AcceptedEnvelope.terms` の 3 項 ← §4」 ↔ 06:323-327（profile 4 項 + INTRINSIC）・06:333（「profile 4 項 … v0.2.6」）・05:367（enum 5 member）・05:806（OP-2「profile 4 term」）。
- 凍結: 該当なし（候補内部の数の不一致）。
- 再現: 06:340 だけを読んだ実装は INTER_ARM を lease へ写さず、05:235 条件 7 の `satisfiable` 評価と INV-36 が term 無しで評価される（不能 = FALSE で fail-closed だが写像文が誤り）。
- 最小修正: 06:340 を「4 項（CONTROLLER / DEPLOYMENT_WORKSPACE / SAFETY_RESTRICTION / INTER_ARM）← §4」に。

### R5-10（LOW）06 MIN_FAULT_INJECTION の `#DEADLINE` / `#EVENT` subject を照合する 05 §8 の trigger 副因 payload が 2 code 分しか無い（R4-13 fold の 05 側未完）

- 候補: 06:387（`R_CALIBRATION_EXPIRED_ACTIVE#DEADLINE` / `R_EVIDENCE_EXPIRED_ACTIVE#DEADLINE` / `R_PROFILE_SUSPENDED#EVENT`）・06:389（「05 §8 FAULT payload の trigger 副因と照合」）・06:297（`P_FAULT_EVIDENCE_UNVERIFIED`）・05:700（trigger 副因を持つのは `R_SAFETY_LAYER_LOST` と `R_BOUNDARY_DWELL_EXCEEDED` のみ）。
- 凍結: 該当なし。
- 再現: commissioning で TCP calibration 期限到達を発火させ FAULT record を得る。record の payload に DEADLINE 副因が無い（05:700 は要求しない）→ subject `R_CALIBRATION_EXPIRED_ACTIVE#DEADLINE` を 06:389 のとおり照合すると不一致 → `P_FAULT_EVIDENCE_UNVERIFIED` → CLOSED_LOOP 受理が到達不能（fail-closed 側）。逆に実装が照合を緩めれば `#EVENT` 経路の evidence を `#DEADLINE` で代替できる。
- 最小修正: 05:700 の列挙に `R_CALIBRATION_EXPIRED_ACTIVE` / `R_EVIDENCE_EXPIRED_ACTIVE`: DEADLINE | HEAD_CHECK、`R_PROFILE_SUSPENDED`: EVENT | HEAD_CHECK | CAS_COND12 を追加。

### R5-11（LOW）`pending_invalidate` の deadline 副 case は gateway が検出できず空文、manager の deadline 検出遅延は未束縛

- 候補: 05:517（`pending_invalidate`（VALIDITY_EXPIRED の deadline / event を含む）= gateway が trigger を検出してから）・05:82（gateway の状態 = 線形化読み + 直近 admitted command + `restart_pending`）・05:122-139（`AuthorityState` に deadline 無し）・05:290（検出者 = AuthorityManager）・05:732（INV-24 manager 単独検出窓 ≤ `command_deadline_s`）。
- 凍結: D0 `:354-357`（単調 clock・detection→completion 定義）。
- 再現: deadline 到達。gateway は自分の状態から到達を知り得ない → `pending_invalidate` は立たない → 実効窓は manager の検出（周期未記載）+ CAS + 通知 = INV-24 の窓。R3-06 fold の deadline 副 case は挙動を変えていない。
- 最小修正: 05:517 の括弧から「deadline」を外し「event（gateway へ通知されるもの）」に限定、05:290 に「manager は `validity_deadline_mono` に timer を設定し、検出遅延 ≤ baseline 項目 X」を追記（または gateway の lease copy に `validity_deadline_mono` を持たせ自己検出させる）。

### R5-12（LOW）cell 単位停止の兄弟検査が (i) のみで、条件 12 / head-check に無く、「原因未解消」は機械判定不能

- 候補: 05:191 (i)（「原因未解消の SUSPENDED」）・05:240（条件 12 = 自 profile head のみ）・05:292（head-check = `lease.profile_hash` の head のみ）・06:509（registry が同一 `cell_identity_hash` の全 record を停止）・06:513（`P_ACCEPTANCE_RECORD_INVALID` に「原因解消」field 無し）。
- 凍結: 該当なし。
- 再現: cell trigger（INCIDENT）で P1 が SUSPENDED、registry の兄弟伝播（P2）が遅延。P2 の permit は伝播前に発行済（(i) 通過）→ CAS 条件 12 は P2 head = ACCEPTED で通過 → L2 活性。head-check も P2 head のみ。P2 の SUSPENDED 伝播後に event で無効化 — 曝露 = 伝播遅延（未束縛）。
- 最小修正: 05:191 (i) の兄弟条件を「同一 `cell_identity_hash` の head record に state == SUSPENDED のものが存在」に置換（機械判定可能）、同条件を条件 12 と head-check 行に追加。

### R5-13（LOW）06 §8.3 の状態遷移に ACCEPTED → ACCEPTED(gen+1) が無く、良性の新 generation を head-check が SAFE_STOP で扱う

- 候補: 06:507（PROPOSED→ACCEPTED / ACCEPTED→SUSPENDED / SUSPENDED→ACCEPTED(new gen) / →REVOKED のみ）・05:191 (i)「追加 evidence = 新 generation」・06:514「新しい ACCEPTED record は必ず generation +1」・05:292（`record_hash ≠ lease.acceptance_record_hash` → `R_PROFILE_SUSPENDED`・表に無い = SAFE_STOP）。
- 凍結: 該当なし。
- 再現: 活性 lease L（gen 1）中に evidence 追加のため gen 2 ACCEPTED を append（06:514 が許容）。head-check で `record_hash ≠` → TRANSFER_TO_SAFEHOLD、fault = `R_PROFILE_SUSPENDED`（誤名）、disposition = 表に無い = SAFE_STOP。
- 最小修正: 06:507 に「ACCEPTED → ACCEPTED(gen+1)（evidence 追加・同じ two-key）」を追加し、05:292 の副 case「`record_hash ≠` ∧ head.state == ACCEPTED」→ HOLD（`R_PROFILE_SUSPENDED` payload = GENERATION_SUPERSEDED）と定める。

### R5-14（LOW）06 §5 (b) 表が CONTROLLER_ENVELOPE_MEASUREMENT の失効を SAFE_STOP 級に分類する一方、`P_VALIDITY_UNBOUNDED` は同 kind の無期限を許す

- 候補: 06:342（EVIDENCE(SAFETY_LAYER_ACCEPTANCE | CONTROLLER_ENVELOPE_MEASUREMENT) → SAFE_STOP）・06:296（`P_VALIDITY_UNBOUNDED` の kind 集合 = {CALIBRATION_RECORD, SAFETY_LAYER_ACCEPTANCE}）・06:287（`P_SAFETY_BUDGET_EXCEEDED` は CEM の `stop_time_worst_case_s` を消費）・06:364。
- 凍結: 該当なし（リスク分類の内部不整合）。
- 再現: CEM record を `valid_until = None` で登録 → 期限検査対象外 → 停止時間の実測が無期限に有効。
- 最小修正: 06:296 の kind 集合に CONTROLLER_ENVELOPE_MEASUREMENT を追加（R5-04 の評価点修正と同時に）。

## 4. PASS 2 — 反証で構成できなかったもの（PASS・根拠行）

| 狙い | 結果 | 根拠 |
|---|---|---|
| SUSPENDED profile 上で command が admit される | 構成不可（有界窓のみ） | permit 発行 (i) 05:191 / CAS 条件 12 05:240 / event 行 05:291（記録が先）/ head-check 05:292。曝露 = event 通知遅延（baseline）または head-check 周期 — Rs 裁定の設計上限。ただし R5-03 の flapping と R5-12 の伝播窓は上限が本文に無い |
| validity deadline 後に command が admit される | 構成不可（INV-24 窓のみ） | 05:290 + 05:249 lease-scoped 再試行 + 05:517。deadline 検出遅延は未記載（R5-11） |
| 2-arm command が独立な 2 つの 1-arm command として評価される | 構成不可 | 05:506 `arm_targets` は 1 record、05:461 は同一 cmd_seq の線分の対を連言、1-arm 提案時は他 arm の静止確認（不能 = FALSE）。逐次 1-arm 命令は他 arm が動いていれば FALSE → SAFEHOLD（保守側） |
| permit が副失効後に消費される | 構成不可（境界 1 点を除く） | 条件 2 = min（05:188 / 05:230）、条件 3（ACK）05:231、条件 6（age）05:234、条件 12（registry）05:240、INV-35 05:743。境界のずれ = R5-07 |
| SAFEHOLD が要求 clearance 無しに解除される | 構成不可 | 条件 10 05:238 = §5.4 集合 05:543（O7 PASS）、clearance record 定義 05:541、SAFE_STOP 遷移の理由付け規則 05:249。例外的に R5-03（monitor 欠落）は clearance 対象外理由なので「解除」ではなく「再 lease の門が無い」 |
| 同一事象に 2 record が食い違う | 構成可（R5-02 (2)：registry 不読 = HOLD と SAFE_STOP） | 上記 |
| fail-closed 既定の静かな緩和 | 構成可（R5-04・R5-05） | 上記 |
| profile hash / acceptance record / timing baseline / evidence の循環 | 構成不可 | 06:512（一方向）・06:497-498（baseline は cell_id / layout_revision_ref の文字列のみ）・06:360（evidence → audit 範囲 → profile_hash）・06:299（`P_EVIDENCE_POLICY_CIRCULAR`）・05:447（SHADOW で timeout evidence を作れる） |
| 二重所有 / 所有不在 / replay / permit 一回性 / 非復活（P1–P5） | 反例なし | 05:351-359 証明 A–E を条件 1-12・事後条件 05:243-247・INV-02/03/04/05/11/32 に当てて確認 |
| crash matrix（executor CAS 後 crash / manager CAS 中再起動 / gateway 再起動 / 転送窓の safety override / permit 後 Orchestrator 喪失 / monitor crash / registry 不読 / recorder 不能） | recorder 不能のみ未定義（R5-06） | 05:284・05:313 / 05:140・05:315・T-30 / 05:295・05:524・INV-31 / 05:316・T-08 / 05:296 / 05:292 / 05:342 |

## 5. 主張しないこと

- 本 review は AI（別 context）の準備レビューであり pS / pY / Rs の two-key を代替しない。gate を flip しない。
- 凍結 4 file・`eval_runs/` 配下は一切編集していない（書込み先 = `$OUT` のみ）。
- v0.2.5 の設計方針（三層 OP-19・registry・INTER_ARM・timing baseline）は finding にしていない。
- open = 0 を宣言しない。上記 14 件の他に、本 review が触れていない領域（06 §6.1 の evidence 内容の物理妥当性・execution profile 軸・composition）は対象外。

## 6. 成果物

- 本報告: `$OUT/R5_review_v026.md`
- findings JSON: `$OUT/R5_findings_v026.json`
- pin 再測: `$OUT/pin_recheck.txt`／ checker: `$OUT/checker_05.txt`

（`$OUT` = `/tmp/claude-0/-home-user-IsaacLab/f88a4426-3d65-5cc2-ba28-87f4976e6a66/scratchpad/review4`）
