# 軸 A2 — contract fidelity 独立レビュー（v0.2.1 review candidate・別 model の第 2 意見）

- reviewer: REVIEWER A2（軸 A = contract fidelity・pS 相当）。他軸（B / C）の finding は本 report 確定まで参照していない（08 計画 §1 項 6）。
- 対象: `05_WMSO_RUNTIME_SPEC_v0.2_REVIEW_CANDIDATE_20260903.md`（792 行・全読）／ `06_WMSO_INDUSTRIAL_PROFILE_v0.2_REVIEW_CANDIDATE_20260903.md`（456 行・全読）／ `02_FIELD_OVERLAP_AND_HOME_MATRIX_20260903.csv`（40 行）／ `03_IDENTITY_HASH_EVIDENCE_IMPACT_MATRIX_20260903.csv`（14 行）。checklist A11 の範囲で `04` / `07` も読んだ。
- 版: 文書内容は **v0.2.1**（file 名は v0.2 のまま — 05:1/05:4・06:1/06:4 で確認）。label ではなく内容と content sha で照合した。
- ⛔ 本 report は gate を flip しない。two-key・Rs 裁定・freeze・impl 解錠を主張しない。

## 0. 共通前提の履行記録（08 計画 §1）

### 0.1 pin 再測（項 1）— 11/11 PASS（自分で実行）

```
$ bash $D/WMSO_EXACT_BASELINE_REVIEW_v0.2_20260903/verify_exact_baseline_pins.sh
PASS  D1.1-A DESIGN v2.11.2 (worktree)          00192d20ca00b654cf6cfdb9d04b03ca14adfd0b93f2105c0fea28c295ff8aff
PASS  EvidencePolicy v1.9 md (worktree)         c474acea7c58acc22050c2ad9944fd45a18f5c76967964b42d11922e28fa27e7
PASS  EvidencePolicy v1.9 json (worktree)       e63176af9bc3a246b1c32db369ec59f8d09a4c96c381bb03a3a6024bd9811c6e
PASS  D1.1-B DESIGN v13 (worktree)              5a1874d3be8b98b8aeaace73890d8021cbc7f9814e048741f7f58d6746f349a6
PASS  D1.1-A DESIGN git blob id (HEAD)          1353430228a90ad36dd190a9359cd81da52e8242
PASS  D1.1-B DESIGN git blob id (HEAD)          ebe8154abf2461a6b24db05728b20e5cee949fb2
PASS  D1.1-A DESIGN sha256 of blob 135343…      00192d20…
PASS  D1.1-B DESIGN sha256 of blob ebe815…      5a1874d3…
PASS  D1.1-A DESIGN at frozen commit 54f90a7d   00192d20…
PASS  D1.1-B DESIGN at frozen commit 07250f4a   5a1874d3…
PASS  evidence_policy_definition_hash (embedded command)  e7ca43093084c167a209b008533a66d26a1fd3223d2a3c11274d28306c3ff803
policy_semver=1.9.0 ; HEAD=297c850bb31a61b56d5f2def9dd616a7c254f6d2 branch=claude/v0-2-design-candidates-snqbmy ; EXIT=0
```

### 0.2 content sha で引く（項 2）— `sha256sum -c SHA256SUMS.txt` = 15/15 OK（自分で再計算）

05 = `f62b0c307d1c90375a8afc03552f5b7a7050c96a959d45f5b7d2002c30075a1c` ／ 06 = `852d05724c9c595570e833bf9bd9fd248e51da02bed5835a8f98db74130584f9` ／ 02 = `2a2704d4…` ／ 03 = `c42f93a2…`。

### 0.3 機械検査（項 3）— FAIL = 0（読む前に実行）

```
05 (--runtime): citations=139 FAIL=0 WARN=6
06:             citations=55  FAIL=0 WARN=19
```
WARN は C2 heuristic（backtick 識別子が引用行 ±3 行に現れない）であり、私が sed で個別確認した限り引用先の内容は主張を支えている。ただし WARN が「複数 locus を 1 行に並べる」書式に起因する点は下の A2-10 と関係する。

### 0.4 陽性対照の非重複（項 5）

`10_POSITIVE_CONTROL_RECORD_20260903.md` の注入 8 欠陥（PC-05-A/B/C/D・PC-06-E/F/G/H）は実文書に存在しないことを自分で確認した（例: 05:233 で旧 epoch 拒否は CAS 事後条件に在る＝PC-05-A なし／05:231 に `∪ {consume_offer_id}` が在る＝PC-05-C なし／05:419 で SHADOW の actuation port = DISABLED＝PC-05-D なし／06 に action-space bounds field が無い＝PC-06-E なし／06:231 が等値＝PC-06-F なし／06:117 が True 必須＝PC-06-G なし／06:69 の enum に緩和値なし＝PC-06-H なし）。**以下の finding はいずれも注入欠陥ではない。**

### 0.5 引用の手動 sed 検証（A4）— 実施件数

distinct citation は 4 文書合わせて 94 種。うち **74 種**を `sed`/`awk` で実読して照合した（要求は ≥ 15）。誤りは 0 件。主要な確認結果は §2 A4 に列挙する。

---

## 1. Findings（severity 降順）

### A2-01 [HIGH] CAS 前提条件 10 の clearance 集合が、doc 自身が宣言する clearance 必要集合より狭い — frozen slice 境界（checkpoint 切替禁止）の唯一の runtime guard が線形化点で執行されない

- **候補 doc:line**: `05_…:228`（CAS 前提条件 10）。関連 = `05_…:466-470`（§5.4 解除表）・`05_…:275`/`:307`（GATEWAY_RESTART）・`06_…:179`（`EscalationPolicy.clearance_roles`）。
- **凍結 file:line（根拠）**: `WMSO_D11BC_SLICE_SCOPE_PREREG_RSTECHLEAD2_20260720.md:59` — 「**checkpoint 途中切替の実行 = Phase G / 別 gate**。checkpoint-enabled slice に変える場合は **chunk 改称 + scope 再審査**」。
- **主張の食い違い**:
  - `05:228` は「`safehold_reason ∈ {SAFETY, SAFE_STOP, MANAGER_RESTART}` の SAFEHOLD からの転送では、該当する clearance record（§5.4）が存在する」と、**3 member だけ**を CAS 前提にしている。
  - `05:469` は `CHECKPOINT_DISABLED` の解除に「profile が定義する **operator clearance** + 再観測（doc 06）」を要求する。
  - `06:179` は clearance_roles の対象を「**SAFETY / SAFE_STOP / CHECKPOINT_DISABLED / MANAGER_RESTART / GATEWAY_RESTART** の解除に要する追加の role（05 §5.4）」と **5 member**で宣言する。
  - ⇒ 宣言集合 {SAFETY, SAFE_STOP, CHECKPOINT_DISABLED, MANAGER_RESTART, GATEWAY_RESTART} ⊋ 執行集合 {SAFETY, SAFE_STOP, MANAGER_RESTART}。**CHECKPOINT_DISABLED と GATEWAY_RESTART は宣言されているが CAS で検査されない。**
- **再現 / 反例（構成）**: executor が自発的に `InterruptOutcome{checkpoint_id, reason}` を出す → `05:269` により `TRANSFER_TO_SAFEHOLD(CHECKPOINT_DISABLED)`・fault `R_CHECKPOINT_SWITCH_DISABLED`・disposition `HOLD` → Orchestrator が `05:488` B0 から通常どおり再選択を開始 → B4 の全 validation（certified / `validate_invocation_start`（now 明示）/ envelope / timing / mode）は成立し得る → `ReadinessAck` → `CommitPermit` → `AuthorityCas(TRANSFER_TO_EXECUTOR)`。CAS 前提条件 1–9 は全て真、条件 10 は `CHECKPOINT_DISABLED ∉ {SAFETY, SAFE_STOP, MANAGER_RESTART}` ゆえ **発動しない** ⇒ operator clearance record なしに転送が成功する。結果として「checkpoint で止め、別 skill を選び直して走らせる」経路が、`05:481`/`05:483`/`INV-21` の宣言（受理 + HOLD・fail-closed）にもかかわらず成立する。同様に gateway 再起動後の SAFEHOLD(GATEWAY_RESTART) も clearance なしに抜けられる（`05:275` は「再起動前の lease は再 admit されない」としか言わない）。
- **なぜ contract fidelity 問題か**: slice prereg `:59` が別 gate に留保しているのは「checkpoint 途中切替の**実行**」であり、v0.2 でそれを塞ぐ機構は「HOLD + clearance」以外に無い（`05:696` 項 6・`05:716` OP-11 もそう述べる）。clearance が CAS の前提でない以上、この境界は宣言のみで、`05:627` の「fail-closed 既定: 評価不能 = FALSE」にも整合しない。
- **最小修正（schema delta 0）**: `05:228` の条件 10 の集合を `06:179` の宣言集合に一致させる — `safehold_reason ∈ {SAFETY, SAFE_STOP, MANAGER_RESTART, GATEWAY_RESTART, CHECKPOINT_DISABLED}`。併せて `05:466` の「追加前提 = なし」行から誤解を避けるため、条件 10 の集合が §5.4 表と `06 EscalationPolicy.clearance_roles` の**両方から導出される**ことを 1 文で明記する。新型・新 field・新 enum member は不要。

### A2-02 [HIGH] INV-16 / T-18 が本 doc 自身の runtime 型で成立しない。かつ frozen :550 の assert を「static hash の WCJ payload key 集合」から「frozen 型の WCJ payload key 集合」へ言い換えて範囲を広げている

- **候補 doc:line**: `05_…:646`（INV-16）・`05_…:680`（T-18）・`05_…:770`（Review anchor 1 が INV-16 を schema-delta 0 の機械的根拠として引く）。
- **凍結 file:line（根拠）**: `WMSO_D11A_CONTRACTS_V2_DESIGN_RSTECHLEAD2_20260719.md:550` — 「metamorphic **#3 差替（D-CH8）**: 『runtime 型の全 field 名が **static hash の WCJ payload key 集合**に不在』の **serialization-boundary assert**」。
- **主張の食い違い**: INV-16 は「runtime 型の全 field 名が **frozen 型の** WCJ payload key 集合に不在」と書く。frozen は *static hash* の payload key（`SkillDefinition` / `ExecutionBundle` / `TensorBindingSpec` 系）を対象にしているのに対し、候補は *frozen 型全般*（runtime 型 `SkillInvocation` / `HandoffOffer` / `TransitionRecord` を含む）へ広げている。
- **再現 / 反例（両方の読みで破れる）**:
  - *frozen の読み（static hash payload）で破れる*: `TensorBindingSpec` 側の hash-visible key は `policy_rate_hz` / `obs_sampling_rate_hz`（`WMSO_D11B_TENSOR_BINDING_DESIGN_RSTECHLEAD2_20260720.md:97-98`）・`action_rate_hz` / `hold`（同 `:126-127`）・`control_mode`（同 `:132`）。候補の runtime 型 `TimingBinding`（`05_…:333-336`）は `policy_rate_hz` / `obs_sampling_rate_hz` / `action_rate_hz` / `hold` を**同名**で持ち、`ActiveAuthorityLease`（`05_…:376`）は `control_mode` を同名で持つ。⇒ 交差は空でない。
  - *候補の読み（frozen 型全般）でさらに破れる*: `ActiveAuthorityLease.invocation_id` / `.skill_action_id` / `.skill_definition_hash` / `.control_epoch` / `.ownership`（`05_…:370-379`）、`ReadinessAck.invocation_id` / `.skill_action_id`（`05_…:150-151`）、`RuntimeAuditRecord.invocation_id` / `.episode_id` / `.schema_versions` / `.control_epoch`（`05_…:612-619`）、`RuntimeAssessmentRecord.outcome` / `.belief_ref`（`05_…:517-518`）はいずれも frozen `SkillInvocation`（`:479-486`）/ `TransitionRecord`（`:509-522`）/ `HandoffOffer`（`:505-506`）の key と同名。
  - ⇒ T-18（「runtime 型 field 名 ∩ frozen WCJ key 集合 = ∅」）は仕様どおりに走らせれば**必ず非空**を返し、宣言された assert は成立しない。
- **なぜ問題か**: `05:770` の anchor 1 は「本 doc は frozen 4 file に field / enum member を足していない — §1・§11 項 3・**INV-16**」と、INV-16 を schema-delta 0 の機械的裏付けとして提示している。その機械検査が構成上通らない以上、anchor 1 の証拠は空（appearance-only）になる。**実体としての schema delta は無い**（私は §2 A1 のとおり全 frozen 型を突き合わせて確認した）— 壊れているのは主張の裏付け機構であって schema そのものではない。
- **最小修正（schema delta 0）**: INV-16 を frozen `:550` の述語に戻し、方向を「混入禁止」に限定する。例: 「INV-16 — runtime 型の値が frozen **静的 hash（SkillDefinitionHash / ExecutionBundleHash / tensor_binding_hash / BehaviorSignature）の WCJ preimage** に serialize されない（serialization-boundary assert・frozen `:550` と同型）。runtime 型が frozen 型と**同名の field 名を持つこと自体は違反ではない**（join key の再利用 — `05 §8`）」。T-18 も同じ述語に書き換える（「runtime 型を静的 hash preimage に入れる経路が存在しないこと」を assert）。

### A2-03 [MEDIUM] `P_RESOURCE_UNATTESTED` の trigger が v0.2.1 で削除済みの field `attested_by` を参照している — §3 (v) の唯一の widening 防止検査が発火しない

- **候補 doc:line**: `06_…:236`（§3 単調性表の `required_control_resources` 行）「| `attested_by` 空 ⇒ `P_RESOURCE_UNATTESTED`（CELL_COMMISSIONING evidence 必須）|」。
- **矛盾する同 doc の line**: `06_…:141-142` — 「v0.2.1（C-08）: 旧 `attested_by`（record_id を profile preimage に置く）は **削除** — evidence → profile の一方向束縛のみ（record が profile_hash を持つ）。`P_RESOURCE_UNATTESTED` は登録時に evidence store（kind = CELL_COMMISSIONING ∧ record.profile_hash == 本 profile の hash）を照会して判定する」。`06_…:425`（fold-map C-08）と `06_…:365`（PT-09）は正しく更新済み。**§3 表だけが fold から漏れている。**
- **凍結側の関係 line**: `WMSO_D11A_CONTRACTS_V2_DESIGN_RSTECHLEAD2_20260719.md:380`（`required ⊆ offered` の frozen 検査）— profile は offered 集合を宣言するだけで frozen 検査の向きを変えない、という §3 の主張はこの検査の発火に依存する。
- **再現**: `ResourceAvailability`（`06_…:136-142`）には `attested_by` field が存在しない。さらに `06_…:221` の strict codec により、profile に `attested_by` を書けば `P_UNKNOWN_FIELD` で拒否される。したがって「`attested_by` 空」という条件は**どの受理可能な profile でも真になり得ず**、`P_RESOURCE_UNATTESTED` は §3 表の規定どおりには決して発火しない。`06_…:260` は「(v) 資源を偽って offered にする — attestation 必須」を「広げる」9 経路のうち唯一 evidence 側に委ねる経路としているので、規範表の側で trigger が死んでいるのは実害がある。
- **最小修正（schema delta 0）**: `06:236` の機械検査セルを `06:141-142` の規則に合わせる — 「`kind = CELL_COMMISSIONING` ∧ `record.profile_hash == H_WCJ(profile)` の `DeploymentEvidenceRecord` が evidence store に存在しない ⇒ `P_RESOURCE_UNATTESTED`（登録時・第 1 評価点）」。

### A2-04 [MEDIUM] frozen `ActionBinding` に存在しない field `bounds` を持たせて記述している（§0 中心構造）

- **候補 doc:line**: `05_…:19` — 「D1.1-B: TimingSpec / ActionTimingSpec{action_rate_hz, hold} / **ActionBinding{bounds, action_scale, control_mode}** / tensor_binding_hash」。
- **凍結 file:line**: `WMSO_D11B_TENSOR_BINDING_DESIGN_RSTECHLEAD2_20260720.md:128-135` — `class ActionBinding:` の field は `features` / `total_dim` / `control_mode` / `action_scale` / `timing` / `container_dtype` の 6 個で、**`bounds` は無い**。`bounds` は per-feature の `FeatureBinding.bounds`（同 `:81`、型は `BoundsSpec` 同 `:52`）であり、`:138`「全 action feature は `bounds` 必須」も feature 側の規定である。
- **本 doc 内の正しい記述との不一致**: `05_…:405`（「D1.1-B `ActionBinding.features[].bounds`」）・`06_…:30`（同）・`05_…:58`（`ActionBinding {control_mode, action_scale, timing}`）は正しい。誤りは `05:19` の 1 箇所のみだが、そこは読者が frozen 語彙の要約として最初に読む §0 中心構造ブロックである。
- **再現**: `05:19` の記述に従って `ActionBinding.bounds` を参照する実装／レビューは、frozen 型に無い key を触ることになり、`WMSO_D11A_CONTRACTS_V2_DESIGN_RSTECHLEAD2_20260719.md:385` の strict codec（unknown field 拒否）に当たる。checklist A1（frozen 型に field を追加しない）に対する**文面上の field 追加**にあたる。
- **最小修正（schema delta 0）**: `05:19` を `ActionBinding{features[].bounds, action_scale, control_mode, timing}` もしくは `ActionBinding{control_mode, action_scale, timing} + FeatureBinding.bounds` に直す。

### A2-05 [MEDIUM] Review anchor 2 が、§2 表が示していない責務一致を主張している（旧 epoch command 拒否の帰属）

- **候補 doc:line**: `05_…:771`（anchor 2）— 「`AuthorityManager` の責務（CAS・発行・**旧 epoch 拒否**・`handoff_offer_id` 単位の使用済み拒否・snapshot 読出し）は frozen の O0 層要求仕様に一致 — **§2 表** ↔ `…:382`」。
- **凍結 file:line**: `WMSO_D11A_CONTRACTS_V2_DESIGN_RSTECHLEAD2_20260719.md:382` — 「epoch の CAS 更新・新 epoch 発行・**旧 epoch command 拒否**・使用済み offer 拒否（`handoff_offer_id` 単位）・validator へ渡す `authority_epoch_snapshot` の読出し = **authority manager（O0 層）の状態所有責務**」。
- **矛盾する同 doc の line**: `05_…:81`（§2 表 AuthorityManager 行）の責務欄は「`AuthorityState` の唯一 writer。`AuthorityCas` の実行、`CommitPermit` の発行/失効/一回性、`authority_epoch_snapshot` の読出し提供、`consumed_offer_ids` の保持、restart 時の fail-closed 復帰」で、**旧 epoch command 拒否を含まない**。同責務は `05_…:82`（CommandGateway 行）に置かれ、その根拠欄が `:382` を引いている。
- **再現**: anchor 2 を検証しようとする reviewer が §2 表を読むと、anchor が「一致」と述べる 5 責務のうち 1 つ（旧 epoch 拒否）が別の構成要素の行にある。設計としては擁護可能（gateway は manager 所有の `AuthorityState` を線形化して読むだけで、状態所有は manager に残る — `05:233`・`05:312`）が、**その再帰属を明示していないため、anchor が凍結逐語との一致を過大に主張している**。checklist A10（honesty / anchor の正確さ）と A5（責務分離の整合）に触れる。
- **最小修正（schema delta 0）**: `05:81` の AuthorityManager 責務欄に「旧 epoch command 拒否（**所有**は manager・**執行点**は CommandGateway の admission。gateway は `AuthorityState` を線形化して読むのみで書かない）」を追記し、`05:771` の anchor 2 を「所有 = manager / 執行点 = gateway、両者の分離は `:382` の状態所有責務を保つ」と書き換える。

### A2-06 [MEDIUM] 07 §3 の「全 certified skill の identity が変わり」が frozen 逐語 `:219` と矛盾し、同 package の 03 matrix とも矛盾する

- **候補 doc:line**: `07_SUCCESSOR_CONTRACT_DECISION_20260903.md:39` — 「新 field は既定で `BehaviorSignature` に入り（補集合定義・`:218`）、`SkillDefinitionHash`（`:159`）も動く ⇒ **全 certified skill の identity が変わり**、`behavior_revision` の registry 不変量（§3-4）と certificate の再発行を伴う」。
- **凍結 file:line**: `WMSO_D11A_CONTRACTS_V2_DESIGN_RSTECHLEAD2_20260719.md:219` — 「registry 登録時、同 key の既存 entry と BehaviorSignature 不一致 → `E_BEHAVIOR_REVISION_STALE`（fail-closed）。**ActionId は不変のまま**、忘却は登録境界で機械捕捉される」。加えて `:158` — `SkillActionId = H_WCJ({"ns", "skill", "bundle", "variant", "brev"})`（`SkillDefinitionHash` は preimage に入らない）。
- **矛盾する同 package の記述**: `03_IDENTITY_HASH_EVIDENCE_IMPACT_MATRIX_20260903.csv` の C08–C12（行 8–12）は同じ変更クラスに対し `affects_SkillActionId_or_ExecutionBundleHash = **no**` と判定している。`:158`/`:219` に照らすと **03 の判定が正しく、07:39 の言い回しが誤り**である（identity は自動では動かない。動くのは `behavior_revision` を人が bump した時だけで、bump しなければ registry が登録を拒否する）。
- **再現**: `SkillDefinition` に新 static field を 1 つ足すと `SkillDefinitionHash`（`:159`）と `BehaviorSignature`（`:218` 補集合定義）は変わるが、`SkillActionId` は `{ns, skill, bundle, variant, brev}` のみの関数なので不変。同 `(ns, skill_id, variant, brev)` で再登録すると `E_BEHAVIOR_REVISION_STALE` で fail-closed。⇒ 「identity が変わる」ではなく「**identity は変わらず、登録が拒否される**」が frozen の挙動。
- **最小修正（schema delta 0）**: `07:39` を「新 field は既定で `BehaviorSignature` に入り（`:218`）`SkillDefinitionHash`（`:159`）も動く。`SkillActionId` は `{ns, skill, bundle, variant, brev}` の関数ゆえ自動では動かないが（`:158`）、同 key の再登録は `E_BEHAVIOR_REVISION_STALE` で拒否される（`:219`）⇒ `behavior_revision` の bump = ActionId 変更と certificate 再発行が事実上必須になる」と書き換える。03 matrix は変更不要。

### A2-07 [LOW] 03 matrix C08–C12 の「ActionId 不変」判定を entail する frozen 行が citation 欄に無い

- **候補 doc:line**: `03_…csv` 行 8–12（C08 continuation predicate / C09 intrinsic recovery / C10 phase-level interruptibility / C11 safe-hold / C12 outcome enum）。`affects_SkillActionId_or_ExecutionBundleHash = no`。citation 欄は `:159`（SkillDefinitionHash）・`:218`（BehaviorSignature）・`:385`（strict codec）・`:148`・`:147`・`:151`。
- **凍結 file:line（欠けている根拠）**: `…:158`（`SkillActionId = H_WCJ({"ns","skill","bundle","variant","brev"})`）と `…:219`（「ActionId は不変のまま」）。この 2 行だけが「no」を導く。
- **再現**: 引用された `:159`/`:218`/`:385` はいずれも「SkillDefinitionHash が動く」「signature に入る」「codec が拒否する」であり、ActionId が不変であることは導かない。checklist A11（「凍結の hash / codec 規則から**導かれている**」）を厳密に取ると、この列は未接地。
- **最小修正（schema delta 0）**: C08–C12 の citation 欄に `…:158` と `…:219` を追加する（行数・語彙は不変）。

### A2-08 [LOW] §5.4（HOLD を解除できる者）が `SafeHoldReason` に対して total でない

- **候補 doc:line**: `05_…:464-470`（§5.4 表）。
- **矛盾する同 doc の line**: `05_…:122` — `SafeHoldReason` は 14 member（INITIAL / BOUNDARY_WAIT / TRANSFER / HEALTH_FAIL / NO_CHAIN / EXECUTOR_LOST / MANAGER_RESTART / **GATEWAY_RESTART** / DEADLINE_MISS / **TIMING_VIOLATION** / ENVELOPE_VIOLATION / SAFETY / SAFE_STOP / CHECKPOINT_DISABLED）。§5.4 表は 12 member しか行を持たず、v0.2.1 で追加された `GATEWAY_RESTART`（B-09）と `TIMING_VIOLATION`（B-10）の行が無い。
- **再現**: `05:305`（`R_TIMING_VIOLATION` ⇒ `TRANSFER_TO_SAFEHOLD(TIMING_VIOLATION)`）・`05:307`（`GATEWAY_RESTART`）で到達可能な 2 状態について、解除経路と追加前提が §5.4 から読めない。`05:627` の「評価不能 = FALSE」に従えば解除不能（永久 HOLD）と読めてしまい、`05:466` の「追加前提なし」行に含める意図とも取れる — どちらとも決まらない。
- **最小修正（schema delta 0）**: §5.4 表に 2 行を追加する。`TIMING_VIOLATION` は `05:466` の「追加前提 = なし」行へ member を足す。`GATEWAY_RESTART` は A2-01 の修正と整合させ、`06:179` が clearance_roles の対象に挙げているとおり clearance 必要側へ置く。

### A2-09 [LOW] §2.3 反循環規則の文が字義的に自己矛盾している（`SkillActionId` を内包しない ↔ `binding_expectations[].skill_action_id`）

- **候補 doc:line**: `06_…:220` — 「profile は自身の `profile_hash`・lease id・`control_epoch`・**`SkillActionId`**・`ExecutionBundleHash` を内包しない（`…:23` と同型）。`binding_expectations[].skill_action_id` は**他者の** id であり自己参照ではない」。同 doc `06_…:183` は `BindingExpectation.skill_action_id: str` を定義している。
- **凍結 file:line**: `WMSO_D11B_TENSOR_BINDING_DESIGN_RSTECHLEAD2_20260720.md:23` — 「TensorBindingSpec は **自身の hash・SkillActionId・ExecutionBundleHash・および「自身と同一である」ことを主張する任意の hash** を内包しない。M2 の教訓は field 名 2 個ではなく『**自己参照 hash を preimage に置かない**』という構造 pattern」。
- **再現**: 凍結側で `SkillActionId` が禁止されるのは、それが `TensorBindingSpec` **から導かれる** id だから（`:185` の派生鎖）である。profile_hash は `SkillActionId` の preimage に入らないので、profile が他者の `skill_action_id` を持っても不動点は生じない — 候補の読み（後半の但し書き）は正しい。問題は前半の禁止列挙が `:23` の field 名を機械的に写しており、直後の但し書きで打ち消される点。実装者が前半だけを読めば `binding_expectations` を落とす。
- **最小修正（schema delta 0）**: `06:220` の前半を構造 pattern の側に揃える — 「profile は**自身から導かれる hash**（`profile_hash`）および『自身と同一である』ことを主張する任意の hash を preimage に置かない（`:23` の構造 pattern）。profile_hash は `SkillActionId` / `ExecutionBundleHash` の preimage に入らないため、`binding_expectations[].skill_action_id` のような**他者の** id 参照は自己参照ではない。`P_SELF_HASH`」。

### A2-10 [LOW] 規範主張の根拠に package 外の scratch artifact を引いている（self-containment）

- **候補 doc:line**: `05_…:8`（「critic 報告（`07_critic_report.md` X1 / X2 / §3.3）の fold」）・`05_…:97`（「EP md/json で `controller`/`calibrat`/`deploy` = 0 hit — **03 map §6(e)**」）・`06_…:330`（「EP md / JSON に … の語彙は無い（**critic 報告 §3.7** の grep 結果）」）。`04_…:26` / `07_…:23` も同一 artifact を引く。
- **根拠が無い場所**: `SHA256SUMS.txt`（15 entry）に `07_critic_report.md` も `03 map` も含まれない。両者は本 session の scratchpad（`/tmp/.../scratchpad/maps/`）にあり、banked package の読者には解決できない。
- **再現 / 独立確認**: 当該主張自体は真である — 私が独立に再実行した結果、`grep -ic` は EP md / EP JSON の両方で `controller`=0/0・`calibrat`=0/0・`deploy`=0/0・`inject`=0/0・`epoch`=0/0・`lease`=0/0（`cell` は md=10 / json=3 hit で、`06:330` が「usage matrix の表 cell の意味ゆえ除外」と訂正しているとおり）。したがって内容は正しく、欠けているのは**証拠の package 内での解決可能性**だけである。
- **最小修正（schema delta 0）**: 引用を「本 package 内で再現可能な command + 結果」に置き換える。例: `06:330` を「`grep -ic '<pat>' <EP md> <EP json>` = 0/0（pat ∈ {controller, calibrat, deploy, inject, epoch, lease}；`cell` は usage matrix の表 cell の語として存在するため対象外）」とし、`05:8` の系譜行は「critic fold の内容は §13 に inline 済（外部 file は本 package の SHA256SUMS 外）」と明記する。

### A2-11 [LOW] timestamp に placeholder（`x`）が残っており、少なくとも 1 件は実測と一致しない

- **候補 doc:line**: `05_…:8`（「本版（**16:4x** UTC）」）・`05_…:748`（「v0.2.1 fold（2026-09-04 **01:5x** UTC）」）・`06_…:3`（「v0.2.1 = 2026-09-04 **01:5x** UTC」）・`06_…:419`（同）。`10_POSITIVE_CONTROL_RECORD_20260903.md:3` にも「記録 = 2026-09-04 01:5x UTC」。
- **反例（実測）**: `TZ=UTC ls --time-style=full-iso` で 05 の mtime = **2026-09-04 01:47:57 UTC**（= 01:4x）、06 の mtime = 2026-09-04 01:51:18 UTC。⇒ `05:748` の「01:5x」は 05 自身の最終更新時刻と一致しない。
- **なぜ問題か**: `06_…:433` は fold-map で C-16「作成時刻が非厳密」を **header について修正済み**と記録しているが（`06:3` の作成時刻は「16:36 UTC（`date -u` 実測）」に直っている）、v0.2.1 側の時刻は両 doc とも placeholder のまま残った。CLAUDE.md §15 records-must-match-fact（date-THEN-write・無時刻／非実測の状態主張を禁止）に照らすと未了。
- **最小修正（schema delta 0）**: 5 箇所の `x` を `date -u` 実測値（もしくは file mtime）に置換する。

### A2-12 [LOW] `validate_outcome` 失敗に固有の fault 行が無く、同一 fault code に §6.2 と §3.7 で別の disposition が割り当てられている

- **候補 doc:line**: `05_…:489`（B1「`validate_outcome(invocation, outcome, definition)` … issues 非空 = `R_INVOCATION_INVALID` → **HOLD**」）／ `05_…:290`（§3.7 失敗表「`validate_invocation_start` 失敗 | Orchestrator | `R_INVOCATION_INVALID` | 変化なし | **`RESELECT` / `RE_OBSERVE`**」）。
- **凍結 file:line**: `WMSO_D11A_CONTRACTS_V2_DESIGN_RSTECHLEAD2_20260719.md:374`（`validate_outcome(invocation, outcome, definition) -> LifecycleValidationReport`）・`:380`（検査内容）— frozen は 2 つの validator を別関数として定義しており、候補 §7 規則 3（`05:579`）も「frozen `E_*` は validator report に留まり、runtime は `R_OFFER_INVALID` / `R_INVOCATION_INVALID` で包んで disposition に写す」と、包み分けを規則化している。
- **再現**: `validate_outcome` が issues 非空を返した run では、B1 は HOLD を、§3.7 表は同じ code に対し RESELECT / RE_OBSERVE を指示する。`05:578` 規則 2「fault は常に disposition を伴う」と `INV-27`（全 fault member が §3.7 に行を持つ）は満たされるが、逆向き（全失敗経路が一意な disposition を持つ）は満たされない。
- **最小修正（schema delta 0）**: §3.7 失敗表に 1 行を追加する — 「`validate_outcome` 失敗 | Orchestrator | `R_INVOCATION_INVALID` | 変化なし（`S_LEASE_ACTIVE` 継続） | `HOLD`」。既存 `RuntimeFaultCode` member を再利用するので enum 追加は不要（新 code を作らないほうが `INV-27` と `06 MIN_FAULT_INJECTION` に影響しない）。

---

## 2. Checklist 判定（08 計画 §2 の A1–A12）

### A1 — 凍結型に field の追加・削除・改名が無い ⇒ **FINDING（A2-04）／実体は PASS**

frozen 型を 1 つずつ突き合わせた（`sed` 実読）:

| frozen 型 | 凍結 locus（自分で確認） | 候補側の扱い | 判定 |
|---|---|---|---|
| `SkillDefinition`（`required_control_resources` / `freshness_policy` / `fail_closed_action`） | `:142` / `:146` / `:147` | 参照のみ（`05:98`・`06:38`）。field を足さない | PASS |
| `ExecutionBundle` | `:157` | `control_mode` の値を lease が写すのみ | PASS |
| `TensorBindingSpec` / `ObsBinding` / `ActionBinding` / `TimingSpec` / `ActionTimingSpec` | D1.1-B `:96-99` / `:111-118` / `:125-135` | hash 参照 + 等値。ただし `05:19` が `ActionBinding` に無い `bounds` を書く ⇒ **A2-04** | FINDING |
| `EvidenceRecord` / `ProofItem` / certificate 型（`ContractCertificate` / `EvidenceEvaluationCertificate` / `UsageEligibilityReport`） | `:259` / `:296` / `:305` / `:330` | `05:645` が `ContractCertificate` を存在検査に使うのみ。field 参照なし | PASS |
| `HandoffOffer` | `:496-506` | `handoff_offer_id` / `control_epoch` / `ownership` を参照（`05:44`・`05:390`） | PASS |
| `SkillInvocation` / `SkillOutcome` | `:477-486` / `:488-494` | `control_epoch` / `invocation_id` / `outcome` を参照 | PASS |
| `TransitionRecord` / `SchemaVersionStamp` | `:508-522` / `:524-529` | `episode_id` / `invocation_id` / `safety_events`（`:521`）を join key として使い、stamp を再利用（`05:619`・`05:623`） | PASS |
| `BeliefRef` | `:151` | 下記 A3 | PASS |
| `LifecycleValidationReport` / `HandoffValidationReport` | `:360` / `:366` | `05:172-173` が ref として参照 | PASS |
| `AuthorityDecision` | `:345-352` | `05:47`・`05:391`（読むだけ・`granted` を設定しない） | PASS |

### A2 — 凍結 enum に member 追加が無い ⇒ **PASS**

- `ProducerOutcome` = `TerminalOutcome | InterruptOutcome`（`:151`）が不変であること: `05:577` 規則 1 + `INV-20`（`05:650`）+ T-19（`05:681`）。機械検査 F2 も「`NO_CHAIN` が `ProducerOutcome` enum block 内に無い」を FAIL 0 で確認。
- `NO_CHAIN` の所在は runtime のみ — `SafeHoldReason`（`05:122`）と `RuntimeDisposition`（`05:559`）。`02 F12` も home = runtime/orchestrator。
- `ControlMode`（`:62`）/ `ActionHold`（D1.1-B `:40`）/ `ExprKind`（`:151`）は参照のみ（`05:54`・`05:336`・`06:122`・`06:189`）。`Dtype` / `FeatureSource` / `IdentityKind` / `InterruptReason` / `ResumeCapability` は候補に出現しない（`grep` 0 hit）— U-4（D1.1-B `:377`：`FeatureSource` から HANDOFF を除外）の再導入も無い。
- `TrainingLineage` / `ProofKind` は「足さない」の宣言文脈でのみ出現（`05:693`・`05:513`・`06:329`）。
- 新 enum は全て runtime / deployment 層（`OwnerKind` / `SafeHoldReason` / `CasKind` / `CasResult` / `LeaseMode` / `EnvelopeTermKind` / `BoundaryKind` / `AuditKind` / `RuntimeDisposition` / `RuntimeFaultCode` / `StopClass` / `HealthCheckKind` / `HealthCheckStage` / `EscalationTarget` / `DeploymentEvidenceKind` / `ValidityRule`）。`LeaseMode` の member 名を EP profile 名と一致させない設計（`05:327`・`05:425`）は軸の混同を型で防いでおり適切。

### A3 — `BeliefRef` の逐語再利用 ⇒ **PASS**

凍結 `:151` = 「**BeliefRef** = {value: SnapshotRef | HashRef, t_obs: float, ttl: float, confidence: float, ood_flag: bool}（**SnapshotRef = {canonical_belief_snapshot: dict} / HashRef = {ref_hash: str〔64-hex〕}**）」。候補は `05:17`・`05:46`・`05:360`・`05:490`・`05:518`・`05:537`・`02 F04` で同一表記を用い、`05:360` は「tag enum は frozen に無い」と明記。`BeliefSnapshotRef` は 4 文書のいずれにも無い（機械検査 F1 = FAIL 0、`grep` でも 0 hit。02 F05 の 1 行は `disposition = removed` の fold 記録）。v0.1 の重複 belief 参照型は残っていない。

### A4 — 全 `file:line` 引用が正しい ⇒ **PASS（誤り 0 件 / 74 種を手動 sed 検証）**

機械検査 C1 が 194 citation の存在と range を FAIL 0 で通し、私は distinct 94 種のうち 74 種を実読した。特に検証した代表:

- contracts_v2: `:39`（certificate を無効化しない）・`:62`（ControlMode 3 member）・`:142`/`:146`/`:147`・`:148`（recovery_rollback_target loud-discard）・`:151`（構成型 inline）・`:158`/`:159`・`:168`（WCJ 4 規則）・`:218`（BehaviorSignature 13 面・補集合定義）・`:219`・`:345-352`・`:351`・`:354`（granted == True 必要条件・非十分／UNKNOWN = fail-closed DENY）・`:372-373`（now 明示）・`:375-378`（authority_epoch_snapshot 明示引数）・`:380`（required ⊆ offered・interrupt checkpoint ∈ checkpoint_specs）・`:381`（等値検査・`E_HANDOFF_EPOCH_STALE`）・`:382`（責務分離）・`:385`（strict codec 5 拒否 — `06:221` の「同じ 5 拒否」も正しい）・`:477-486`・`:486`・`:488-494`・`:496-506`・`:498`・`:503`・`:506`・`:508-529`・`:521`・`:524-529`・`:537`（U14 defer）・`:550`（→ A2-02）・`:570`（carry (iii) failure/no-chain outcome）。
- D1.1-B: `:4`（frozen 3 file を編集せず・schema delta を導入しない）・`:5`（pS C-1）・`:23`（反循環）・`:24`（WCJ 継承 + B-declared 3 規約）・`:40`（ActionHold 2 member）・`:52`（BoundsSpec）・`:65-66`（適用順 raw→normalizer→transform→bounds→cast）・`:75-76`（unit / frame 等値必須）・`:81`（per-feature bounds）・`:96-99`・`:125-127`・`:128-135`・`:132`・`:133`・`:138`・`:185`（bump = identity event）・`:374`（U-1）・`:377`（U-4）。
- EP md: `:101`（INITIATION_SPEC claim target = H_WCJ(当該 spec)）・`:136`（SHADOW（非 authority）列）・`:148`・`:154`（profile 充足の意味論）・`:155`（「SHADOW は定義上 非 authority」逐語）・`:156`（usage matrix = ceiling ／ two-key + 独立安全 gate を必ず conjoin）・`:163`（definition hash = H_WCJ(policy_definition)・17 member・metadata は hash 外）。
- EP JSON: `:5`（hash_projection_rule）・`:199`（CLOSED_LOOP external_conjuncts 3 個）・`:213`（usage_ceiling）。加えて JSON を実 parse し `policy_definition` = **17 member**・`claim_targets` = 14 key（うち `_rule` は meta ⇒ **component 13**）・`proof_binding` = **15 ProofKind**・`grades` = **5**・`profiles` = 3 を確認 — `04:21`・`04:26`・`06:329` の数値は正しい。
- D0: `:179`（H<5）・`:207-215`・`:210`・`:217-223`・`:243-247`・`:246`（Recovery Skill = must be designed; ABSENT today）・`:267-274`・`:270-272`（single authoritative writer・one atomic token flip・exactly one owner at every instant）・`:273-274`（producer retains ownership and safe-stops・never released into a vacuum and never duplicated）・`:280-281`・`:310-320`・`:317`（SafetyDecision 4 action）・`:318`（heartbeat）・`:319`（preemption acts first, does not wait for ack）・`:320`（stabilized_post_action_state）・`:354-355`（CLOCK_MONOTONIC 単一 origin）・`:363-365`（D_event 等）・`:367-372`（provisional・real-time claim 禁止）。
- 統治文書: Rs C3 `:5` / `:32-34` / `:63` / `:65`・slice prereg `:59`・charter `:13`（"boundary-only or a real-time execution profile" 逐語）/ `:13-15` / `:129`（S0 = zero control authority）・skill ownership ruling `:9` / `:24`（DDR#32 は両 court にまたがる）・D11C custody `:36-38`・D11B freeze record `:39`・`T-WMSO/state.md:91`（D0 EXIT GRANTED）・`00-DESIGN-STATUS-LEDGER.md:136`（= DDR#32 行・正しい）・`HANDOFF_pQ_rstechlead2_wmso.md:11`（DDR#41）。
- `06:398` OPP-7 の「RS71 §0 の型式（UR15）」も `RS71-System-Spec-SSOT.md:23`（INVARIANT #1 DUAL-ARM = UR15 × 2）で裏が取れる。

C2 WARN（05 で 6・06 で 19）は全て heuristic の取りこぼしで、私の実読では引用先が主張を支えている。ただし WARN を生む書式（1 行に複数 locus を並べる／frozen 行を型根拠に引きつつ同じ行で新語を導入する）は A2-10 の self-containment と同根なので、fold 時に併せて整理するのが望ましい。

### A5 — authority state machine と §5B / §5A3 の整合 ⇒ **FINDING（A2-01・A2-05）／中核は PASS**

- `validate_handoff` に `authority_epoch_snapshot` を明示引数で渡す（`05:50`・`05:245`）＝ `:375-378` と一致。**PASS**
- `E_HANDOFF_EPOCH_STALE` は等値のみ（`05:51`・`05:71`・`05:243`）＝ `:381` と一致。単調性・再利用拒否を validator に負わせない読みも `:382` と一致。**PASS**
- 消費は CAS 内・検証は CAS 前（`05:246-247`）で、`consumed_offer_ids` が CAS tuple の一部（`05:134`・`05:231`）。二重遮断の証明 C（`05:316`）は成立している。**PASS**
- `granted == True` は必要条件であって十分条件ではない（`05:48`・`05:420`・`05:691`）。CAS 条件 6（`05:224`）は `granted == True` に加えて decision の inputs 束縛一致まで要求しており、frozen `:354` の「上位 orchestrator gate は自由に追加可」と矛盾しない。UNKNOWN gate state = fail-closed DENY は `evaluate_authority_grant` の内側（`:354`）にあり、候補は decision を**読むだけ**なので迂回経路は無い。**PASS**
- 責務分離の帰属（旧 epoch command 拒否）は §2 表と anchor 2 の間で食い違う ⇒ **A2-05**。clearance 集合の執行漏れ ⇒ **A2-01**。

### A6 — `max_obs_staleness_s` = 訓練時仮定 / runtime 鮮度 = frozen FreshnessPolicy + 明示 `now` ⇒ **PASS**

`05:57`・`05:72`・`05:337`（`training_max_obs_staleness_s` は記録のみ・執行しない）・`05:398`・`06:33` が D1.1-B `:99` の役割分離を正しく引く。runtime 側は `FreshnessPolicy.max_staleness_s`（`:151`）を出発点に profile が stricter-or-equal に narrow（`05:338`・`06:235`）。`now` は全て明示引数（`05:494` B4・`05:504`）で、D0 `:354-355` の単調 clock 単一 origin を引き、隠れ wall-clock 参照を禁じている。`max_staleness_s = None` の意味が frozen に無いことを loud に記録し（`05:399`・OP-16 `05:722`・`06 OPP-1`）、runtime 層の読みだと明言しているのは honest。

### A7 — hash 面に何も入らない / runtime 記録は U14 defer ⇒ **PASS**

- `SkillDefinitionHash`（`:159`）/ `SkillActionId`（`:158`）/ `ExecutionBundleHash`（`:157`）/ `BehaviorSignature`（`:218`）/ `tensor_binding_hash` / `evidence_policy_definition_hash`（EP `:163`・17 member）に候補が足すものは無い。`03` matrix C01–C07 の全 hash 列が `no` で、私の独立確認とも一致する。
- 新しい hash は `profile_hash`（`06:214`）と `deployment_evidence_policy_hash`（`06:324`）だけで、どちらも凍結 hash の preimage に入らない（`06:37`・`06:220`）。
- runtime 記録の content-hash 方式を定めない旨は `05:624` が `:537`（U14 明示 defer）を引いて明記。OP-5（`05:710`）にも open として残す。
- ⚠ この項の**機械的裏付け**として挙げられている INV-16 は破れている（A2-02）。実体判定は PASS だが、根拠機構は修正が要る。

### A8 — EP evidence profile / deployment profile / execution profile の 3 軸分離 ⇒ **PASS**

`05:73`（読み方の固定 3）・`05:694` 項 4・`06:20-23`・`06:52-58` の 3 軸表・`06:380` 項 4 が一貫。SHADOW = 非 authority の帰属は EP `:136` / `:155` の逐語のみに依拠し（`05:61`・`05:418`・`04:49`）、Rs C3 `:32-34` の「SHADOW（rank 2・非 authority）」と整合。usage ceiling は `06:377`・`05:625` が JSON `:213` を引いて不変と宣言。conjoin 規則は `05:691` が EP `:156` を、`05:62`/`05:780` が JSON `:199`/`:213`（acceptance_test・two_key_O0_S0_V0・independent_safety_gate）を引いており、弱められていない。`LeaseMode` を EP profile 名と別名にした判断（`05:425`）は軸の混同を型で防ぐ方向であり、A8 に対して強化的。

### A9 — 凍結の委譲・carry を再決定していない ⇒ **PASS**

- U-1（B は epoch field を持たない・D1.1-B `:374`）: `05:60`・`02 F01` が epoch を runtime 層の状態として扱い、binding に持ち込まない。**PASS**
- U-4（`FeatureSource` から HANDOFF 除外・D1.1-B `:377`）: 候補に `FeatureSource` / HANDOFF source の再導入は 0 hit。**PASS**
- D1.1-C carry（manifest 未凍結・open-11）: `05:700` 項 10・`06:386` 項 10 が「manifest の field を参照しない」と明記。**PASS**
- 合成 = pX court / Rs 専権 OPEN: `05:695` 項 5・`06:340-343`・`02 F39`・`03 C14` が `WMSO_RS_D11C_SCOPE_APPROVAL_CUSTODY_20260721.md:36-38` と `WMSO_D11B_FREEZE_RECORD_20260721.md:39` と `WMSO_RS_SKILL_OWNERSHIP_RULING_20260720.md:9` を引いて OUT に留めている。`ParallelExecutionProfile` は 4 文書の規範本文に無い（機械検査 F1 = FAIL 0）。**PASS**
- execution-profile 軸 = Rs（Rs C3 `:65`）: `05:694`・`06:57`・`06:380` が「決めない」と明記。**PASS**
- DDR#32 を解かずに open として列挙（`05:706` ↔ skill ownership `:24`・LEDGER `:136`）。**PASS**

### A10 — self-containment と honesty ⇒ **FINDING（A2-05・A2-10・A2-11）／主要部は PASS**

- 「主張しないこと」は 05 §11（10 項）・06 §9（10 項）に在り、内容は正確（authority を付与しない・impl を含まない・schema delta なし・execution profile を裁定しない・composition OUT・checkpoint 切替なし・EP 不変・freeze / two-key / gate PASS を主張しない・D1.1-C manifest 不参照）。**PASS**
- Open points は 0 と宣言していない（05 §12 = OP-1..OP-17 の 17 項・06 §10 = OPP-1..OPP-10）。両方に「⛔ open = 0 を宣言しない」の見出し注記あり。**PASS**（なお 05 §12 は OP-15 → OP-17 → OP-16 の順で並んでおり、id の欠落は無いが並びが乱れている — 実害なし・fold 時に整列を推奨）
- gate / authority を主張していない（status 行が「未 bank・two-key 未・Rs 未裁定・gate PASS を主張しない」）。judge panel 未実施を `05:8`・`05:746` で honest に開示している点も評価できる。**PASS**
- header pin: 4 pin とも full 64-hex で、commit `54f90a7de1e02fb14eaf793bf3c60d9503d0d82e` / `07250f4a0208b3bbd27eae6fef7d980743c4b538` は §0.1 の再測と一致。**PASS**
- anchor 2 の過大主張 ⇒ A2-05。package 外 artifact への依拠 ⇒ A2-10。placeholder timestamp ⇒ A2-11。

### A11 — 07 / 04 の論理が凍結の hash / codec 規則から導かれている ⇒ **FINDING（A2-06・A2-07）／大枠は PASS**

- `04:20-22`: `evidence_policy_definition_hash = H_WCJ(policy_definition)`・metadata は hash 外（EP `:163`・JSON `:5`）から「17 member に何も足さないので hash 不変」を導く。私が JSON を parse して 17 member を確認済み。**PASS**
- `04:26-28`: runtime 事象語彙が EP に無いことを grep で示す（私の再現でも `epoch`/`lease` = 0/0）＋ U14 defer（`:537`）。**PASS**（ただし根拠 file が package 外 ⇒ A2-10）
- `07:38`（strict codec `:385` ゆえ v2 型に静かに field を足す経路が無い）・`07:40`（certificate が EP definition hash を結合する `:163`）は正しく entail。**PASS**
- `07:39` の identity 主張 ⇒ **A2-06**。`03` C08–C12 の citation 未接地 ⇒ **A2-07**。

### A12 — CSV matrices ⇒ **PASS（行数・語彙・引用・05/06 との整合）／citation 完全性のみ A2-07**

- 行数: `02` = header + **40** データ行 ✓（handoff 決定 G の「40 rows」と一致）。`03` = header + **14** ✓。
- 語彙: `02.layer_of_origin` ∈ {frozen_A, frozen_B, v0.1_proposal, v0.2_runtime, v0.2_profile, future}・`overlaps_frozen` ∈ {yes, no, partial}・`disposition_v0_2` ∈ {reuse_frozen, reference_only, new_runtime_type, new_profile_type, removed, not_adopted, deferred}。`03.verdict` ∈ {no_frozen_impact, profile_only, runtime_audit_only, successor_contracts_v3_required, EP_successor_required, composition_delta_required}、hash 列は {yes, no, n/a}。いずれも一貫して使われ、未定義値は無い。
- 引用: CSV 内の全 citation を C1 で存在確認、主要行（F01 `:486`/`:503`、F02 `:498`、F03 `:375-376`/`:381`、F04/F06/F08/F09/F11 `:151`、F07 `:380`、F10 `:354`、F15/F40 `:537`、F24-F28 D1.1-B、F30 `:148`、F32 `:218`、F34 `:147`、F39 custody `:36-38`、C08-C12 `:159`/`:218`/`:385`）を実読で照合し、指し先の誤りは 0。
- 05/06 との矛盾: `02 F13` の `RuntimeDisposition` 8 member は `05:559` と完全一致。`02 F23` の envelope 4 項は `05:328`/`05:403-408` と一致。`02 F20` の「EP は『非 authority』のみ規定・gateway 出力無効は runtime 層の定義（handoff 外の追加）」は `05:744` の自己申告と一致。`02 F35` の「v0.2 = 受理 + HOLD（切替無効）」は `05:481` と一致。`03 C01-C07` は `05 §8`・`06 §8.1` 規則 (4) と一致。**矛盾は検出できなかった。**
- 唯一の弱点は C08–C12 の「ActionId 不変」を entail する frozen 行が引かれていないこと（A2-07・LOW）。行数・語彙は変えずに citation 欄の追記だけで解消する。

---

## 3. Verdict

**HOLD**（08 計画 §5 の判定様式に従う — 未反証の HIGH が 2 件残るため）。

- CRITICAL: 0
- HIGH: 2（A2-01 clearance 執行漏れ・A2-02 INV-16/T-18 不成立）
- MEDIUM: 4（A2-03 / A2-04 / A2-05 / A2-06）
- LOW: 6（A2-07 / A2-08 / A2-09 / A2-10 / A2-11 / A2-12）

補足: **12 件すべて、修正は本文の書き換えのみで済み、frozen 4 file への編集も schema delta も新 field / 新 enum member も要さない**（A2-01 は既存 enum member を条件式の集合に足すだけ、A2-02 は invariant の述語を frozen `:550` の形に戻すだけ、A2-12 は既存 `RuntimeFaultCode` member の再利用）。したがって fold 後に HIGH 2 件が解消すれば PASS-WITH-CONDITIONS へ移行し得る。ただし本 report は fold の結果を先取りしない。

また、本 review で**実体としての schema delta は検出できなかった** — 凍結 4 file は無編集（11/11 PASS 再測）、frozen 型・enum への field / member 追加は無く、`BeliefRef` は逐語再利用され、hash 面（`SkillDefinitionHash` / `SkillActionId` / `ExecutionBundleHash` / `BehaviorSignature` / `tensor_binding_hash` / `evidence_policy_definition_hash`）はいずれも不変である。HIGH 2 件は「凍結との一致そのもの」ではなく「一致を担保する機構（CAS 前提条件・機械検査 invariant）が宣言どおりに働かない」点にある。

## 4. 本 report が主張しないこと

1. gate の flip・two-key・Rs 裁定・freeze・bank を主張しない（08 計画 §6 のとおり本レビューは準備レビュー）。
2. 他軸（B runtime-safety / C deployment）の finding は参照していない。同意は独立確認ではない。
3. impl / training / closed-loop authority / slice の解錠に関与しない。
4. 私の finding もまた 3 lens（text / logic / impact）の反証検証にかけられるべきであり、既定は refuted である。
