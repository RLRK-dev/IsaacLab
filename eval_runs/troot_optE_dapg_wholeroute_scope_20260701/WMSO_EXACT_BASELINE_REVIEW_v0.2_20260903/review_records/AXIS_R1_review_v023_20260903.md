# R1 — WMSO v0.2.3 REVIEW CANDIDATE 独立レビュー（contract fidelity + runtime safety・adversarial）

- reviewer: R1（別 context・authority 無し・凍結物へ非接触・`eval_runs/` 無改変）
- 実施: 2026-09-04 22:21–22:40 UTC（`date -u` 実測。開始 22:21:24、premise 確認 22:31:25、report 書出し 22:38:27、JSON 書出し 22:40:01）
- 対象: `$PKG/05_WMSO_RUNTIME_SPEC_v0.2_REVIEW_CANDIDATE_20260903.md`（header v0.2.3・928 行・全行 read）／ `$PKG/06_WMSO_INDUSTRIAL_PROFILE_v0.2_REVIEW_CANDIDATE_20260903.md`（v0.2.3・526 行・全行 read）
- `$PKG` = `eval_runs/troot_optE_dapg_wholeroute_scope_20260701/WMSO_EXACT_BASELINE_REVIEW_v0.2_20260903`、`$D` = `eval_runs/troot_optE_dapg_wholeroute_scope_20260701`
- 凍結参照（sed で内容確認済）: contracts_v2 `$D/WMSO_D11A_CONTRACTS_V2_DESIGN_RSTECHLEAD2_20260719.md`（以下 A）:39/:62/:151/:341/:345-354/:372-385/:477-529/:537 ／ tensor_binding `$D/WMSO_D11B_TENSOR_BINDING_DESIGN_RSTECHLEAD2_20260720.md`（以下 B）:23-24/:40/:96-99/:125-138/:255/:374 ／ D0 `$D/WMSO_D0_ARCHITECTURE_DRAFT_RSTECHLEAD2_20260718.md`:207-223/:243-247/:267-274/:310-320/:354-372 ／ EP md :101/:136/:154-156/:163 ／ EP JSON :5/:199/:213 ／ Rs C3 :5/:32-34/:63/:65 ／ slice prereg :59
- 先行 round（`$PKG/11_THREE_AXIS_REVIEW_RECORD_20260903.md`・05 §13 / 06 §11 の fold 表）は読み、**fold 済み項目は再報告していない**。陽性対照（`10_*`）の注入欠陥 8 件は実文に存在しないことを確認（PC-05-A: :237 で同一 CAS 内拒否／PC-05-B: :185 one-shot／PC-05-C: :235 `∪ {consume_offer_id}` 存在／PC-05-D: :448 DISABLED／PC-06-E: bounds field 無し／PC-06-F: 06:242 等値／PC-06-G: 06:120 True 必須／PC-06-H: 06:70 enum に緩和値無し）。

## 0. 共通前提の実測

### 0.1 pin 再測（premise 1）

```text
$ bash $PKG/verify_exact_baseline_pins.sh      # 2026-09-04 22:21 UTC
PASS  D1.1-A DESIGN v2.11.2 (worktree)  00192d20ca00b654cf6cfdb9d04b03ca14adfd0b93f2105c0fea28c295ff8aff
PASS  EvidencePolicy v1.9 md (worktree)  c474acea7c58acc22050c2ad9944fd45a18f5c76967964b42d11922e28fa27e7
PASS  EvidencePolicy v1.9 json (worktree)  e63176af9bc3a246b1c32db369ec59f8d09a4c96c381bb03a3a6024bd9811c6e
PASS  D1.1-B DESIGN v13 (worktree)  5a1874d3be8b98b8aeaace73890d8021cbc7f9814e048741f7f58d6746f349a6
PASS  D1.1-A DESIGN git blob id (HEAD)  1353430228a90ad36dd190a9359cd81da52e8242
PASS  D1.1-B DESIGN git blob id (HEAD)  ebe8154abf2461a6b24db05728b20e5cee949fb2
PASS  D1.1-A DESIGN sha256 of blob 135343…  00192d20ca00b654cf6cfdb9d04b03ca14adfd0b93f2105c0fea28c295ff8aff
PASS  D1.1-B DESIGN sha256 of blob ebe815…  5a1874d3be8b98b8aeaace73890d8021cbc7f9814e048741f7f58d6746f349a6
PASS  D1.1-A DESIGN at frozen commit 54f90a7d  00192d20ca00b654cf6cfdb9d04b03ca14adfd0b93f2105c0fea28c295ff8aff
PASS  D1.1-B DESIGN at frozen commit 07250f4a  5a1874d3be8b98b8aeaace73890d8021cbc7f9814e048741f7f58d6746f349a6
PASS  evidence_policy_definition_hash (embedded command)  e7ca43093084c167a209b008533a66d26a1fd3223d2a3c11274d28306c3ff803
policy_semver=1.9.0
HEAD=ddb37db1d3eee2a4fa92331312cfbb4ff22f3af1 branch=claude/v0-2-design-candidates-snqbmy
EXIT=0
```
→ **11/11 PASS**（原本 = `review2/R1_pin_recheck.txt`）。

### 0.2 機械検査（premise 2）

```text
$ python3 $PKG/check_review_candidate.py $PKG/05_WMSO_RUNTIME_SPEC_v0.2_REVIEW_CANDIDATE_20260903.md --runtime
== …/05_WMSO_RUNTIME_SPEC_v0.2_REVIEW_CANDIDATE_20260903.md: citations=152 FAIL=0 WARN=10
  (WARN は全て C2 heuristic: line 97 ×2, 144, 227, 527, 529, 780, 788, 922 ×2)
EXIT=0
```
→ **FAIL=0 / WARN=10**（原本 = `review2/R1_checker.txt`）。WARN 10 件は 05 anchor 23 の説明（引用行が別規範を担い同文に新語が並ぶ）と一致し、sed で引用先を再確認した（A:351 = AuthorityDecision に timestamp 無し／D0:354 = 単調 clock／A:323 = certificate-first／prereg:59 = TERMINAL のみ／A:151 = FreshnessPolicy 型のみ）。

## 1. 判定

**verdict = HOLD**（08 §5 の定義に従う: HIGH が 1 件残る）。ただし全 finding の最小修正は **text-only・frozen schema delta 0**（runtime 層型への field 追加を含む案は 1 件 = R1-03 の `AuthorityState.safehold_disposition`、これも凍結 4 file 外）。HIGH（R1-05）は 1 行の列挙追加で閉じるため、次 fold で PASS-WITH-CONDITIONS に到達し得る。

| severity | 件数 | id |
|---|---|---|
| CRITICAL | 0 | — |
| HIGH | 1 | R1-05 |
| MEDIUM | 5 | R1-01, R1-02, R1-03, R1-04, R1-06 |
| LOW | 10 | R1-07 … R1-16 |

## 2. PASS 1 — v0.2.3 fold 表（05 §13 :880-902）の行ごとの回帰検査

判定基準: (a) finding が記述した欠陥を新文が実際に除去したか／(b) 05・06 の他文と矛盾しないか（概念を両 doc で grep）／(c) 到達不能状態・未定義記号・対応物と不一致な集合を新たに導入していないか。

| # | fold 行 | 読んだ節（05:line / 06:line） | (a) 除去 | (b) 矛盾 | (c) 新規不整合 | 判定 |
|---|---|---|---|---|---|---|
| 1 | A-01 / B-M5（残差） | 06 §5 :299 ↔ 05 §3.5 条件 6 :227・条件 2 :223・§3.4 (f) :184 | yes（06 が「条件 2・§3.4 (f) の等値束縛」を指し、条件 6 は certificate の skill_action_id のみ） | なし | なし | **OK** |
| 2 | A-10（残差） | 05 §6.1 :527 ↔ §3.7 :273-274 ↔ §7 :639-640 ↔ §5.4 :510 | yes（reason で分岐・SAFETY_STABILIZED → SAFETY） | なし | SAFETY_STABILIZED の解除前提（ISL clearance record・`stabilized_post_action_state`）が ISL 決定不在時に未定義 → **R1-14（LOW）** | OK（注記付き） |
| 3 | A2-12（残差） | 05 §7 規則 3 :627 ↔ §3.7 :326 ↔ INV-27 :708 | yes | なし | なし | **OK** |
| 4 | B-M1 / B2-04（残差） | 05 §3.5 :241 ↔ §5.2 (ii) :492 ↔ INV-24 :705 ↔ §2 :82 | yes（非 safety 起因の窓を正直化） | §2 表 :82 の gateway 状態列に `pending_invalidate` 無し（**R1-11**） | `pending_invalidate` の clear 条件が「再試行 CAS の SUCCESS 観測」のみで、lease-scoped CAS が破棄された場合（:241「不一致なら CAS を捨て」）に永久に立つ → **R1-16（LOW）** | finding（LOW ×2） |
| 5 | B-M2（残差） | 05 §3.7 失敗表 :301 ↔ T-09 :728 ↔ 遷移表 :282 ↔ §3.5 優先順位 :241 ↔ INV-30 :711 | 部分（SAFETY / SAFE_STOP の保持は入った） | 遷移表 :282 は無条件に `S_SAFEHOLD(MANAGER_RESTART)`、失敗表 :301 は条件付き — **同一事象で 2 記録が不一致** | persisted ∈ {MANAGER_RESTART, GATEWAY_RESTART, CHECKPOINT_DISABLED}（同 tier）の分岐が :301 に無い（「より弱い」でも SAFETY/SAFE_STOP でもない）→ **R1-03（MEDIUM）** | finding |
| 6 | B-M7（残差） | 06 §8.1 (3) :373 ↔ 06 §5 :302 ↔ 05 OP-19 :787 ↔ 05 §4.5 :458 | yes | :373「lease を無効化するのは … のみ」が 05 §4.5 の 12 trigger と字義上矛盾（scope 語の欠落）→ **R1-15（LOW）** | なし | OK（注記付き） |
| 7 | B-L3（残差） | 05 §8 :673 ↔ AuditKind :653 ↔ INV-17 :698 | yes | INV-17 は「§3.7 の各事象」に scope されるが :673 は「欠落 = INV-17 違反」を COMMAND_ADMITTED（§3.7 事象でない）に適用 → **R1-10（LOW）** | なし | OK（注記付き） |
| 8 | B2-05（残差） | 05 §3.5 例外 :241 ↔ §5.2 (iii) :493 ↔ §3.7 :278-279 / :324 ↔ §2 :82 | 部分 | (iii) は「当該 SAFEHOLD 遷移の記録 disposition」を要するが、gateway の状態（:82）にも `AuthorityState`（:130-137）にも disposition が無く、audit 参照が必要。ISL STOP/HOLD 遷移行 :278 は DISPOSITION record を要求しない（失敗表 :302 は HOLD / SAFE_STOP）→ **R1-09（LOW）** | 同一 reason（SAFETY）への再遷移が「強める方向にしか書き換えない」規則で no-op になると、ISL HOLD（HOLD）→ heartbeat 喪失（SAFE_STOP）の順で SafeStop が clearance 無しに SafeHold へ緩む → **R1-03（MEDIUM）** | finding |
| 9 | B2-14（残差） | 05 §3.4 (e) :184 ↔ A:341 / :345-352 | **no** — age の基準を「manager が最初に解決した時刻」に置いたため、初回使用時は age = 0 で常に通る（decision の鮮度ではなく manager cache の鮮度）→ **R1-02（MEDIUM）** | なし | なし | finding |
| 10 | B2-17（残差） | 05 T-17 :736 ↔ INV-17 :698 ↔ §3.7 :269 ↔ §8 :673 | yes | :269 の record 集合（LEASE_INVALIDATED(旧) を S_SAFEHOLD 起点でも要求／PERMIT_CONSUMED・CAS_ATTEMPT を含まない）と :673 の記録義務が不一致 → **R1-10（LOW）** | なし | OK（注記付き） |
| 11 | CD-12 | 05 §4.2 :428 ↔ 06 §3 :246 ↔ 06 §5 :301 | yes | なし | なし | **OK** |
| 12 | CD-13 | 05 INV-22 :703・§4.1 :359 ↔ §3.2 :144・§3.3 :160 ↔ 06 §5 :301 | yes | なし | なし | **OK** |
| 13 | CD-10 | 05 :281 / :325 / :371 / INV-33 :714 / T-32 :751 / OP-19 :787 ↔ 06 :161 / :258 / :302 / :393 | yes | INV-33「全 lease」 vs 遷移行 :281 の from = `S_LEASE_ACTIVE / S_HEALTH_PENDING` のみ（S_BOUNDARY_WAIT でも lease は活性 :243/:503）→ 上限超過が S_BOUNDARY_WAIT で最大 `boundary_dwell_s` 放置される → **R1-15（LOW）** | なし | OK（注記付き） |
| 14 | CD-05 | 05 §3.4 (g) :184 ↔ 06 :193-200 / :242 ↔ INV-12 :693 | yes | なし | なし | **OK** |
| 15 | CD-06 | 05 §3.4 (h) :184 ↔ 06 :140-143 / :247 | yes（`ownership.resource` は opaque ゆえ未検査 = 06 :143 の宣言どおり・finding ではない） | なし | なし | **OK** |
| 16 | CD-17 | 05 §3.4 (i) :184 ↔ 06 §8.1 :373 / OPP-13 :435 | yes | なし | なし | **OK** |
| 17 | CD-04 | 05 §3.4 (j) :184・§4.3 :437 ↔ 06 :101 / :106 / :119 / :168 / :271 / PT-25 :403 | **部分** — 同じ攻撃面 `ZoneRef.geometry_sha256`（06 :106）が (j)・`P_PREDICATE_HASH_MISMATCH`・gateway 再検証のいずれにも無い → **R1-05（HIGH）** | なし | なし | finding |
| 18 | CD-02 | 05 §5.4 :516-518 ↔ 06 :190 / :261 / PT-22 :400 | yes（形は定義された） | 05 :516 は条件 10 集合の全 reason（SAFETY 含む）に `record.role ∈ clearance_roles[reason]` を要求、06 :190/:261 は SAFETY を必須集合から除外 → SAFETY role 無し profile が登録を通り SAFETY hold が解除不能 → **R1-04（MEDIUM）** | なし | finding |
| 19 | CD-07 | 05 §4.3 :436 ↔ 06 :86-93 / :268 / :270 / :286 | yes | なし | なし | **OK** |
| 20 | CD-18 | 05 §4.3 :435 ↔ 06 §4 :295 / OPP-14 :436 | **部分** — ZOH で「点」評価は DIFF_IK_EE_TARGET の連続運動と整合しない（線分は hold 種別と無関係に traverse される）→ **R1-06（MEDIUM）** | なし | なし | finding |
| 21 | CD-03 | 05 §3.4 (b) :184 ↔ 06 :264-265 / :373 | yes | なし | なし | **OK** |

集計: OK 12 行（うち注記付き 5）／ finding 9 行。

## 3. PASS 2 — 状態機械への adversarial 構成（§3.2–§3.7・§4・§5.2・§5.4・§6.2）

検査した攻撃面と結果（PASS の根拠行も併記）:

- **B1 二重所有**: 同一 `expected` への 2 permit の並行 CAS → 条件 1 の tuple 等値（:222）+ 線形化点 = durable commit（:142）で高々 1 SUCCESS。旧 epoch 拒否は事後条件 :237 で同一線形化点。stale ACK に束縛された permit → 条件 3 `now ≤ ack.valid_until`（:224）。→ 構成不能（PASS）。ただし条件 3 失効時の fault code が無い（R1-07）。
- **B2 所有不在**: CAS 線形化点で (ii) が空に（:497）、(i)(i′)(iii) が全域（:489-493）。→ PASS。ただし (iii) の SafeHold/SafeStop 選択が audit 参照に依存（R1-03(c)）。
- **B3 replay**: `consumed_offer_ids` が tuple 要素（:134, :225, :235）／ permit one-shot（:185, :338）／ genesis 下限（:143）。→ PASS。
- **B4 crash matrix**: 新 executor crash（:299）／ manager restart（:301, INV-32）／ gateway restart（:283, :499, INV-25/31）／ 転送窓内の safety override（:302）／ permit 後の Orchestrator 喪失（:284）— 各々に disposition。→ PASS。ただし同 tier reason の manager restart（R1-03(a)）と、INV-31 :712 と §5.2 :499 の不一致（R1-03(d)）。
- **B5 lease**: 活性 lease はちょうど 1（INV-01）／ profile・decision・init・envelope・timing 束縛（:396-411）／ 変更 = 新 CAS（:416）／ SHADOW port DISABLED（:448, INV-09）／ CLOSED_LOOP は `granted == True` 必要条件（:227, :449, INV-08）。→ PASS。ただし decision の鮮度検査が初回使用で空文（R1-02）。
- **B6 boundary-only**: (d) :184 + 条件 11 :232 + INV-28 で mid-skill 転送を authority 層で遮断。InterruptOutcome = 受理 + HOLD（:527）。→ PASS。ただし §6.2 B0 snapshot が MARK_BOUNDARY_WAIT に先行する race（R1-13）。
- **B7 safety 優先**: (i) 最優先・ack 不要（:490, :496）、(i′) heartbeat（:491）。→ PASS。
- **B8 disposition/fault 完全性**: CAS 条件 9・10 の REJECTED に code 無し（R1-07）／ §7 :638 INVALID_STATE → SAFE_STOP が §3.7 表外遷移（R1-08）。
- **B9 audit**: ISL STOP/HOLD 行に DISPOSITION 無し（R1-09）／ INV-17 の record 集合不一致（R1-10）。
- **B10 timing**: 数値定数なし（INV-22）／ rate/hold 等値（INV-12）。→ PASS。ただし **`command_deadline_s` の計時開始・停止条件が未定義で S_BOUNDARY_WAIT / 初回 command 前に発火する（R1-01）**。
- **§3.4 (g)(h)(i)(j) と 06 の timeout 順序 :258 の循環・充足可能性**: (i) の `accepted_profiles` は登録時に P_* = 0 を要し、登録時の evidence（MIN_SHADOW）は profile_hash を参照する record であって受理を前提としない（06 :145, :264）→ 循環なし。CLOSED_LOOP 前提の HEALTH_CHECK_RUN / FAULT_INJECTION_RESULT（audit の (lease_id, seq) に解決 :320）は SHADOW lease で生成可能 → 充足可能。timeout 順序 `safety_heartbeat_timeout_s ≤ command_deadline_s`、`inter_command_jitter_s < 1/action_rate_hz`、`decision_max_age_s ≤ permit_ttl_s ≤ boundary_dwell_s`、`ack_validity_s ≤ boundary_dwell_s`、`lease_max_duration_s ≥ boundary_dwell_s`、`command_deadline_s ≥ 1/action_rate_hz` は同時充足可能（例: rate 48 Hz → deadline 25 ms・heartbeat 20 ms・jitter 5 ms・decision 10 s・permit 10 s・dwell 30 s・ack_validity 5 s・lease_max 600 s）→ 不充足なし。`ack_validity_s` と `permit_ttl_s` の間に順序が無いため ACK が permit より先に失効し得る（R1-07 の code 欠落として扱う）。`command_deadline_s` と `boundary_dwell_s` の間に順序が無いのは R1-01 の帰結（deadline が boundary で発火する設計のままなら順序を要する）。

## 4. Findings

各 finding = {id, severity, 候補 doc:line, 凍結 file:line（矛盾 or 欠落の根拠）, 再現 / 反例, 最小修正（frozen schema delta 0）}。

### R1-01（MEDIUM）— `command_deadline_s` の計時に開始・停止条件が無く、S_BOUNDARY_WAIT と初回 command 前に発火する

- 候補: 05 §4.2 :423「次 command が `command_deadline_s` 内に届かなければ … SafeHold（R_DEADLINE_MISS）」／ :425「`command_deadline_s` 超過で SafeHold + R_DEADLINE_MISS」／ §3.7 :277「any (EXECUTOR) | gateway: command deadline miss | TRANSFER_TO_SAFEHOLD(DEADLINE_MISS)」／ §4.1 :363／ §5.3 :503「producer の SkillOutcome 受領後も lease は活性のまま（S_BOUNDARY_WAIT）、executor は新しい運動 command を出さず … 転送が成立しなければその状態が続き」／ §6.2 B1 :535「SAFEHOLD からの起動（producer 無し）では B1 と offer 検証を skip」／ 06 §3 :258（timeout 順序に `command_deadline_s` と `boundary_dwell_s` の関係なし）。`grep -n command_deadline_s 05` = :241/:363/:423/:425/:426/:705 のみ — 停止条件の文は無い。
- 凍結: D0 :273-274「`timeout`（no `accept` within the handoff deadline, §G）→ producer retains ownership and safe-stops」— boundary の deadline は handoff deadline（`D_event` :364）であって command 周期ではない。A:381-382（validate_handoff 経路 = chained handoff の契約検査）。
- 再現: (1) L1 が TerminalOutcome を出す → MARK_BOUNDARY_WAIT（:243）。(2) :503 により executor は command を止める。(3) `command_deadline_s ≥ 1/action_rate_hz`（数十 ms 級）が経過 → :423/:277 が発火 → TRANSFER_TO_SAFEHOLD(DEADLINE_MISS)・epoch+1・全 UNUSED permit VOID。Orchestrator の再選択（ack_timeout_s + permit + CAS）は通常これより長い。(4) 以後の再選択は S_SAFEHOLD からの起動になり、:535 により `validate_handoff`（A:381）と offer 検証が **skip** され、`lease.ownership` は profile の ResourceAvailability から構成される（:417）。すなわち「chained handoff（frozen §5B/§5D 互換検査を通る経路）」が構造的に到達不能になり、毎 boundary が DEADLINE_MISS FAULT を記録する。同様に、新 lease の CAS 直後（S_HEALTH_PENDING・最初の command 前）に計時が始まるなら `health_confirm_timeout_s` と競合する。
- 影響: fail-closed（安全側）だが、TERMINAL boundary での handoff 契約検査が事実上迂回される・§5.3 / §3.7 boundary 行（`boundary_dwell_s` が滞留の上限）と矛盾する。
- 最小修正: §4.2 と §3.7 に「`command_deadline_s` / `inter_command_jitter_s` の計時は `owner == EXECUTOR ∧ ¬boundary_wait ∧ mode == CLOSED_LOOP_AUTHORITY` かつ当該 lease の最初の admitted command 以降にのみ有効。S_BOUNDARY_WAIT では停止（滞留上限 = `boundary_dwell_s`）、S_HEALTH_PENDING の最初の command 前は `health_confirm_timeout_s` が上限」を追記。併せて「executor heartbeat の義務は LEASE_INVALIDATED まで継続（S_BOUNDARY_WAIT を含む）」を §2 Executor 行に明記。schema delta 0。

### R1-02（MEDIUM）— §3.4 (e) の age 基準が「manager が最初に解決した時刻」であり、初回使用時に鮮度検査が空文（B2-14 残差の未解消）

- 候補: 05 §3.4 (e) :184「`age = now(manager) − t_mono`、`t_mono` = manager が当該 `authority_decision_ref` を**最初に解決した時**に記録した `RuntimeAuditRecord`（kind = ASSESSMENT・payload = decision ref）の `t_mono`」。
- 凍結: A:345-352（`evaluate_authority_grant` は 4 record の純関数・`AuthorityDecision` に timestamp なし :351）、A:341「profile は後段で何度でも（現行 policy で）再評価可」、A:354（granted == True は必要条件）。
- 再現: O0 が時刻 t0 に decision D（granted = True）を発行。gate 状態（`safety_gate_state`）が t1 > t0 で FAIL に変わる。manager が t2 ≫ t1 に初めて D を解決 → ASSESSMENT record（t_mono = t2）→ `age = 0 ≤ decision_max_age_s` → (e) PASS → CLOSED_LOOP permit 発行。B2-14 の指摘「decision は時刻依存の 4 record の関数」は解消していない（bound されるのは 2 回目以降の permit だけ）。
- 影響: CLOSED_LOOP は CLOSED 継続（§11）ゆえ現時点の実害は無いが、fold 表 :890 が「CONFIRMED / 残差処置」と記録する検査が定義上 vacuous。
- 最小修正: (e) の `t_mono` を「manager が `evaluate_authority_grant` を **呼び出した**（または O0 から decision と共に受領した評価時刻の audit record を受け取った）時刻の ASSESSMENT record（payload = decision ref + 4 入力 record の ref）」に置き換え、「評価時刻 record を欠く decision = 不在（R_AUTHORITY_DECISION_ABSENT）」とする。A:341 が再評価を許すため、`decision_max_age_s` 超過時は再評価（新 ref）で回復。schema delta 0。

### R1-03（MEDIUM）— `SafeHoldReason` 優先順位が半順序で、同 tier / 同 reason への TRANSFER_TO_SAFEHOLD の意味論が未定義（B-M2 / B2-05 残差の回帰）

- 候補: 05 §3.5 :241「`SAFETY / SAFE_STOP` > `MANAGER_RESTART / GATEWAY_RESTART / CHECKPOINT_DISABLED` > その他 … 理由を**強める方向にしか**書き換えない（弱い理由への上書きは記録付き no-op）」／ 失敗表 :301「persisted owner が EXECUTOR、または persisted が SAFEHOLD で reason が MANAGER_RESTART より弱いときのみ epoch+1 … persisted が SAFEHOLD(SAFETY / SAFE_STOP) なら reason と epoch を保持」／ 遷移表 :282「any | AuthorityManager 再起動 | … | `S_SAFEHOLD(MANAGER_RESTART)`」／ §5.2 (iii) :493「safehold_reason == SAFETY かつ当該遷移の記録 disposition が SAFE_STOP なら clearance record まで SafeStop」／ §2 :82（gateway の状態 = 線形化読み + 直近 admitted command + restart_pending）／ `AuthorityState` :130-137（disposition field 無し）／ INV-30 :711／ INV-31 :712 vs §5.2 :499。
- 凍結: D0 :318「monitor `failed` ⇒ system fails to safe-stop」、:320。A:382（authority manager = 状態所有）。
- 再現 (a) 同 tier 未定義: persisted = SAFEHOLD(CHECKPOINT_DISABLED)（解除 = operator clearance + 再観測 :512）で manager restart。:301 のどの分岐にも該当しない（EXECUTOR でも「より弱い」でも SAFETY/SAFE_STOP でもない）。:241 を「no-op」と読めば reason 保持で MANAGER_RESTART の「durable 整合検査記録」（:513）が要求されず、「上書き」と読めば CHECKPOINT_DISABLED の operator clearance が MANAGER_RESTART の整合検査へ**弱まる**。遷移表 :282 は後者を書いており、失敗表 :301 と同一事象で不一致。
- 再現 (b) 同 reason no-op が B2-05 fix を無効化: (1) ISL が HOLD 決定 → TRANSFER_TO_SAFEHOLD(SAFETY)・記録 disposition HOLD（:278/:302）。(2) ISL heartbeat が `safety_heartbeat_timeout_s` を超えて欠落 → gateway (i′) SafeStop・manager が TRANSFER_TO_SAFEHOLD(SAFETY)・DISPOSITION(SAFE_STOP)（:279）— current は既に SAFETY ゆえ「強める方向にしか書き換えない」= 記録付き no-op（状態不変）。(3) heartbeat 復帰・ISL が HOLD を解除 → (i)(i′) が外れ (iii) へ。(iii) は「当該 SAFEHOLD 遷移の記録 disposition」= 手順 (1) の HOLD → **SafeHold**。R_SAFETY_LAYER_LOST の disposition SAFE_STOP は clearance record 無しに緩んだ。:241 の「heartbeat が戻り (i′) が外れても SafeHold に緩まない」に反する。
- 再現 (c) gateway が disposition を知る手段が無い: (iii) の判定に「記録 disposition」が要るが、`AuthorityState` にも gateway 状態にも無く、AuditRecorder の読みが必要。評価不能時の既定（SafeStop か SafeHold か）が未記述。
- 再現 (d) INV-31 :712「`safehold_reason == GATEWAY_RESTART` を観測するまで (ii) を評価しない」 vs §5.2 :499「（または seq > 起動時観測 seq）」。SAFEHOLD(SAFETY) 中の gateway 再起動は no-op（reason 不変・seq 不変）ゆえ、INV-31 字義では `restart_pending` が永久に立つ（§5.2 の seq 条項でも次の CAS までは立つ）。
- 最小修正: `AuthorityState` に `safehold_disposition: RuntimeDisposition | None`（runtime 層・凍結外）を追加し、TRANSFER_TO_SAFEHOLD が既 SAFEHOLD に当たったときの規則を「reason = tier 順で max（同 tier は現 reason 保持 + 要求 clearance の**和**を CAS_ATTEMPT に記録）、disposition = max(SAFE_STOP > HOLD)」と定義（no-op でも disposition は昇格）。(iii) は線形化読みの `safehold_disposition` で SafeStop/SafeHold を決め、audit を読まない。:282 の to 列を :301 と同一条件に。INV-31 に seq 条項を追記。T-28 に「同 reason 再遷移で disposition が SAFE_STOP へ昇格し clearance まで保持」を追加。frozen schema delta 0。

### R1-04（MEDIUM）— 05 §5.4 の clearance 定義は SAFETY にも role を要求するが、06 は SAFETY の role を必須にしない → SAFETY hold が解除不能になり得る

- 候補: 05 §5.4 :516「CAS 条件 10 の「該当する clearance record」= `record.safehold_reason == current.safehold_reason ∧ record.role ∈ profile.escalation.clearance_roles[reason] ∧ …`。SAFETY 行は加えて IndependentSafetyLayer の clearance record（AND）。profile が当該 reason の role を 1 つも宣言しない場合は登録時に doc 06 `P_CLEARANCE_ROLE_MISSING`（定義なし = 解除不能 HOLD にも「任意の record で解除」にもしない）」／ :518（条件 10 の集合 = {SAFETY, SAFE_STOP, CHECKPOINT_DISABLED, MANAGER_RESTART, GATEWAY_RESTART}）／ 06 :190「{SAFE_STOP, CHECKPOINT_DISABLED, MANAGER_RESTART, GATEWAY_RESTART} の各 reason に ≥ 1 role が必須」／ 06 :261 同（SAFETY は「ISL clearance record が主・role は AND」）。
- 凍結: D0 :320（`stabilized_post_action_state` は「§D が resumption eligibility を判断できるようにする」— 解除経路の存在が前提）。
- 再現: `clearance_roles = ((SAFE_STOP, op), (CHECKPOINT_DISABLED, op), (MANAGER_RESTART, op), (GATEWAY_RESTART, op))` の profile → 06 §3 :261 PASS・登録。ISL STOP → SAFEHOLD(SAFETY)。ISL が clearance record を発行しても、05 :516 の role 条項 `record.role ∈ clearance_roles[SAFETY] = ∅` は充足不能 → 条件 10 永久 FALSE → TRANSFER_TO_EXECUTOR 不能。05 :516 自身が禁じた「定義なし = 解除不能 HOLD」が発生。
- 最小修正: 06 :190 / :261 / PT-22 の必須集合に SAFETY を追加（= 05 :518 の集合と同一定義にする — A2-01/B2-07 と同じ型の同期）。代替（05 側）: SAFETY 行のみ「`clearance_roles[SAFETY] ≠ ∅` のとき role 条項を適用」。前者を推奨（fail-closed・集合の一元化）。schema delta 0。

### R1-05（HIGH）— CD-04 fold が `ZoneRef.geometry_sha256` を対象に含めず、keep-out 幾何の登録後差し替えが検出されない

- 候補: 05 §3.4 (j) :184「profile が参照する外部内容（`predicate_refs` / `evaluator_ref` / `tcp_offset_ref` の解決先）の content sha256 が profile の宣言と一致」／ 05 §4.3 :437（SAFETY_RESTRICTION のみ「gateway は評価前に predicate の解決内容 sha256 を再検証」）／ :435（DEPLOYMENT_WORKSPACE 行に再検証なし）／ 06 :106 `geometry_sha256: str  # zone 幾何（外部 artifact）の content hash`／ 06 §3 :271 `P_PREDICATE_HASH_MISMATCH` = `predicate_refs` / `evaluator_ref` のみ／ 06 :280 (xi)（zone に対する防御 = `P_WORKSPACE_EMPTY` のみ = 構造検査）／ PT-25 :403（predicate の差し替え試験は在るが zone の試験は無い）。`grep -n geometry_sha256 05 06` = 06:106 の 1 hit のみ。
- 凍結: B:255（凍結側は content-addressed resolver を要求していない — ゆえに宣言 hash は解決時に**検証しなければ**束縛にならない。CD-04 が predicate に要求したのと同じ根拠）、B:23-24（content-addressing 規律）、D0 :207（safety predicate permits）。
- 反例: PT-25 と同型。登録時に keep-out zone Z（geometry_sha256 = h）で P_* = 0。その後 resolver 側で h に対応する artifact を空多角形に差し替える（profile 不変・profile_hash 不変・`accepted_profiles` に留まる）。第 2 評価点 (j) は zone を検査しない → permit 発行。gateway の DEPLOYMENT_WORKSPACE 評価は差し替え後の幾何を使う → keep-out 消失。predicate と異なり CalibrationRef（`artifact_sha256` は (j) の tcp_offset_ref 経由で束縛）・GatewayConfigRef（GATEWAY_CONFIG_MATCH health check :260 で束縛）は覆われるが、zone だけが無防備。
- 影響: 静的 safety envelope（DEPLOYMENT_WORKSPACE）が admission で無効化される。CD-04 の verifier severity（HIGH）と同じ攻撃面・同じ帰結ゆえ HIGH。代替読み（「content hash と宣言した以上 resolver が検証する」）は B:255 と本 doc の fail-closed 規律（明示規則のみ）に照らして採らない。
- 最小修正: (j) に `zones[].geometry_sha256` を追加／ 06 :271 の検査対象に `zones[].geometry_sha256` を追加（code は `P_PREDICATE_HASH_MISMATCH` を流用可）／ 05 :435 DEPLOYMENT_WORKSPACE 行に「gateway は評価前に geometry の解決内容 sha256 を `geometry_sha256` と再検証し、不一致 = 評価不能 = FALSE」を追記／ PT に zone 差し替え試験を 1 行追加。schema delta 0（field は既存）。

### R1-06（MEDIUM）— CD-18 の「ZERO_ORDER_HOLD のとき点」評価は DIFF_IK_EE_TARGET の連続運動と整合せず、keep-out を横断する setpoint 対が admit される

- 候補: 05 §4.3 :435「評価対象 = hold が LINEAR_INTERPOLATE のとき直前 admitted setpoint → 新 setpoint の線分（TCP 点・FK 後）、ZERO_ORDER_HOLD のとき点」／ 06 §4 :295 同文／ 05 §4.2 :425（hold = admitted 間の setpoint 保持規約）。
- 凍結: A:62 `ControlMode.DIFF_IK_EE_TARGET`（EE target を IK で追従 = target 間を連続に移動）、B:127 `hold` = 「policy step 間の保持規約」（setpoint 列の補間規約であって、物理軌道が点に留まる意味ではない）。
- 反例: keep-out Z。setpoint p1, p2 ∈ Z^c、線分 p1p2 ∩ Z ≠ ∅。hold = ZERO_ORDER_HOLD。両 command は点評価で admit（:435）。DIFF_IK は p1 から p2 へ EE を連続移動 → TCP が Z を通過。LINEAR_INTERPOLATE なら同じ p1,p2 が拒否されるので、hold 種別で安全判定が変わる根拠が無い。
- 影響: DEPLOYMENT_WORKSPACE 項の admission が hold = ZOH の lease で無効化される（ISL の監視は残るが、本 doc が「静的 restriction = admission 項」と置いた設計目的に反する）。
- 最小修正: :435 / 06 :295 を「DIFF_IK_EE_TARGET では hold 種別にかかわらず（直前 admitted setpoint → 新 setpoint）の線分（最初の command は計測 TCP → setpoint）を評価。hold は時間 parameterization のみを変える」に。schema delta 0。

### R1-07（LOW）— CAS 条件 9・10 の REJECTED と、条件 3 の ACK 失効に fault code が無い

- 候補: 05 §3.5 :230（条件 9: ISL health / 未処理決定）・:231（条件 10: clearance record）・:224（条件 3 `now ≤ ack.valid_until`）／ `AuthorityCas.fault: RuntimeFaultCode | None`（:206）／ §7 規則 2 :626「fault は常に disposition を伴う」／ 失敗表 :286-328（条件 9/10 に対応する行なし — `sed -n 286,328p | grep -i clearance` = 0 hit）／ :290 R_ACK_TIMEOUT（Orchestrator 側の未着）・:291 R_ACK_MISBOUND（束縛不一致）— 失効は列挙外／ 06 :258（`ack_validity_s` と `permit_ttl_s` の順序なし ⇒ ACK が permit より先に失効し得る）。
- 凍結: A:382（拒否は manager の責務 — code 付きで記録されるべき事象）。
- 再現: SAFEHOLD(SAFE_STOP) から clearance 無しに CAS を試みる → 条件 10 FALSE → REJECTED。`fault` に入れる member が enum（:607-620）に無い（INV-27 は code → 行の方向しか検査しない）。
- 最小修正: 条件 9 → `R_SAFETY_OVERRIDE`（未処理決定）/ `R_SAFETY_LAYER_LOST`（heartbeat）、条件 10 → `R_PERMIT_MISBOUND`（CAS_ATTEMPT payload に欠落 clearance を記録）、条件 3 の失効 → `R_ACK_MISBOUND`（定義を「束縛不一致 or 失効」へ拡張）。各々 §3.7 失敗表に行を追加（INV-27 維持）。schema delta 0。

### R1-08（LOW）— §7 表の「INVALID_STATE 解消せず → SAFE_STOP」が §3.7 の表外遷移

- 候補: 05 §7 :638「Terminal INVALID_STATE | 解消せず | SAFE_STOP（TRANSFER_TO_SAFEHOLD(SAFE_STOP)・原因 fault は別途記録）」／ §3.7 :265「表外遷移は存在しない」／ :275（S_BOUNDARY_WAIT からの唯一の SAFEHOLD 遷移 = NO_CHAIN | SAFE_STOP per `on_no_chain`）／ §6.2 B5 :546（尽きれば NO_CHAIN → on_no_chain）。
- 凍結: A:151 `TerminationClass.INVALID_STATE`。
- 再現: INVALID_STATE → RE_OBSERVE 反復 → `boundary_dwell_s` 超過。§6.2 B5 は NO_CHAIN → `on_no_chain`（既定 HOLD）、§7 は無条件 SAFE_STOP。同一事象で 2 記録が異なる disposition。「原因 fault」の code も未指定。
- 最小修正: §7 :638 を「NO_CHAIN → HOLD | SAFE_STOP per `on_no_chain`」に揃えるか、§3.7 に行「S_BOUNDARY_WAIT | INVALID_STATE かつ RE_OBSERVE 尽きる | TRANSFER_TO_SAFEHOLD(SAFE_STOP) | FAULT(R_BOUNDARY_DWELL_EXCEEDED)」を追加。schema delta 0。

### R1-09（LOW）— ISL STOP/HOLD 遷移行に DISPOSITION record が無い（(iii) が依存する記録）

- 候補: 05 §3.7 :278「any | IndependentSafetyLayer STOP / HOLD | TRANSFER_TO_SAFEHOLD(SAFETY) | … | SAFETY_OVERRIDE, PERMIT_VOIDED(全), FAULT(R_SAFETY_OVERRIDE)」（DISPOSITION なし）／ 失敗表 :302「HOLD（STOP なら SAFE_STOP）」／ §5.2 (iii) :493（記録 disposition に依存）／ INV-17 :698・T-17 :736（行が要求する record kind に基づく）。
- 凍結: D0 :317（SafetyDecision.action ∈ {STOP, HOLD, RETRACT, FORCE_LIMIT}）。
- 再現: ISL STOP。:278 に従うと DISPOSITION record が書かれない → (iii) の「当該遷移の記録 disposition が SAFE_STOP」が判定不能 → SafeStop/SafeHold の選択が未定義。
- 最小修正: :278 の記録列に `DISPOSITION(HOLD | SAFE_STOP by decision.action)` を追加。schema delta 0。

### R1-10（LOW）— INV-17 / T-17 の record 集合が行と記録義務で不一致

- 候補: 05 :269（S_SAFEHOLD 起点でも「LEASE_INVALIDATED(旧)」を要求 — 旧 lease が無いので「ちょうど 1 件」:698 が充足不能；PERMIT_CONSUMED / CAS_ATTEMPT が行に無い）／ :673（記録義務は全 permit 消費・CAS・COMMAND_ADMITTED を要求し「欠落 = INV-17 違反」）／ INV-17 :698 は「§3.7 の各事象」に scope。
- 凍結: A:524-529（SchemaVersionStamp）— 直接の矛盾なし。欠落型。
- 最小修正: :269 を「EPOCH_TRANSITION, LEASE_ACTIVATED, PERMIT_CONSUMED, CAS_ATTEMPT, LEASE_INVALIDATED(旧・旧 lease がある場合のみ)」に；INV-17 の scope を「§3.7 の各事象 + §8 記録義務の各事象」に。schema delta 0。

### R1-11（LOW）— `S_TRANSFER_PENDING` の定義が到達不能な合成を含む・§2 表に `pending_invalidate` が無い

- 候補: 05 :263「`S_TRANSFER_PENDING` | 上記いずれか + UNUSED permit ≥ 1」— §3.4 (d) :184 / INV-28 :709 により S_LEASE_ACTIVE / S_HEALTH_PENDING では permit が発行されない（v0.2.2 以降）ため、その合成は到達不能。§2 :82 の gateway 状態列 = 「線形化読み + 直近 admitted command + restart_pending」— v0.2.3 で追加された `pending_invalidate`（:492）が無い。
- 最小修正: :263 を「`S_SAFEHOLD` ∨ `S_BOUNDARY_WAIT` + UNUSED permit ≥ 1」に；:82 に `pending_invalidate` を追加。schema delta 0。

### R1-12（LOW）— gateway admission が `cmd.executor_id == lease.executor_id` を検査しない

- 候補: 05 `GatewayCommand.executor_id`（:479）／ admission 述語 :82（`(lease_id, control_epoch)` 等値 ∧ ¬boundary_wait ∧ envelope ∧ kind ∧ cmd_seq）／ INV-03 :684（lease_id / control_epoch / cmd_seq のみ）。
- 凍結: A:382（旧 epoch command 拒否の要求仕様 — executor 同一性は言及なし = 欠落）。
- 再現: lease_id・control_epoch・次 cmd_seq を知る第三者 process が command を投入 → admit。`executor_id` を運ぶ型を定義しながら検査しないのは INV-03 の趣旨（線形化点での束縛）と不整合。
- 最小修正: admission と INV-03 に `cmd.executor_id == lease.executor_id` を追加（違反 = `R_LEASE_UNKNOWN_COMMAND`）。schema delta 0。

### R1-13（LOW）— §6.2 B0 の snapshot が manager の MARK_BOUNDARY_WAIT に先行し得る race に規則が無い

- 候補: 05 §6.2 B0 :534「snapshot := AuthorityManager.read_snapshot()」／ §3.5 :243（MARK_BOUNDARY_WAIT は manager が受領の事実で実行）／ §3.4 (d) :184（permit は `expected.boundary_wait == True` を要求）／ 失敗表 :323（R_MIDSKILL_TRANSFER_BLOCKED の disposition = 「変化なし（audit・lease 継続）」— 再試行規則なし）。
- 再現: Orchestrator と manager が同じ SkillOutcome を受ける。Orchestrator が B0 で snapshot（bw = False）→ B4 → ACK → permit 要求 → (d) 違反 → R_MIDSKILL_TRANSFER_BLOCKED → Orchestrator に次の指示が無い（`RESELECT` でも再読でもない）。
- 最小修正: B0 に「producer あり（offer あり）の boundary では `snapshot.boundary_wait == True` を要求；False なら `boundary_dwell_s` 内で再読」を追記。schema delta 0。

### R1-14（LOW）— executor 発 `InterruptOutcome(SAFETY_STABILIZED)` の SAFETY hold の clearance が ISL 決定不在時に未定義

- 候補: 05 §3.7 :274 / §6.1 :527（SAFETY_STABILIZED → TRANSFER_TO_SAFEHOLD(SAFETY)・fault なし）／ §5.4 :510（SAFETY 行の追加前提 = ISL clearance record（`stabilized_post_action_state` の参照を含む））。
- 凍結: A:151 `InterruptReason.SAFETY_STABILIZED`（executor 側 outcome）／ D0 :320（`stabilized_post_action_state` = monitor が**自身の** safety action 後に宣言する状態）。
- 再現: executor が ISL 決定なしに自発的に SAFETY_STABILIZED を出す（frozen 型はこれを許す）→ SAFEHOLD(SAFETY)。ISL は action を取っていないので :320 の状態を宣言する根拠が無い → :510 の record が発行され得ず、解除経路が未定義。
- 最小修正: §5.4 SAFETY 行に「ISL 決定が無い SAFETY_STABILIZED では、ISL clearance record は executor 報告の stabilized state を ISL が確認して発行する（無ければ CHECKPOINT_DISABLED と同じ operator clearance + 再観測）」を追記。schema delta 0。

### R1-15（LOW）— lease 無効化 trigger の scope 語と INV-33 の from-state 集合の不一致

- 候補: 06 §8.1 (3) :373「lease を無効化するのは health check 失敗（§5 (a)）と `lease_max_duration_s` 超過（CD-10）のみ」 vs 05 §4.5 :458（12 trigger）／ 05 INV-33 :714「全 lease の活性時間は `lease_max_duration_s` 以下」 vs 遷移行 :281 の from = `S_LEASE_ACTIVE / S_HEALTH_PENDING`（S_BOUNDARY_WAIT を含まない；lease は活性のまま :243/:503）。
- 再現: activated_at = t0、outcome を t0 + lease_max_duration_s − ε に受領 → S_BOUNDARY_WAIT で最大 `boundary_dwell_s`（06 :258 `lease_max_duration_s ≥ boundary_dwell_s` は逆向きの制約）→ INV-33 違反が遷移なしに継続。
- 最小修正: 06 :373 を「**profile 起因で** lease を無効化するのは…のみ」に；05 :281 の from に `S_BOUNDARY_WAIT` を追加（または INV-33 を「S_BOUNDARY_WAIT 到達まで」に scope）。schema delta 0。

### R1-16（LOW）— `pending_invalidate` の clear 条件が lease-scoped CAS 破棄の場合を覆わない

- 候補: 05 §5.2 :492「`pending_invalidate` = gateway が TRANSFER_TO_SAFEHOLD の trigger を検出してから**再試行 CAS の SUCCESS** を観測するまで True」／ §3.5 :241「不一致なら CAS を捨て元 lease への FAULT のみ記録」（lease-scoped 理由の再試行規則）／ HealthConfirmation `first_command_admitted`（:214）／ :271（HEALTH_FAIL）。
- 再現: L1 の最終 command で gateway が ENVELOPE_VIOLATION を検出 → `pending_invalidate := True`・TRANSFER_TO_SAFEHOLD(ENVELOPE_VIOLATION) 試行。同時に outcome → MARK_BOUNDARY_WAIT が先に成功 → CONFLICT → 再読（active = L1）→ 再試行の前に TRANSFER_TO_EXECUTOR(L2) が成功（bw = True ゆえ可）→ 再読で active = L2 ≠ L1 → CAS 破棄。gateway は「再試行 CAS の SUCCESS」を観測しない → `pending_invalidate` が立ったまま → L2 の (ii) が永久に評価されず → `first_command_admitted = False` → HEALTH_FAIL → 以後の全 lease も同様（flag は gateway 局所・clear 条件未充足）。
- 最小修正: clear 条件を「trigger の lease が活性でなくなったことを線形化読みで観測（`state.active_lease_id ≠ trigger.lease_id ∨ state.owner.kind == SAFEHOLD`）」に置換。schema delta 0。

## 5. 反証しなかった点・限界（honest scope）

- 本レビューは AI（別 context）による準備レビュー。08 §6 のとおり two-key・Rs 裁定を代替しない。
- 各 finding の severity は本 reviewer の判定。R1-05 は「宣言 hash の検証規則欠落」を CD-04 と同 class と見て HIGH としたが、verifier が「hash 宣言済 = 検証は含意」と読めば MEDIUM に下がり得る（その場合 verdict = PASS-WITH-CONDITIONS）。判断材料 = B:255（凍結側は content-addressed resolver を要求しない）。
- 陽性対照の注入欠陥 8 件（`10_*` §1）は実文に無いことを §0 冒頭の行で確認し、報告していない。
- 凍結物・`eval_runs/` は無改変（本 report と JSON は `review2/` のみ）。

## 6. 出力

- 本 report: `/tmp/claude-0/-home-user-IsaacLab/f88a4426-3d65-5cc2-ba28-87f4976e6a66/scratchpad/review2/R1_review_v023.md`
- findings JSON: `/tmp/claude-0/-home-user-IsaacLab/f88a4426-3d65-5cc2-ba28-87f4976e6a66/scratchpad/review2/R1_findings_v023.json`
- pin 再測原本: `review2/R1_pin_recheck.txt`／ checker 原本: `review2/R1_checker.txt`
