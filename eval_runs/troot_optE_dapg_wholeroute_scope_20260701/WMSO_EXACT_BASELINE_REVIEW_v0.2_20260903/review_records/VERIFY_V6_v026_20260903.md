# V6 — R6 finding 21 件の adversarial verification（WMSO v0.2.6・3 lens）

- verifier: V6（別 context・R6 report は読んだが verdict は本 verifier が `cat -n` / `sed -n` / `grep` で独立に再現）。実施 = 2026-09-05 23:05–23:16 UTC（`date -u` 実測・date-THEN-write。本 file の書出し直前の実測 = 2026-09-05 23:16:44 UTC）。
- 対象 = `$PKG/06_WMSO_INDUSTRIAL_PROFILE_v0.2_REVIEW_CANDIDATE_20260903.md`（v0.2.6・sha256 `e93112ff…` = SHA256SUMS 一致・実測）／ `$PKG/05_WMSO_RUNTIME_SPEC_v0.2_REVIEW_CANDIDATE_20260903.md`（v0.2.6・sha256 `596db986…` = SHA256SUMS 一致・実測）／ `$PKG/check_review_candidate.py`（sha256 `07d818e2…` = SHA256SUMS 一致）。repo HEAD `f9fe9757`（`git log -1 --oneline` 実測）・branch `claude/v0-2-design-candidates-snqbmy`。`$PKG` = `eval_runs/troot_optE_dapg_wholeroute_scope_20260701/WMSO_EXACT_BASELINE_REVIEW_v0.2_20260903`、`$D` = `eval_runs/troot_optE_dapg_wholeroute_scope_20260701`。
- 凍結物・`eval_runs/`・`$PKG` は未編集（実 05/06 の sha256 は作業後も同値 — §0 末尾）。書出しは本 file・`V6_results_v026.json`・複製試験 dir `q2test_v6/` のみ。
- authority 無し: gate PASS / two-key / Rs 裁定を主張しない（§3）。

## 0. 手続記録（date-THEN-write・全て実測）

| # | command | 実測出力（要点） | 時刻（UTC） |
|---|---|---|---|
| 1 | `git log -1 --oneline` / `git branch --show-current` | `f9fe9757 WMSO v0.2.6: fold third review round (R3/R4 -> V3/V4) into 05 and 06` / `claude/v0-2-design-candidates-snqbmy`・working tree clean | 23:05:24 |
| 2 | `bash $PKG/verify_exact_baseline_pins.sh` | **11/11 PASS**（D1.1-A `00192d20…` / EP md `c474acea…` / EP json `e63176af…` / D1.1-B `5a1874d3…` / blob id `13534302…` `ebe8154a…` / frozen commit 54f90a7d・07250f4a / definition hash `e7ca4309…`、`policy_semver=1.9.0`、`HEAD=f9fe9757…`） | 23:05:29 |
| 3 | `python3 $PKG/check_review_candidate.py $PKG/05_… --runtime` | `citations=153 FAIL=0 WARN=11`・exit 0（⚠ 引数は file path。task 指示の `05` 短縮形は `FileNotFoundError` — usage 行 `check_review_candidate.py:4` のとおり path 必須） | 23:05:45 |
| 4 | `python3 $PKG/check_review_candidate.py $PKG/06_… --profile` | `citations=63 FAIL=0 WARN=25`・exit 0（WARN は全て C2 heuristic） | 23:05:45 / rc 再測 23:06 |
| 5 | `sha256sum` 05 / 06 / checker vs `SHA256SUMS.txt` | 05 `596db986236b8db121aeee2573c936e6f7c8ea2845d7b2dbf15c027e3f8f264a` ✓ / 06 `e93112ffd8f347d39c7ae7c440c2970ed1499fd803ef5862a58391c04f4ffa5e` ✓ / checker `07d818e21f99745f97362721097cc5b606e2bd67efac3d9fcd1a9d3505618595` ✓（3/3 一致） | 23:05:45 |
| 6 | 読了 | `R6_findings_v026.json`（list・21 件・R6-01..R6-21）全文／ `R6_review_v026.md` 全文／ `R5_findings_v026.json` 14 件の claim を skim（重複は §1 に注記・merge はしない）／ 06 全 690 行・05 全 1022 行を `cat -n`／ 凍結: RS71 `:18-32`（§0 #1–#5）、D0 `:315-325` / `:350-358`、EP md `:150-160`、contracts_v2 `:350-358` / `:383-387`、`task_config.py:15-30`／ 11_ §8・`rs_consult/A_GPT56sol_20260904.md` 全文／ 10_ §1（注入 8 欠陥）／ 08_ `:15-35` | 23:06–23:13 |
| 7 | R6-13 複製試験（`$OUT/q2test_v6/`・05 / 06 / checker を複製・実物非接触） | baseline 複製 = FAIL 0；**MUT-1**（05 複製の (j) 列挙から `evaluator_sha256` のみ削除・(a) の同語は残す）= **FAIL 0**；**MUT-2**（対照: (j) から `pairwise_keepout_sha256[]` 削除）= **FAIL 1** `Q2 … 'pairwise_keepout_sha256' is not named in 05 §3.4 (j)`；**MUT-3**（(j) から `deployment_evidence_policy_hash` 削除）= **FAIL 0**（06 §2.2 は走査外）。checker 論理の再現: §2.1 抽出 = 8 名（`artifact_sha256`×3 型が 1 名に潰れる）、(j) 行 = 4,140 字の 1 行、`tensor_binding_hash` は (j) 部分文字列には**無く** (g) で通る | 23:14:20–23:14:53 |
| 8 | 作業後の実物 sha 再測 | 05 `596db986…` / 06 `e93112ff…`（不変） | 23:14:53 |

判定規則（task 指示）: finding ごとに text / logic / impact の 3 lens を独立に走らせ**反証を試みる**。再現できなければ refuted（既定）。2 lens 以上が refute せず impact ≠ NOT_A_DEFECT で confirmed。Rs 裁定の**方向**（B+ / 外部 `CellSafetyTimingBaseline` / `ProfileRegistry` + `AcceptanceRecord` / OP-19 三層）は defect でない（impact で refute）、方向の**実現不全**（不整合・非 fail-closed）は defect。陽性対照 8 欠陥（10_ §1: PC-05-A/B/C/D・PC-06-E/F/G/H）は実体に無いことを 06:147 / 06:212-217 / 06:267-269 / 05:192 / 05:243 / 05:472 で再確認 — 以下の finding はいずれもそれらの再演ではない。severity は本 verifier の判定（R6 と異なる場合は理由を書く）。

## 1. 集計

| id | R6 sev | text | logic | impact | verdict | V6 sev |
|---|---|---|---|---|---|---|
| R6-01 | HIGH | not refuted | not refuted | not refuted | **CONFIRMED** | HIGH |
| R6-02 | HIGH | not refuted | (a) refuted / (b) not refuted | not refuted | **CONFIRMED** | MEDIUM（↓: (a) の fail-open 読みは 06:291 の字義で閉じる） |
| R6-03 | MEDIUM | not refuted | not refuted | not refuted | **CONFIRMED** | MEDIUM |
| R6-04 | HIGH | not refuted | not refuted | not refuted | **CONFIRMED** | HIGH |
| R6-05 | MEDIUM | not refuted | not refuted | not refuted（issuer は Rs） | **CONFIRMED** | MEDIUM |
| R6-06 | MEDIUM | not refuted | not refuted | not refuted | **CONFIRMED** | MEDIUM |
| R6-07 | MEDIUM | not refuted | not refuted | not refuted | **CONFIRMED** | MEDIUM |
| R6-08 | MEDIUM | not refuted | 部分 refuted（空集合は (l) で止まる） | not refuted | **CONFIRMED** | MEDIUM（範囲縮小） |
| R6-09 | MEDIUM | not refuted | not refuted | not refuted（fail-closed 側） | **CONFIRMED** | MEDIUM |
| R6-10 | LOW | not refuted | not refuted（範囲縮小） | not refuted | **CONFIRMED** | LOW |
| R6-11 | MEDIUM | not refuted | not refuted | not refuted | **CONFIRMED** | MEDIUM |
| R6-12 | LOW | not refuted | not refuted | not refuted | **CONFIRMED** | LOW |
| R6-13 | LOW | not refuted | not refuted（複製で再現） | not refuted | **CONFIRMED** | LOW |
| R6-14 | LOW | not refuted（部分 refuted） | not refuted | not refuted | **CONFIRMED** | LOW |
| R6-15 | LOW | not refuted | not refuted | not refuted | **CONFIRMED** | LOW |
| R6-16 | LOW | not refuted | not refuted | not refuted | **CONFIRMED** | LOW |
| R6-17 | LOW | not refuted | not refuted | not refuted | **CONFIRMED** | LOW |
| R6-18 | LOW | not refuted | not refuted | not refuted | **CONFIRMED** | LOW |
| R6-19 | LOW | not refuted（(a) のみ） | refuted（(b)(c)） | not refuted（(a)・記述） | **CONFIRMED** | LOW（(a) のみ） |
| R6-20 | LOW | not refuted | not refuted | not refuted | **CONFIRMED** | LOW |
| R6-21 | LOW | not refuted | not refuted | not refuted | **CONFIRMED** | LOW |

集計: confirmed **21** / refuted **0** / merged **0**。severity（V6）: CRITICAL 0 / **HIGH 2**（R6-01, R6-04）/ **MEDIUM 8**（R6-02, R6-03, R6-05, R6-06, R6-07, R6-08, R6-09, R6-11）/ **LOW 11**。R6 との差 = R6-02 HIGH→MEDIUM、R6-08 / R6-19 は範囲縮小、他は同一。

- **R6 内の重複**: 21 件を突合し完全重複 0。近接対（R6-05 ↔ R6-06 = role 資格照合の欠落〔acceptance record / clearance record で対象行が別〕、R6-07 ↔ R6-08 = baseline field の欠落〔許容値 / 診断項目〕、R6-16 ↔ R6-17 ↔ R6-21 = §5 (b) / 05:290 周辺〔SHADOW / FI 行 / ACCEPTANCE fault〕）はいずれも対象 field・規則が異なり別 finding として維持（`merged_into = ""`）。
- **R5 との重なり（注記のみ・merge しない）**: R6-16 ≈ R5-01、R6-11 ⊃ R5-04（第 2 評価点 list の欠落は同一論点）、R6-21 ≈ R5-08、R6-12 ≈ R5-09、R6-03 ⊃ R5-10、R6-17 は R5-14 と同じ表を別角度から見る。独立 reviewer 2 名が同じ行に到達しており、いずれも本 verifier が本文で再現した。
- 全 confirmed の minimal fix は凍結 4 file 非接触（deployment / runtime 層の型・散文のみ）。Rs 専権に触れる要素（role 実名・baseline 値・許容値・issuer）は「open item として記録」に留めた。

## 2. finding 別の検証

### R6-01 — CONFIRMED / HIGH — gripper finger 幾何（RS71 §0 #4 human-LOCK）が profile のどこにも束縛されない

**sed 確認**: 06:123-128 `ToolPayloadSpec` = `tool_id`（「tool の個体 serial（機種 id ではない）— TOOL_IDENTITY の照合先」）/ `tool_mass_kg` / `tool_com_m` / `payload_mass_max_kg` / `tcp_offset_ref` の 5 field のみ／ 06:68「TOOL_IDENTITY = tool が報告する個体 serial と `tool_payload.tool_id` … の等値」／ 06:359「TOOL_PAYLOAD_IDENTIFICATION = arms[].resource_id#tool_payload.tool_id（arm ごと）」／ 06:13「cell・controller・tool・payload・… の宣言」／ 06:547 OPP-14 = collision hull の workspace 評価のみ。`grep -in "finger\|koshape\|V-groove\|GRIPPER GEOMETRY"` 06 / 05 = **0 hit**（`gripper` の hit は資源 key `gripper_left/right` のみ・06:39/95/96/168/301-303）。frozen: RS71 `:26` #4「GRIPPER GEOMETRY LOCKED — the active finger DESIGN is コ-shape … human-LOCKED … V-groove … DISCARDED … authoritative committed asset = `_ur15_2f85_koshape_actuated.xml`（16.00 mm opening）」。比較: 同じ §0 の #1 は `P_ARM_CAPABILITY_MISSING`（06:302）、#2 は `P_BASE_LAYOUT_MISMATCH`（06:311）+ `P_ENVELOPE_EMPTY`（06:310）、#3 は `control_mode` 等値（06:270）、型式は `P_ROBOT_MODEL_MISMATCH`（06:314）で **RS71 §0 を名指しして pin** している。

- **text**: not refuted。tool の同一性は個体 serial のみで、finger 設計 / 幾何を宣言する field も、それを照合する `P_*` も、TPI evidence の subject にも無い。RS71 §0 の 5 前提のうち #4 だけが 06 に束縛点を持たない（他 3 つは pin あり、#5 は profile の対象外）。
- **logic**: not refuted。Robotiq 2F-85 の fingertip は交換部品ゆえ、本体 serial を保ったまま finger を別形状（DISCARDED の V-groove 等）に替えた cell を構成できる。TOOL_IDENTITY（serial 等値）pass、TPI record は「存在・束縛」のみ要求（06:403・OPP-5）で幾何を要求しない、他の `P_*` は finger に依存しない → §3 の全 `P_*` = 0・全 health check pass・全 evidence 存在 → CLOSED_LOOP permit の profile 側条件を全て満たす。反証として「`tool_mass_kg` / `tool_com_m` が違う」を試みたが、06:318「物理事実（… tool 質量）は全て evidence に委ねる」で契約層は照合しないため反証にならない。
- **impact**: not refuted。RS71 §0 は「変更 = PREMISE CHANGE = STOP」を要求する不変前提であり、06 は #1/#2/型式を機械 pin する構造を既に持つ（同一 pattern の欠落 = 非対称）。Rs 裁定 OPP-15 ④「gripper geometry LOCK にも変更を加えません」は**変更しない**宣言であって束縛ではない。学習 policy（コ-shape で訓練）が LOCK 外の幾何で走る経路が profile 側で閉じられていないのは fail-closed bar 未達。裁定の方向への異議ではない。
- **verdict / severity**: CONFIRMED / **HIGH**（task 基準「再現可能 ∧ RS71 §0 不変前提の束縛を欠く」を満たす。R4-14〔物理変更の検出・MEDIUM〕の同族だが、対象が §0 不変前提そのものである点で上）。
- **minimal fix（凍結 delta 0）**: (1) `ToolPayloadSpec` に deployment 層 field `finger_design_ref: str`（RS71 §0 #4 の設計 id）+ `finger_geometry_sha256: str`（RS71 #4 の authoritative asset の content hash。**値は本 doc に書かない** — `P_BASE_LAYOUT_MISMATCH` / `P_ROBOT_MODEL_MISMATCH` と同 pattern・照合先 = RS71 `:26` / §0-A）を追加。(2) §3 に `P_TOOL_GEOMETRY_MISMATCH`（第 1 評価点・`finger_geometry_sha256 ≠ RS71 #4 pin` または `finger_design_ref` 未解決 = 違反）を追加し §8.1 code list へ。(3) 06:308 の hash 列挙と 05:191 (j) に `arms[].tool_payload.finger_geometry_sha256` を追加（checker Q2 が自動で要求する）。(4) 06:359 TPI の artifact に「`finger_design_ref` の attest を含む」（存在・束縛のみ。開口 16.00 mm 等の実測・許容は commissioning content = OPP-5 / RT0 へ）。(5) PT 行「同 serial・別 finger の cell」→ `P_TOOL_GEOMETRY_MISMATCH`。R6 案の「TOOL_IDENTITY = serial ∧ finger 設計 id」は finger が id を報告しない前提では機械検査にならないため採らない。pin の値の所在（RS71 §0-A の asset custody）は Rs 側 SSOT を参照するのみ。

### R6-02 — CONFIRMED / MEDIUM（R6 = HIGH）— `P_FAULT_EVIDENCE_UNVERIFIED` が NC# subject を評価できず、health 分岐と 06:291 / 06:391 が整合しない

**sed 確認**: 06:297「FAULT_INJECTION_RESULT / HEALTH_CHECK_RUN の `artifact_ref` が解決する audit 範囲に、`kind = FAULT ∧ fault == subject_ref の code ∧ profile_hash 一致`（fault）／ `kind ∈ {HEALTH_CONFIRMED, HEALTH_FAILED} ∧ payload に check_id`（health）の `RuntimeAuditRecord` が ≥ 1 件（第 2 評価点）」／ 06:291「mandatory kind の各 `check_id` について、誘発した不一致で `R_HEALTHCHECK_FAILED` / HEALTH_FAILED を出した negative-control の HEALTH_CHECK_RUN record（artifact_ref → **当該 record を含む audit 範囲**）が CLOSED_LOOP lease 前に存在 | `P_EVIDENCE_UNBOUND`（subject = check_id#NEGATIVE）」／ 06:294 `P_EVIDENCE_UNBOUND` の一般定義 = 「(kind × subject) 単位で … 存在」／ 06:359 subject 文法: HEALTH_CHECK_RUN = check_id、FAULT_INJECTION_RESULT = fault code（timeout 駆動は '<code>#<trigger>'）— 'NC#<name>' / '#NEGATIVE' 無し／ 06:378 `negative_controls`「各 control に FAULT_INJECTION_RESULT（subject_ref = 'NC#<name>'）」／ 06:391（```python block 内の `#` 行・block = 06:349-396）「P_FAULT_EVIDENCE_UNVERIFIED は下の期待 audit 内容で照合」／ 06:392 期待 audit 表／ 06:422 (1)「評価不能 = 違反」。

- **text**: not refuted。(b) 06:297 の fault 分岐は「subject_ref の code」を要するが 'NC#ARM_SWAP' に code は無く、06:391 の comment だけが別規則を主張する（規範行と comment の矛盾）。06:359 の subject 文法は 'NC#<name>' も '<check_id>#NEGATIVE' も定義せず、R4-03 fold（V4 minimal fix (2)(3)）が未完。(a) 06:297 の health 分岐は '#NEGATIVE' subject でも CONFIRMED を受理する — ただし後述のとおり 06:291 が別途内容を要求しており、同一 code に対して 2 行が異なる強さを持つ「重複定義」が text 上の欠陥。
- **logic**: (a) **refuted**。06:291 は字義で「HEALTH_FAILED を出した record（artifact_ref → 当該 record を含む audit 範囲）」を要求するため、constant-True evaluator の `hc1#NEGATIVE` record（範囲に HEALTH_CONFIRMED のみ）は 06:291 を満たさず `P_EVIDENCE_UNBOUND` で止まる — fail-open 読みは 06 全体からは再現できない（06:294 / 06:422 の「存在」framing だけを実装した validator は通すが、06:291 の明文を無視する読み）。(b) **not refuted**。NC# record は kind = FAULT_INJECTION_RESULT ゆえ 06:297 の fault 分岐に入り、「subject_ref の code」が評価不能 → 規則 (1) で違反 → MIN_NEGATIVE_CONTROLS は必須（06:316）ゆえ **全 profile で第 2 評価点の `P_FAULT_EVIDENCE_UNVERIFIED` が立ち CLOSED_LOOP 不成立**（fail-closed 側の経路死）。comment 06:391 に従う実装なら動くが、06:382 が「comment ではなく本文の規則」と宣言したのは MIN 集合行であり 06:391-394 は依然 `#` 行 — 規範性が二義。
- **impact**: not refuted。R4-03（HIGH・v0.2.6 fold）の negative-control 層が、結線（field / `P_EVIDENCE_UNBOUND`）は入ったが**内容述語**は未実現 = fold 不完全。ただし fail-open 読みは 06:291 で閉じ、残る欠陥は「経路死 or comment 依存」の二義性ゆえ、task 基準「fail-closed 保証を破る」には達しない。
- **verdict / severity**: CONFIRMED / **MEDIUM**（R6 の HIGH から下げ: (a) が 06:291 で反証され、(b) は保守側）。
- **minimal fix（凍結 delta 0）**: (1) 06:297 を subject 文法別の期待内容表に書き換える — `<check_id>` → HEALTH_CONFIRMED(check_id)；`<check_id>#NEGATIVE` → HEALTH_FAILED(check_id) ∧ FAULT(R_HEALTHCHECK_FAILED)；`<code>` → FAULT(fault == code)；`<code>#<trigger>` → FAULT(fault == code ∧ payload.trigger == trigger)（R6-03 と同時）；`NC#<name>` → 06:392 の写像（表を `#` comment から本文へ昇格）；それ以外 = FALSE。(2) 06:359 の subject 文法に 'NC#<name>'（FAULT_INJECTION_RESULT）と '<check_id>#NEGATIVE'（HEALTH_CHECK_RUN）を追記。(3) 06:291 の内容要求は 06:297 へ一本化し、06:291 は存在（subject = check_id#NEGATIVE）のみに整理（同一 code の二重定義を解消）。R6 案の「BASE_POSE_VERIFY の #NEGATIVE 誘発手順の明記」は commissioning 手順（RT0 / cell court）であり最小修正から外す。

### R6-03 — CONFIRMED / MEDIUM — MIN_FAULT_INJECTION の '#DEADLINE' / '#EVENT' member と '<code>#<trigger>' subject が `FaultInjectionRequirement` で表現不能

**sed 確認**: 06:387 MIN_FAULT_INJECTION ∪ {`R_CALIBRATION_EXPIRED_ACTIVE#DEADLINE`, `R_EVIDENCE_EXPIRED_ACTIVE#DEADLINE`, `R_PROFILE_SUSPENDED#EVENT`, …}（v0.2.6 R4-13）／ 06:389「timeout 駆動 code の FAULT_INJECTION_RESULT は subject_ref = '<code>#<timeout_trigger>' … 05 §8 FAULT payload の trigger 副因と照合」／ 06:367-369 `FaultInjectionRequirement` = `runtime_fault_code`（「05 enum に無い名 = P_FAULT_CODE_UNKNOWN」）+ `must_be_exercised_before` のみ／ 06:298 `P_FAULT_CODE_UNKNOWN` = `runtime_fault_code ∉ 05 RuntimeFaultCode`／ 06:294 `P_EVIDENCE_UNBOUND`: 「FAULT_INJECTION_RESULT ∀ fault_injection[] … `subject_ref == runtime_fault_code`」／ 06:316 `P_EVIDENCE_POLICY_TOO_WEAK` = MIN_FAULT_INJECTION を含まない／ 05:632-648 `RuntimeFaultCode` に '#' 付き member 無し／ 05:700 trigger 副因の payload 義務 = `R_SAFETY_LAYER_LOST`: HEARTBEAT_TIMEOUT | HEALTH_FAILED、`R_BOUNDARY_DWELL_EXCEEDED`: DWELL_TIMEOUT | ATTEMPTS_EXHAUSTED の 2 code のみ（`grep DEADLINE\|EVENT` に副因語彙なし）。

- **text**: not refuted。MIN 集合の member は '#'-付き文字列だが、policy が持てるのは enum 名のみ（06:368）。`subject_ref == runtime_fault_code`（06:294）と subject '<code>#<trigger>'（06:389・06:287 の `R_SAFETY_LAYER_LOST#HEARTBEAT_TIMEOUT`）は両立しない — この後者の矛盾は v0.2.4（R2-01）から潜在し、v0.2.6 で MIN 集合に '#' member を足したことで必須集合が充足不能になった。
- **logic**: not refuted。`runtime_fault_code = "R_PROFILE_SUSPENDED#EVENT"` → `P_FAULT_CODE_UNKNOWN`；`"R_PROFILE_SUSPENDED"` → ⊇ MIN の文字列比較で `P_EVIDENCE_POLICY_TOO_WEAK`、かつ `P_EVIDENCE_UNBOUND` は subject `R_PROFILE_SUSPENDED` を要求し 06:389 の subject と不一致。DEADLINE / EVENT は 05:700 に副因語彙が無く「照合」不能。いずれの読みでも CLOSED_LOOP 不成立（fail-closed 側の経路死）。
- **impact**: not refuted。R4-13 fold（MEDIUM）の実現不全。方向は保守側だが、OPP-11 裁定 ③「timeout 駆動 fault の commissioning 発火」を機械検査で執行する仕組みが構造的に動かない。
- **verdict / severity**: CONFIRMED / **MEDIUM**。
- **minimal fix（凍結 delta 0）**: (1) `FaultInjectionRequirement` に `trigger: str | None` を追加（deployment 層）。(2) `P_FAULT_CODE_UNKNOWN` = `runtime_fault_code ∉ RuntimeFaultCode ∨ (trigger ≠ None ∧ trigger ∉ 05 §8 の当該 code の副因語彙)`。(3) `P_EVIDENCE_UNBOUND` / 06:389 / 06:287 の subject = `code` if trigger None else `code#trigger`；MIN 集合との ⊇ 比較も同形。(4) 05:700 の payload 義務を `R_CALIBRATION_EXPIRED_ACTIVE` / `R_EVIDENCE_EXPIRED_ACTIVE`: DEADLINE、`R_PROFILE_SUSPENDED`: EVENT へ拡張し、code → 副因語彙の閉じた表を §8 に置く（既存 HEARTBEAT_TIMEOUT / HEALTH_FAILED / DWELL_TIMEOUT / ATTEMPTS_EXHAUSTED を含む）。R6 案と実質同一（語彙表を「MIN 集合が使う code に限る」と明示する点のみ絞る）。

### R6-04 — CONFIRMED / HIGH — one-key の SUSPENDED / REVOKED record が `P_ACCEPTANCE_RECORD_INVALID` の無限定条件に落ち、失効 event が出ない

**sed 確認**: 06:513「検査 `P_ACCEPTANCE_RECORD_INVALID`（record 書込時 + …）: 必須 field 欠落；**generation が同一 profile_hash の直前 record + 1 でない**；**CLOSED_LOOP ∈ allowed_lease_modes ⇒ ∃ SAFETY_APPROVER ∧ ∃ CUSTODY_APPROVER** …；SHADOW のみ ⇒ ∃ COMMISSIONING ∧ ∃ INDEPENDENT_REVIEWER で相異；…；SUSPENDED で `suspension_reason` None」（`state` による限定なし・`grep "state ∈"` = 0 hit）／ 06:514「新しい ACCEPTED record は必ず generation +1；… SUSPENDED / REVOKED record は superseded record の **hash field を複写**し、approvals = 認可された一者または MONITOR、valid_until = superseded 値」（`allowed_lease_modes` / `generation` の扱い無し・`grep "allowed_lease_modes = "` = 0 hit）／ 06:481「generation … 同一 profile_hash の受理世代（再受理ごとに +1）」／ 06:507「SUSPENDED → ACCEPTED は新 generation の新 record」／ 06:509「認可された一者または自動 monitor が `SUSPENDED(reason)` record を append する（記録が先・通知が後）」／ 05:291「registry に `SUSPENDED(reason)` record を append した**後**の通知 … 記録なしの event は無い」／ 05:292 head-check = `record_hash ≠ … ∨ state ≠ ACCEPTED`（head の state のみ）／ 06:422 (1)。frozen: D0 `:318`「monitor `failed` ⇒ system fails to safe-stop」。裁定: OPP-13 ③「SUSPENDED = one-key fail-safe」・A_GPT56sol:35「認可された一者または自動 monitor が即時 SUSPENDED にできます」。

- **text**: not refuted。two-key 条件と generation +1 条件が state で限定されず「record 書込時 … 全 record」に適用される一方、06:514 は SUSPENDED / REVOKED について hash field の複写しか定めない。`allowed_lease_modes`（hash field ではない）と `generation` は未規定。
- **logic**: not refuted。gen-1 ACCEPTED(allowed = {SHADOW, CLOSED_LOOP}) が head、ISL 異常で monitor が SUSPENDED(ISL_ANOMALY, approvals = ((MONITOR, mon, t),)) を append する場合を全読みで追う: 読み A（allowed 複写・generation 同値）→ two-key 偽 ∧ generation ≠ 直前 + 1 → 拒否。読み B（allowed 複写・generation +1）→ two-key 偽 → 拒否。読み C（allowed 未指定）→ 必須 field 欠落 or 評価不能 → 違反。**反証として読み D**（allowed = ()・generation +1）を構成すると 06:513 は通る — しかし () も +1 も本文に根拠が無く、+1 は 06:481 の定義（generation = 受理世代）と 06:514「新しい **ACCEPTED** record は必ず +1」の含意（他 state は +1 でない）に矛盾する。自動 monitor は裁量を持てないため、決定論的規則が無いこと自体が欠陥。拒否された場合 05:291 により event は発生せず、05:292 head-check も ACCEPTED を見続け、活性 CLOSED_LOOP lease は deadline / `lease_max_duration_s` まで継続。
- **impact**: not refuted。Rs 裁定 OPP-13 の「one-key fail-safe」（安全側の即時停止経路）が validator に拒否され得る構造は、D0 `:318` の fail-closed と逆方向（安全記録が書けない = event 層の fail-open）。裁定の方向への異議ではなく実現不全。frozen `two_key_state` は actuation 開始を守るが、**既に活性な** lease の停止経路には効かない。
- **verdict / severity**: CONFIRMED / **HIGH**（task 基準: 本文から再現可能 ∧ fail-closed 保証〔one-key 失効〕を破る）。
- **minimal fix（凍結 delta 0）**: (1) 06:513 の two-key / SHADOW 条項と「generation +1」条項を `state ∈ {PROPOSED, ACCEPTED}` に限定。(2) 06:514 に SUSPENDED / REVOKED record の規則を明記: `generation = superseded record と同値`・`supersedes_record_hash ≠ None`（必須）・`allowed_lease_modes = ()`・approvals ≥ 1（role_registry に登録された operator_ref または MONITOR）・hash field 複写・valid_until = superseded 値。(3) 「SUSPENDED / REVOKED の append を approvals 不足で拒否しない — 拒否理由は fork / supersedes 欠落・未知 / `suspension_reason` None のみ」を §8.3 に 1 文（fail-safe 方向の明示）。(4) PT-43「MONITOR 単独の SUSPENDED append が受理され、head-check（PT-40）が SAFEHOLD へ移す」。「認可された一者」の実名は OPP-13 残 open（Rs）のまま。

### R6-05 — CONFIRMED / MEDIUM — `role_registry_hash` が record 著者の自己証明で、権威ある head も資格取消の伝播も無い

**sed 確認**: 06:489「role_registry_hash: operator_ref → 資格 role 集合の content-addressed registry（受理時に照合・profile preimage 外）」／ 06:513「… role_registry で資格あり」／ 06:508「cell operator 単独では CLOSED_LOOP を許可できない」／ 06:511（namespace・ACL・journal writer の分離は ProfileRegistry についての記述）／ 05:191 (i)「P_ACCEPTANCE_RECORD_INVALID == 0 を head で再評価（role 充足・operator 相異）… record の hash field を発行時に再検証」（= record 自身の `role_registry_hash` に対して）。`grep role_registry` 05 = 0 hit（05 に head / issuer の概念なし）。frozen: EP `:156`「closed-loop authority は O0/S0/V0 two-key + 独立安全 gate を必ず conjoin」。裁定: OPP-13 ④・A_GPT56sol:36「不変前提に反する profile を単独 operator が受理する経路を閉じます」。

- **text**: not refuted。registry を「誰が発行し、どれが現行か」を定める文が無く、照合先は record が指す artifact。
- **logic**: not refuted。ProfileRegistry へ append できる 1 人が、operator_ref `a` = SAFETY_APPROVER・`b` = CUSTODY_APPROVER と書いた artifact R を作り `role_registry_hash = H(R)` で ACCEPTED(CLOSED_LOOP) を書く → 06:513 の存在・相異・資格の全条件が真。後日 `a` の資格を実 registry から外しても record は R を指し続け、05 (i) の再評価も R に対して真。反証として「approvals の真正性は OP-5 / U14 defer」（V4 R4-04 fix (3)）を当てたが、それは署名の話であり「どの registry が権威か」は別論点。
- **impact**: not refuted。deployment 層 two-key（裁定 OPP-13 ③④）の「閉じる」効果が本文から導けない（06:508 は主張のみ）。frozen two-key（O0/S0/V0）は AND で残るため単独で actuation には至らず MEDIUM。registry の**発行主体**は OPP-13 残 open（role 実名）と同じ court = Rs。
- **verdict / severity**: CONFIRMED / **MEDIUM**。
- **minimal fix（凍結 delta 0）**: (1) §8.3 に「role registry は ProfileRegistry と同じ custody 下の content-addressed artifact で **head を 1 つ持つ**（head の発行主体・ACL = OPP-13 残 open へ carry）」を定義。(2) `P_ACCEPTANCE_RECORD_INVALID` に `record.role_registry_hash == 書込時の role-registry head` を追加し、05 (i) / 条件 12 の head 再評価でも「現 head との等値」を要求（不一致 = `R_PROFILE_SUSPENDED`）。(3) head 更新 = bound する全 ACCEPTED record を SUSPENDED（`SuspensionReason` に deployment 層 member `ROLE_REGISTRY_SUPERSEDED` を追加し 06:342 表に → HOLD 行を追加）。record の `role_registry_hash` は監査用に残す。issuer の実名は Rs（open item として §10 OPP-13 に追記）。

### R6-06 — CONFIRMED / MEDIUM — clearance record の role が自由文字列の集合所属だけで、資格照合が無い

**sed 確認**: 05:541「clearance record = `RuntimeAuditRecord`（kind = CLEARANCE_RECORDED・payload = `{safehold_reason, role, operator_ref, profile_hash, evidence_ref}`）。CAS 条件 10 の『該当する clearance record』= `record.safehold_reason == current.safehold_reason ∧ record.role ∈ profile.escalation.clearance_roles[reason] ∧ record.profile_hash == permit.profile_hash ∧ record.seq > 当該 SAFEHOLD 遷移の record.seq`。SAFETY 行は加えて IndependentSafetyLayer の clearance record（AND）」／ 06:218 `clearance_roles: tuple[tuple[str, str], ...]`（role = 自由文字列）／ 05:534-539（SAFE_STOP / CHECKPOINT_DISABLED / MANAGER_RESTART / GATEWAY_RESTART の追加前提 = profile role〔+ GATEWAY_CONFIG_MATCH / durable 整合〕）。`grep 資格` 05 = 0 hit。frozen: D0 `:319-320`（stabilized_post_action_state による resumption eligibility）。

- **text**: not refuted。「該当」の定義に operator_ref の資格照合が無い。role 語彙は `AcceptanceRole` でも role registry でもない。
- **logic**: not refuted。SAFE_STOP 後、audit へ append できる者が `CLEARANCE_RECORDED{role = "SAFETY_OFFICER"（profile が列挙した文字列）, operator_ref = 自分, profile_hash, seq > hold}` を書く → 条件 10 真 → 完全経路で再 lease。SHADOW / 同 profile の再起動は 1 人で成立（CLOSED_LOOP は head ACCEPTED + frozen authority が要るが、SAFE_STOP 解除の人的 gate はこの record のみ）。SAFETY 行は ISL record と AND ゆえ対象外。
- **impact**: not refuted。R4-04（two-key が label 存在検査・HIGH）と同型の欠陥が clearance 側に残存（v0.2.6 は acceptance 側だけを `AcceptanceRole` + registry へ移した）。SAFE_STOP 解除は D0 §F の「stabilized state からの resumption」に直結する人的 gate。
- **verdict / severity**: CONFIRMED / **MEDIUM**。
- **minimal fix（凍結 delta 0）**: (1) 05:541 の payload に `role_registry_hash` を追加し、「該当」に `record.role_registry_hash == 現 role-registry head ∧ (record.operator_ref, record.role) ∈ registry` を AND（R6-05 の head を共用）。(2) 06:218 の role 語彙を「role registry が定義する role 名」に限定（自由文字列を廃止・未知 = codec 拒否）。`AcceptanceRole` への限定は clearance の意味（safety officer 等）に合わないため採らず、registry の role 集合とする。

### R6-07 — CONFIRMED / MEDIUM — BASE_POSE_VERIFY / `P_BASE_LAYOUT_MISMATCH` の許容値が baseline に無く、評価点・左右割当も未定義

**sed 確認**: 06:68「BASE_POSE_VERIFY = 実測 base pose と kinematic_layout の T_cell←base の差が **baseline 許容内**（arm ごと）」／ 06:311「kinematic_layout の各 arm base の Y が RS71 §0 #2 の固定 base（`RS71-System-Spec-SSOT.md:24`・値は本 doc に書かない）と **baseline 許容内**で一致 | `P_BASE_LAYOUT_MISMATCH`」（評価点の記載なし）／ 06:495-505 `CellSafetyTimingBaseline` の field = baseline_id / cell_id / layout_revision_ref / max_acceptance_validity_s / approved_ceiling / safety_response_budget_s / worst_case_attempt_s / selector_overhead_s / diagnostic_items（`grep 許容\|tolerance\|base_pose` → 許容値 field 無し）／ 06:422 (2) 第 1 list（… `P_ARM_*` / `P_TIMEOUT_ORDER`）・第 2 閉じた list のどちらにも `P_BASE_LAYOUT_MISMATCH` 無し／ 06:83「固定 base Y = ∓0.35 を含む」。frozen: RS71 `:24`「bases fixed at Y = ∓0.35 [`task_config.py:21-22`]」、`task_config.py:21-22` `ROBOT_LEFT_BASE = (0.0, -0.35, …)` / `ROBOT_RIGHT_BASE = (0.0, 0.35, …)`。

- **text**: not refuted。「baseline 許容」に対応する field が baseline に無い。評価点が無い。「各 arm base の Y」が resource_id と符号を対応づけていない。
- **logic**: not refuted。(i) 許容値不在 → 規則 (1) で評価不能 = 違反 → 全 profile が `P_BASE_LAYOUT_MISMATCH` / BASE_POSE_VERIFY FAIL（fail-closed だが機構が動かない）。(ii) 緩く「各 arm の Y ∈ {+0.35, −0.35}」と読む実装では ee_left = +0.35 / ee_right = −0.35 の鏡像 layout が通り、BASE_POSE_VERIFY（実測 vs layout）も整合するため通る。反証として「RS71 が task_config を引くので割当は導ける」を当てたが、06:311 は「値は本 doc に書かない」と言うのみで resource_id ↔ 符号の対応規則を書いていない。
- **impact**: not refuted。R4-17 fold（LOW）の実現不全で、RS71 §0 #2 の pin が unevaluable か鏡像を許すかの二択。裁定 OPP-15 ③「arm 別 base transform を hash 束縛」の検査側の穴。
- **verdict / severity**: CONFIRMED / **MEDIUM**。
- **minimal fix（凍結 delta 0）**: (1) `CellSafetyTimingBaseline` に `base_pose_tolerance_m: CanonicalDecimal` + `base_pose_tolerance_rad: CanonicalDecimal` を追加（値 = RT0・本 doc に書かない）。(2) `P_BASE_LAYOUT_MISMATCH` の評価点 = record 書込時（`P_TIMEOUT_CEILING` と同点・baseline が束縛される最初の点）+ 第 2 評価点、06:422 (2) の閉じた list に追加。(3) 06:311 の検査を「∀ arm: `kinematic_layout[arm.base_transform_id]` の Y が、arm.resource_id に対応する RS71 #2 の固定値（ee_left ↔ `ROBOT_LEFT_BASE`、ee_right ↔ `ROBOT_RIGHT_BASE`・`task_config.py:21-22` が RS71 `:24` の引く機械可読 SSOT）と符号付きで一致（許容 = baseline）」と定義（値は引き続き書かない）。(4) 06:68 の BASE_POSE_VERIFY も同じ許容 field を参照。

### R6-08 — CONFIRMED / MEDIUM（範囲縮小）— baseline `diagnostic_items` の最小集合が無く、05 が名指す項目の存在が保証されない

**sed 確認**: 06:504「diagnostic_items: tuple[tuple[str, CanonicalDecimal, CanonicalDecimal], ...]  # (項目, 周期 [s], worst-case detection [s]) — OP-19 の周期診断はこの項目のみ」／ 06:68「identity 系 check … は baseline diagnostic item として lease 中も周期再検証する」／ 06:341「周期診断は timing baseline（§8.3）が定める drift / proof-test 項目のみ」／ 05:88「上限 = baseline 項目 `VALIDITY_MONITOR_LIVENESS`」「event 通知の遅延上限 = baseline 項目」／ 05:191 (l)「wall / mono の drift が baseline diagnostic item の許容内」／ 05:292「manager 側 head-check（baseline `diagnostic_items` の周期 …）… monitor heartbeat が baseline 項目 `VALIDITY_MONITOR_LIVENESS` の上限を超えて欠落、または baseline diagnostic item の失敗」。`grep MIN_DIAGNOSTIC\|VALIDITY_MONITOR_LIVENESS\|CLOCK_DRIFT` 06 = 0 hit（06 は 05 が名指す項目名を定義していない）。最小集合を要求する `P_*` は 06:284-316 に無い。frozen: D0 `:318` / `:321`。裁定 OP-19 ③「周期再評価が必要な drift／proof-test 項目だけ、RT0 safety case が定める診断周期と worst-case detection time を timing_baseline_hash に収容」。

- **text**: not refuted。05 は `VALIDITY_MONITOR_LIVENESS`・drift 項目・head-check 周期を baseline 項目として**名指しで依存**するが、06 §8.3 はその項目名を語彙化も必須化もしない。
- **logic**: **部分 refuted**。`diagnostic_items = ()` の baseline では 05:191 (l) の drift 項が評価不能 = FALSE → `R_CLOCK_ANOMALY` で permit が出ない（fail-closed・R6 自身も認める）。しかし drift 項目 1 つだけの baseline では (l) は通り、`VALIDITY_MONITOR_LIVENESS` が無い → 05:292 の「上限を超えて欠落」は発火し得ない（trigger の評価不能 = 非発火）、identity の周期再検証（06:68）も無い → monitor 停止が沈黙する構成が `P_*` = 0 で通る。
- **impact**: not refuted。裁定 OP-19 は項目の**値と選定**を RT0 に置くが、05/06 が機構の前提として名指す項目の**存在**まで RT0 の裁量にするのは実現不全（05:88 の liveness 契約が空文になり得る）。方向は fail-open（monitor 停止の不検出）。
- **verdict / severity**: CONFIRMED / **MEDIUM**（R6 と同じだが範囲は「05/06 が名指す項目」に縮小）。
- **minimal fix（凍結 delta 0）**: (1) 06 §8.3 に `REQUIRED_DIAGNOSTIC_ITEMS = {VALIDITY_MONITOR_LIVENESS, CLOCK_DRIFT, REGISTRY_HEAD_CHECK, CONTROLLER_IDENTITY, TOOL_IDENTITY, SAFETY_LAYER_IDENTITY, BASE_POSE_VERIFY}` を閉じた項目語彙として置き（周期・detection の**値**は RT0）、`baseline.diagnostic_items ⊇ REQUIRED` を record 書込時 + 第 2 評価点で `P_TIMEOUT_CEILING`（baseline 妥当性）に含める。(2) 項目 → 実行者の対応（identity 4 項目 = `health_checks[].check_id` の周期実行・`VALIDITY_MONITOR_LIVENESS` / `REGISTRY_HEAD_CHECK` = manager・`CLOCK_DRIFT` = manager (l)）を 1 行で明記。⚠ identity 4 項目を必須にするかは 06:68 が既に「周期再検証する」と決めているため整合側だが、RT0 が不要と判断する余地を残すなら 06:68 を「baseline が定める場合」に緩める — この二択は OPP-16 の open item として記録（Rs / RT0）。

### R6-09 — CONFIRMED / MEDIUM — `P_TIMEOUT_ORDER` の baseline 依存項が第 1 評価点で循環する

**sed 確認**: 06:285「… `max_reselect_attempts × worst_case_attempt_s + selector_overhead_s ≤ boundary_dwell_s`（worst_case / overhead は timing baseline の値）… | `P_TIMEOUT_ORDER`」／ 06:422 (2)「第 1 評価点 = 登録時（… `P_ARM_*` / `P_TIMEOUT_ORDER`）」「受理 = 全 `P_*` = 0 を満たした profile_hash について `ProfileAcceptanceRecord(state = ACCEPTED)` を … append」／ 06:286「baseline = acceptance record の `timing_baseline_hash` が指す artifact・§8.3」／ 06:507「PROPOSED → ACCEPTED（受理）」（PROPOSED record を**いつ**書くかの記述なし）／ 06:486 `timing_baseline_hash` は record の必須 field。

- **text**: not refuted。第 1 評価点で baseline を束縛する record が存在する、という文が無い。baseline の束縛は record 経由のみ。
- **logic**: not refuted。登録時に baseline が束縛されていなければ当該不等式は評価不能 → 規則 (1) で `P_TIMEOUT_ORDER` → 全 `P_*` = 0 に到達せず ACCEPTED record を書けず → baseline を束縛できない。反証として「PROPOSED record（`timing_baseline_hash` 必須）を登録時に先に書く」読みを構成した — これは整合する唯一の読みだが本文に無く、V4 が R4-08 で提案した「第 1 = PROPOSED record 起票時」は fold されなかった（06:422 は「登録時」とだけ書く）。
- **impact**: not refuted。方向は fail-closed（受理不能）だが、裁定 OPP-11 ① の追加順序検査が仕様どおりには実行不能 — 実装者の解釈に委ねる箇所が受理 gate の入口にある。
- **verdict / severity**: CONFIRMED / **MEDIUM**。
- **minimal fix（凍結 delta 0）**: 06 §8.1 (2) / §8.3 に「登録 = `ProfileAcceptanceRecord(state = PROPOSED, timing_baseline_hash, …)` の起票。第 1 評価点はこの PROPOSED record の下で行い、baseline 依存項（06:285 の当該不等式・`P_BASE_LAYOUT_MISMATCH`）は PROPOSED record の `timing_baseline_hash` を解決して評価する。PROPOSED record への `P_ACCEPTANCE_RECORD_INVALID` は必須 field + baseline 解決可能性のみ」と明記（R4-08 fix の未 fold 分）。代替 = 当該不等式を record 書込時へ移す（06:286 と同点）— どちらでも可、前者を推奨。

### R6-10 — CONFIRMED / LOW — baseline supersession の検出が monitor 単独経路で、「baseline registry」が未定義語

**sed 確認**: 05:88「最小入力 = controller / ISL / tool の identity 報告・layout と **baseline registry** の supersession・registry の state 変化」（`grep "baseline registry"` 05/06 = 05:88 の 1 箇所のみ・定義なし）／ 05:292 head-check の対象 = registry head の record_hash / state・registry 可読・monitor heartbeat・diagnostic item（baseline head との比較なし）／ 05:191 (i)「`timing_baseline_hash` の artifact を解決して再検証」（permit 発行時のみ）／ 06:509「timing baseline の supersession（bound する全 record）… 認可された一者または自動 monitor が `SUSPENDED(reason)` record を append」／ 06:549 OPP-16（発行主体・改訂手続 = RT0 へ carry）。frozen: D0 `:321`（response budget = worst-case）。

- **text**: not refuted。「baseline registry」は定義されず、head-check は baseline head を照合しない。
- **logic**: not refuted（範囲縮小）。反証: supersession で SUSPENDED record が append されれば head-check が `state ≠ ACCEPTED` で捕らえる（monitor の通知が落ちても head-check 周期内に SAFEHOLD）。よって R6 の「event 消失」だけでは再現しない。**残る経路** = monitor が baseline registry を読めず SUSPENDED を append できない（誰も append しない）場合で、そのとき head は ACCEPTED のまま、head-check は真、旧 ceiling が `lease_max_duration_s + boundary_dwell_s` まで残る。
- **impact**: not refuted。改訂された worst-case budget が lease 中に反映されない窓（D0 `:321`）で、backstop が monitor の検出能力に単独依存。発行主体は OPP-16（Rs / RT0）だが、「head を持つ store」という定義と head-check の 1 項は runtime 層。
- **verdict / severity**: CONFIRMED / **LOW**。
- **minimal fix（凍結 delta 0）**: (1) 06 §8.3 に「baseline registry = cell_id ごとに head を 1 つ持つ content-addressed store（発行主体・改訂手続 = OPP-16）」を定義し 05:88 の語をそこへ参照。(2) 05:292 head-check に「`baseline_head(cell_id) ≠ record.timing_baseline_hash` ⇒ TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED)・reason BASELINE_SUPERSEDED（06:342 → HOLD）」を追加。

### R6-11 — CONFIRMED / MEDIUM — validity に上限が無く、`P_VALIDITY_UNBOUNDED` が第 2 評価点の閉じた list に無い

**sed 確認**: 06:296「`CalibrationRef.valid_until_iso8601`、および required record のうち kind ∈ {CALIBRATION_RECORD, SAFETY_LAYER_ACCEPTANCE} の `valid_until_iso8601` が **None**（無期限） | `P_VALIDITY_UNBOUNDED`」／ 06:364「None の意味 = 失効なし（期限検査の対象外）」／ 06:499 `max_acceptance_validity_s`（acceptance record のみ）／ 06:422 (2) 第 2 閉じた list = {`P_CALIBRATION_EXPIRED`, `P_EVIDENCE_UNBOUND`, `P_EVIDENCE_EXPIRED`, `P_EXPECTATION_UNCERTIFIED`, `P_TIMEOUT_CEILING`, `P_SAFETY_BUDGET_EXCEEDED`, `P_ENVELOPE_EXCEEDS_MEASURED`, `P_FAULT_EVIDENCE_UNVERIFIED`, `P_PREDICATE_HASH_MISMATCH`, `P_ACCEPTANCE_RECORD_INVALID`}（`P_VALIDITY_UNBOUNDED` 無し）／ 06:384 CALIBRATION_RECORD ∈ MIN_CLOSED_LOOP（第 2 評価点で初めて required）／ 06:342 CALIBRATION(TCP) 失効 → SAFE_STOP。`grep max_validity\|2999` 06 = 0 hit。frozen: D0 `:354-355`。

- **text**: not refuted。上限は acceptance にしか無い。required record の None 検査が置かれる評価点が無い。
- **logic**: not refuted。`CalibrationRef(kind = TCP, valid_until = "2999-01-01T00:00:00Z")` + 同 valid_until の CALIBRATION_RECORD → `P_VALIDITY_UNBOUNDED`（None でない）pass・`P_CALIBRATION_EXPIRED` / `P_EVIDENCE_EXPIRED` pass・`validity_deadline_mono` は acceptance 期限が支配 → TCP calibration は実質無期限（R2-18 の目的を有限巨大値で迂回 — PT-36「同率巨大化」と同型）。第 2 の論点: CALIBRATION_RECORD が None 期限で置かれても、第 2 評価点の閉じた list に `P_VALIDITY_UNBOUNDED` が無いため検査されず、06:364 により期限検査からも外れる（R5-04 と同一）。
- **impact**: not refuted。06:342 自身が TCP calibration 失効を SAFE_STOP 級と分類する一方、その期限を実質無効化できる。R4-16 が acceptance にだけ上限を置いた非対称。値は RT0（baseline）に置けるため裁定と衝突しない。
- **verdict / severity**: CONFIRMED / **MEDIUM**。
- **minimal fix（凍結 delta 0）**: (1) `CellSafetyTimingBaseline` に `max_validity_s: dict`（key = CalibrationKind 名 / DeploymentEvidenceKind 名 → [s]・値は RT0）を追加。(2) 06:296 を「None ∨ (valid_until − measured_at) > baseline.max_validity_s[kind]」に拡張し、評価点 = record 書込時（CalibrationRef・MIN_SHADOW の required record）+ 第 2 評価点（当該 mode で required になった record）。(3) 06:422 (2) の閉じた list に `P_VALIDITY_UNBOUNDED` を追加。

### R6-12 — CONFIRMED / LOW — 06:340「`AcceptedEnvelope.terms` の 3 項 ← §4」が旧文

**sed 確認**: 06:340「`AcceptedEnvelope.terms` の 3 項 ← §4」／ 06:333「profile 4 項（DEPLOYMENT_WORKSPACE / CONTROLLER / SAFETY_RESTRICTION / INTER_ARM・v0.2.6）」／ 06:323-327 = 5 term／ 05:367 `EnvelopeTermKind` 5 member／ 05:806 OP-2「profile 4 term」。

- **text**: not refuted（同一 doc 内で 3 項 / 4 項が並存）。**logic**: not refuted（字義では INTER_ARM 項が lease へ写されない）。**impact**: not refuted（R3-02 fold の取りこぼし・規範文の矛盾。05 側は正）。
- **verdict / severity**: CONFIRMED / **LOW**（R5-09 と同一）。
- **minimal fix**: 06:340 を「`AcceptedEnvelope.terms` の 4 項（DEPLOYMENT_WORKSPACE / CONTROLLER / SAFETY_RESTRICTION / INTER_ARM）← §4」に修正。

### R6-13 — CONFIRMED / LOW — checker Q2 の主張「06 §2.1 の hash field ⊆ 05 §3.4 (j) 列挙」が実装より強い（複製で再現）

**sed / code 確認**: `check_review_candidate.py:23`「Q2 … every *_sha256 / *_hash field declared in 06 §2.1 is named in the sibling 05 §3.4 (j) content-hash enumeration」／ `:179-180` `s21 = text[find("### 2.1"):find("### 2.2")]`・`hf = {…(_sha256|_hash)…}`（§2.1 のみ・3 型の `artifact_sha256` は 1 名に潰れる）／ `:183` `jl = [l … if "(j) profile が宣言する" in l]`（= 05:191 の **bullet 全行**・4,140 字・(a)–(l) を含む）／ `:186-187` `if h not in jl[0]`（部分文字列）。08:23・06:690 anchor 16・05:191 は「機械検査」と主張。

- **text**: not refuted（主張と実装の乖離は code で確認）。
- **logic**: not refuted。**複製試験（§0 #7・実物非接触）**: MUT-1（(j) から `evaluator_sha256` のみ削除・(a) の同語は残す）→ **FAIL 0**；MUT-3（(j) から `deployment_evidence_policy_hash` 削除）→ **FAIL 0**（§2.2 は走査外）；対照 MUT-2（(j) から `pairwise_keepout_sha256[]` 削除）→ **FAIL 1**（(j) 以外に出ない名は検出される）。`tensor_binding_hash` は (j) 部分文字列に無く (g) の出現で通る（R6 の指摘どおり。ただし (g) が等値検査するため (j) 外で正しい）。
- **impact**: not refuted。records-only helper（凍結 file ではない）の弁別力の過大主張。R4-02 の再発防止装置ゆえ、08 §1 項 5「弁別できない述語は証拠でない」に照らし直す価値がある。
- **verdict / severity**: CONFIRMED / **LOW**。
- **minimal fix（package 内 helper・凍結 delta 0）**: Q2 の照合対象を `jl[0]` から「`(j) profile が宣言する` 〜 `checker Q2`」の部分文字列に限定し、走査範囲を §2.1–§2.2 に広げ、`artifact_sha256` は宣言 context（`CellKinematicLayoutRef` / `CalibrationRef` / `GatewayConfigRef` / required record）ごとに qualified 名で照合。08:23 / 06:690 / 05:191 の主張文を実装に合わせて修正。

### R6-14 — CONFIRMED / LOW — `P_EVIDENCE_UNBOUND` 行の ∀ 列挙に arm 単位 kind が無い

**sed 確認**: 06:294「required kinds が **(kind × subject) 単位**で … 存在（… CALIBRATION_RECORD ∀ `calibration_refs[].calibration_id`・HEALTH_CHECK_RUN ∀ `health_checks[].check_id`・FAULT_INJECTION_RESULT ∀ `fault_injection[]` および ∀ `negative_controls[]` …）」／ 06:359「CONTROLLER_ENVELOPE_MEASUREMENT = arms[].resource_id#robot_serial_ref（**arm ごと**・同一機種でも別 record・v0.2.6 R4-12）/ TOOL_PAYLOAD_IDENTIFICATION = arms[].resource_id#tool_payload.tool_id（**arm ごと**）」／ 06:307 `P_ENVELOPE_EXCEEDS_MEASURED` = 「∀ arm: … 自身の record」。

- **text**: not refuted（部分 refuted）。「arm ごと」は 06:359 に明記され「(kind × subject) 単位」の framing から ∀ arm は導ける — しかし 06:294 は 3 kind だけ ∀ を明示し、多 subject の残り 2 kind を落としている（列挙の不整合）。
- **logic**: not refuted。列挙を網羅と読む validator は TPI 1 record（ee_left）で両 arm の要件を満たす。CEM の同 case は 06:307 の ∀ arm（評価不能 = 違反）で CLOSED_LOOP 時に止まるが、TPI には対応する `P_*` が無い。
- **impact**: not refuted（LOW: 06:359 が SSOT で、修正は 1 句）。
- **verdict / severity**: CONFIRMED / **LOW**。
- **minimal fix**: 06:294 の列挙に「CONTROLLER_ENVELOPE_MEASUREMENT / TOOL_PAYLOAD_IDENTIFICATION ∀ `cell.arms[]`（subject = §6.1）」を追加。

### R6-15 — CONFIRMED / LOW — `P_SAFETY_BUDGET_EXCEEDED` の「当該 arm の FI」が subject 文法から解決不能

**sed 確認**: 06:287「FI = **当該 arm の** R_SAFETY_LAYER_LOST#HEARTBEAT_TIMEOUT の FAULT_INJECTION_RESULT artifact（struct: audit_range / detection_to_safehold_worst_case_s / gateway_switch_worst_case_s / load_condition_ref）、CEM = 当該 arm の … artifact」／ 06:359「FAULT_INJECTION_RESULT = fault code（timeout 駆動は '<code>#<trigger>'）」（arm 成分なし）／ 06:294「(kind × subject) 単位」。

- **text**: not refuted。FI の subject に arm が無く、struct にも arm field が無い。
- **logic**: not refuted。両 arm で別々の FI record を作ると subject が同一（`R_SAFETY_LAYER_LOST#HEARTBEAT_TIMEOUT`）になり「当該 arm の」を選べない → 評価不能 = 違反（fail-closed）か実装者の解釈。式の FI 項（detection・gateway 切替）は manager / gateway の cell 共通量であり、arm 固有なのは CEM の stop time だけ — 「当該 arm の FI」は物理的にも不自然。
- **impact**: not refuted（LOW・fail-closed 側の仕様不備。R4-05 fold の残差）。
- **verdict / severity**: CONFIRMED / **LOW**。
- **minimal fix**: 06:287 を「FI = cell 単位（`load_condition_ref` が両 arm 負荷を含む worst-case・subject は既存のまま）。検査 = ∀ arm: `safety_heartbeat_timeout_s + FI.detection_to_safehold_worst_case_s + FI.gateway_switch_worst_case_s + CEM[arm].stop_time_worst_case_s[arm.controller.stop_class] ≤ baseline.safety_response_budget_s`」に書き換える。

### R6-16 — CONFIRMED / LOW — SHADOW lease の validity deadline の扱いが 06 と 05 で矛盾

**sed 確認**: 06:341「SHADOW_NON_AUTHORITY lease は actuation が無いため次 permit 評価で可」／ 06:442 PT-13「SHADOW lease は次 permit まで継続」／ 06:654「（SHADOW は次 permit）」 vs 05:290 from 列「（mode を問わず・v0.2.6）」／ 05:427「到達 = TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED)（mode を問わず・保守側・v0.2.6 R4-18。SHADOW は actuation が無いので実害なし）」／ 05:742 INV-34「mode を問わず」／ 05:822 OP-19「SHADOW は次 permit 評価で可」（裁定の文言）。裁定 A_GPT56sol:41「SHADOW は actuation がないため、次 permit 評価を**許容できます**」。

- **text**: not refuted（05 内でも遷移表 / INV-34 と OP-19 が食い違い、06 は OP-19 側）。**logic**: not refuted（PT-13 の期待結果は 05:290 の遷移と矛盾）。**impact**: not refuted。裁定は SHADOW の次 permit を**許容**するのみで要求しないため、05 が保守側を採ったのは裁定内。問題は doc 間の不一致（R5-01 と同一）。
- **verdict / severity**: CONFIRMED / **LOW**。
- **minimal fix**: 06:341 末文と PT-13 を「SHADOW lease も `validity_deadline_mono` 到達で TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED)（効果 = 記録のみ・05 INV-34）」に揃え、05:822 OP-19 と 06:654 に「05 / 06 は裁定が許容する『次 permit』ではなく保守側（mode を問わず）を採用」と注記。

### R6-17 — CONFIRMED / LOW — 停止性能 evidence（FI）の失効が HOLD に写像され 05:340 の基準文言と不整合

**sed 確認**: 06:342「EVIDENCE(SAFETY_LAYER_ACCEPTANCE | CONTROLLER_ENVELOPE_MEASUREMENT) → SAFE_STOP；EVIDENCE(その他) → HOLD」／ 05:340 disposition 列「`HOLD` / `SAFE_STOP`（安全機能・TCP・**停止性能に関わる失効**）」／ 06:287 FI の `detection_to_safehold_worst_case_s` / `gateway_switch_worst_case_s` が予算式の入力／ 05:290「doc 06 §5 (b) 表で引く」（表が SSOT）。

- **text**: not refuted（05:340 の基準文言と 06 表の分類が一致しない）。**logic**: not refuted（FI record に valid_until があり失効すれば「その他」→ HOLD）。**impact**: not refuted（LOW: 機械決定は 06 表で一意、乖離は 05 の説明文。HOLD でも運動は止まる〔SafeHold〕が停止 class・解除経路が異なる）。
- **verdict / severity**: CONFIRMED / **LOW**。
- **minimal fix**: 06:342 の SAFE_STOP 行に `FAULT_INJECTION_RESULT`（subject ∈ `R_SAFETY_LAYER_LOST#…`）を追加（保守側）し、05:340 の括弧書きを「doc 06 §5 (b) 表」への参照にする。

### R6-18 — CONFIRMED / LOW — approvals の相異範囲・validity 上限の基準時刻が未定義

**sed 確認**: 06:488「operator_ref は role ごとに相異」 vs 06:513「両者〔SAFETY / CUSTODY〕の `operator_ref` が相異かつ OPERATOR の operator_ref と相異 … SHADOW のみ ⇒ ∃ COMMISSIONING ∧ ∃ INDEPENDENT_REVIEWER で相異」／ 06:513「`valid_until` None または baseline `max_acceptance_validity_s` 超過」（起点の記載なし・06:499 も「上限」とのみ）。

- **text / logic / impact**: not refuted。例: COMMISSIONING = a・INDEPENDENT_REVIEWER = b・OPERATOR = a の SHADOW-only record は 06:513 で有効、06:488 で無効。「超過」は valid_until − (書込 wall time か approvals[].t か) が未定義で、規則 (1) では評価不能 = 違反にもなり得る。LOW（範囲の食い違い・1 語の欠落）。
- **verdict / severity**: CONFIRMED / **LOW**。
- **minimal fix**: 06:488 の注記を「相異の範囲 = 本 §8.3 検査の定義」に改め、06:513 に「超過 = `valid_until − registry 書込 wall time（append 記録の t）> max_acceptance_validity_s`」と起点を明記。

### R6-19 — CONFIRMED / LOW（(a) のみ）— identity 検査が自己申告を信頼することが honest scope に無い

**sed 確認**: 06:68-70（CONTROLLER_IDENTITY / TOOL_IDENTITY / SAFETY_LAYER_IDENTITY = 「報告する」値の等値）／ 06:128 `tcp_offset_ref` = arm ごとの TCP calibration_id、06:359 CALIBRATION_RECORD subject = calibration_id／ 06:318「契約層で検証できるのは形・単調性・宣言と evidence の束縛まで … 物理事実 … は全て evidence に委ねる」／ 06:538 OPP-5／ 06:525 §9 項 7（機能安全の内容のみ）。`grep 自己申告\|self-report` 06 = 0 hit。frozen: RS71 `:23` #1。

- **text**: not refuted（(a): 「identity は自己申告に依拠する」の明文なし）。(b)(c) は text で「欠落」だが下記 logic で反証。
- **logic**: **refuted**（(b)(c)）。(b) TCP calibration は `ArmSpec.tool_payload.tcp_offset_ref` で arm slot（resource_id → robot_serial_ref）に構造的に束縛済み。個体入替 + serial 再書込は controller / tool 双方の serial を偽る内部者行為で、06:318 / OPP-5 が契約層の外と宣言する「物理事実の真偽」の範疇 — 契約層で閉じるべき欠陥ではない。(c) 2F-85 が serial を報告できるかは integration 事実で、本文から欠陥を再現できない。(a) のみ「明文が無い」ことが残る。
- **impact**: not refuted（(a)・記述の欠落・LOW）。
- **verdict / severity**: CONFIRMED / **LOW**（(a) に限定。R6 の (b)(c) 修正案〔subject 拡張・KINEMATIC_SIGNATURE・代替 attest〕は新 diagnostic 項目 / 新手続 = RT0・Rs の court ゆえ採らない）。
- **minimal fix**: §9 または OPP-5 に 1 文「identity 系 check（CONTROLLER / TOOL / SAFETY_LAYER）は機器の自己申告値と宣言の等値であり、個体の物理同一性・報告の真正性は evidence（CELL_COMMISSIONING / TPI）と cell の運用管理の court」。

### R6-20 — CONFIRMED / LOW — FI artifact の worst-case 値が引用 audit と照合されない

**sed 確認**: 06:287（`FI.detection_to_safehold_worst_case_s` 等を式に使う）／ 06:297（audit 照合 = FAULT record の存在のみ）／ 06:360（artifact_ref は audit の (lease_id | None, seq 範囲)）／ 05:687 `t_mono`・05:678 `FAULT | DISPOSITION` kind／ 06:318（物理事実は evidence へ）。frozen: D0 `:321`（worst-case, not mean）。

- **text**: not refuted（数値の整合検査なし）。**logic**: not refuted（`detection_to_safehold_worst_case_s = 0` + 実 FAULT を含む範囲 → 予算 pass）。**impact**: not refuted — 06:318 / OPP-5 は「上限値の真偽」を evidence に委ねるが、ここでは validator が**既に読む** audit 範囲（06:297）に FAULT / DISPOSITION の `t_mono` があり、record 間の整合（R2-02 と同型）として cheap に検査できる。裁定 OPP-11 ③「実測上限を含める」の実現を強める。LOW（強化・fail-closed 方向）。
- **verdict / severity**: CONFIRMED / **LOW**。
- **minimal fix**: 06:297 に「FI struct を持つ record: `detection_to_safehold_worst_case_s ≥ max over 引用範囲 of (t_mono(DISPOSITION) − t_mono(FAULT(subject code)))`、偽 = `P_FAULT_EVIDENCE_UNVERIFIED`」を追加。

### R6-21 — CONFIRMED / LOW — 05:290 の記録列が ACCEPTANCE deadline の fault を欠く

**sed 確認**: 05:290 記録列「FAULT(R_CALIBRATION_EXPIRED_ACTIVE \| R_EVIDENCE_EXPIRED_ACTIVE)」／ 05:340 fault 列も同 2 code／ 05:742 INV-34 違反時「R_CALIBRATION_EXPIRED_ACTIVE / R_EVIDENCE_EXPIRED_ACTIVE / **R_PROFILE_SUSPENDED**」／ 05:230 条件 2「validity なら該当 `R_*_EXPIRED_ACTIVE` / `R_PROFILE_SUSPENDED`」／ 05:184 `validity_deadline_source.kind ∈ {CALIBRATION, EVIDENCE, ACCEPTANCE}`。

- **text / logic / impact**: not refuted（ACCEPTANCE 到達の fault が遷移行・失敗表に無く INV-34 / 条件 2 と不一致。INV-27 の total 性〔O5〕は enum → 行の方向のみ検査するため機械検査を通る）。LOW（R5-08 と同一）。
- **verdict / severity**: CONFIRMED / **LOW**。
- **minimal fix**: 05:290 の記録列と 05:340 の fault 列に `R_PROFILE_SUSPENDED`（ACCEPTANCE 到達）を追加。

## 3. 主張しないこと

- 本 verify は AI verifier の反証検証（08 §6）であり、two-key / Rs 裁定 / gate PASS / freeze / impl 解錠を代替・主張しない。verdict は fold 判断の材料に過ぎない。
- 凍結 4 file・`eval_runs/`・`$PKG` は非接触（§0 #8 で sha 不変を実測）。複製試験は `$OUT/q2test_v6/` のみ。
- Rs 専権に触れる要素は minimal fix に**値**を書かず open item として記録した: role 実名・role registry の発行主体（OPP-13）、baseline の許容値 / 上限値 / 診断項目の値（OPP-16 / RT0）、RS71 #4 asset pin の custody（§0-A）、SHADOW の deadline 扱い（裁定は許容・05 が保守側を選択）。
- R5 との重なりは注記のみで merge していない（reviewer 横断の統合は fold 側の判断）。
- 本 verifier は R6 と同じ 3 lens 手法だが独立に本文で再現しており、R6 の再現 script / 出力を根拠に用いていない（q2test は本 verifier が別 dir で再実施）。
