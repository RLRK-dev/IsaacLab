#!/usr/bin/env python3
"""v0.2.5 -> v0.2.6 fold (round 3: R3/R4 findings gated by verifier V3/V4 verdicts). Records-only; frozen files untouched.
Usage: python3 fold_v026.py <PKG_DIR> <verdicts_v026.json>   verdicts = {id: {"confirmed": bool, "severity": str, "merged_into": str}}"""
import sys, json, re, subprocess
P = sys.argv[1]; VERD = json.load(open(sys.argv[2]))
NOW = subprocess.check_output(["date", "-u", "+%Y-%m-%d %H:%M UTC"]).decode().strip()
A = "$D/WMSO_D11A_CONTRACTS_V2_DESIGN_RSTECHLEAD2_20260719.md"; D0 = "$D/WMSO_D0_ARCHITECTURE_DRAFT_RSTECHLEAD2_20260718.md"
RS71 = "thread_isaac_lab/thread-vault/04-Specs/RS71-System-Spec-SSOT.md"
def conf(i):
    v = VERD.get(i, {})
    if v.get("merged_into"): return conf(v["merged_into"])
    return bool(v.get("confirmed"))
def any_conf(*ids): return any(conf(i) for i in ids)
def vstr(i):
    v = VERD.get(i)
    if not v: return "（verifier 未了）"
    if v.get("merged_into"): return f"重複 → {v['merged_into']}"
    return f"CONFIRMED（{v['severity']}）" if v["confirmed"] else f"refuted（{v['severity']}）"
class Doc:
    def __init__(self, name): self.name = name; self.t = open(f"{P}/{name}", encoding="utf-8").read()
    def rep(self, old, new, count=1):
        n = self.t.count(old); assert n == count, (self.name, old[:80], n); self.t = self.t.replace(old, new)
    def repline(self, prefix, new):
        L = self.t.split("\n"); idx = [i for i, l in enumerate(L) if l.startswith(prefix)]
        assert len(idx) == 1, (self.name, prefix[:80], idx); L[idx[0]] = new; self.t = "\n".join(L)
    def line(self, prefix):
        L = [l for l in self.t.split("\n") if l.startswith(prefix)]; assert len(L) == 1, (self.name, prefix[:80], len(L)); return L[0]
    def insert_after_line(self, prefix, block):
        L = self.t.split("\n"); idx = [i for i, l in enumerate(L) if l.startswith(prefix)]
        assert len(idx) == 1, (self.name, prefix[:80], idx); L[idx[0]+1:idx[0]+1] = block.split("\n"); self.t = "\n".join(L)
    def insert_before_line(self, prefix, block):
        L = self.t.split("\n"); idx = [i for i, l in enumerate(L) if l.startswith(prefix)]
        assert len(idx) == 1, (self.name, prefix[:80], idx); L[idx[0]:idx[0]] = block.split("\n"); self.t = "\n".join(L)
    def save(self): open(f"{P}/{self.name}", "w", encoding="utf-8").write(self.t); print("wrote", self.name)
applied = []
def gate(*ids):
    ok = any_conf(*ids)
    if ok: applied.append(" / ".join(ids))
    return ok
newR = []

# ======================================================================= 05
d = Doc("05_WMSO_RUNTIME_SPEC_v0.2_REVIEW_CANDIDATE_20260903.md")
d.rep("RUNTIME SPEC (v0.2.5 REVIEW CANDIDATE)", "RUNTIME SPEC (v0.2.6 REVIEW CANDIDATE)")
d.rep("- status: **REVIEW CANDIDATE v0.2.5（未 bank・two-key 未・gate PASS を主張しない）**", "- status: **REVIEW CANDIDATE v0.2.6（未 bank・two-key 未・gate PASS を主張しない）** — v0.2.5 → v0.2.6 = v0.2.5 本文への再レビュー round 3（R3 / R4 → V3 / V4）の確定 finding の fold（§13）。旧版の系譜:")
d.rep("→ **Rs 裁定（外部推奨採用）の fold = 本版 v0.2.5（§13）**", "→ Rs 裁定（外部推奨採用）の fold = v0.2.5（2026-09-05 01:33 UTC）→ **round 3（R3 / R4 → V3 / V4）の fold = 本版 v0.2.6（§13）**")

if gate("R3-01", "R4-01", "R3-16"):
    d.rep("    kind: ControlMode                                 # frozen enum（lease.control_mode と等値必須）\n    payload_ref: str", "    kind: ControlMode                                 # frozen enum（lease.control_mode と等値必須）\n    arm_targets: tuple[tuple[str, str], ...]          # v0.2.6（R3-01 / R4-01 / R3-16）: (resource_id ∈ {ee_left, ee_right}, setpoint_ref) — この command が提案する arm ごとの setpoint。DUAL-ARM regime では通常 2 entry。1 entry の command は他 arm が直近 admitted setpoint で静止していることを前提にし、gateway はそれを線形化 snapshot で確認する\n    payload_ref: str")
    old = d.line("| INTER_ARM（v0.2.5・OPP-15 B+） |")
    d.repline("| INTER_ARM（v0.2.5・OPP-15 B+） |", "| INTER_ARM（v0.2.5・OPP-15 B+・v0.2.6 R3-01 / R4-01） | profile `InterArmRestrictionSet`（min separation / pairwise keep-out / swept-volume predicate・content hash 付き）+ `CellKinematicLayoutRef`（両 arm の base transform） | workspace（両 arm） | CommandGateway | 評価対象 = **同一 command（同一 cmd_seq）の `arm_targets` が提案する両 arm の線分の対**（直前 admitted setpoint → 新 setpoint・同一時間 parameterization・FK 後の TCP 点）を pairwise keep-out / min separation（TCP 点間距離）/ swept-volume predicate で連言評価。command が 1 arm しか提案しない場合、他 arm は直近 admitted setpoint での**点**（同一線形化 snapshot で静止を確認・動いていれば評価不能 = FALSE）。評価前に layout / pairwise keep-out / swept-volume predicate の解決内容 sha256 を宣言と再検証し、不一致 = 評価不能 = FALSE（R3-17 / R4-02）。動的干渉の独立監視は IndependentSafetyLayer（本 term の代替ではない・AND） |")
    d.rep("| any (EXECUTOR) | INTER_ARM 項の違反（両 arm の提案状態が pairwise keep-out / min separation / swept-volume predicate に反する）（v0.2.5・OPP-15 B+） |", "| any (EXECUTOR) | INTER_ARM 項の違反（同一 command の両 arm 提案線分の対、または提案 arm の線分 × 静止 arm の点が pairwise keep-out / min separation / swept-volume predicate に反する）（v0.2.5・OPP-15 B+・v0.2.6） |")
    d.repline("| INV-36 |", "| INV-36 | admitted command は INTER_ARM 項を満たす: 同一 command の `arm_targets` が提案する両 arm の線分の対（1 arm 提案なら他 arm は線形化 snapshot で静止した点）を同一評価で連言（v0.2.5・OPP-15・v0.2.6 R3-01） | R_INTER_ARM_VIOLATION |")
if gate("R3-02"):
    d.rep("class EnvelopeTermKind(Enum): INTRINSIC_D11B | DEPLOYMENT_WORKSPACE | CONTROLLER | SAFETY_RESTRICTION", "class EnvelopeTermKind(Enum): INTRINSIC_D11B | DEPLOYMENT_WORKSPACE | CONTROLLER | SAFETY_RESTRICTION | INTER_ARM   # v0.2.6（R3-02）: INTER_ARM を term として実体化（条件 7 / INV-36 の対象）")
    d.rep("| T-13 | AcceptedEnvelope: 各 term 単独違反 × 4・充足不能 profile |", "| T-13 | AcceptedEnvelope: 各 term 単独違反 × 5（INTER_ARM 含む・v0.2.6）・充足不能 profile |")
    d.rep("AcceptedEnvelope は profile 3 term のみ |", "AcceptedEnvelope は profile 4 term（DEPLOYMENT_WORKSPACE / CONTROLLER / SAFETY_RESTRICTION / INTER_ARM・v0.2.6）のみ |")
if gate("R3-17", "R4-02"):
    d.rep("(j) profile が宣言する**全ての** content hash（`predicate_sha256` / `evaluator_sha256` / `zones[].geometry_sha256` / `calibration_refs[].artifact_sha256` / `gateway_config.artifact_sha256` / `deployment_evidence_policy_hash` = H_WCJ(解決した policy) / required `DeploymentEvidenceRecord.artifact_sha256`）", "(j) profile が宣言する**全ての** content hash = doc 06 §2.1 の `*_sha256` / `*_hash` 名の全 field（列挙: `predicate_sha256` / `evaluator_sha256` / `zones[].geometry_sha256` / `calibration_refs[].artifact_sha256` / `gateway_config.artifact_sha256` / `deployment_evidence_policy_hash` = H_WCJ(解決した policy) / `cell.kinematic_layout.artifact_sha256` / `inter_arm_restrictions.pairwise_keepout_sha256[]` / `inter_arm_restrictions.swept_volume_predicate_sha256[]` / `initiation_strengthening.conjuncts[].schema_hash`（frozen InitiationSpec と同型の payload schema hash） / required `DeploymentEvidenceRecord.artifact_sha256`。checker Q2 が 06 §2.1 の hash field 名 ⊆ 本列挙を機械検査・v0.2.6 R3-17 / R4-02）")
if gate("R3-03", "R4-18"):
    d.rep("    effective_expires_at: float                       # v0.2.5（OPP-11）: = min(expires_at, ack.valid_until, decision.t_mono + decision_max_age_s, validity_deadline_mono)。条件 2 はこちらを使う", "    effective_expires_at: float                       # v0.2.5（OPP-11）: = min(expires_at, ack.valid_until, validity_deadline_mono, [decision.t_mono + decision_max_age_s — authority_decision_ref ≠ None のときのみ])。定義される項だけの min（v0.2.6 R3-03 / R4-18: SHADOW で不能判定にならない）。条件 2 はこちらを使う")
    d.rep("    authority_decision_ref: str | None                # proposed_lease.mode == CLOSED_LOOP_AUTHORITY ⇔ 非 null ∧ granted == True\n    profile_hash: str                                 # IndustrialDeploymentProfile（doc 06）\n    acceptance_record_hash", "    authority_decision_ref: str | None                # CLOSED_LOOP_AUTHORITY ⇒ 非 null ∧ granted == True；SHADOW では None 可（存在すれば記録のみ・§4.4 と同一・v0.2.6 R3-03）\n    profile_hash: str                                 # IndustrialDeploymentProfile（doc 06）\n    acceptance_record_hash")
    d.rep("    authority_decision_ref: str | None                # CLOSED_LOOP_AUTHORITY ⇔ 非 null ∧ granted == True\n    initialization", "    authority_decision_ref: str | None                # CLOSED_LOOP_AUTHORITY ⇒ 非 null ∧ granted == True；SHADOW では None 可（存在すれば記録のみ・v0.2.6 R3-03）\n    initialization")
if gate("R3-04", "R4-06", "R3-05"):
    d.rep("    validity_deadline_mono: float                     # v0.2.5（OP-19）: min(calibration 期限, evidence 期限, acceptance 期限, issued_at + lease_max_duration_s) を manager clock へ変換した値（wall→mono は発行時に確定・doc 06 OPP-3）", "    validity_deadline_mono: float                     # v0.2.5（OP-19）: min(calibration 期限, required evidence 期限, acceptance 期限) を manager clock へ変換した値（wall→mono は発行時に確定・doc 06 OPP-3）。lease_max_duration_s は含めない（INV-33 / R_LEASE_DURATION_EXCEEDED が担う・v0.2.6 R3-04 / R4-06）\n    validity_deadline_source: tuple[tuple[str, str, float], ...]   # v0.2.6（R3-05）: (kind ∈ {CALIBRATION, EVIDENCE, ACCEPTANCE}, id = calibration_id | record_id | record_hash, deadline_mono) — 全項目の個別 deadline。到達時の fault と disposition は kind / id から doc 06 §5 (b) の表で決まる（表に無い = SAFE_STOP）")
    d.rep("    validity_deadline_mono: float                     # v0.2.5（OP-19）: permit から複写。CLOSED_LOOP では到達 = TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED)。SHADOW では記録のみ（次 permit 評価で扱う）", "    validity_deadline_mono: float                     # v0.2.5（OP-19）: permit から複写。到達 = TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED)（mode を問わず・保守側・v0.2.6 R4-18。SHADOW は actuation が無いので実害なし）\n    validity_deadline_source: tuple[tuple[str, str, float], ...]   # v0.2.6（R3-05）: permit から複写")
    d.rep("| `S_LEASE_ACTIVE` / `S_HEALTH_PENDING` / `S_BOUNDARY_WAIT`（CLOSED_LOOP_AUTHORITY） | `now ≥ lease.validity_deadline_mono`（calibration / evidence / acceptance の期限到達・manager clock・v0.2.5 OP-19） | TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED \\| SAFE_STOP if 失効項目が安全機能・TCP・停止性能に関わる) | `S_SAFEHOLD(VALIDITY_EXPIRED \\| SAFE_STOP)` |", "| `S_LEASE_ACTIVE` / `S_HEALTH_PENDING` / `S_BOUNDARY_WAIT`（mode を問わず・v0.2.6） | `now ≥ lease.validity_deadline_mono`（calibration / evidence / acceptance の期限到達・manager clock・v0.2.5 OP-19。lease 時間上限は別行 R_LEASE_DURATION_EXCEEDED） | TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED \\| SAFE_STOP — `validity_deadline_source` の到達項目を doc 06 §5 (b) 表で引く・表に無い = SAFE_STOP・v0.2.6 R3-05) | `S_SAFEHOLD(VALIDITY_EXPIRED \\| SAFE_STOP)` |")
    d.repline("| INV-34 |", "| INV-34 | lease は `validity_deadline_mono` を超えて活性でない（到達 = TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED)・mode を問わず）。deadline = permit 発行時に wall-clock 期限を manager clock へ変換して固定した calibration / evidence / acceptance の min（lease 時間上限は INV-33）。到達項目は `validity_deadline_source` から決まる（v0.2.5・OP-19・v0.2.6 R3-04 / R3-05） | R_CALIBRATION_EXPIRED_ACTIVE / R_EVIDENCE_EXPIRED_ACTIVE / R_PROFILE_SUSPENDED |")
if gate("R3-06"):
    d.rep("**lease-scoped な理由**（EXECUTOR_LOST / HEALTH_FAIL / DEADLINE_MISS / TIMING_VIOLATION / ENVELOPE_VIOLATION / CHECKPOINT_DISABLED / NO_CHAIN）の再試行は", "**lease-scoped な理由**（EXECUTOR_LOST / HEALTH_FAIL / DEADLINE_MISS / TIMING_VIOLATION / ENVELOPE_VIOLATION / CHECKPOINT_DISABLED / NO_CHAIN / VALIDITY_EXPIRED の deadline 到達）の再試行は")
    d.rep("non-lease-scoped（SAFETY / SAFE_STOP / MANAGER_RESTART / GATEWAY_RESTART）は無条件に再試行。", "non-lease-scoped（SAFETY / SAFE_STOP / MANAGER_RESTART / GATEWAY_RESTART）は無条件に再試行。**profile-scoped**（VALIDITY_EXPIRED の registry / monitor event・v0.2.6 R3-06）: event は `profile_hash` + `acceptance_record_hash` を運び、再試行は `current.active_lease.profile_hash == trigger.profile_hash` の間続ける（別 lease に切り替わっても同じ profile なら無効化する — SUSPENDED profile 上の新 lease が走り続けない）。")
    d.rep("`pending_invalidate` = gateway が TRANSFER_TO_SAFEHOLD の trigger を検出してから", "`pending_invalidate`（VALIDITY_EXPIRED の deadline / event を含む・v0.2.6 R3-06）= gateway が TRANSFER_TO_SAFEHOLD の trigger を検出してから")
if gate("R3-07", "R3-08", "R3-20"):
    d.rep("| registry（doc 06 §8.3）が読めない（v0.2.5・OPP-13） | AuthorityManager | `R_REGISTRY_UNAVAILABLE` | permit 不発行・CAS REJECTED・actuation 全面拒否（診断用 SAFEHOLD での起動は可） | `HOLD` |", "| registry（doc 06 §8.3）が読めない（v0.2.5・OPP-13・v0.2.6 R3-07 で範囲を明示） | AuthorityManager | `R_REGISTRY_UNAVAILABLE` | 起動時 / permit 発行 / CAS では拒否（診断用 SAFEHOLD での起動は可・新規 actuation なし）。活性 lease は継続するが、head-check 周期（baseline diagnostic item）を超えて読めない場合は TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED)（R3-08） | `HOLD` |")
    d.insert_after_line("| any (EXECUTOR・CLOSED_LOOP_AUTHORITY) | DeploymentValidityMonitor の失効 event", "| any (EXECUTOR) | **manager 側 head-check**（baseline `diagnostic_items` の周期・monitor 非依存・v0.2.6 R3-08）: `head(registry, lease.profile_hash)` の `record_hash ≠ lease.acceptance_record_hash ∨ state ≠ ACCEPTED`、または registry が head-check 周期を超えて読めない、または DeploymentValidityMonitor の heartbeat が baseline 項目 `VALIDITY_MONITOR_LIVENESS` の上限を超えて欠落、または baseline diagnostic item の失敗（R3-20） | TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED \\| SAFE_STOP per doc 06 §5 (b) 表) | `S_SAFEHOLD(VALIDITY_EXPIRED \\| SAFE_STOP)` | FAULT(R_PROFILE_SUSPENDED \\| R_REGISTRY_UNAVAILABLE \\| R_EVIDENCE_EXPIRED_ACTIVE), DISPOSITION |")
    d.rep("周期診断は timing baseline が定める drift / proof-test 項目のみ | 監視対象の直近観測（durable 不要） |", "周期診断は timing baseline が定める drift / proof-test 項目のみ。**liveness 契約（v0.2.6・R3-08）**: monitor は heartbeat を manager へ送り（上限 = baseline 項目 `VALIDITY_MONITOR_LIVENESS`）、manager は monitor に依存しない head-check（§3.7）を周期実行する。event 通知の遅延上限 = baseline 項目（R3-20）。最小入力 = controller / ISL / tool の identity 報告・layout と baseline registry の supersession・registry の state 変化（R4-14） | 監視対象の直近観測（durable 不要） |")
if gate("R3-09"):
    d.rep("registry 不読 = `R_REGISTRY_UNAVAILABLE`・permit 不発行；SUSPENDED / REVOKED / 世代不一致 = `R_PROFILE_SUSPENDED`・v0.2.5 OPP-13。", "registry 不読 = `R_REGISTRY_UNAVAILABLE`・permit 不発行；SUSPENDED / REVOKED / 世代不一致 = `R_PROFILE_SUSPENDED`・v0.2.5 OPP-13。**cell 単位の停止（v0.2.6・R3-09）**: 同一 `cell_identity_hash` を持つ他 record が原因未解消の SUSPENDED なら本 profile も `R_PROFILE_SUSPENDED`（兄弟 profile による迂回を閉じる）。")
if gate("R3-11"):
    d.rep("計時は `owner == EXECUTOR ∧ ¬boundary_wait ∧ mode == CLOSED_LOOP_AUTHORITY ∧ 当該 lease で admitted command ≥ 1` のときのみ。", "計時は `owner == EXECUTOR ∧ ¬boundary_wait ∧ 当該 lease で admitted command ≥ 1` のときのみ（mode は問わない — SHADOW でも admission 判定は行うため deadline / jitter は計測でき、結果の SafeHold は記録のみ。v0.2.6 R3-11: R2-01 の commissioning evidence が最初の CLOSED_LOOP 前に作れる）。")
if gate("R3-13"):
    d.rep("事後条件 = `(SAFEHOLD, epoch+1, None, consumed そのまま, reason, False, seq+1)` + 旧 lease 無効化 + 全 UNUSED permit の VOID。", "事後条件 = `(SAFEHOLD, epoch+1, None, consumed そのまま, reason, False, seq+1)` + `safehold_disposition := 当該遷移の実効 disposition`（reason SAFE_STOP ⇒ SAFE_STOP・v0.2.6 R3-13）+ 旧 lease 無効化 + 全 UNUSED permit の VOID。")
    d.rep("- 初期状態 = `(SAFEHOLD, epoch=e0, None, ∅, INITIAL, False, 0)`。", "- 初期状態 = `(SAFEHOLD, epoch=e0, None, ∅, INITIAL, False, 0)` + `safehold_disposition = HOLD`（v0.2.6）。")
    d.rep("（(iii) は `safehold_reason == SAFE_STOP` で SafeStop を出す）。", "（(iii) は `state.safehold_disposition == SAFE_STOP` で SafeStop を出す — reason SAFE_STOP なら構成上 disposition も SAFE_STOP・v0.2.6 R3-13）。")
if gate("R3-14"):
    d.rep("2. `permit.state == UNUSED ∧ now < permit.effective_expires_at`（v0.2.5・OPP-11: permit_ttl だけでなく ACK・decision age・validity deadline の最小値）` ∧", "2. `permit.state == UNUSED ∧ now < permit.effective_expires_at`（v0.2.5・OPP-11: permit_ttl だけでなく ACK・decision age・validity deadline の最小値。**fault の優先（v0.2.6 R3-14）**: 到達した項が ACK なら `R_ACK_MISBOUND`（条件 3）、decision age なら `R_AUTHORITY_DECISION_ABSENT`（条件 6）、validity なら該当 `R_*_EXPIRED_ACTIVE` / `R_PROFILE_SUSPENDED`、ttl のみなら `R_PERMIT_EXPIRED` — 下位の失効が `R_PERMIT_EXPIRED` に優先する）` ∧")
    d.rep("失効は `now < expires_at` の", "失効は `now < effective_expires_at`（v0.2.6）の")
if gate("R3-15"):
    d.rep("その record が無い decision は不在として扱う（`R_AUTHORITY_DECISION_ABSENT`）。age 超過 = 再評価（新 ref）を要求し permit 不発行; ", "その record が無い decision は不在として扱う（`R_AUTHORITY_DECISION_ABSENT`）。`t_mono` は AuditRecorder が manager の宣言 origin で刻印し、`t_mono > now(manager)` の record は不在扱い（clock domain・v0.2.6 R3-15）。age 超過 = 再評価（新 ref）を要求し permit 不発行; ")
if gate("R3-18", "R4-08"):
    d.rep("（doc 06 §8.1 の第 2 評価点 = `P_CALIBRATION_EXPIRED` / `P_EVIDENCE_UNBOUND` / `P_EVIDENCE_EXPIRED`（required record の valid_until 超過・v0.2.3 CD-03）/ `P_EXPECTATION_UNCERTIFIED`）", "（doc 06 §8.1 (2) が第 2 評価点として列挙する**全 code**（SSOT = doc 06 §8.1 (2)・v0.2.6 R3-18 / R4-08）")
if gate("R3-19"):
    d.repline("| INV-03 |", "| INV-03 | admitted command: `(cmd.lease_id, cmd.control_epoch) == (state.active_lease_id, state.control_epoch)` を線形化点で満たし、`cmd.executor_id == lease.executor_id`（v0.2.4・R1-12・不一致 = `R_LEASE_UNKNOWN_COMMAND`・v0.2.6 R3-19）、`cmd.cmd_seq` は lease 内で厳密単調（v0.2.2） | R_EPOCH_STALE_COMMAND / R_LEASE_UNKNOWN_COMMAND / R_TIMING_VIOLATION |")
    d.rep("| lease_id 不明の command（v0.2.1・B-10） | CommandGateway | `R_LEASE_UNKNOWN_COMMAND` |", "| lease_id 不明の command・または `cmd.executor_id ≠ lease.executor_id`（v0.2.1・B-10・v0.2.6 R3-19） | CommandGateway | `R_LEASE_UNKNOWN_COMMAND` |")
if gate("R3-21", "R4-09", "R4-16", "R4-04"):
    d.rep("(i) doc 06 §8.3 の `ProfileRegistry` で `permit.profile_hash` の最新 `ProfileAcceptanceRecord` が `state == ACCEPTED ∧ proposed_lease.mode ∈ allowed_lease_modes ∧ 有効期限内` であり、permit はその `acceptance_record_hash` / `generation` を束縛する", "(i) doc 06 §8.3 の `ProfileRegistry` で `permit.profile_hash` の head record（= registry append seq 最大・fork は registry が拒否）が `state == ACCEPTED ∧ proposed_lease.mode ∈ allowed_lease_modes ∧ 有効期限内 ∧ P_ACCEPTANCE_RECORD_INVALID == 0 を head で再評価（role 充足・operator 相異）`（v0.2.6 R3-21 / R4-04）であり、record の hash field を発行時に再検証する（v0.2.6 R4-09: 第 2 評価点の required set は `evidence_set_hash` が指す集合の**内側**で満たす（追加 evidence = 新 generation）；validator の content hash == `record.validator_hash`；`timing_baseline_hash` の artifact を解決して再検証；不一致 = `R_PROFILE_MISBOUND`）。permit はその `acceptance_record_hash` / `generation` を束縛する")
    d.rep("12. **registry 再検査（v0.2.5・OPP-13）**: doc 06 §8.3 registry の線形化読みで `permit.profile_hash` の最新 record が `state == ACCEPTED ∧ generation == permit.acceptance_generation ∧ proposed_lease.mode ∈ allowed_lease_modes`。", "12. **registry 再検査（v0.2.5・OPP-13）**: doc 06 §8.3 registry の線形化読みで `permit.profile_hash` の head record が `record_hash == permit.acceptance_record_hash ∧ state == ACCEPTED ∧ generation == permit.acceptance_generation ∧ proposed_lease.mode ∈ allowed_lease_modes`（v0.2.6 R4-16: hash 等値を含む）。")
if gate("R4-15"):
    d.rep("(k) `validity_deadline_mono` と `effective_expires_at` を発行時に導出して permit に固定（v0.2.5・OPP-11 / OP-19）", "(k) `validity_deadline_mono` と `effective_expires_at` を発行時に導出して permit に固定（v0.2.5・OPP-11 / OP-19）; (l) **clock 健全性（v0.2.6・R4-15）**: `wall_now ≥ max(required record の measured_at, approvals[].t)` ∧ wall / mono の drift が baseline diagnostic item の許容内 ∧ `wall_now` が直前の PERMIT_ISSUED の wall より単調。いずれか偽 = `R_CLOCK_ANOMALY`・permit 不発行。PERMIT_ISSUED payload に `(wall_now, mono_now)` を記録。周期 drift 検査（baseline 項目）の失敗 = TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED \\| SAFE_STOP)")
    d.insert_after_line("| registry（doc 06 §8.3）が読めない", "| wall-clock の異常（過去へ戻る / required evidence や approvals より前 / drift 超過）（v0.2.6・R4-15） | AuthorityManager（permit 発行時・周期 drift 検査） | `R_CLOCK_ANOMALY` | permit 不発行。活性 lease 中の周期検査失敗 = TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED \\| SAFE_STOP) | `HOLD` / `SAFE_STOP` |")
    newR.append("R_CLOCK_ANOMALY")
if gate("R4-11"):
    d.rep("| any (EXECUTOR・CLOSED_LOOP_AUTHORITY) | DeploymentValidityMonitor の失効 event（incident / tool 交換 / firmware 更新 / layout revision / ISL build 変更 / profile SUSPENDED・REVOKED）（v0.2.5・OP-19 / OPP-13） | TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED \\| SAFE_STOP if 安全起因) |", "| any (EXECUTOR) | DeploymentValidityMonitor の失効 event = registry に `SUSPENDED(reason)` record を append した**後**の通知（reason ∈ doc 06 §8.3 `SuspensionReason`・v0.2.6 R4-11: 記録なしの event は無い。event は profile_hash + acceptance_record_hash を運ぶ） | TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED \\| SAFE_STOP per doc 06 §5 (b) reason→disposition 表) |")
    d.rep("| INITIAL / NO_CHAIN / EXECUTOR_LOST / DEADLINE_MISS / TIMING_VIOLATION / ENVELOPE_VIOLATION / HEALTH_FAIL / VALIDITY_EXPIRED（v0.2.5） | 完全経路（ACK → permit → CAS）による TRANSFER_TO_EXECUTOR のみ（VALIDITY_EXPIRED では新 permit が §3.4 (b)(i) で失効原因の解消を要求する） | なし |", "| INITIAL / NO_CHAIN / EXECUTOR_LOST / DEADLINE_MISS / TIMING_VIOLATION / ENVELOPE_VIOLATION / HEALTH_FAIL / VALIDITY_EXPIRED（v0.2.5） | 完全経路（ACK → permit → CAS）による TRANSFER_TO_EXECUTOR のみ（VALIDITY_EXPIRED では新 permit が §3.4 (b)(i) で失効原因の解消を要求する。event 起因なら registry の SUSPENDED record が新 generation + two-key で置換されるまで (i) が拒否する = 自動再 lease は無い・v0.2.6 R4-11） | なし |")
if newR:
    d.rep("    R_PROFILE_SUSPENDED | R_REGISTRY_UNAVAILABLE | R_CALIBRATION_EXPIRED_ACTIVE | R_EVIDENCE_EXPIRED_ACTIVE | R_INTER_ARM_VIOLATION   # v0.2.5", "    R_PROFILE_SUSPENDED | R_REGISTRY_UNAVAILABLE | R_CALIBRATION_EXPIRED_ACTIVE | R_EVIDENCE_EXPIRED_ACTIVE | R_INTER_ARM_VIOLATION |   # v0.2.5\n    " + " | ".join(newR) + "   # v0.2.6（round 3）")
rows05 = [("R3-01 / R4-01 / R3-16","INTER_ARM が他 arm を静的に扱う・GatewayCommand に arm が無い","`GatewayCommand.arm_targets`・§4.3 INTER_ARM・§3.7・INV-36"),("R3-02","EnvelopeTermKind に INTER_ARM が無い","§4.3 enum・T-13・OP-2"),("R3-17 / R4-02","content hash 列挙に v0.2.5 の 3 hash が無い","§3.4 (j)・checker Q2"),("R3-03 / R4-18","effective_expires_at が SHADOW で不能判定","§3.4 CommitPermit・§4.1"),("R3-04 / R4-06 / R3-05","validity_deadline_mono の lease_max 項・到達項目が不明","§3.4 / §4.1 `validity_deadline_source`・§3.7・INV-34"),("R3-06","VALIDITY_EXPIRED の再試行 scope","§3.5・§5.2 (ii)"),("R3-07 / R3-08 / R3-20","registry 不読の範囲・monitor の liveness・診断失敗","§2・§3.7 head-check 行・失敗表"),("R3-09","兄弟 profile による cell 停止の迂回","§3.4 (i)"),("R3-11","R1-01 × R2-01 の衝突","§4.2 計時範囲"),("R3-13","safehold_disposition が遷移で設定されない","§3.2 初期状態・§3.5 事後条件"),("R3-14","effective expiry の fault 優先順・証明 D","§3.5 条件 2・§3.8"),("R3-15","decision t_mono の clock domain","§3.4 (e)"),("R3-18 / R4-08","第 2 評価点の list が 06 と不一致","§3.4 (b)"),("R3-19","executor_id 不一致の fault","INV-03・失敗表"),("R3-21 / R4-09 / R4-16 / R4-04","head record の再評価・hash field の再検証・fork","§3.4 (i)・§3.5 条件 12"),("R4-15","clock 異常の検査が無い","§3.4 (l)・`R_CLOCK_ANOMALY`・失敗表"),("R4-11","event の記録・disposition 決定・自動再 lease","§3.7・§5.4")]
tbl = [f"- **v0.2.6 fold（{NOW}）— round 3（reviewer R3 / R4・v0.2.5 対象 → verifier V3 / V4・3 lens）の確定 finding の fold**。記録 = `11_THREE_AXIS_REVIEW_RECORD_20260903.md` §9:", "", "| finding | 内容 | 変更節 | verdict |", "|---|---|---|---|"]
for i, c, s in rows05: tbl.append(f"| {i} | {c} | {s if any_conf(*i.split(' / ')) else '— 適用せず'} | {'; '.join(f'{x}={vstr(x)}' for x in i.split(' / '))} |")
tbl.append("| （06 側の fold 行は doc 06 §11） | | | |")
d.insert_before_line("## 14. Review anchors", "\n".join(tbl) + "\n")
d.save()

# ======================================================================= 06
d = Doc("06_WMSO_INDUSTRIAL_PROFILE_v0.2_REVIEW_CANDIDATE_20260903.md")
d.rep("DEPLOYMENT PROFILE SPEC (v0.2.5 REVIEW CANDIDATE)", "DEPLOYMENT PROFILE SPEC (v0.2.6 REVIEW CANDIDATE)")
d.rep("／ v0.2.5 = ", "／ v0.2.6 = %s（`date -u` 実測）／ v0.2.5 = " % NOW)
d.rep("- status: **REVIEW CANDIDATE v0.2.5（未 bank・two-key 未・gate PASS を主張しない）**", "- status: **REVIEW CANDIDATE v0.2.6（未 bank・two-key 未・gate PASS を主張しない）** — v0.2.5 → v0.2.6 = round 3（R3 / R4 → V3 / V4）の確定 finding の fold（§11）。旧版の系譜:")
newP, newPT, newOPP = [], [], []
if gate("R3-01", "R4-01", "R3-16", "R3-02"):
    d.rep("                            , CONTROLLER(profile.cell.arms[cmd.resource].controller)  … joint space（command が指す arm の envelope・v0.2.4 R2-04）", "                            , CONTROLLER(profile.cell.arms[r].controller for r in cmd.arm_targets[].resource_id)  … joint space（command が提案する各 arm の envelope・v0.2.4 R2-04・v0.2.6 R3-16）")
    d.rep("                            , SAFETY_RESTRICTION(profile.safety_restrictions) … gateway predicate（静的）}", "                            , SAFETY_RESTRICTION(profile.safety_restrictions) … gateway predicate（静的）\n                            , INTER_ARM(profile.inter_arm_restrictions, cell.kinematic_layout) … 両 arm の提案線分の対（05 §4.3・v0.2.6 R3-02）}")
    d.rep("      (b) AcceptedEnvelope の非 frozen 項（ControllerEnvelope / WorkspaceRestriction / SafetyRestrictionSet）を供給する", "      (b) AcceptedEnvelope の非 frozen 項（ControllerEnvelope / WorkspaceRestriction / SafetyRestrictionSet / InterArmRestrictionSet + CellKinematicLayoutRef）を供給する")
    d.rep("では INTRINSIC 項が欠け、profile 3 項のみ（05 OP-2）。", "では INTRINSIC 項が欠け、profile 4 項（DEPLOYMENT_WORKSPACE / CONTROLLER / SAFETY_RESTRICTION / INTER_ARM・v0.2.6）のみ（05 OP-2）。")
    d.rep("    # 評価 = 05 §4.3 INTER_ARM 項（両 arm の同一線形化 snapshot で連言評価）。", "    # 評価 = 05 §4.3 INTER_ARM 項（同一 command の両 arm 提案線分の対を連言評価。1 arm 提案なら他 arm は線形化 snapshot で静止した点・v0.2.6 R3-01 / R4-01）。")
if gate("R3-17", "R4-02"):
    d.rep("| profile が宣言する**全ての** content hash（`predicate_sha256` / `evaluator_sha256` / `zones[].geometry_sha256` / `calibration_refs[].artifact_sha256` / `gateway_config.artifact_sha256` / `deployment_evidence_policy_hash` / required record の `artifact_sha256`）", "| profile が宣言する**全ての** content hash = §2.1 の `*_sha256` / `*_hash` 名の全 field（`predicate_sha256` / `evaluator_sha256` / `zones[].geometry_sha256` / `calibration_refs[].artifact_sha256` / `gateway_config.artifact_sha256` / `deployment_evidence_policy_hash` / `kinematic_layout.artifact_sha256` / `pairwise_keepout_sha256[]` / `swept_volume_predicate_sha256[]` / `conjuncts[].schema_hash` / required record の `artifact_sha256`・v0.2.6 R3-17 / R4-02）")
if gate("R4-03"):
    d.rep("    validity_rule: ValidityRule            # v0.2.1（C-09）: enum。v1.0 の唯一の member = ALL_REQUIRED_VALID_AT_PERMIT_ISSUE", "    validity_rule: ValidityRule            # v0.2.1（C-09）: enum。v1.0 の唯一の member = ALL_REQUIRED_VALID_AT_PERMIT_ISSUE\n    negative_controls: tuple[str, ...]     # v0.2.6（R4-03）: ⊇ MIN_NEGATIVE_CONTROLS（下記）でなければ P_EVIDENCE_POLICY_TOO_WEAK。各 control に FAULT_INJECTION_RESULT（subject_ref = 'NC#<name>'）を CLOSED_LOOP 前に要求（P_EVIDENCE_UNBOUND）")
    d.rep("MIN_NEGATIVE_CONTROLS（CLOSED_LOOP_AUTHORITY 前・v0.2.5 OPP-15 B+ / OPP-11）= FAULT_INJECTION_RESULT with subject_ref ∈ {CELL#ARM_SWAP, CELL#ARM_MISSING, CELL#BASE_TRANSFORM_TAMPER, CELL#INTER_ARM_COLLISION, ENV#CLOCK_ANOMALY, ENV#SCHEDULER_STALL, ENV#COMM_LOSS}", "MIN_NEGATIVE_CONTROLS（CLOSED_LOOP_AUTHORITY 前・v0.2.5 OPP-15 B+ / OPP-11・v0.2.6 R4-03 で執行）= {ARM_SWAP, ARM_MISSING, BASE_TRANSFORM_TAMPER, INTER_ARM_COLLISION, CLOCK_ANOMALY, SCHEDULER_STALL, COMM_LOSS}\n#   evidence = FAULT_INJECTION_RESULT with subject_ref = 'NC#<name>'（fault code ではないので P_FAULT_CODE_UNKNOWN の対象外。P_FAULT_EVIDENCE_UNVERIFIED は下の期待 audit 内容で照合）\n#   期待 audit 内容: ARM_SWAP / ARM_MISSING → HEALTH_FAILED（check kind = CONTROLLER_IDENTITY | TOOL_IDENTITY | BASE_POSE_VERIFY）；BASE_TRANSFORM_TAMPER → FAULT R_PROFILE_MISBOUND（05 §3.4 (j)）；INTER_ARM_COLLISION → FAULT R_INTER_ARM_VIOLATION；CLOCK_ANOMALY → FAULT R_CLOCK_ANOMALY；SCHEDULER_STALL → FAULT R_DEADLINE_MISS | R_SAFETY_LAYER_LOST；COMM_LOSS → FAULT R_EXECUTOR_LOST | R_SAFETY_LAYER_LOST\n#   物理手順（R4-14）: ARM_SWAP = 左右 controller の接続を入替えて permit 要求；ARM_MISSING = 片腕 controller 停止；BASE_TRANSFORM_TAMPER = layout artifact の transform 改変；INTER_ARM_COLLISION = 交差する両腕 setpoint の command")
    d.rep("| `DeploymentEvidencePolicy` が MIN_SHADOW / MIN_CLOSED_LOOP / MIN_FAULT_INJECTION を含む（v0.2.1） | `P_EVIDENCE_POLICY_TOO_WEAK` |", "| `DeploymentEvidencePolicy` が MIN_SHADOW / MIN_CLOSED_LOOP / MIN_FAULT_INJECTION / MIN_NEGATIVE_CONTROLS を含む（v0.2.1・v0.2.6 R4-03） | `P_EVIDENCE_POLICY_TOO_WEAK` |")
    d.rep("FAULT_INJECTION_RESULT ∀ `fault_injection[]` with must_be", "FAULT_INJECTION_RESULT ∀ `fault_injection[]` および ∀ `negative_controls[]`（subject 'NC#<name>'・v0.2.6 R4-03） with must_be")
if gate("R4-04", "R3-21", "R4-16", "R4-19"):
    d.rep("class AcceptanceState(Enum):     PROPOSED | ACCEPTED | SUSPENDED | REVOKED     # REVOKED は終端。再使用 = 新 generation の新 record のみ", "class AcceptanceState(Enum):     PROPOSED | ACCEPTED | SUSPENDED | REVOKED     # REVOKED は終端。再使用 = 新 generation の新 record のみ\nclass AcceptanceRole(Enum):      SAFETY_APPROVER | CUSTODY_APPROVER | COMMISSIONING | INDEPENDENT_REVIEWER | OPERATOR | MONITOR   # v0.2.6（R4-04）: 閉じた語彙\nclass SuspensionReason(Enum):    INCIDENT | ISL_ANOMALY | IDENTITY_CHANGE | LAYOUT_CHANGE | BASELINE_SUPERSEDED | DIAGNOSTIC_FAILURE | CLOCK_ANOMALY | OPERATOR   # v0.2.6（R4-11）")
    d.rep("    approvals: tuple[tuple[str, str, str], ...]        # (role, operator_ref, t_iso8601)", "    approvals: tuple[tuple[AcceptanceRole, str, str], ...]   # (role, operator_ref, t_iso8601)。v0.2.6（R4-04）: operator_ref は role ごとに相異・role_registry で資格照合\n    role_registry_hash: str                # v0.2.6（R4-04）: operator_ref → 資格 role 集合の content-addressed registry（受理時に照合・profile preimage 外）\n    suspension_reason: SuspensionReason | None   # v0.2.6（R4-11）: state == SUSPENDED のとき必須")
    d.rep("- 検査: record の必須 field 欠落・generation 非単調・CLOSED_LOOP 許可で two-key 不足・`valid_until` None = `P_ACCEPTANCE_RECORD_INVALID`（受理拒否）。", "- 検査 `P_ACCEPTANCE_RECORD_INVALID`（record 書込時 + 05 §3.4 (i) / 条件 12 で head record に再評価・v0.2.6 R4-04 / R3-21 / R4-16）: 必須 field 欠落；generation が同一 profile_hash の直前 record + 1 でない；CLOSED_LOOP ∈ allowed_lease_modes ⇒ ∃ SAFETY_APPROVER ∧ ∃ CUSTODY_APPROVER で両者の `operator_ref` が相異かつ OPERATOR の operator_ref と相異かつ role_registry で資格あり；SHADOW のみ ⇒ ∃ COMMISSIONING ∧ ∃ INDEPENDENT_REVIEWER で相異；`valid_until` None または baseline `max_acceptance_validity_s` 超過；`cell_identity_hash ≠ H_WCJ(profile.cell)`（R4-09）；SUSPENDED で `suspension_reason` None。\n- **registry 規則（v0.2.6・R4-16 / R3-21）**: 新しい ACCEPTED record は必ず generation +1；REVOKED → 新 generation は SUSPENDED → ACCEPTED と同条件（原因除去 evidence + two-key）；SUSPENDED / REVOKED record は superseded record の hash field を複写し、approvals = 認可された一者または MONITOR、valid_until = superseded 値；head = registry append seq 最大；同一 record を 2 度 supersede する append は registry が拒否（fork なし）。\n- **two-key の位置づけ（v0.2.6・R4-19）**: 本 §8.3 の two-key は deployment 層の gate であり、frozen `evaluate_authority_grant` の `two_key_state` を満たすものでも代替するものでもない（常に AND）。")
    d.rep("1. **authority**: profile は authority を付与しない。", "1. **authority**: profile は authority を付与しない（§8.3 の受理 two-key も frozen `two_key_state` の代替ではない・v0.2.6 R4-19）。")
if gate("R4-05", "R3-12"):
    d.rep("| end-to-end 予算: `safety_heartbeat_timeout_s + detection + gateway 切替 + measured stop time ≤ baseline.safety_response_budget_s`（measured 値は CONTROLLER_ENVELOPE_MEASUREMENT / FAULT_INJECTION_RESULT の実測 artifact）（v0.2.5・OPP-11） | `P_SAFETY_BUDGET_EXCEEDED` |", "| end-to-end 予算（v0.2.5・OPP-11・v0.2.6 R4-05 / R3-12 で項の出所と評価点を固定）: `safety_heartbeat_timeout_s + FI.detection_to_safehold_worst_case_s + FI.gateway_switch_worst_case_s + CEM.stop_time_worst_case_s[stop_class] ≤ baseline.safety_response_budget_s`。FI = 当該 arm の R_SAFETY_LAYER_LOST#HEARTBEAT_TIMEOUT の FAULT_INJECTION_RESULT artifact（struct: audit_range / detection_to_safehold_worst_case_s / gateway_switch_worst_case_s / load_condition_ref）、CEM = 当該 arm の CONTROLLER_ENVELOPE_MEASUREMENT artifact（schema = ControllerEnvelope の field + `stop_time_worst_case_s: dict[StopClass → s]`）。評価点 = **第 2 評価点・`proposed_lease.mode == CLOSED_LOOP_AUTHORITY`**（登録時は評価しない） | `P_SAFETY_BUDGET_EXCEEDED` |")
if gate("R3-10", "R4-07", "R3-18", "R4-08"):
    old = d.line("規則: (1) 全検査は fail-closed（評価不能 = 違反）。")
    a = old.find("(2) profile の受理"); b = old.find("(4) `P_*` は certificate")
    assert a > 0 and b > a
    new = old[:a] + "(2) profile の受理 = 全 `P_*` = 0 を満たした profile_hash について `ProfileAcceptanceRecord(state = ACCEPTED)` を §8.3 registry に append すること（受理は評価点ではなく record 書込時の `P_ACCEPTANCE_RECORD_INVALID` 検査・v0.2.6 R4-08）。**評価点は 2 つ**: 第 1 評価点 = 登録時（構造・単調性・反循環・codec・`P_RESOURCE_UNATTESTED` / `P_EVIDENCE_POLICY_TOO_WEAK` / `P_ROBOT_MODEL_MISMATCH` / `P_FRAME_MISMATCH` / `P_ARM_*` / `P_TIMEOUT_ORDER`）／ 第 2 評価点 = **permit 発行時**（閉じた list・SSOT・v0.2.6 R3-18 / R4-08）= {`P_CALIBRATION_EXPIRED`, `P_EVIDENCE_UNBOUND`（当該 lease mode の集合・subject 単位・negative controls 含む）, `P_EVIDENCE_EXPIRED`, `P_EXPECTATION_UNCERTIFIED`, `P_TIMEOUT_CEILING`, `P_SAFETY_BUDGET_EXCEEDED`（CLOSED_LOOP）, `P_ENVELOPE_EXCEEDS_MEASURED`（CLOSED_LOOP）, `P_FAULT_EVIDENCE_UNVERIFIED`, `P_PREDICATE_HASH_MISMATCH`（全宣言 hash の再照合）, `P_ACCEPTANCE_RECORD_INVALID`（head record 再評価）} — 05 §3.4 (b) が `R_PROFILE_MISBOUND` として扱う。(3) 受理後の事実変化（期限切れ・evidence 失効・identity 変更）は profile を変えない。lease は §5 (b) の三層（deadline / event / 根拠付き周期診断）で無効化される（05 §4.5・v0.2.6 R3-10 / R4-07: 旧「次 permit まで継続」は撤回）。profile 起因で lease を無効化するのは §5 (a)・§5 (b)・`lease_max_duration_s`（05 INV-33）。" + old[b:]
    d.repline("規則: (1) 全検査は fail-closed（評価不能 = 違反）。", new)
if gate("R4-10", "R3-22"):
    d.rep("    baseline_id: str\n    approved_ceiling: dict", "    baseline_id: str\n    cell_id: str                           # v0.2.6（R4-10）: == profile.cell.cell_id（不一致 = P_TIMEOUT_CEILING）\n    layout_revision_ref: str               # v0.2.6（R4-10）: == profile.cell.layout_revision_ref\n    max_acceptance_validity_s: CanonicalDecimal   # v0.2.6（R4-16）: acceptance record の valid_until 上限\n    approved_ceiling: dict")
    d.rep("| 各 `RuntimeTimeouts` 値 ≤ `CellSafetyTimingBaseline.approved_ceiling[field]`（baseline = acceptance record の `timing_baseline_hash` が指す content-addressed artifact・§8.3。評価 = 受理時 + 第 2 評価点）（v0.2.5・OPP-11） | `P_TIMEOUT_CEILING` |", "| 各 `RuntimeTimeouts` 値 ≤ `CellSafetyTimingBaseline.approved_ceiling[field]` ∧ `baseline.cell_id == cell.cell_id ∧ baseline.layout_revision_ref == cell.layout_revision_ref`（baseline = acceptance record の `timing_baseline_hash` が指す artifact・§8.3。評価 = record 書込時 + 第 2 評価点）（v0.2.5・OPP-11・v0.2.6 R4-10） | `P_TIMEOUT_CEILING` |")
    d.rep("- **失効の権限（非対称）**: incident・ISL 異常・layout / tool / controller / safety-layer identity の変更のいずれでも、認可された一者または自動 monitor（05 DeploymentValidityMonitor）が即時 SUSPENDED にできる。解除は上記の再受理のみ。", "- **失効の権限（非対称）**: incident・ISL 異常・layout / tool / controller / safety-layer identity の変更・**timing baseline の supersession（bound する全 record・v0.2.6 R4-10 / R3-22）**・diagnostic 失敗・clock 異常のいずれでも、認可された一者または自動 monitor（05 DeploymentValidityMonitor）が `SUSPENDED(reason)` record を append する（記録が先・通知が後・R4-11）。cell 単位の trigger は同一 `cell_identity_hash` の全 record を停止する（R3-09）。解除は上記の再受理のみ。")
if gate("R4-11", "R3-05"):
    d.rep("    clearance_roles: tuple[tuple[str, str], ...]   #", "    on_validity_expired: EscalationTarget  # v0.2.6（R4-11）: VALIDITY_EXPIRED の既定 disposition（下表）を SAFE_STOP 側へのみ強化\n    clearance_roles: tuple[tuple[str, str], ...]   #")
    old = d.line("- lease 活性中に profile の前提が崩れた場合")
    d.repline("- lease 活性中に profile の前提が崩れた場合", old + "\n- **VALIDITY_EXPIRED の disposition 表（v0.2.6・R4-11 / R3-05・機械決定）**: 到達項目（05 `validity_deadline_source`）= CALIBRATION(kind = TCP) → SAFE_STOP；CALIBRATION(camera / force) → HOLD；EVIDENCE(SAFETY_LAYER_ACCEPTANCE | CONTROLLER_ENVELOPE_MEASUREMENT) → SAFE_STOP；EVIDENCE(その他) → HOLD；ACCEPTANCE → HOLD。event（`SuspensionReason`）= INCIDENT / ISL_ANOMALY / CLOCK_ANOMALY → SAFE_STOP；IDENTITY_CHANGE / LAYOUT_CHANGE / BASELINE_SUPERSEDED / DIAGNOSTIC_FAILURE / OPERATOR → HOLD。表に無い項目 = SAFE_STOP。`EscalationPolicy.on_validity_expired` は HOLD を SAFE_STOP へのみ強化。")
if gate("R4-12"):
    d.rep("CONTROLLER_ENVELOPE_MEASUREMENT = arms[].controller_firmware_ref（arm ごと） / TOOL_PAYLOAD_IDENTIFICATION = arms[].tool_payload.tool_id（arm ごと）", "CONTROLLER_ENVELOPE_MEASUREMENT = arms[].resource_id#robot_serial_ref（arm ごと・同一機種でも別 record・v0.2.6 R4-12） / TOOL_PAYLOAD_IDENTIFICATION = arms[].resource_id#tool_payload.tool_id（arm ごと）")
    d.rep("| `ControllerEnvelope` の各上限 ≤ `CONTROLLER_ENVELOPE_MEASUREMENT` record の測定値（artifact schema = ControllerEnvelope と同一 field）。", "| ∀ arm: `arms[].controller` の各上限 ≤ subject = `arms[].resource_id#robot_serial_ref` の `CONTROLLER_ENVELOPE_MEASUREMENT` record の測定値（arm ごとに自身の record・v0.2.6 R4-12。artifact schema = ControllerEnvelope の field + stop_time_worst_case_s）。")
if gate("R4-13"):
    d.rep("R_LEASE_DURATION_EXCEEDED, R_MANAGER_UNAVAILABLE}   # v0.2.3（CD-01）+ v0.2.4（R2-01）", "R_LEASE_DURATION_EXCEEDED, R_MANAGER_UNAVAILABLE}\n                 ∪ {R_CALIBRATION_EXPIRED_ACTIVE#DEADLINE, R_EVIDENCE_EXPIRED_ACTIVE#DEADLINE, R_PROFILE_SUSPENDED#EVENT, R_REGISTRY_UNAVAILABLE, R_INTER_ARM_VIOLATION, R_CLOCK_ANOMALY}   # v0.2.6（R4-13）\n                 # v0.2.3（CD-01）+ v0.2.4（R2-01）")
if gate("R4-14"):
    d.rep("| GATEWAY_CONFIG_MATCH | ENVELOPE_READBACK | CONTROLLER_IDENTITY | SAFETY_LAYER_IDENTITY\n", "| GATEWAY_CONFIG_MATCH | ENVELOPE_READBACK | CONTROLLER_IDENTITY | SAFETY_LAYER_IDENTITY | BASE_POSE_VERIFY\n#   v0.2.6（R4-14）: BASE_POSE_VERIFY = 実測 base pose と kinematic_layout の T_cell←base の差が baseline 許容内（arm ごと）。TOOL_IDENTITY = tool が報告する個体 serial と `tool_payload.tool_id`（個体 serial・機種 id では不可）の等値。identity 系 check（CONTROLLER_IDENTITY / TOOL_IDENTITY / SAFETY_LAYER_IDENTITY / BASE_POSE_VERIFY）は baseline diagnostic item として lease 中も周期再検証する\n")
    d.rep("{SAFETY_LAYER_HEARTBEAT, SAFETY_LAYER_IDENTITY, CONTROLLER_LIVENESS, ENVELOPE_READBACK, GATEWAY_CONFIG_MATCH, CONTROLLER_IDENTITY, TOOL_IDENTITY} の各 kind が ≥ 1", "{SAFETY_LAYER_HEARTBEAT, SAFETY_LAYER_IDENTITY, CONTROLLER_LIVENESS, ENVELOPE_READBACK, GATEWAY_CONFIG_MATCH, CONTROLLER_IDENTITY, TOOL_IDENTITY, BASE_POSE_VERIFY} の各 kind が ≥ 1（v0.2.6 R4-14 で BASE_POSE_VERIFY 追加）")
    d.rep("    tool_id: str\n", "    tool_id: str                           # v0.2.6（R4-14）: tool の個体 serial（機種 id ではない）— TOOL_IDENTITY の照合先\n")
if gate("R4-17"):
    d.rep("    min_separation_m: CanonicalDecimal     # > 0（P_INTER_ARM_RESTRICTION_MISSING）", "    min_separation_m: CanonicalDecimal     # > 0（P_INTER_ARM_RESTRICTION_MISSING）。v0.2.6（R4-17）: 距離の定義 = 両 arm の TCP 点間距離（link / hull は ISL 側）。RS71 §0 #2 の grasp span（`%s:24`・値は本 doc に書かない）以上に設定すると適合 grasp を全て拒否するため P_ENVELOPE_EMPTY で拒否" % RS71)
    d.rep("| AcceptedEnvelope 静的充足可能性（§4） | `P_ENVELOPE_EMPTY` |", "| AcceptedEnvelope 静的充足可能性（§4）。v0.2.6（R4-17）: `min_separation_m ≥ RS71 §0 #2 の grasp span` も明らかな不能として含む | `P_ENVELOPE_EMPTY` |\n| kinematic_layout の各 arm base の Y が RS71 §0 #2 の固定 base（`%s:24`・値は本 doc に書かない）と baseline 許容内で一致（v0.2.6・R4-17） | `P_BASE_LAYOUT_MISMATCH` |" % RS71)
    newP.append("`P_BASE_LAYOUT_MISMATCH`")
if newP:
    cl = d.line("`P_INTRINSIC_OVERRIDE` / `P_RATE_MISMATCH`"); d.repline("`P_INTRINSIC_OVERRIDE` / `P_RATE_MISMATCH`", cl + " / " + " / ".join(newP) + "（v0.2.6）")
newPT = []
if conf("R4-04"): newPT.append("| PT-39 | 同一 operator_ref が SAFETY_APPROVER と CUSTODY_APPROVER を名乗る record（v0.2.6） | `P_ACCEPTANCE_RECORD_INVALID`（operator 相異） |")
if any_conf("R3-08"): newPT.append("| PT-40 | DeploymentValidityMonitor を停止したまま profile を SUSPENDED（v0.2.6） | manager の head-check が周期内に TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED)・R_PROFILE_SUSPENDED |")
if any_conf("R4-15"): newPT.append("| PT-41 | wall-clock を過去へ戻して permit 要求（v0.2.6） | `R_CLOCK_ANOMALY`・permit 不発行 |")
if any_conf("R3-01", "R4-01"): newPT.append("| PT-42 | 両腕が互いへ向かう線分対（各単独では keep-out 外）の 1 command（v0.2.6） | INTER_ARM 項 FALSE（線分の対で評価）・R_INTER_ARM_VIOLATION |")
if newPT: d.insert_after_line("| PT-38 |", "\n".join(newPT))
rows06 = [("R3-01 / R4-01 / R3-16 / R3-02","INTER_ARM の評価対象・cmd.resource・term 実体化","§0 (b)・§2.1 comment・§4"),("R3-17 / R4-02","hash 列挙","§3"),("R4-03","MIN_NEGATIVE_CONTROLS を執行する P_* が無い・subject 文法","§6.1 `negative_controls`・§3"),("R4-04 / R3-21 / R4-16 / R4-19","two-key が role label の存在検査・fork・REVOKED 再利用・two-key の語","§8.3 `AcceptanceRole` / `role_registry_hash`・registry 規則・§9"),("R4-05 / R3-12","予算検査の artifact と評価点","§3 `P_SAFETY_BUDGET_EXCEEDED`"),("R3-10 / R4-07 / R3-18 / R4-08","§8.1 (2)(3) の旧文・第 2 評価点の list","§8.1"),("R4-10 / R3-22","baseline の cell 束縛・supersession","§8.3 baseline・§3 `P_TIMEOUT_CEILING`"),("R4-11 / R3-05","event の記録・disposition 表","§2.1 `on_validity_expired`・§5 (b) 表・§8.3 `SuspensionReason`"),("R4-12","arm 単位 evidence の subject","§6.1・§3"),("R4-13","MIN_FAULT_INJECTION の v0.2.5 code","§6.1"),("R4-14","物理変更の検出（base 再取付・同機種 tool・ISL hot update）","§2.1 BASE_POSE_VERIFY・tool_id 個体 serial・§3 必須集合"),("R4-17","min_separation の定義と 88 mm・固定 base の照合","§2.1・§3 `P_BASE_LAYOUT_MISMATCH`")]
tbl = [f"- **v0.2.6 fold（{NOW}）— round 3（R3 / R4 → V3 / V4）の確定 finding の fold**。記録 = `11_THREE_AXIS_REVIEW_RECORD_20260903.md` §9:", "", "| finding | 内容 | 変更節 | verdict |", "|---|---|---|---|"]
for i, c, s in rows06: tbl.append(f"| {i} | {c} | {s if any_conf(*i.split(' / ')) else '— 適用せず'} | {'; '.join(f'{x}={vstr(x)}' for x in i.split(' / '))} |")
d.insert_before_line("## 12. Review anchors", "\n".join(tbl) + "\n")
d.save()
for name in ("07_SUCCESSOR_CONTRACT_DECISION_20260903.md", "04_EVIDENCE_POLICY_IMPACT_ASSESSMENT_20260903.md", "08_INDEPENDENT_REVIEW_PLAN_20260903.md"):
    d = Doc(name); d.rep("(v0.2.5 REVIEW CANDIDATE)", "(v0.2.6 REVIEW CANDIDATE)"); d.rep("REVIEW CANDIDATE v0.2.5（", "REVIEW CANDIDATE v0.2.6（"); d.save()
print("applied:", applied); print("NOW =", NOW)
