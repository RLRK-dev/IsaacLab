# V5 verification — R5 findings against WMSO v0.2.6 REVIEW CANDIDATE（3 lens・adversarial・refute-first）

- verifier = V5（別 context・authority 無し・凍結物へ非接触・`eval_runs/` 未編集・書込み先 = `$OUT` のみ）; 実施 = 2026-09-05 23:04–23:2x UTC（`date -u` 実測: 開始 23:04:59 / 最終 grep 23:13:15 / JSON 書込 23:17:09 / 本報告書込 = 末尾）; 対象 commit = `f9fe9757`（branch `claude/v0-2-design-candidates-snqbmy`）
- 対象 finding = `$OUT/R5_findings_v026.json`（14 件・全件読了）+ `$OUT/R5_review_v026.md`（全読）。重複注記のため `$OUT/R6_findings_v026.json`（21 件）を skim。対象 doc = `$PKG/05_…`（1022 行・sha256 `596db986…264a` = SHA256SUMS 一致）／ `$PKG/06_…`（690 行・sha256 `e93112ff…fa5e` = 一致）。両 doc とも全行を `cat -n` で読了し、finding ごとに引用行を `sed -n` / `grep` で再確認した。
- 略号: `$PKG` = `eval_runs/troot_optE_dapg_wholeroute_scope_20260701/WMSO_EXACT_BASELINE_REVIEW_v0.2_20260903`、`$D` = `eval_runs/troot_optE_dapg_wholeroute_scope_20260701`、`$OUT` = `/tmp/claude-0/-home-user-IsaacLab/f88a4426-3d65-5cc2-ba28-87f4976e6a66/scratchpad/review4`、A = `$D/WMSO_D11A_CONTRACTS_V2_DESIGN_RSTECHLEAD2_20260719.md`、D0 = `$D/WMSO_D0_ARCHITECTURE_DRAFT_RSTECHLEAD2_20260718.md`、EP = `$D/WMSO_D11A_EVIDENCE_POLICY_V1_RSTECHLEAD2_20260719.md`、RS71 = `thread_isaac_lab/thread-vault/04-Specs/RS71-System-Spec-SSOT.md`、GPT-A = `$PKG/review_records/rs_consult/A_GPT56sol_20260904.md`。
- 拘束: 各 finding に text / logic / impact の 3 lens を独立に当て、**反証を試みる**（既定 `refuted = true`）。confirmed ⇔ 2 lens 以上が反証できず impact ≠ NOT_A_DEFECT。Rs 裁定（`11_` §8・GPT-A）の**設計方向は欠陥ではない** — 裁定に反対するだけの finding は impact lens で refuted、裁定を本文が正しく・整合的に・fail-closed に実現し損ねている finding は confirmed。陽性対照の注入 8 欠陥（`10_` §1: PC-05-A〜D / PC-06-E〜H）を再現するだけの finding は confirm しない（R5 の 14 件はいずれも該当せず — 旧 epoch 拒否遅延 / permit 再使用 / `∪ {consume_offer_id}` 欠落 / SHADOW actuation / bounds field / rate ≤ / safety False / escalation 緩和 のどれでもない）。

## 0. 手続記録（自己実測・`date -u` 順）

```text
$ date -u                                            → Sat Sep  5 23:04:59 UTC 2026
$ git log -1 --oneline                               → f9fe9757 WMSO v0.2.6: fold third review round (R3/R4 -> V3/V4) into 05 and 06
$ git branch --show-current                          → claude/v0-2-design-candidates-snqbmy（git status --short = clean）
$ date -u; bash $PKG/verify_exact_baseline_pins.sh   → 23:05:04 UTC / 11/11 PASS
    PASS D1.1-A DESIGN v2.11.2 (worktree)  00192d20…8aff / PASS EP v1.9 md c474acea…27e7 / PASS EP v1.9 json e63176af…1c6e /
    PASS D1.1-B DESIGN v13 5a1874d3…49a6 / PASS blob 1353430… / PASS blob ebe8154… / PASS blob sha256 ×2 /
    PASS at frozen commit 54f90a7d / PASS at frozen commit 07250f4a / PASS evidence_policy_definition_hash e7ca4309…3803
    policy_semver=1.9.0  HEAD=f9fe975783aa63f43989c4be7c7bb2ae544ba90f branch=claude/v0-2-design-candidates-snqbmy
$ python3 $PKG/check_review_candidate.py 05 --runtime  → FileNotFoundError（checker は path 引数を要する — 指示文の短縮形は不可）
$ date -u; python3 $PKG/check_review_candidate.py $PKG/05_WMSO_RUNTIME_SPEC_v0.2_REVIEW_CANDIDATE_20260903.md --runtime
                                                     → 23:05:19 UTC / citations=153 FAIL=0 WARN=11（全 C2 heuristic・R5 の自記と同一）
$ python3 $PKG/check_review_candidate.py $PKG/06_WMSO_INDUSTRIAL_PROFILE_v0.2_REVIEW_CANDIDATE_20260903.md --profile
                                                     → citations=63 FAIL=0 WARN=25（全 C2 heuristic）
$ sha256sum 05 06 ↔ $PKG/SHA256SUMS.txt              → 05 596db986236b8db121aeee2573c936e6f7c8ea2845d7b2dbf15c027e3f8f264a = 一致
                                                        06 e93112ffd8f347d39c7ae7c440c2970ed1499fd803ef5862a58391c04f4ffa5e = 一致
$ cat -n 05 | sed -n 1,1022p ／ cat -n 06 | sed -n 1,690p   → 全行読了（chunk 読み・23:05–23:10 UTC）
$ sed -n 270,274p / 316,320p / 352,359p / 362,366p D0；sed -n 353-355p / 381-382p / 536-538p A；sed -n 154-156p EP；sed -n 18,32p RS71
$ awk '/^## 8/…' $PKG/11_THREE_AXIS_REVIEW_RECORD_20260903.md；cat -n GPT-A | sed -n 1,80p；awk '/^## 1/…' $PKG/10_POSITIVE_CONTROL_RECORD_20260903.md
$ grep / sed 再測（finding 別・§2 に引用）                → 23:11–23:13 UTC
$ date -u → 23:17:09 UTC；V5_results_v026.json 書込 → python3 json.load OK / 14 id 一意・順序 R5-01..14 / schema key 一致
```

重複統合（R5 内）: 14 件を claim 単位で突合。R5-02（head-check 行 05:292 の副 case × fault / disposition の非 total）↔ R5-08（deadline 行 05:290 の ACCEPTANCE 項）↔ R5-13（06:507 の状態遷移欠落と良性新 generation の SAFE_STOP）は同じ「遷移行 → code / disposition」family だが対象行と claim が異なる。R5-04（`P_VALIDITY_UNBOUNDED` の評価点）↔ R5-14（同 check の kind 集合）も claim が異なる。**完全重複 0 件・全件 `merged_into = ""`**。別 reviewer R6 との重複（R5-01 ↔ R6-16 / R5-04 ↔ R6-11 / R5-08 ↔ R6-21 / R5-09 ↔ R6-12 / R5-10 ↔ R6-03 / R5-14 ↔ R6-17 一部）は各節に注記し、reviewer をまたぐ merge はしない。

## 1. 集計

| verdict | 件数 | id |
|---|---|---|
| **confirmed** | **13** | R5-01, 02, 03, 04, 05, 06（MEDIUM）／ R5-07, 08, 09, 10, 12, 13, 14（LOW） |
| refuted | 1 | R5-11（NOT_A_DEFECT） |
| merged | 0 | — |

severity 別 = CRITICAL 0 / HIGH 0 / MEDIUM 6 / LOW 7 / NOT_A_DEFECT 1。R5 の severity から変えたもの: **なし**（R5-11 は refuted）。全 confirmed の最小修正は **frozen 4 file の schema delta 0**（runtime / deployment 層の enum member・散文のみ。新 timeout field は足さない — R5-06 の `audit_liveness_s` は baseline diagnostic item へ置換）。R5 の提案 fix を置換したもの: R5-03（manager が registry へ SUSPENDED を書く部分 = OPP-13 裁定「read-only consumer」に反するため却下）・R5-06（新 `RuntimeTimeouts` field → baseline item）・R5-02（`SuspensionReason.MONITOR_LOST` 追加案 → 不要）。

## 2. Finding 別 verdict

### R5-01（MEDIUM・CONFIRMED）— SHADOW lease の validity deadline 到達の扱いが 05 内・05 ↔ 06 で分裂（R4-18 fold の未完）
- **sed 実測**: 05:290「`S_LEASE_ACTIVE` / `S_HEALTH_PENDING` / `S_BOUNDARY_WAIT`（**mode を問わず**・v0.2.6）」／ 05:427「到達 = TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED)（**mode を問わず**・保守側・v0.2.6 R4-18。SHADOW は actuation が無いので実害なし）」／ 05:742 INV-34「mode を問わず」 ↔ 05:822 OP-19「**SHADOW は次 permit 評価で可**」／ 05:341 失敗表「活性 **CLOSED_LOOP** lease は TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED | SAFE_STOP if 安全起因)」／ 06:341「**SHADOW_NON_AUTHORITY lease は actuation が無いため次 permit 評価で可**」／ 06:442 PT-13「**SHADOW lease は次 permit まで継続**（曝露 = actuation 無し）」／ 06:510「活性 **CLOSED_LOOP** lease を SafeHold（安全起因なら SafeStop）へ移す」。grep「次 permit」05 = 822 / 896（fold 履歴）、06 = 341 / 442 / 452 / 454 / 465（後 3 者は hash 差替えの話で無関係）/ 594 / 654（fold 履歴）。裁定: GPT-A:41「SHADOWはactuationがないため、次permit評価を**許容できます**」（許容 = 任意・必須ではない）。凍結: EP:155「SHADOW は定義上 非 authority」は actuation の有無のみ。
- **text lens（未反証）**: 引用 7 箇所すべて実在。§3.7 行・§4.1・INV-34 と、失敗表行・OP-19・06 §5 (b)・PT-13（test 期待）・06 §8.3 が SHADOW lease の deadline 到達に**逆の**挙動を規定する。PT-13 は規範の試験期待であって設計注記ではない。
- **logic lens（未反証）**: SHADOW lease L（profile P・TCP 期限 t_c）。05:290 に従えば t_c で TRANSFER_TO_SAFEHOLD(SAFE_STOP) + FAULT(R_CALIBRATION_EXPIRED_ACTIVE)・L 無効化；06:341 / PT-13 に従えば L は TERMINAL まで継続し次 permit が `P_CALIBRATION_EXPIRED` → `R_PROFILE_MISBOUND`。どちらも裁定の文面（許容）と両立するため、これは裁定への反対ではなく候補文書内の二重規範。
- **impact lens（未反証）**: 06 読みを採ると MIN_FAULT_INJECTION の `R_CALIBRATION_EXPIRED_ACTIVE#DEADLINE`（06:387・CLOSED_LOOP 前必須）が SHADOW commissioning で作れず `P_EVIDENCE_UNBOUND` → CLOSED_LOOP 受理が到達不能（確定済 R3-11 と同型）。05 読みを採れば R3-11 の fold（05:447「SHADOW でも admission 判定は行う」）と整合。実装者の選択で evidence の作成可否と `LEASE_INVALIDATED` record の有無が変わる。**MEDIUM**（R6-16 は LOW と付値・merge しない）。
- **verdict**: CONFIRMED / MEDIUM。
- **minimal fix（frozen delta 0・散文のみ）**: 05:822「SHADOW は次 permit 評価で可」→「SHADOW も mode を問わず deadline / event / head-check で無効化（記録上の SafeHold・§3.7・INV-34。裁定の『許容』は採らず保守側に統一）」；05:341「活性 CLOSED_LOOP lease は」→「活性 lease は（mode を問わず）」；06:341 SHADOW 文 →「SHADOW_NON_AUTHORITY lease も同じ三層で無効化される（記録上の SafeHold・05 §3.7）」；06:442 PT-13 の SHADOW 文 →「SHADOW lease も deadline 到達で TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED)（記録上）— `R_CALIBRATION_EXPIRED_ACTIVE#DEADLINE` の commissioning evidence はここで作る」；06:510「活性 CLOSED_LOOP lease を」→「活性 lease（mode を問わず）を」。06:654 の fold 履歴行には「（v0.2.6 で mode を問わずへ変更）」の注記可。

### R5-02（MEDIUM・CONFIRMED）— head-check 行（05:292）の副 case × (fault, disposition) 写像が total でなく、manager 単独検出には 06 §5 (b) 表の key が無い
- **sed 実測**: 05:292 全文 = trigger 4 副 case（`head(registry, lease.profile_hash)` の `record_hash ≠ … ∨ state ≠ ACCEPTED`／ registry が head-check 周期を超えて読めない／ monitor heartbeat が `VALIDITY_MONITOR_LIVENESS` 超過で欠落／ baseline diagnostic item の失敗）、CAS 列 =「TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED | SAFE_STOP **per doc 06 §5 (b) 表**)」、記録列 =「FAULT(R_PROFILE_SUSPENDED | R_REGISTRY_UNAVAILABLE | R_EVIDENCE_EXPIRED_ACTIVE), DISPOSITION」（副 case 対応なし）。grep「線形化」on 05:292 = **0**（05:240 条件 12 は「registry の線形化読み」を宣言）。06:342 表の key = `validity_deadline_source` の kind と `SuspensionReason`、「表に無い項目 = SAFE_STOP」。06:476 `SuspensionReason` = INCIDENT | ISL_ANOMALY | IDENTITY_CHANGE | LAYOUT_CHANGE | BASELINE_SUPERSEDED | DIAGNOSTIC_FAILURE | CLOCK_ANOMALY | OPERATOR（monitor 喪失・evidence 失効の member 無し）。06:511「AuthorityManager は read-only consumer」／ 06:509 append は認可者か monitor。05:342 `R_REGISTRY_UNAVAILABLE` 行 disposition = HOLD／ 05:343 `R_CLOCK_ANOMALY` 行 = HOLD / SAFE_STOP。05:298「各 timeout / failure に disposition を必ず割り当てる — fail-closed 既定」／ 05:653 規則 2「fault は常に disposition を伴う」。凍結: D0:318（heartbeat 欠落 ⇒ alive を仮定しない・failed ⇒ safe-stop）。
- **text lens（未反証）**: 行の記述どおり。4 副 case に対し 3 code が非対応で列挙され、disposition 源は副 case 2〜4 に key を持たない。
- **logic lens（未反証）**: (1) monitor 欠落 → manager CAS；fault は 3 候補のどれの意味にも当たらず、disposition key 無し → SAFE_STOP。(2) registry 不読（周期超過）→ 05:342 = HOLD、05:292 経由 06:342 = SAFE_STOP — 同一事象に 2 disposition（05:653 規則 2 の一意性・05:298 の割当義務に反する）。(3) manager 検出の diagnostic 失敗は record を伴わず、`DIAGNOSTIC_FAILURE → HOLD` に到達しない。(4) 05:291 event 行の `R_EVIDENCE_EXPIRED_ACTIVE` に対応 reason 無し（06:341 は evidence 失効を deadline 経路に振る）→ 到達不能。
- **impact lens（未反証）**: 既定 SAFE_STOP は保守側だが、HOLD / SAFE_STOP は解除経路（§5.4: SAFE_STOP = operator clearance）を変え、05:342 を読む実装は HOLD・05:292+06:342 を読む実装は SAFE_STOP と割れる。D0:318 の「欠落を名前付き処置へ写す」要求が本文どおりに実装できない。R6-21（ACCEPTANCE code）・R5-08 / R5-13 と family が同じだが claim は別。**MEDIUM**。
- **verdict**: CONFIRMED / MEDIUM。
- **minimal fix（frozen delta 0）**: 05:292 を副 case 別に分割し各々に (fault, disposition) を固定 — (a) head.state ∈ {SUSPENDED, REVOKED} → `R_PROFILE_SUSPENDED`・disposition = head の `suspension_reason` を 06 表で引く（REVOKED = 表に無い = SAFE_STOP のまま）；(a′) head.state == ACCEPTED ∧ `record_hash ≠` → R5-13（HOLD）；(b) registry 不読 → `R_REGISTRY_UNAVAILABLE` → HOLD（05:342 と同一）；(c) monitor heartbeat 欠落 → `R_REGISTRY_UNAVAILABLE`（FAULT payload trigger = MONITOR_LOST）→ HOLD（`SuspensionReason.MONITOR_LOST` は不要 — 死んだ monitor は record を書けず、manager は 06:511 のとおり read-only）；(d) baseline diagnostic item 失敗 → drift 項目は `R_CLOCK_ANOMALY` → SAFE_STOP（05:343 / 06:342 CLOCK_ANOMALY 行）、他項目（identity 再検証 / proof-test）は `R_EVIDENCE_EXPIRED_ACTIVE`（payload trigger = DIAGNOSTIC:<item>）→ 06 表 DIAGNOSTIC_FAILURE 行（HOLD・`on_validity_expired` で強化可）。「registry の読みは条件 12 と同じ線形化読み」を行に明記。05:291 event 行の FAULT から `R_EVIDENCE_EXPIRED_ACTIVE` を外す。(c)(d) の disposition 値は起草者既定として OP-19 残 open に記録（Rs が強化可）。

### R5-03（MEDIUM・CONFIRMED）— permit 発行前提に monitor liveness / diagnostic 通過が無く、monitor 欠落起因の SAFEHOLD(VALIDITY_EXPIRED) が即再 lease される（flapping）
- **sed 実測**: 05:191 (a)–(l) に monitor liveness / diagnostic 通過の項なし（grep `VALIDITY_MONITOR_LIVENESS` 05 = 88 / 292 のみ；(a) の check kind = 06:67 の 9 kind に monitor 無し・grep monitor on 06:67 = 0；(b) = 06:422 閉じた list に該当項なし；(l) は wall/mono drift のみ）。05:534「VALIDITY_EXPIRED では新 permit が **§3.4 (b)(i) で失効原因の解消を要求する**。event 起因なら registry の SUSPENDED record が … 置換されるまで (i) が拒否する」。06:511 manager read-only → manager 検出の monitor 欠落には SUSPENDED record が書かれない。05:296 / 05:560（Orchestrator 無状態・SAFEHOLD 起動は B1 skip）。凍結: D0:318・D0:273-274。裁定: GPT-A:43 三層のうち層 2 = event 通知。
- **text lens（未反証）**: 前提 (a)–(l) の grep で欠落を確認。§5.4:534 の主張は monitor 欠落副 case に当てはまらない。
- **logic lens（未反証・trace 再現）**: t0 monitor crash → t0+L head-check（05:292）→ TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED) SUCCESS → §5.4 VALIDITY_EXPIRED 行は clearance 不要（完全経路のみ）→ 新 Orchestrator が B0 から再開 → 新 permit: (a) pass・(b) pass・(i) head = ACCEPTED（不変）・(l) pass → 発行 → CAS 条件 1–12 pass（9 = ISL health・12 = ACCEPTED）→ L2 活性・command admit → t0+2L で再 SAFEHOLD → 以後 L 周期で actuation 窓が繰り返される。event 検出層（裁定の層 2）が死んだまま actuation が再開する。同型: health check kind でも drift でもない diagnostic 項目（proof-test）の失敗。
- **impact lens（未反証）**: 裁定 OP-19 の三層実現が層 2 欠落時に崩れ、§5.4 の「自動再 lease は無い」が偽になる。fail-closed bar 未達。**MEDIUM**。
- **verdict**: CONFIRMED / MEDIUM。
- **minimal fix（frozen delta 0）**: 05:191 に前提 (m) を追加 —「(m) 監視系の生存: DeploymentValidityMonitor の直近 heartbeat が baseline 項目 `VALIDITY_MONITOR_LIVENESS` 内 ∧ baseline `diagnostic_items` の全項目が直近周期内に pass（評価不能 / 欠落 = FALSE）。違反 = `R_REGISTRY_UNAVAILABLE`（FAULT payload trigger = MONITOR_LOST | DIAGNOSTIC:<item>）・permit 不発行」；05:534「§3.4 (b)(i) で」→「§3.4 (b)(i)(m) で（monitor 欠落 / diagnostic 失敗は (m) が拒否）」；05:342 の効果列に「permit 発行時の (m) 違反も本 code」。⛔ R5 提案の後半（manager 検出を MONITOR role で registry へ SUSPENDED append）は**採らない** — AuthorityManager を registry writer にすることになり 06:511 と OPP-13 裁定（GPT-A:37「AuthorityManagerはread-only consumer」）に反する。(m) だけで flap は閉じる。

### R5-04（MEDIUM・CONFIRMED）— `P_VALIDITY_UNBOUNDED`（required record 側）に評価点が無く、無期限 evidence が permit を通る
- **sed 実測**: 06:296「`CalibrationRef.valid_until_iso8601`、および required record のうち kind ∈ {CALIBRATION_RECORD, SAFETY_LAYER_ACCEPTANCE} の `valid_until_iso8601` が None → `P_VALIDITY_UNBOUNDED`」／ 06:364「None の意味 = 失効なし（期限検査の対象外）。ただし … None 不可（P_VALIDITY_UNBOUNDED）」／ 06:422 第 1 評価点 =「構造・単調性・反循環・codec・`P_RESOURCE_UNATTESTED` / `P_EVIDENCE_POLICY_TOO_WEAK` / `P_ROBOT_MODEL_MISMATCH` / `P_FRAME_MISMATCH` / `P_ARM_*` / `P_TIMEOUT_ORDER`」、第 2 評価点 =「**閉じた list・SSOT**」・grep `P_VALIDITY_UNBOUNDED` on 06:422 = **0**／ 05:191 (b) は「doc 06 §8.1 (2) が列挙する全 code … SSOT」／ 05:184 `validity_deadline_source` は deadline を持つ項目のみ／ 06:295 `P_EVIDENCE_EXPIRED` は値がある場合の比較／ 06:384 MIN_CLOSED_LOOP に CALIBRATION_RECORD（CLOSED_LOOP のみ必須）／ 06:459 PT-32 = CalibrationRef 側のみ。凍結: A:354（UNKNOWN / 欠落 = DENY）。
- **text lens（未反証・一部弱化）**: CalibrationRef 側は第 1 評価点の「構造」に読めるが、record 側は第 1 list にも閉じた第 2 list にも無い。
- **logic lens（未反証）**: CALIBRATION_RECORD は CLOSED_LOOP で初めて required（06:384）= 第 2 評価点でしか照合されず、閉じた list が本 check を含まないため `valid_until = None` の record は `P_EVIDENCE_EXPIRED`（None 除外）を通り、`validity_deadline_source` に項を残さず lease 中に一切失効しない。後から追加された SAFETY_LAYER_ACCEPTANCE record も同じ。06:342 はその失効を SAFE_STOP 級と分類している。
- **impact lens（未反証）**: R2-18 の fail-closed 既定が、それが効くべき唯一の点で未執行（fail-open 方向・安全関連 evidence）。R6-11（MEDIUM）と重複・merge しない。**MEDIUM**。
- **verdict**: CONFIRMED / MEDIUM。
- **minimal fix（frozen delta 0）**: 06:422 第 2 評価点 list に「`P_VALIDITY_UNBOUNDED`（当該 lease mode の required set のうち kind ∈ {CALIBRATION_RECORD, SAFETY_LAYER_ACCEPTANCE} の record 側）」を追加；第 1 評価点 list に「`P_VALIDITY_UNBOUNDED`（CalibrationRef 側 + MIN_SHADOW の SAFETY_LAYER_ACCEPTANCE）」を明記；任意で PT-32b（CALIBRATION_RECORD を None で登録し CLOSED_LOOP permit → `P_VALIDITY_UNBOUNDED` → `R_PROFILE_MISBOUND`）。05:191 (b) は変更不要。

### R5-05（MEDIUM・CONFIRMED）— TRANSFER_TO_EXECUTOR の事後条件が `safehold_disposition` を None に戻さない（R3-13 fold の未完）
- **sed 実測**: 05:243「`AuthorityState := (EXECUTOR(executor_id), expected.control_epoch + 1, new_lease_id, expected.consumed_offer_ids ∪ {consume_offer_id}, None, False, expected.seq + 1)`」= 7 要素・`safehold_disposition` 無し。grep `safehold_disposition` 05 = 139（型 comment「owner.kind == EXECUTOR では None」「条件 1 の等値比較の対象外」）/ 143（初期 HOLD）/ 249（TRANSFER_TO_SAFEHOLD と no-op で設定・max 更新）/ 518（(iii) が線形化読みで参照）/ 777・947・988（test / fold）。05:246「SafeHold 活性: … 最初の admissible command が admit されるまで `SafeHold`」／ 05:528 §5.3「gateway 出力は (iii) SafeHold」／ 05:496 SafeStop = stop-class 要求・06:66 `StopClass` = CONTROLLED_STOP | POWER_REMOVED_STOP。
- **text lens（未反証）**: reset は dataclass comment にしか無く、「事後条件（同一線形化点）」の明示 list から欠落。R3-13 の fold は SAFEHOLD 側の設定のみ足した。
- **logic lens（未反証）**: SAFEHOLD(SAFE_STOP)（例: TCP 期限）→ clearance → TRANSFER_TO_EXECUTOR SUCCESS で durable 値は SAFE_STOP のまま → L2 の最初の admissible command まで out = (iii)（05:246）→ 05:518 で **SafeStop**（05:246 / §5.3 の SafeHold と矛盾）。boundary_wait / pending_invalidate / restart_pending の各窓も同様。
- **impact lens（未反証）**: 方向は保守側だが、SAFE_STOP hold 後の全 lease で出力関数 §5.2 が状態機械の本文と食い違い、lease 開始ごとに stop-class 要求（POWER_REMOVED_STOP を含む）が出る。宣言不変量（05:139）を実現する遷移が無い。**MEDIUM**（R3-13 と同格）。
- **verdict**: CONFIRMED / MEDIUM。
- **minimal fix（frozen delta 0）**: 05:243 の tuple の後に「+ `safehold_disposition := None`（同一線形化点・durable）」；INV-01（05:709）に「`state.owner.kind == SAFEHOLD ⇔ state.safehold_disposition != None`」を併記。MARK_BOUNDARY_WAIT（05:251「他の要素不変」）は変更不要。

### R5-06（MEDIUM・CONFIRMED）— AuditRecorder の不能に fault code・失敗表行・fail-closed 規則が無い
- **sed 実測**: grep `R_AUDIT` 05 / 06 = 0 / 0；失敗表 05:302-347 に recorder 行なし（recorder / audit の hit 6 件は payload 記載のみ）；`RuntimeFaultCode` 05:632-647 に recorder 系 code なし；05:144「通知・audit・`permit.state := CONSUMED` はその後にのみ行う」（3 者の順序は未規定）；05:725 INV-17 違反 =「記録欠落 = fault」（code 名なし）；05:298「各 timeout / failure に disposition を必ず割り当てる — fail-closed 既定」；05:87 AuditRecorder = 構成要素（状態 = 記録列）；05:541 clearance record = `RuntimeAuditRecord`；06:360 FI / health evidence は audit 範囲に束縛。凍結: A:537 は content-hash **方式**のみ defer（存在義務は defer せず）。
- **text lens（未反証）**: 名前付き構成要素に失敗行が無い。
- **logic lens（未反証・一部有界）**: TRANSFER_TO_EXECUTOR の durable commit 完了（= SUCCESS・05:144）→ `AuditRecorder.append(EPOCH_TRANSITION)` 失敗 → 「recorder 不能 ⇒ 通知保留 / CONSUMED 化保留 / SAFEHOLD」の規則が無く L2 は EPOCH_TRANSITION / LEASE_ACTIVATED / PERMIT_CONSUMED / COMMAND_ADMITTED 無しで走る。INV-17 / T-17 は事後にしか検出できず code 無し（INV-27 の逆方向）。**verifier の反証試行**: 前提 (l)（05:191）は「直前の PERMIT_ISSUED の wall より単調」の照合に recorder 読みを要し、不読 = 評価不能 = FALSE（§9 既定）→ `R_CLOCK_ANOMALY` → **新 permit は出ない**。よって曝露は現 lease（≤ `lease_max_duration_s + boundary_dwell_s`）に有界 — 「何も fail-closed にしない」は permit 経路については部分反証、活性 lease の無記録は残る。
- **impact lens（未反証）**: doc 自身の fail-closed bar（05:298）に反し、clearance record・FI evidence が依存する recorder の喪失に名前が無い。R3-07（活性 lease 中の構成要素喪失に遷移が無い）と同型で **MEDIUM**（(l) 有界を注記。LOW も論じ得る）。
- **verdict**: CONFIRMED / MEDIUM。
- **minimal fix（frozen delta 0・runtime 層 enum + 散文）**: `RuntimeFaultCode` に `R_AUDIT_UNAVAILABLE`；失敗表 1 行（検出者 = AuthorityManager / CommandGateway；効果 =「CAS SUCCESS の通知・`permit.state := CONSUMED`・HealthConfirmation 受理は当該 record の durable append 後（05:144 に追記）；recorder 不能が baseline diagnostic item `AUDIT_RECORDER_LIVENESS` の周期を超えれば head-check 行 (d) と同経路で TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED)」；disposition = HOLD）；INV-17 違反列に code 名；06 MIN_FAULT_INJECTION に `R_AUDIT_UNAVAILABLE`。**R5 の新 `RuntimeTimeouts.audit_liveness_s` は採らず baseline diagnostic item に置換**（新 timeout field は `P_TIMEOUT_CEILING` / `P_TIMEOUT_ORDER` / baseline ceiling へ波及し、OP-19 層 3 の器で足りる）。gateway が COMMAND_ADMITTED の append 失敗で admission を同期停止するかは設計選択 — OP-5 の注記として記録し本文にしない。

### R5-07（LOW・CONFIRMED）— 条件 2 の失効境界が条件 3 / 6・失敗表と 1 点ずれ、CAS 時の validity / age 到達に効果・disposition の行が無い
- **sed 実測**: 05:230「`now < permit.effective_expires_at`」（strict・優先順: ACK → `R_ACK_MISBOUND` / age → `R_AUTHORITY_DECISION_ABSENT` / validity → `R_*_EXPIRED_ACTIVE` / `R_PROFILE_SUSPENDED` / ttl → `R_PERMIT_EXPIRED`）／ 05:231「`now ≤ ack.valid_until`」／ 05:234「`now − decision.t_mono ≤ decision_max_age_s`」／ 05:303「CAS 時点で ACK 失効（`now > ack.valid_until`・条件 3）」／ 05:340 効果 =「TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED | SAFE_STOP)・lease 無効」／ 05:306「permit 失効 → `R_PERMIT_EXPIRED` → permit VOID → RESELECT」／ 05:715 INV-07「`ack.valid_until ≥ permit.issued_at`」。
- **text lens（未反証）**: 演算子どおり。`now == ack.valid_until` で条件 2 偽（`R_ACK_MISBOUND`）・条件 3 真、05:303 の条件はこの点を含まない。
- **logic lens（未反証）**: 第 2 case は permit の profile が活性 lease と異なるとき（S_BOUNDARY_WAIT で profile A → B）に構成可: B の calibration 期限が CAS 前に到達 → 条件 2 → `R_CALIBRATION_EXPIRED_ACTIVE` REJECTED — 05:340 の「lease 無効」は L1 に適用されず、permit VOID / RESELECT を述べる行が無い。CAS 未試行の ledger 側失効は 05:306 の汎用行に自然に写るが、同一原因で CAS 時の code と食い違う。
- **impact lens（未反証・LOW）**: 方向は保守側（A:354）。測度零の境界と効果 / disposition 行の欠落 = 一意性欠陥（05:653 規則 2）のみ。**LOW**。
- **verdict**: CONFIRMED / LOW。
- **minimal fix**: 05:231 → `now < ack.valid_until`；05:234 → `< decision_max_age_s`；05:303 → `now ≥ ack.valid_until`；失敗表 1 行「CAS 条件 2 の validity / decision-age 項到達（活性 lease と別 profile / generation の permit）| AuthorityManager | 条件 2 の優先順の code | CAS REJECTED・permit VOID・状態不変 | RESELECT（新 permit は (b)(e)(i) で原因解消を要求）」；05:306 に「= `effective_expires_at` 到達（CAS 未試行・どの項でも本 code）」。INV-07 は不変。

### R5-08（LOW・CONFIRMED）— `validity_deadline_source.kind = ACCEPTANCE` の到達に fault code が割り当てられていない
- **sed 実測**: 05:184「kind ∈ {CALIBRATION, EVIDENCE, ACCEPTANCE}」・「到達時の fault と disposition は kind / id から doc 06 §5 (b) の表で決まる（表に無い = SAFE_STOP）」／ 06:342「ACCEPTANCE → HOLD」／ 05:290 記録列「FAULT(R_CALIBRATION_EXPIRED_ACTIVE | R_EVIDENCE_EXPIRED_ACTIVE)」／ 05:340 同 2 code／ 05:742 INV-34 違反列「R_CALIBRATION_EXPIRED_ACTIVE / R_EVIDENCE_EXPIRED_ACTIVE / **R_PROFILE_SUSPENDED**」／ 05:230 条件 2「validity なら … / `R_PROFILE_SUSPENDED`」。
- **text lens（未反証）**: 遷移行・失敗行の列挙が INV-34 / 条件 2 の割当を欠く。
- **logic lens（未反証）**: acceptance の `valid_until` に活性 lease 中に到達 → 05:290 発火 → FAULT の `fault` は 2 候補のどちらでもない（実装者は INV-34 から `R_PROFILE_SUSPENDED` を自然に選ぶ — 列挙漏れ）。副 kind の解決は id → content-addressed で不変な profile の `calibration_refs` / evidence store で決定論的（wall-clock 再読なし）、不読 = 表に無い = SAFE_STOP（05:184）で fail-closed だが暗黙。
- **impact lens（未反証・LOW）**: 内部 totality のみ・凍結矛盾なし。R6-21 と重複。**LOW**。
- **verdict**: CONFIRMED / LOW。
- **minimal fix**: 05:290 FAULT 列・05:340 fault 列に「`R_PROFILE_SUSPENDED`（ACCEPTANCE 到達）」；05:184 に「副 kind は id を lease.profile_hash の profile `calibration_refs`（CALIBRATION）/ evidence store（EVIDENCE の record kind）で解決する（profile は content-addressed で不変・wall-clock 再読なし）；解決不能 = 表に無い = SAFE_STOP」。

### R5-09（LOW・CONFIRMED）— 06 §5 の lease 写像文が「`AcceptedEnvelope.terms` の 3 項」のまま（R3-02 fold の未完）
- **sed 実測**: grep「の 3 項」06 = 340 のみ（「`AcceptedEnvelope.terms` の 3 項 ← §4」）／ 06:323-327 = INTRINSIC + CONTROLLER / DEPLOYMENT_WORKSPACE / SAFETY_RESTRICTION / INTER_ARM／ 06:333「profile 4 項（… INTER_ARM・v0.2.6）」／ 05:367 enum 5 member／ 05:762 T-13「× 5」／ 05:806 OP-2「profile 4 term」。
- **text / logic lens（未反証）**: 数の不一致。06:340 だけを読む実装は INTER_ARM を lease に写さない（term object が無ければ 05:463 の「評価不能 = FALSE」も働かない）。
- **impact lens（未反証・LOW）**: 規範の写像文の編集残差。R6-12 と重複。**LOW**。
- **verdict**: CONFIRMED / LOW。
- **minimal fix**: 06:340「の 3 項 ← §4」→「の 4 項（CONTROLLER / DEPLOYMENT_WORKSPACE / SAFETY_RESTRICTION / INTER_ARM・v0.2.6）← §4」。

### R5-10（LOW・CONFIRMED）— 06 MIN_FAULT_INJECTION の `#DEADLINE` / `#EVENT` subject を照合する 05 §8 の trigger 副因 payload が 2 code 分しか無い（R4-13 fold の 05 側未完）
- **sed 実測**: 05:700「FAULT record の payload は trigger 副因を持つ — `R_SAFETY_LAYER_LOST`: HEARTBEAT_TIMEOUT | HEALTH_FAILED、`R_BOUNDARY_DWELL_EXCEEDED`: DWELL_TIMEOUT | ATTEMPTS_EXHAUSTED」（2 code のみ）／ 06:387「∪ {R_CALIBRATION_EXPIRED_ACTIVE#DEADLINE, R_EVIDENCE_EXPIRED_ACTIVE#DEADLINE, R_PROFILE_SUSPENDED#EVENT, …}（v0.2.6 R4-13）」／ 06:389「subject_ref = '<code>#<timeout_trigger>' … 05 §8 FAULT payload の trigger 副因と照合」／ 06:297 `P_FAULT_EVIDENCE_UNVERIFIED`。
- **text lens（未反証）**: 05 は 3 subject の副因 payload を義務づけていない。
- **logic lens（未反証）**: commissioning で TCP 期限を発火 → FAULT(R_CALIBRATION_EXPIRED_ACTIVE) に DEADLINE 副因なし → 06:389 の厳密照合で不一致 → `P_FAULT_EVIDENCE_UNVERIFIED` → CLOSED_LOOP 到達不能（fail-closed 側）；照合を緩めれば `R_PROFILE_SUSPENDED` の 4 経路（event / head-check / CAS 条件 12 / permit）を `#EVENT` で区別できない。
- **impact lens（未反証・LOW）**: R4-13 の 05 側 fold 漏れ・over-closed 方向。R6-03（subject 文法 vs `FaultInjectionRequirement` も問う）と重複。**LOW**。
- **verdict**: CONFIRMED / LOW。
- **minimal fix**: 05:700 の列挙に「`R_CALIBRATION_EXPIRED_ACTIVE` / `R_EVIDENCE_EXPIRED_ACTIVE`: DEADLINE | HEAD_CHECK | DIAGNOSTIC:<item>（R5-02 (d)）、`R_PROFILE_SUSPENDED`: EVENT | HEAD_CHECK | CAS_COND12 | PERMIT」。06:389 は不変。

### R5-11（NOT_A_DEFECT・REFUTED）— `pending_invalidate` の deadline 副 case は gateway が検出できず空文、manager の deadline 検出遅延は未束縛
- **sed 実測**: 05:517「`pending_invalidate`（VALIDITY_EXPIRED の deadline / event を含む・v0.2.6 R3-06）= gateway が TRANSFER_TO_SAFEHOLD の trigger を検出してから」／ 05:427 `ActiveAuthorityLease.validity_deadline_mono`（permit から複写）／ 05:82 gateway admission =「`cmd.executor_id == lease.executor_id` ∧ `AcceptedEnvelope` 連言 ∧ command 種別 = `lease.control_mode`」・§5.2 (ii)「mode == CLOSED_LOOP_AUTHORITY」= gateway は admission ごとに lease（05:415「不変記録」）を解決する／ 05:447 gateway は `command_deadline_s` / `inter_command_jitter_s` を単調 clock で計時済／ D0:354-355 単一 origin の単調 clock／ 05:732 INV-24。
- **text lens（反証）**: 「`validity_deadline_mono` は AuthorityState にも gateway の宣言状態にも無い」は AuthorityState（05:122-139）については正しいが、datum は gateway が admission のたびに読む lease record に在る。R3-06 の句は空文ではなく、データが揃った暗黙の gateway 義務。
- **logic lens（反証）**: gateway の自己検出 = 単一 origin の単調 clock の now と `lease.validity_deadline_mono` の比較。R5 の代替 fix「gateway の lease copy に `validity_deadline_mono` を持たせる」は現行本文そのもの。manager 側の timer 粒度は実装 / RT0 実測（D0 §G provisional・OP-19 残 open）であり INV-24 が manager 単独窓を既に束縛する。
- **impact lens（反証）**: NOT_A_DEFECT。
- **verdict**: REFUTED。任意の明文化（欠陥修正ではない）: 05:517 に「gateway は lease（不変 record）の `validity_deadline_mono` を単一 origin の単調 clock で比較して deadline trigger を自己検出する」を 1 句。

### R5-12（LOW・CONFIRMED）— cell 単位停止の兄弟検査が (i) のみで、条件 12 / head-check に無く、registry 伝播窓が未束縛
- **sed 実測**: 兄弟句は 05:191 (i) のみ（「同一 `cell_identity_hash` を持つ他 record が原因未解消の SUSPENDED なら本 profile も `R_PROFILE_SUSPENDED`」）；grep `cell_identity` on 05:240 / 05:292 = 0 / 0；06:509「cell 単位の trigger は同一 `cell_identity_hash` の全 record を停止する（R3-09）」（原子性は未記載）；06:513 に「原因解消」field なし。
- **text lens（主張の主部は未反証・副部は反証）**: 条件 12 / head-check が自 head のみなのは事実。「原因未解消」を判定する field が無い、は反証 — head.state ∈ {SUSPENDED, REVOKED}（新 generation の ACCEPTED で置換されていない = 06:507 / 514）として機械判定可能。ただし本文はその語で書いていない。
- **logic lens（未反証）**: INCIDENT で P1 が SUSPENDED、兄弟 P2 への registry 伝播が非原子なら、伝播前に発行済の P2 permit は条件 12（P2 head = ACCEPTED）を通り L2 活性；P1 の event の profile-scoped 再試行（05:249）は L2 の profile_hash と一致せず、L2 は P2 自身の SUSPENDED record まで走る。窓 = 伝播遅延（本文に上限なし）。
- **impact lens（未反証・LOW）**: 曝露は registry 内部操作（通常 1 append batch）で小さいが、原子性も条件 12 / head-check の兄弟検査も書かれていない。**LOW**。
- **verdict**: CONFIRMED / LOW。
- **minimal fix**: 05:191 (i) の兄弟句 →「同一 `cell_identity_hash` を持つ他 profile の head record が state ∈ {SUSPENDED, REVOKED}（新 generation の ACCEPTED で置換されていない）なら本 profile も `R_PROFILE_SUSPENDED`」；同句を条件 12（05:240）と head-check 副 case (a)（05:292）に追加；06:509 に「cell 単位の停止は同一 append batch（原子・連続 seq）で全兄弟 record に適用する」。

### R5-13（LOW・CONFIRMED）— 06 §8.3 の状態遷移に ACCEPTED → ACCEPTED(gen+1) が無く、良性の新 generation を head-check が SAFE_STOP で扱う
- **sed 実測**: 06:507「PROPOSED → ACCEPTED; ACCEPTED → SUSPENDED; SUSPENDED → ACCEPTED は新 generation の新 record; ACCEPTED / SUSPENDED → REVOKED」（ACCEPTED → ACCEPTED(gen+1) 無し）／ 06:514「新しい ACCEPTED record は必ず generation +1」／ 05:191 (i)「追加 evidence = 新 generation」／ 05:292 trigger「`record_hash ≠ lease.acceptance_record_hash` ∨ state ≠ ACCEPTED」・CAS 列 = 06 表で引く（ACCEPTED head は key 無し → 表に無い = SAFE_STOP）／ 05:341「世代不一致 … `R_PROFILE_SUSPENDED` … HOLD / SAFE_STOP if 安全起因」。
- **text lens（未反証）**: 状態遷移 list が 06:514 / 05 (i) の前提する遷移を欠く。
- **logic lens（未反証）**: gen 1 の活性 lease 中に evidence 追加で gen 2 ACCEPTED を append（06:514 許容）→ head-check → TRANSFER_TO_SAFEHOLD・fault `R_PROFILE_SUSPENDED`（誤名）・disposition = SAFE_STOP（05:292 経路）か HOLD（05:341 経路）。generation 束縛の lease を新 generation で無効化すること自体は条件 12 / INV-35 と整合（保守側）— 欠陥は遷移の欠落と非安全原因への SAFE_STOP 既定。R5-02 と family が同じだが claim（状態機械の完全性）は別。
- **impact lens（未反証・LOW）**: over-closed + disposition 二義。**LOW**。
- **verdict**: CONFIRMED / LOW。
- **minimal fix**: 06:507 に「ACCEPTED → ACCEPTED(gen+1)（evidence 追加 / 再受理・受理条件は同じ・`supersedes_record_hash` = 旧 ACCEPTED）」；05:292 副 case (a′)「`record_hash ≠` ∧ head.state == ACCEPTED」→ `R_PROFILE_SUSPENDED`（payload trigger = GENERATION_SUPERSEDED）→ HOLD（05:341 の非安全「世代不一致」と同一）；06:342 表に「GENERATION_SUPERSEDED（head ACCEPTED の新 generation・head-check 副 case）→ HOLD」（`SuspensionReason` の member にはしない）。

### R5-14（LOW・CONFIRMED）— 06 §5 (b) 表が CONTROLLER_ENVELOPE_MEASUREMENT の失効を SAFE_STOP 級に分類する一方、`P_VALIDITY_UNBOUNDED` は同 kind の無期限を許す
- **sed 実測**: 06:342「EVIDENCE(SAFETY_LAYER_ACCEPTANCE | CONTROLLER_ENVELOPE_MEASUREMENT) → SAFE_STOP」／ 06:296・06:364 の kind 集合 = {CALIBRATION_RECORD, SAFETY_LAYER_ACCEPTANCE}／ 06:287 `P_SAFETY_BUDGET_EXCEEDED` は CEM の `stop_time_worst_case_s` を消費／ 05:340・GPT-A:43「停止性能に関わる失効」= SAFE_STOP 級。
- **text lens（未反証）**: doc 自身が CEM の陳腐化を安全重要と分類しつつ無期限宣言を許す。
- **logic lens（未反証）**: CEM を `valid_until = None` で登録 → `P_EVIDENCE_EXPIRED` と `validity_deadline_source` の対象外 → 06:342 の CEM 行は到達不能・停止時間の実測が無期限に有効。
- **impact lens（未反証・LOW）**: どの kind に期限を必須とするかは設計選択（R2-18 は 2 kind）ゆえ LOW。逆解（06:342 から CEM を外す）も 1 行で等価。R6-17（FI record の失効級）と一部重複。**LOW**。
- **verdict**: CONFIRMED / LOW。
- **minimal fix**: 06:296 と 06:364 の kind 集合に CONTROLLER_ENVELOPE_MEASUREMENT を追加（R5-04 の評価点修正と同時）；任意で PT-32c。Rs が CEM 無期限を選ぶなら代わりに 06:342 の SAFE_STOP 行から CEM を外す — いずれも OPP-3 / OP-19 に選択を記録。

## 3. 反証 trace の要約（lens 別）

| finding | text | logic | impact | verdict | severity | R6 重複 |
|---|---|---|---|---|---|---|
| R5-01 | 未反証 | 未反証 | 未反証（裁定「許容」と両立する二重規範） | CONFIRMED | MEDIUM | R6-16 |
| R5-02 | 未反証 | 未反証（4 副 case） | 未反証 | CONFIRMED | MEDIUM | R6-21 一部 |
| R5-03 | 未反証 | 未反証（crash trace） | 未反証（fix 後半は却下） | CONFIRMED | MEDIUM | — |
| R5-04 | 未反証（CalibrationRef 側は弱化） | 未反証（CALIBRATION_RECORD） | 未反証 | CONFIRMED | MEDIUM | R6-11 |
| R5-05 | 未反証 | 未反証 | 未反証（保守側だが本文矛盾） | CONFIRMED | MEDIUM | — |
| R5-06 | 未反証 | 未反証（(l) で permit 側は有界） | 未反証 | CONFIRMED | MEDIUM | — |
| R5-07 | 未反証 | 未反証 | 未反証（LOW） | CONFIRMED | LOW | — |
| R5-08 | 未反証 | 未反証 | 未反証（LOW） | CONFIRMED | LOW | R6-21 |
| R5-09 | 未反証 | 未反証 | 未反証（LOW） | CONFIRMED | LOW | R6-12 |
| R5-10 | 未反証 | 未反証 | 未反証（LOW） | CONFIRMED | LOW | R6-03 |
| R5-11 | **反証**（datum は lease に在る） | **反証**（gateway 自己検出可） | **反証** | REFUTED | NOT_A_DEFECT | — |
| R5-12 | 主部未反証・副部反証 | 未反証 | 未反証（LOW） | CONFIRMED | LOW | — |
| R5-13 | 未反証 | 未反証 | 未反証（LOW） | CONFIRMED | LOW | — |
| R5-14 | 未反証 | 未反証 | 未反証（LOW・設計選択の余地） | CONFIRMED | LOW | R6-17 一部 |

## 4. 主張しないこと・境界

- 本報告は AI verifier の判定であり two-key / Rs 裁定 / gate PASS を主張しない。凍結 4 file は不変（11/11 PASS を 23:05:04 UTC に再測）。`eval_runs/` は未編集。出力は本 file と `V5_results_v026.json`（同 dir `$OUT`）のみ。
- Rs 裁定 4 件（OPP-15 B+ / OPP-11 / OPP-13 / OP-19）の設計方向は検証対象にしていない。confirmed 13 件はいずれも「裁定・自己宣言を本文が正しく・一貫して・fail-closed に実現しているか」の欠陥であり、最小修正は全て frozen schema delta 0（runtime / deployment 層の enum member 追加を含むのは R5-06 の `R_AUDIT_UNAVAILABLE` のみ・新 timeout field は追加しない）。R5-02 (c)(d) / R5-06 / R5-14 の disposition・kind 集合の**値**は起草者既定として提示したもので、Rs が強化・変更し得る（OP-19 / OPP-3 の残 open に記録するのが妥当）。
- R5-03 の R5 提案後半（AuthorityManager が MONITOR role で registry へ書く）は OPP-13 裁定（read-only consumer）に反するため採らない — 本報告はその代替（前提 (m)）を最小修正とする。
- R6（21 件）との重複 6 件は注記のみ。reviewer をまたぐ merge は行わない。
- 08 §5 の語彙では HIGH 0 / CRITICAL 0 ゆえ本 round の group verdict は **PASS-WITH-CONDITIONS**（MEDIUM 6 件の fold を条件）— ただし R6 側の verdict（V6）と合わせて判断する事項であり本報告単独では確定しない。
- 実測 timestamp = 本 file 書込直前の `date -u`（下記手続記録末尾に転記）。
- 本報告書込 = 2026-09-05 23:21:32 UTC（`date -u` 実測）
