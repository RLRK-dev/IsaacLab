# V1 — R1 finding 16 件（WMSO v0.2.3 REVIEW CANDIDATE）の 3 lens 反証検証（adversarial verifier・独立）

- verifier: V1（別 context・authority 無し・凍結物へ非接触・`eval_runs/` 無改変。本 report と JSON は `review2/` のみ）
- 実施: 2026-09-04 22:41–22:50 UTC（`date -u` 実測。pin 再測 22:41:03・本 report 書出し 22:50:17）
- 対象: `$PKG/05_WMSO_RUNTIME_SPEC_v0.2_REVIEW_CANDIDATE_20260903.md`（v0.2.3・928 行・sha256 `0a25494bf2221928…`・全行 read）／ `$PKG/06_WMSO_INDUSTRIAL_PROFILE_v0.2_REVIEW_CANDIDATE_20260903.md`（v0.2.3・526 行・sha256 `48b01a2f94245adc…`・全行 read）
- `$PKG` = `eval_runs/troot_optE_dapg_wholeroute_scope_20260701/WMSO_EXACT_BASELINE_REVIEW_v0.2_20260903`、`$D` = `eval_runs/troot_optE_dapg_wholeroute_scope_20260701`、A = contracts_v2 DESIGN、B = tensor_binding DESIGN、D0 = architecture draft
- 入力: `review2/R1_findings_v023.json`（16 件）＋ `review2/R1_review_v023.md`（全文 read）。手続 = `review/briefs/VERIFY.md`（text / logic / impact の 3 lens を各 finding で独立に走らせ**反証を試みる**。既定 = refuted。2 lens 以上が未反証のときのみ confirmed）
- 凍結参照（sed で内容確認済）: A:39 / :62 / :148-151 / :306 / :323 / :341-354 / :372-385 / :477-529 / :537 / :550 / :570 ／ B:4-5 / :23-24 / :40 / :65-66 / :96-99 / :125-138 / :255 / :313 / :374 ／ D0:207-223 / :243-247 / :267-274 / :310-320 / :354-372 ／ prereg:59 ／ C3:5 / :32-34 / :63 / :65 ／ handoff 決定（`scratchpad/handoff/HANDOFF_DECISIONS_v0.2_20260903.md` §B–§F）

## 0. 前提の実測

### 0.1 pin 再測

```text
$ bash $PKG/verify_exact_baseline_pins.sh        # 2026-09-04 22:41:03 UTC
PASS ×11（D1.1-A worktree / EP md / EP json / D1.1-B worktree / A blob / B blob / A blob sha / B blob sha / A @54f90a7d / B @07250f4a / definition hash e7ca4309…）
policy_semver=1.9.0  HEAD=ab52137f8ecf0c9016d78b99423c74816ce7d7a8 branch=claude/v0-2-design-candidates-snqbmy  EXIT=0
```
→ **11/11 PASS**（R1 の再測 22:21 と同一 hash・HEAD は R1 時点 `ddb37db1…` から `ab52137f…` へ進んでいるが、frozen 4 file の hash は不変）。

### 0.2 機械検査

```text
$ python3 $PKG/check_review_candidate.py $PKG/05_…md --runtime
== …/05_WMSO_RUNTIME_SPEC_v0.2_REVIEW_CANDIDATE_20260903.md: citations=152 FAIL=0 WARN=10   EXIT=0
```
→ **FAIL=0 / WARN=10**（WARN は全て C2 heuristic：line 97 ×2 / 144 / 227 / 527 / 529 / 780 / 788 / 922 ×2 — R1 の実測と同一。05 anchor 23 の説明と一致）。

### 0.3 重複統合

16 件を claim 単位で突合。関連はあるが claim が異なる組（R1-03(c) ↔ R1-09 = 「状態に disposition が無い」 vs 「遷移行が DISPOSITION record を要求しない」／ R1-01 ↔ R1-15 = S_BOUNDARY_WAIT での `command_deadline_s` vs `lease_max_duration_s`／ R1-04 ↔ R1-14 = SAFETY clearance の role vs ISL 決定不在）は**完全重複ではない**。全件 `merged_into = ""`。

## 1. 判定総括

| finding | text | logic | impact | verdict | severity（V1） | severity（R1） |
|---|---|---|---|---|---|---|
| R1-01 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | MEDIUM | MEDIUM |
| R1-02 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | MEDIUM | MEDIUM |
| R1-03 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | MEDIUM | MEDIUM |
| R1-04 | 未反証 | 未反証（「永久」は過大・新 profile で回復可） | 未反証 | **CONFIRMED** | MEDIUM | MEDIUM |
| R1-05 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | **HIGH** | HIGH |
| R1-06 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | MEDIUM | MEDIUM |
| R1-07 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | LOW | LOW |
| R1-08 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | LOW | LOW |
| R1-09 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | LOW | LOW |
| R1-10 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | LOW | LOW |
| R1-11 | 未反証（一部） | **反証** | **反証** | refuted | NOT_A_DEFECT | LOW |
| R1-12 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | LOW | LOW |
| R1-13 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | LOW | LOW |
| R1-14 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | LOW | LOW |
| R1-15 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | LOW | LOW |
| R1-16 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | LOW | LOW |

集計: **confirmed 15 / refuted 1 / merged 0**。severity 別 = HIGH 1（R1-05）／ MEDIUM 5（R1-01, 02, 03, 04, 06）／ LOW 9（R1-07, 08, 09, 10, 12, 13, 14, 15, 16）／ NOT_A_DEFECT 1（R1-11）。全 confirmed finding の最小修正は **text-only・frozen schema delta 0**（R1-03 の代替案は runtime 層型への field 追加を含むが frozen 4 file 外。本 report は text-only 案を主案として併記）。08 §5 の語彙では HIGH が 1 件残るため group verdict = **HOLD**（fold 後は PASS-WITH-CONDITIONS に移行可能）。

## 2. finding 別の検証

### R1-01（MEDIUM・CONFIRMED）— `command_deadline_s` の計時開始・停止条件が無く、S_BOUNDARY_WAIT で発火する

- **sed 実測**: 05:423「次 command が `command_deadline_s` 内に届かなければ hold 継続ではなく `SafeHold`（`R_DEADLINE_MISS`）」／ 05:425「後続 command が未 admit の間、gateway は直近 admitted setpoint を保持する … `command_deadline_s` 超過で SafeHold + `R_DEADLINE_MISS`」／ 05:277「**any (EXECUTOR)** | gateway: command deadline miss / envelope 違反 | TRANSFER_TO_SAFEHOLD(DEADLINE_MISS \| ENVELOPE_VIOLATION)」／ 05:319 失敗表「command deadline 超過 | CommandGateway | R_DEADLINE_MISS | TRANSFER_TO_SAFEHOLD(DEADLINE_MISS) | HOLD」／ 05:503「producer の SkillOutcome 受領後も lease は活性のまま（S_BOUNDARY_WAIT）、executor は新しい運動 command を出さず … **転送が成立しなければその状態が続き**、NO_CHAIN なら TRANSFER_TO_SAFEHOLD」／ 05:535「SAFEHOLD からの起動（producer 無し）では B1 と offer 検証を skip」／ 06:258（`command_deadline_s` と `boundary_dwell_s` の順序なし）。`grep -n command_deadline_s 05` = :241/:363/:423/:425/:426/:705 のみ、`grep -E '計時|suspend|一時停止'` = 0 hit（R1 の実測と一致）。
- **text lens**: 未反証。05:277 の from 列は `any (EXECUTOR)` で `S_BOUNDARY_WAIT`（owner.kind == EXECUTOR・:262）を含み、停止条件はどこにも無い。05:503 は「その状態が続く」と書くので同一事象について 2 記述が矛盾する。「最初の command 前」については 05:425 が deadline を「直近 admitted setpoint」に結び付けているため「最初の admitted command 以降に計時」と読める余地があるが、明文は無い。
- **logic lens**: 未反証。TerminalOutcome → MARK_BOUNDARY_WAIT（:243）→ executor は command を出さない（:503）→ 最後の admitted command から `command_deadline_s`（≥ 1/action_rate_hz、`safety_heartbeat_timeout_s ≤ command_deadline_s` :06:258 — ms 級が想定される）経過 → :277/:319 発火 → epoch+1・全 UNUSED permit VOID・旧 lease 無効。以後 producer の offer は `E_HANDOFF_EPOCH_STALE`（epoch が動いた・:249）で `R_OFFER_INVALID` → 再選択は S_SAFEHOLD(DEADLINE_MISS) からの起動（:535 で offer 検証 skip・:417 で ownership は profile 由来）。`ack_timeout_s` / `permit_ttl_s` と `command_deadline_s` の間に順序が無いため（06:258）、chained handoff は「再選択全体が command_deadline_s 内に完了する」場合にしか成立しない。反例（deadline が S_BOUNDARY_WAIT で自然に止まる読み）を本文から構成できない — :277 は無条件行であり「表外遷移は存在しない」（:265）。
- **impact lens**: 未反証。方向は fail-closed だが、(a) 05:503 と :277 の同一事象矛盾、(b) D0 §E の offer → accept → commit 経路（`validate_handoff` A:381）が構造的に到達不能、(c) handoff 決定 C.2–C.4 の順序（ACK → permit → CAS）が boundary_wait 状態を前提に置いているのにその状態が deadline で潰される、(d) 毎 boundary に DEADLINE_MISS FAULT が記録される。設計意図に反する挙動ゆえ MEDIUM（safety 側へ倒れるので HIGH ではない）。
- **verdict**: CONFIRMED / **MEDIUM**。
- **minimal fix（frozen delta 0）**: §4.2 に「`command_deadline_s` / `inter_command_jitter_s` の計時は当該 lease の**最初の admitted command から開始**し、`state.boundary_wait == True` の線形化点で**停止**する（S_BOUNDARY_WAIT の滞留上限 = `boundary_dwell_s`；最初の command 前の上限 = `health_confirm_timeout_s`（`first_command_admitted` が HealthConfirmation の要素））」を追記。§3.7 :277 の from を `S_LEASE_ACTIVE / S_HEALTH_PENDING（最初の admitted command 以降）` に限定。§2 Executor 行に「heartbeat 義務は LEASE_INVALIDATED まで継続（S_BOUNDARY_WAIT を含む）」を追記。INV-24 の「最大 `command_deadline_s`」は変更不要。

### R1-02（MEDIUM・CONFIRMED）— §3.4 (e) の age 基準が「manager の初回解決時刻」で、初回使用時に鮮度検査が空文

- **sed 実測**: 05:184 (e)「`age = now(manager) − t_mono`、`t_mono` = manager が当該 `authority_decision_ref` を**最初に解決した時**に記録した `RuntimeAuditRecord`（kind = ASSESSMENT・payload = decision ref）の `t_mono`（frozen `AuthorityDecision` は timestamp を持たない A:351）。その record が無い decision は不在として扱う」／ 05:227 条件 6（granted == True + certificate の skill_action_id のみ）／ 05:308 `R_AUTHORITY_DECISION_MISBOUND`（inputs の hash/ref 束縛不一致 — 現行性は検査しない）／ 05:418「runtime はこれを読むだけ」／ A:345-352（純関数・4 record 入力・timestamp 無し）／ A:341「profile は後段で何度でも再評価可」／ A:354。
- **text lens**: 未反証。文面は finding の言うとおり。decision の**評価時刻**を束縛する規則は 05 内に他に無い（条件 6・:308 とも inputs の同一性であって現行性ではない。OP-18 は gate の内容を O0/S0/V0 に carry するのみ）。
- **logic lens**: 未反証。t0 で D 発行 → t1 > t0 で `safety_gate_state` FAIL → t2 ≫ t1 で manager が初めて D を解決 → ASSESSMENT(t_mono = t2) → age = 0 ≤ `decision_max_age_s` → (e) PASS。(e) の「発行時に**再**解決」も、最初の解決が permit 発行時（Orchestrator が B4 で mode 要件を見るのは Orchestrator 側 :543）である以上、初回 permit では age ≈ 0。B2-14 の懸念（decision は時刻依存 4 record の関数）は 2 回目以降の permit にしか効かない。
- **impact lens**: 未反証。CLOSED_LOOP は CLOSED 継続（§11）ゆえ現時点の物理的害は無いが、(e) は CLOSED_LOOP lease の必要条件の鮮度検査として v0.2.3 fold（05:890）で「残差処置」と記録されており、その検査が初回使用で定義上 vacuous なのは fold の主張と一致しない。安全側でない（stale gate の decision が通る）。MEDIUM（B2-14 の verifier severity と同じ）。
- **verdict**: CONFIRMED / **MEDIUM**。
- **minimal fix（frozen delta 0）**: (e) の `t_mono` を「`evaluate_authority_grant` を**呼び出した**（O0 層 = AuthorityManager・A:382）時に書く ASSESSMENT record（payload = decision ref + 4 入力 record の ref）の `t_mono`」に置換し、「評価時刻 record を欠く decision = 不在（`R_AUTHORITY_DECISION_ABSENT`）」「`decision_max_age_s` 超過 = 再評価（新 ref・A:341）で回復」を明記。「最初に解決した時」の文は削除（解決 ≠ 評価）。

### R1-03（MEDIUM・CONFIRMED）— `SafeHoldReason` 優先順位が半順序で、同 tier / 同 reason 再遷移の意味論が未定義

- **sed 実測**: 05:241「`SAFETY / SAFE_STOP` > `MANAGER_RESTART / GATEWAY_RESTART / CHECKPOINT_DISABLED` > その他 … 理由を**強める方向にしか**書き換えない（弱い理由への上書きは記録付き no-op = FAULT + audit・状態不変・epoch 不変）」「(iii) は `safehold_reason == SAFETY` でも、当該 SAFEHOLD 遷移の記録 disposition が SAFE_STOP なら … SafeStop（heartbeat が戻り (i′) が外れても SafeHold に緩まない）」／ 05:301「persisted が SAFEHOLD で reason が **MANAGER_RESTART より弱い**ときのみ epoch+1 … persisted が SAFEHOLD(SAFETY / SAFE_STOP) なら reason と epoch を保持」／ 05:282「any | AuthorityManager 再起動 | … | `S_SAFEHOLD(MANAGER_RESTART)`」（無条件）／ 05:728 T-09 同型／ 05:279 from = `any (EXECUTOR)`／ 05:324 失敗表（from 制限なし）／ 05:493 (iii)／ 05:82（gateway 状態 = 線形化読み + 直近 admitted command + restart_pending）／ 05:130-137 `AuthorityState`（disposition field 無し）／ INV-30 :711／ INV-31 :712 vs §5.2 :499。
- **text lens**: 未反証。(a) 同 tier（persisted ∈ {CHECKPOINT_DISABLED, GATEWAY_RESTART, MANAGER_RESTART}）は :301 のどの分岐にも該当せず、:241 の「強める / 弱い」いずれでもない。:282 の to 列は :301 と同一事象で不一致（「表外遷移は存在しない」:265 の下で to 列は規範）。(b) 同 reason（SAFETY → SAFETY）も「強める」でも「弱い」でもなく未定義。(c) (iii) が要する「記録 disposition」は `AuthorityState` にも gateway 状態列にも無い。(d) INV-31 に seq 条項が無い（:499 にはある）。全て文面で再現。
- **logic lens**: 未反証。(b) の trace: ①ISL HOLD → TRANSFER_TO_SAFEHOLD(SAFETY)・disposition HOLD（:278/:302）。②ISL heartbeat 欠落 — 遷移表 :279 は from = `any (EXECUTOR)` で **SAFEHOLD 中には該当行が無い**が、失敗表 :324 と INV-29 は from 制限なく TRANSFER_TO_SAFEHOLD(SAFETY) 発行を要求（ここにも 2 記録の不一致）。発行されれば同 reason → :241 の未定義域。no-op と読めば状態不変・DISPOSITION 記録は step ① の HOLD のまま。③heartbeat 復帰・ISL が HOLD 解除 → (i)(i′) 外れ → (iii) は「当該 SAFEHOLD 遷移」= ① の HOLD → **SafeHold**。:241 の「緩まない」と矛盾。3 通りの読み（行なし／no-op／実遷移）のうち 2 通りで緩む。(a) の trace: persisted SAFEHOLD(CHECKPOINT_DISABLED) + manager restart → no-op 読みなら MANAGER_RESTART の「durable 整合検査記録」（:513）が要求されず、上書き読みなら CHECKPOINT_DISABLED の operator clearance + 再観測（:512）が失われる — どちらも解除条件の**弱化**。(d): SAFEHOLD(SAFETY) 中の gateway 再起動 → TRANSFER_TO_SAFEHOLD(GATEWAY_RESTART) は弱いので no-op（seq 不変）→ INV-31 字義では `restart_pending` が永久に立ち (ii) が評価されない → 新 lease の `first_command_admitted = False` → HEALTH_FAIL。:499 の seq 条項があれば次の実 CAS で解消 — 2 記録が異なる liveness を与える。
- **impact lens**: 未反証。(a) restart 後の clearance 弱化・(b) SafeStop → SafeHold の clearance 無し緩和は fail-closed bar に反する（(i)/(i′) が active な間は preempt されるが、解除後の (iii) が非保守）。(c)(d) は LOW 級だが同根。合わせて MEDIUM。
- **verdict**: CONFIRMED / **MEDIUM**。
- **minimal fix（frozen delta 0・主案 = text-only）**: (1) §3.5 に「既 SAFEHOLD への TRANSFER_TO_SAFEHOLD の規則」を定義: reason は tier 順で max（**同 tier の別 reason** = 現 reason 保持・記録付き no-op、ただし §5.4 に「AuthorityManager / CommandGateway 再起動後は persisted reason にかかわらず当該 restart 行の追加前提（durable 整合検査記録 / GATEWAY_CONFIG_MATCH + role）を **AND で追加**」を書く = clearance の和）；**同 reason で disposition が SAFE_STOP > 記録済 HOLD** のときは no-op ではなく実 CAS（epoch+1・seq+1・DISPOSITION(SAFE_STOP) 記録）。(2) (iii) の「当該 SAFEHOLD 遷移」= `seq == state.seq` の CAS に対する DISPOSITION record と定義し、gateway が `manager_liveness_s` 内に解決できなければ **SafeStop**（fail-closed 既定）。（代替 = R1 案の `AuthorityState.safehold_disposition` 追加。runtime 層型ゆえ frozen delta 0 だが CAS tuple が変わる。）(3) :282 の to 列を :301 と同一条件（persisted SAFETY / SAFE_STOP は保持・同 tier は保持 + 前提追加）に。(4) :279 の from を `any` に（:324 / INV-29 と揃える）。(5) INV-31 に「（または `seq` > 起動時観測 seq）」を追記。(6) T-28 に「同 reason 再遷移で disposition が SAFE_STOP へ昇格し clearance まで保持」を追加。

### R1-04（MEDIUM・CONFIRMED）— 05 §5.4 は SAFETY にも role を要求するが 06 は SAFETY の role を必須にしない

- **sed 実測**: 05:516「該当する clearance record = `record.safehold_reason == current.safehold_reason ∧ record.role ∈ profile.escalation.clearance_roles[reason] ∧ …`。SAFETY 行は加えて ISL の clearance record（AND）。profile が当該 reason の role を 1 つも宣言しない場合は登録時に doc 06 `P_CLEARANCE_ROLE_MISSING`（定義なし = 解除不能 HOLD にも … しない）」／ 05:518 条件 10 の集合 = {SAFETY, SAFE_STOP, CHECKPOINT_DISABLED, MANAGER_RESTART, GATEWAY_RESTART}／ 06:190「{SAFE_STOP, CHECKPOINT_DISABLED, MANAGER_RESTART, GATEWAY_RESTART} の各 reason に ≥ 1 role が必須（P_CLEARANCE_ROLE_MISSING）」／ 06:261 同（「SAFETY は ISL clearance record が主・role は AND」）／ 06:400 PT-22。
- **text lens**: 未反証。05 の role 条項は条件 10 集合の全 reason に一様に適用され、05 自身が「役割未宣言は登録で弾かれる」ことを前提にしているが、06 の必須集合は SAFETY を含まない。06:261 の「role は AND」は「宣言されていれば AND」とも読めるが、05:516 の式は `∈ ∅` を許さない。両 doc の集合が v0.2.2 で「同一定義」（A2-01 / B2-07）とされた後の再分岐。
- **logic lens**: 未反証（ただし「永久」は過大）。SAFETY role 無し profile → 06:261 PASS → 登録 → ISL STOP → SAFEHOLD(SAFETY) → ISL clearance record があっても `record.role ∈ ∅` は充足不能 → 条件 10 FALSE。回復経路は「SAFETY role を宣言した**新 profile** を登録し（OPP-13・受理権限 = Rs 側）、その `profile_hash` で permit を取り、record.profile_hash をそれに合わせる」— 文面上は存在するが、05:516 が禁じた「定義なし = 解除不能 HOLD」状態が**登録で防がれない**点は変わらない。
- **impact lens**: 未反証。availability 側（fail-closed 方向）の欠陥で、CD-02（verifier C が MEDIUM に降格）と同型。両 doc の集合の再同期が要る。MEDIUM。
- **verdict**: CONFIRMED / **MEDIUM**。
- **minimal fix（frozen delta 0）**: 06:190 / :261 / PT-22 の必須集合に **SAFETY** を追加し、05:518 と同一集合（{SAFETY, SAFE_STOP, CHECKPOINT_DISABLED, MANAGER_RESTART, GATEWAY_RESTART}）を 06 §3 に「05 §5.4 条件 10 集合と同一定義」と明記。（代替 = 05:516 の SAFETY 行のみ「`clearance_roles[SAFETY] ≠ ∅` のとき role 条項を適用」— 集合が二重化するため非推奨。）

### R1-05（HIGH・CONFIRMED）— `ZoneRef.geometry_sha256` が宣言されるが検証されない

- **sed 実測**: `grep -n geometry_sha256 05 06` = **06:106 の 1 hit のみ**（`ZoneRef.geometry_sha256: str  # zone 幾何（外部 artifact）の content hash`）。`grep -n -E 'zones|ZoneRef|zone' 05` = 0 hit。05:184 (j)「profile が参照する外部内容（`predicate_refs` / `evaluator_ref` / `tcp_offset_ref` の解決先）の content sha256 が profile の宣言と一致」／ 05:435 DEPLOYMENT_WORKSPACE 行（再検証なし）／ 05:437 SAFETY_RESTRICTION 行「gateway は評価前に predicate の解決内容 sha256 を `predicate_sha256` と再検証し、不一致 = 評価不能 = FALSE」／ 06:271 `P_PREDICATE_HASH_MISMATCH` = `predicate_refs` / `evaluator_ref` のみ／ 06:280 (xi) zone に対する防御 = `P_WORKSPACE_EMPTY`（構造検査）／ 06:403 PT-25（predicate 差替のみ）／ B:255（凍結側は content-addressed resolver を要求しない）／ B:23-24。
- **text lens**: 未反証。宣言 hash の検証規則が (j)・06 §3・05 §4.3 のいずれにも無い。「content hash と宣言した以上 resolver が検証する」という含意は、05:437 が predicate について**明示的に**再検証を書き足した事実（= 含意では足りないと起草者自身が判断した）と B:255 に照らして採れない。
- **logic lens**: 未反証。PT-25 と同型: 登録時 keep-out Z（geometry_sha256 = h・P_* = 0）→ resolver 側で h の裏の artifact を空多角形に差替（profile / profile_hash 不変・`accepted_profiles` に残る）→ 第 2 評価点 (j) は zone を見ない → permit 発行 → gateway の DEPLOYMENT_WORKSPACE 評価が差替後幾何を使う → keep-out 消失。「評価不能 = FALSE」（05:439）は差替後幾何が評価可能ゆえ効かない。`P_WORKSPACE_EMPTY` は登録時の構造検査で内容を見ない。CalibrationRef（tcp_offset_ref 経由で (j)）・GatewayConfigRef（GATEWAY_CONFIG_MATCH :260）は覆われ、zone だけが無防備 — R1 の対照は正しい。
- **impact lens**: 未反証。静的 safety envelope の 1 項（keep-out）が admission で無効化される。CD-04（verifier C = HIGH）と同じ攻撃面・同じ帰結。OPP-5 の「幾何の正しさは evidence に委ねる」は「宣言した幾何が正しいか」であって「解決した内容が宣言どおりか」ではなく、後者は 06:280 が「契約層で検証できる = 宣言と evidence の束縛」と自ら範囲に含めている。CLOSED_LOOP が CLOSED である点は CD-04 でも同じで severity を下げる理由にならない。**HIGH**。
- **verdict**: CONFIRMED / **HIGH**。
- **minimal fix（frozen delta 0・field は既存）**: 05:184 (j) の列挙に `zones[].geometry_sha256` を追加／ 06:271 の検査対象に `zones[].geometry_sha256` を追加（code は `P_PREDICATE_HASH_MISMATCH` を流用可、または名称を `P_CONTENT_HASH_MISMATCH` に一般化）／ 05:435 DEPLOYMENT_WORKSPACE 行に「gateway は評価前に zone 幾何の解決内容 sha256 を `geometry_sha256` と再検証し、不一致 = 評価不能 = FALSE」を追記／ 06 §8.2 に PT-27「登録後に zone 幾何 artifact を差替 → 次 permit で `R_PROFILE_MISBOUND`・gateway 再検証で FALSE」を追加。

### R1-06（MEDIUM・CONFIRMED）— CD-18 の「ZERO_ORDER_HOLD のとき点」評価は DIFF_IK_EE_TARGET の連続運動と整合しない

- **sed 実測**: 05:435「評価対象 = hold が LINEAR_INTERPOLATE のとき直前 admitted setpoint → 新 setpoint の線分（TCP 点・FK 後）、ZERO_ORDER_HOLD のとき点」／ 06:295 同文／ 05:423 hold = 「policy step 間の保持規約であり gateway がそのとおりに再生する」／ 05:425（未 admit 間は ZOH 相当で保持）／ A:62 `DIFF_IK_EE_TARGET`／ B:127 `hold` = 「policy step 間の保持規約（ZOH / 線形補間）」／ 05:468 SafeHold(DIFF_IK) = 「計測現在姿勢で凍結」。
- **text lens**: 未反証。文面は finding どおり。B:127 の hold は **setpoint 列の補間規約**であって「EE が点に留まる」意味ではない。06:295 の「tool 形状は ISL 監視」は形状に関する委譲であって経路に関する委譲ではない（OPP-14 も同じ）。
- **logic lens**: 未反証。keep-out Z、p1, p2 ∉ Z、線分 p1p2 ∩ Z ≠ ∅、hold = ZOH → 両 command が点評価で admit → DiffIK は setpoint の跳びに対して EE を連続移動させる（DifferentialIKController は目標姿勢へ PD で追従する — 瞬間移動ではない）→ TCP が Z を通過。同じ p1, p2 は LINEAR_INTERPOLATE なら拒否されるので、安全判定が物理経路を変えない parameter（補間規約）に依存する。反例（ZOH なら EE が跳ぶ、あるいは step が微小で線分が zone を横切れない）は、action が絶対 EE target を取り得る（bounds は policy action space の値域であって連続 setpoint 差ではない）ため一般には成立しない。
- **impact lens**: 未反証。DEPLOYMENT_WORKSPACE term は「静的 restriction = admission 項」（06:116・05:435）として設計されており、ZOH lease でその項が経路について無効になる。ISL（D0:315 near-contact / penetration）は接触危険の backstop であって keep-out 領域（非接触の禁止域）を覆うとは書かれていない。CD-07（評価不能な量を admission 項に置く = 評価の非整合）と同じ型の設計誤りゆえ MEDIUM（verifier C の CD-18 = LOW は「点評価のみ」の段階の判定で、v0.2.3 が導入した hold 依存の分岐はその後）。
- **verdict**: CONFIRMED / **MEDIUM**。
- **minimal fix（frozen delta 0）**: 05:435 / 06:295 を「`DIFF_IK_EE_TARGET` では hold 種別にかかわらず（直前 admitted setpoint → 新 setpoint）の線分（TCP 点・FK 後）を評価する。lease の最初の command は（計測 TCP → setpoint）。hold は線分上の時間 parameterization だけを変え、評価対象を変えない。`SCRIPTED_SEQUENCE`（hold = None）も同じく FK 後の線分」に置換。

### R1-07（LOW・CONFIRMED）— CAS 条件 9・10 の REJECTED と条件 3 の ACK 失効に fault code が無い

- **sed 実測**: 05:230 条件 9（ISL health ∈ {ok, degraded} ∧ 未処理 STOP/HOLD 無し）／ 05:231 条件 10（clearance record）／ 05:224 条件 3（`now ≤ ack.valid_until`）／ 05:206 `AuthorityCas.fault: RuntimeFaultCode | None  # REJECTED / CONFLICT の理由`／ 05:626 規則 2「fault は常に disposition を伴う」／ 失敗表 :286-328 に「clearance」「条件 9」「条件 10」= 0 hit（`sed -n 286,328p | grep -i -E 'clearance|条件 9|条件 10|未処理'` = 0）／ :291 `R_ACK_MISBOUND` = 「束縛不一致（epoch/offer/invocation）」／ :290 `R_ACK_TIMEOUT` = Orchestrator 側の未着／ 06:258（`ack_validity_s` と `permit_ttl_s` の順序なし）／ INV-27 :708（code → 行の方向のみ）。
- **text lens**: 未反証。条件 2/4/5/6/7/11 には対応 code（R_PERMIT_*・R_OFFER_REUSED・R_CAS_CONFLICT・R_AUTHORITY_DECISION_*・R_ENVELOPE_EMPTY・R_MIDSKILL_TRANSFER_BLOCKED）が失敗表にあるが、9・10 と 3 の失効には無い。
- **logic lens**: 未反証。SAFEHOLD(SAFE_STOP) から clearance 無しで permit（(a)–(j) に clearance 検査は無い）→ CAS → 条件 10 FALSE → REJECTED で `fault` に入れる member が無い。条件 9 は ISL 決定発行から safety CAS 線形化までの窓で FALSE になり得る（その後は条件 1 で CONFLICT）。条件 3 は `ack_validity_s < permit_ttl_s` の profile で失効し得る（INV-07 の `valid_until ≥ issued_at` は発行時のみ）。
- **impact lens**: 未反証（LOW）。B8 の event → code 方向の完全性・audit の欠落。安全側の挙動（REJECTED）は変わらない。
- **verdict**: CONFIRMED / **LOW**。
- **minimal fix（frozen delta 0）**: 条件 9 → `R_SAFETY_OVERRIDE`（未処理決定）/ `R_SAFETY_LAYER_LOST`（health failed）；条件 10 → 新 runtime code `R_CLEARANCE_ABSENT`（runtime enum・frozen 外。行: AuthorityManager | 拒否・変化なし | `HOLD`（clearance 待ち）。CAS_ATTEMPT payload に欠落 clearance を記録）— または `R_PERMIT_MISBOUND` 流用；条件 3 失効 → `R_ACK_MISBOUND` の定義を「束縛不一致 or 失効」に拡張（:291 の 2 列目）。各々 §3.7 失敗表に行を追加（INV-27 維持・T-25 に発火 case 追加）。任意: 06:258 に `ack_validity_s ≥ permit_ttl_s` を加えると条件 3 の失効経路が消えるが設計選択ゆえ必須にしない。

### R1-08（LOW・CONFIRMED）— §7 の「INVALID_STATE 解消せず → SAFE_STOP」が §3.7 / B5 の `on_no_chain` 経路と不一致

- **sed 実測**: 05:638「Terminal INVALID_STATE | 解消せず | SAFE_STOP（TRANSFER_TO_SAFEHOLD(SAFE_STOP)・原因 fault は別途記録・v0.2.2）」／ 05:265「表外遷移は存在しない」／ 05:275「S_BOUNDARY_WAIT | 候補なし / boundary_dwell_s 超過 / max_reselect_attempts 超過 | TRANSFER_TO_SAFEHOLD(NO_CHAIN \| SAFE_STOP per profile.escalation.on_no_chain)」／ 05:546 B5 同／ 05:241 SAFE_STOP 規則（実効 disposition が SAFE_STOP の遷移は TRANSFER_TO_SAFEHOLD(SAFE_STOP)）／ 05:629「§7 = B3 の既定表。profile は SAFE_STOP 側へのみ強化可」。
- **text lens**: 未反証。INVALID_STATE が RE_OBSERVE で解消しないまま滞留 / 回数上限に達した事象について、§7 は無条件 SAFE_STOP、§3.7 :275 / B5 は `on_no_chain`（既定 HOLD）。「原因 fault」の code も §7 に無い。
- **logic lens**: 未反証。INVALID_STATE → RE_OBSERVE 反復 → `boundary_dwell_s` 超過。B5 を辿れば NO_CHAIN → HOLD（既定）、§7 を辿れば SAFE_STOP。CAS 機構としては :241 の SAFE_STOP 規則で表現可能だが、from = S_BOUNDARY_WAIT で reason SAFE_STOP を無条件に取る行は :275 に無い（`on_no_chain` 条件付きのみ）。
- **impact lens**: 未反証（LOW）。どちらも fail-closed で、どちらが規範かの二義性のみ。
- **verdict**: CONFIRMED / **LOW**。
- **minimal fix（frozen delta 0）**: :275 と B5 に「`TerminalOutcome.termination_class == INVALID_STATE` の boundary では `on_no_chain` にかかわらず SAFE_STOP（§7 既定表が HOLD → SAFE_STOP の強化を既に適用）」を追記し、:638 の「原因 fault」を `FAULT(R_BOUNDARY_DWELL_EXCEEDED | R_NO_CANDIDATE)` と明記。（逆方向 = :638 を `on_no_chain` に揃える案も可。どちらか 1 つに統一する。）

### R1-09（LOW・CONFIRMED）— ISL STOP/HOLD 遷移行に DISPOSITION record が無い

- **sed 実測**: 05:278「any | IndependentSafetyLayer STOP / HOLD | TRANSFER_TO_SAFEHOLD(SAFETY) | `S_SAFEHOLD(SAFETY)` | SAFETY_OVERRIDE, PERMIT_VOIDED(全), FAULT(R_SAFETY_OVERRIDE)」（DISPOSITION なし）／ 05:302 失敗表 disposition = HOLD（STOP なら SAFE_STOP）／ 05:493 (iii)（「当該遷移の記録 disposition」に依存）／ INV-17 :698・T-17 :736（行が要求する record kind 単位）／ 05:668-669 `RuntimeAuditRecord.fault` / `.disposition`／ 05:673「全 fault/disposition」記録義務。
- **text lens**: 未反証。他行（:271 / :273 / :274 / :276 / :277 / :279 …）は DISPOSITION(…) を明示するのに :278 だけ無い。§8 の「全 disposition」と `RuntimeAuditRecord.disposition` field で FAULT record に disposition を載せる読みは可能だが、INV-17 / T-17 が行駆動ゆえ (iii) が参照する record は**義務化されていない**。
- **logic lens**: 未反証。ISL STOP → :278 どおり 3 record → (iii) の「記録 disposition が SAFE_STOP」が判定不能（(i) が外れた後に SafeStop / SafeHold のどちらかが未定義）。
- **impact lens**: 未反証（LOW）。R1-03(c) と同根の記録欠落。修正 1 行。
- **verdict**: CONFIRMED / **LOW**。
- **minimal fix（frozen delta 0）**: :278 の記録列に `DISPOSITION(HOLD | SAFE_STOP by SafetyDecision.action)` を追加（D0:317 の action ∈ {STOP, HOLD, RETRACT, FORCE_LIMIT}: STOP → SAFE_STOP、HOLD → HOLD）。

### R1-10（LOW・CONFIRMED）— INV-17 / T-17 の record 集合が :269 と :673 で不一致

- **sed 実測**: 05:269「`S_SAFEHOLD` / `S_BOUNDARY_WAIT` | ack → permit → CAS 成功 | TRANSFER_TO_EXECUTOR | `S_HEALTH_PENDING` | EPOCH_TRANSITION, LEASE_ACTIVATED, LEASE_INVALIDATED(旧)」／ 05:673 記録義務（§3.7 全事象 + 全 permit 発行/消費/失効 + … + `COMMAND_ADMITTED`。「欠落 = INV-17 違反」）／ INV-17 :698「§3.7 の各事象について、その行が要求する record kind ごとにちょうど 1 件」／ T-17 :736「(事象, 行が要求する kind) ↔ record の全単射」。
- **text lens**: 未反証。S_SAFEHOLD 起点の TRANSFER_TO_EXECUTOR に旧 lease は無く、`LEASE_INVALIDATED(旧)` の「ちょうど 1 件」は充足不能。:673 が要求する PERMIT_CONSUMED / CAS_ATTEMPT は :269 の集合に無く、`COMMAND_ADMITTED` は §3.7 事象でないのに INV-17 違反と書かれる。
- **logic lens**: 未反証（S_SAFEHOLD(INITIAL) からの初回転送で再現）。
- **impact lens**: 未反証（LOW・audit 仕様の整合のみ）。
- **verdict**: CONFIRMED / **LOW**。
- **minimal fix（frozen delta 0）**: :269 の記録列を「EPOCH_TRANSITION, CAS_ATTEMPT, PERMIT_CONSUMED, LEASE_ACTIVATED, LEASE_INVALIDATED(旧・旧 lease がある場合のみ)」に；INV-17 の scope を「§3.7 の各事象 **および §8 記録義務の各事象**」に；T-17 の「全単射」を「行が要求する kind については全単射、§8 のみが要求する kind は存在検査」に。

### R1-11（NOT_A_DEFECT・refuted）— `S_TRANSFER_PENDING` の到達不能合成・§2 表の `pending_invalidate` 欠落

- **sed 実測**: 05:263「`S_TRANSFER_PENDING` | 上記いずれか + UNUSED permit ≥ 1」／ 05:184 (d)・INV-28 :709（permit 発行は SAFEHOLD ∨ boundary_wait のみ）／ **05:236「同じ `expected` に束縛された他の permit は次の CAS 試行で条件 1 を満たさず CONFLICT（自動的に VOID）」**／ 05:185（UNUSED | CONSUMED | VOID）／ 05:82（gateway 状態列 = 線形化読み + 直近 admitted command + restart_pending）／ 05:241「局所 flag `pending_invalidate`」／ 05:492（定義）。`grep pending_invalidate 05` = :241/:492/:705/:885。
- **text lens**: 一部未反証（:82 に `pending_invalidate` が無いのは事実。:263 の文面も finding どおり）。
- **logic lens**: **反証**。「S_LEASE_ACTIVE / S_HEALTH_PENDING では permit が存在し得ない」は誤り。permit P1・P2 が同じ `expected`（S_SAFEHOLD または S_BOUNDARY_WAIT）に発行され P1 の CAS が成功すると、P2 は**次の CAS 試行または失効まで UNUSED のまま**（:236 は「次の試行で CONFLICT」であって即時 VOID ではない。TRANSFER_TO_SAFEHOLD の :241「全 UNUSED permit の VOID」とは異なる）。よって S_HEALTH_PENDING（さらに `permit_ttl_s > health_confirm_timeout_s` なら S_LEASE_ACTIVE — 06:258 に両者の順序は無い）+ UNUSED permit ≥ 1 は到達可能な射影状態であり、:263 の定義は正しい。`pending_invalidate` の :82 欠落は、:241 が「局所 flag」・:492 が「gateway が … 検出してから … まで True」と所有者を明示するため、挙動上の欠陥を生まない。
- **impact lens**: **反証**。:82 の「持つ状態」列への 1 語追加は編集上の整合であって fail-closed bar・handoff 決定・凍結制約のいずれにも関わらない。
- **verdict**: refuted（1 lens のみ未反証）/ **NOT_A_DEFECT**。
- **任意の編集（defect ではない）**: :82 の gateway 状態列に `pending_invalidate` flag を併記。

### R1-12（LOW・CONFIRMED）— gateway admission が `cmd.executor_id == lease.executor_id` を検査しない

- **sed 実測**: 05:479 `GatewayCommand.executor_id: str`／ 05:82 admission = `(lease_id, control_epoch)` 等値 ∧ ¬boundary_wait ∧ AcceptedEnvelope ∧ kind ∧ cmd_seq／ INV-03 :684（lease_id / control_epoch / cmd_seq）／ 05:237 事後条件（epoch / lease_id）／ **05:332 証明 A「lease は `executor_id` を 1 つだけ持つ（§4.1）ため executor についても同じ」**／ A:382（executor 同一性は言及なし）／ OP-13（Executor の process 境界 = interface のみ）。
- **text lens**: 未反証。command 記録型が `executor_id` を運ぶのに admission・INV-03 のどちらも照合しない。他所にも無い。
- **logic lens**: 未反証。(lease_id, epoch, 次 cmd_seq) を知る第三者 process の command は admit される。加えて証明 A の「executor についても同じ」は admission が executor を束縛していない以上、lease 側の一意性からは導けない（証明の欠落）。
- **impact lens**: 未反証（LOW）。transport 認証は OP-13 の外だが、handoff 決定 C.6（lease への完全束縛）と証明 A の主張に対し、運んでいる識別子を検査しないのは fail-open の省略。修正 1 節。
- **verdict**: CONFIRMED / **LOW**。
- **minimal fix（frozen delta 0）**: :82 の admission と INV-03 に `cmd.executor_id == lease.executor_id` を追加（違反 = `R_LEASE_UNKNOWN_COMMAND`・:307 行の記述を「lease_id 不明 / executor 不一致」に拡張）。証明 A の該当文を「admission の executor_id 等値により」に補強。

### R1-13（LOW・CONFIRMED）— §6.2 B0 の snapshot が MARK_BOUNDARY_WAIT に先行する race に規則が無い

- **sed 実測**: 05:534「B0 … snapshot := AuthorityManager.read_snapshot()」／ 05:243「MARK_BOUNDARY_WAIT の前提条件 = … 当該 lease の `SkillOutcome` を受領した事実」（manager が実行）／ 05:535 B1「MARK_BOUNDARY_WAIT は受領の事実で**既に実行済**」／ 05:184 (d)（`expected.boundary_wait == True`）／ 05:323「R_MIDSKILL_TRANSFER_BLOCKED | permit 不発行 / CAS REJECTED・状態不変 | 変化なし（audit・lease 継続）」／ 05:545 B4「いずれか失敗 → 次候補（fault を記録）」／ INV-21 :702／ 05:284（新 Orchestrator は B0 から再開）。
- **text lens**: 未反証。「既に実行済」は前提の**記述**であって順序を保証する規則ではなく、outcome の配送経路（manager 経由か並行か）は未規定。stale snapshot で (d) が偽になった後の Orchestrator の次手（再読）は書かれていない。
- **logic lens**: 未反証（配送が並行なら race が成立）。Orchestrator が bw = False の snapshot を取り B4 → ACK → permit 要求 → (d) 違反 → `R_MIDSKILL_TRANSFER_BLOCKED`（実際には mid-skill ではない誤帰属）→ B4 の「失敗 → 次候補」で候補が消費され、最悪 `max_reselect_attempts` を使い切って NO_CHAIN。
- **impact lens**: 未反証（LOW）。fail-closed で安全性影響は無いが、誤った fault code の記録と候補浪費は仕様の穴。
- **verdict**: CONFIRMED / **LOW**。
- **minimal fix（frozen delta 0）**: B0 に「producer あり（offer あり）の boundary では `snapshot.owner.kind == EXECUTOR ∧ snapshot.boundary_wait == True` を確認するまで `read_snapshot()` を再読する（上限 `boundary_dwell_s`）。bw = False の snapshot からは permit を要求しない」を追記。（または「Orchestrator は manager の MARK_BOUNDARY_WAIT 記録（CAS_ATTEMPT SUCCESS）を受けて B0 を開始する」と配送順序を固定。）

### R1-14（LOW・CONFIRMED）— executor 発 `SAFETY_STABILIZED` の SAFETY hold の clearance が ISL 決定不在時に未定義

- **sed 実測**: 05:274「InterruptOutcome（reason = SAFETY_STABILIZED）受領 | TRANSFER_TO_SAFEHOLD(SAFETY) | … 解除 = §5.4 SAFETY 行」／ 05:527 同／ 05:510 SAFETY 行「IndependentSafetyLayer の clearance record（`stabilized_post_action_state` D0:320 の参照を含む）」／ A:151 `InterruptReason = {PLANNED_SWITCH, EVENT, SAFETY_STABILIZED}`（executor 側 outcome）／ D0:320「`stabilized_post_action_state` = the declared safe state after a safety action」（monitor が自身の action 後に宣言）。
- **text lens**: 未反証。ISL の action を伴わない SAFETY_STABILIZED（frozen 型が許す）に対し、ISL が参照すべき `stabilized_post_action_state` が存在しない場合の規則が無い。
- **logic lens**: 未反証。executor が自発的に SAFETY_STABILIZED を出す → SAFEHOLD(SAFETY) → :510 の record を ISL が発行する根拠（自身の action 後の状態）が無い → 解除経路が未定義（ISL が独自に評価して発行する読みは可能だが本文に無い）。
- **impact lens**: 未反証（LOW・availability 側）。
- **verdict**: CONFIRMED / **LOW**。
- **minimal fix（frozen delta 0）**: §5.4 SAFETY 行に「ISL 決定を伴わない SAFETY_STABILIZED では、ISL clearance record は executor 報告の stabilized state（`InterruptOutcome.checkpoint_id`）を ISL が自身の観測で確認して発行する。確認できなければ CHECKPOINT_DISABLED 行と同じ operator clearance + 再観測」を追記。

### R1-15（LOW・CONFIRMED）— 06 §8.1 (3) の「のみ」と INV-33 の from-state 不一致

- **sed 実測**: 06:373 (3)「受理後の事実変化（期限切れ・evidence 失効）は profile を変えない。… lease を無効化するのは health check 失敗（§5 (a)）と `lease_max_duration_s` 超過（CD-10）**のみ**」／ 05:458 §4.5（12 trigger）／ INV-33 :714「**全 lease** の活性時間は `lease_max_duration_s` 以下」／ 05:281 from = `S_LEASE_ACTIVE / S_HEALTH_PENDING`／ 05:325 失敗表（from 制限なし）／ 05:262（S_BOUNDARY_WAIT = lease は活性）／ 06:258 `lease_max_duration_s ≥ boundary_dwell_s`。
- **text lens**: 未反証。06:373 は文脈上「profile 起因の事実変化のうち」と読めるが字義は無限定で 05 §4.5 と衝突。INV-33 / :325 は全 lease・全状態、:281 は 2 状態のみ — 3 記録が不一致。
- **logic lens**: 未反証。activated_at = t0、outcome 受領 = t0 + lease_max − ε → S_BOUNDARY_WAIT で最大 `boundary_dwell_s` 活性継続 → INV-33 違反が遷移なしに継続。⚠ ただし **:281 に S_BOUNDARY_WAIT を足す修正は R1-01 と同型の害**（boundary 中に producer lease を潰し offer を stale にする）を生むため採らない。
- **impact lens**: 未反証（LOW）。S_BOUNDARY_WAIT では出力 = SafeHold・command は拒否済ゆえ安全影響なし。不変量の文言と scope 語の問題。
- **verdict**: CONFIRMED / **LOW**。
- **minimal fix（frozen delta 0）**: 06:373 を「**profile 起因で** lease を無効化するのは … のみ（他の trigger = 05 §4.5）」に；INV-33 と :325 を「lease の活性時間（LEASE_ACTIVATED から `boundary_wait == True` の線形化点または無効化まで）は `lease_max_duration_s` 以下」に scope（S_BOUNDARY_WAIT の上限は `boundary_dwell_s` が担う）。:281 の from は**変えない**。

### R1-16（LOW・CONFIRMED）— `pending_invalidate` の clear 条件が lease-scoped CAS 破棄を覆わない

- **sed 実測**: 05:492「`pending_invalidate` = gateway が TRANSFER_TO_SAFEHOLD の trigger を検出してから**再試行 CAS の SUCCESS を観測するまで** True」／ 05:241「lease-scoped な理由の再試行は `current.active_lease_id == trigger.lease_id` のときのみ … 不一致なら CAS を捨て元 lease への FAULT のみ記録」「manager 単独検出で gateway への通知が CAS 完了より遅れる窓」／ INV-24 :705／ 05:214 `first_command_admitted`／ 05:271（HEALTH_FAIL）。
- **text lens**: 未反証。clear 条件は「再試行 CAS の SUCCESS」の 1 つのみで、CAS が破棄された場合（SUCCESS が存在しない）と、通知遅延で gateway が flag を立てた時点で既に当該 CAS が完了していた場合のどちらも覆わない。
- **logic lens**: 未反証（race は狭いが本文が排除していない）。L1 の最終 command の ENVELOPE_VIOLATION → flag → CAS → MARK_BOUNDARY_WAIT が先に成功 → CONFLICT → 再読（active = L1）→ 再試行前に TRANSFER_TO_EXECUTOR(L2) 成功 → 再読で active = L2 ≠ L1 → CAS 破棄（FAULT のみ）→ gateway は「再試行 CAS の SUCCESS」を観測しない → flag 立ったまま → L2 の (ii) 不評価 → `first_command_admitted = False` → HEALTH_FAIL（この CAS は別 trigger の CAS で、字義上 clear 条件を満たさない）→ 以後の全 lease で反復。「即座に再試行」は原子性を保証しない。
- **impact lens**: 未反証（LOW）。fail-closed 方向の liveness 欠陥。修正 1 文。
- **verdict**: CONFIRMED / **LOW**。
- **minimal fix（frozen delta 0）**: :492 の clear 条件を「再試行 CAS の SUCCESS を観測、**または線形化読みで trigger の lease が活性でなくなったこと**（`state.active_lease_id ≠ trigger.lease_id ∨ state.owner.kind == SAFEHOLD`）を観測するまで」に置換。INV-24 も同文に。

## 3. 反証しなかった点・限界（honest scope）

- 本検証は AI verifier による準備検証で two-key・Rs 裁定を代替しない。severity は 08 §5 の語彙で本 verifier の判定。
- R1-05 を HIGH としたのは CD-04（verifier C・HIGH）との一貫性による。「宣言 hash = 検証含意」の読みを採れば MEDIUM だが、05:437 が predicate について再検証を**明示的に**足した事実がその読みを否定する。
- R1-06 は verifier C が CD-18 を LOW とした判断より重い MEDIUM とした。理由は v0.2.3 が導入した hold 依存分岐が安全項の評価を物理と無関係な parameter に依存させる点（CD-07 と同型）。ISL backstop を重く見れば LOW。
- R1-11 の反証は 05:236（他 permit は次の試行で CONFLICT）に基づく。TRANSFER_TO_SAFEHOLD の「全 UNUSED permit の VOID」（:241）と異なり TRANSFER_TO_EXECUTOR は他 permit を即時 VOID しない — この非対称自体は finding ではない（stale permit は必ず CONFLICT する・証明 D）。
- 陽性対照 8 件（`10_*` §1）は R1 が既に実文に無いことを確認済（R1_review §0 冒頭）。本 verifier は再確認していない（対象外）。
- 凍結物・`eval_runs/` は無改変。出力 = `review2/V1_verify_v023.md`・`review2/V1_results_v023.json` のみ。

## 4. 出力

- 本 report: `/tmp/claude-0/-home-user-IsaacLab/f88a4426-3d65-5cc2-ba28-87f4976e6a66/scratchpad/review2/V1_verify_v023.md`
- results JSON: `/tmp/claude-0/-home-user-IsaacLab/f88a4426-3d65-5cc2-ba28-87f4976e6a66/scratchpad/review2/V1_results_v023.json`
