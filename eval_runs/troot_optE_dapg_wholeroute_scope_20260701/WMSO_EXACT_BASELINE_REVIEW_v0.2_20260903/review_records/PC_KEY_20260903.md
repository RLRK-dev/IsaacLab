# Positive-control answer key — planted defects in 05_pc.md / 06_pc.md

- Source (unmodified, real package): `$D/WMSO_EXACT_BASELINE_REVIEW_v0.2_20260903/05_WMSO_RUNTIME_SPEC_v0.2_REVIEW_CANDIDATE_20260903.md` and `.../06_WMSO_INDUSTRIAL_PROFILE_v0.2_REVIEW_CANDIDATE_20260903.md` (`$D` = `eval_runs/troot_optE_dapg_wholeroute_scope_20260701`). Real files were NOT modified.
- Mutated copies: `/tmp/claude-0/-home-user-IsaacLab/f88a4426-3d65-5cc2-ba28-87f4976e6a66/scratchpad/review/pc/05_pc.md` (739 lines, no insertions — line numbers identical to source) and `.../pc/06_pc.md` (422 lines; two inserted lines, so source lines ≥172 shift by +1 and ≥186 by +2).
- Exactly 4 defects per doc. No markers/comments were left in the mutated docs. Every other line is byte-identical to the source.
- Scoring rule: a finding "catches" a defect only if it (1) points at the mutated location (section or line), (2) names the property/invariant that is broken, and (3) cites at least one of the contradicted anchors listed under "must say". A finding that merely notes "wording changed" without the safety consequence does not count.

---

## 05_pc.md (runtime spec) — 4 defects

### PC-05-A — old-epoch rejection deferred until post-commit HealthConfirmation (double-ownership window)

Lines changed: **231, 232, 262**

- L231 original: `- **旧 epoch 拒否が有効化**: CommandGateway の admission は AuthorityState を線形化して読むため、この点以降 control_epoch ≠ expected.control_epoch + 1 の command は R_EPOCH_STALE_COMMAND、…`
- L231 mutated: `- **旧 epoch 拒否の予約**: … 旧 epoch の拒否は新 lease の HealthConfirmation 受領（HEALTH_CONFIRMED 記録）の線形化点から有効化する。それ以降 …`
- L232 original: `- **SafeHold 活性**: gateway 出力は新 lease の最初の admissible command が admit されるまで SafeHold（最初の安全 command）。旧 lease の直近 command は hold 地平にかかわらず即座に出力から外れる。`
- L232 mutated: `- **SafeHold 活性**: gateway 出力は新 lease の最初の admissible command が admit されるまで、旧 lease の直近 admitted command（hold 地平内）があればそれを、無ければ SafeHold とする（health 確認前の姿勢保持）。`
- L262 original 記録 column: `HEALTH_CONFIRMED`
- L262 mutated 記録 column: `HEALTH_CONFIRMED（この線形化点で旧 epoch 拒否が有効化・旧 lease の command は以後 R_EPOCH_STALE_COMMAND）`

A correct finding must say: the CAS linearization point no longer arms old-epoch rejection; between CAS success and HEALTH_CONFIRMED the old lease's commands are still admitted/emitted while the new lease is already `active_lease_id` at epoch e+1 — this reopens exactly the "new epoch → new owner ACK → old epoch rejection" double-ownership window that v0.2 claims to close (§13 fold-map row 4 L701; §13 L710; §0 L31-32 "旧 epoch 拒否（同一線形化点）"). It contradicts P1/P2 (L111-112), proof A/B (L294, L296: "旧 lease の command は線形化点以降 admit されない"), INV-03 (L611), T-06 (L641 "CAS 直後に旧 lease の command を投入 → R_EPOCH_STALE_COMMAND"), §5.2 (ii) (L431, only the *active* lease's commands in (ii)) and frozen O0 responsibility "旧 epoch command 拒否" (`contracts_v2 :382`, L52/L81). Required fix: rejection must take effect at the CAS linearization point itself; HealthConfirmation may only gate *new-lease* activation, never *old-lease* deactivation.

### PC-05-B — CommitPermit reusable until expiry (one-shot property removed)

Lines changed: **180, 612**

- L180 original: `… CONSUMED は CAS 成功の同一線形化点でのみ設定される。VOID = 失効 / 安全介入 / CAS 失敗。`
- L180 mutated: `… CONSUMED は expires_at 到達時に設定される（失効前の permit は CAS 成功後も UNUSED のまま残り、同一 expected への再試行に再提示できる — ACK からの再取得を省くため）。VOID = 安全介入 / CAS 失敗。`
- L612 original: `| INV-04 | permit.state の遷移は UNUSED→CONSUMED（CAS SUCCESS の線形化点のみ）or UNUSED→VOID。CONSUMED の permit を持つ CAS SUCCESS は高々 1 | R_PERMIT_REUSED |`
- L612 mutated: `| INV-04 | permit.state の遷移は UNUSED→CONSUMED（expires_at 到達時）or UNUSED→VOID。CONSUMED / VOID の permit を持つ CAS SUCCESS は 0 | R_PERMIT_EXPIRED |`

A correct finding must say: the permit ledger no longer enforces one-shot use — a permit stays `UNUSED` after a successful CAS, so P4 (L114 "高々 1 回の AuthorityCas にしか使えず") and proof D (L300, which relies on `UNUSED → CONSUMED` at the CAS linearization point making condition 2 false) no longer hold; INV-04 no longer emits `R_PERMIT_REUSED`, contradicting the failure-path row "permit 再使用 → R_PERMIT_REUSED" (L279), T-02 (L637), anchor 8 (L724), and the §0 line "CommitPermit（one-shot・失効付き）" (L31). Reviewer should note that relying on tuple equality (condition 1 / seq) alone is a single-barrier, and after any state where `expected` recurs is impossible only by construction, not by the permit ledger — i.e., defence-in-depth is removed and the stated proof is false. Fix: `CONSUMED` must be set at the CAS SUCCESS linearization point; INV-04 must reject `CONSUMED` permits with `R_PERMIT_REUSED`.

### PC-05-C — `consumed_offer_ids` no longer updated in the CAS postcondition (replay proof breaks)

Line changed: **229**

- Original: `AuthorityState := (EXECUTOR(executor_id), expected.control_epoch + 1, new_lease_id, expected.consumed_offer_ids ∪ {consume_offer_id}, None, expected.seq + 1)`
- Mutated: `AuthorityState := (EXECUTOR(executor_id), expected.control_epoch + 1, new_lease_id, expected.consumed_offer_ids, None, expected.seq + 1)` (the `∪ {consume_offer_id}` term is dropped)

A correct finding must say: the CAS success no longer records the consumed `handoff_offer_id` in the tuple, so §3.6 item 2 ("CAS の tuple 要素 consumed_offer_ids として CAS 内で消費", L242), condition 4 (L220) becomes vacuous, INV-05 (`o ∈ new.consumed_offer_ids`, L613) is violated by the postcondition itself, and proof C (L298 "x の成功後、状態の consumed_offer_ids は o を含む（事後条件）") is false — replay protection degrades to the `validate_handoff` epoch-equality check alone, which the doc itself says the validator cannot guarantee (§1 reading 1, L71: "単調性・再利用拒否は validator では保証できず authority manager の状態所有責務"; frozen `:382` "使用済み offer 拒否（handoff_offer_id 単位）= authority manager"). Also contradicts T-04 (L639 "CAS で R_OFFER_REUSED") and anchor 7 (L723 "二重遮断"). Fix: restore `∪ {consume_offer_id}` in the postcondition.

### PC-05-D — SHADOW_NON_AUTHORITY lease emits its first admissible command

Line changed: **400**

- Original: `| gateway actuation | **DISABLED** — lease の command は admission 判定・記録（SHADOW_COMMAND_RECORDED）のみ。actuation port から何も出ない | admissible command を出力（§5.2 (ii)） |`
- Mutated: `| gateway actuation | **DISABLED（初回 admit を除く）** — 最初の admissible command のみ actuation port へ出力し（HealthConfirmation.first_command_admitted を実 readback で確認するため）、以後の command は admission 判定・記録（SHADOW_COMMAND_RECORDED）のみ | … |`

A correct finding must say: a SHADOW lease now drives actuation once, which is control authority — this violates INV-09 (L617: actuation port から当該 lease 由来の command が 0 件), T-11 (L646 "actuation port 出力 0 件"), §2 CommandGateway row (L82 "SHADOW lease の actuation port = DISABLED"), §5.1 (L424 "物理 command は出ない"), §5.2 (ii) which admits output only for `mode == CLOSED_LOOP_AUTHORITY` (L431), the §4.4 "本 doc は SHADOW でも authority を主張しない" claim (L404), anchor 12 (L728), EP "SHADOW は定義上 非 authority" (`EP :155`), charter S0 "zero control authority" (`:129`), and Rs C3 (slice = SHADOW, non-authority). It also contradicts `HealthConfirmation.first_command_admitted` semantics at L209 ("SHADOW: 記録された", not emitted). Since the slice runs in SHADOW mode, this single exception would make the slice a closed-loop actuation path without `granted == True` / two-key. Fix: actuation port stays DISABLED for all commands; `first_command_admitted` for SHADOW means "recorded", not "emitted".

---

## 06_pc.md (industrial deployment profile) — 4 defects

### PC-06-E — field carrying policy action-space bounds into the profile (widening path)

Line changed: **186** (inserted; the enclosing `BindingExpectation` class is L177-187; next line `\`\`\`` is L187)

- Inserted: `    action_bounds_cell: tuple[tuple[CanonicalDecimal, CanonicalDecimal], ...] | None   # policy action space での cell 固有 bounds（非 None なら INTRINSIC 項はこの値で評価。None = D1.1-B の bounds）`

A correct finding must say: the profile now carries policy-action-space bounds and the comment makes them *replace* the INTRINSIC term's D1.1-B bounds when non-None — with no `P_*` check that they are ⊆ the frozen bounds, this is a widening path over the certified action space. It contradicts §0 "持たないもの: policy action space の bounds" (L19), §1 row 1 "本 doc は写さない・上書きしない" (L30), §3 row 1 "profile 側の field = 無し … field が現れたら P_INTRINSIC_OVERRIDE" (L226), §3 (i) enumeration "action-space bounds を緩める — field が無い" (L254), §4 "INTRINSIC_D11B … D1.1-B、写さない" (L259), PT-05 (L348: `action_bounds` unknown field must be rejected — "INTRINSIC override の唯一の経路が閉じる"), anchor 1 (L407), fold-map row B (`IntrinsicExecutionLimits` 不採用, L398), 05 §4.3 "bounds は再定義しない" (05 L386) and frozen `tensor_binding :138`. Fix: delete the field; INTRINSIC term is referenced by `tensor_binding_hash` only.

### PC-06-F — `action_rate_hz` allowed `≤` instead of equality

Line changed: **228** (source L226)

- Original: `| policy_rate_hz / obs_sampling_rate_hz（:97-98）/ action_rate_hz（:126）/ hold（:127） | BindingExpectation.{…} | **等値**（== TensorBindingSpec の値） | P_RATE_MISMATCH / P_HOLD_MISMATCH |`
- Mutated: `| … | **等値**（== TensorBindingSpec の値）。action_rate_hz のみ **stricter-or-equal**（p ≤ TensorBindingSpec の値 — controller が支える周期まで下げてよい） | P_RATE_MISMATCH（action_rate_hz は p > のとき）/ P_HOLD_MISMATCH |`

A correct finding must say: running the executor at a lower rate than the trained `ActionTimingSpec.action_rate_hz` changes the closed-loop dynamics the policy was certified under (hold duration, latency, ZOH/interpolation horizon) — "narrowing" a rate is not monotone strengthening. It contradicts R1 (§1 row 3 L32 "等値のみ（R1）。profile は変えられない"; §0 L19 "制御周波数・hold の変更権（等値のみ）"), §3 (ii) enumeration L254 ("周波数・hold・control_mode を変える — 等値検査"), PT-01 (L344: declaring `action_rate_hz` smaller ⇒ `P_RATE_MISMATCH`), anchor 2 (L408), §11 L402 (the brief's "rate ≤" was explicitly superseded by R1 — critic X3), 05 §4.2 R5 "等値であって narrow ではない" (05 L374-376), 05 INV-12 (05 L620), 05 T-14 (05 L649), 05 `TimingBinding.action_rate_hz  # 等値必須` (05 L317), and `command_deadline_s ≥ 1/action_rate_hz` coupling (L145/L244). Fix: restore strict equality for all four timing facts.

### PC-06-G — `requires_safety_layer_health` made optional (False allowed for SHADOW leases)

Lines changed: **114, 235** (source L114, L233)

- L114 original: `    requires_safety_layer_health: bool     # **True 必須**（P_SAFETY_LAYER_NOT_REQUIRED）`
- L114 mutated: `    requires_safety_layer_health: bool = True   # 既定 True。SHADOW_NON_AUTHORITY lease のみ False を許容（actuation port DISABLED ゆえ）`
- L235 original: `| D0 §F safety 優先（:319） | SafetyRestrictionSet.requires_safety_layer_health | **True 必須** | P_SAFETY_LAYER_NOT_REQUIRED |`
- L235 mutated: `| … | 既定 True（SHADOW_NON_AUTHORITY lease のみ False 可） | CLOSED_LOOP_AUTHORITY で False ⇒ P_SAFETY_LAYER_NOT_REQUIRED |`

A correct finding must say: the profile can now declare that the independent safety layer's health is not required, so a profile is accepted (all `P_*` = 0) even though 05 CAS precondition 9 (05 L225: IndependentSafetyLayer health ∈ {ok, degraded} for *every* TRANSFER_TO_EXECUTOR, no mode exception) and D0 §F "heartbeat missed ⇒ safety alive を仮定しない" (`D0 :318-319`) require it unconditionally; the "SHADOW does not actuate" rationale is invalid because 05 §4.4 says all other stages are identical for SHADOW (05 L402) and the shadow run is exactly the run that must exercise the safety path before any CLOSED_LOOP lease (§6.1 `required_before_shadow_lease` includes `SAFETY_LAYER_ACCEPTANCE`, L307). Contradicts §3 (vi) enumeration L254 ("安全層を不要にする — True 必須"), PT-06 (L349, still says False ⇒ violation unconditionally — now inconsistent), anchor 7 (L413 "True 必須"), `P_HEALTHCHECK_MISSING` requiring `SAFETY_LAYER_HEARTBEAT` (L245). A default-True bool that may be False is a relaxation path in the exact place the doc claims none exist (L220 "緩和を表現する field は存在しない"). Fix: back to unconditional `True 必須` with `P_SAFETY_LAYER_NOT_REQUIRED` on any False.

### PC-06-H — EscalationPolicy may keep HOLD where the 05 default is SAFE_STOP (relaxation)

Lines changed: **169, 172 (inserted), 234** (source L169, new, L232)

- L169 original: `class EscalationPolicy:                   # 05 の既定 disposition を SAFE_STOP 側へ強化するだけ（緩和は表現不能）`
- L169 mutated: `class EscalationPolicy:                   # 05 の既定 disposition に対する cell 側の escalation 選択（clearance_roles と対で運用）`
- L172 inserted: `    on_invalid_state_unresolved: EscalationTarget   # 05 §7 既定 = SAFE_STOP。clearance_roles が整備された cell では HOLD を選択可`
- L234 original: `| fail_closed_action（:147）/ 05 既定 disposition | EscalationPolicy | **SAFE_STOP 側へのみ**（HOLD → SAFE_STOP 可・逆不可）。invalidation は False → True のみ | HOLD への緩和は型上 表現不能（EscalationTarget に「NONE」「CONTINUE」が無い） |`
- L234 mutated: `| … | HOLD ↔ SAFE_STOP の選択（05 既定 SAFE_STOP の経路も、clearance_roles が整備された cell では HOLD に留めてよい）。invalidation は False → True のみ | 「NONE」「CONTINUE」への緩和は型上 表現不能（EscalationTarget に無い） |`

A correct finding must say: the profile can now downgrade a 05-default `SAFE_STOP` disposition (Terminal INVALID_STATE unresolved — 05 §7 table L569; and frozen `FailClosedAction.SAFE_STOP → SAFE_STOP` mapping, 05 L574) to `HOLD`, which is a relaxation of a frozen fail-closed action (`contracts_v2 :147 fail_closed_action`) and of the 05 rule "profile は SAFE_STOP 側へのみ強化可" (05 L560, L286 "profile が SAFE_STOP へ強化可"). It contradicts the doc's own monotonicity claim (§3 (vii) L254 "disposition を緩める — enum に緩和値が無い"), the `EscalationTarget` enum comment (L69 "SAFE_STOP 側へのみ強化"), PT-07 (L350), anchor 6 (L412 "escalation は SAFE_STOP 側へのみ強化"), fold-map row 7 (L396), and §8.1 rule that all checks are fail-closed. Note the mutated L234 also narrows the "cannot express relaxation" claim to NONE/CONTINUE only, hiding that HOLD-from-SAFE_STOP is now expressible. Fix: remove `on_invalid_state_unresolved` (or constrain it to SAFE_STOP only), restore "SAFE_STOP 側へのみ・逆不可".

---

## Quick locator table

| id | doc | lines (mutated file) | grep anchor |
|---|---|---|---|
| PC-05-A | 05_pc.md | 231, 232, 262 | `旧 epoch 拒否の予約` / `health 確認前の姿勢保持` / `この線形化点で旧 epoch 拒否が有効化` |
| PC-05-B | 05_pc.md | 180, 612 | `expires_at\` 到達時に設定` / `R_PERMIT_EXPIRED \|` in INV-04 |
| PC-05-C | 05_pc.md | 229 | `expected.consumed_offer_ids, None` |
| PC-05-D | 05_pc.md | 400 | `初回 admit を除く` |
| PC-06-E | 06_pc.md | 186 | `action_bounds_cell` |
| PC-06-F | 06_pc.md | 228 | `action_rate_hz\` のみ **stricter-or-equal**` |
| PC-06-G | 06_pc.md | 114, 235 | `requires_safety_layer_health: bool = True` / `SHADOW_NON_AUTHORITY lease のみ False 可` |
| PC-06-H | 06_pc.md | 169, 172, 234 | `on_invalid_state_unresolved` / `HOLD ↔ SAFE_STOP` |

Expected false-positive surface (do NOT count as catches): the untouched contradictions the mutations leave behind (e.g., 05 L114/L300/L637 still state one-shot; 06 L349 PT-06 still unconditional; 06 L69 enum comment) are *evidence for* the planted defects, not separate defects.
