# R7 review — WMSO RUNTIME SPEC v0.2.7 REVIEW CANDIDATE（contract fidelity + runtime safety・adversarial）

- reviewer: R7（別 context・authority 無し・凍結物へ非接触・`eval_runs/` 配下へ書込みなし・書込み先 = `$OUT` のみ）
- 実施: 2026-09-05 23:39 – 2026-09-06 00:02 UTC（`date -u` 実測。開始 23:39:15 / pin 再測 23:39:22 / checker 23:39:28 / 最終 grep 23:51:41 / 書込開始 23:53:45 / 本報告 mtime 23:59 / findings JSON 検証完了 00:02:25）。同 `$OUT` に別 reviewer（R8）の file が並存するが、独立性のため読んでいない・触れていない
- 対象: `$PKG/05_WMSO_RUNTIME_SPEC_v0.2_REVIEW_CANDIDATE_20260903.md`（header v0.2.7・1050 行・`cat -n` で完読・sha256 `007d753d…`= SHA256SUMS:6 と一致）／ 副次 `$PKG/06_WMSO_INDUSTRIAL_PROFILE_v0.2_REVIEW_CANDIDATE_20260903.md`（v0.2.7・733 行・完読・sha256 `2d35d144…`= SHA256SUMS:7 と一致）。`$PKG` = `eval_runs/troot_optE_dapg_wholeroute_scope_20260701/WMSO_EXACT_BASELINE_REVIEW_v0.2_20260903`、`$D` = `eval_runs/troot_optE_dapg_wholeroute_scope_20260701`。HEAD = `f1393050`（branch `claude/v0-2-design-candidates-snqbmy`・`git status --short` = clean）。
- 文脈として読んだもの: 08（§6 = AI レビューは準備レビュー）／ 10 §1（注入 8 欠陥）／ 11 §8（Rs 裁定）・§10・§10.1（起草既定 awaiting Rs の bullet）／ `review_records/rs_consult/A_GPT56sol_20260904.md`／ `review_records/fold/fold_v027.py`（全 gate ブロック）／ `check_review_candidate.py`（全文）／ 様式 = `review_records/AXIS_R5_review_v026_20260903.md`。
- 凍結参照（`sed -n` で読んだ範囲）: contracts_v2 `:39` / `:62` / `:142-151` / `:306` / `:323` / `:345-354` / `:372-385` / `:477-529` / `:537` / `:550` / `:570`；tensor_binding `:4-5` / `:23-24` / `:40` / `:52` / `:65-66` / `:75-76` / `:81` / `:96-99` / `:125-138` / `:173-185` / `:255` / `:289` / `:313` / `:332` / `:337` / `:374`；D0 `:179` / `:207-223` / `:243-247` / `:267-281` / `:310-320` / `:354-372`；EP md `:101` / `:136` / `:148` / `:154-156` / `:163`；EP JSON `:5` / `:195-215`；C3 `:5` / `:32-34` / `:63` / `:65`；slice prereg `:59`；RS71 §0 `:16-34`（`:24` = #2 GRASP SPAN / FIXED BASES、`:26` = #4 GRIPPER GEOMETRY を個別 sed で再確認）。
- 前提 (3)(6) 遵守: 陽性対照の注入 8 欠陥（10_ §1）は実本文で再現しない（旧 epoch 拒否 = CAS 同一線形化点 05:245／ permit one-shot 05:192・05:244・証明 D 05:359／ CAS 事後条件に `∪ {consume_offer_id}` 05:243（checker O3 PASS）／ SHADOW actuation DISABLED 05:474／ 06 に action bounds field 無し（06:269 strict codec）／ rate 等値 06:271／ `requires_safety_layer_health` True 必須 06:149／ escalation は SAFE_STOP 側のみ 06:214）ため報告しない。Rs 裁定の設計方針（B+ / 外部 baseline / registry・acceptance record / OP-19 三層）は finding にしない。11_ §10.1 末尾 bullet・05 OP-19 残 open・06 OPP-16 に列挙された起草既定は内部不整合が無い限り finding にしない（本 review では該当なし）。

## 0. 前提の実測

### 0.1 pin 再測（`bash $PKG/verify_exact_baseline_pins.sh`・素の bash・2026-09-05 23:39:22 UTC）

```text
PASS  D1.1-A DESIGN v2.11.2 (worktree)  00192d20ca00b654cf6cfdb9d04b03ca14adfd0b93f2105c0fea28c295ff8aff
PASS  EvidencePolicy v1.9 md (worktree)  c474acea7c58acc22050c2ad9944fd45a18f5c76967964b42d11922e28fa27e7
PASS  EvidencePolicy v1.9 json (worktree)  e63176af9bc3a246b1c32db369ec59f8d09a4c96c381bb03a3a6024bd9811c6e
PASS  D1.1-B DESIGN v13 (worktree)  5a1874d3be8b98b8aeaace73890d8021cbc7f9814e048741f7f58d6746f349a6
PASS  D1.1-A DESIGN git blob id (HEAD)  1353430228a90ad36dd190a9359cd81da52e8242
PASS  D1.1-B DESIGN git blob id (HEAD)  ebe8154abf2461a6b24db05728b20e5cee949fb2
PASS  D1.1-A DESIGN sha256 of blob 135343…  00192d20ca00b654cf6cfdb9d04b03ca14adfd0b93f2105c0fea28c295ff8aff
PASS  D1.1-B DESIGN sha256 of blob ebe815…  5a1874d3be8b98b8aeaace73890d8021cbc7f9814e048741f7f58d6746f349a6
PASS  D1.1-A DESIGN at frozen commit 54f90a7d  00192d20ca00b654cf6cfdb9d04b03ca14adfd0b93f2105c0fea28c295ff8aff
PASS  D1.1-B DESIGN at frozen commit 07250f4a  5a1874d3be8b98b8aeaace73890d8021cbc7f9814e048741f7f58d6746f349a6
PASS  evidence_policy_definition_hash (embedded command)  e7ca43093084c167a209b008533a66d26a1fd3223d2a3c11274d28306c3ff803
policy_semver=1.9.0
HEAD=f1393050f1df3a82be587afc6f735b4e0cb41fcb branch=claude/v0-2-design-candidates-snqbmy
exit=0
```
→ **11/11 PASS**（原本 = `$OUT/pin_recheck_R7.txt`）。

### 0.2 機械検査（読む前に実行・2026-09-05 23:39:28 UTC）

```text
$ python3 $PKG/check_review_candidate.py $PKG/05_WMSO_RUNTIME_SPEC_v0.2_REVIEW_CANDIDATE_20260903.md --runtime
== …/05_WMSO_RUNTIME_SPEC_v0.2_REVIEW_CANDIDATE_20260903.md: citations=153 FAIL=0 WARN=11   (exit 0)
  WARN C2 line 88 / 98 / 98 / 146 / 234 / 554 / 556 / 822 / 832 / 1044 / 1044（全て C2 heuristic）
$ python3 $PKG/check_review_candidate.py $PKG/06_… --profile        （記録用・23:51:45 UTC）
== …/06_WMSO_INDUSTRIAL_PROFILE_v0.2_REVIEW_CANDIDATE_20260903.md: citations=65 FAIL=0 WARN=26   (exit 0)
```
→ **FAIL = 0**（原本 = `$OUT/checker_05_R7.txt` / `$OUT/checker_06_R7.txt`）。O1–O8 / Q1–Q2 通過。⚠ 機械検査は「fault code → 失敗表の行」「06 hash field → (j) 区間に名が現れる」の方向だけを見る。「遷移行 → fault / disposition が total」「(j) の名指しが除外文でない」「前提条件が全状態で評価可能」は本 review で手動確認し、R7-01 / R7-02 / R7-09 / R7-16 で欠落を検出した。

## 1. 判定

**PASS-WITH-CONDITIONS** — 未反証 CRITICAL / HIGH = 0。MEDIUM 7・LOW 12。全 finding は凍結 4 file への schema delta 0 の fold 案を持つ（runtime / deployment 層の文言・列挙・1 conjunct 追加のみ）。P1–P5（05 §3.1）は v0.2.7 本文で反例を構成できなかった（§4）。

| severity | 件数 | id |
|---|---|---|
| CRITICAL | 0 | — |
| HIGH | 0 | — |
| MEDIUM | 7 | R7-01 / R7-02 / R7-03 / R7-04 / R7-05 / R7-06 / R7-07 |
| LOW | 12 | R7-08 〜 R7-19 |

MEDIUM の型: (a) v0.2.7 で追加した前提条件 (m) が SAFEHOLD からの permit で評価不能（= FALSE）になり liveness deadlock（R7-01）、(b) head-check 行が registry の PROPOSED head を列挙せず良性の再登録が SAFE_STOP に写る（R7-02）、(c) v0.2.7 の role registry に OP-19 三層のうち event / 周期層の検出者が無い（R7-03）、(d) manager 不読で (iii) が SafeStop → SafeHold に緩む（R7-04）、(e) lease の `ownership` が admission で執行されない（R7-05）、(f) 06 `P_VALIDITY_UNBOUNDED` の上限項が `CalibrationRef` で評価不能 = 全 profile 登録拒否（R7-06）、(g) 06 MIN_FAULT_INJECTION が timeout 経路を要求できない bare code を残す（R7-07）。

## 2. PASS 1 — v0.2.7 fold 表（05 §13 05:1009-1023・15 行 + 06 §11 05 接触行）の回帰

各行について (a) 変更が実在するか、(b) 変更が完全か（隣接規則・05 ↔ 06 の同期）、(c) 副作用（新たな不整合・未定義記号・到達不能・評価不能）を本文 grep / sed で確認した。

| # | fold 行 | (a) 実在 | (b) 完全性 | (c) 副作用 | 判定 |
|---|---|---|---|---|---|
| 1 | R5-01 / R6-16 — SHADOW の deadline 扱い統一 | ✓ 05:829（OP-19「SHADOW も mode を問わず…保守側に統一」）・05:342（失敗表「活性 lease（mode を問わず）」）・05:290・05:429・05:744 INV-34 | ✓ 06:345（§5 (b)）・06:448（PT-13）・06:525（§8.3）・06:673（§11 裁定行）が全て同期 | 裁定（`A_GPT56sol:41`「SHADOW は次 permit 評価を許容できる」）より厳格側の選択で裁定の許容範囲内。⚠ 11_ §10.1 末尾の「起草既定 awaiting Rs」bullet には載っていない（情報として付記。finding にしない） | **OK** |
| 2 | R5-02 — head-check 行の total 化・event 行の到達不能 code 除去 | ✓ 05:292 副場合 (a)-(e) に fault / disposition・05:291 event 行から `R_EVIDENCE_EXPIRED_ACTIVE` 除去・05:829 残 open | △ (a) は head.state ∈ {SUSPENDED, REVOKED} と「新 generation の ACCEPTED」しか列挙せず、06:530 が明示的に許す **PROPOSED(gen+1) head** が無い → R7-02。兄弟 REVOKED の sub 無し | 新 payload field `sub` の語彙が §8 SSOT（05:702）に無い・(e) の `（BASELINE_SUPERSEDED）` が trigger か sub か不明 → R7-09 | **R7-02（MEDIUM）・R7-09（LOW）** |
| 3 | R5-03 — permit 前提 (m)・§5.4 の解消条件 | ✓ 05:191 (m)・05:536（§5.4「(b)(i)(l)(m)」）・05:343（失敗表）・05:791 T-40 | ✗ (m) は「直近の head-check が周期内に成功 ∧ diagnostic_items 全項目が直近周期内に pass（評価不能 / 欠落 = FALSE）」を要求するが、head-check 行の from 列 = `any (EXECUTOR)`（05:292）・identity 系 item は「lease 中に周期再実行」（06:531）→ SAFEHOLD（INITIAL / genesis 直後 / 1 周期超の HOLD）からの最初の permit で (m) = FALSE → 恒久 deadlock。05:88「周期実行する」（無条件）と from 列が矛盾 | (i)/条件 12 に baseline registry head の等値が無く (e) は SAFEHOLD で走らない → R7-15 | **R7-01（MEDIUM）・R7-15（LOW）** |
| 4 | R5-05 — TRANSFER_TO_EXECUTOR で `safehold_disposition := None`・INV-01 | ✓ 05:243・05:711・05:790 T-39 | ✓ 05:139（条件 1 の等値対象外）・05:143 初期 HOLD・05:249 no-op max・MARK_BOUNDARY_WAIT「他の要素不変」と整合 | — | **OK** |
| 5 | R5-06 — `R_AUDIT_UNAVAILABLE`・失敗表・記録が先・INV-17 | ✓ 05:649 enum・05:345 行・05:702 §8・05:727 INV-17・05:789 T-38・05:292 (d) recorder・06:393 MIN・06:531 `AUDIT_RECORDER_LIVENESS` | ✓ head-check (d)「他の項目 = HOLD」と失敗行 HOLD が一致。T-38「executor へ LEASE_ACTIVATED が通知されない」と 05:345 一致 | (l) が「直前の PERMIT_ISSUED の wall」を audit から読む（05:191）→ recorder 不能時に評価不能 → `R_CLOCK_ANOMALY` に誤帰属 → R7-13 | **OK + R7-13（LOW）** |
| 6 | R5-07 — 条件 2 / 3 / 6・(e) の厳密境界・CAS 時失効の行 | ✓ 05:231 `now < ack.valid_until`・05:234 `< decision_max_age_s`・05:191 (e) `age <`・05:303 `now ≥`・05:306 / 05:307 行・05:792 T-41 | ✗ INV-35（05:745）は `decision age ≤ decision_max_age_s` のまま；INV-07（05:717）`ack.valid_until ≥ permit.issued_at` は `now == ack.valid_until` で発行された permit（条件 2 で即死）を許す | — | **R7-08（LOW）** |
| 7 | R5-08 / R6-21 — ACCEPTANCE 到達 = `R_PROFILE_SUSPENDED`・sub-kind の解決元 | ✓ 05:290・05:341・05:184・05:744 INV-34 | ✓ §8 trigger に `R_PROFILE_SUSPENDED: … DEADLINE`（05:702）・06:346「ACCEPTANCE → HOLD」 | — | **OK** |
| 8 | R5-10 / R6-03 — trigger 語彙 SSOT | ✓ 05:702・06:374 `FaultInjectionRequirement.trigger`・06:300 `P_FAULT_CODE_UNKNOWN`・06:296 `code[#trigger]`・06:392 の `#DEADLINE` / `#EVENT` は語彙内 | ✗ 06:391 は `R_SAFETY_LAYER_LOST` / `R_BOUNDARY_DWELL_EXCEEDED` を bare のまま MIN に置き、06:394-395「timeout 経路そのものを要求する（health==failed / attempts 尽きで代替できない）」が `trigger = None` で空文化 → R7-07 | `DIAGNOSTIC:<item>` の `<item>` 語彙が P_FAULT_CODE_UNKNOWN に束縛されない → R7-09 に含む | **R7-07（MEDIUM）** |
| 9 | R5-11 — refuted・適用せず | ✓ `VALIDITY_DEADLINE_DETECTION` / 「manager から通知」= 05 / 06 とも grep 0（fold script の R5-11 ブロックは gate で不発） | — | — | **OK** |
| 10 | R5-12 — 兄弟 SUSPENDED / REVOKED を head 状態の述語で (i)・条件 12・head-check に | ✓ 05:191 (i)・05:240・05:292 (a)・05:793 T-42・06:524（原子 batch） | ✓ 3 箇所の述語が同文 | REVOKED は終端（06:486）で「退役」状態が無いため、良性理由で REVOKED した兄弟が cell を恒久 lock → R7-19（Rs 事項・open item 化） | **OK + R7-19（LOW）** |
| 11 | R6-05 / R6-06 — role registry 照合（(i)・条件 12・clearance record） | ✓ 05:191・05:240・05:543・06:529・06:488 `ROLE_REGISTRY_SUPERSEDED`・06:346・06:220・06:479 PT-46 | △ role registry head の変更を検出する義務者が無い: 05:88 monitor 最小入力に role registry 無し／ head-check に (f) 無し／ 06:529「認可された一者または monitor」は許容文 → R7-03。(i) の `P_ACCEPTANCE_RECORD_INVALID` head 再評価（→ 05:191 (b) 経由 `R_PROFILE_MISBOUND`）と R6-05 節（→ `R_PROFILE_SUSPENDED`）が同一条件（role_registry_hash ≠ head・06:530 にも同節）に 2 code → R7-10 | genesis record の role（05:143）は registry 資格照合が無い → R7-14 | **R7-03（MEDIUM）・R7-10 / R7-14（LOW）** |
| 12 | R6-10 — baseline registry の定義先 | ✓ 05:88・05:292 (e)・06:528 | △ (i) は `timing_baseline_hash` の artifact を「解決して再検証」（content 整合）するだけで head 等値を見ない；条件 12 も無し。(e) は from = EXECUTOR → R7-15 | — | **R7-15（LOW）** |
| 13 | R6-17 — 失効 disposition の根拠を 06 表へ | ✓ 05:341・06:346（FI subject `R_SAFETY_LAYER_LOST#HEARTBEAT_TIMEOUT` → SAFE_STOP）・06:298 kind 集合 | ✓ | — | **OK** |
| 14 | R6-01 — `finger_geometry_sha256` の (j) 列挙 | ✓ 05:191 (j)・06:125-126・06:315 `P_TOOL_GEOMETRY_MISMATCH`・06:310・06:363・06:477 PT-44・06:428 第 1 評価点 list。RS71 `:26` = #4、`:24` = #2 を sed で確認 | ✓ Q2 PASS（`finger_geometry_sha256` 1 class・1 回） | — | **OK** |
| 15 | R6-13 — (j) 区間の除外宣言（checker Q2 強化と対） | ✓ 05:191「(j) の対象外 = `binding_expectations[].tensor_binding_hash`」・checker Q2 = §2.1–2.2 走査・class ごと 1 回 | ✗ Q2 は除外文の中の名指しも「現れる」と数える（checker :189-195 は区間文字列全体で count）→ R6-13 の強化は除外文で満たされる → R7-16 | — | **R7-16（LOW）** |
| 06-a | R5-04 / R5-14 / R6-11 / R6-17 — `P_VALIDITY_UNBOUNDED` の評価点・kind・上限 | ✓ 06:298・06:428 両評価点・06:512 `max_validity_s`・06:368 | ✗ 上限項 `valid_until − measured_at` の `measured_at` が `CalibrationRef`（06:202-206）に無い → 第 1 評価点（MIN_SHADOW = CALIBRATION_RECORD 非必須）で評価不能 = 違反（06:428 規則 (1)）→ 全 profile 登録不能 → R7-06 | — | **R7-06（MEDIUM）** |
| 06-b | R5-09 / R6-12 — §5 写像文「4 項」 | ✓ 06:344 | ✓ 05:369 enum 5 member・06:327-331 | — | **OK** |
| 06-c | R5-13 — ACCEPTED → ACCEPTED(gen+1)・GENERATION_SUPERSEDED → HOLD | ✓ 06:522・06:346・05:292 (a) | △ 06:530 は PROPOSED(gen+1) を同時に許す（head = seq 最大・06:532）→ R7-02 | — | **R7-02 参照** |
| 06-d | R5-06（06 側）/ R6-02 / R6-20 / R6-04 / R6-07 / R6-08 / R6-09 / R6-14 / R6-15 / R6-18 / R6-19 | ✓ 06:393・06:299・06:363・06:530・06:513-514・06:531 `REQUIRED_DIAGNOSTIC_ITEMS`・06:314・06:428・06:287・06:296・06:289・06:500・06:547 | ✓ 05 が名指す baseline 項目（`VALIDITY_MONITOR_LIVENESS` / `AUDIT_RECORDER_LIVENESS` / drift / head-check）は全て 06:531 の閉じた語彙に在る | — | **OK** |

## 3. Findings（id / severity / 候補 doc:line / 凍結 file:line / 再現 / 最小修正）

### R7-01（MEDIUM）permit 前提条件 (m) は SAFEHOLD からの permit で「評価不能 = FALSE」になり、genesis 直後・1 周期を超える HOLD の後に permit を二度と発行できない（head-check 行の from 列と 05:88 の矛盾・R5-03 fold の副作用）

- 候補: 05:191 (m)「DeploymentValidityMonitor の直近 heartbeat が … 内 ∧ baseline `diagnostic_items` の全項目が直近周期内に pass（評価不能 / 欠落 = FALSE）∧ 直近の head-check が周期内に成功。違反 = `R_REGISTRY_UNAVAILABLE`」・05:292 head-check 行の from 列 = `any (EXECUTOR)`・05:88「manager は monitor に依存しない head-check（§3.7）を周期実行する」（owner 条件なし）・06:531「identity 系 4 項目 = `health_checks[]` の同 kind の check_id を **lease 中に**周期再実行（結果は (m) と head-check が参照）」・05:143 初期状態 = SAFEHOLD(INITIAL)。
- 凍結: D0 `$D/WMSO_D0_ARCHITECTURE_DRAFT_RSTECHLEAD2_20260718.md:273-274`（fail-closed は「producer retains + safe-stops」であって「二度と再開しない」ではない）。方向は保守側で凍結違反ではないが、本文どおりでは実装できない（liveness 0）。
- 再現: genesis 投入 → 起動 = SAFEHOLD(INITIAL)。§3.7 の head-check 行は from = `any (EXECUTOR)` なので一度も走らない；identity 系 diagnostic item は「lease 中に」しか走らない。最初の permit 要求で (m) を評価: 「直近の head-check」= 不在 → 欠落 = FALSE；identity item の「直近周期内の pass」= 不在 → FALSE。⇒ `R_REGISTRY_UNAVAILABLE`（trigger = HEAD_CHECK | DIAGNOSTIC:<item>）・permit 不発行。以後この状態は変わらない（head-check は EXECUTOR でしか走らない）。同型: どの SAFEHOLD でも滞留が 1 周期を超えれば同じ。05:88 の無条件文を採れば head-check だけは走るが、identity item（06:531「lease 中に」）は依然 FALSE。(a) の BEFORE_PERMIT 実行を diagnostic item の結果として数えるかは書かれていない。
- 最小修正（schema delta 0）: (1) 05:292 の from 列を `any`（SAFEHOLD でも周期実行）に改め、05:343 と整合させる；(2) 05:191 (m) に「SAFEHOLD 中は head-check と非 identity 項目（LIVENESS / DRIFT / HEAD_CHECK / RECORDER）を周期実行し、identity 系 item は (a) の BEFORE_PERMIT 実行（同 kind の check_id）を当該周期の結果として扱う」を明記；(3) 06:531「lease 中に」→「lease 中および SAFEHOLD 中（BEFORE_PERMIT 実行を含む）」。T-40 に「INITIAL からの最初の permit が (m) を通る」対照 case を追加。

### R7-02（MEDIUM）head-check (a)・(i)・条件 12 は registry の `PROPOSED` head を列挙しておらず、06 が明示的に許す gen+1 の PROPOSED 登録（R5-13 の再受理 workflow の第 1 歩）が活性 CLOSED_LOOP lease を「表に無い = SAFE_STOP」で止める

- 候補: 05:292 (a)「`record_hash ≠ … ∨ state ≠ ACCEPTED`」→ disposition は「head.state ∈ {SUSPENDED, REVOKED} = suspension_reason を表で（REVOKED / 不読 = SAFE_STOP）、head が新 generation の ACCEPTED = HOLD」のみ・FAULT sub = `SUSPENDED | REVOKED | GENERATION_SUPERSEDED | SIBLING_SUSPENDED`；06:530「state ∈ {PROPOSED, ACCEPTED} の record について: generation が同一 profile_hash の直前 ACCEPTED / PROPOSED record + 1 でない」= PROPOSED(gen+1) を ACCEPTED head の後に append 可；06:532「head = registry append seq 最大」；06:522 ACCEPTED → ACCEPTED(gen+1)「受理条件は同じ」・06:428 規則 (2)「第 1 評価点 = PROPOSED record の書込みの下で評価」（再受理も PROPOSED を経る読みが自然）；06:346「表に無い項目 = SAFE_STOP」。
- 凍結: 該当なし（runtime / deployment 層の内部 totality）。
- 再現: profile P・gen 1 ACCEPTED・CLOSED_LOOP lease L 活性。evidence 追加のため commissioning が PROPOSED(gen 2) を append（06:530 で合法）。次の head-check: `head(registry, P)` = PROPOSED(gen 2)、`record_hash ≠ L.acceptance_record_hash ∧ state ≠ ACCEPTED` → (a) 発火。disposition: SUSPENDED / REVOKED でも新 generation ACCEPTED でもない → 表に無い → SAFE_STOP。FAULT の sub は語彙に無い。L は SAFE_STOP で停止し、§5.4 SAFE_STOP 行の operator clearance が要る。R5-13 が設計した HOLD(GENERATION_SUPERSEDED) には到達しない（ACCEPTED(gen 2) が書かれる前に止まる）。同じ head 読みで (i) / 条件 12 も `state == ACCEPTED` 偽 → `R_PROFILE_SUSPENDED`（意図どおり拒否だが sub 無し）。
- 最小修正（schema delta 0）: 05:292 (a)・05:191 (i)・05:240 条件 12・06:532 の「head」を「state ∈ {ACCEPTED, SUSPENDED, REVOKED} の record のうち append seq 最大（PROPOSED は authority を持たないので head 判定から除外）」と定義する。代替: (a) に副場合「head.state == PROPOSED（gen+1）= HOLD・sub = GENERATION_PROPOSED」を足し、06:346 に同 key を追加。

### R7-03（MEDIUM）v0.2.7 で追加した role registry の head 変更に、OP-19 三層のうち event 層の義務者も周期層（head-check）の副場合も無い（新型が検出網に結線されない pattern）

- 候補: 06:529「head の変更 = supersession: 認可された一者または monitor が bound する全 ACCEPTED record に `SUSPENDED(ROLE_REGISTRY_SUPERSEDED)` を append」（許容文・義務者なし）；05:88 monitor の最小入力 = 「controller / ISL / tool の identity 報告・layout と baseline registry の supersession・registry の state 変化」（role registry 無し）；05:292 head-check は (e) で baseline registry head を monitor 非依存に検出するが role registry の (f) が無い；05:191 (i)・05:240 条件 12 は permit / CAS 時にしか head 等値を見ない；05:829 OP-19「(2) 変更 event の即時通知 … (3) 周期診断」。
- 凍結: D0 `$D/WMSO_D0_ARCHITECTURE_DRAFT_RSTECHLEAD2_20260718.md:318`（監視の欠落 ⇒ alive を仮定しない）。role registry は v0.2.7（R6-05）で「資格の SSOT」になったが、その supersession を誰も検出しない構成は「alive を仮定」する側。
- 再現: CLOSED_LOOP lease L 活性（acceptance record A・`A.role_registry_hash = h1`）。role registry head が h2 に変わる（例: SAFETY_APPROVER の資格失効）。monitor は 05:88 の入力に無いので event を出さない；「認可された一者」が append しなければ registry は無変化；head-check (a)-(e) は全て pass（A は ACCEPTED のまま・baseline 不変）。L は `lease_max_duration_s` / validity deadline まで走る。次の permit で (i) が `R_PROFILE_SUSPENDED` を出す（05:191）ので新 lease は止まるが、活性 lease の無効化という OP-19 (2)(3) の要求が role registry に対しては無い。
- 最小修正（schema delta 0）: (1) 05:88 の最小入力に「role registry（06 §8.3・1 head）の supersession」を追加；(2) 05:292 に副場合 (f)「`head(role_registry) ≠ record.role_registry_hash`（R6-05）→ FAULT `R_PROFILE_SUSPENDED`（trigger = HEAD_CHECK・sub = ROLE_REGISTRY_SUPERSEDED）→ disposition = 06 §5 (b) ROLE_REGISTRY_SUPERSEDED 行（HOLD）」；(3) 06:529「認可された一者または monitor が … append」→「monitor は event を出し（05 §2）、manager は head-check (f) で monitor 非依存に検出する（(e) と同型）」。

### R7-04（MEDIUM）CommandGateway 出力 (iii) は `safehold_disposition` の線形化読みに依存するため、manager 不読（`R_MANAGER_UNAVAILABLE`）の間は SAFE_STOP disposition の SAFEHOLD が SafeStop から SafeHold へ緩む（05:249 の「clearance record まで SafeStop」が manager 喪失で破れる）

- 候補: 05:520 (iii)「else SafeHold（`state.safehold_disposition == SAFE_STOP` なら SafeStop — 線形化読み）」；05:525 / INV-19 05:729「読みが `manager_liveness_s` 内に完了しないとき (ii) を評価しない」（(iii) の SafeStop 分岐の扱いは無し）；05:327 失敗表 `R_MANAGER_UNAVAILABLE`「出力 = (iii) **SafeHold**」；05:82 gateway の状態 = 線形化読み + 直近 admitted command + `restart_pending`（disposition の局所 copy 無し・「cache を持たない」）；05:249「(iii) は … 当該 SAFEHOLD 遷移の記録 disposition が SAFE_STOP なら §5.4 SAFETY 行の clearance record が記録されるまで SafeStop を出す」；06:66 `StopClass` = CONTROLLED_STOP | POWER_REMOVED_STOP。
- 凍結: D0 `$D/WMSO_D0_ARCHITECTURE_DRAFT_RSTECHLEAD2_20260718.md:318`「missed heartbeat ⇒ consumers must not assume … alive」— manager 不読を「disposition は HOLD」と読むのは同じ原則の逆（評価不能を弱い側に倒している）。ISL 起因の停止は (i)/(i′) が独立に守るので本 finding の対象外。
- 再現: TCP calibration 期限到達 → TRANSFER_TO_SAFEHOLD(SAFE_STOP)（05:249 の SAFE_STOP 規則）・`safehold_disposition = SAFE_STOP` → gateway 出力 = SafeStop（stop-class 要求）。manager ↔ gateway link 断（manager は生きている）。gateway の線形化読みが `manager_liveness_s` を超える → `R_MANAGER_UNAVAILABLE` → 出力 = (iii) SafeHold（05:327 逐語）。(iii) の `safehold_disposition == SAFE_STOP` は評価不能 → 既定「評価不能 = FALSE」→ SafeHold。link 復帰まで stop-class 要求が外れる。ISL 決定は無いので (i)/(i′) は効かない。
- 最小修正（schema delta 0）: 05:82 の gateway 局所状態に `last_disposition`（直近の線形化読みで観測した `safehold_disposition`・`restart_pending` と同格の局所 flag）を加え、05:520 (iii) を「線形化読みが完了しない場合は `last_disposition == SAFE_STOP` なら SafeStop、未観測（起動直後）なら SafeHold（INV-25）」とする；05:327 の効果列を「(iii)（直近観測 disposition を保持）」に；INV-19 に同文を追記。T-27 と同型の T（SAFE_STOP 中に manager link 断）を追加。

### R7-05（MEDIUM）CommandGateway の admission は command の資源（`arm_targets[].resource_id`）が lease に束縛された `ownership.control` の内側であることを検査せず、gripper 資源は `GatewayCommand` に現れない — frozen `required ⊆ offered` は invocation 開始時の validator 検査に留まり、lease の `ownership` は actuation 境界で不活性

- 候補: 05:82 admission = `(lease_id, control_epoch)` 等値 ∧ `cmd.executor_id == lease.executor_id` ∧ `¬boundary_wait` ∧ `AcceptedEnvelope` 連言 ∧ command 種別 ∧ `cmd_seq` 単調（ownership 無し）；05:508 `GatewayCommand.arm_targets: (resource_id ∈ {ee_left, ee_right}, setpoint_ref)`（gripper 無し）；05:426 `ActiveAuthorityLease.ownership: Ownership`「offered 集合。required_control_resources ⊆ ownership.control」；05:438「profile・authority decision・初期化・envelope・timing を**完全に** lease へ束縛」；05:441；06:328 CONTROLLER 項は `cmd.arm_targets[].resource_id` で index（lease.ownership を見ない）；INV-14（05:724）は definition ↔ lease のみ。
- 凍結: contracts_v2 `$D/WMSO_D11A_CONTRACTS_V2_DESIGN_RSTECHLEAD2_20260719.md:380`（required ⊆ offered は `validate_invocation_start` の検査）・`:506` `HandoffOffer.ownership: Ownership`・`:151` `ControlResourceSpec = {ee_left, ee_right, gripper_left, gripper_right: bool}`（部分 ownership は frozen で表現可能）。frozen は runtime 側の執行点を定めていないので凍結違反ではないが、05 が「完全束縛」と主張する field が admission に現れない。
- 再現: producer が `HandoffOffer.ownership.control = {ee_left: True, ee_right: False, gripper_left: True, gripper_right: False}` を出す（frozen 型上合法）。consumer definition の required = {ee_left, gripper_left} → `validate_invocation_start` pass → lease L（ownership = 上記）。L の executor が `arm_targets = ((ee_left, s1), (ee_right, s2))` の command を出す。admission: 全条件 pass（CONTROLLER 項は `arms[ee_right].controller` で評価・INTER_ARM は両線分の対で評価）→ admit → ee_right が動く。§11 項 5 は「2 executor の同時保持」を OUT と言うが、本 case は 1 executor である。RS71 §0 #1 の every-motion 両腕 regime では offer が両腕 True になるのが通常で、その場合 conjunct は恒真 — ゆえ被害は regime 外に限られるが、guard の追加は 1 conjunct で済む。
- 最小修正（schema delta 0）: 05:82 / INV-03 に conjunct「`∀ (r, _) ∈ cmd.arm_targets: lease.ownership.control[r] == True`（未知 r / False = CONTROLLER 項 評価不能 = FALSE → `R_ENVELOPE_VIOLATION`・新 code 不要）」を追加；05:508 に「gripper 資源の command は本 record に無い（gripper 動作の admission = §12 に OP として記録）」を明記するか、`gripper_targets` を追加して同じ conjunct を掛ける。

### R7-06（MEDIUM）06 `P_VALIDITY_UNBOUNDED` の上限項 `valid_until − measured_at > baseline.max_validity_s[kind]`（R6-11）は `CalibrationRef` に `measured_at` が無いため第 1 評価点で評価不能 = 違反となり、全 profile が登録できない

- 候補: 06:298（上限項・「baseline に当該 kind の上限が無い = 違反」・評価点 = §8.1 (2) の両評価点）；06:202-206 `CalibrationRef = {calibration_id, kind, artifact_sha256, valid_until_iso8601}`（`measured_at` 無し）；06:428 規則 (1)「全検査は fail-closed（評価不能 = 違反）」・第 1 評価点 list に「`P_VALIDITY_UNBOUNDED`（`CalibrationRef` 側 + MIN_SHADOW の SAFETY_LAYER_ACCEPTANCE）」；06:388 MIN_SHADOW に CALIBRATION_RECORD 無し（→ 第 1 評価点で `measured_at_iso8601` を持つ record が存在しない）；06:366 `DeploymentEvidenceRecord.measured_at_iso8601`。
- 凍結: 該当なし（deployment 層の型不整合）。
- 再現: 任意の profile を登録（第 1 評価点・PROPOSED record）。`P_VALIDITY_UNBOUNDED` の `CalibrationRef` 側を評価: None 検査は pass しても上限項の `measured_at` が型に無い → 評価不能 → 違反 → 登録拒否。PT-43（06:476）は第 2 評価点の record 側しか試験していない。
- 最小修正（schema delta 0）: 06:298 を「`CalibrationRef` 側 = 第 1 評価点では None 検査のみ；上限項は subject = `calibration_id` の CALIBRATION_RECORD の `measured_at` で第 2 評価点に評価（record 不在 = `P_EVIDENCE_UNBOUND` 側）」と分離するか、`CalibrationRef` に `measured_at_iso8601: str` を追加（deployment 層の型・凍結外）。06:428 第 1 評価点 list の括弧を同期。

### R7-07（MEDIUM）06 MIN_FAULT_INJECTION は `R_SAFETY_LAYER_LOST` / `R_BOUNDARY_DWELL_EXCEEDED` を bare code のまま置くため、同 block が要求する「timeout 経路そのもの」（`#HEARTBEAT_TIMEOUT` / `#DWELL_TIMEOUT`）を `trigger = None` で回避できる（R2-01 → R5-10 / R6-03 fold の未完）

- 候補: 06:391（bare `R_SAFETY_LAYER_LOST`, `R_BOUNDARY_DWELL_EXCEEDED`）↔ 06:392（`R_CALIBRATION_EXPIRED_ACTIVE#DEADLINE` 等は `#trigger` 付き）；06:394-395「timeout 駆動 code の FAULT_INJECTION_RESULT は subject_ref = '<code>#<timeout_trigger>' … で timeout 経路そのものを要求する（… subject = trigger が None なら code・あれば code#trigger … health==failed / attempts 尽きの別経路で代替できない）」；06:379 `P_EVIDENCE_POLICY_TOO_WEAK` = MIN を「含む」か；06:296 `P_EVIDENCE_UNBOUND` = `subject_ref == runtime_fault_code[#trigger]`（宣言どおりに照合するだけ）；05:702 §8 語彙 `R_SAFETY_LAYER_LOST: HEARTBEAT_TIMEOUT | HEALTH_FAILED`・`R_BOUNDARY_DWELL_EXCEEDED: DWELL_TIMEOUT | ATTEMPTS_EXHAUSTED`。
- 凍結: 該当なし（06 内の規範文と集合定義の不一致）。
- 再現: policy に `FaultInjectionRequirement(R_BOUNDARY_DWELL_EXCEEDED, CLOSED_LOOP, trigger = None)` を置く → MIN に code が含まれるので `P_EVIDENCE_POLICY_TOO_WEAK` 通過 → 要求 subject = `R_BOUNDARY_DWELL_EXCEEDED`（bare）→ `max_reselect_attempts` を尽くした FAULT（payload.trigger = ATTEMPTS_EXHAUSTED）の audit 範囲で `P_FAULT_EVIDENCE_UNVERIFIED` の `<code>` 枝（trigger を問わない）が pass → dwell timeout 経路が一度も発火せずに CLOSED_LOOP 受理。`R_SAFETY_LAYER_LOST` も同型（`P_SAFETY_BUDGET_EXCEEDED` が `#HEARTBEAT_TIMEOUT` record を別途要求するため間接的に止まるが、MIN 側は空文）。
- 最小修正（schema delta 0）: 06:391 を `R_SAFETY_LAYER_LOST#HEARTBEAT_TIMEOUT`, `R_BOUNDARY_DWELL_EXCEEDED#DWELL_TIMEOUT` に書き換え（06:392 と同形式）、`P_EVIDENCE_POLICY_TOO_WEAK` が `code#trigger` 単位で包含を判定すると明記。PT-24 に「trigger = None で MIN の timeout code を宣言 → `P_EVIDENCE_POLICY_TOO_WEAK`」を追加。

### R7-08（LOW）R5-07 の厳密境界化が INV-35 / INV-07 に及んでいない

- 候補: 05:745 INV-35「decision age ≤ decision_max_age_s（条件 6・12）」↔ 05:234 条件 6 `<`・05:191 (e) `age <`；05:717 INV-07「`ack.valid_until ≥ permit.issued_at`」↔ 05:188 `effective_expires_at = min(…, ack.valid_until, …)` と 05:230 条件 2 `now < effective_expires_at`。
- 再現: (1) age == max で条件 6 は REJECTED だが INV-35 は充足 → 機械検査と CAS が食い違う。(2) `ack.valid_until == issued_at` の permit は INV-07 を満たして発行されるが `effective_expires_at = issued_at` で条件 2 は発行直後から偽（dead-on-arrival permit・`R_ACK_MISBOUND`）。
- 最小修正: INV-35 を `<` に；INV-07 を `ack.valid_until > permit.issued_at` に；05:191 (k) に「`effective_expires_at > issued_at` でなければ permit 不発行（`R_ACK_MISBOUND` / `R_PERMIT_EXPIRED` の優先規則に従う）」を 1 文。

### R7-09（LOW）head-check 行が導入した FAULT payload の `sub` 語彙・(e) の `（BASELINE_SUPERSEDED）`・`DIAGNOSTIC:<item>` の `<item>` が §8 SSOT に束縛されない（新 field / 語彙が列挙されない pattern）

- 候補: 05:292「FAULT: (a) R_PROFILE_SUSPENDED（payload trigger = HEAD_CHECK・sub = SUSPENDED | REVOKED | GENERATION_SUPERSEDED | SIBLING_SUSPENDED）… (e) R_PROFILE_SUSPENDED（BASELINE_SUPERSEDED）」；05:702 §8「trigger 語彙の SSOT = 本列挙」に `sub` は無く、`R_PROFILE_SUSPENDED` の trigger = EVENT | HEAD_CHECK | CAS_COND12 | PERMIT | DEADLINE（BASELINE_SUPERSEDED 無し）；06:300 `P_FAULT_CODE_UNKNOWN` は `trigger ∉ 05 §8 語彙` を見るが `DIAGNOSTIC:<item>` の `<item>` の語彙（06:531 REQUIRED_DIAGNOSTIC_ITEMS ∪ baseline 項目名）を参照しない；兄弟 REVOKED の sub が無い（SIBLING_SUSPENDED のみ）。
- 再現: (e) を「trigger = BASELINE_SUPERSEDED」と実装すると 06 の FI requirement は `P_FAULT_CODE_UNKNOWN` で宣言不能・`P_FAULT_EVIDENCE_UNVERIFIED` の `<code>#<trigger>` 枝も一致しない。`sub` は `RuntimeAuditRecord.payload_ref` の typed payload に無い field。
- 最小修正: 05:702 に「`sub` 語彙（R_PROFILE_SUSPENDED のみ）= SUSPENDED | REVOKED | GENERATION_SUPERSEDED | SIBLING_SUSPENDED | SIBLING_REVOKED | BASELINE_SUPERSEDED（| ROLE_REGISTRY_SUPERSEDED — R7-03 採用時）」を追加し、(e) を「trigger = HEAD_CHECK・sub = BASELINE_SUPERSEDED」に統一；`DIAGNOSTIC:<item>` の `<item>` ∈ REQUIRED_DIAGNOSTIC_ITEMS ∪ `baseline.diagnostic_items` 名と明記（06:300 の検査に追記）。

### R7-10（LOW）acceptance record の head 再評価失敗に 2 つの code が当たる／「registry 不読 = CAS 拒否」の文が TRANSFER_TO_SAFEHOLD まで含むと読める

- 候補: 05:191 (b)「doc 06 §8.1 (2) の全 code … 失敗 = `R_PROFILE_MISBOUND`」＋ 06:428 第 2 評価点 list に `P_ACCEPTANCE_RECORD_INVALID`（head record 再評価）↔ 05:191 (i)「`record.role_registry_hash` == 現 head（不一致 = `R_PROFILE_SUSPENDED`）」＋ 06:530 `P_ACCEPTANCE_RECORD_INVALID` の定義に「`role_registry_hash ≠ role registry の現 head`」節。同一条件（head の role_registry_hash ≠ 現 head）が (b) 経由なら MISBOUND、(i) 節なら SUSPENDED。／ 05:343「起動時 / permit 発行 / CAS では拒否」・06:526「permit 発行・CAS・actuation は全面拒否」↔ 05:249 TRANSFER_TO_SAFEHOLD の前提 = 条件 1 のみ（registry 非依存）。
- 再現: role registry head 変更後に permit 要求 → 実装が (b) を先に評価すれば `R_PROFILE_MISBOUND`（disposition HOLD・05:324）、(i) を先に評価すれば `R_PROFILE_SUSPENDED`（同 HOLD）。FI evidence の subject（`R_PROFILE_SUSPENDED#PERMIT`）照合と T-25 の決定論が実装順に依存。／ registry 不読中に ISL STOP → TRANSFER_TO_SAFEHOLD(SAFETY) の CAS を 06:526 の字義で拒否すると SafeHold へ落ちる CAS が無い（(i) 出力は独立に出るが状態遷移が止まる）。
- 最小修正: 05:191 (i) に「head 再評価の `P_ACCEPTANCE_RECORD_INVALID` は role_registry_hash 節を除いて評価する（同節は本 (i) の `R_PROFILE_SUSPENDED`）」を 1 文；05:343 / 06:526 / 05:787 T-36 の「CAS」を「TRANSFER_TO_EXECUTOR の CAS」に限定（TRANSFER_TO_SAFEHOLD は registry 非依存と明記）。

### R7-11（LOW）profile-scoped event の再試行述語が `profile_hash` だけを比較し、event が運ぶ `acceptance_record_hash` を使わない — 旧 generation の遅延 / 再送 event が新 generation の有効 lease を無効化する

- 候補: 05:249「event は `profile_hash` + `acceptance_record_hash` を運び、再試行は `current.active_lease.profile_hash == trigger.profile_hash` の間続ける」；05:291 event 行「registry に SUSPENDED record を append した後の通知」；06:522 ACCEPTED → ACCEPTED(gen+1)（同一 profile_hash で generation が進む）。
- 再現: gen 1 SUSPENDED(OPERATOR) → 新 gen 2 ACCEPTED → lease L2（gen 2）。gen 1 の event が遅延 / 再送で到着（profile_hash 同一）→ 再試行述語が真 → TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED) → L2 無効（spurious・保守側）。manager は event を registry の線形化読みで検証していない。
- 最小修正: 述語を `current.active_lease.acceptance_record_hash == trigger.acceptance_record_hash`（または manager が CAS 前に head-check (a) と同じ線形化読みで head 状態を確認）に。

### R7-12（LOW）同 tier / 同 reason no-op で「要求される clearance の和集合」を記録するが、条件 10 は current reason の clearance しか消費しない（R1-03 fold の未完）

- 候補: 05:249「要求される clearance の**和集合**を CAS_ATTEMPT payload に記録する」；05:238 条件 10「該当する clearance record（§5.4）が存在する」；05:543「該当」= `record.safehold_reason == current.safehold_reason ∧ …`。
- 再現: SAFEHOLD(GATEWAY_RESTART) 中に InterruptOutcome(PLANNED_SWITCH) → TRANSFER_TO_SAFEHOLD(CHECKPOINT_DISABLED) は同 tier → no-op・和集合 = {GATEWAY_RESTART, CHECKPOINT_DISABLED} を記録。条件 10 は reason = GATEWAY_RESTART の record（GATEWAY_CONFIG_MATCH + role・05:541）だけを要求し、CHECKPOINT_DISABLED 行の「operator clearance + 再観測」（05:539）は要求されない。SAFETY / SAFE_STOP 間は条件 9（未処理 ISL 決定なし）と (b)(i)(l)(m) が実質的に埋めるが、tier 2 内は埋まらない。
- 最小修正: 条件 10 を「当該 SAFEHOLD 期間に記録された和集合の各 reason について該当 record が存在」に（和集合は AuthorityState 外の manager 台帳で保持・durable）。

### R7-13（LOW）(l) の「`wall_now` が直前の PERMIT_ISSUED の wall より単調」は audit 読出しに依存し、recorder 不能時に `R_CLOCK_ANOMALY` へ誤帰属する（R1-03「(iii) は audit 読出しに依存しない」と同根）

- 候補: 05:191 (l)；05:345 `R_AUDIT_UNAVAILABLE` 行（recorder 不能の扱い）；05:139（R1-03: runtime 判断を audit 読出しに依存させない方針）。
- 再現: recorder 停止中に permit 要求 → (l) の第 3 項が評価不能 → FALSE → `R_CLOCK_ANOMALY`（PERMIT）・disposition HOLD／SAFE_STOP の周期側と混同され、原因が clock でないのに CLOCK_ANOMALY 系の clearance / evidence に写る。
- 最小修正: manager が `last_permit_wall` を durable（AuthorityState と同 store・CAS tuple 外）に保持して比較；recorder 不能は (m) の `AUDIT_RECORDER_LIVENESS` 経由で `R_REGISTRY_UNAVAILABLE`（DIAGNOSTIC:AUDIT_RECORDER_LIVENESS）に統一。

### R7-14（LOW）genesis record が束縛する `profile_hash`（R2-15）と `role = clearance_roles[GENESIS]` はどこでも検査されない（束縛のみ・検査なし・R6-06 の自己申告 pattern が GENESIS に残る）

- 候補: 05:143「genesis record（epoch 起点・…・`profile_hash`（この genesis が権限づける profile）・role = doc 06 `clearance_roles[GENESIS]`）」；05:191 (a)-(m)・05:229-240 条件 1-12 に genesis の profile_hash / role を参照する項なし（grep `genesis` = 05:143 / 145 / 683 / 736 / 775 のみ）；05:543 clearance record の資格照合（R6-06）は CAS 条件 10 の record にしか及ばない；06:220「GENESIS … に要する追加の role」。
- 再現: 任意の operator_ref が role 文字列 GENESIS を自己申告して genesis record を書く → manager 起動。permit は別 profile_hash でも (i) さえ通れば発行される（genesis.profile_hash は比較されない）。
- 最小修正: `GENESIS_RECORDED` payload に `role_registry_hash` と `(operator_ref, GENESIS) ∈ registry` を要求（05:543 と同文）；`profile_hash` は「この genesis が権限づける」の意味を「(i) の `cell_identity_hash` == genesis profile の `cell_identity_hash`（cell 束縛）」として条件化するか、束縛主張を provenance に弱める。

### R7-15（LOW）(i)・条件 12 は role registry head・profile registry head を見るが baseline registry head の等値を見ない（head-check (e) のみ・その (e) は R7-01 の from 列で SAFEHOLD では走らない）

- 候補: 05:191 (i)「`timing_baseline_hash` の artifact を解決して再検証」（content 整合のみ）；05:240 条件 12（baseline 項なし）；05:292 (e) `head(baseline_registry, cell_id) ≠ record.timing_baseline_hash`（from = EXECUTOR）；06:524「timing baseline の supersession（bound する全 record）」は「認可された一者または自動 monitor」が SUSPENDED を append する前提。
- 再現: baseline head が h2 に更新・monitor の append が遅延（または monitor 停止 → (m) で止まる）。SAFEHOLD からの permit: (i) は h1 の artifact を解決できるので pass → CAS 条件 12 pass → lease 活性 → 次の head-check (e) で HOLD。曝露 = 1 head-check 周期（保守側だが (i) の対称性欠如）。
- 最小修正: (i) と条件 12 に「`head(baseline_registry, profile.cell.cell_id) == record.timing_baseline_hash`（不一致 = `R_PROFILE_SUSPENDED`・trigger = PERMIT | CAS_COND12・sub = BASELINE_SUPERSEDED）」を追加（R6-05 の role registry 節と同形）。

### R7-16（LOW）checker Q2 は 05 §3.4 (j) 区間内の**除外文**に現れた hash field 名も「名指しあり」と数える — R6-13 の強化が除外宣言で充足される

- 候補: `check_review_candidate.py:186-195`（(j) 区間 = 「(j) profile が宣言する」〜「が解決先の内容と一致」を文字列として count）；05:191「(j) の対象外 = `binding_expectations[].tensor_binding_hash`（frozen D1.1-B の hash・(g) が等値束縛する）」（区間内）；06:225 `BindingExpectation.tensor_binding_hash`（Q2 の宣言 class 1）。
- 再現: Q2 は `tensor_binding_hash` を除外文で 1 回検出して PASS。checker は「(j) が再照合する集合 ⊇ 06 の宣言 hash」を保証すると読めるが、実際は「区間内に文字列が現れる」= 除外でも可。将来 (j) で別 field を「対象外」に書いても Q2 は FAIL しない。
- 最小修正: Q2 で区間から「(j) の対象外 = …」の sub-span を除いて count し、除外された名は明示 allowlist（`tensor_binding_hash`）でのみ許す。設計変更なし。

### R7-17（LOW）header / anchor の鮮度: 06 header に v0.2.7 の時刻が無く、05 anchor 23 の C2 WARN 件数（6）が実測（11）と一致しない

- 候補: 06:3（v0.2.1〜v0.2.6 の時刻を列挙・v0.2.7 = §11 06:692 の 23:32 UTC のみ）；05:1050「残る C2 WARN（6 件）… slice prereg `:59` … と frozen `:151`」↔ checker 実測 WARN=11（line 88 / 98×2 / 146 / 234 は anchor の特徴づけに無い）。
- 最小修正: 06:3 に「v0.2.7 = 2026-09-05 23:32 UTC（`date -u` 実測）」；05:1050 を 11 件に更新し 88 / 98 / 146 / 234 の性格（D0 `:318` / `:354`・contracts_v2 `:323` / `:570`・D11B `:255` の散文引用）を 1 文で足す。

### R7-18（LOW）`R_MANAGER_UNAVAILABLE` 行の disposition「manager 復帰時に TRANSFER_TO_SAFEHOLD(MANAGER_RESTART) と同じ経路で HOLD」は link 断のみ（manager 非再起動）では実現されない

- 候補: 05:327；05:525 / INV-19（読みが戻れば (ii) を再評価）；05:294 MANAGER_RESTART 行（manager 自身の再起動が trigger）；05:449 `command_deadline_s` の計時。
- 再現: link 断 < `command_deadline_s` → gateway は (iii) → 読み復帰 → (ii) を再開（CAS 無し）。行の「HOLD」経路を発行する主体が無い。link 断 ≥ `command_deadline_s` なら `R_DEADLINE_MISS` 経路（`pending_invalidate`）で HOLD — 行はこれを書いていない。
- 最小修正: 行を「読み復帰が `command_deadline_s` 内なら (ii) 再開（新 CAS 不要）、超えれば `R_DEADLINE_MISS` 行に従う；gateway は不読を manager へ報告し `COMMAND_REJECTED`（R_MANAGER_UNAVAILABLE）を記録」に書き換える。

### R7-19（LOW）兄弟節は REVOKED（終端）を含むため、良性理由で REVOKED した旧 profile が同一 `cell_identity_hash` の全 profile を恒久 lock する — registry に「退役」状態が無い（Rs 事項・open item 化を推奨）

- 候補: 05:191 (i)・05:240・05:292 (a)「他 profile の head record が state ∈ {SUSPENDED, REVOKED}（新 generation の ACCEPTED で置換されていない）」；06:486「REVOKED は終端。再使用 = 新 generation の新 record のみ」；06:532「REVOKED → 新 generation は SUSPENDED → ACCEPTED と同条件（原因除去 evidence + two-key）」。
- 再現: cell C で profile P1（timeout 値 v1）を運用後、P2（v2）へ移行し P1 を REVOKED（原因 = 単なる置換）。以後 P2 の permit は (i) 兄弟節で `R_PROFILE_SUSPENDED`（SIBLING_SUSPENDED）。解除には P1 を two-key で再受理するしかない（意味論上不自然）。
- 最小修正案（Rs 専権のため提案のみ）: 06 §8.3 に `RETIRED`（終端・非安全起因・兄弟節の対象外）を足すか、兄弟節を「head.state == SUSPENDED、または REVOKED で `suspension_reason` が cell 単位 trigger（INCIDENT / ISL_ANOMALY / LAYOUT_CHANGE / CLOCK_ANOMALY / BASELINE_SUPERSEDED）」に限定。OPP として記録。

## 4. PASS 2 — 反証で構成できなかったもの（PASS・根拠行）

| 狙い | 結果 | 根拠 |
|---|---|---|
| 二重所有 / 所有不在 / offer replay / permit 一回性 / 旧 owner 復活（P1–P5） | 反例なし | 05:229-240 条件 1-12・05:243-247 事後条件（`∪ {consume_offer_id}` 05:243・`safehold_disposition := None`）・証明 A–E 05:353-361・INV-02/03/04/05/11/32 |
| SUSPENDED / REVOKED / 世代不一致 / 兄弟 / role registry 不一致の profile 上で command が admit される | 構成不可（有界窓のみ） | (i) 05:191・条件 12 05:240・event 行 05:291（記録が先）・head-check 05:292（monitor 非依存）・06:524（原子 batch）。曝露 = event 通知遅延 / head-check 周期（baseline）。⚠ role registry head 変更は event / 周期の義務者が無い（R7-03）・PROPOSED head は SAFE_STOP 側に倒れる（R7-02） |
| validity deadline 後に command が admit される | 構成不可（INV-24 窓のみ） | 05:290（S_BOUNDARY_WAIT を含む from 集合・mode 非依存）・05:249 lease-scoped 再試行・05:519 `pending_invalidate`・05:429 |
| permit が副失効後に消費される | 構成不可（境界はすべて strict） | 05:188 min・05:230 条件 2（優先規則）・05:231 条件 3・05:234 条件 6・05:240 条件 12・T-41。INV-07 / INV-35 の残差 = R7-08 |
| monitor 喪失で落ちた SAFEHOLD が monitor 不在のまま再 lease（flap） | 構成不可 — ただし逆に永久 deadlock | (m) 05:191・T-40。(m) が SAFEHOLD で評価不能 = R7-01 |
| AuditRecorder 停止中に無記録の lease が走る | 構成不可（`AUDIT_RECORDER_LIVENESS` 周期まで） | 05:345・05:702「記録が先」・T-38・06:531。gateway の `COMMAND_ADMITTED` 同期 block は 11_ §10.1 の起草既定（finding にしない） |
| SHADOW lease から actuation | 構成不可 | 05:474・INV-09 05:719・T-11。SHADOW も deadline / event / head-check で記録上 SAFEHOLD（05:829・06:345） |
| 2-arm command が独立な 1-arm 評価に分解される／逐次 1-arm command で keep-out を跨ぐ | 構成不可 | 05:508 `arm_targets` = 1 record・05:463 同一 cmd_seq の線分の対・他 arm 動作中 = 評価不能 = FALSE・INV-36 05:746・T-37 / PT-42。⚠ lease の ownership 外の arm を動かす command は admit される（R7-05） |
| SAFEHOLD が要求 clearance 無しに解除される | 構成不可（tier 2 内 no-op の和集合を除く） | 条件 10 05:238 = §5.4 集合 05:545（O7 PASS）・clearance record 定義 05:543（role registry 照合）・SAFE_STOP 規則 05:249・条件 9 05:237（ISL 未処理決定なし）。残差 = R7-12 / R7-14 |
| SAFE_STOP が意図せず緩む / 漏れる | 構成可（R7-04: manager 不読で SafeStop → SafeHold） | 05:520・05:525・05:327 |
| crash matrix（executor CAS 後 crash / manager CAS 中再起動 / gateway 再起動 / 転送窓の safety override / permit 後 Orchestrator 喪失 / monitor crash / registry 不読 / recorder 不能 / manager link 断） | link 断のみ行の記述と実挙動が不一致（R7-18） | 05:284・05:314 / 05:144・05:316・T-30 / 05:295・05:526・INV-31 / 05:317・T-08 / 05:296 / 05:292 (c)・(m) / 05:343 / 05:345 / 05:327 |
| clock: wall→mono 変換・drift・逆行 | 構成不可 | 05:183・05:191 (l)・05:343 `R_CLOCK_ANOMALY`・06:346 CLOCK_ANOMALY → SAFE_STOP・06:396 NC。(l) の audit 依存 = R7-13 |
| frozen schema delta / enum member 追加 / hash preimage 汚染 | なし | 05 §1 05:38・§11 項 3 05:799・INV-16 05:726・06 §9 項 3 06:539・06 §2.3 06:260。`RuntimeFaultCode` / `SafeHoldReason` / `LeaseMode` / `AcceptanceState` 等は全て runtime / deployment 層。`TransitionRecord.safety_events` は frozen `tuple[str, ...]` の語彙内（05:703）。RS71 §0 #1/#2/#4 の束縛（06:304 / 06:313 / 06:315）は参照のみ・値を書かない |
| RuntimeDisposition / SafeHoldReason / RuntimeFaultCode の各 member の到達性 | 全 member に生成経路あり | §7 表 05:660-670・§3.7 遷移表・失敗表（O5 PASS）・`R_LEASE_INVALIDATED` = 記録 code（05:333） |

## 5. 主張しないこと

- 本 review は AI（別 context）の準備レビュー（08 §6）であり pS / pY / pN / Rs の two-key を代替しない。gate を flip しない。PASS-WITH-CONDITIONS は 08 §5 の語彙に従う reviewer 付値であって裁定ではない。
- 凍結 4 file・`eval_runs/` 配下・`check_review_candidate.py` は一切編集していない（書込み先 = `$OUT` のみ）。
- Rs 裁定の設計方針（B+ / 外部 `CellSafetyTimingBaseline` / `ProfileRegistry`・`AcceptanceRecord` / OP-19 三層）と、11_ §10.1・05 OP-19・06 OPP-16 に列挙された起草既定は finding にしていない。R7-19 は Rs 専権（registry 意味論）ゆえ open item 化の提案に留める。
- open = 0 を宣言しない。本 review が触れていない領域: 06 §6.1 evidence の物理内容・execution profile 軸・composition・EP v1.9 内部・`SafeHold` の物理実現（OP-4）。
- 各 finding の severity は reviewer 付値であり verifier（3 lens）の判定で置換され得る。R7-04 / R7-05 は verifier が「regime 外」または「設計選択」と読めば LOW に下がり得るし、R7-04 は stop-class を安全機能と見なす場合 HIGH 側の議論があり得る（本 review は MEDIUM とした）。

## 6. 成果物

- 本報告: `$OUT/R7_review_v027.md`
- findings JSON: `$OUT/R7_findings_v027.json`（19 件・severity 降順・`json.load` で検証済）
- pin 再測: `$OUT/pin_recheck_R7.txt`／ checker: `$OUT/checker_05_R7.txt`（--runtime）・`$OUT/checker_06_R7.txt`（--profile・記録用）

（`$OUT` = `/tmp/claude-0/-home-user-IsaacLab/f88a4426-3d65-5cc2-ba28-87f4976e6a66/scratchpad/review5`）
