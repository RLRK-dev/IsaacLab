# R4 — deployment / commissioning & functional-safety review of WMSO v0.2.5 (06 primary, 05 secondary)

- reviewer: R4（別 context・deployment / commissioning lens・adversarial）。対象 = `$PKG/06_WMSO_INDUSTRIAL_PROFILE_v0.2_REVIEW_CANDIDATE_20260903.md`（header v0.2.5・sha256 `184d71e8fd4097da84919ecac2150471095fd4bee7ca4135df965628ab6afe49` = SHA256SUMS 一致）／ `$PKG/05_WMSO_RUNTIME_SPEC_v0.2_REVIEW_CANDIDATE_20260903.md`（sha256 `c3c8b5bab864d8bb0c113d20d84ed447d00d2b4e8ab4c42399217117c8946fe2` = SHA256SUMS 一致）／ `04_*`。repo HEAD `50119dc3`・branch `claude/v0-2-design-candidates-snqbmy`。実施 = 2026-09-05 01:44 UTC（`date -u` 実測）。
- `$PKG` = `eval_runs/troot_optE_dapg_wholeroute_scope_20260701/WMSO_EXACT_BASELINE_REVIEW_v0.2_20260903`、`$D` = `eval_runs/troot_optE_dapg_wholeroute_scope_20260701`。凍結物は未編集（本 report と JSON のみ scratchpad へ書出し）。
- 前提 (7) のとおり、Rs 裁定 4 件（B+ / CellSafetyTimingBaseline / append-only registry + two-key / validity deadline + event）の**方向**は finding にしない。本 report は v0.2.5 の本文がそれを正しく・整合的に・機械検査可能に・fail-closed に実現しているかだけを見る。

## 0. 前提の実測（08 §1 項 1-3）

### 0.1 pin 再測（`bash $PKG/verify_exact_baseline_pins.sh`・素の bash）= **11/11 PASS**

```
PASS  D1.1-A DESIGN v2.11.2 (worktree)  00192d20ca00b654cf6cfdb9d04b03ca14adfd0b93f2105c0fea28c295ff8aff
PASS  EvidencePolicy v1.9 md (worktree)  c474acea7c58acc22050c2ad9944fd45a18f5c76967964b42d11922e28fa27e7
PASS  EvidencePolicy v1.9 json (worktree)  e63176af9bc3a246b1c32db369ec59f8d09a4c96c381bb03a3a6024bd9811c6e
PASS  D1.1-B DESIGN v13 (worktree)  5a1874d3be8b98b8aeaace73890d8021cbc7f9814e048741f7f58d6746f349a6
PASS  D1.1-A DESIGN git blob id (HEAD)  1353430228a90ad36dd190a9359cd81da52e8242
PASS  D1.1-B DESIGN git blob id (HEAD)  ebe8154abf2461a6b24db05728b20e5cee949fb2
PASS  D1.1-A DESIGN sha256 of blob 135343…  00192d20ca00b654cf6cfdb9d04b03ca14adfd0b93f2105c0fea28c295ff8aff
PASS  D1.1-B DESIGN sha256 of blob ebe815…  5a1874d3be8b98b8aeaace73890d8021cbc7f9814e048741f7f58d6746f349a6
PASS  D1.1-A DESIGN at frozen commit 54f90a7d  00192d20ca00b654cf6cfdb9d04b03ca14adfd0b93f2105c0fea28c295ff8aff
PASS  D1.1-B DESIGN at frozen commit 07250f4a  5a1874d3be8b98b8aeaace73890d8021cbc7f9814e048741f7f58d6746f349a6
PASS  evidence_policy_definition_hash (embedded command)  e7ca43093084c167a209b008533a66d26a1fd3223d2a3c11274d28306c3ff803
policy_semver=1.9.0
HEAD=50119dc3dccdf9f840bd53404dfc85217e71dfb6 branch=claude/v0-2-design-candidates-snqbmy
EXIT=0
```

### 0.2 機械検査（`python3 $PKG/check_review_candidate.py $PKG/06_… --profile`）= **FAIL=0 / WARN=24（全て C2 heuristic）**・exit 0

```
== …/06_WMSO_INDUSTRIAL_PROFILE_v0.2_REVIEW_CANDIDATE_20260903.md: citations=61 FAIL=0 WARN=24
```
（WARN 24 件は全て C2「引用行 ±3 行に backtick 識別子が無い」の heuristic。原本 = `scratchpad/review3/checker_06.txt`。⚠ checker Q1 は「使用 P_* が §8.1 に列挙されている」「PT/OPP 連番」しか見ない。**§3 で言及される規範集合（MIN_NEGATIVE_CONTROLS 等）が P_* に結線されているか、05 側の enumerations が 06 の hash field を網羅しているかは機械検査の外**であり、本 report の HIGH 3 件はその盲点に落ちている。）

### 0.3 読了範囲

06 全 649 行（`awk` で 4 chunk）／ 05 §0–§9・§10・§11–§12・§13 v0.2.5 表・§14（全 993 行のうち fold-map の v0.2.1–v0.2.4 行は 11_ §3/§7 で代替）／ 04 全文／ 08・10・11 §3/§7/§8／ `review_records/rs_consult/{Q,A}_GPT56sol_20260904.md` 全文／ checker 全文／ 凍結: contracts_v2 `:39,:142-152,:159,:168,:218,:345-354,:370-386,:537`・tensor_binding `:20-26,:38-41,:50-54,:65-66,:75-82,:94-100,:123-140,:173-186,:287-291,:332`・D0 `:314-322,:352-356`・RS71 `:19-33`・C3 `:5,:65`・prereg `:59`。

## 1. 総合判定

**HOLD**（08 §5: CRITICAL / HIGH が残る）。CRITICAL 0 / **HIGH 4** / MEDIUM 11 / LOW 4 = 19 finding。全 finding は本文から再現可能で、凍結 schema delta 0 の最小修正案を付す。設計方向（Rs 裁定 4 件）への異議は含まない。

HIGH 4 件の要旨: (R4-01) INTER_ARM 項の評価対象が「片腕の提案線分 × 他腕の**静止**状態」であり、RS71 §0 #1 が要求する**両腕同時**の command で両提案線分の相互干渉を評価しない（+ 06 §4 が参照する `cmd.resource` が 05 `GatewayCommand` に存在しない）／ (R4-02) v0.2.5 で足した 3 種の content hash（`kinematic_layout.artifact_sha256`・`pairwise_keepout_sha256`・`swept_volume_predicate_sha256`）が 06 §3 と 05 §3.4 (j) の「全ての content hash」の enumeration にも gateway の評価前再検証にも無い（R1-05 / R2-03 と同型の再発。PT-38 の期待が本文に裏付けられない）／ (R4-03) `MIN_NEGATIVE_CONTROLS` を要求する P_* が無く、policy に宣言 field も無く、subject 文法 `CELL#…` / `ENV#…` が `P_FAULT_CODE_UNKNOWN` / `P_FAULT_EVIDENCE_UNVERIFIED` の規則と衝突して**満たしようがない**（CLOSED_LOOP 受理を negative control 無しで通せる。`ENV#CLOCK_ANOMALY` には対応する runtime 規則も fault も無い）／ (R4-04) two-key の機械検査が「2 role が揃っているか」だけで、**同一 `operator_ref` が 2 role を名乗れば 1 人で CLOSED_LOOP 受理に到達**する（role は自由文字列・登録・認証なし）。

## 2. PASS 1 — 06 §11 v0.2.4 fold（19 行）+ v0.2.5 fold（4 行）の回帰

判定語彙: **OK** = (a) 実現 ∧ (b) 他文と矛盾なし ∧ (c) 循環 / 充足不能なし。**OK\*** = 実現しているが v0.2.5 の追加で新たな不整合が出た（finding 番号）。**FINDING** = 実現が不完全 / 矛盾。

| # | fold 行 | 実現節（読了） | 判定 | 根拠 / finding |
|---|---|---|---|---|
| 1 | R1-04 / R2-05 SAFETY clearance role 必須 | 06:216・06:288・PT-22（06:438）↔ 05:536-538 | OK | 集合 {SAFETY, SAFE_STOP, CHECKPOINT_DISABLED, MANAGER_RESTART, GATEWAY_RESTART}+GENESIS が 05 条件 10（05:237）と同一（checker O7 も PASS） |
| 2 | R1-05 / R2-03 content hash 照合を全宣言 hash へ一般化 | 06:306・05:190 (j)・05:454/456 | **FINDING R4-02** | v0.2.5 で足した `kinematic_layout.artifact_sha256`（06:100）・`pairwise_keepout_sha256` / `swept_volume_predicate_sha256`（06:106/108）が両 enumeration に無く、INTER_ARM 行（05:457）に評価前再検証句が無い |
| 3 | R1-06 / R2-19 線分評価 | 06:330 ↔ 05:454 | OK | hold 種別によらず線分。tool 形状 = OPP-14 で loud |
| 4 | R1-15 / R2-12 §8.1 (3) の「のみ」 | 06:411 ↔ 06:337・05:478 | **FINDING R4-07**（+ R4-06） | OP-19 fold で §5 (b) が「CLOSED_LOOP は次 permit まで待たない」に変わったのに §8.1 (3) は「活性 lease は継続し…無効化するのは (a) と CD-10 **のみ**」のまま。同行に置換済 `accepted_profiles` も残存 |
| 5 | R2-01 timeout 束縛の迂回（別経路・欠落 code・順序漏れ） | 06:283・06:379-381・05:694 | OK\* → **R4-13** | v0.2.4 集合は実現。v0.2.5 の deadline 駆動 code（`R_CALIBRATION_EXPIRED_ACTIVE` 等 5 件）が MIN_FAULT_INJECTION に足されていない（R2-01 の規則「timeout 駆動 fault は必ず発火」が新 code に未適用） |
| 6 | R2-02 fault / health evidence の内容検証 | 06:295・PT-28 | OK\* → **R4-03 / R4-08** | 実現。ただし v0.2.5 の negative control（06:382）は subject が R_ code でないため本検査を満たせない；本検査は「第 2 評価点」と書かれるが 06:411 (2) / 05:190 (b) の列挙に無い |
| 7 | R2-04 DUAL-ARM 表現（ArmSpec / P_ARM_COVERAGE） | 06:83-95・06:299・06:321・PT-29 ↔ 05:455/457 | OK\* → **R4-01 / R4-12** | 型は実現。06:321 `arms[cmd.resource]` の `cmd.resource` が 05 `GatewayCommand`（05:496-504）に無い；両腕同時 command の INTER_ARM 評価が定義されていない；arm 単位 evidence の subject が両腕で衝突 |
| 8 | R2-06 constant-True evaluator | 06:289・OPP-9 | OK\*（軽微） | 実現。subject `check_id#NEGATIVE` は 06:354 の HEALTH_CHECK_RUN subject 規則（= check_id）に無い文法（R4-03 に併記） |
| 9 | R2-07 BEFORE_PERMIT evidence の lease_id | 06:355 ↔ 05:688 | OK | `lease_id = None` + `profile_hash` join |
| 10 | R2-08 profile の失効経路 | 06 §8.1 (2)・§8.3（OPP-13 で置換） | OK\* → R4-16 | 経路は §8.3 に実現。state machine の細部（REVOKED の再使用条件・同 generation ACCEPTED）は R4-16 |
| 11 | R2-09 ISL identity | 06:67-68・06:84・06:287・PT-30 | OK | SAFETY_LAYER_IDENTITY 必須集合入り |
| 12 | R2-10（refuted） | 適用せず | OK | 変更なし（verifier 判定どおり） |
| 13 | R2-11 P_ENVELOPE_EXCEEDS_MEASURED の評価点 | 06:305 | OK\* → **R4-08** | 「第 2 評価点・CLOSED_LOOP」と書かれるが 06:411 (2) と 05:190 (b) の第 2 評価点 code 列挙に無い（結線が無い） |
| 14 | R2-13 frame_ref default | 06:139 | OK | default 無し・欠落 = codec 拒否 |
| 15 | R2-14 evidence policy の循環 | 06:297・PT-31 | OK | v0.2.5 の CELL#INTER_ARM_COLLISION は SHADOW lease で誘発可（05:291 は any (EXECUTOR)）ゆえ循環なし |
| 16 | R2-15 GENESIS の role | 06:216 ↔ 05:143 | OK | — |
| 17 | R2-16 singleton kind の subject | 06:354 | OK\* → **R4-12** | 定義は実現。arm ごとの kind の subject（controller_firmware_ref / tool_id）が同型 2 腕で同値になり「arm ごと」が壊れる |
| 18 | R2-17 evaluator の実行直前再検証 | 06:194 ↔ 05:190 (a) | OK | — |
| 19 | R2-18 無期限 calibration / acceptance | 06:294・PT-32・06:472 | OK\*（軽微） | 実現。有限だが実質無期限（例: 100 年）を縛る ceiling は無い（R4-16 に併記） |
| v5-1 | 裁定 OPP-15 B+ | 06:82-109・06:238・06:299-302・06:382・05:457-459・05:291・INV-36・T-37 | **FINDING R4-01 / R4-02 / R4-03**（+ R4-14 / R4-17） | 型と code は入ったが、評価意味論・hash 照合・negative control の結線が欠ける |
| v5-2 | 裁定 OPP-11 | 06:283-285・06:476-482・05:183/187・05:229/233・INV-35 | **FINDING R4-05**（+ R4-08 / R4-09 / R4-10 / R4-18） | ceiling 検査は実現。budget 検査の入力に schema が無い；baseline が cell に束縛されない・改訂が失効 trigger でない |
| v5-3 | 裁定 OPP-13 | 06:456-491・05:181-182・05:190 (i)・05:239・05:339-340 | **FINDING R4-04**（+ R4-09 / R4-16 / R4-19） | registry / record / CAS 再検査は実現。two-key が one-key に潰れる経路が残る；record の束縛 field が runtime で照合されない |
| v5-4 | 裁定 OP-19 | 06:337・PT-13・05:88・05:123・05:183・05:289-290・05:338-339・05:424・05:478・05:529・INV-34 | **FINDING R4-06 / R4-07 / R4-11**（+ R4-13 / R4-15） | 三層は実現。deadline に `lease_max_duration_s` を混ぜて INV-33 と衝突；event 起因の VALIDITY_EXPIRED に clearance が無い；SAFE_STOP 強化の判定材料が record に無い |

循環検査（(c)）: `profile_hash → ProfileAcceptanceRecord → CellSafetyTimingBaseline` は一方向（06:490）。evidence record → profile も一方向（06:170）。CLOSED_LOOP 世代 g+1 の `evidence_set_hash` が参照する audit range は世代 g の lease に属し、g+1 の record hash を含まない。**循環は検出されず**。充足不能は R4-03（negative control が `P_FAULT_EVIDENCE_UNVERIFIED` を満たせない）と R4-05（`P_SAFETY_BUDGET_EXCEEDED` の入力が無く fail-closed で常に違反）の 2 件。

## 3. Findings（PASS 1 由来 + PASS 2 adversarial）

型: {id, severity, candidate doc:line, frozen file:line, 再現 / 反例, 最小修正（凍結 schema delta 0）}。

### R4-01 — HIGH — INTER_ARM 項が「両腕同時 command」の相互干渉を評価しない／`cmd.resource` が 05 に無い

- doc:line: 06:109（「両 arm の同一線形化 snapshot で連言評価」）・06:321（`CONTROLLER(profile.cell.arms[cmd.resource].controller)`）・05:457（INTER_ARM 行「評価対象 = 提案 command の arm の線分 × 他 arm の同一線形化 snapshot での状態（直近 admitted setpoint または実測）」）・05:459・05:738（INV-36）・05:780（T-37「左右 arm の提案線分」）・05:496-504（`GatewayCommand` = {lease_id, control_epoch, executor_id, cmd_seq, kind, payload_ref, t_issue} — `resource` / arm を識別する field が無い）。
- frozen: RS71 `thread_isaac_lab/thread-vault/04-Specs/RS71-System-Spec-SSOT.md:23`（「BOTH arms … in EVERY motion」）；tensor_binding `$D/WMSO_D11B_TENSOR_BINDING_DESIGN_RSTECHLEAD2_20260720.md:129-134`（`ActionBinding` は policy step あたり 1 つ・全 action feature を含む — 両腕の目標は 1 command に同乗する）；D0 `$D/WMSO_D0_ARCHITECTURE_DRAFT_RSTECHLEAD2_20260718.md:315-319`（ISL は独立監視で AND、代替ではない — 06:109 も同旨）。
- 再現（反例）: 左 TCP x=−0.30, 右 TCP x=+0.30（separation 0.60 > `min_separation_m` 0.20、pairwise keep-out 外）。LEARNED policy の 1 step が両腕へ同時に x=0.00 を command。05:457 の定義で評価すると、左の提案線分 (−0.30→0) × 右の**直近 admitted**（+0.30）= 最短 0.30 で admit、右の提案線分 (+0.30→0) × 左の直近 admitted（−0.30）= 0.30 で admit。両提案の終点は一致（separation 0）だが INTER_ARM は TRUE を返す。T-37 は「左右 arm の提案線分」の両線分評価を期待しているので、§4.3 の定義と T-37 は同一 doc 内で矛盾する。さらに CONTROLLER 項は `cmd.resource` で arm を引くが、05 の command 記録形にその field は無く、両腕同乗 command の arm 分解規則（どの action feature がどの arm か）は 05 / 06 のどこにも無い。
- 影響: RS71 #1 の運用（every motion で両腕）では**全 command が両腕同時**なので、この評価定義では arm 間衝突の主経路（相互接近）が gateway を素通りし、ISL の動的監視だけが残る（06:109「AND・代替ではない」に反して実質 ISL 単独）。
- 最小修正: 05 §4.3 INTER_ARM 行を「command が両 arm の setpoint を含む場合は**両提案線分の対**（同一時間 parameterization）を pairwise keep-out / min separation / swept-volume predicate で評価し、片腕のみの command では他腕の直近 admitted setpoint（無ければ実測）を静止線分として評価する」に改める。`GatewayCommand` に `arm_targets: tuple[tuple[str, str], ...]`（(resource_id, setpoint_ref)・runtime 層の新 field・凍結 delta 0）を足し、06:321 の `cmd.resource` を `cmd.arm_targets[].resource_id` に揃える。両腕分解の出所は「tensor_binding_hash が指す `semantic_action_schema` の field → resource_id の写像を `BindingExpectation` に記録（等値 readback）」と書く。T-37 の期待は変更不要。

### R4-02 — HIGH — v0.2.5 の content hash 3 種が「全ての content hash」の enumeration と gateway 再検証に無い（R1-05 / R2-03 の再発）

- doc:line: 06:306（enumeration = `predicate_sha256` / `evaluator_sha256` / `zones[].geometry_sha256` / `calibration_refs[].artifact_sha256` / `gateway_config.artifact_sha256` / `deployment_evidence_policy_hash` / required record の `artifact_sha256`）；05:190 (j)（同じ enumeration）；05:454 / 05:456（DEPLOYMENT_WORKSPACE / SAFETY_RESTRICTION は「評価前に…再検証」を明記）↔ 05:457（INTER_ARM 行に再検証句なし）；06:100（「05 §3.4 (j) の照合対象」と主張）・06:302（「content hash は 05 §3.4 (j) の照合対象」と主張）・PT-38（06:454）。
- frozen: tensor_binding `$D/WMSO_D11B_TENSOR_BINDING_DESIGN_RSTECHLEAD2_20260720.md:23`（content-addressed 参照の規律 — 06 §2.3 が同型適用を宣言）；RS71 `:24`（固定 base = layout の意味内容）。
- 再現: PT-25 / PT-27 と同じ手順を新 artifact に当てる。受理後に `swept_volume_predicate_refs[0]` の解決先を常時 admit の predicate に差し替える（hash は profile に固定されたまま）。permit 発行時 (j) の enumeration に `swept_volume_predicate_sha256` が無いため `R_PROFILE_MISBOUND` は出ない。gateway の INTER_ARM 評価（05:457）には「評価前に再検証」句が無いため差し替え内容で評価 → admit。base transform についても同様: layout artifact を書き換えても (j) が `kinematic_layout.artifact_sha256` を照合しないので PT-38 の期待「次 permit で R_PROFILE_MISBOUND (j)」は本文で裏付けられない。「全ての」という語はあるが、両 doc とも直後に閉じた列挙を置いており、実装者は列挙に従う（R2-03 が指摘した構造そのもの）。
- 最小修正: 06:306 と 05:190 (j) の enumeration に `cell.kinematic_layout.artifact_sha256` / `inter_arm_restrictions.pairwise_keepout_sha256[]` / `inter_arm_restrictions.swept_volume_predicate_sha256[]` を追加。05:457 に「gateway は評価前に layout・pairwise keep-out・swept-volume predicate の解決内容 sha256 を再検証し、不一致 = 評価不能 = FALSE」を追加。再発防止として enumeration を「§2.1 で `*_sha256` / `artifact_sha256` / `*_hash` を名に持つ**全 field**（機械抽出）」と定義し、checker に「06 §2.1 の hash field 名 ⊆ (j) enumeration」の check（O9）を足す（checker は package 内 helper なので凍結 delta 0）。

### R4-03 — HIGH — `MIN_NEGATIVE_CONTROLS` は P_* に結線されておらず、subject 文法が既存規則と衝突して満たせない

- doc:line: 06:382-383（MIN_NEGATIVE_CONTROLS = FAULT_INJECTION_RESULT with subject_ref ∈ {CELL#ARM_SWAP, CELL#ARM_MISSING, CELL#BASE_TRANSFORM_TAMPER, CELL#INTER_ARM_COLLISION, ENV#CLOCK_ANOMALY, ENV#SCHEDULER_STALL, ENV#COMM_LOSS}）；06:313（`P_EVIDENCE_POLICY_TOO_WEAK` = MIN_SHADOW / MIN_CLOSED_LOOP / MIN_FAULT_INJECTION のみ）；06:367-372（`DeploymentEvidencePolicy` に negative control を宣言する field が無い。`fault_injection: tuple[FaultInjectionRequirement]` の `runtime_fault_code` は 05 enum 名必須 = 06:363 / `P_FAULT_CODE_UNKNOWN` 06:296）；06:354（FAULT_INJECTION_RESULT の subject_ref = fault code、timeout 駆動は `<code>#<trigger>`）；06:295（`P_FAULT_EVIDENCE_UNVERIFIED`: audit 範囲に `kind = FAULT ∧ fault == subject_ref の code` が必要）；06:292（`P_EVIDENCE_UNBOUND` の FAULT_INJECTION_RESULT 句は `fault_injection[]` に対してのみ）；05:627-641（`RuntimeFaultCode` に clock anomaly / scheduler stall を表す member が無い）；06:289（`check_id#NEGATIVE` も 06:354 の HEALTH_CHECK_RUN subject 規則 = check_id に無い文法）。
- frozen: contracts_v2 `$D/WMSO_D11A_CONTRACTS_V2_DESIGN_RSTECHLEAD2_20260719.md:385`（strict codec — 未知 enum 値は拒否。`CELL` / `ENV` は fault code として decode できない）；RS71 `:23-24`（ARM_SWAP / BASE_TRANSFORM_TAMPER の negative control が守るべき不変前提）。
- 再現（通る profile）: `DeploymentEvidencePolicy.fault_injection` = MIN_FAULT_INJECTION の 17 code のみ、`required_before_closed_loop_lease` = MIN_CLOSED_LOOP。evidence store に negative-control record を 1 件も置かない。第 1・第 2 評価点の全 `P_*` = 0（`P_EVIDENCE_POLICY_TOO_WEAK` は 06:313 の 3 集合しか見ず、`P_EVIDENCE_UNBOUND` は `fault_injection[]` の code しか要求しない）→ CLOSED_LOOP 受理が可能。**逆に**規則どおり記録しようとしても: `subject_ref = "CELL#INTER_ARM_COLLISION"` の FAULT_INJECTION_RESULT は 06:295 が `fault == "CELL"` の audit record を要求するため `P_FAULT_EVIDENCE_UNVERIFIED`（評価不能 = 違反）になる（fail-closed の読みでは negative control を置いた profile の方が落ちる）。加えて `ENV#CLOCK_ANOMALY` には 05 側に検出規則も fault も無いので、どんな audit range を指しても「拒否 / SafeHold / SafeStop に到達」（06:383）を示す record を作れない。副作用: `InterArmRestrictionSet` を `min_separation_m = 1e-9` + 常時 admit の predicate 1 本で構成しても `P_INTER_ARM_RESTRICTION_MISSING`（06:302）は通り、空虚さを排除するはずの CELL#INTER_ARM_COLLISION が効かない。
- 最小修正: (1) `DeploymentEvidencePolicy` に `negative_controls: tuple[str, ...]`（⊇ MIN_NEGATIVE_CONTROLS でなければ `P_EVIDENCE_POLICY_TOO_WEAK`）を追加し、06:292 の `P_EVIDENCE_UNBOUND` に「∀ negative_controls[]: subject_ref 一致の FAULT_INJECTION_RESULT が CLOSED_LOOP 前に存在」を足す。(2) 06 §6.1 に **negative control → 期待 audit 内容の写像表**を置き（例: CELL#ARM_SWAP / ARM_MISSING → HEALTH_FAILED with check_id ∈ CONTROLLER_IDENTITY|TOOL_IDENTITY；CELL#BASE_TRANSFORM_TAMPER → FAULT R_PROFILE_MISBOUND (j)；CELL#INTER_ARM_COLLISION → FAULT R_INTER_ARM_VIOLATION；ENV#SCHEDULER_STALL → FAULT R_DEADLINE_MISS|R_TIMING_VIOLATION；ENV#COMM_LOSS → FAULT R_EXECUTOR_LOST|R_MANAGER_UNAVAILABLE|R_SAFETY_LAYER_LOST）、06:295 を「subject が `<R_code>[#trigger]` なら fault 等値、`CELL#…` / `ENV#…` なら写像表の内容」に分岐。(3) `ENV#CLOCK_ANOMALY` は 05 に検出規則（manager 単調 clock の逆行 / wall-clock と mono の drift が baseline `diagnostic_items` の閾値超過 → TRANSFER_TO_SAFEHOLD(SAFE_STOP)・新 fault `R_CLOCK_ANOMALY`・runtime enum への追加のみ）を置いてから要求する（R4-15 と同根）。(4) 06:289 の `check_id#NEGATIVE` を 06:354 の subject 規則に追記。

### R4-04 — HIGH — two-key が one-key に潰れる: 同一 `operator_ref` が 2 role を名乗れば cell operator 単独で CLOSED_LOOP 受理に到達できる

- doc:line: 06:471（`approvals: tuple[tuple[str, str, str], ...]  # (role, operator_ref, t_iso8601)` — role / operator_ref とも自由文字列）；06:486（「CLOSED_LOOP_AUTHORITY を許す受理 = 『独立 safety approver』と『deployment / operations custody approver』の two-key。cell operator 単独では CLOSED_LOOP を許可できない」）；06:491（検査 = 「CLOSED_LOOP 許可で two-key 不足」のみ）；PT-37（06:453）；06:462（record_hash = H_WCJ(record) — 署名・認証は未言及。§6.3 の「hash chain / 署名は 05 OP-5（U14 defer）」は evidence record についてのみ）。
- frozen: contracts_v2 `$D/WMSO_D11A_CONTRACTS_V2_DESIGN_RSTECHLEAD2_20260719.md:354`（closed-loop 権限は `granted == True` が必要条件で十分条件ではない — profile 側 gate が緩いと「必要条件を足すだけ」の主張〔06:495〕が空になる）；EP `$D/WMSO_D11A_EVIDENCE_POLICY_V1_RSTECHLEAD2_20260719.md:156`（closed-loop authority = O0/S0/V0 two-key + 独立安全 gate — 独立性が本質）。
- 再現: cell operator `op-7` が record を起票し `approvals = (("independent safety approver","op-7","…"), ("deployment/operations custody approver","op-7","…"))` を書く。06:491 の検査対象「two-key 不足」は role 2 種の存在で満たされ、`operator_ref` の相異は要求されていない。role 名は自由文字列なので typo 版 `"indep. safety approver"` を第 3 の tuple で足しても弾く規則が無く、逆に「どの文字列が two-key の role か」を機械が判定する語彙も無い。**結果: PT-37 の期待（`P_ACCEPTANCE_RECORD_INVALID`）は本文の検査定義からは出ない。** 06:486 の「cell operator 単独では不可」は散文の意図であって検査ではない。
- 最小修正（凍結 delta 0・role の実名割当〔OPP-13 残 open〕には触れない）: (1) `AcceptanceRole(Enum): SAFETY_APPROVER | CUSTODY_APPROVER | COMMISSIONING | INDEPENDENT_REVIEWER | OPERATOR | MONITOR` を deployment 層に置き `approvals[].role` を enum に。(2) 検査規則を「CLOSED_LOOP ∈ allowed_lease_modes ⇒ ∃ SAFETY_APPROVER ∧ ∃ CUSTODY_APPROVER ∧ 両者の `operator_ref` が相異 ∧ いずれも OPERATOR の operator_ref と相異；SHADOW のみ ⇒ ∃ COMMISSIONING ∧ ∃ INDEPENDENT_REVIEWER ∧ 相異」と書き、`P_ACCEPTANCE_RECORD_INVALID` の定義に入れる（06:486 の SHADOW-only 条件も現状は機械検査されていない）。(3) `operator_ref` → role の資格は content-addressed `role_registry_hash` を record に持たせ解決（registry の中身 = Rs / OPP-13）。(4) approvals の**認証**（署名）は評価 record と同じく OP-5 / U14 defer に従うと明記し、「hash だけでは approvals の真正性を主張しない」を §9 に足す。

### R4-05 — MEDIUM — `P_SAFETY_BUDGET_EXCEEDED` の入力（detection / gateway 切替 / measured stop time）に schema が無く、fail-closed では常に違反

- doc:line: 06:285（`safety_heartbeat_timeout_s + detection + gateway 切替 + measured stop time ≤ baseline.safety_response_budget_s`、「measured 値は CONTROLLER_ENVELOPE_MEASUREMENT / FAULT_INJECTION_RESULT の実測 artifact」）；06:305（CONTROLLER_ENVELOPE_MEASUREMENT の artifact schema = 「ControllerEnvelope と同一 field」）；06:112-119（`ControllerEnvelope` に停止時間 field は無い）；06:355（FAULT_INJECTION_RESULT の `artifact_ref` は audit range に解決 — 構造化された「worst-case 負荷下の検出→到達上限」を持たない）；06:383（同上限を「含む」と要求）；06:411 規則 (1)（評価不能 = 違反）。
- frozen: D0 `$D/WMSO_D0_ARCHITECTURE_DRAFT_RSTECHLEAD2_20260718.md:321`（`response_budget` = worst-case, not mean — 入力は worst-case 実測でなければならない）。
- 再現: 規則どおり artifact を作ろうとすると、measured stop time を置ける schema が無い（ControllerEnvelope 同一 field）。audit range から detection を導くなら「どの record 対の `t_mono` 差か」（FAULT → CAS_ATTEMPT SUCCESS? → 物理停止完了は audit に無い）が未定義。規則 (1) で「評価不能 = 違反」ゆえ、書面どおりには**どの profile も CLOSED_LOOP 受理に到達できない**（=「no profile can pass」）。実装者が独自に値を読めば、それは機械検査ではなく解釈になる。
- 最小修正: 06 §6.1 に `SafetyResponseMeasurement` artifact（deployment 層・content-addressed）を定義し、CONTROLLER_ENVELOPE_MEASUREMENT の artifact schema を「ControllerEnvelope 同一 field + `stop_time_worst_case_s: dict[StopClass, CanonicalDecimal]`」に拡張、FAULT_INJECTION_RESULT（timeout 駆動 code）の artifact を「audit range + `detection_to_safehold_worst_case_s`・`gateway_switch_worst_case_s`・`load_condition_ref`」の構造体にする。06:285 の各項の出所を field 名で書く（detection = FAULT_INJECTION_RESULT[R_SAFETY_LAYER_LOST#HEARTBEAT_TIMEOUT].detection_to_safehold_worst_case_s 等）。

### R4-06 — MEDIUM — `validity_deadline_mono` に `issued_at + lease_max_duration_s` を混ぜたため INV-33（LEASE_ACTIVATED 起点・boundary 滞留は別枠）と衝突し、fault 名も誤る

- doc:line: 05:183（`min(calibration 期限, evidence 期限, acceptance 期限, issued_at + lease_max_duration_s)`）；05:424（lease は permit から複写）；05:289（row: S_LEASE_ACTIVE / S_HEALTH_PENDING / **S_BOUNDARY_WAIT** で `now ≥ validity_deadline_mono` → VALIDITY_EXPIRED、fault = R_CALIBRATION_EXPIRED_ACTIVE | R_EVIDENCE_EXPIRED_ACTIVE）；05:735（INV-33: 活性時間は **LEASE_ACTIVATED 起点**、S_BOUNDARY_WAIT は `boundary_dwell_s` 枠で総活性 ≤ lease_max + dwell — R1-15 / R2-12 の fold）；05:288 / 05:337（超過 = TIMING_VIOLATION + R_LEASE_DURATION_EXCEEDED）。
- frozen: D0 `:354-355`（単一 origin の単調 clock — 起点の二重化は仕様の曖昧化）。
- 再現: `lease_max_duration_s = 600`。permit 発行 t=0、CAS 成功（activated_at）t=3、skill が t=598 に TERMINAL outcome → S_BOUNDARY_WAIT（INV-33 は dwell 枠で t=598+boundary_dwell_s まで許す）。しかし deadline = 0+600 = 600 で row 289 が発火 → TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED) + fault `R_EVIDENCE_EXPIRED_ACTIVE`（何も失効していない）。以後 §5.4 VALIDITY_EXPIRED 行の解除経路に落ち、Orchestrator の再選択（boundary 滞留中の正常経路）が潰れる。また「失効項目が安全機能・TCP・停止性能に関わる」判定はこの項に対して定義不能。
- 最小修正: 05:183 の min から `issued_at + lease_max_duration_s` を外す（lease 時間上限は INV-33 / R_LEASE_DURATION_EXCEEDED が既に担う）。row 289 の from に S_BOUNDARY_WAIT を残すのは可（calibration 期限は boundary でも効く）。06 §5 (b) / PT-13 は影響なし。

### R4-07 — MEDIUM — 06 §8.1 規則 (3) が OP-19 fold 後の §5 (b) / 05 §4.5 と矛盾（R1-15 / R2-12 の再発）・置換済 `accepted_profiles` が残存

- doc:line: 06:411（「(3) 受理後の事実変化…は profile を変えない。**活性 lease は継続し**、次の permit 発行が…不成立になる（§5 (b)）。**profile 起因で lease を無効化するのは health check 失敗（§5 (a)）と lease_max_duration_s 超過（CD-10）のみ**」／ 同行「(2) profile の受理（`profile_hash` の登録 = `accepted_profiles` への追加…）」）↔ 06:337（§5 (b): CLOSED_LOOP は `validity_deadline_mono` 到達・event で即時無効化）↔ 05:478（§4.5 無効化 trigger に validity deadline / event / SUSPENDED を列挙）↔ 06:459-491（`accepted_profiles` は record に置換）。
- frozen: —（doc 内矛盾）。
- 再現: §8.1 (3) を規範として読む実装は CLOSED_LOOP lease を calibration 失効後も継続させる（§5 (b) と逆）。「のみ」は R1-15 で一度直した語であり、同じ行が別 fold で再び矛盾した（fold script が §5 だけを書き換え §8.1 を触っていない）。
- 最小修正: 06:411 (3) を「SHADOW lease は継続し次 permit で不成立；CLOSED_LOOP lease は §5 (b) の deadline / event で無効化（05 §4.5）。profile 起因の無効化 = §5 (a)・§5 (b)・CD-10」に書き換え、(2) の `accepted_profiles` を `ProfileAcceptanceRecord（state = ACCEPTED）の append` に置換。

### R4-08 — MEDIUM — 第 2 評価点（permit 発行時）の `P_*` 集合が 06 §3 / 06 §8.1 (2) / 05 §3.4 (b) で一致しない・「受理時」という第 3 の評価点が未定義

- doc:line: 06:284（`P_TIMEOUT_CEILING` 「評価 = **受理時** + 第 2 評価点」）・06:295（`P_FAULT_EVIDENCE_UNVERIFIED` 第 2 評価点）・06:305（`P_ENVELOPE_EXCEEDS_MEASURED` 第 2 評価点）・06:289（negative-control HEALTH_CHECK_RUN「CLOSED_LOOP lease 前」）↔ 06:411 (2)（第 2 評価点 = `P_CALIBRATION_EXPIRED` / `P_EVIDENCE_UNBOUND` / `P_EVIDENCE_EXPIRED` / `P_EXPECTATION_UNCERTIFIED` のみ、「評価点は 2 つ」）↔ 05:190 (b)（同じ 4 code のみを `R_PROFILE_MISBOUND` に写す）。
- frozen: —（doc 間結線）。
- 再現: 実装者が 05 §3.4 (b) の列挙に従うと、permit 発行時に `P_TIMEOUT_CEILING`（baseline artifact の差し替え検出）・`P_ENVELOPE_EXCEEDS_MEASURED`（CLOSED_LOOP でのみ評価できる唯一の点）・`P_FAULT_EVIDENCE_UNVERIFIED` は評価されない。とくに `P_ENVELOPE_EXCEEDS_MEASURED` は R2-11 で「登録時には評価できない」と確定した code なので、(b) に無いと**どこでも評価されない**。「受理時」は 登録時（第 1）とも permit 発行時（第 2）とも別の瞬間で、§8.1 (2) の「2 つ」と整合しない。
- 最小修正: 06 §8.1 (2) を「第 1 = 登録（PROPOSED record 起票）時、第 2 = permit 発行時；受理（ACCEPTED record 追記）は第 1 評価点の結果 + `P_ACCEPTANCE_RECORD_INVALID` で確定し新たな評価点ではない」と書き、第 2 評価点の集合を §3 表の「第 2 評価点」指定から機械抽出した閉じた列挙（+ `P_TIMEOUT_CEILING` / `P_SAFETY_BUDGET_EXCEEDED` / `P_ENVELOPE_EXCEEDS_MEASURED` / `P_FAULT_EVIDENCE_UNVERIFIED` / negative-control 句）に更新。05:190 (b) を「doc 06 §8.1 (2) の第 2 評価点集合の全て」と参照形にする。

### R4-09 — MEDIUM — `ProfileAcceptanceRecord` の束縛 field（`evidence_set_hash` / `validator_hash` / `timing_baseline_hash` / `cell_identity_hash`）が runtime で照合されず、two-key が承認した evidence 集合の外の record で permit が成立する

- doc:line: 06:467-470（4 hash field）；05:190 (i)（照合 = state / mode / 有効期限のみ）；05:239（条件 12 = state / generation / mode のみ）；06:292（permit 発行時の required record 評価は「evidence store」に対して — record の `evidence_set_hash` に対してではない）；06:284（ceiling の baseline は「record の `timing_baseline_hash` が指す artifact」だが、その artifact の内容 hash 再検証は (j) に無い〔R4-02 と同型〕）。
- frozen: tensor_binding `:23-24`（content-addressed 束縛は「hash を持つ」ではなく「照合する」ことで効く）。
- 再現: CLOSED_LOOP を許す record g=2（two-key 済・`evidence_set_hash = H(E)`）。その後 evidence store に FAULT_INJECTION_RESULT を追加（E′ ⊋ E）／ あるいは E 内の HEALTH_CHECK_RUN を失効させ新 record で置換。permit 発行時の `P_EVIDENCE_UNBOUND` / `P_EVIDENCE_EXPIRED` は store を見るので E′ で通る。two-key が承認した集合 E は照合対象でなく、`evidence_set_hash` は装飾になる。`validator_hash` も同様: 第 2 評価点の validator 版が受理時と異なっても検出されない。`cell_identity_hash` は `profile_hash` が既に CellIdentity を含むため冗長で、何と比較するかも未定義。
- 最小修正: 05:190 (i) に「record の `evidence_set_hash` を解決し、第 2 評価点の required 集合は**その集合の部分集合**で充足すること（追加 evidence を用いる場合は新 generation）」「第 2 評価点の validator の content hash == `record.validator_hash`」「`record.timing_baseline_hash` の解決内容を再検証（不一致 = R_PROFILE_MISBOUND）」を追加。`cell_identity_hash` は削除するか「== H_WCJ(profile.cell)」の検査を `P_ACCEPTANCE_RECORD_INVALID` に入れる。

### R4-10 — MEDIUM — `CellSafetyTimingBaseline` が cell に束縛されず、baseline の改訂が失効 trigger に無い

- doc:line: 06:476-482（field = baseline_id / approved_ceiling / safety_response_budget_s / worst_case_attempt_s / selector_overhead_s / diagnostic_items — `cell_id` / `layout_revision_ref` / `cell_identity_hash` が無い）；06:487（失効 trigger = incident・ISL 異常・layout / tool / controller / safety-layer identity の変更 — **baseline 改訂は無い**）；06:525（OPP-16 = 改訂手続は RT0 へ carry — しかし「改訂されたら既存 record をどうするか」は runtime 側の規則で、本文に無い）；05:817（OP-20 同旨）。
- frozen: D0 `:321`（response budget は cell 固有の worst-case）；RS71 `:24`（固定 base = cell 固有 geometry）。
- 再現 (a): 別 cell（実験室・停止距離短・`approved_ceiling[safety_heartbeat_timeout_s] = 5 s`）向けに発行された baseline artifact を、本番 cell の record に `timing_baseline_hash` として束縛する。`P_TIMEOUT_CEILING` は artifact の値しか見ず、baseline と cell の対応を検査する code が無いので通る。two-key 承認者が baseline_id を目視で見分けるしかない。再現 (b): RT0 が baseline を改訂（ceiling を 1/2 に）しても、旧 hash を束縛した ACCEPTED record は `valid_until` まで有効で、旧 ceiling の timeout で運転が続く。DeploymentValidityMonitor の event 一覧（05:88・06:337）に baseline supersession が無い。
- 最小修正: `CellSafetyTimingBaseline` に `cell_id` と `layout_revision_ref`（または `cell_identity_hash`）を足し、`P_TIMEOUT_CEILING` の前提に `baseline.cell_id == profile.cell.cell_id ∧ baseline.layout_revision_ref == profile.cell.layout_revision_ref` を入れる。06:487 / 05:88 の失効 event に「timing baseline の supersession（baseline registry が旧 hash を superseded と記す）」を追加し、該当 record を SUSPENDED、活性 CLOSED_LOOP lease は TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED)。

### R4-11 — MEDIUM — event 起因の VALIDITY_EXPIRED（incident 等）に clearance が無く runtime が自動再 lease できる／SAFE_STOP 強化の判定材料が record に無い

- doc:line: 05:529（§5.4: VALIDITY_EXPIRED の追加前提 = **なし**、「新 permit が §3.4 (b)(i) で失効原因の解消を要求する」）；05:290（event 一覧 = incident / tool 交換 / firmware 更新 / layout revision / ISL build 変更 / SUSPENDED・REVOKED、fault = `R_PROFILE_SUSPENDED | R_EVIDENCE_EXPIRED_ACTIVE`、「SAFE_STOP if 安全起因」）；05:289 / 05:338（「SAFE_STOP if 失効項目が安全機能・TCP・停止性能に関わる」）；06:462-473（record に `reason` が無い）；06:487（monitor は SUSPENDED に「できる」= 任意）；06:210-216（`EscalationPolicy` に validity 系の強化 knob が無い）；06:337。
- frozen: D0 `:318-320`（fail-to-safe-stop と `stabilized_post_action_state` — 事故後の再開は資格判断を要する）。
- 再現: monitor が「incident」event を manager に通知（record は SUSPENDED にしない — 06:487 は「できる」）。manager は TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED)。§5.4 の VALIDITY_EXPIRED 行は追加前提なし、§3.4 (b)(i) は wall-clock 期限と record state しか見ない（incident は期限を動かさない）、health check（(a)）も incident を検出しない → 次の boundary で Orchestrator が permit を取り、人の clearance なしに CLOSED_LOOP 運転が再開する。また fault は `R_EVIDENCE_EXPIRED_ACTIVE`（失効していない）になり、commissioning evidence の subject 照合（06:354）と噛み合わない。SAFE_STOP 判定について: record に理由が無いので manager は「安全起因」を機械判定できず、規則 (1) の fail-closed を当てれば**常に SAFE_STOP**（散文と不一致）、当てなければ **常に HOLD**（安全側でない）。calibration 種別ごとの SAFE_STOP 表（TCP = SAFE_STOP は PT-13 のみ、CAMERA_* / acceptance 期限は不明）も無い。
- 最小修正: (1) monitor event は必ず registry へ `SUSPENDED` record（`reason: SuspensionReason(Enum) = INCIDENT | ISL_ANOMALY | IDENTITY_CHANGE | LAYOUT_CHANGE | BASELINE_SUPERSEDED | OPERATOR`）を append してから manager へ通知し、fault = `R_PROFILE_SUSPENDED` に統一（解除 = 新 generation + two-key のみ）。(2) `reason → disposition` の固定表（INCIDENT / ISL_ANOMALY / IDENTITY_CHANGE(safety layer) → SAFE_STOP、他 → HOLD）と、期限到達の `item → disposition` 表（TCP / SAFETY_LAYER_ACCEPTANCE / acceptance 期限 → SAFE_STOP、CAMERA_* / 他 → HOLD）を 06 §5 (b) に置き、`EscalationPolicy.on_validity_expired: EscalationTarget`（SAFE_STOP 側へのみ強化）を足す。

### R4-12 — MEDIUM — arm 単位 evidence の subject（`controller_firmware_ref` / `tool_id`）が同型 2 腕で同値になり「arm ごと」が壊れる

- doc:line: 06:354（CONTROLLER_ENVELOPE_MEASUREMENT = `arms[].controller_firmware_ref`（arm ごと）／ TOOL_PAYLOAD_IDENTIFICATION = `arms[].tool_payload.tool_id`（arm ごと））；06:305（`P_ENVELOPE_EXCEEDS_MEASURED`: 「各上限 ≤ CONTROLLER_ENVELOPE_MEASUREMENT record の測定値」— arm と record の対応規則なし）；06:292（(kind × subject) 単位の存在）；06:88-95（`ArmSpec.resource_id` は arms 内で一意 — subject に適した key が既にある）。
- frozen: RS71 `:15`（UR15 × 2 + Robotiq 2F-85 — 同型・同 firmware・同 tool 型番が既定の構成）。
- 再現: 両 arm とも `controller_firmware_ref = "UR-5.15.2"`、`tool_id = "2F-85"`。左腕の CONTROLLER_ENVELOPE_MEASUREMENT 1 件（subject "UR-5.15.2"）で (kind × subject) の存在が両腕分満たされ、`P_ENVELOPE_EXCEEDS_MEASURED` は右腕の envelope も左腕の測定値と比較する。右腕の実 controller が減速モード等で上限が低くても検出されず、宣言上限（= admission 上限）が右腕の実能力を超える。
- 最小修正: arm 単位 kind の subject_ref を `arms[].resource_id`（または `resource_id#robot_serial_ref`）に変更し、`P_ENVELOPE_EXCEEDS_MEASURED` を「∀ arm: arm.controller ≤ record(subject = arm.resource_id).measured」と書く。

### R4-13 — MEDIUM — v0.2.5 で追加した 5 fault code が `MIN_FAULT_INJECTION` に無く、validity-deadline / inter-arm 経路を一度も発火させずに CLOSED_LOOP へ行ける

- doc:line: 06:379-380（MIN_FAULT_INJECTION の列挙に `R_CALIBRATION_EXPIRED_ACTIVE` / `R_EVIDENCE_EXPIRED_ACTIVE` / `R_PROFILE_SUSPENDED` / `R_REGISTRY_UNAVAILABLE` / `R_INTER_ARM_VIOLATION` が無い）；06:380-381（R2-01 の規則「timeout 駆動の fault は…必ず発火させる」）；05:641（5 code の追加）；05:776-780（T-33 / T-34 / T-36 / T-37 は 05 の test plan にある）。
- frozen: D0 `:321`（worst-case を実測で示す — 経路の到達可能性は commissioning で示す設計思想〔R2-01 で bank〕）。
- 再現: MIN_FAULT_INJECTION どおりの 17 code の evidence だけで `P_EVIDENCE_UNBOUND` = 0。`validity_deadline_mono` 到達（timer 駆動 — R2-01 の class そのもの）や registry 不読、INTER_ARM 拒否は本 cell で一度も実行されないまま CLOSED_LOOP 受理可（negative control で補うはずの CELL#INTER_ARM_COLLISION は R4-03 で無効）。
- 最小修正: MIN_FAULT_INJECTION に `R_CALIBRATION_EXPIRED_ACTIVE#DEADLINE` / `R_EVIDENCE_EXPIRED_ACTIVE#DEADLINE` / `R_PROFILE_SUSPENDED#EVENT` / `R_REGISTRY_UNAVAILABLE` / `R_INTER_ARM_VIOLATION` を追加（timer 駆動 2 件は `#<trigger>` 文法で deadline 経路を要求）。

### R4-14 — MEDIUM — 記録にも health check にも痕跡を残さない物理変更（base 再取付・同型 tool 交換・ISL の hot update）が DeploymentValidityMonitor の未定義 event にしか依存しない

- doc:line: 06:67-69（HealthCheckKind に base pose / layout の readback が無い。TOOL_IDENTITY は列挙のみで定義文が無い）；06:98-100（layout は artifact の hash 束縛 — 物理と artifact の一致は検査されない）；06:382（CELL#ARM_SWAP / CELL#BASE_TRANSFORM_TAMPER の手順が未定義 — 「swap」が channel ごと入れ替わる完全 swap なら serial 照合は通る）；05:88（monitor は「検出し」とだけ — 入力源未定義。周期診断は baseline 項目のみ）；06:337（event 一覧）；PT-21 / PT-30（firmware / ISL 更新は**次 permit**の identity check で検出 — lease 中は監視されない）。
- frozen: RS71 `:24`（bases fixed at Y = ∓0.35 — 物理配置が不変前提）；D0 `:318`（heartbeat ≠ identity）。
- 再現 (a) base 再取付: 右腕 base を +5 cm ずらして再取付。layout artifact 不変 → hash 一致、CONTROLLER_IDENTITY / ENVELOPE_READBACK / TOOL_IDENTITY pass、CAMERA_EXTRINSIC は wall-clock 有効、layout revision event を誰も出さない → permit 成立。DEPLOYMENT_WORKSPACE / INTER_ARM は宣言 base で FK する（05:454, 457）ので keep-out 評価が 5 cm ずれ、実 TCP が keep-out に入っても admit。(b) 同型 tool 交換（2F-85 → 別個体の 2F-85、TCP 偏差あり）: `tool_id` が型番なら TOOL_IDENTITY pass、TCP calibration は期限内 → 誤った TCP で運転。(c) ISL の build 更新を lease 中に実施（heartbeat 継続）: SAFETY_LAYER_IDENTITY は permit / health confirmation 時のみ → lease 終了まで未検出。いずれも「監視対象の直近観測」（05:88）を誰がどう得るか書かれていないため、v0.2.5 の event 層は**入力が無い**。
- 最小修正（凍結 delta 0）: (1) HealthCheckKind に `BASE_POSE_VERIFY`（fiducial / 外部計測で各 arm の base pose を layout の T_cell←base と照合・許容差は baseline `diagnostic_items`）を追加し必須集合に入れる。(2) `ToolPayloadSpec.tool_id` を「個体 serial」と定義し TOOL_IDENTITY を「tool が報告する serial == tool_id」と定義文で置く。(3) 05:88 の monitor に入力源の最小集合（controller / ISL / tool の identity report 購読・layout / baseline registry の supersession 購読）を書き、identity 系は baseline `diagnostic_items` に周期項目として必須化。(4) CELL#ARM_SWAP / CELL#BASE_TRANSFORM_TAMPER の negative control に「物理手順（channel ごとの完全 swap・artifact 不変の再取付）」を明記し期待検出 = BASE_POSE_VERIFY fail とする。

### R4-15 — MEDIUM — wall-clock の健全性が検査されず、`validity_deadline_mono` と `P_*_EXPIRED` が誤設定 wall-clock で無効化される；`ENV#CLOCK_ANOMALY` に対応する runtime 規則が無い

- doc:line: 05:183（wall→mono 変換は発行時に確定）；06:293（`P_EVIDENCE_EXPIRED` は「permit 発行の wall-clock」）；06:291（`P_CALIBRATION_EXPIRED` も同様）；06:512（OPP-3 は対応の「取り方」だけを扱い健全性は扱わない）；05:146（clock domain = manager 単調 clock — wall-clock の出所・検証は未定義）；05:627-641（clock anomaly の fault 無し）；06:382（ENV#CLOCK_ANOMALY を negative control として要求）。
- frozen: D0 `:354-355`（monotonic clock + 単一 origin — wall-clock は仕様外ゆえ 05/06 が自前で健全性を定義する必要がある）。
- 再現: NTP 障害で host の wall-clock が 30 日戻った状態で permit 要求。TCP calibration（valid_until = 昨日）は「期限内」と評価され、`validity_deadline_mono = mono_now + (valid_until − wall_now)` = +29 日 → 失効 calibration で CLOSED_LOOP lease が成立し、deadline も来ない。逆方向（進み）は可用性のみ。commissioning で ENV#CLOCK_ANOMALY を「拒否 / SafeHold に到達」と示す手段が本文に無い（R4-03）。
- 最小修正: 05 §3.4 に (l)「permit 発行時の `wall_now` は ①全 required record の `measured_at_iso8601` と acceptance `approvals[].t` の最大値以上、②baseline `diagnostic_items` の wall/mono drift 閾値内、③直前の PERMIT_ISSUED record の wall_now 以上（単調）— いずれか偽 = `R_CLOCK_ANOMALY`（runtime enum 追加）・permit 不発行」を足し、PERMIT_ISSUED payload に `(wall_now, mono_now)` の記録を義務化。lease 中は monitor が同 drift を周期診断（baseline 項目）し超過 = TRANSFER_TO_SAFEHOLD(SAFE_STOP)。

### R4-16 — LOW — acceptance state machine の細部が未定義（REVOKED の再使用条件・SUSPENDED record の field 意味・同 generation の ACCEPTED 再記録・「最新」の順序・有限だが実質無期限の validity）

- doc:line: 06:459（「REVOKED は終端。再使用 = 新 generation の新 record のみ」）↔ 06:485（SUSPENDED → ACCEPTED は「原因除去 evidence + two-key」— REVOKED からの新 generation には条件が書かれておらず、終端のはずの REVOKED の方が緩い）；06:471-472（monitor が書く SUSPENDED record の `approvals` / `valid_until`（None 不可）/ `evidence_set_hash` の意味未定義）；06:485（ACCEPTED → ACCEPTED〔同 generation・allowed_lease_modes の拡大や valid_until 延長〕が遷移表に無く、06:491 は「generation 非単調」しか弾かない〔等値は非単調でない〕）；05:190 (i) / 05:239（「最新 record」の順序 = append seq か generation か未定義；CAS 条件 12 は generation を比較し `acceptance_record_hash` を比較しない）；06:472（valid_until は None 不可だが上限なし — 例 2126 年）。
- frozen: —。
- 最小修正: 06 §8.3 に「新 ACCEPTED record は常に generation を +1（同 generation の ACCEPTED 再記録は不可）；REVOKED からの新 generation は SUSPENDED と同じ条件（原因除去 evidence + 該当 two-key）；SUSPENDED / REVOKED record は superseded record の hash field を複写し approvals は失効者 1 名（MONITOR 可）、valid_until は superseded と同値；最新 = registry append seq 最大」を追記。CAS 条件 12 に `record_hash == permit.acceptance_record_hash` を追加。baseline に `max_acceptance_validity_s` を持たせ `P_ACCEPTANCE_RECORD_INVALID` で `valid_until − approvals.t ≤` を検査。

### R4-17 — LOW — `min_separation_m` の測定対象が未定義で RS71 88 mm grasp span との静的整合検査が無い／layout の base transform が RS71 固定 base と照合されない

- doc:line: 06:104（`min_separation_m > 0` のみ — TCP 点間か link 間か未定義）；06:302；06:308 / 06:327（`P_ENVELOPE_EMPTY` は「明らかな不能」の検出 — 本項を含まない）；06:82（layout は「RS71 §0 の固定 base Y = ∓0.35 を含む」と comment するが検査 code 無し）；06:311（`P_ROBOT_MODEL_MISMATCH` は robot model のみ）。
- frozen: RS71 `:24`（88 mm two-EE grasp span・bases fixed at Y = ∓0.35）。
- 再現: commissioning engineer が「安全側」に `min_separation_m = 0.10` を宣言。P_* = 0 で受理されるが、RS71 準拠の把持（両 TCP 間 0.088 m）は INTER_ARM で全て拒否 → cell は一度も task を実行できない（fail-closed だが「明らかな不能」）。逆に layout の base を Y = ∓0.50 で登録しても検査は無い（robot model は検査されるのに base は検査されない非対称）。
- 最小修正: 06:104 に「separation = 両 TCP 点間（collision hull は ISL）」と明記し、`P_ENVELOPE_EMPTY` の静的判定に `min_separation_m < RS71 grasp span（0.088 m・照合先 = RS71:24・値は本 doc に書かない）` を追加。`P_ROBOT_MODEL_MISMATCH` と対称に `P_BASE_LAYOUT_MISMATCH`（layout の T_cell←base の Y 成分が RS71 §0 #2 と一致・許容差は baseline）を追加するか、OPP-7 に「base 配置の照合は未検査」と loud に書く。

### R4-18 — LOW — `effective_expires_at` の min に「decision 項が存在する場合」の条件が無く、fail-closed の字義では SHADOW permit が消費できない；SHADOW の permit に validity deadline を混ぜつつ lease は無視する

- doc:line: 05:187（`min(expires_at, ack.valid_until, decision.t_mono + decision_max_age_s, validity_deadline_mono)`）；05:179 / 05:469（SHADOW では `authority_decision_ref` 任意 = null 可）；05:229（条件 2 は `now < effective_expires_at`・不能判定 = FALSE）；05:424（SHADOW lease では validity deadline は記録のみ）。
- frozen: contracts_v2 `:354`（granted は CLOSED_LOOP の必要条件 — SHADOW に decision を要求しない）。
- 再現: SHADOW permit で decision が null → 第 3 項が評価不能 → 05:227「不能判定 = FALSE」を字義どおり当てると条件 2 が常に偽 → slice（SHADOW）が一度も CAS できない。意図は明白だが本文は fail-closed を宣言しているので書面どおりでは成立しない。
- 最小修正: 05:187 を「存在する項の min（SHADOW で decision が無ければその項を除く）」と書き、SHADOW の permit では `validity_deadline_mono` を min から外すか「permit にも適用（保守側）」と明示する。

### R4-19 — LOW — 「two-key」の語が frozen `two_key_state`（O0/S0/V0）と区別されず、deployment 受理の two-key が authority 側 two-key の代替と読める

- doc:line: 06:486・06:522・05:784（「O0/S0/V0 two-key」と同じ語）；06:495（§9 項 1 は「profile は必要条件を足すだけ」と言うが、受理 two-key と frozen two-key の関係〔別物・AND〕を明記しない）。
- frozen: contracts_v2 `$D/WMSO_D11A_CONTRACTS_V2_DESIGN_RSTECHLEAD2_20260719.md:349`（`two_key_state` は `evaluate_authority_grant` の入力）；EP `:156`。
- 最小修正: 06 §8.3 と §9 項 1 に「本 doc の acceptance two-key は deployment 層の受理権限であり、frozen `evaluate_authority_grant.two_key_state`（O0/S0/V0）を充足・代替しない（常に AND）」を 1 文追記。

## 4. PASS 2 — adversarial 構成の要約（上記 finding への対応）

| 攻撃 / cell 状況 | 通る? | 対応 finding |
|---|---|---|
| 両腕同時 command で相互接近（各腕は他腕の旧位置に対して clear） | **通る** | R4-01 |
| 受理後に swept-volume predicate / layout artifact を差し替え | **通る**（(j) 列挙外・gateway 再検証句なし） | R4-02 |
| negative control 無しで CLOSED_LOOP 受理 | **通る** | R4-03 |
| 同一 operator_ref が 2 role を名乗って CLOSED_LOOP 受理 | **通る** | R4-04 |
| 別 cell の緩い baseline を束縛 / baseline 改訂後も旧 ceiling で継続 | **通る** | R4-10 |
| two-key 後に足した evidence で permit | **通る** | R4-09 |
| arm swap（channel ごとの完全 swap） / base 再取付 / 同型 tool 交換 / ISL hot update（lease 中） | **通る**（event が出なければ） | R4-14 |
| wall-clock 逆行下の permit | **通る** | R4-15 |
| incident event 後の自動再 lease（record を SUSPEND しない場合） | **通る** | R4-11 |
| registry SUSPENDED mid-lease（CLOSED_LOOP） | 止まる（05:290・条件 12・T-34）— ただし SAFE_STOP/HOLD の判定材料なし | R4-11 |
| 全 timeout 同率 100 倍（順序維持） | 止まる（`P_TIMEOUT_CEILING`・PT-36）— baseline 自体の妥当性は OPP-16 | — |
| rate / hold / control_mode の narrow | 止まる（等値・PT-01/02、前提 (6) どおり） | — |
| profile_hash ↔ record ↔ baseline の循環 | 無し（06:490 一方向） | — |

## 5. 主張しないこと

- Rs 裁定 4 件の方向（B+ / 外部 baseline / append-only registry + two-key / deadline + event）には異議を述べない。全 finding は実現の欠陥であり、修正案は凍結 4 file を触らない（runtime / deployment 層の型・field・enum・規則のみ）。
- 本 report は AI reviewer の準備レビューであり、two-key / Rs 裁定 / gate を flip しない。impl / training / closed-loop authority は CLOSED のまま。
- open = 0 を宣言しない。未読領域: 05 §13 の v0.2.1–v0.2.4 fold 行（11_ §3/§7 で代替）、02/03 CSV。
