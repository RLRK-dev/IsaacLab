# VERIFY_C_v022 — finding group C（reviewer C・CD-01〜CD-18・v0.2.2 候補対象）の 3 lens 反証検証（独立・再実施）

- 検証者 = adversarial verifier（別 context・authority 無し・凍結物へ非接触）。実施 = 2026-09-04 20:33–20:39 UTC（`date -u` 実測: pin 再測 20:33:05 UTC、最終 sed/grep 20:39:04 UTC）。⚠ 同 path に 16:27 UTC 実施の先行 verifier 報告が存在したが、独立性のため header 3 行のみ確認し本文は読まずに上書きした（08 §1 項 6）。
- 対象: `$R = eval_runs/troot_optE_dapg_wholeroute_scope_20260701/WMSO_EXACT_BASELINE_REVIEW_v0.2_20260903/` の 05（893 行）・06（474 行）を `wc -l` 実測後に全行 `cat -n` で読了。04（76 行）§8・08（121 行）・10（55 行）全読。frozen 4 file は finding が引く行を `awk NR==n` で個別に確認（本文中 `A:` = D1.1-A contracts_v2、`B:` = D1.1-B tensor_binding、`EP:` = EvidencePolicy md、`D0:` = D0 architecture）。
- 規則（briefs/VERIFY.md）: text / logic / impact の 3 lens を各 finding に独立適用。既定 = refuted（文書どおりに欠陥を再現できなければ反証）。2 lens 以上が未反証のときのみ confirmed。severity は verifier が再判定。fold_status = NOT_APPLICABLE（本 group は未 fold）。
- 重複統合: 18 件を突合し**完全重複なし**（CD-02/CD-09 は GATEWAY_CONFIG_MATCH 必須化という修正案を共有するが claim が異なる。CD-03/CD-08 は `P_FAULT_CODE_UNKNOWN` を共有するが claim が異なる。CD-01/CD-13 は INV-22 を共有するが claim が異なる）。全件 merged_into = ""。

## 0. 共通前提の実測（08 §1）

1. **pin 再測**: repo root で `bash $R/verify_exact_baseline_pins.sh`（素の bash）→ **PASS 11 / FAIL 0**（`grep -c '^PASS'` = 11）。D1.1-A `00192d20ca00b654cf6cfdb9d04b03ca14adfd0b93f2105c0fea28c295ff8aff` @ `54f90a7d`（blob `1353430228a9…`）／ D1.1-B `5a1874d3be8b98b8aeaace73890d8021cbc7f9814e048741f7f58d6746f349a6` @ `07250f4a`（blob `ebe8154abf24…`）／ EP md `c474acea7c58acc22050c2ad9944fd45a18f5c76967964b42d11922e28fa27e7` ／ EP JSON `e63176af9bc3a246b1c32db369ec59f8d09a4c96c381bb03a3a6024bd9811c6e`（definition hash `e7ca43093084c167a209b008533a66d26a1fd3223d2a3c11274d28306c3ff803`・policy_semver 1.9.0）。HEAD = `f3c6ca350687fd766a01862c287f868107cbb0ba` branch `claude/v0-2-design-candidates-snqbmy`。出力全文 = `scratchpad/review/pin_C.txt`。
2. **content sha**: `sha256sum -c SHA256SUMS.txt` → 22 行全て OK（SHA256SUMS mtime 2026-09-04 15:49:41 UTC・05 は 15:48:58・06/04/07/08/10 は 15:41:57）。04 = `e85490d7…`、05 = `6e57398d…`、06 = `c0faa7bf…`。package は HEAD `f3c6ca35` で commit 済・working tree clean（`git status --short` = 空）。
3. **機械検査**: `python3 check_review_candidate.py 05 --runtime` → citations=151 **FAIL=0** WARN=10 ／ `06` → citations=59 **FAIL=0** WARN=23 ／ `04` → FAIL=0 WARN=7（読む前に実行）。
4. **陽性対照**（10_）: PC-06-E/F/G/H（bounds field / rate ≤ / requires_safety_layer_health=False / SAFE_STOP→HOLD 緩和）は複製にのみ注入。本 group の 18 件はいずれもこれらと一致しない（CD-14 は「True 必須 bool の存在」を指摘するもので、PC-06-G の「False 可」注入とは逆向き — 実 doc 06:117 は True 必須のまま）。
5. **11_ の実在**: `find $D -name '11_*'` = 0 件、repo 内 `git ls-files` にも無し（scratchpad の `design/record/11_dryrun.md` は別物）。

## 1. 判定一覧

| id | text | logic | impact | verdict | severity（verifier） | reviewer |
|---|---|---|---|---|---|---|
| CD-01 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | HIGH | HIGH |
| CD-02 | 未反証 | 未反証 | 未反証（availability 側） | **CONFIRMED** | MEDIUM（降格） | HIGH |
| CD-03 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | HIGH | HIGH |
| CD-04 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | HIGH | HIGH |
| CD-05 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | MEDIUM | MEDIUM |
| CD-06 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | MEDIUM | MEDIUM |
| CD-07 | 未反証 | 未反証（(v) over-claim 部分は反証・force/torque 部分は成立） | 未反証 | **CONFIRMED（範囲縮小）** | MEDIUM | MEDIUM |
| CD-08 | 未反証 | 未反証（typo 例は弱い・required set 未定義は成立） | 未反証 | **CONFIRMED** | MEDIUM | MEDIUM |
| CD-09 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | MEDIUM | MEDIUM |
| CD-10 | 未反証 | 未反証 | 未反証（OP-19 が一部を既に open 化） | **CONFIRMED** | MEDIUM | MEDIUM |
| CD-11 | 未反証（11_ 不在）／ SHA 部分は**反証（stale）** | n/a（事実命題） | 未反証 | **CONFIRMED（SHA 部分を除く）** | MEDIUM | MEDIUM |
| CD-12 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | LOW | LOW |
| CD-13 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | LOW | LOW |
| CD-14 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | LOW | LOW |
| CD-15 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | LOW | LOW |
| CD-16 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | LOW | LOW |
| CD-17 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | LOW | LOW |
| CD-18 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | LOW | LOW |

集計: confirmed 18 / 18（うち severity 変更 1 件 = CD-02 HIGH→MEDIUM、範囲縮小 2 件 = CD-07・CD-11）。HIGH 残 = CD-01 / CD-03 / CD-04 の 3 件 ⇒ 08 §5 の語彙で本 group 単独の verdict は **HOLD**（HIGH が残る）。

## 2. finding 別の検証

### CD-01（C1・HIGH）RuntimeTimeouts に上限が無く、profile が 05 の安全機構を空文化できる

- **sed 実測**: 06:252「全 `RuntimeTimeouts` field > 0・有限 ⇒ `P_TIMEOUT_NONPOSITIVE`」／ 06:253「`command_deadline_s ≥ 1 / action_rate_hz` ⇒ `P_DEADLINE_BELOW_PERIOD`」— timeouts を読む `P_*` はこの 2 つのみ（06:355 の code 一覧・06:250-264 の表を全走査）。06:266 の「広げる」経路 (i)-(ix) に timeout の項は無い。06:386「本 doc は timeout の器を与えるだけ」。05:696 INV-22「本 doc に数値定数は無い」、05:770 OP-10「全 timeout の値 = profile parameter・RT0 測定前」、05:181 `expires_at = issued_at + profile.permit_ttl_s`、05:488 (i′)・05:703 INV-29 は `safety_heartbeat_timeout_s` 超過で発火、05:420「無期限 ZOH を安全と見なさない」は `command_deadline_s` に依存。D0:318「missed heartbeat ⇒ consumers must not assume safety alive」。`grep -i 'timeout\|上限\|ceiling'` in 06 = 上限を課す文は無し。
- **text**: 未反証。引用は正確で、上限・順序を課す規則は 06 のどこにも無い。
- **logic**: 未反証。反例 profile（全 timeout = 1e9・`inter_command_jitter_s` = 1e9）は `P_TIMEOUT_NONPOSITIVE`（>0・有限）と `P_DEADLINE_BELOW_PERIOD`（1e9 ≥ 1/rate）を通り、他の `P_*` は timeouts を読まない ⇒ 受理。効果: (i′)/INV-29 は実質発火せず（B2-01/B-H3 fold の空文化）、`R_DEADLINE_MISS` は発火せず直近 setpoint を事実上無期限保持、permit の失効・ACK の valid_until・`decision_max_age_s` は vacuous。
- **impact**: 未反証。緩和要因 = (a) profile 採否は two-key + Rs（06:58）、(b) 数値は RT0 測定後（OP-10 / OP-14）、(c) ISL 自身の「failed ⇒ safe-stop」（D0:318）は独立。しかし 08 §4 C1 は「検査を通る『広げる』profile を構成できるか」を直接問うており、構成できる。06:266 が (v) のみ判定不能と主張する網羅性主張は timeouts を落としている。**HIGH 維持**。
- **minimal fix（schema delta 0・数値定数を書かない）**: (1) 06 §6.1 `MIN_FAULT_INJECTION` に timeout 駆動の fault code（`R_SAFETY_LAYER_LOST` / `R_DEADLINE_MISS` / `R_PERMIT_EXPIRED` / `R_ACK_TIMEOUT` / `R_HEALTH_CONFIRM_TIMEOUT` / `R_TIMING_VIOLATION` / `R_BOUNDARY_DWELL_EXCEEDED`）を追加し、同一 `profile_hash` の下で各 timeout が実際に発火した `FAULT_INJECTION_RESULT` を CLOSED_LOOP_AUTHORITY 前に要求する（1e9 秒の timeout は commissioning で発火させられない ⇒ 自然に排除。CD-03 の per-code 検査と対）。(2) 06 §3 表に相対順序検査 `P_TIMEOUT_ORDER`（候補: `safety_heartbeat_timeout_s ≤ command_deadline_s`、`inter_command_jitter_s < 1/action_rate_hz`（LEARNED）、`decision_max_age_s ≤ permit_ttl_s`、`ack_validity_s ≤ boundary_dwell_s`）を追加 — 順序自体に物理的意味があるため Rs 確認付き。(3) 06:266 に経路 (x)「timeout を伸ばす」を追記し、絶対上限は OPP（RT0 / 05 OP-10 / OP-14 依存）として登録。

### CD-02（C4/C1・MEDIUM に降格）clearance_roles / GATEWAY_CONFIG_MATCH の未強制と clearance record の未定義

- **sed 実測**: 06:185 `clearance_roles: tuple[tuple[str,str],...]`「追加の role … ISL の clearance record の代替ではない（常に AND）」。06 の `clearance` 出現 = 185・436 のみ（`P_*` に clearance 関連なし）。06:254 必須 health check = SAFETY_LAYER_HEARTBEAT / CONTROLLER_LIVENESS / ENVELOPE_READBACK（GATEWAY_CONFIG_MATCH は 06:67 の enum member のみ）。05:231 条件 10「該当する clearance record（§5.4）が存在する」、05:508 SAFE_STOP「profile が定義する operator / health-check clearance（doc 06）」、05:509 CHECKPOINT_DISABLED「profile が定義する operator clearance + 再観測」、05:511 GATEWAY_RESTART「`GATEWAY_CONFIG_MATCH` health check の pass + profile が定義する clearance role」。05 の `clearance` 出現 = 141/231/507-511/634/647/771/815 — clearance record の payload 型は無い（AuditKind `CLEARANCE_RECORDED` 05:646 のみ）。05:671 §9 既定「評価不能 = FALSE」。D0:273-274 fail-closed producer retains。
- **text**: 未反証。
- **logic**: 未反証。`clearance_roles = ()` + 必須 3 kind のみの profile は全 `P_*` = 0 で受理。runtime で NO_CHAIN × `on_no_chain = SAFE_STOP` ⇒ `S_SAFEHOLD(SAFE_STOP)`。条件 10 の「該当 record」は profile が何も定義しないため未定義 ⇒ 05:671 の既定で FALSE ⇒ この profile の下では解除不能。GATEWAY_RESTART も profile が GATEWAY_CONFIG_MATCH を持たなければ pass を評価できず FALSE。⚠ ただし「永久 HOLD」ではない: clearance role を定義した**新 profile**（新 hash）の permit 経路で解除可能（permit は新 profile_hash を束縛）。
- **impact**: 未反証だが方向は **availability**（fail-closed 既定が曖昧さを安全側に解く）。「loose reading = 任意 record で解除」は本文に根拠が無く採用できない。ゆえに HIGH ではなく **MEDIUM**。
- **minimal fix（schema delta 0）**: 06 §3 表に `P_CLEARANCE_ROLE_MISSING`（∀ reason ∈ {SAFE_STOP, CHECKPOINT_DISABLED, MANAGER_RESTART, GATEWAY_RESTART}: ∃ (reason, role) ∈ clearance_roles。SAFETY は ISL primary + AND のまま）；06:254 の必須集合に `GATEWAY_CONFIG_MATCH`（stage ∈ {BEFORE_PERMIT, BOTH}）を追加；05 §5.4 に clearance record の payload（`safehold_reason, role, operator_ref, profile_hash, t_mono` = `CLEARANCE_RECORDED` の typed payload）と条件 10 の「該当」述語（`record.reason == current.safehold_reason ∧ record.role ∈ profile.clearance_roles[reason] ∧ record.profile_hash == permit.profile_hash`）を明文化。

### CD-03（C5・HIGH）DeploymentEvidencePolicy の validity / per-subject coverage が未強制

- **sed 実測**: 06:257「§6 の required kinds が profile_hash に対し存在 ⇒ `P_EVIDENCE_UNBOUND`」（kind 存在のみ）。06:256 `P_CALIBRATION_EXPIRED` は `CalibrationRef.valid_until` のみ。06:309 `DeploymentEvidenceRecord.valid_until_iso8601` を読む `P_*` は無し（06 の `valid_until` 出現 = 146/172/256/309）。06:322/324 `ValidityRule.ALL_REQUIRED_VALID_AT_PERMIT_ISSUE` は enum 宣言のみで対応 code 無し。06:321/329 `MIN_FAULT_INJECTION`（8 code・06:326「規範」）に対し per-code の record 存在検査は無い（06:264 `P_EVIDENCE_POLICY_TOO_WEAK` は policy が集合を**宣言**しているかのみ）。06:304 `subject_ref` は per-subject record を想定。05:184 (b) は「`P_CALIBRATION_EXPIRED` / `P_EVIDENCE_UNBOUND` / `P_EXPECTATION_UNCERTIFIED`」の再評価のみ。05 の `FAULT_INJECTION|valid_until|DeploymentEvidence` = 6 hit、いずれも ACK の valid_until か EP 分離文で、強制文なし。EP:154 per-component 不在 = UNKNOWN(0) = 失格（06 が写すと主張する pattern）。A:354 granted == True 必要条件（06:383「profile は条件を足すだけ」）。
- **text**: 未反証。
- **logic**: 未反証。(1) `SAFETY_LAYER_ACCEPTANCE` record の `valid_until = 2025-01-01` でも kind 存在で `P_EVIDENCE_UNBOUND` 不発火。(2) `subject_ref = R_OFFER_REUSED` の 1 record で kind `FAULT_INJECTION_RESULT` 充足 ⇒ 他 7 code 未実施でも 05:184 (b) pass。(3) 06:257（登録時表）と 06:357（第 2 評価点）の両方に現れるが、登録時にどの mode の required set を使うか未記述。
- **impact**: 未反証。規範と宣言された `MIN_FAULT_INJECTION` / `validity_rule` に強制述語が無い = 「未 wire = appearance-only」。CLOSED_LOOP は CLOSED 継続ゆえ今日の運転影響は無いが、C5 の fail-closed bar に対する設計欠陥。**HIGH 維持**。
- **minimal fix（schema delta 0）**: 06 §3 表の `P_EVIDENCE_UNBOUND` を (kind × subject) で定義: `CALIBRATION_RECORD ∀ calibration_id`、`HEALTH_CHECK_RUN ∀ check_id`、`FAULT_INJECTION_RESULT ∀ req ∈ policy.fault_injection with must_be_exercised_before ⊑ proposed_lease.mode: ∃ record(kind, subject_ref == req.runtime_fault_code, profile_hash 一致)`；`P_EVIDENCE_EXPIRED`（required record の `valid_until` < permit 発行の wall-clock）を第 2 評価点に追加し 05:184 (b) の列挙に加える；登録時 = MIN_SHADOW 集合・permit 発行時 = mode 別集合と明記；`P_FAULT_CODE_UNKNOWN`（`runtime_fault_code ∉ 05 RuntimeFaultCode`）。

### CD-04（C3/C1・HIGH）predicate_refs / evaluator_ref / tcp_offset_ref が content hash を持たない

- **sed 実測**: 06:116 `predicate_refs: tuple[str, ...]`、06:163 `evaluator_ref: str`、06:98 `tcp_offset_ref: str`（いずれも sha256 無し）。対照 06:103 `ZoneRef.geometry_sha256`、06:171 `CalibrationRef.artifact_sha256`、06:177 `GatewayConfigRef.artifact_sha256`。06:14「content-addressed artifact」、06:285「活性 lease の profile を in-place で差し替える経路は無い」、06:389 項 7「存在・束縛・fail-closed を要求」。05:375 `EnvelopeTerm.predicate_ref`「内容は source 側が持つ」。06:406 OPP-9 は `evaluator_ref` の登録・版管理を open と認める（predicate_refs には無い）。B:23 preimage で identity が決まる、A:151 InitiationSpec は schema_hash + payload_canonical_json で内容束縛（06 は ExtraInitiationConjunct でこれを踏襲、SafetyRestrictionSet では踏襲していない）、B:255 凍結側は content-addressed store を要求しない（= 本 fix は profile 層の自己宣言で足り、凍結側に要求を足さない）。
- **text**: 未反証。
- **logic**: 未反証。`keepout_pred_v1` の resolver 内容を always-admit に差し替えても `profile_hash`・lease・`P_*`/`R_*` は不変で gateway は差し替え後の述語で admit。evaluator_ref も同様。
- **impact**: 未反証。handoff 決定 6（envelope を lease に**完全に**束縛）に対し SAFETY_RESTRICTION 項は名前束縛に留まる。C3「in-place 更新無し」が実質破れる。evaluator_ref は OPP-9 が部分的に覆うが predicate_refs は覆われていない。**HIGH 維持**。
- **minimal fix（frozen delta 0・06 自身の型に field 追加）**: `SafetyRestrictionSet.predicate_sha256: tuple[str, ...]`（predicate_refs と並行）、`HealthCheckSpec.evaluator_sha256: str`；第 2 評価点で解決内容の sha256 と宣言を比較し不一致 = `R_PROFILE_MISBOUND`；gateway は評価前に再照合し不一致 = 評価不能 = FALSE（05:436 と整合）；`tcp_offset_ref` は `calibration_refs` の kind tcp entry に解決必須（`P_TCP_REF_UNRESOLVED`）。

### CD-05（C1/C3・MEDIUM）SCRIPTED/WAIT の BindingExpectation と 05 側の membership 不在

- **sed 実測**: 06:188「SCRIPTED / WAIT では唯一の source」、06:190 `tensor_binding_hash: str | None # LEARNED ⇔ 非 null`、06:237 `P_RATE_MISMATCH` = 「== TensorBindingSpec の値」、06:357 規則 (1)「評価不能 = 違反」、06:19「制御周波数・hold の変更権（等値のみ）」を持たない。05 の `BindingExpectation|binding_expectation` = **0 hit**（grep -c）。05:184 (c)「TimingBinding の等値（INV-12）」→ 05:686 INV-12 は LEARNED lease のみ。05:222-232 CAS 条件 1-11 に membership 無し。B:289 SCRIPTED/WAIT は slot EXPLICIT_NONE（TensorBindingSpec 無し）、A:62 ControlMode に SCRIPTED_SEQUENCE / WAIT。
- **text**: 未反証。
- **logic**: 未反証。(a) SCRIPTED expectation `{tensor_binding_hash=None, action_rate_hz=50}` は `P_RATE_MISMATCH` の比較対象を持たない ⇒ 規則 (1) で違反（全 SCRIPTED profile 拒否）か、検査 skip（profile が rate の唯一 source = 06:19 と矛盾）かのいずれかで、本文はどちらも決めていない。(b) profile に列挙されていない certified skill の lease を 05 は拒否しない。
- **impact**: 未反証。slice の skill は SCRIPTED/WAIT を含み得るため (a) は登録可否に直結、(b) は `binding_expectations` を 05 に対して装飾にする。**MEDIUM**。
- **minimal fix（schema delta 0）**: 06 §3 表に「`P_RATE_MISMATCH` / `P_HOLD_MISMATCH` / `P_BINDING_HASH_MISMATCH` は `tensor_binding_hash ≠ null` のときのみ評価。null expectation は `policy_rate_hz / obs_sampling_rate_hz / action_rate_hz / hold = None` 必須（非 null = `P_INTRINSIC_OVERRIDE` の実用途）。SCRIPTED の timing は `command_deadline_s` のみ」と明記し 05 §4.2 と同期；05 §3.4 に前提 (g)「∃ e ∈ profile.binding_expectations: e.skill_action_id == proposed_lease.skill_action_id ∧ e.tensor_binding_hash == proposed_lease.timing.tensor_binding_hash ∧ e.control_mode == proposed_lease.control_mode、違反 = `R_PROFILE_MISBOUND`」。

### CD-06（C8・MEDIUM）chained handoff の ownership が ResourceAvailability と交差されない

- **sed 実測**: 05:414「`ownership` は frozen `HandoffOffer.ownership` をそのまま束縛し … SAFEHOLD からの初回起動では … profile が宣言する cell の資源可用性から構成する（§12 OP-3）」、05:763 OP-3、06:137「SAFEHOLD からの初回起動で offered 集合となる」、06:242 表は宣言のみ、06:286「`lease.ownership`（SAFEHOLD 起動時）← `ResourceAvailability`」。05 の `ResourceAvailability|resource_availability` = 414/763 の散文のみ；`offer.ownership ⊆ producer lease.ownership` の検査も無し（05 `ownership` 全出現 15/44/53/63/82/111/330/402/414/498/500/688/802/875 を確認）。A:380 required ⊆ offered（bool 包含）、A:506 `HandoffOffer.ownership`。
- **text**: 未反証。
- **logic**: 未反証。profile `control = {ee_left: True, ee_right: False, …}`、producer offer `control = {ee_left, ee_right: True}`、consumer required = {ee_right} ⇒ A:380 pass、05 §3.4/§3.5 に profile との比較無し ⇒ lease は cell に無い資源を束縛。
- **impact**: 未反証。lease は常に 1 executor（05:750）ゆえ二重所有ではなく、存在しない腕への command は 05:436「評価不能 = FALSE」で落ちる可能性が高い（安全影響は限定）。しかし handoff 決定 D「cell 固有条件 = profile」に対し chain 途中の ownership が profile と無関係になるのは束縛の抜け。**MEDIUM**。
- **minimal fix（schema delta 0）**: 05 §3.4 前提 (h)「`proposed_lease.ownership.control ⊆ profile.resource_availability.control ∧ (ownership.contact ⇒ resource_availability.contact)`、違反 = `R_PROFILE_MISBOUND`」；06 §3 `required_control_resources` 行に「全 lease の offered 集合 ⊆ 宣言」を追記。

### CD-07（C1/C2・MEDIUM・範囲縮小）ControllerEnvelope の force/torque が command から評価不能・06:266 の over-claim

- **sed 実測**: 06:82-90 ControllerEnvelope（`ee_force_max: CanonicalDecimal | None`、`joint_torque_max: tuple | None`）、06:109 `zones: tuple[ZoneRef, ...]`（空許容）、06:116 `predicate_refs`（空許容）、06:258 `P_ENVELOPE_SHAPE`（tuple 長のみ）、06:259/278 `P_ENVELOPE_EMPTY`（`ee_speed_max = 0` 等の明らかな不能のみ）、06:266「(v) だけは契約層では真偽を判定できない」、06:272 CONTROLLER 項「velocity / acceleration / jerk / force / torque」、06:320 CONTROLLER_ENVELOPE_MEASUREMENT は kind 存在のみ。05:433 CONTROLLER 項の評価者 = CommandGateway、05:436「評価不能 = FALSE」。D0:317 FORCE_LIMIT は ISL の SafetyDecision、B:65-66 command = EE target（transform 後）。
- **text**: 未反証（引用どおり）。
- **logic**: **部分反証**。(a)「06:266 は over-claim」— joint_velocity_max = 1e6・zones = ()・predicate_refs = () は受理されるが、これらは frozen に存在しない**追加の**連言項であり、緩くても frozen INTRINSIC 項（D1.1-B bounds）より緩い挙動は生じない ⇒ 06:266 の「frozen より緩い挙動を引き起こす経路」の網羅性は破れない（(v) は offered 宣言が frozen 検査 A:380 の入力になる点で異質）。この sub-claim は反証。(b)「force/torque は EE-target command から評価不能」— 成立。DIFF_IK_EE_TARGET command は目標姿勢のみを持ち接触力・関節 torque は導出不能。05:436 の既定に従えば `ee_force_max` を設定した profile の CONTROLLER 項は全 command で FALSE（lease 不能）か、gateway が黙って項を skip する（未規定の非執行）かの二択で、本文はどちらも決めていない。velocity/acceleration/jerk は直近 admitted setpoint と action_rate から有限差分で評価可能ゆえ問題なし。
- **impact**: 未反証（(b) について）。安全限界が「常時拒否」か「未執行」かに割れるのは fail-closed bar の内部矛盾。**MEDIUM**（(a) 縮小後）。
- **minimal fix（schema delta 0）**: 05 §4.3 / 06 §4 で ControllerEnvelope を admission-evaluable {joint velocity / acceleration / jerk, ee_speed_max} と monitored {ee_force_max, joint_torque_max}（ISL / controller 側・CONTROLLER 項の外・D0:317 FORCE_LIMIT に接続）に分け、後者を gateway 述語から外すと明記；06:266 を「`P_*` は形式と単調性のみ検査し、物理事実 field（ResourceAvailability / ControllerEnvelope 値 / zone 幾何 / predicate 内容 / tool 質量）は evidence 委譲」と書き直し OPP-5 を拡張；任意で `P_ENVELOPE_NONPOSITIVE`（全 limit > 0）。

### CD-08（C4/C1・MEDIUM）free string の kind / representation / runtime_fault_code と「belief を担う camera 系」の未定義

- **sed 実測**: 06:170 `CalibrationRef.kind: str # "tcp" | "camera_extrinsic" | … | ...`、06:104 `ZoneRef.kind: str`、06:90 / 06:110 `representation: str`、06:313 `runtime_fault_code: str`、06:256「必須 kind（`tcp` + belief を担う camera 系）」、06:227 strict codec（enum strict は enum field のみ）。B:31 FeatureSource = SEMANTIC_OBS | SEMANTIC_ACTION | BELIEF（sensor identity 無し）、A:385 enum strict / unknown = `E_ENUM_UNKNOWN`。
- **text**: 未反証。
- **logic**: 未反証（主張の核）。「belief を担う camera 系」は profile にも frozen にも導出元が無く、`P_CALIBRATION_MISSING` の required set が計算不能 ⇒ 06:357 規則 (1) で「評価不能 = 違反」となり全 profile 拒否か、実装者裁量かの二択。⚠ 反例の一部は弱い: `camera_extrnsic` の typo は required kind 不在として fail-closed に落ちる（欠陥ではなく仕様どおり）；`R_EPOCH_STALE_COMAND` の typo は required member 欠落として `P_EVIDENCE_POLICY_TOO_WEAK` が発火する（追加 entry の typo のみ通過し無害）。`representation = "workspace"` は登録時に通り runtime で FALSE（遅いが fail-closed）。
- **impact**: 未反証。C4 の機械検査可能性に対する欠陥（`P_CALIBRATION_MISSING` が決定的でない）。**MEDIUM**。
- **minimal fix（schema delta 0）**: 06 §2.1 に閉 enum `CalibrationKind {TCP, CAMERA_EXTRINSIC, CAMERA_INTRINSIC, FORCE_SENSOR}`、`ZoneKind {KEEP_OUT, REACH_LIMIT, HEIGHT_BAND}`、`Representation {JOINT_SPACE, WORKSPACE}`（型で固定し自由 field を削除）；§3 に `REQUIRED_CALIBRATION_KINDS(control_mode)`: LEARNED = {TCP, CAMERA_EXTRINSIC, CAMERA_INTRINSIC}、SCRIPTED/WAIT = {TCP}；`P_FAULT_CODE_UNKNOWN`（CD-03 と共有）。

### CD-09（C4・MEDIUM）permit 時に robot_serial / firmware / tool を確認する HealthCheckKind が無い・ENVELOPE_READBACK 未定義

- **sed 実測**: 06:67 HealthCheckKind = CONTROLLER_LIVENESS | SAFETY_LAYER_HEARTBEAT | CALIBRATION_VALID | TOOL_IDENTITY | GATEWAY_CONFIG_MATCH | ENVELOPE_READBACK；06:77-78 `robot_serial_ref` / `controller_firmware_ref`；06:254 必須 3 kind（TOOL_IDENTITY / CALIBRATION_VALID / GATEWAY_CONFIG_MATCH は任意）；06 の ENVELOPE_READBACK 出現 = 67・254 のみ（定義無し）；05:158 `envelope_readback_ok` は executor 側の読み戻し；06:296 TOOL_PAYLOAD_IDENTIFICATION は commissioning record。B:185 bump = identity event（frozen 側は hash で識別変化を検出、deployment identity には対応物なし）、D0:318。
- **text**: 未反証。
- **logic**: 未反証。commissioning 後に firmware 更新 + tool 交換 → profile 不変 → 必須 3 check pass（controller alive）→ permit・CLOSED_LOOP lease 成立。
- **impact**: 未反証。C4「識別・版・evidence 束縛 … の fail-closed」に対する欠落。**MEDIUM**。
- **minimal fix（schema delta 0）**: 06 §2.1 に `CONTROLLER_IDENTITY`（serial + firmware が CellIdentity と等値）を追加し、06:254 の必須集合を {SAFETY_LAYER_HEARTBEAT, CONTROLLER_LIVENESS, CONTROLLER_IDENTITY, TOOL_IDENTITY, GATEWAY_CONFIG_MATCH, ENVELOPE_READBACK}（各 stage ∈ {BEFORE_PERMIT, BOTH}）へ；`ENVELOPE_READBACK` を「controller 側の設定運動学限界の読み戻し vs ControllerEnvelope」と定義（executor 側読み戻しは 05:158 のまま）。

### CD-10（C3/C4・MEDIUM）「曝露 ≤ 1 skill」が時間的に非有界

- **sed 実測**: 06:287「曝露 = 最大 1 skill（TERMINAL boundary まで）」、06:377 PT-13 同旨、05:778 OP-19（周期再評価は未裁定）、05:423 WAIT の liveness は heartbeat のみ、05 の `duration|lease_max` = 0 hit。A:151 `TerminationSpec = {declared_classes（非空 frozenset）, …}` — TIMEOUT は必須でない。
- **text**: 未反証。
- **logic**: 未反証。declared_classes = {SUCCESS, FAILURE} の skill（内部 timeout 無し）や WAIT skill は TERMINAL boundary に到達しないまま lease を保持でき、calibration 失効の検出点（次 permit 発行）に永久に到達しない。
- **impact**: 未反証（OP-19 が周期再評価を open 化しているため一部は既知）。残る欠陥 = B-M7 fold 文が「≤ 1 skill」を bound として提示する点。**MEDIUM**（WAIT は command を出さないが DIFF_IK skill では曝露が実在）。
- **minimal fix（frozen delta 0）**: `RuntimeTimeouts.lease_max_duration_s` を追加し 05 TimingBinding へ写像、超過 = TRANSFER_TO_SAFEHOLD(DEADLINE_MISS または新 reason) + fault；最低限、06:287 / PT-13 / 05 OP-19 を「≤ 1 skill・TerminationSpec が TIMEOUT を宣言しない限り時間非有界」と書き直し OPP 登録。

### CD-11（C10・MEDIUM・SHA 部分は反証）11_ record の不在・fold verdict の自己検証のみ

- **実測**: 06:4「verdict と処置の全体 = `11_THREE_AXIS_REVIEW_RECORD_20260903.md`」、06:445「全 finding の一覧・処置 = 11_」、06:474「v0.2.2 の実測出力は 11_」、05:825-864 各行「独立 verifier 未了・11_ §4」、05:866「fold しなかった finding … は 11_ に理由付きで列挙する」、04:68、08:113、10:51 — 参照 6 doc。`find $D -name '11_*'` = 0、`git ls-files` にも無し。**SHA256SUMS**: 現物は mtime 15:49:41 UTC で `sha256sum -c` 22/22 OK、04/05/06 の値は現 file と一致（`e85490d7…` / `6e57398d…` / `c0faa7bf…`）、HEAD `f3c6ca35` に commit 済・tree clean ⇒ reviewer の「v0.2.1 の sums のまま（mtime 01:51）」は測定時点の stale 観測で**本測定では不成立**。verdict 列「起草者再検証=CONFIRMED（独立 verifier 未了）」は本文が自ら明記（honest scope）。
- **text**: 11_ 不在 = 未反証（再現）。SHA 不一致 = 反証（現物一致）。
- **logic**: n/a（事実命題）。
- **impact**: 未反証。custody 目的の records artifact が存在しない file を verdict・機械検査出力・未 fold finding 一覧の所在として 6 doc から引く = records-must-match-fact 違反（CLAUDE.md §15 (b)・hard stop「引用先に根拠が存在しない」）。設計欠陥ではなく package 整合性欠陥。**MEDIUM**。
- **minimal fix**: `11_THREE_AXIS_REVIEW_RECORD_20260903.md` を package に作成する（内容 = 各 finding の verdict・fold 先・refuted 一覧・checker 出力）か、6 箇所の参照を「未作成（作成予定）」へ改める；06:4 / 05:4 の status 行に「v0.2.2 fold = 起草者自己検証のみ・独立 verifier pending」を追記。SHA256SUMS は再生成済み（追加処置不要）。

### CD-12（C1/C8・LOW）P_FRESHNESS_RELAXED の trigger が 05 と 06 で不一致

- **sed 実測**: 05:425「profile は有限値への強化のみ可（有限 → None は `P_FRESHNESS_RELAXED`・doc 06）」；06:134「None = 強化なし」、06:241「frozen None に対し … None = 強化なし」、06:286「`runtime_max_staleness_s` ← min(frozen, profile)（None 規則は §3）」。A:151 型のみ。
- **text/logic/impact**: いずれも未反証。frozen f = 0.5・profile None は 06 では受理（実効 0.5）、05:425 では `P_FRESHNESS_RELAXED`。同一 code の trigger が二重定義。運転影響は小（05 側が厳しい）。**LOW**。
- **minimal fix**: 05:425 を「profile None = 強化なし（実効 = frozen）；`P_FRESHNESS_RELAXED` iff p > f（06 が SSOT）」へ。

### CD-13（C3・LOW）INV-22 の列挙不足・05:357 の t_ack 注記

- **sed 実測**: 05:696 INV-22 は 6 timeout（ack validity / ack / permit / health / command / manager liveness）のみ；05:357-368 TimingBinding は 12 profile 由来 field；05:357「`ReadinessAck.valid_until = t_ack + ack_validity_s`」 vs 05:144 / 05:160「`t_receive + TimingBinding.ack_validity_s`（executor の t_ack は参考値）」。
- **text/logic/impact**: 未反証。`safety_heartbeat_timeout_s` を定数化しても INV-22 の字義に反しない。内部整合のみ。**LOW**。
- **minimal fix**: INV-22 を「全 `RuntimeTimeouts` field」へ；05:357 注記を `t_receive` へ。

### CD-14（C9・LOW）P_INTRINSIC_OVERRIDE が P_UNKNOWN_FIELD と重複・True 必須 bool

- **sed 実測**: 06:235「field が現れたら `P_INTRINSIC_OVERRIDE`（codec = unknown field）」 vs 06:367 PT-05「`P_UNKNOWN_FIELD`（strict codec）」；06:117 `requires_safety_layer_health: bool # True 必須`、06:165 `fail_closed: bool # True 必須`。A:385 unknown field 拒否は codec の単一結果。
- **text/logic/impact**: 未反証。同一事象に 2 code。strict codec が先に拒否するため `P_INTRINSIC_OVERRIDE` は到達不能（CD-05 (a) の null-binding 上の非 null timing 値に再定義すれば有用）。bool は False で拒否されるため無害だが「拒否されるためだけの knob」。**LOW**。
- **minimal fix**: `P_INTRINSIC_OVERRIDE` を CD-05 (a) の場合に再定義するか削除；bool 2 つは削除するか保持理由（負の対照 PT-06）を注記。

### CD-15（C3・LOW）profile_label が hash preimage にあり「pin ではない」が誤解を招く

- **sed 実測**: 06:204「人間向け label（pin ではない — content sha で引く）」、06:220 `profile_hash = H_WCJ(IndustrialDeploymentProfile)`（label を含む）、06:225「新 canonicalization 規則は足さない」、06:142/242 CELL_COMMISSIONING record は profile_hash を束縛。
- **text/logic/impact**: 未反証。1 文字の label 変更 ⇒ 新 hash ⇒ `P_RESOURCE_UNATTESTED` / `P_EVIDENCE_UNBOUND`（全 evidence 再取得）。content-addressing の帰結として意図的とも読めるが、注記が hash 可視性を隠す。**LOW**。
- **minimal fix**: 06:204 注記を「label 変更 = identity event（evidence 再取得を要する）」へ；label を preimage から外す案は §2.3 と衝突するため OPP 登録。

### CD-16（C5・LOW）§6.3 の join key に lease_id があるが record に無い

- **sed 実測**: 06:342「両者は `profile_hash` と `lease_id` で join する」；06:300-309 `DeploymentEvidenceRecord` の field に `lease_id` 無し（06 の `lease_id` 出現 = 342 のみ）。05:650-664 `RuntimeAuditRecord` は `lease_id` / `profile_hash` を持つ。
- **text/logic/impact**: 未反証。HEALTH_CHECK_RUN / FAULT_INJECTION_RESULT がそれを生んだ audit trail に辿れない。**LOW**。
- **minimal fix**: §6.1 に「HEALTH_CHECK_RUN / FAULT_INJECTION_RESULT の `artifact_ref` は RuntimeAuditRecord の seq 範囲 + lease_id に解決すること」を明記（新型なし）。

### CD-17（C3/C10・LOW）「登録」（registry・受理権限・受理済 hash のみ解決）が未定義・OPP 不在

- **sed 実測**: 06:142「登録時に evidence store を照会」、06:357「profile の受理（`profile_hash` の登録）」、06:371 PT-09「登録」— registry / 受理者の定義無し；05:184 は `profile_hash` を解決するだけで accepted set 前提無し（05 に `accepted_profile|登録` = 0 hit）；06 §10 OPP-1〜10 に registry 項目無し（OPP-9 は evaluator registry のみ）。06:58「採否 = two-key + Rs」は doc 自体の採否。
- **text/logic/impact**: 未反証。governance の open point 未登録。**LOW**。
- **minimal fix**: OPP-11（profile registry・受理権限・two-key or operator）を追加；05 §3.4 前提に `profile_hash ∈ accepted_profiles`。

### CD-18（C2・LOW）DEPLOYMENT_WORKSPACE が FK 後の点評価のみ・hold 経路と tool 幾何が未カバー

- **sed 実測**: 05:432「CommandGateway（FK 後）」、06:273 workspace 項、06:93-98 ToolPayloadSpec に collision hull 無し、05:422 hold は連続 admitted command 間に適用（LINEAR_INTERPOLATE）。B:65-66 command = EE target。06 に `hull|collision|経路|segment` = 0 hit（boundary_dwell_s 等の無関係 hit を除く）。
- **text/logic/impact**: 未反証。現在 EE と目標がともに keep-out 外で補間経路が keep-out を横切る場合、点評価は admit。連続幾何安全は ISL の領域（D0:315 near-contact / penetration）とも読めるが本文は決めていない。**LOW**。
- **minimal fix**: 06 §4 に「本項は点評価（FK 後の目標）であり経路・tool 幾何の安全は ISL に委譲する」か「区間（実測 EE → 目標）+ 宣言 tool hull で評価する」を明記；未決なら OPP 登録。

## 3. 陽性対照との照合

- 本 group の 18 件はいずれも 10_ の注入欠陥（PC-05-A〜D・PC-06-E〜H）と一致しない。CD-14 が触れる `requires_safety_layer_health` は実 doc 06:117 で True 必須のままであり、PC-06-G（False 可）の再報告ではない。

## 4. 本 group の verdict（08 §5 語彙）

- HIGH 未反証 = CD-01 / CD-03 / CD-04（3 件）⇒ **HOLD**（fold 案は全て schema delta 0 で明確ゆえ、fold 後は PASS-WITH-CONDITIONS へ移行可能）。
- fold_status = NOT_APPLICABLE（本 group は未 fold）。frozen 4 file は不変（pin 11/11 PASS・本 verifier は `$D` 配下を編集していない）。

— 2026-09-04 20:39 UTC（`date -u` 実測）
