# V9 verification — GPT-astra（GA-01..GA-07）findings against WMSO v0.2.8 REVIEW CANDIDATE（3 lens・adversarial・refute-first）

- verifier = V9（別 context・別 AI family の外部 reviewer の finding を検証・authority 無し・凍結物へ非接触・`eval_runs/` 未編集・書込み先 = `$OUT` のみ）; 実施 = 2026-09-06 08:13–08:3x UTC（`date -u` 実測: 開始 08:13:33 / pin 再測・checker 08:13:40–08:13:42 / 全文読了 08:14–08:25 / artifact 保存 08:26:34 / JSON 書込 08:30:29 / 本報告 Write 直前 08:30:40）; 対象 commit = HEAD `8078c92b`（branch `claude/v0-2-design-candidates-snqbmy`）。05 / 06 は reviewer の対象 commit `0c6f11c9` と **git blob 同一**（05 `ce176b4359e1364c0b71abc0da640e3f2fdaf86f` / 06 `b796830db84a43136584820f3c1525626fc6cef2` — `git rev-parse 0c6f11c9:…` と `HEAD:…` が一致）
- 対象 finding = `$C/01_findings.json`（7 件・GA-01..GA-07・4 HIGH / 3 MEDIUM・verdict HOLD・全件読了）+ `$C/00_REVIEW_JA.md`（報告・全読）+ `$C/05_CLAUDE_HANDOFF.md` + `$C/02_read_scope.json`。`$C/03_guard_witnesses.py` / `04_guard_witness_results.json` は reviewer 自作の toy model（反例の意図を読むためだけに参照・**evidence として用いない**・全 verdict は候補 text から再構成）。対象 doc = `$PKG/05_…`（v0.2.8・1082 行・sha256 `2e508b4e…10da` = SHA256SUMS 一致）／ `$PKG/06_…`（v0.2.8・773 行・sha256 `45212f20…aa49` = 一致）
- 略号: `$PKG` = `eval_runs/troot_optE_dapg_wholeroute_scope_20260701/WMSO_EXACT_BASELINE_REVIEW_v0.2_20260903`、`$D` = `eval_runs/troot_optE_dapg_wholeroute_scope_20260701`、`$C` = `…/scratchpad/consult/gptastra_v028/WMSO_REVIEW_v0.2.8_20260906`、`$OUT` = `/tmp/claude-0/-home-user-IsaacLab/f88a4426-3d65-5cc2-ba28-87f4976e6a66/scratchpad/review6`、A = `$D/WMSO_D11A_CONTRACTS_V2_DESIGN_RSTECHLEAD2_20260719.md`、D0 = `$D/WMSO_D0_ARCHITECTURE_DRAFT_RSTECHLEAD2_20260718.md`
- 拘束: 各 finding に text / logic / impact の 3 lens を独立に当て、**反証を試みる**（既定 `refuted = true`）。confirmed ⇔ 2 lens 以上が反証できず impact ≠ NOT_A_DEFECT。Rs 裁定（`11_` §8・`A_GPT56sol`: B+ / 外部 timing baseline / registry + acceptance record / OP-19 三層）の**設計方向は欠陥ではない**。`11_` §10.1 / §11.1 の起草既定（head-check (c)(d) = HOLD・`P_VALIDITY_UNBOUNDED` の kind 集合・`base_pose_tolerance_*` / `max_validity_s` の値・identity 系 item 必須化・role registry head の ACL・`COMMAND_ADMITTED` 同期 block・TPI の kind 集合・base pose frame 規約・PROPOSED / authority head の registry 意味論・MIN_FAULT_INJECTION の trigger 選択・**link 断後の HOLD 必須化（R7-18）**・**不読時 (iii) 常時 SafeStop（R7-04）**）への単なる反対は impact lens で refute する。HIGH の確定条件 = text から再現可能 ∧ fail-closed 保証 / RS71 §0 不変前提の束縛 / 凍結 delta 0 bar のいずれかを破る。陽性対照の注入 8 欠陥（`$PKG/10_*` §1）は実体に無い（本 verify でも該当 finding 無し）。

## 0. 手続記録（自己実測・`date -u` 順）

```text
$ date -u                                            → Sun Sep  6 08:13:33 UTC 2026
$ git log -1 --oneline                               → 8078c92b WMSO: re-issue the external review request pinned to v0.2.8
$ git branch --show-current                          → claude/v0-2-design-candidates-snqbmy
$ git rev-parse 0c6f11c9:$PKG/05_…  /  HEAD:$PKG/05_… → ce176b4359e1364c0b71abc0da640e3f2fdaf86f（両方・一致）
$ git rev-parse 0c6f11c9:$PKG/06_…  /  HEAD:$PKG/06_… → b796830db84a43136584820f3c1525626fc6cef2（両方・一致）
$ sha256sum 05 06 ↔ $PKG/SHA256SUMS.txt              → 05 2e508b4e6a689b2dc14fb094d5c875efb1dc660dd301e2b157a733bc433010da = 一致
                                                        06 45212f203f4b95f0dc6d28e5bd805c077e4ea64faaa356f46c65ac1dba8baa49 = 一致
$ wc -l 05 06                                        → 1082 / 773（reviewer 宣言と一致）
$ date -u; bash $PKG/verify_exact_baseline_pins.sh   → 08:13:40 UTC / 11/11 PASS（grep -c ^PASS = 11・^FAIL = 0）
    PASS D1.1-A DESIGN v2.11.2 00192d20…8aff / PASS EP v1.9 md c474acea…27e7 / PASS EP v1.9 json e63176af…1c6e /
    PASS D1.1-B DESIGN v13 5a1874d3…49a6 / PASS blob 1353430… / PASS blob ebe8154… / PASS blob sha256 ×2 /
    PASS at frozen commit 54f90a7d / PASS at frozen commit 07250f4a / PASS evidence_policy_definition_hash e7ca4309…3803
    policy_semver=1.9.0  HEAD=8078c92bb19cd647f0b5eb7e704dbf3e0a2e597b（原本 = $OUT/pins_V9.txt）
$ python3 $PKG/check_review_candidate.py /home/user/IsaacLab/$PKG/05_… --runtime
                                                     → citations=153 FAIL=0 WARN=11（全 C2 heuristic・行 88/98/98/146/234/554/556/838/1076/1076 = 05 §14 anchor 23 の分類どおり。原本 = $OUT/checker_05_V9.txt）
$ python3 $PKG/check_review_candidate.py /home/user/IsaacLab/$PKG/06_… --profile
                                                     → citations=65 FAIL=0 WARN=26（全 C2 heuristic。原本 = $OUT/checker_06_V9.txt）→ 08:13:42 UTC
$ cat $C/01_findings.json / 00_REVIEW_JA.md / 05_CLAUDE_HANDOFF.md / 02_read_scope.json  → 全読（7 finding・verdict HOLD・reviewer 自身は pin 再測 / checker / 盲検対照 未実施と明記）
$ cat -n 05（1-140 / 140-300 / 300-380 / 380-460 / 460-540 / 540-620 / 620-700 / 700-790 / 790-870 / 870-960 / 960-1082）→ 全 1082 行読了
$ cat -n 06（1-110 / 110-200 / 200-290 / 290-400 / 400-470 / 470-560 / 560-640 / 640-773）→ 全 773 行読了（08:14–08:25 UTC）
$ sed -n 140,165p / 295,330p / 340,390p / 470,510p / 535,540p / 118,139p / 214,222p A；grep -n brev|skill_definition_hash|certified|E_REGISTRY_DUPLICATE A
$ sed -n 305,325p D0；sed -n 14,34p RS71 §0
$ sed -n 214,222p / 329,347p / 407,425p $PKG/11_THREE_AXIS_REVIEW_RECORD_20260903.md（§8 / §10.1 / §11.1）
$ sed -n 9,14p / 43,44p + grep $PKG/review_records/rs_consult/A_GPT56sol_20260904.md（裁定の逐語）
$ sed -n 1,75p + §3 $PKG/review_records/VERIFY_V7_v027_20260903.md（様式のみ）
$ head -150 $C/03_guard_witnesses.py（toy model の意図のみ）
$ grep -n 05/06: last_disposition|ラッチ|latch / pending_invalidate / (i′) / certificate|definition_c|new_invocation|H_WCJ / skill_definition_hash /
    ttl|t_obs|runtime_max_staleness_s|鮮度 / cell_identity_hash|cell_id|site_id / supersedes_record_hash / INV-02|両 kind|ちょうど +1 / CLOCK_DRIFT|drift|許容
$ date -u → 08:26:34 UTC；pin / checker 出力を $OUT/{pins_V9,checker_05_V9,checker_06_V9}.txt に保存；git status --short eval_runs/ → 空（実 file 無変更）
$ date -u → 08:30:29 UTC；V9_results_GA_v028.json 書込 → json.load OK / list / 7 entry / id 一意・順序 GA-01..07 / schema key 11 個一致 /
    confirmed 7・refuted 0・merged 0・severity {HIGH 3, MEDIUM 3, LOW 1}・premise_accurate 7/7
$ date -u → 08:30:40 UTC（本 file の Write 直前に実測）；本報告書込
```

## 1. 集計

| 区分 | 件数 | id |
|---|---|---|
| CONFIRMED / HIGH | 3 | GA-01 / GA-02 / GA-03 |
| CONFIRMED / MEDIUM | 3 | GA-04（HIGH → MEDIUM）/ GA-05 / GA-07 |
| CONFIRMED / LOW | 1 | GA-06（MEDIUM → LOW） |
| REFUTED（NOT_A_DEFECT） | 0 | — |
| MERGED（GA 内重複） | 0 | — |
| severity 変更 | 2 | GA-04（兄弟停止の key 欠陥は多層に緩和され live ISL の迂回ではない — reviewer 自認）、GA-06（本文が epoch 不変で一貫し安全側の解が一意・文言のみ） |
| reviewer の fix を置換 / 補正 | 4 | GA-01（latch の解除条件を「manager の SAFETY CAS 線形化の観測」に固定 + gateway の報告義務を追加）、GA-02（新 code を足さず既存 `R_INVOCATION_INVALID` / `R_AUTHORITY_DECISION_MISBOUND`・definition_c の解決規則を追加）、GA-04（安定 id = 既存 `(site_id, cell_id)`・record に複写・custody は Rs OPP 新設）、GA-07（`clock_drift_tolerance_s` を baseline に置き比較量を (wall_now, mono_now) 記録対で定義） |
| premise 事実照合 | 7/7 正確 | 行番号ずれ 2 件（GA-04 06:531 → :532、GA-05 06:529 → :530・各 1 行・主張に影響なし） |

CRITICAL = 0。全 7 件の minimal fix は凍結 4 file への schema delta 0（runtime / deployment 層の型・散文のみ）。round 1→5 の HIGH = 5 → 3 → 5 → 2 → 0 に対し、本 round（外部・別 family）= reviewer 4 → verifier 3。3 件の HIGH はいずれも「別所に規則がある」grep で反証できず、text から 1 遷移 / 1 時刻順序で再現でき、本 doc 自身が宣言する fail-closed 保証（INV-29 / §5.4 clearance・certificate-first / INV-15・frozen FreshnessPolicy at start）を機構が実現していない class。Rs 裁定の設計方向・§10.1 / §11.1 起草既定に反対する finding は無かった（GA-01 の R7-04 起草既定との関係は §2 に明記）。**verifier 追加の反例 2 件**（GA-01 の (ii) 再開経路・GA-02 の未登録 definition 変種）は reviewer の主張を強める方向で、reviewer の記載範囲は別に評価した（premise 表）。

## 2. Finding 別 verdict

### GA-01（HIGH・CONFIRMED）— gateway が自ら出した (i′) SafeStop を保持する局所 latch が無く、manager 不読中に heartbeat が戻ると解除なしに SafeHold へ緩む（verifier 追加: manager が読める場合は (ii) が再開し得る）
- **sed 実測**: 05:82 gateway 状態 =「… `last_disposition`（直近に成功した線形化読みで観測した `safehold_disposition`・v0.2.8 R7-04）」／ 05:518「(i′) else if IndependentSafetyLayer の heartbeat が safety_heartbeat_timeout_s を超えて欠落 → SafeStop（(ii) を評価しない）」／ 05:519「(ii) else if owner == EXECUTOR ∧ ¬state.boundary_wait ∧ ¬restart_pending ∧ ¬pending_invalidate ∧ mode == CLOSED_LOOP_AUTHORITY ∧ 直近 admitted command あり → その setpoint」／ 05:520「(iii) … 線形化読みが `manager_liveness_s` 内に完了しない場合は gateway 局所の `last_disposition == SAFE_STOP` なら SafeStop・それ以外 SafeHold」／ 05:249「safety 起因（SAFETY / SAFE_STOP）の CONFLICT では §5.2 (i) / (i′) / (iii) に既に落ちている。非 safety 起因（…）では、CommandGateway は … `pending_invalidate` を立てて (iii) に落ち」・同「(iii) は `safehold_reason == SAFETY` でも … 記録 disposition が SAFE_STOP なら §5.4 SAFETY 行の clearance record が記録されるまで SafeStop を出す（heartbeat が戻り (i′) が外れても SafeHold に緩まない）」／ 05:327 R_MANAGER_UNAVAILABLE 行「読みが `command_deadline_s` 内に回復すれば (ii) を再開（CAS なし）；超過は `R_DEADLINE_MISS` 行（`pending_invalidate` 経由）」／ 05:739 INV-29「heartbeat が … 欠落した線形化点以降、(ii) は評価されず出力 = SafeStop、かつ TRANSFER_TO_SAFEHOLD(SAFETY) が発行される」／ 05:795 T-44「SAFE_STOP disposition の SAFEHOLD 中に manager との link 断 → 出力は SafeStop のまま」／ 06:287 `safety_heartbeat_timeout_s ≤ command_deadline_s`／ D0:318「missed heartbeat ⇒ consumers must not assume safety alive; monitor failed ⇒ system fails to safe-stop」。
- **text lens（未反証）**: grep `ラッチ|latch` = 0 hit。gateway が (i′) を出した事実を保持する状態は `last_disposition`（読みの結果）にも `pending_invalidate`（非 safety 起因に限定・05:249/734）にも無い。05:249 の「緩まない」は manager の SAFEHOLD(SAFETY, SAFE_STOP) record を**読めた場合**にしか成立しない文で、不読枝を覆っていない。(i′) は現在形の条件（欠落している間）で、INV-29 の「以降」は機構で担保されていない。
- **logic lens（未反証）**: [reviewer scope] 直近読み = EXECUTOR（05:139 により `safehold_disposition = None` → `last_disposition = None`）→ manager link 断 → ISL heartbeat 欠落 → (i′) SafeStop → heartbeat 復帰・manager 依然不読 → (i) 未解除決定なし・(i′) 偽・(ii) は読み不能 → (iii): `last_disposition ≠ SAFE_STOP` → **SafeHold**。解除 record も新 state 読みも介在しない。[verifier 追加] manager が読める・state = EXECUTOR のまま（manager が欠落を独立観測していない partition、または SAFETY CAS が未線形化）・欠落継続時間 Δ が `safety_heartbeat_timeout_s < Δ < command_deadline_s − (t0 − t_last_admit)`（06:287 の順序により非空）→ deadline miss 未発生・`pending_invalidate` = False → heartbeat 復帰の瞬間に (ii) の全項が真（直近 admitted setpoint は §4.2 の ZOH 保持中）→ executor command の admit **再開**。INV-29 と §5.4 SAFETY 行（ISL clearance 必須）に反する。反証候補 =「manager は必ず同じ heartbeat を観測し即 CAS する」— 05 は heartbeat の consumer / 経路を規定せず（§2 ISL 行・05:339 検出者 = gateway / manager）、CAS の線形化には retry 窓（INV-24）があるため反証にならない。
- **impact lens（未反証）**: 本 doc が SAFE_STOP > HOLD を順序付け（INV-30・05:249）、SAFETY 由来の解除に clearance を要求（05:537-538・条件 10）する fail-closed 保証が、gateway 起点の stop で機構的に破れる。R7-04（v0.2.8 fold）の残差 — T-44 は「既読の SAFE_STOP disposition」だけを試験。起草既定 R7-04（「不読なら常に SafeStop」）は不読枝の症状だけを塞ぎ、(ii) 再開経路には効かない → 裁定 / 既定への反対ではない。凍結違反ではないが INV-29 / §5.4 の fail-closed 保証の欠落 → **HIGH**（reviewer 記載の SafeStop→SafeHold だけなら非運動出力間の緩和で MEDIUM 相当。HIGH の根拠 = 同じ latch 欠落が開く (ii) 再開経路）。
- **verdict**: CONFIRMED / HIGH。
- **minimal fix（frozen delta 0・05 散文のみ）**: (1) §2 gateway 状態に局所 `stop_latch: bool`（durable 不要）— gateway 自身が (i′) で SafeStop を出した時点で True；(2) §5.2 (i′) →「heartbeat 欠落 ∨ stop_latch → SafeStop（(ii) を評価しない）」、解除 = 線形化読みで **seq > latch 時観測 seq の SAFEHOLD state** を観測したときのみ（= manager の TRANSFER_TO_SAFEHOLD(SAFETY) が線形化済）。以後は既存の (iii) 規則（disposition SAFE_STOP は §5.4 clearance まで SafeStop）が担う；(3) gateway は (i′) 観測を manager へ報告する義務（R_MANAGER_UNAVAILABLE 行の outage 報告と同形）— manager が heartbeat を独立観測していない場合の SAFETY CAS の起点；(4) INV-29 に「(ii) は stop_latch 解除まで評価されない」、INV-19 / 失敗表 R_MANAGER_UNAVAILABLE 行の (iii) を「max(last_disposition, stop_latch)」に；(5) T-44 の対 case: (a) link 断中に heartbeat 欠落→復帰（link 依然断）→ SafeStop 維持、(b) heartbeat 欠落→`command_deadline_s` 内に復帰・manager state = EXECUTOR → SAFETY CAS 観測まで (ii) 不再開。AuthorityManager の唯一 writer 権限・「不読なら常に SafeStop」の採否（R7-04 Rs 項目）は不変。⚠ reviewer 案「解除は対象を束縛した正規の解除確認後のみ」は gateway に clearance record の照合（registry 読み）を求めるため過大 — manager の SAFEHOLD 線形化の観測を解除条件にすれば既存 §5.4 で足りる。
- **premise 正確性**: 正確（05:82 / :249 / :518 / :520・D0:318-320 とも現行 text に実在し要約どおり。「pending_invalidate も (iii) へ送るだけで停止強度を保存しない」= 05:519/249 のとおり）。

### GA-02（HIGH・CONFIRMED）— consumer の certificate を SkillActionId だけで照合し、実際に検証・実行する definition 物の SkillDefinitionHash を certificate / lease / invocation に束縛していない
- **sed 実測**: 05:234 条件 6「… certificate（certificate-first `A:323`・`certificate.skill_action_id` `A:306`）の `skill_action_id == proposed_lease.skill_action_id`」／ 05:236 条件 8「`proposed_lease.skill_action_id` が certified（certificate 存在）」／ 05:423「`skill_definition_hash: str`」（ActiveAuthorityLease・comment 無し）— grep `skill_definition_hash` 05 = **この 1 hit のみ**／ 05:567 B4「validate_invocation_start(new_invocation(c, control_epoch = …), definition_c, belief, now)」（definition_c の出所・`new_invocation` の hash 設定規則なし）／ 05:725 INV-15「`lease.skill_action_id` に対する `ContractCertificate` が存在」／ 05:191 (g) = skill_action_id + tensor_binding_hash + control_mode／ A:158「SkillActionId = H_WCJ({"ns", "skill", "bundle", "variant", "brev"})」・A:159「SkillDefinitionHash = H_WCJ(SkillDefinition 全体)」／ A:218「BehaviorSignature = … {identity 入力群, ExecutionBundle, TrainingProvenance, ExecutionProvenance, 記録専用 field} を除く全て」／ A:219「registry 登録時、同 key の既存 entry と BehaviorSignature 不一致 → E_BEHAVIOR_REVISION_STALE … ActionId は不変のまま」／ A:306 ContractCertificate =「skill_definition_hash: str; skill_action_id: str」／ A:381 validate_handoff「`offer.producer_definition_hash == invocation.skill_definition_hash == H_WCJ(producer_definition)`」／ A:481 SkillInvocation.skill_definition_hash／ A:380 validate_invocation_start = initiation / freshness / required ⊆ offered（definition hash 照合の記載なし）。
- **text lens（未反証）**: certificate ↔ lease ↔ invocation ↔ definition 物の結線は全て ActionId 単位。lease の `skill_definition_hash` は宣言だけで、条件 1-12・(a)-(m)・INV-*・T-* のどこにも等値が無い。凍結 validate_handoff が producer 側に置く 3 重等値（A:381）の consumer 側対応物は凍結にも 05 にも無い（凍結側の欠落は本 doc の対象外だが、runtime 束縛を担う 05 が写していない点が欠陥）。
- **logic lens（未反証）**: D_B := D_A の `training_provenance` のみ変更。ExecutionBundle 同一 → ActionId 同一（A:158）、BehaviorSignature 同一（A:218 で provenance は除外）→ registry 不変量は拒否しない（A:219）、SkillDefinitionHash は相違（A:159）。B4 が definition_c = D_B で validate → 通過（挙動 field 同一）。条件 6/8/INV-15 は cert_A（H(D_A)・ActionId X）で真。lease.skill_definition_hash = H(D_B) が無束縛のまま活性化。hash 衝突を仮定しない。[verifier 追加] D_B を**未登録**にすれば BehaviorSignature 面（initiation_spec / freshness_policy / required_control_resources）を緩めても ActionId は不変で同経路を通り、緩めた述語で validate_invocation_start が評価される（A:219 は登録境界の検査で未登録物に及ばない）。反証候補 =「definition_c は registry から ActionId で引くから certified 物である」— 05 に規定が無く、同 ActionId の複数 entry（provenance 相違）の解決規則も無い。
- **impact lens（未反証）**: 凍結の certificate-first（A:323「未認証 definition の eligibility 評価を型で不可能に」）・certified SkillActionId のみ（A:537・05:74）・05 §4.1「完全に束縛」の趣旨に対し、実際に検証・実行される definition 物が certificate / AuthorityDecision の対象（H(D_A)）と結ばれない。CD-04 / R1-05 / R6-01（名前参照のみで内容 hash 束縛が無い = HIGH）と同 class。凍結 delta 0 で修正可 → **HIGH**（provenance 相違のみなら audit / provenance 整合の欠陥で MEDIUM 相当。HIGH の根拠 = 束縛欠落が未登録 definition 物の挙動 field 差替えも通す点）。
- **verdict**: CONFIRMED / HIGH。
- **minimal fix（frozen delta 0・05 のみ）**: (1) 条件 8 →「`cert = certificate(proposed_lease.skill_action_id)` が存在 ∧ `cert.skill_definition_hash == proposed_lease.skill_definition_hash == invocation.skill_definition_hash == H_WCJ(definition_c)`」（definition_c = B4 で validator に渡し executor が実行する物）。不一致 = REJECTED・既存 `R_INVOCATION_INVALID`；(2) 条件 6 の certificate 節 →「decision の eligibility_report が依拠する certificate が (1) の cert と同一（skill_definition_hash 等値）」。不一致 = 既存 `R_AUTHORITY_DECISION_MISBOUND`；(3) §6.2 B4 に「definition_c = `certificate.skill_definition_hash` と H_WCJ が一致する登録 definition（同 ActionId の複数 entry は certificate hash で解決・解決不能 = R_INVOCATION_INVALID）」、`new_invocation` は `skill_definition_hash := H_WCJ(definition_c)`；(4) §3.4 permit 発行前提に同じ等値、InitializationRecord（runtime 型）に `definition_hash_used` を追加し report ref を (invocation_id, definition_hash_used, now_used) に束縛；(5) INV-15 →「certificate 存在 ∧ cert.skill_definition_hash == lease.skill_definition_hash」、INV-14 の definition を同物に固定；(6) T: 同 ActionId・異 DefinitionHash の D_B で lease 提案 → 条件 8 REJECTED。frozen certificate / report への field 追加なし（reviewer と同じ方針・code は新設せず既存 2 code）。
- **premise 正確性**: 正確（05:233-236 / 05:414-442〔lease block = :416-436〕/ 05:565-571、A:157-160 / :306 / :372-382 とも実在。「hash 衝突を仮定した反例ではない」= A:158/:218 で確認）。

### GA-03（HIGH・CONFIRMED）— 開始条件・鮮度の検証（B4）から CAS までの待機中に BeliefRef が失効しても、過去の成功 report で lease が commit される
- **sed 実測**: 05:563 B2「belief := 現在の BeliefRef … stale/OOD（ttl 超過・ood_flag）= RE_OBSERVE」／ 05:567 B4「validate_invocation_start(…, belief, now) … now 明示」・05:569「鮮度 = runtime_max_staleness_s（profile 強化後の値）で再判定」／ 05:571「全て通過 → ReadinessAck 要求（ack_timeout_s）→ CommitPermit 要求 → AuthorityCas」／ 05:188「effective_expires_at = min(expires_at, ack.valid_until, validity_deadline_mono, [decision.t_mono + decision_max_age_s])」／ 05:233 条件 5「… `permit.invocation_validation_report_ref` が issues 空であり」／ 05:409 InitializationRecord.now_used（lease 記録）／ 05:824 OP-7「§4.2 は boundary の鮮度のみ」／ A:369-372 LifecycleValidationReport = {invocation_id, issues, validator_artifact_hash}（評価時刻を運ばない）／ A:151 BeliefRef = {value, t_obs, ttl, confidence, ood_flag}・FreshnessPolicy = {max_staleness_s}・FailClosedAction ∋ RE_OBSERVE／ 06:287 P_TIMEOUT_ORDER（permit_ttl_s / ack_* ≤ boundary_dwell_s — staleness 項なし）。
- **text lens（未反証）**: grep `ttl|t_obs|runtime_max_staleness_s|鮮度` の非履歴 hit は §1 表・§4.2・B2・B4・OP-7 のみ。permit の期限項にも CAS 条件 1-12 にも belief 期限が無い。report 型が時刻を持たないため CAS は「いつの now で通ったか」を検査できない。
- **logic lens（未反証）**: 時刻順序 `t_validate < t_obs + min(ttl, runtime_max_staleness_s) ≤ t_CAS < effective_expires_at`。owner / epoch（条件 1）・permit（2）・ACK（3）・offer（4）・report issues（5）・decision age（6）・envelope（7）・certified（8）・ISL（9）・clearance（10）・boundary guard（11）・registry（12）が全て不変なら CAS SUCCESS。executor は失効 belief で validate された initiation 述語の下で起動し、`InitializationRecord.start_belief_ref` に失効 belief が記録される。反証候補 = `ack_validity_s` / `permit_ttl_s` が短ければ実害なし — profile 値は staleness と無関係に設定可能（06:287）で規則にならない。
- **impact lens（未反証）**: frozen FreshnessPolicy / BeliefRef.ttl / RE_OBSERVE が守る「開始時の鮮度」という fail-closed 入力が commit 点で非拘束。Rs 裁定 OPP-11 の原則（`A_GPT56sol`:11「permit 発行時には有効でも、permit 消費時には失効している可能性」→ effective_expires_at + CAS 再検査）を ACK / decision / validity に適用しながら belief に適用していない一貫性の欠落 — 裁定の設計方向への反対ではなく、その未実現。曝露は `boundary_dwell_s` で有界だが staleness 上限と結ばれていない。CD-03（失効の未執行 = HIGH）と同 class → **HIGH**（executor の live obs と ISL が二次防護であること・修正が 1 項追加であることは付記 — Rs が MEDIUM と読み替える余地はある）。
- **verdict**: CONFIRMED / HIGH。
- **minimal fix（frozen delta 0・05 のみ）**: (1) §3.4 CommitPermit に `start_belief_ref: BeliefRef`（B4 の belief・frozen 型のまま）と `belief_valid_until_mono = t_obs + min(ttl, runtime_max_staleness_s〔None なら ttl のみ〕)`（validate 時に manager clock で確定）を追加し `effective_expires_at` の min に含める；(2) 条件 2 の fault 優先規則に「belief 項到達 = 既存 `R_INVOCATION_INVALID`・disposition RE_OBSERVE（失敗表 05:313 行）→ B2 から再観測・再検証・再 ACK・再 permit」；(3) 条件 5 →「report が (start_belief_ref, now_used, H_WCJ(definition_c)) に束縛され now < permit.belief_valid_until_mono」；(4) InitializationRecord.start_belief_ref / now_used は permit から複写（同一物を INV に）；(5) INV-35 に belief 項、T: `t_validate < t_belief_expiry ≤ t_CAS < t_effective_expiry` で REJECTED。⚠ 06 P_TIMEOUT_ORDER に `permit_ttl_s ≤ runtime_max_staleness_s` を足す案は不採用（skill ごとの値で profile 順序に写せず、CAS 再検査で足りる）。mid-skill の鮮度執行（OP-7）は変えない（reviewer と同意）。
- **premise 正確性**: 正確（05:183-191 / :230-234 / :558-577、A:151 / :372-380 とも実在。「05:188 … BeliefRef の TTL / 開始判断の鮮度期限を含まない」= 正）。

### GA-04（MEDIUM・CONFIRMED・HIGH から降格）— 「cell 単位の停止」が構成 hash `cell_identity_hash` で対象を切り、同一物理 cell の異構成 profile に伝播しない
- **sed 実測**: 05:191 (i)「**cell 単位の停止（v0.2.6・R3-09）**: 同一 `cell_identity_hash` を持つ他 profile の head record が `state ∈ {SUSPENDED, REVOKED}` … なら本 profile も `R_PROFILE_SUSPENDED`」／ 05:240 条件 12「同一 cell_identity_hash の他 profile の head record に state ∈ {SUSPENDED, REVOKED} が無い」／ 05:292 head-check (a)「同一 `cell_identity_hash` を持つ他 profile の head record が state ∈ {SUSPENDED, REVOKED}」／ 06:507「cell_identity_hash: str  # CellIdentity（layout_revision_ref・kinematic_layout を含む）の hash」／ 06:538「`cell_identity_hash ≠ H_WCJ(profile.cell)`（R4-09）」／ 06:532「cell 単位の trigger は同一 `cell_identity_hash` の全 record を停止する（R3-09）— 同一 append batch」／ 06:77-96 CellIdentity = {cell_id, site_id, robot_model_ref, base_frame_ref, kinematic_layout, arms: tuple[ArmSpec, …], safety_layer_ref, layout_revision_ref}／ 06:259「tuple → array」／ 06:303 P_ARM_COVERAGE（順序非依存）／ `cell_id` の用途 = baseline registry の head(cell_id)（05:88/191/240/292・06:536）と baseline.cell_id 照合（06:517/288）のみ／ `site_id` = 06:80 field のみ／ 05:143「cell は複数 profile を走らせ得る（OPP-8・PT-14）」／ 05:543 clearance record「record.profile_hash == permit.profile_hash」。
- **text lens（未反証）**: 兄弟述語の 4 箇所全てが構成 hash。物理 cell を表す既存 field（`site_id`, `cell_id`）は兄弟停止に使われていない。「cell 単位」という本 doc 自身の語と機構が一致しない。
- **logic lens（未反証）**: 同 site_id / cell_id で `arms` の列順だけ異なる A / B → tuple→array で異 hash、P_ARM_COVERAGE / P_ARM_CAPABILITY_MISSING / P_BASE_LAYOUT_MISMATCH は順序非依存で両方 ACCEPTED 可。A に INCIDENT の SUSPENDED（+ 同 hash batch）→ B の (i) / 条件 12 / head-check (a) の兄弟節は不成立。[verifier 補足] 列順は敵対的だが、OPP-8 が想定する正当な同 cell 複数 profile（例: `arms[].controller` の上限が異なる保守的 / 高速 profile）は必然的に異 hash になり、非敵対的に同じ欠落が生じる。
- **impact lens（部分反証 → 降格）**: 宣言した規則（R3-09）を一貫して実現していない点は反証できない。しかし多層の緩和がある: INCIDENT / ISL_ANOMALY / CLOCK_ANOMALY は SAFE_STOP（06:346）で、B の permit にも §5.4 SAFE_STOP 行の clearance（05:543・profile_hash 単位・人間の role）を CAS 条件 10 が要求する；IDENTITY_CHANGE は B 自身の BEFORE_PERMIT identity check（06:68/291）が拒否；LAYOUT_CHANGE / BASELINE_SUPERSEDED は head(cell_id)（06:536）経由で全 profile に及ぶ。残る経路 = HOLD 級の OPERATOR / DIAGNOSTIC_FAILURE 起因 SUSPENDED が兄弟へ伝播しない点。reviewer 自身が「live SafetyDecision の迂回ではない」と限定。fail-closed の主経路ではなく v0.2.6 MEDIUM 機構（R3-09）の対象 key の欠陥 → **MEDIUM**。安定物理 cell id の custody（一意性・改名 / 別名）は Rs 専権。
- **verdict**: CONFIRMED / MEDIUM。
- **minimal fix（frozen delta 0・05 / 06 deployment 層のみ）**: (1) 兄弟述語の key を `cell_identity_hash` から **`(profile.cell.site_id, profile.cell.cell_id)`** に変更（05:191 (i)・05:240 条件 12・05:292 head-check (a)・06:532 batch 規則の 4 箇所を同一定義で）。`cell_identity_hash` は構成等値検査（06:538 R4-09）に残す；(2) ProfileAcceptanceRecord（06 型）に `site_id` / `cell_id` を追加し `P_ACCEPTANCE_RECORD_INVALID` に「record.cell_id ≠ profile.cell.cell_id ∨ record.site_id ≠ profile.cell.site_id」（record 書込時 + head 再評価）— manager が profile を解決せずに兄弟を列挙できる。baseline registry が既に head(cell_id) を安定 key として使うため本 doc 内で一貫；(3) T-42 の対 case: 同 cell_id・異 cell_identity_hash の兄弟を SUSPENDED → 条件 12 REJECTED（SIBLING_SUSPENDED）；(4) **Rs 項目（OPP 新設・OPP-13 / OPP-16 隣接）**: 安定物理 cell id の発行主体・一意性・改名 / 別名の custody（改名で未解消停止が消えない保証）。暫定 = 06 §8.3 に「cell_id は baseline registry と同じ custody（OPP-16）の下で site 内一意」の 1 文。⚠ reviewer 案「認証された安定した物理セル ID」の新設は既存 `(site_id, cell_id)` で足り、新 id 体系は不要。
- **premise 正確性**: 正確（05:191 / :240 / :292、06:75-100〔CellIdentity = :77-96〕/ :255-260〔tuple→array = :259〕/ :507 は実在）。06:531 は「受理の権限」行で、兄弟 batch 規則は **06:532**（1 行ずれ・主張に影響なし）。

### GA-05（MEDIUM・CONFIRMED）— log head が PROPOSED のとき、SUSPENDED event が運ぶ対象 hash が活性 lease の ACCEPTED record を指さず、即時無効化（裁定 OP-19 第 2 層）が周期 head-check まで遅れる
- **sed 実測**: 06:530「ACCEPTED → PROPOSED(gen+1)（再登録の第 1 評価点文脈）→ ACCEPTED(gen+1)（PROPOSED(gen+1) は authority head を変えず、活性 lease は ACCEPTED(gen+1) が append されるまで ACCEPTED(gen) に束縛されたまま …）」／ 06:512「supersedes_record_hash: str | None  # 前 generation / 前 state の record」／ 06:538「state ∈ {SUSPENDED, REVOKED} の record について: `supersedes_record_hash` 必須（head を指す）；generation = superseded record と同一；… hash field と `valid_until` は superseded record から複写」／ 06:540「head = registry append seq 最大（fork 検出・`supersedes_record_hash` の照合用）；authority head = state ∈ {ACCEPTED, SUSPENDED, REVOKED} のうち seq 最大；同一 record を 2 度 supersede する append は registry が拒否（fork なし）」／ 05:249「profile-scoped … 再試行は `current.active_lease.acceptance_record_hash == trigger.acceptance_record_hash` の間続ける … `trigger.acceptance_record_hash` = supersede された ACCEPTED record の hash（= SUSPENDED record の `supersedes_record_hash`）ゆえ lease の束縛と等値比較できる。v0.2.8 R7-11: … manager は CAS 前に head-check (a) と同じ線形化読みで head.state を確認する」／ 05:291 event 行「event は profile_hash + acceptance_record_hash を運ぶ」／ 05:797 T-46「acceptance_record_hash 不一致で無効化しない・head.state を線形化読みで確認」／ 05:292 head-check (a)「`record_hash ≠ lease.acceptance_record_hash ∨ state ≠ ACCEPTED`」。
- **text lens（未反証）**: 05:249 の同一視「supersede された ACCEPTED record の hash = SUSPENDED の supersedes_record_hash」は、06:538/540 の下で log head が PROPOSED のとき偽（S は P を指す・A_g を指す append は fork 拒否）。R7-11 の「head.state を確認する」は結果の扱いを規定せず、T-46 は「不一致 = 無効化しない」を規範化している。
- **logic lens（未反証）**: A_g（ACCEPTED, gen g）に lease L 活性。P_(g+1) append（P.supersedes = A_g・06:512）。INCIDENT → S append: S.supersedes = head = P（06:538）、S.generation = g+1（複写）。event.acceptance_record_hash = hash(P) ≠ hash(A_g) = L.acceptance_record_hash → 05:249 / T-46 の読みで event は L を無効化しない。周期 head-check (a): authority head = S（SUSPENDED・seq 最大）→ `record_hash ≠ ∨ state ≠ ACCEPTED` で発火・disposition = INCIDENT 行（SAFE_STOP）。= reviewer の「永続的な失効回避とは判定しない」と一致。反証候補 = R7-11 文を「head.state ≠ ACCEPTED なら hash 不一致でも invalidate」と読む — T-46 と矛盾し、text は二義。
- **impact lens（未反証）**: Rs 裁定 OP-19 三層の第 2 層「変更 event の即時通知」が再登録進行中の INCIDENT で機能せず、停止が baseline REGISTRY_HEAD_CHECK の周期まで遅れる。有界遅延・永続迂回ではないが「停止しない」方向の誤りゆえ R7-11（誤停止 = 安全側・LOW）より重い。PROPOSED の registry 意味論（R8-05 Rs 項目）を変えずに直せる → 裁定 / 起草既定との対立ではない → **MEDIUM**（reviewer 付値維持）。
- **verdict**: CONFIRMED / MEDIUM。
- **minimal fix（frozen delta 0・05 / 06 散文）**: (1) 05:249 の同一視を撤回し「`trigger.target_accepted_hash` = SUSPENDED record から `supersedes_record_hash` を辿って最初に到達する state == ACCEPTED の record の hash（PROPOSED を透過 = 停止対象の authority record）」と定義、event（05:291）は target_accepted_hash と SUSPENDED record 自身の hash の両方を運ぶ；(2) 再試行条件を `current.active_lease.profile_hash == trigger.profile_hash ∧ current.active_lease.acceptance_record_hash == trigger.target_accepted_hash` に、R7-11 の head.state 確認は「head.state ∈ {SUSPENDED, REVOKED} ならこの照合で invalidate・head が新 generation の ACCEPTED なら捨てる」と結果の扱いを明記；(3) 06:538 に「PROPOSED を supersede する SUSPENDED / REVOKED の複写元（hash field / valid_until / generation）は透過先の ACCEPTED（authority head）」、06:530 に「ACCEPTED(g) + PROPOSED(g+1) 並存中の SUSPENDED は log head = PROPOSED を supersede しつつ authority head = ACCEPTED(g) を停止対象とする」を各 1 文；(4) T-46 の対 case: A_g 活性・P_(g+1) append 後に INCIDENT SUSPENDED → event 経路で即時 TRANSFER_TO_SAFEHOLD(SAFE_STOP)。PROPOSED を停止扱いにする変更はしない（reviewer と同意）。
- **premise 正確性**: 正確（05:249 / :292、06:538 / :540 実在。「A_g を直接 supersede すると fork 規則に抵触」= 06:540）。06:529 と引く状態遷移文は現行 **06:530**（1 行ずれ・主張に影響なし）。

### GA-06（LOW・CONFIRMED・MEDIUM から降格）— 合法な MARK_BOUNDARY_WAIT SUCCESS（epoch 不変）が「全 CAS SUCCESS で epoch+1」の INV-02 / P5 / 証明 E / T-16 と矛盾する
- **sed 実測**: 05:251「MARK_BOUNDARY_WAIT … 事後条件 = `boundary_wait := True`（他の要素不変・`seq+1`・epoch は**進めない** — lease は活性のまま）」／ 05:712 INV-02「全 CAS SUCCESS: `new.control_epoch == expected.control_epoch + 1 ∧ new.seq == expected.seq + 1`」／ 05:116 P5「`control_epoch` は CAS 成功ごとにちょうど +1」／ 05:133「CAS 成功ごとに +1」／ 05:361 証明 E「全ての CAS 成功は `control_epoch := expected.control_epoch + 1`（両 kind 共通）」／ 05:198 CasKind = 3 member（v0.2.1 B-05）／ 05:280 遷移表 MARK_BOUNDARY_WAIT 行／ 05:767 T-16「全 trace で INV-01/02/03/10/11」／ 05:257・A:381 `offer.control_epoch == authority_epoch_snapshot`。
- **text lens（未反証）**: 「両 kind 共通」は v0.2.1 で第 3 kind が入って以降 stale。INV-02 は kind を区別せず、§3.5 の記録付き no-op（INV-30・epoch 不変）も文言上は対象。
- **logic lens（未反証）**: EXECUTOR で TerminalOutcome 受領 → MARK SUCCESS（05:280）の 1 遷移で INV-02 の epoch 節が偽。逆に INV-02 に従って MARK で epoch を進めると producer の offer.control_epoch（旧 epoch）が validate_handoff で常に `E_HANDOFF_EPOCH_STALE` → 全 handoff が NO_CHAIN（fail-closed だが機能破壊）。
- **impact lens（部分反証 → 降格）**: §3.5 / §3.6 / §3.7 / INV-23 / B0 は epoch 不変で一貫し、安全側の解が本文から一意に読める。runtime 曝露は無く、修正は文言のみ。R7-08 / R3-14（境界・証明の文言不整合 = LOW）と同 class → **LOW**。
- **verdict**: CONFIRMED / LOW。
- **minimal fix（frozen delta 0・05 文言のみ）**: (1) INV-02 を CasKind 別に: TRANSFER_TO_EXECUTOR / TRANSFER_TO_SAFEHOLD SUCCESS: epoch+1 ∧ seq+1；MARK_BOUNDARY_WAIT SUCCESS: epoch 不変 ∧ seq+1 ∧ boundary_wait True；INV-30 の記録付き no-op は SUCCESS ではなく状態不変（CAS_ATTEMPT 記録のみ）と明記；(2) P5（05:116）・05:133 comment →「owner 遷移 CAS（TRANSFER 2 kind）成功ごとにちょうど +1・MARK は epoch 不変」、証明 E（05:361）「両 kind 共通」→「TRANSFER の 2 kind」+ MARK が単調性を壊さない（epoch 非減少・seq 厳密増加）1 文；(3) T-16 の期待を kind 別 INV-02 に。MARK で epoch を進める修正は採らない（reviewer と同意）。
- **premise 正確性**: 正確（05:251 / :712 / 証明 E :361 / T-16 :767 実在）。

### GA-07（MEDIUM・CONFIRMED）— 05 §3.4 (l) が参照する「CLOCK_DRIFT の許容」に対し、baseline `diagnostic_items` は (項目, 周期, worst-case detection) しか持たず、比較量・判定規則・許容値を束縛する入力経路が無い
- **sed 実測**: 05:191 (l)「`wall_now ≥ max(…)` ∧ wall / mono の drift が baseline diagnostic item の許容内 ∧ `wall_now` が … `last_permit_wall` より単調。いずれか偽 = `R_CLOCK_ANOMALY`・permit 不発行。PERMIT_ISSUED payload に `(wall_now, mono_now)` を記録。周期 drift 検査（baseline 項目）の失敗 = TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED | SAFE_STOP)」／ 05:292 (d)「drift 項目 = CLOCK_ANOMALY 行（SAFE_STOP）」／ 05:344 R_CLOCK_ANOMALY 行「… drift 超過」／ 06:527「diagnostic_items: tuple[tuple[str, CanonicalDecimal, CanonicalDecimal], ...]   # (項目, 周期 [s], worst-case detection [s])」／ 06:539「REQUIRED_DIAGNOSTIC_ITEMS … ⊇ {…, CLOCK_DRIFT, …} … CLOCK_DRIFT = AuthorityManager（05 §3.4 (l)）。周期・worst-case の値は baseline = OPP-16」／ 06:576 OPP-16 の open 列挙（drift 許容なし）／ 05:707「fail-closed 既定: 評価不能 = FALSE」。
- **text lens（未反証）**: grep `drift|CLOCK_DRIFT|許容`（05 / 06・非履歴）で許容値・比較量・基準対を定義する文は無い。baseline 型に許容値の slot が無いため、RT0 が値を決めても `timing_baseline_hash` 経由で 05 (l) に届かない。
- **logic lens（未反証）**: baseline が REQUIRED_DIAGNOSTIC_ITEMS を全て含み P_BASELINE_INCOMPLETE を通っても、(l) の「許容内」は評価不能 → 05:707 で FALSE → 毎回 `R_CLOCK_ANOMALY` → permit が一度も出ない（R7-01 と同 class の liveness 0）。さもなければ実装者が許容値を捏造する。周期 / worst-case detection を許容誤差に読み替える経路は正当化できない。
- **impact lens（未反証）**: 値未決定（Rs / RT0 項目・OPP-16）ではなく、値を束縛する schema / 規則の欠落 — reviewer の区別は正しい。fail-closed 側（permit 不発行）に倒れるため凍結違反・安全側破綻ではないが、Rs 裁定 OP-19 第 3 層（根拠付き周期診断）の drift 項目が baseline から実現できない。R6-07「base pose の許容が無い」（MEDIUM）と同 class → **MEDIUM**。
- **verdict**: CONFIRMED / MEDIUM。
- **minimal fix（frozen delta 0・06 deployment 層型 + 05 散文）**: (1) 06 §8.3 CellSafetyTimingBaseline に `clock_drift_tolerance_s: CanonicalDecimal`（値 = RT0・本 doc に書かない）を追加（または diagnostic_items を (項目, 周期, worst-case detection, tolerance | None) に拡張し CLOCK_DRIFT では非 None 必須）；`P_BASELINE_INCOMPLETE` に「CLOCK_DRIFT の許容値が解決できない = 違反」（record 書込時 + 第 2 評価点）；(2) 05 §3.4 (l) を明示参照へ結線: 比較量 = `|(wall_now − wall_ref) − (mono_now − mono_ref)|`、基準対 (wall_ref, mono_ref) = 直前の PERMIT_ISSUED / 直前の CLOCK_DRIFT 診断で manager の durable store に記録した対（05:191 の `(wall_now, mono_now)` 記録を再利用・audit 読出し非依存 = R7-13 と同形）、判定 = 値 ≤ `baseline.clock_drift_tolerance_s`、単位 s、評価周期 = diagnostic_items の CLOCK_DRIFT 周期、許容値未解決 / 基準対欠落 = `R_CLOCK_ANOMALY`；(3) OPP-16 に「clock_drift_tolerance_s の値・wall 源の同期方式（時計対応の前提）」を追記。数値は RT0 に残す（reviewer と同意）。
- **premise 正確性**: 正確（05:191 (l)、06:514-527〔baseline block = :515-528〕/ :539 実在。「数値未決定でなく入力経路の欠落」= 06:527 の tuple 型で確認）。

## 3. 反証 trace の要約（lens 別）

| lens | 反証できた finding | 反証できなかった主因 |
|---|---|---|
| text | 0 | 7 件とも引用行は現行 text（blob `ce176b43…` / `b796830d…`）に実在し、概念の grep（latch / pending_invalidate / skill_definition_hash / ttl・t_obs / cell_id・site_id / supersedes_record_hash / INV-02・両 kind / drift・許容）で「別所に規則あり」は見つからなかった。行番号ずれは GA-04 / GA-05 の各 1 行のみ |
| logic | 0 | 各 scenario は本 doc 群と凍結の定義（(i′) 現在形 05:518・05:707 fail-closed 既定・A:158/:218/:219 の ActionId と registry 不変量・06:538/540 の fork 規則・06:259 tuple→array・06:527 の型）の下で step ごとに追跡できた。GA-01 / GA-02 では reviewer 記載より強い変種（(ii) 再開・未登録 definition）が同じ欠落から構成できた |
| impact | 0（severity 降格 2 = GA-04 / GA-06） | いずれも凍結違反ではないが、fail-closed 保証の未実現（GA-01 INV-29 / §5.4・GA-02 certificate-first / INV-15・GA-03 frozen 鮮度）または Rs 裁定の実現の不整合（GA-05 OP-19 第 2 層・GA-07 第 3 層）・自己矛盾（GA-06）として欠陥性が残る。裁定の設計方向・§10.1 / §11.1 起草既定への反対に当たる finding は無かった（GA-01 は R7-04 既定と別軸であることを確認） |

## 4. 主張しないこと・境界

- 本 verification は AI（別 context・別 family の reviewer に対する Claude 側 verifier）の準備 verify であり、pS / pY / pN / Rs の two-key・gate PASS・Rs 裁定を代替しない。verdict は verifier 付値で、fold 採否は起草者と Rs の判断。外部 reviewer の HOLD 推奨も本 verify で PASS に読み替えない（reviewer 自身の手続未了 = pin 再測 / checker / 盲検対照は本 verifier が代行実測したが、それは reviewer の記録を書き換えるものではない）。
- 凍結 4 file・`eval_runs/` 配下・`$C/` の受領物は一切編集していない（`git status --short eval_runs/` = 空を 08:26 UTC に再確認）。書込み先 = `$OUT` のみ（`V9_verify_GA_v028.md` / `V9_results_GA_v028.json` / `pins_V9.txt` / `checker_05_V9.txt` / `checker_06_V9.txt` / `_write_results.py`）。
- reviewer の toy model（`03_` / `04_`）は反例の意図を読むためだけに参照し、evidence として一切用いていない。全 verdict は候補 text と凍結 text からの再構成。
- severity は本 doc 群の従来 scale（名前参照のみで内容 hash 束縛が無い / 失効未執行 = HIGH、fold 未完・head-check 系 = MEDIUM、境界・語彙・証明文言 = LOW）に合わせた。GA-01 / GA-02 の HIGH は verifier 追加の変種（(ii) 再開・未登録 definition）を根拠に含む — reviewer 記載範囲のみなら MEDIUM 相当であることを各節に明記した。GA-03 の HIGH は曝露が `boundary_dwell_s` で有界な点を付記 — 起草者 / Rs が MEDIUM と読み替える余地は残る。
- minimal fix は全て凍結 schema delta 0 を確認した提案であり、設計を確定するものではない。Rs 専権として区別したもの: GA-04 の安定物理 cell id の custody（OPP 新設）、GA-01 の「不読なら常に SafeStop」（R7-04 既定）、GA-05 の PROPOSED / authority head 意味論（R8-05 既定・本 fix は触れない）、GA-07 の許容値そのもの（RT0 / OPP-16）。
- open = 0 を宣言しない。本 verification が触れていない領域: 凍結 validate_invocation_start 自体が consumer 側 definition hash を検査しない点（凍結側の事項・本 doc の対象外として 05 側の束縛のみを finding とした）・ISL heartbeat の配送 topology（05 未規定・GA-01 の変種の前提として明記）・06 §6.1 evidence の物理内容・execution profile 軸・composition・EP v1.9 内部・`SafeHold` の物理実現（OP-4 / OPP-4）。
