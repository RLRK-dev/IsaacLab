# REVIEWER C（deployment / commissioning lens）— blind review of 06_pc.md（primary）+ 05_pc.md §4

- reviewer: Claude Code web session subagent（authority 無し・凍結物へ非接触）; 作成 = 2026-09-03 16:58 UTC（`date -u` 実測）
- 対象: `$S/review/pc/06_pc.md` sha256 `0c197e87f5b368ced016f02b5c173b36a1cf264df6796e01d6ffb6a2e7813145`（422 行・全行読了）／ `$S/review/pc/05_pc.md` sha256 `a8f63183a3edb711a9040a238bf7f496dad9301a7f9ad1356f42ec03aad848a9`（§4 = 304-411 行 全読 + §3.4-3.5 / §5 / §6.2 / §7 / §9 / §12 を結線確認のため読了）
- ⛔ `$S/review/pc/KEY.md` は**開いていない**（blind 前提・08 計画 §1 項 6）。本 report の finding は全て候補文書と凍結 file の内容照合のみから導出。
- `$D` = `eval_runs/troot_optE_dapg_wholeroute_scope_20260701`、`$S` = scratchpad。A = `$D/WMSO_D11A_CONTRACTS_V2_DESIGN_RSTECHLEAD2_20260719.md`、B = `$D/WMSO_D11B_TENSOR_BINDING_DESIGN_RSTECHLEAD2_20260720.md`、EP = `$D/WMSO_D11A_EVIDENCE_POLICY_V1_RSTECHLEAD2_20260719.md`、D0 = `$D/WMSO_D0_ARCHITECTURE_DRAFT_RSTECHLEAD2_20260718.md`。

## 0. 共通前提の履行記録

| 前提 | 結果 | 記録 |
|---|---|---|
| (1) pin 再測 | **11/11 PASS**（2026-09-03 16:51:10 UTC、HEAD `17f04fd6a8f5742a64ecfa70a0d614bbecd8dd43` branch `claude/v0-2-design-candidates-snqbmy`、policy_semver 1.9.0、素の bash 実行） | `$S/review/pin_recheck_C.txt` |
| (2) content sha | 候補 2 doc の sha256 を自分で再計算（上記） | 本 report header |
| (3) 機械検査 | 06: citations=55 **FAIL=0** WARN=19 ／ 05 `--runtime`: citations=138 **FAIL=0** WARN=6 → 読解へ進んだ。19 WARN は全て「1 行複数 locus」or「frozen 行を型根拠に引き同行で新語導入」の heuristic であり、私が sed で当該 locus を再確認した結果 引用先の内容は一致（B:23/24/40/52/65-66/75-76/81/96-99/125-127/132/133/138/185、A:39/142/146/147/151/159/168/218/372-373/380/385、EP:101/148/154/156/163、JSON:5/213、D0:317/318/319/354-355、charter:13/129、C3:5/65、BCS:59、D11C custody:36-38、D11B freeze:39、ownership ruling:9、state.md:91、HANDOFF_pQ:11） | 本 report §0 |
| (5) 陽性対照 | 自作 instrument `$S/review/axisC_instrument.py`（I1 bounds field / I2 self-ref field / I3 composition field / I4 safety exempt / I5 rate relation / I6 escalation relax / I7 bounds override prose）を、自己注入した欠陥複製 `$S/review/06_injected_ctrl.md`（top-level に `profile_hash` self field + `parallel_region: ParallelRegion` field を注入）に盲検適用 → **I2 / I3 とも検出**。陰性対照 `$S/review/06_neg_ctrl.md`（既知欠陥 4 点を除去）では I1/I4/I6 が消失、残る I5(253,401)/I7(347)/I2(290) は「規則を述べる散文」「`DeploymentEvidenceRecord.profile_hash`（record 型の正当な参照）」の偽陽性であり、手動で除外した | 実行 log は本 turn の Bash 出力（16:58:18 UTC） |
| (6) 独立 | 他 reviewer の finding・KEY.md を読まずに本 report を確定 | — |

貴 brief（AXIS_C_DEPLOYMENT.md C1）の骨子「rate ≤ / hold stricter-or-equal」は、binding の 08 計画 §4 C1「rate/hold/control_mode 等値」および critic X3（`$S/maps/07_critic_report.md:141`、R1 supersede）と食い違う。本 review は **binding 側（等値）**を採用して判定した。

## 1. Findings（severity 順・id = C-xx）

### C-01 ｜ CRITICAL ｜ profile が policy action space の bounds を**上書きできる** field を持つ（`BindingExpectation.action_bounds_cell`）

- 候補 doc: `06_pc.md:186` 「`action_bounds_cell: tuple[tuple[CanonicalDecimal, CanonicalDecimal], ...] | None  # policy action space での cell 固有 bounds（非 None なら INTRINSIC 項はこの値で評価。None = D1.1-B の bounds）」
- 自己矛盾: `06:19`「持たないもの: policy action space の bounds（D1.1-B が hash で持つ）」／ `06:226`「profile 側の field = **無し** … field が現れたら `P_INTRINSIC_OVERRIDE`」／ `06:254` (i)「field が無い」／ `06:348` PT-05「INTRINSIC override の唯一の経路が閉じる」／ `06:407` anchor 1「bounds を表す field が無く」／ `06:398` handoff B「intrinsic bounds は D1.1-B を hash 参照」
- 凍結との矛盾: B:138「全 action feature は `bounds` 必須」（bounds は TensorBindingSpec の hash-visible な一部）＋ B:185「bump = identity event（`tensor_binding_hash` → `ExecutionBundleHash` → `SkillActionId`）」— bounds を変えた実行は別 identity であり、同じ `tensor_binding_hash` を名指したまま別 bounds で走ることは D1.1-B の identity 規律に反する。B:52 `BoundsSpec = {lower|None, upper|None}` と型も異なる。05 側: `05:386`「**bounds は再定義しない**」、handoff 決定 B（`IntrinsicExecutionLimits` 不採用）。
- 反例（検査を通る「広げる」profile）: D1.1-B bounds = [-1, 1] の action feature に対し `action_bounds_cell = ((-10, 10), …)` を宣言。既知 field ゆえ `P_UNKNOWN_FIELD` は出ず、`P_INTRINSIC_OVERRIDE` は「unknown field」条件（`06:226`）でしか発火せず、⊆ 検査 code は §8.1 list（`06:336`）に存在しない（VOCABULARY.md:24 例示の `P_BOUNDS_WIDENED` も未定義）。全 `P_*` = 0 で受理 → gateway の INTRINSIC 項が 10 倍広い bounds で評価される。
- 最小修正（schema delta 0）: `06:186` の field を削除。`BindingExpectation` は `tensor_binding_hash` 等値（`P_BINDING_HASH_MISMATCH`）のみで INTRINSIC を名指す。PT-05 はそのまま有効。

### C-02 ｜ HIGH ｜ `action_rate_hz` を profile が「下げてよい」（stricter-or-equal `p ≤`）とする行が §3 表にあり、等値規則と自己矛盾

- 候補 doc: `06:228` 「`action_rate_hz` のみ **stricter-or-equal**（`p ≤` TensorBindingSpec の値 — controller が支える周期まで下げてよい） | `P_RATE_MISMATCH`（`action_rate_hz` は `p >` のとき）」
- 自己矛盾: `06:19`「制御周波数・hold の変更権（等値のみ）」／ `06:32`「**等値のみ**（R1）」／ `06:344` PT-01「TensorBindingSpec より小さく宣言 → `P_RATE_MISMATCH`（等値のみ）」／ `06:402`「brief 骨子の rate ≤ は R1 で supersede — critic X3」／ `06:408` anchor 2「等値検査 … narrow を許さない」
- 05 / 凍結との矛盾: `05:376`「executor は**ちょうどその値**で走る」、`05:388`「制御周波数は含まない（R5）」、`05:620` INV-12「`timing.action_rate_hz == TensorBindingSpec(...)`（等値）→ `R_PROFILE_MISBOUND`」。凍結 B:126-127 `ActionTimingSpec{action_rate_hz, hold}` は hash-visible（B:185 identity）— 周期を半分にした ZOH は訓練時と異なる dynamics（`hold` が 2 倍長）であり certified 挙動の外。
- 反例: 10 Hz 訓練 policy に `BindingExpectation.action_rate_hz = 5`。`06:228` の検査は PASS、`05` INV-12 は FAIL — profile 受理と lease 成立の判定が一致しない。gateway が profile 値に従えば uncertified 周期で運転。
- 最小修正: `06:228` の「`action_rate_hz` のみ stricter-or-equal…」句を削除し 4 量とも `==`／`P_RATE_MISMATCH` は `≠` で発火、と書く。PT-01 と整合。

### C-03 ｜ HIGH ｜ `EscalationPolicy.on_invalid_state_unresolved` が 05 既定 SAFE_STOP を **HOLD へ緩和**できる

- 候補 doc: `06:172`「`on_invalid_state_unresolved: EscalationTarget  # 05 §7 既定 = SAFE_STOP。clearance_roles が整備された cell では HOLD を選択可`」、`06:234`「05 既定 SAFE_STOP の経路も、`clearance_roles` が整備された cell では HOLD に留めてよい」
- 自己矛盾: `06:69`「EscalationTarget … SAFE_STOP 側へのみ強化」、`06:254` (vii)「disposition を緩める — enum に緩和値が無い」、`06:412` anchor 6「escalation は SAFE_STOP 側へのみ強化」
- 05 / 凍結との矛盾: `05:560`「profile は SAFE_STOP 側へのみ強化可」、`05:569`「Terminal INVALID_STATE 解消せず → SAFE_STOP」、`05:574` frozen `FailClosedAction.SAFE_STOP → SAFE_STOP`（A:151）。handoff 決定 7（NARROW のみ）、08 計画 C1「escalation は SAFE_STOP 側のみ」。
- 反例: `on_invalid_state_unresolved = HOLD` の profile。HOLD も SAFE_STOP も enum member ゆえ codec 拒否なし、`P_*` に該当 code なし → 受理。belief が再観測でも有効化しない状態で robot が SAFE_STOP でなく HOLD（姿勢保持・電源維持）に留まる = frozen fail-closed action の緩和。
- 最小修正: field を削除（`on_invalid_state_unresolved` は常に SAFE_STOP）か `Literal[SAFE_STOP]` に固定し、`06:172` / `06:234` の例外文を削除。

### C-04 ｜ HIGH ｜ `requires_safety_layer_health = False` を SHADOW lease で許容し、根拠「actuation port DISABLED」が 05 §4.4 と食い違う／検査時点が存在しない

- 候補 doc: `06:114`「既定 True。SHADOW_NON_AUTHORITY lease のみ False を許容（actuation port DISABLED ゆえ）」、`06:235`「CLOSED_LOOP_AUTHORITY で False ⇒ `P_SAFETY_LAYER_NOT_REQUIRED`」
- 自己矛盾: `06:254` (vi)「安全層を不要にする — True 必須」、`06:349` PT-06「`= False` → `P_SAFETY_LAYER_NOT_REQUIRED`」（無条件）、`06:413` anchor 7「True 必須」
- 05 / 凍結との矛盾: (a) `05:400` §4.4 SHADOW row「**DISABLED（初回 admit を除く）** — 最初の admissible command のみ actuation port へ出力」→ SHADOW でも物理 command が 1 発出る（C-05 参照）ので「DISABLED ゆえ不要」は成立しない。(b) `05:225` CAS 前提 9「IndependentSafetyLayer の直近 health ∈ {ok, degraded}」は lease mode を問わず無条件（D0:318「missed heartbeat ⇒ consumers must **not** assume safety alive」）— profile flag に 05 側の消費者は無い（grep `requires_safety_layer_health` in 05 = 0 hit）。よって flag は (i) 死んだ field（誤解を招く）か (ii) CAS 前提 9 を緩める意図、のどちらかで、後者は D0 §F 違反。(c) `P_SAFETY_LAYER_NOT_REQUIRED` は lease mode 依存だが `06:338` 規則 (2) は `P_*` を **profile 登録時**に評価する — 登録時に mode は未知ゆえ検査は空振りし、False の profile が受理されて後に CLOSED_LOOP lease へ束縛され得る。
- 反例: `requires_safety_layer_health = False` の profile を登録（mode 不明 → `P_*` = 0）→ 同 `profile_hash` で CLOSED_LOOP_AUTHORITY lease を要求。06 側に再評価点が無い。
- 最小修正: field を削除（安全層 health は 05 CAS 前提 9 が常に要求）か `bool = True` を定数化し SHADOW 例外文（`06:114`, `06:235`）を削除。PT-06 は無条件のまま。

### C-05 ｜ HIGH ｜（05 §4・in-scope）SHADOW lease が「最初の admissible command」を actuation port へ出す = 非 authority 帰属の破れ

- 候補 doc: `05:400`「gateway actuation | **DISABLED（初回 admit を除く）** — 最初の admissible command のみ actuation port へ出力し（`HealthConfirmation.first_command_admitted` を実 readback で確認するため）」
- 自己矛盾（05 内）: `05:82`「SHADOW lease の actuation port = DISABLED」、`05:424`「SHADOW_NON_AUTHORITY では actuation port が DISABLED なので … 物理 command は出ない」、`05:617` INV-09「SHADOW ⇒ actuation port から当該 lease 由来の command が **0 件**」、`05:209`「first_command_admitted … SHADOW: 記録された」（出力ではない）
- 凍結との矛盾: charter `:129`「S0 Shadow mode … zero control authority」、EP:155「SHADOW は定義上 非 authority」、C3:32-34（slice = SHADOW rank 2 非 authority）、A:354（authority = `granted == True` 必要条件 + two-key）。SHADOW lease は `authority_decision_ref` 任意（`05:401`）ゆえ、`granted` 無しに 1 command が物理出力される = 認可なしの制御 authority。
- 反例: SHADOW lease の最初の command が keep-out 境界近傍の EE target。gateway は admit → 実 actuation → 06 の 3 軸分離が依拠する「SHADOW（非 authority）」（`06:43`、`06:366` §9 項 5）が runtime で偽になる。
- 最小修正: `05:400` の括弧「（初回 admit を除く）…実 readback で確認するため」を削除し「DISABLED（全 command は admission 判定・記録のみ）」とする。`first_command_admitted`（SHADOW）= 記録済みの意味で既に定義済み（`05:209`）。06 側は変更不要（C-04 の修正と両立）。

### C-06 ｜ HIGH ｜ `InitiationStrengthening` の評価点が runtime（05 §6.2 B4）に**存在しない** — 06 の引用先に当該内容が無い

- 候補 doc: `06:40`「profile の強化は **runtime が別 conjunct として評価**（05 §6.2 B4）」、`06:231`「評価は runtime 側 conjunct」
- 引用先: `05:473-478` B4 = certified 検査 / `validate_invocation_start` / `validate_handoff` / `AcceptedEnvelope.satisfiable`・`TimingBinding` 等値・mode 要件 — **profile conjunct の評価行は無い**。05 全文 grep `conjunct|InitiationStrengthening|initiation_strengthening` = EP conjuncts の言及のみ（05:62, 05:727）で profile 述語の消費者 0。
- 凍結との関係: A:380 の frozen 検査（initiation predicate / freshness / required ⊆ offered）は profile 述語を知らない（正しい・不変）。従って profile 述語は**誰も評価しない** = handoff 決定 D「initiation 制約 = profile の単調強化」が書面上だけ。緩和ではないが「強化が有効」という主張が偽（ABSENT-IN-CODE 相当）。
- 反例: `conjuncts = (THRESHOLD: cable_tension < 2 N)` を持つ profile を受理・lease 化しても、B4 は frozen predicate のみで候補を通す。
- 最小修正（runtime 層のみ）: `05:477` の直後に 1 行「`profile(lease.profile_hash).initiation_strengthening.conjuncts` を **同一 belief・同一 now** で評価。いずれか FALSE / 評価不能 → 候補除外（`R_INVOCATION_INVALID` に包む）」を追加し、`06:40` / `06:231` の参照をその行へ向ける。

### C-07 ｜ MEDIUM ｜ `HealthCheckStage.BEFORE_PERMIT` の束縛点が 05 に無い（permit 発行前の health check が未執行）

- 候補 doc: `06:68` enum、`06:245`「3 kind が各 ≥ 1（… BEFORE_PERMIT 必須）」、`06:274`「`HealthConfirmation.profile_health_checks` ← `health_checks`（stage ∈ {AT_HEALTH_CONFIRMATION, BOTH}）」— BEFORE_PERMIT 分の写し先が無い。
- 05 側: `CommitPermit`（`05:166-177`）に health check 結果 field 無し、CAS 前提 1-10（`05:217-226`）に profile check 無し、消費者は post-commit の `05:210` のみ。grep `BEFORE_PERMIT|health_checks` in 05 = `05:210` のみ。
- 反例: `CONTROLLER_LIVENESS(stage=BEFORE_PERMIT)` を宣言した profile。permit は controller 死亡中でも発行され、CAS 成功→ SafeHold →HealthConfirmation で初めて失敗（旧 owner は既に無効化済 — `05:233`）。「permit 前」に止める設計意図が実現しない。
- 最小修正: `05 §3.4` に permit 発行前提「stage ∈ {BEFORE_PERMIT, BOTH} の全 check が ok。結果 ref を `CommitPermit.profile_health_check_results_ref` に束縛」を追加（runtime 層 field）。06 は `06:274` に「BEFORE_PERMIT 分 → `CommitPermit`」を追記。

### C-08 ｜ MEDIUM ｜ `ResourceAvailability.attested_by` ↔ `DeploymentEvidenceRecord.profile_hash` の相互参照（間接循環）と、`P_RESOURCE_UNATTESTED` の空虚さ

- 候補 doc: `06:138`「`attested_by: tuple[str, ...]  # DeploymentEvidenceRecord.record_id（CELL_COMMISSIONING）— 空 = P_RESOURCE_UNATTESTED`」、`06:291`「`profile_hash: str  # どの profile についての evidence か（束縛）`」、`06:233`「`attested_by` 空 ⇒ `P_RESOURCE_UNATTESTED`」
- 論理: profile の hash preimage に `record_id` が入り、record の内容に `profile_hash` が入る。record が content-addressed なら循環（record_id が profile_hash に依存し profile_hash が record_id に依存）。content-addressed でないなら `record_id` は label であり「cite content, never labels」に反し、かつ record は profile_hash 確定後に**書き換え**が要る（measured_at 付き attestation の不変性が崩れる）。`06:217` 反循環規則（B:23 同型）は直接自己参照しか閉じていない。
- 加えて検査が空虚: `P_RESOURCE_UNATTESTED` は「空でない」だけを見る（`06:233`）。任意文字列 1 個で通る。record の内容（どの ControlResourceSpec を attest したか）と `control` の照合が無い。
- 反例: `attested_by = ("x",)`、`control = {全 True}`、cell は片腕。全 `P_*` = 0 → frozen `required ⊆ offered`（A:380）を「事実誤り」の offered で通す（06 自身が §3 (v) で認める honest scope だが、検査を「attestation」と呼ぶには足りない）。
- 最小修正: `attested_by` を profile から削除し、`P_RESOURCE_UNATTESTED` を `P_EVIDENCE_UNBOUND`（`06:248`）と同じ経路 —「`profile_hash` = 本 profile・kind = CELL_COMMISSIONING・`subject_ref` = cell_id の record が存在し、その payload の attested ControlResourceSpec == `resource_availability.control`」— に置き換える（登録時 + permit 発行時に評価、C-10 参照）。循環が消える。

### C-09 ｜ MEDIUM ｜ `DeploymentEvidencePolicy` の「最低」集合と `validity_rule` が散文 / 自由文字列で、機械検査されない

- 候補 doc: `06:307-309`「最低 = CELL_COMMISSIONING, …」（comment のみ）、`06:310`「`validity_rule: str  # "all required records valid at permit issue time"`」、`06:248`「`P_EVIDENCE_UNBOUND` = policy hash が解決でき、§6 の required kinds が存在」
- 論理: `P_EVIDENCE_UNBOUND` は policy が**要求した** kind の存在しか見ない。`required_before_closed_loop_lease = ()`・`fault_injection = ()`・`validity_rule = "never"` の policy を hash で束縛した profile は空虚に PASS する。C5 checklist「kind の網羅」は型では満たすが、網羅を**強制**する検査が無い。
- 反例: 上記 policy + profile → CLOSED_LOOP lease が CONTROLLER_ENVELOPE_MEASUREMENT / FAULT_INJECTION_RESULT 無しで成立。
- 最小修正: 最低集合と fault-injection 最低集合を 06 の定数（policy field ではない）にし `P_EVIDENCE_POLICY_BELOW_MINIMUM` を追加、`validity_rule` を単一 member enum `VALID_AT_PERMIT_ISSUE` にする。

### C-10 ｜ MEDIUM ｜ 時刻 / lease-context 依存の `P_*` の評価点が未定義（登録時 vs permit 発行時）

- 候補 doc: `06:338` 規則 (2)「profile の受理（`profile_hash` の登録）は全 `P_*` = 0 が前提」 vs `06:356` PT-13「新 lease は `P_CALIBRATION_EXPIRED` で不成立」／ `06:310`「valid at permit issue time」／ `06:247`「`valid_until` が**評価時刻**より後」／ `06:235` mode 依存 code。
- 論理: `P_CALIBRATION_EXPIRED` / `P_EVIDENCE_UNBOUND` / `P_EXPECTATION_UNCERTIFIED` / `P_SAFETY_LAYER_NOT_REQUIRED` は登録時に真でも permit 時に偽になり得る（またはその逆）。どの時点で誰が再評価し、結果を何に束縛するかが無い（OPP-3 は wall-clock の対応だけを扱う）。
- 反例: 登録時有効な calibration が 3 週後に失効。§5 は「lease 無効化」（`06:275`）を言うが、**新 lease の permit 発行時**に `P_CALIBRATION_EXPIRED` を誰が出すかは PT-13 の期待だけで規定されていない。
- 最小修正: §8.1 に「静的集合 = 登録時 1 回／動的集合 {P_CALIBRATION_EXPIRED, P_EVIDENCE_UNBOUND, P_EXPECTATION_UNCERTIFIED} = `CommitPermit` 発行時に再評価し結果 ref を permit に束縛、違反 = permit 不発行」を明記。

### C-11 ｜ MEDIUM ｜ 空間項（zone / tool CoM / EE speed）に `frame` 宣言が無い — gateway の FK 変換先が未定義

- 候補 doc: `06:100-103` `ZoneRef{zone_id, geometry_sha256, kind}`、`06:106-108` `WorkspaceRestriction`、`06:95` `tool_com_m`、`06:85` `ee_speed_max`。単位は comment、frame は無し。`06:35` / `06:267`「変換は gateway が FK / 逆写像で行い、変換不能 = 拒否」。
- 凍結との関係: B:75-76 `FeatureBinding.unit` / `frame` は**等値必須**（暗黙換算禁止）。profile 項は別空間ゆえ literal 交差はしない（正しい）が、その空間の frame が profile に無ければ「変換」は定義できない。`layout_revision_ref`（`06:78`）は layout 版であって frame ではない。
- 反例: keep-out 幾何が world frame、gateway が robot base frame で FK。fail-closed なら全 command 拒否（使用不能）、暗黙仮定なら keep-out が base-world 変位分ずれる（commissioning で最も起きる事故）。
- 最小修正: `ZoneRef` / `WorkspaceRestriction` / `ToolPayloadSpec.tool_com` / `ControllerEnvelope.ee_speed_max` に `frame: str` を追加し、`CalibrationRef.kind` に `base_to_world` を含めて frame 解決を calibration に束縛（`P_FRAME_UNRESOLVED` は評価不能 = FALSE の既定で足りる）。

### C-12 ｜ MEDIUM ｜ `RuntimeTimeouts` に `ack_validity_s` が無い（05 §3.3 が `profile.ack_validity_s` を消費）

- 候補 doc: `06:141-146` `RuntimeTimeouts{ack_timeout_s, permit_ttl_s, health_confirm_timeout_s, command_deadline_s, manager_liveness_s}`、`06:274` TimingBinding への写し（同 5 値）。
- 05 側: `05:155`「`valid_until: float  # = t_ack + profile.ack_validity_s`」、`05:321-325` TimingBinding にも無し、`05:630` INV-22「全 timeout は `profile_hash` 由来 … 本 doc に数値定数は無い」。
- 論理: ReadinessAck の有効期限（CAS 前提 3 `now ≤ ack.valid_until`、`05:219`）の profile 出所が無く、INV-22 と矛盾。
- 最小修正: `RuntimeTimeouts.ack_validity_s` と `TimingBinding.ack_validity_s` を追加（両 doc）、または両 doc に「`ack_validity_s := ack_timeout_s`」を明記。

### C-13 ｜ MEDIUM ｜ `clearance_roles` が SAFETY / MANAGER_RESTART の解除条件を profile 側で定義できると読める

- 候補 doc: `06:175`「`clearance_roles: … # (safehold_reason, role) — SAFETY / SAFE_STOP / CHECKPOINT_DISABLED / MANAGER_RESTART の解除に要する role（05 §5.4）`」
- 05 側: `05:448` SAFETY = 「IndependentSafetyLayer の clearance record（`stabilized_post_action_state` D0:320 を含む）。runtime は自ら解除しない」、`05:451` MANAGER_RESTART = 「durable state の整合検査記録」。profile が定義するのは SAFE_STOP（`05:449`）と CHECKPOINT_DISABLED（`05:450`）のみ。
- 論理: profile の role 定義が ISL clearance record の**代替**と読めれば D0 §F（`:319` 一方向 authority）の緩和。追加条件（連言）なら強化。文面は「解除に要する role」で両義。
- 最小修正: `clearance_roles` の対象を {SAFE_STOP, CHECKPOINT_DISABLED} に限定し、「05 §5.4 の条件に**追加**される連言であり代替ではない」と 1 文で固定。

### C-14 ｜ LOW ｜ `06:317` の grep 主張が過大（EP md に `cell` は存在する・critic の grep 対象に `cell` は無い）

- 候補 doc: `06:317`「EP md / JSON に `controller` / `calibrat` / `deploy` / `inject` / `cell` の語彙は無い（critic 報告 §3.7 の grep 結果）」
- 事実: EP:36, 37, 39, 52, 84, 156 に `cell`（表 cell の意味）が存在。critic の語彙検査（`$S/maps/07_critic_report.md:215`）は `epoch, lease, controller, calibrat, inject, deploy, real-time, boundary-only` で `cell` を含まない。結論（deployment 語彙は EP 外）は変わらないが、主張と出典が一致しない。
- 最小修正: `cell` を list から外すか「deployment の意味の `cell` は無い（表 cell の語は EP:36 等）」と限定。

### C-15 ｜ LOW ｜ `robot_model_ref` の RS71 照合に `P_*` code が無い

- 候補 doc: `06:75`「RS71 §0 の型式（UR15）と一致必須」、`06:249` `P_ENVELOPE_SHAPE` は関節数のみ、OPP-7（`06:383`）は依存を述べるのみ。
- 最小修正: `P_ROBOT_MODEL_MISMATCH`（照合先 = RS71 §0、値は書かない）を追加するか、「照合は CELL_COMMISSIONING evidence 側」と明記。

### C-16 ｜ LOW ｜ header の作成時刻が非厳密（`16:5x UTC`）で file mtime（16:47 UTC）より後

- 候補 doc: `06:3`「作成 = 2026-09-03 16:5x UTC（`date -u` 実測）」。`ls -la` mtime = 16:47。date-THEN-write（§15 records-must-match-fact）に反する。
- 最小修正: 実測値を書く。

### C-17 ｜ LOW ｜ `P_COMPOSITION_CONTENT` は直列化本文の substring 検査（heuristic）で、語境界・意味論を扱わない

- 候補 doc: `06:329`「直列化本文に合成語彙（… `arity`）が現れたら `P_COMPOSITION_CONTENT`」
- 論理: `profile_label = "similarity-cell-A"` は `arity` を含み拒否される（fail-closed 側ゆえ安全だが運用不能）。逆に別名の合成述語（`predicate_refs` 経由）は検出不能。
- 最小修正: 検査対象を「serialized key 名 / enum 値」に限定した word-boundary match とし「heuristic・意味論は §7 の宣言で担う」と明記。

### C-18 ｜ CRITICAL（⚠ 軸 B 領域・lens 外の観測・本軸 verdict には算入しない）｜ 05 §3.5 が旧 epoch 拒否を HEALTH_CONFIRMED まで**遅延**させる

- 候補 doc: `05:231`「旧 epoch の拒否は新 lease の `HealthConfirmation` 受領（`HEALTH_CONFIRMED` 記録）の線形化点から有効化する」、`05:262`「HEALTH_CONFIRMED（この線形化点で旧 epoch 拒否が有効化）」
- 矛盾: `05:32`「旧 epoch 拒否（同一線形化点）」、`05:701` fold-map 4「旧 epoch 拒否は同一 CAS で有効（post-transfer ACK に遅延しない）」、handoff 決定 C.4。CAS 後 HealthConfirmation 前の窓で旧 lease の command が admit され得る（二重所有窓の復活）。
- 06 への波及: 06 §5（`06:275`）の「lease 無効化 → SAFEHOLD」は旧 lease 無効化が CAS 時点で command 拒否を伴う前提。
- 最小修正: `05:231` を「旧 epoch / 旧 lease_id の command 拒否は CAS 事後条件として**同一線形化点**で有効」に戻す。軸 B reviewer の確認に委ねる。

### C-19 ｜ LOW ｜ `BindingExpectation` が rate / hold / control_mode を D1.1-B から**写している**（hash 等値で決まる冗長 copy）

- 候補 doc: `06:181-185`。`tensor_binding_hash` 等値（`06:230`）で TensorBindingSpec は一意に決まるため、4 値の copy は整合面を増やすだけ（C2「never copied」の精神）。widening 経路ではない。
- 最小修正: `tensor_binding_hash` + `control_mode` のみ残し、他は「TensorBindingSpec から導出・05 INV-12 が等値を執行」と書く（任意）。

## 2. Checklist 別判定（C1-C10）

| id | 判定 | 根拠（PASS 項は content 引用） |
|---|---|---|
| C1 monotone strengthening 機械検査 | **FAIL** | 検査を通る「広げる」profile を 4 通り構成（C-01 bounds override / C-02 rate ≤ / C-03 escalation HOLD / C-04 safety exempt）。PASS 部分: control_mode `==`（`06:229`）、binding hash `==`（`06:230`）、initiation = AND のみの型（`06:127`、ExprKind 参照 A:151 = {THRESHOLD, REGION, BOOLEAN_AND, CALLABLE_REF} member 追加なし）、freshness `p ≤ f` + None 規則 loud（`06:232`, OPP-1）、resources ⊇ required は frozen 検査不変（`06:233` ↔ A:380）、unknown field 拒否（`06:218` ↔ A:385）。 |
| C2 AcceptedEnvelope | **PASS-WITH-CONDITIONS** | 4 項の出所 `06:259-262` ↔ `05:386-389`（EnvelopeTermKind 4 member `05:310`）、空 = fail-closed 二重化 `06:266` ↔ `05:392` / INV-13 `05:621`、空間別評価 `06:265` ↔ `05:393`、SCRIPTED/WAIT の INTRINSIC 欠落を loud（`06:268` ↔ 05 OP-2）。条件: C-01（bounds copy）、C-11（frame）。 |
| C3 lease 束縛 | **PASS-WITH-CONDITIONS** | `profile_hash` ∈ lease `05:360` ∧ permit `05:174`、変更 = 新 lease `06:273` ↔ `05:370`、in-place 無し、top-level に self hash field 無し（instrument I2 = 0 in profile types）+ `P_SELF_HASH`（`06:217`）、WCJ 継承 `06:216` ↔ A:168 / B:24（新規則なし）。条件: C-08（間接循環）。 |
| C4 cell/controller/tool/payload | **PASS-WITH-CONDITIONS** | 識別・版 `06:72-78`（serial / firmware / layout_revision）、calibration `06:157-161`（sha256 + valid_until）、gateway config `06:164-166`（sha256）+ `GATEWAY_CONFIG_MATCH` check、health fail-closed 必須 `06:154` + `P_HEALTHCHECK_FAIL_OPEN`（`06:246`）、evidence 束縛 `06:288-297`（subject_ref）。条件: C-07（BEFORE_PERMIT 未束縛）、C-10（評価点）、C-11（frame）、C-15。 |
| C5 DeploymentEvidencePolicy | **PASS-WITH-CONDITIONS** | 名前空間分離: 18 名（kind 8 + health kind 6 + StopClass 2 + stage 2）を frozen 4 file で grep → 0 hit；EP claim_targets / ProofKind / grade / profiles 非改訂 `06:316` ↔ EP:163、JSON:5；kind の網羅 `06:284-285`（cell / controller / tool / calibration / gateway / safety layer / health / fault injection）；join `06:323`（`profile_hash` + `lease_id`、hash 方式 U14 defer A:537）；未認証範囲の明示 `06:318-319`, `06:368`（certificate 非影響 A:39）。条件: C-09（最低集合の未検査）、C-14。 |
| C6 composition OUT | **PASS** | `06:327-330`；code block 内に ParallelExecutionProfile / ParallelRegion / arity / branch_count / barrier = 0（instrument I3）；court pointer 実在確認: D11C custody:36-38（合成 = pX court・ParallelRegion 枝数 + DUAL-ARM = Rs 専権）、D11B freeze:39（arity SUSPENDED 独立）、ownership ruling:9（SKILL = pX）。`06:330` の両腕 resource 宣言 ≠ 合成 の注記は 05 §11 項 5（`05:663`）と整合。注: C-17（heuristic）。 |
| C7 3 軸分離 | **PASS（06）／FAIL（05 §4.4）** | 06: `06:20-23`, `06:52-58`, `06:365`（execution 軸 = Rs、C3:65 逐語「execution profile を決めていない」、charter:13）；LeaseMode 名 ≠ EP profile 名（`05:406`）。05 §4.4: C-05（SHADOW の物理 1 command）が「SHADOW = 非 authority（EP:155 逐語）」を破る。 |
| C8 initiation / resources / freshness | **PASS（型）／FAIL（執行）** | 型: 置換 / OR / NOT 不能（`06:127`）、frozen InitiationSpec 不変 ⇒ EP:101 claim target 不変（`06:44`）、`required_control_resources` 不変（`06:38` ↔ A:142）、freshness = `min`（`06:274`）で FreshnessPolicy 迂回不能（frozen 検査 A:380 が先に走る）。執行: C-06（profile conjunct の評価点なし）。 |
| C9 code 名 / 設計宣言 | **PASS-WITH-CONDITIONS** | `P_*` 23 個（`06:336`）× frozen `E_*` 45 個（A 全文 grep）衝突 0；frozen 4 file に `P_` / `R_` 0 hit；06 内で使用 code と list の差分 0（機械照合）；06 が参照する `R_*` 8 個は全て `05:541-551` に存在；§8 は「設計時宣言 — impl CLOSED」（`06:332`）。条件: PT-01（`06:344`）が §3（`06:228`）と矛盾（C-02）。 |
| C10 header / 境界 / open / fold-map / anchors | **PASS（存在）／FAIL（正確性）** | header pin `06:5` = 再測 pin と一致（4 sha + 2 commit）；境界 §9（10 項）；open §10（10 項・open = 0 非宣言）；fold-map §11（handoff 6/7/9/B/F/D → 節）；anchors §12（16）。正確性: anchor 1 (`06:407`) / 2 (`06:408`) / 6 (`06:412`) / 7 (`06:413`) は本文 `06:186` / `06:228` / `06:172,234` / `06:114,235` に反する（C-01〜C-04）。anchor 16 の WARN 説明は私の sed 再確認で成立。C-16（時刻）。 |

## 3. Verdict（軸 C）

**HOLD** — 未反証の CRITICAL 1（C-01）+ HIGH 5（C-02, C-03, C-04, C-05, C-06）。全修正案は frozen 4 file を触らない（runtime / deployment 層の field 削除・1 文追加・評価点の明記）ため、修正後は PASS-WITH-CONDITIONS（MEDIUM: C-07〜C-13 を open として記録）へ移行し得る。C-18 は軸 B へ引き渡す（本軸 verdict 非算入だが fold 時に必ず扱う）。

## 4. 反証記録（自分の finding に対する 3 lens）

| id | text lens | logic lens | impact lens | 判定 |
|---|---|---|---|---|
| C-01 | `06:186` 逐語に「INTRINSIC 項はこの値で評価」あり、`06:226` に「無し」あり — 両立不能 | 既知 field ⇒ strict codec 通過、⊆ code 不在 ⇒ 検査通過 | 10 倍 bounds で gateway 評価 = 物理事故 | 未反証 |
| C-02 | `06:228` に `p ≤` 逐語、`06:344` に「等値のみ」逐語 | INV-12 との不一致は 2 doc の判定分裂 | 周期変更 = uncertified dynamics | 未反証 |
| C-03 | `06:172` 逐語「HOLD を選択可」 | enum 2 値とも合法・code 無し | fail-closed action の緩和 | 未反証 |
| C-04 | `06:114` 逐語 vs `05:400` 逐語（初回 admit は出力） | 登録時 mode 未知 ⇒ 検査空振り | 安全層 health 無しで物理 1 command | 未反証 |
| C-05 | `05:400` vs `05:424` / INV-09 逐語 | 1 command は「0 件」に反する | 非 authority 帰属の破れ | 未反証 |
| C-06 | `05:473-478` に conjunct 行なし（grep 0） | 評価者不在 ⇒ 述語は無効 | 強化の主張が偽 | 未反証 |

## 5. 補足（instrument の偽陽性・限界）

- I2 は `DeploymentEvidenceRecord.profile_hash`（`06:291`）を拾うが、これは record 型の正当な back-reference（top-level profile ではない）— 手動除外。ただし C-08 の間接循環はこの参照経由。
- I5 / I7 は規則を述べる散文（`06:254`, `06:402`, `06:348`）にも反応する — 手動で「規則の記述」と「規則の逸脱」を弁別した。
- 本 instrument は 06 の構造（code block 内の dataclass）に依存し、frame 欠落（C-11）や評価点欠落（C-10）は検出できない — これらは 05 との結線読解で得た。
