# V4 — R4 finding 19 件の adversarial verification（WMSO v0.2.5・3 lens）

- verifier: V4（別 context・R4 report は読んだが verdict は本 verifier が sed / grep で独立に再現）。実施 = 2026-09-05 02:05 UTC（`date -u` 実測）。
- 対象 = `$PKG/06_WMSO_INDUSTRIAL_PROFILE_v0.2_REVIEW_CANDIDATE_20260903.md`（sha256 `184d71e8…` = SHA256SUMS 一致・実測）／ `$PKG/05_WMSO_RUNTIME_SPEC_v0.2_REVIEW_CANDIDATE_20260903.md`（sha256 `c3c8b5ba…` = SHA256SUMS 一致・実測）。repo HEAD `50119dc3`（`git log -1` 実測）。`$PKG` = `eval_runs/troot_optE_dapg_wholeroute_scope_20260701/WMSO_EXACT_BASELINE_REVIEW_v0.2_20260903`、`$D` = `eval_runs/troot_optE_dapg_wholeroute_scope_20260701`。
- 凍結物・`eval_runs/` は未編集。書出しは本 file と `V4_results_v025.json` のみ。

## 0. 前提の実測と規則

- pin 再測 `bash $PKG/verify_exact_baseline_pins.sh` = **11/11 PASS**（D1.1-A `00192d20…` / EP md `c474acea…` / EP json `e63176af…` / D1.1-B `5a1874d3…` / blob id・frozen commit・definition hash `e7ca4309…` 全 PASS、`policy_semver=1.9.0`）。
- checker `python3 $PKG/check_review_candidate.py $PKG/06_… --profile` = **FAIL=0 / WARN=24**（全て C2 heuristic・citations=61）。⚠ checker Q1 は「使用 P_* が §8.1 に列挙」「PT/OPP 連番」のみで、規範集合 → P_* の結線や 05/06 間の enumeration 網羅は検査外（R4 §0.2 の指摘は正）。
- 規則（VERIFY.md）: finding ごとに text / logic / impact の 3 lens を独立に走らせ**反証を試みる**。再現できなければ refuted。2 lens 以上が refute しなければ confirmed。Rs 裁定 4 件（B+ / CellSafetyTimingBaseline / append-only registry + two-key / validity deadline + event）の**方向**は defect でない（impact lens で refute）。方向の**実現不全**は defect。rate / hold / control_mode 等値のみ = 拘束前提。
- 完全重複の merge: 19 件を突合し**完全重複は 0**。近接対（R4-03 ↔ R4-15 = ENV#CLOCK_ANOMALY、R4-02 ↔ R4-09 = hash 照合、R4-06 ↔ R4-07 = §5 (b) 周辺、R4-03 (e) ↔ R4-13 = fault-injection 集合）はいずれも対象 field / 規則が異なり別 finding として維持（`merged_into = ""`）。
- 判定語彙: **refuted** = その lens で defect を再現できない／**not refuted** = 再現できる。severity は本 verifier の判定（R4 と異なる場合は理由を書く）。

## 1. 総括

| id | R4 sev | text | logic | impact | verdict | V4 sev |
|---|---|---|---|---|---|---|
| R4-01 | HIGH | not refuted | not refuted | not refuted | **CONFIRMED** | HIGH |
| R4-02 | HIGH | not refuted | not refuted | not refuted | **CONFIRMED** | HIGH |
| R4-03 | HIGH | not refuted | not refuted | not refuted | **CONFIRMED** | HIGH |
| R4-04 | HIGH | not refuted | not refuted | not refuted | **CONFIRMED** | HIGH |
| R4-05 | MEDIUM | not refuted | not refuted | not refuted | **CONFIRMED** | MEDIUM |
| R4-06 | MEDIUM | not refuted | not refuted | not refuted | **CONFIRMED** | MEDIUM |
| R4-07 | MEDIUM | not refuted | not refuted | not refuted | **CONFIRMED** | LOW（前例 R1-15 / R2-12 と同一行・05 側は正） |
| R4-08 | MEDIUM | not refuted（「第 3 の評価点」部分は refuted） | not refuted | not refuted | **CONFIRMED** | MEDIUM |
| R4-09 | MEDIUM | not refuted | not refuted（範囲を縮小） | not refuted | **CONFIRMED** | MEDIUM |
| R4-10 | MEDIUM | not refuted | not refuted | not refuted（(b) は OPP-16 で部分 defer） | **CONFIRMED** | MEDIUM |
| R4-11 | MEDIUM | not refuted | not refuted | not refuted | **CONFIRMED** | MEDIUM |
| R4-12 | MEDIUM | not refuted | not refuted | not refuted | **CONFIRMED** | MEDIUM |
| R4-13 | MEDIUM | not refuted | not refuted（R4 の修正案は 2 code で循環 — 修正案を差替え） | not refuted | **CONFIRMED** | MEDIUM |
| R4-14 | MEDIUM | not refuted | not refuted | not refuted | **CONFIRMED** | MEDIUM |
| R4-15 | MEDIUM | not refuted | not refuted | not refuted | **CONFIRMED** | MEDIUM |
| R4-16 | LOW | not refuted（一部 refuted） | not refuted | not refuted | **CONFIRMED** | LOW |
| R4-17 | LOW | not refuted | not refuted | not refuted | **CONFIRMED** | LOW |
| R4-18 | LOW | not refuted | not refuted | not refuted（第 2 論点は refuted） | **CONFIRMED** | LOW |
| R4-19 | LOW | refuted（§9 項 1 に AND 関係あり） | refuted | refuted | **REFUTED** | NOT_A_DEFECT |

集計: confirmed **18** / refuted **1** / merged **0**。severity（V4）: HIGH 4 / MEDIUM 10 / LOW 4 / NOT_A_DEFECT 1。CRITICAL 0。

## 2. finding 別の検証

### R4-01 — CONFIRMED / HIGH — INTER_ARM 項が「両腕同時 command」の相互接近を評価しない・`cmd.resource` が 05 に無い

**sed 確認**: 06:109「評価 = 05 §4.3 INTER_ARM 項（両 arm の同一線形化 snapshot で連言評価）」／ 06:321 `CONTROLLER(profile.cell.arms[cmd.resource].controller)`／ 05:457「評価対象 = 提案 command の arm の線分 × 他 arm の**同一線形化 snapshot**での状態（直近 admitted setpoint または実測）」／ 05:459「INTER_ARM は両 arm の状態を同一 snapshot で連言評価」／ 05:738 INV-36／ 05:780 T-37「左右 arm の提案線分が pairwise keep-out を跨ぐ command」／ 05:291「両 arm の提案状態が … に反する」／ 05:496-504 `GatewayCommand = {lease_id, control_epoch, executor_id, cmd_seq, kind, payload_ref, t_issue}`。`grep -n "\.resource\|resource_id\|arm_targets" 05` = **0 hit**（05 に arm を識別する field は無い）。frozen: RS71 `:23`（BOTH arms in EVERY motion）、tensor_binding `:129-134`（`ActionBinding` は policy step あたり 1 つ・全 feature）、contracts_v2 `:142`（`ControlResourceSpec = {ee_left, ee_right, gripper_left, gripper_right}`）。

- **text**: not refuted。05:457 は「他 arm の状態」を**直近 admitted setpoint または実測**と定義し、他 arm の**提案**状態ではない。一方 05:291 / T-37 は「両 arm の提案状態 / 提案線分」を前提に書かれており、同一 doc 内で評価対象の定義が食い違う。06:321 の `cmd.resource` は 05 の command 記録形に存在せず（grep 0）、両腕同乗 command を arm ごとに分解する規則も 05 / 06 のどこにも無い。
- **logic**: not refuted。R4 の反例（左 −0.30→0、右 +0.30→0、`min_separation_m` = 0.20）を 05:457 の定義で評価: 左線分 × 右の直近 admitted（+0.30）= 最短 0.30 → admit、右線分 × 左の直近 admitted（−0.30）= 0.30 → admit。両終点が一致するのに TRUE。**反証を試みた読み**（per-arm 逐次 command 模型: 左を admit した後に左の直近 admitted が 0 に更新され、右線分 × 左点 0 = 0 で FALSE）は健全だが、(i) RS71 #1 + D1.1-B「1 policy step = 1 ActionBinding（全 feature）」+ 05 §2「executor は D1.1-B の rate/hold で command を出す」の下では LEARNED skill の 1 step = 両腕の setpoint を含む 1 command であり、(ii) 05:457 の「同一線形化 snapshot」は両腕を**同じ**事前状態に対して評価することを要求するため、逐次更新の読みは本文から導けない。加えて `cmd.resource` が未定義ゆえ per-arm 模型でも CONTROLLER 項は評価不能 = FALSE（全拒否）。健全かつ機能する読みは本文に存在しない。
- **impact**: not refuted。Rs 裁定 OPP-15 条件 ⑤（`A_GPT56sol_20260904.md`「CommandGateway は両 arm の**提案状態**を同一 snapshot で連言評価」）を本文が実現していない（他 arm = 提案でなく直近 admitted）。DUAL-ARM cell の主要衝突様態（相互接近）が gateway 項を素通りし、06:109 の「ISL は AND・代替ではない」が実質 ISL 単独になる。裁定の方向への異議ではなく実現不全。
- **verdict / severity**: CONFIRMED / **HIGH**（R2-04・R1-05 と同格: 安全項が主要 case で空文）。
- **minimal fix（凍結 delta 0）**: (1) 05:457 を「command が複数 arm の setpoint を含む場合は**両提案線分の対**（同一時間 parameterization）を min separation / pairwise keep-out / swept-volume predicate で評価；command に含まれない arm は直近 admitted setpoint（無ければ実測 TCP）を静止線分として評価」に書き換え、05:459 / INV-36 の「両 arm の状態」を「両 arm の提案状態（含まれない arm は直近 admitted）」に揃える。(2) 05 §5.1 `GatewayCommand` に runtime 層 field `arm_setpoints: tuple[tuple[str, str], ...]`（(resource_id, setpoint_ref)・resource_id ∈ frozen ControlResourceSpec の key）を追加し、LEARNED では `tensor_binding_hash` が指す semantic action schema の feature → resource_id 写像を `BindingExpectation` の readback（等値）として記録すると書く。(3) 06:321 の `cmd.resource` を `∀ r ∈ cmd.arm_setpoints: arms[r].controller` に置換。T-37 は変更不要。

### R4-02 — CONFIRMED / HIGH — v0.2.5 の content hash 3 種が (j) enumeration と gateway 評価前再検証に無い（R1-05 / R2-03 の再発）

**sed 確認**: 06:306「profile が宣言する**全ての** content hash（`predicate_sha256` / `evaluator_sha256` / `zones[].geometry_sha256` / `calibration_refs[].artifact_sha256` / `gateway_config.artifact_sha256` / `deployment_evidence_policy_hash` / required record の `artifact_sha256`）… gateway は predicate と zone geometry を評価前に再検証」／ 05:190 (j) 同一列挙／ 05:454・05:456 に「評価前に … 再検証し、不一致 = FALSE」あり、05:457 INTER_ARM 行には無し／ 06:100「05 §3.4 (j) の照合対象」・06:302「content hash は 05 §3.4 (j) の照合対象」・06:454 PT-38「次 permit で 05 `R_PROFILE_MISBOUND`（(j)）」。`grep -n "kinematic_layout\|pairwise_keepout\|swept_volume" 05` = **0 hit**。frozen: tensor_binding `:23`（content-addressed 参照の規律・06 §2.3 が同型適用）、RS71 `:24`。

- **text**: not refuted。「全ての」の直後に閉じた列挙があり、R1-05 / R2-03（verifier V1 / V2 = CONFIRMED HIGH、`11_` §7）で「列挙が規範」と確定した構造そのもの。06:100 / 06:302 / PT-38 は「(j) の照合対象」と**主張**するが、(j) 側（05:190）と 06:306 の列挙に 3 種は無く、05 に該当語は 0 hit。主張と規則が食い違う（fold script の編集漏れ）。
- **logic**: not refuted。受理後に `swept_volume_predicate_refs[0]` の解決先を常時 admit の述語に差替え → (j) は照合しないので `R_PROFILE_MISBOUND` 無し → 05:457 に評価前再検証句が無いので差替え内容で評価 → admit。layout artifact も同様で PT-38 の期待は本文から出ない。「評価不能 = FALSE」は差替え述語が評価可能ゆえ効かない。
- **impact**: not refuted。content-addressed 束縛は照合して初めて効く（06 §2.3・tensor_binding `:23`）。layout artifact は RS71 #2 固定 base の意味内容を持つ。R1-05 / R2-03 と同型・同 severity。
- **verdict / severity**: CONFIRMED / **HIGH**。
- **minimal fix（凍結 delta 0）**: 06:306 と 05:190 (j) の列挙に `cell.kinematic_layout.artifact_sha256` / `inter_arm_restrictions.pairwise_keepout_sha256[]` / `inter_arm_restrictions.swept_volume_predicate_sha256[]` を追加。05:457 に「gateway は評価前に layout・pairwise keep-out・swept-volume predicate の解決内容 sha256 を再検証し、不一致 = 評価不能 = FALSE」を追加。再発防止（任意）: 列挙を「§2.1 で名に `_sha256` / `artifact_sha256` / `_hash` を持つ全 field」と定義し、checker に O9「06 §2.1 hash field 名 ⊆ (j) 列挙」を追加（package 内 helper・凍結 delta 0）。

### R4-03 — CONFIRMED / HIGH — `MIN_NEGATIVE_CONTROLS` が P_* に結線されず、subject 文法が既存規則と衝突して満たせない

**sed 確認**: 06:382-383 MIN_NEGATIVE_CONTROLS = FAULT_INJECTION_RESULT with subject_ref ∈ {CELL#…×4, ENV#…×3}／ 06:376 comment「規範最小集合 … policy がこれを含まなければ P_EVIDENCE_POLICY_TOO_WEAK」／ 06:313 `P_EVIDENCE_POLICY_TOO_WEAK` = MIN_SHADOW / MIN_CLOSED_LOOP / MIN_FAULT_INJECTION のみ／ 06:367-372 `DeploymentEvidencePolicy` の field = required_before_shadow_lease / required_before_closed_loop_lease / fault_injection[] / validity_rule（negative control の宣言 field 無し）／ 06:363 `runtime_fault_code` = 05 enum 名必須・06:296 `P_FAULT_CODE_UNKNOWN`／ 06:354 subject 規則: FAULT_INJECTION_RESULT = fault code（timeout 駆動は `<code>#<trigger>`）、HEALTH_CHECK_RUN = check_id／ 06:295 `P_FAULT_EVIDENCE_UNVERIFIED` = 「`kind = FAULT ∧ fault == subject_ref の code`」／ 06:292 `P_EVIDENCE_UNBOUND` の FAULT_INJECTION_RESULT 句 = `∀ fault_injection[]`／ 06:289「`P_EVIDENCE_UNBOUND`（subject = check_id#NEGATIVE）」／ 05:627-641 `RuntimeFaultCode` に clock / scheduler の member 無し。`grep -i "negative" 05` = 0 hit、`grep -i "clock" 05` = 単調 clock の記述のみ。frozen: contracts_v2 `:385`（enum strict）。

- **text**: not refuted。MIN_NEGATIVE_CONTROLS を要求する P_* 行は無い（06:313 は 3 集合のみ）。06:376 の block comment を「MIN_NEGATIVE_CONTROLS にも適用」と読んでも、policy にそれを「含める」field が無く、`fault_injection[]` に `CELL#…` を書けば `P_FAULT_CODE_UNKNOWN`（06:296）ゆえ**含めようがない**。subject 文法 `CELL#…` / `ENV#…` / `check_id#NEGATIVE` は 06:354 の規則外。
- **logic**: not refuted。(A) 17 code の evidence だけの policy → 全 P_* = 0 → CLOSED_LOOP 受理可（negative control 0 件）。(B) 規則どおり `subject_ref = "CELL#INTER_ARM_COLLISION"` の record を置くと、06:295 が `fault == "CELL"` の audit record を要求 → 規則 (1) で違反。(C) `ENV#CLOCK_ANOMALY` は 05 に検出規則も fault も無く「拒否 / SafeHold 到達」を示す record を構成できない。(D) 副作用: `min_separation_m = 1e-9` + 常時 admit の predicate は `P_INTER_ARM_RESTRICTION_MISSING`（06:302 = `> 0 ∧ ≥ 1`）を通り、空虚さを排除する手段が無い。反証として「06:295 は required record にのみ適用」と読めば (B) は消えるが、その場合 negative-control record は**何にも検査されない**（(A) が残る）。いずれの読みでも結線が無い。
- **impact**: not refuted。Rs 裁定 OPP-15 ⑦「commissioning で左右入替・片腕欠落・base transform 改変・相互衝突の negative control を要求」と OPP-11 ③「clock 異常・scheduler stall・通信喪失を含める」の実現が**散文のみ**。本 doc の規範は機械検査 P_*（C-09 で min 集合を comment から規則へ昇格した経緯と同じ論点）。CLOSED_LOOP 受理が negative control 無しで通る = fail-closed bar 未達。
- **verdict / severity**: CONFIRMED / **HIGH**（v0.2.5 で足した negative-control 層が全面的に inert。ARM_SWAP / BASE_TRANSFORM_TAMPER は RS71 §0 不変前提の防護）。
- **minimal fix（凍結 delta 0）**: (1) `DeploymentEvidencePolicy.negative_controls: tuple[str, ...]`（⊇ MIN_NEGATIVE_CONTROLS でなければ `P_EVIDENCE_POLICY_TOO_WEAK`）を追加し、06:292 `P_EVIDENCE_UNBOUND` に「∀ negative_controls[]: subject_ref 一致の FAULT_INJECTION_RESULT が CLOSED_LOOP 前に存在」を足す。(2) 06:354 の subject 規則に `CELL#<control>` / `ENV#<control>` / `<check_id>#NEGATIVE` の文法を追記。(3) 06 §6.1 に negative control → 期待 audit 内容の固定写像表を置き（ARM_SWAP / ARM_MISSING → HEALTH_FAILED with check_id ∈ {CONTROLLER_IDENTITY, TOOL_IDENTITY}〔channel-complete swap は R4-14 の BASE_POSE_VERIFY〕；BASE_TRANSFORM_TAMPER → FAULT R_PROFILE_MISBOUND (j)〔R4-02 fold 後〕；INTER_ARM_COLLISION → FAULT R_INTER_ARM_VIOLATION；SCHEDULER_STALL → FAULT R_DEADLINE_MISS | R_TIMING_VIOLATION；COMM_LOSS → FAULT R_EXECUTOR_LOST | R_MANAGER_UNAVAILABLE | R_SAFETY_LAYER_LOST；CLOCK_ANOMALY → FAULT R_CLOCK_ANOMALY〔R4-15〕）、06:295 を subject 文法で分岐。(4) `ENV#CLOCK_ANOMALY` は R4-15 の 05 規則（`R_CLOCK_ANOMALY`）が入るまで MIN 集合に置かない（置くなら同時に入れる）。

### R4-04 — CONFIRMED / HIGH — two-key の機械検査が「2 role の存在」だけで、同一 `operator_ref` の 1 人で CLOSED_LOOP 受理に到達できる

**sed 確認**: 06:471 `approvals: tuple[tuple[str, str, str], ...]  # (role, operator_ref, t_iso8601)`（自由文字列）／ 06:486「CLOSED_LOOP_AUTHORITY を許す受理 = 『独立 safety approver』と『deployment / operations custody approver』の two-key。cell operator 単独では CLOSED_LOOP を許可できない」／ 06:491「検査: … CLOSED_LOOP 許可で two-key 不足 … = P_ACCEPTANCE_RECORD_INVALID」／ 06:453 PT-37／ 06:462 `record_hash = H_WCJ(record)`。`grep -n "operator_ref\|two-key\|相異\|distinct" 06` = 相異性・語彙・認証の規則は **0 hit**。frozen: contracts_v2 `:354`、EP `:156`。

- **text**: not refuted。「two-key 不足」の機械定義が無い。role は自由文字列（どの文字列が two-key の role か判定する語彙が無い）、`operator_ref` の相異は要求されず、approvals の認証も未言及（§6.3 の OP-5 / U14 は evidence record についてのみ）。OPP-13 の残 open は「role の**実名**（pS / pY 等）の割当」であり、role 語彙の enum 化・相異性はそれと衝突しない。
- **logic**: not refuted。`op-7` が `(("independent safety approver","op-7",t1), ("deployment/operations custody approver","op-7",t2))` を書く → 「2 role が存在」の読みでは通る。反証として規則 (1)（評価不能 = 違反）を当てると「two-key 不足」は評価不能 → **全 record が受理拒否**（機能しない）。空文か充足不能かのいずれかで、健全かつ機能する読みは無い。PT-37 の期待は本文の検査定義から出ない。
- **impact**: not refuted。Rs 裁定 OPP-13 ③④（two-key・operator 単独不可）と根拠「不変前提に反する profile を単独 operator が受理する経路を閉じる」の実現不全。frozen `two_key_state`（O0/S0/V0）が AND で残るため実害は緩和されるが、deployment 層の受理 gate が one-key に潰れることは裁定の核心に反し、PT-37 は本文が裏付けない主張になる。
- **verdict / severity**: CONFIRMED / **HIGH**（裁定された機構の機械実現が不在・test plan が本文で裏付けられない）。
- **minimal fix（凍結 delta 0・role 実名の割当〔OPP-13 残 open〕には触れない）**: (1) deployment 層 `AcceptanceRole(Enum): SAFETY_APPROVER | CUSTODY_APPROVER | COMMISSIONING | INDEPENDENT_REVIEWER | OPERATOR | MONITOR` を置き `approvals[].role` を enum に。(2) 06:491 を「CLOSED_LOOP ∈ allowed_lease_modes ⇒ ∃ SAFETY_APPROVER ∧ ∃ CUSTODY_APPROVER ∧ 両者の operator_ref が相異 ∧ いずれも OPERATOR の operator_ref と相異；SHADOW のみ ⇒ ∃ COMMISSIONING ∧ ∃ INDEPENDENT_REVIEWER ∧ 相異」と機械定義に書き換え。(3) 「approvals の真正性（署名）は 05 OP-5 / U14 defer に従い、record_hash は真正性を主張しない」を §8.3 / §9 に 1 文。（role_registry_hash は任意 — OPP-13 残 open へ carry で可。）

### R4-05 — CONFIRMED / MEDIUM — `P_SAFETY_BUDGET_EXCEEDED` の入力 schema が無い

**sed 確認**: 06:285「`safety_heartbeat_timeout_s + detection + gateway 切替 + measured stop time ≤ baseline.safety_response_budget_s`（measured 値は CONTROLLER_ENVELOPE_MEASUREMENT / FAULT_INJECTION_RESULT の実測 artifact）」／ 06:305 CONTROLLER_ENVELOPE_MEASUREMENT の artifact schema = 「ControllerEnvelope と同一 field」／ 06:112-119 `ControllerEnvelope` に停止時間 field 無し（`stop_class` のみ）／ 06:355 FAULT_INJECTION_RESULT の `artifact_ref` = 05 `RuntimeAuditRecord` の (lease_id | None, seq 範囲)／ 06:383「timeout 駆動 code の FAULT_INJECTION_RESULT artifact は … 実測上限を含み、P_SAFETY_BUDGET_EXCEEDED の入力になる」／ 06:411 規則 (1)。`grep -i "stop time\|stop_time\|detection" 06` = 06:285 と 06:482（diagnostic_items の worst-case detection）のみ。frozen: D0 `:321`（response_budget = worst-case, not mean）。

- **text**: not refuted。06:355（artifact = audit 範囲）と 06:383（artifact が実測上限を「含む」）が同一 kind の artifact について矛盾し、measured stop time を置く schema はどこにも無い。
- **logic**: not refuted。書面どおり artifact を作ろうとすると stop time の置き場が無く、audit 範囲から detection を導く record 対も未定義（物理停止完了は audit に無い）。規則 (1) では評価不能 = 違反 → CLOSED_LOOP 受理が常に不成立。実装者が独自に読めば解釈であって機械検査ではない。
- **impact**: not refuted。Rs 裁定 OPP-11 ④ の end-to-end 予算検査の実現不全（方向は正しく、入力の型が無い）。方向は fail-closed（全拒否）で fail-open ではない。
- **verdict / severity**: CONFIRMED / **MEDIUM**。
- **minimal fix（凍結 delta 0）**: 06 §6.1 に deployment 層 `SafetyResponseMeasurement` を定義: CONTROLLER_ENVELOPE_MEASUREMENT の artifact schema を「ControllerEnvelope 同一 field + `stop_time_worst_case_s: dict[StopClass, CanonicalDecimal]`」に、timeout 駆動 code の FAULT_INJECTION_RESULT artifact を「`{audit_range, detection_to_safehold_worst_case_s, gateway_switch_worst_case_s, load_condition_ref}`」の構造体に（06:355 の「audit 範囲に解決」は `audit_range` 成分として保つ）。06:285 の各項の出所を field 名で書く（detection = FAULT_INJECTION_RESULT[R_SAFETY_LAYER_LOST#HEARTBEAT_TIMEOUT].detection_to_safehold_worst_case_s 等）。

### R4-06 — CONFIRMED / MEDIUM — `validity_deadline_mono` の `issued_at + lease_max_duration_s` 項が INV-33 と衝突し fault 名も誤る

**sed 確認**: 05:183「min(calibration 期限, evidence 期限, acceptance 期限, issued_at + lease_max_duration_s)」／ 05:289 row（from = S_LEASE_ACTIVE / S_HEALTH_PENDING / **S_BOUNDARY_WAIT**、fault = R_CALIBRATION_EXPIRED_ACTIVE | R_EVIDENCE_EXPIRED_ACTIVE）／ 05:288 row（lease_max_duration 超過 = from S_LEASE_ACTIVE / S_HEALTH_PENDING のみ・TIMING_VIOLATION・R_LEASE_DURATION_EXCEEDED）／ 05:735 INV-33（**LEASE_ACTIVATED 起点**・S_BOUNDARY_WAIT は `boundary_dwell_s` 枠・R1-15 / R2-12 fold）／ 05:736 INV-34・06:337（deadline = 「既知の期限を manager clock へ変換」— lease 時間の言及無し）／ 05:424。frozen: D0 `:354-355`。

- **text**: not refuted。同じ上限が 2 つの起点（permit 発行 / LEASE_ACTIVATED）で二重化され、row 289 は S_BOUNDARY_WAIT を含み、fault は「失効」系のみ。06:337 / INV-34 は deadline を「既知の期限」と説明し lease 時間項を含まない — 05:183 だけが外れている。
- **logic**: not refuted。R4 の trace（600 s・TERMINAL at 598 → S_BOUNDARY_WAIT、t=600 に row 289 発火）は成立。さらに `issued_at < activated_at` ゆえ CLOSED_LOOP lease では row 289 が row 288 より**常に先に**発火し、`R_LEASE_DURATION_EXCEEDED` 経路は CLOSED_LOOP で到達不能になり、audit は失効していないのに `R_EVIDENCE_EXPIRED_ACTIVE` を記録する（R2-01 の subject 照合と噛み合わない）。方向は保守側（SAFEHOLD）だが仕様矛盾。
- **impact**: not refuted。外部推奨の式を字義どおり写した結果だが、Rs 裁定は方向（deadline + event + 診断）であり、同 doc の INV-33（verified fold）と矛盾する写し方は実現不全。audit 意味論・boundary 再選択の可用性に影響。
- **verdict / severity**: CONFIRMED / **MEDIUM**。
- **minimal fix（凍結 delta 0）**: 05:183 の min から `issued_at + lease_max_duration_s` を外す（lease 時間上限は INV-33 / row 288 が担う）。row 289 の from は据置き可（calibration / evidence / acceptance 期限は boundary でも効く）。06 は変更不要。

### R4-07 — CONFIRMED / LOW — 06 §8.1 規則 (3) が §5 (b) / 05 §4.5 と矛盾・`accepted_profiles` 残存

**sed 確認**: 06:411「(2) profile の受理（`profile_hash` の登録 = `accepted_profiles` への追加・…）… 旧 v0.2.4 の 2 集合 accepted / revoked は `ProfileAcceptanceRecord` に置換 … (3) … **活性 lease は継続し**、次の permit 発行が … 不成立になる（§5 (b)）。**profile 起因で** lease を無効化するのは health check 失敗（§5 (a)）と `lease_max_duration_s` 超過（CD-10）のみ」／ 06:337 §5 (b)「CLOSED_LOOP_AUTHORITY lease では次 permit まで待たない … TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED) … event で即時無効化」／ 05:478 §4.5（validity deadline / event / SUSPENDED を列挙）。`grep accepted_profiles` = 06:411 と 05:190 (i) の「旧 … は本 record に置換」の 2 箇所のみ。fold_v025.py は §8.1 (2) の別文（registry 状態 = {ACCEPTED, REVOKED} の文）を置換し、(3) は触れていない。

- **text**: not refuted。(3) の「継続」「のみ」は §5 (b) と字義どおり逆。(2) の `accepted_profiles` は同一文中で「置換済」と自己訂正されているが、語は残る。
- **logic**: not refuted。(3) を規範として読めば CLOSED_LOOP lease は calibration 失効後も継続 — §5 (b) / 05:289 と逆。同一行が R1-15 / R2-12 で一度直された後、別 fold で再び矛盾（fold script が §5 だけを書き換え §8.1 (3) を触っていない）。
- **impact**: not refuted（様式ではなく規範文の矛盾）。ただし 05（lease 無効化の SSOT）と 06 §5 (b) は正しく整合しており、stale なのは要約規則 (3) のみ。前例（R1-15 / R2-12 同一行 = CONFIRMED LOW）と同格。
- **verdict / severity**: CONFIRMED / **LOW**（R4 = MEDIUM から下げ: 規範本体は正・要約文の陳腐化）。
- **minimal fix（凍結 delta 0）**: 06:411 (3) を「SHADOW lease は継続し次 permit で不成立；CLOSED_LOOP lease は §5 (b) の deadline / event で無効化（05 §4.5）。profile 起因の無効化 = §5 (a)・§5 (b)・CD-10」に書き換え、(2) の「`accepted_profiles` への追加」を「`ProfileAcceptanceRecord(state = ACCEPTED)` の append」に置換。

### R4-08 — CONFIRMED / MEDIUM — 第 2 評価点の P_* 集合が 06 §3 / 06 §8.1 (2) / 05 §3.4 (b) で一致しない

**sed 確認**: 06:284 `P_TIMEOUT_CEILING`「評価 = 受理時 + 第 2 評価点」／ 06:295 `P_FAULT_EVIDENCE_UNVERIFIED`「（第 2 評価点・…）」／ 06:305 `P_ENVELOPE_EXCEEDS_MEASURED`「評価点 = 第 2 評価点・CLOSED_LOOP のとき」（R2-11 fold）／ 06:289 negative-control HEALTH_CHECK_RUN「CLOSED_LOOP lease 前」／ 06:411 (2)「第 2 評価点 = permit 発行時（… `P_CALIBRATION_EXPIRED` / `P_EVIDENCE_UNBOUND` / `P_EVIDENCE_EXPIRED` / `P_EXPECTATION_UNCERTIFIED` を再評価 — 05 §3.4 が `R_PROFILE_MISBOUND` として扱う）」「評価点は 2 つ」／ 05:190 (b) 同じ 4 code のみ（(j) は content hash を別項で持つ）。

- **text**: not refuted（主論点）。§3 表が「第 2 評価点」と指定する `P_TIMEOUT_CEILING` / `P_ENVELOPE_EXCEEDS_MEASURED` / `P_FAULT_EVIDENCE_UNVERIFIED`（および `P_SAFETY_BUDGET_EXCEEDED`）が、第 2 評価点を定義する 06:411 (2) と 05:190 (b) の閉じた列挙に無い。「受理時 = 第 3 の評価点」の部分は **refuted**: 06:411 (2) は「profile の受理（`profile_hash` の登録 …）」と受理と登録を同一視しており、「受理時」= 第 1 評価点と読める（ただし用語の二重化は残る）。
- **logic**: not refuted。05 §3.4 (b) に従う実装は permit 発行時に `P_ENVELOPE_EXCEEDS_MEASURED` を評価しない。R2-11 で「登録時には評価できない」と確定した code ゆえ、(b) に無いと**どこでも評価されない** → 宣言 envelope > 測定値が CLOSED_LOOP lease に入る（fail-open）。`P_TIMEOUT_CEILING` の baseline 差替え検出も同様。
- **impact**: not refuted。05 ↔ 06 の結線欠陥（CD-13 / R1-15 級）で、結果は fail-open 側。
- **verdict / severity**: CONFIRMED / **MEDIUM**。
- **minimal fix（凍結 delta 0）**: 06 §8.1 (2) を「第 1 = 登録（PROPOSED record 起票）時、第 2 = permit 発行時。受理（ACCEPTED record の append）は第 1 評価点の結果 + `P_ACCEPTANCE_RECORD_INVALID` で確定し新たな評価点ではない」と書き、第 2 評価点集合を閉じた列挙で更新（既存 4 + `P_TIMEOUT_CEILING` / `P_SAFETY_BUDGET_EXCEEDED` / `P_ENVELOPE_EXCEEDS_MEASURED`（CLOSED_LOOP）/ `P_FAULT_EVIDENCE_UNVERIFIED` / `P_PREDICATE_HASH_MISMATCH`（= (j)）/ negative-control 句）。05:190 (b) を「doc 06 §8.1 (2) の第 2 評価点集合の全て」と参照形にし、06:284 の「受理時」を「第 1 評価点」に統一。

### R4-09 — CONFIRMED / MEDIUM — `ProfileAcceptanceRecord` の束縛 hash が runtime で照合されない

**sed 確認**: 06:467-470（`validator_hash` / `evidence_set_hash` / `timing_baseline_hash` / `cell_identity_hash`）／ 05:190 (i)（state / mode / 有効期限のみ・record hash / generation を束縛）／ 05:239 条件 12（state / generation / mode）／ 06:292（required record の存在は「evidence store」に対して）／ 06:284（baseline = 「record の `timing_baseline_hash` が指す content-addressed artifact」）。`grep -n "evidence_set_hash\|validator_hash\|cell_identity_hash" 05 06` = 06:467-470 の定義行のみ（比較規則 0 hit）。frozen: tensor_binding `:23-24`。

- **text**: not refuted。4 field とも比較規則が無い。`timing_baseline_hash` は第 2 評価点で**解決して使う**（06:284）にもかかわらず、解決内容の sha256 再検証は (j) にも §8.3 にも無い（R1-05 が確立した「解決するなら再検証」の規範に反する）。
- **logic**: not refuted（範囲を縮小）。(a) `timing_baseline_hash`: 解決先差替えは検出されない（content-addressed store が lookup 時に自己検証すると本文は言っていない）。(b) `validator_hash`: 第 2 評価点の validator 版の drift は検出されない。(c) `evidence_set_hash`: R4 の E′ ⊋ E の trace は成立するが、「追加 evidence = 新 generation」を要求するかは裁定に無い設計選択（TCP 再 calibration のたびに two-key を要する運用になる）— 本 verifier は「意味論を明記する」までを最小修正とし、部分集合要求は選択肢として提示。(d) `cell_identity_hash`: `profile_hash` が CellIdentity を含むため冗長・比較先未定義（無害）。
- **impact**: not refuted。Rs 裁定 OPP-13 ③ は record が validator / evidence / baseline / cell identity を「固定」することを要求し、その目的は「受理根拠の固定」。(a)(b) は enforcement の欠落、(c)(d) は意味論の欠落。
- **verdict / severity**: CONFIRMED / **MEDIUM**（(a) が R4-02 と同型で MEDIUM、他は LOW 級）。
- **minimal fix（凍結 delta 0）**: 05 §3.4 (i) に「`record.timing_baseline_hash` の解決内容 sha256 を再検証（不一致 = `R_PROFILE_MISBOUND`）；第 2 評価点の validator content hash == `record.validator_hash`（不一致 = `R_PROFILE_MISBOUND`・validator 更新 = 新 generation）」を追加。06 §8.3 に `evidence_set_hash` の意味論を 1 文で明記（provenance のみ か、「第 2 評価点の required 集合は当該集合の部分集合で充足・追加 evidence は新 generation」か — 後者が保守側）。`cell_identity_hash` は「== H_WCJ(profile.cell)」を `P_ACCEPTANCE_RECORD_INVALID` に入れるか削除。

### R4-10 — CONFIRMED / MEDIUM — `CellSafetyTimingBaseline` が cell に束縛されず、baseline 改訂が失効 trigger に無い

**sed 確認**: 06:476-482（field = baseline_id / approved_ceiling / safety_response_budget_s / worst_case_attempt_s / selector_overhead_s / diagnostic_items — `cell_id` / `layout_revision_ref` 無し）／ 06:487（失効 trigger = incident・ISL 異常・layout / tool / controller / safety-layer identity の変更）／ 05:88（monitor event 一覧に baseline 無し）／ 06:525 OPP-16・05:817 OP-20（発行主体・改訂手続・値の根拠 = RT0 へ carry）。frozen: D0 `:321`、RS71 `:24`。

- **text**: not refuted。baseline に cell / layout を識別する field が無く、`P_TIMEOUT_CEILING`（06:284）は artifact の値だけを見る。baseline supersession は event 一覧に無い。
- **logic**: not refuted。(a) 実験室 cell 向け baseline（`approved_ceiling[safety_heartbeat_timeout_s] = 5 s`）を本番 cell の record に束縛 → 全 P_* 通過（two-key 承認者の目視のみ）。(b) RT0 が ceiling を 1/2 に改訂しても旧 hash を束縛した ACCEPTED record は `valid_until` まで有効。
- **impact**: not refuted（(b) は部分 refuted）。(a) は D0 `:321`「cell 固有の worst-case」の実現不全で、PT-36 が閉じた「同率巨大化」と同型の silent fail-open（要人為ミス）。(b) は OPP-16 が改訂**手続**を loud に RT0 へ carry しているが、「改訂されたら既存 record をどうするか」は runtime 側の event 定義であり 05 / 06 の責務 — 1 行の event 追加で閉じる。
- **verdict / severity**: CONFIRMED / **MEDIUM**。
- **minimal fix（凍結 delta 0）**: `CellSafetyTimingBaseline` に `cell_id: str` と `layout_revision_ref: str`（または `cell_identity_hash`）を追加し、`P_TIMEOUT_CEILING` の前提に `baseline.cell_id == profile.cell.cell_id ∧ baseline.layout_revision_ref == profile.cell.layout_revision_ref` を入れる。06:487 / 05:88 の event に「timing baseline の supersession（baseline registry が旧 hash を superseded と記す）」を追加し、該当 record を SUSPENDED、活性 CLOSED_LOOP lease は TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED)。OPP-16 に「supersession の通知経路は RT0 側」と残す。

### R4-11 — CONFIRMED / MEDIUM — event 起因 VALIDITY_EXPIRED に clearance が無く自動再 lease が可能／SAFE_STOP 強化の判定材料が無い

**sed 確認**: 05:529 §5.4 VALIDITY_EXPIRED 行「追加前提 = なし（VALIDITY_EXPIRED では新 permit が §3.4 (b)(i) で失効原因の解消を要求する）」／ 05:290（event 一覧・fault = R_PROFILE_SUSPENDED | R_EVIDENCE_EXPIRED_ACTIVE・「SAFE_STOP if 安全起因」）／ 05:289・05:338「SAFE_STOP if 失効項目が安全機能・TCP・停止性能に関わる」／ 06:487「認可された一者または自動 monitor … が即時 SUSPENDED に**できる**」／ 06:462-473（record に reason 無し）／ 06:210-216（`EscalationPolicy` に validity 系 knob 無し）／ PT-13・T-33（TCP → SAFE_STOP のみ明示）。`grep "SuspensionReason\|on_validity_expired"` = 0 hit。frozen: D0 `:318-320`。

- **text**: not refuted。05:529 の「(b)(i) が失効原因の解消を要求する」は、record が SUSPENDED にされない event（incident 等）については成立しない（(b) は期限・(i) は record state しか見ない）。SAFE_STOP 判定の材料（item / reason → disposition 表）は無い。
- **logic**: not refuted。monitor が「incident」を通知し record を SUSPEND しない（06:487 は「できる」= 任意）→ SAFEHOLD(VALIDITY_EXPIRED) → 次 boundary で §3.4 (a)(b)(i) が全て pass → 人の clearance なしに CLOSED_LOOP 再 lease。fault は `R_EVIDENCE_EXPIRED_ACTIVE`（失効していない）。SAFE_STOP 判定: 規則 (1) を当てれば常に SAFE_STOP（散文と不一致）、当てなければ常に HOLD（安全側でない）。
- **impact**: not refuted。Rs 裁定 OP-19 ④「失効時の自動継続を禁止する fail-closed 方向の追加制約」と 05:529 の自己主張の実現不全。裁定は「SUSPENDED に**できる**」と書くが、event で lease を落としながら record を落とさない経路を許すと自動継続が復活する。
- **verdict / severity**: CONFIRMED / **MEDIUM**。
- **minimal fix（凍結 delta 0）**: (1) 05 §2 DeploymentValidityMonitor / 06 §8.3: monitor event は manager 通知の**前に** registry へ `SUSPENDED` record（`reason: SuspensionReason(Enum) = INCIDENT | ISL_ANOMALY | IDENTITY_CHANGE | LAYOUT_CHANGE | BASELINE_SUPERSEDED | OPERATOR`）を append することを必須化（monitor は registry writer であって AuthorityState writer ではない — 05:88 と整合）。event 起因の fault を `R_PROFILE_SUSPENDED` に統一、解除 = 新 generation + two-key。(2) 06 §5 (b) に固定表: 期限到達 item → disposition（TCP / SAFETY_LAYER_ACCEPTANCE / acceptance 期限 → SAFE_STOP、CAMERA_* / 他 → HOLD）と event reason → disposition（INCIDENT / ISL_ANOMALY / safety-layer IDENTITY_CHANGE → SAFE_STOP、他 → HOLD）、`EscalationPolicy.on_validity_expired: EscalationTarget`（SAFE_STOP 側へのみ強化）を追加。

### R4-12 — CONFIRMED / MEDIUM — arm 単位 evidence の subject が同型 2 腕で同値になる

**sed 確認**: 06:354「CONTROLLER_ENVELOPE_MEASUREMENT = arms[].controller_firmware_ref（arm ごと）／ TOOL_PAYLOAD_IDENTIFICATION = arms[].tool_payload.tool_id（arm ごと）」／ 06:305「各上限 ≤ CONTROLLER_ENVELOPE_MEASUREMENT record の測定値」（arm ↔ record の対応規則なし）／ 06:292「(kind × subject) 単位」／ 06:89 `ArmSpec.resource_id`（arms 内で一意）。frozen: RS71 `:15`（UR15 × 2 + 2F-85）。

- **text**: not refuted。「（arm ごと）」は意図の注記で、比較に使われる key は `controller_firmware_ref` / `tool_id` の**値**。同型 2 腕では同値になり (kind × subject) が 1 件に潰れる。
- **logic**: not refuted。両腕 `"UR-5.15.2"` / `"2F-85"` → 左腕の測定 1 件で両腕分の存在が満たされ、`P_ENVELOPE_EXCEEDS_MEASURED` は右腕の envelope を左腕の測定値と比較。右腕の減速 mode は未検出。
- **impact**: not refuted。R2-04 / R2-16 fold の「arm ごと」が THREAD の既定構成（同型 2 腕）で壊れる。fail-open（commissioning 被覆の欠落）。
- **verdict / severity**: CONFIRMED / **MEDIUM**。
- **minimal fix（凍結 delta 0）**: 06:354 の arm 単位 kind の subject_ref を `arms[].resource_id`（必要なら `resource_id#robot_serial_ref`）に変更し、06:305 を「∀ arm ∈ cell.arms: arm.controller の各上限 ≤ record(kind = CONTROLLER_ENVELOPE_MEASUREMENT, subject = arm.resource_id).measured」と書く。

### R4-13 — CONFIRMED / MEDIUM — v0.2.5 の 5 fault code が `MIN_FAULT_INJECTION` に無い（R4 の修正案は 2 code で循環するため差替え）

**sed 確認**: 06:379-380（列挙に `R_CALIBRATION_EXPIRED_ACTIVE` / `R_EVIDENCE_EXPIRED_ACTIVE` / `R_PROFILE_SUSPENDED` / `R_REGISTRY_UNAVAILABLE` / `R_INTER_ARM_VIOLATION` 無し）／ 06:380-381「timeout 駆動の fault は同一 profile_hash の下で commissioning 時に必ず発火させる」（R2-01）／ 05:641（5 code）／ 05:289 row（from に `（CLOSED_LOOP_AUTHORITY）`）・05:290 row（`any (EXECUTOR・CLOSED_LOOP_AUTHORITY)`）・05:424「SHADOW では記録のみ（次 permit 評価で扱う）」／ 05:291 row（`any (EXECUTOR)` — SHADOW でも発火）／ 05:239 条件 12・T-34（permit 発行後・CAS 前の SUSPENDED = mode 非依存）／ T-36（registry 不読 = 起動時）。frozen: D0 `:321`。

- **text**: not refuted。deadline（timer）駆動の新 code 2 件が、本 doc 自身の規則（06:380「timeout 駆動 fault は必ず発火」）の対象なのに列挙に無い。他 3 件は列挙外だが規則の対象外（設計選択）。
- **logic**: not refuted（defect は実在）、ただし **R4 の修正案は差替えが必要**: `R_CALIBRATION_EXPIRED_ACTIVE#DEADLINE` / `R_EVIDENCE_EXPIRED_ACTIVE#DEADLINE` を「CLOSED_LOOP 前に発火」と要求すると、row 289 が CLOSED_LOOP lease に限定され SHADOW では「記録のみ」（05:424）のため、**CLOSED_LOOP 前には発火させられない**（PT-31 / R2-14 が閉じた循環と同型）。`R_PROFILE_SUSPENDED` は permit 発行時 / CAS 条件 12 の経路（T-34）が mode 非依存ゆえ `#CAS` trigger で要求可、`R_REGISTRY_UNAVAILABLE`（T-36）・`R_INTER_ARM_VIOLATION`（row 291 = any EXECUTOR、SHADOW の admission 判定・記録で誘発可）は直接追加可。
- **impact**: not refuted。R2-01（CONFIRMED MEDIUM）の規則の新 code への未適用 = 同 class の回帰。deadline 経路が本 cell で一度も走らないまま CLOSED_LOOP へ行ける。
- **verdict / severity**: CONFIRMED / **MEDIUM**。
- **minimal fix（凍結 delta 0・R4 案を修正）**: (1) 05:289 row と 05:424 を「SHADOW lease でも deadline 到達で LEASE_INVALIDATED + FAULT(R_CALIBRATION_EXPIRED_ACTIVE | R_EVIDENCE_EXPIRED_ACTIVE) を**記録し** TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED)（actuation 影響なし）」に拡張する（裁定 OP-19「SHADOW は次 permit 評価を**許容できる**」の保守側の選択 — 方向と矛盾しない）。その上で MIN_FAULT_INJECTION に `R_CALIBRATION_EXPIRED_ACTIVE#DEADLINE` / `R_EVIDENCE_EXPIRED_ACTIVE#DEADLINE` を追加。(2) `R_PROFILE_SUSPENDED#CAS`（条件 12 経路）/ `R_REGISTRY_UNAVAILABLE` / `R_INTER_ARM_VIOLATION` を追加。（(1) を採らない場合は 06:380 に「deadline 駆動 2 code は CLOSED_LOOP でしか発火せず MIN 集合の対象外」と loud に例外を書き OPP に登録する — いずれか一方を必ず行う。）

### R4-14 — CONFIRMED / MEDIUM — 記録に痕跡を残さない物理変更（base 再取付・同型 tool 交換・ISL hot update）の検出手段が無い

**sed 確認**: 06:67 `HealthCheckKind`（base pose / layout の readback 無し）／ 06:68-69（SAFETY_LAYER_IDENTITY・CONTROLLER_IDENTITY・ENVELOPE_READBACK は定義文あり；**TOOL_IDENTITY は定義文なし** — grep: 06:67 enum・06:287 必須集合・06:437 PT-21 のみ）／ 06:123 `tool_id: str`（個体 serial の要求なし）／ 06:98-100（layout = artifact hash 束縛）／ 06:315・OPP-5（物理事実は evidence に委ねる）／ 06:382（CELL#ARM_SWAP / BASE_TRANSFORM_TAMPER の手順未定義）／ 05:88（monitor は「検出し」のみ・入力源未定義）／ PT-21・PT-30（firmware / ISL 更新は**次 permit**の identity check）。frozen: RS71 `:24`、D0 `:318`。

- **text**: not refuted。base pose を実測と照合する health check / evidence kind は無く、TOOL_IDENTITY は列挙のみで「何と何の等値か」が無い（CD-09 が CONTROLLER_IDENTITY に与えた定義文が無い）。monitor の入力源は未定義。
- **logic**: not refuted。(a) 右 base を +5 cm ずらして再取付 → layout hash 不変・全 identity check pass・CAMERA_EXTRINSIC 期限内・layout revision event 無し → permit 成立；DEPLOYMENT_WORKSPACE / INTER_ARM は宣言 base で FK するため keep-out 評価が 5 cm ずれる。(b) `tool_id` が型番なら同型別個体（TCP 偏差あり）で TOOL_IDENTITY pass。(c) ISL build 更新は 05:88 の event 一覧にあるが、monitor が何を購読して検出するかが無いので event 層に入力が無い。反証として OPP-5「物理事実は契約層で判定不能」があるが、identity 系 health check は正にその物理 readback であり、base pose だけが欠ける非対称は OPP-5 で説明できない。
- **impact**: not refuted。RS71 #2（固定 base）と Rs 裁定 ⑦（BASE_TRANSFORM_TAMPER / ARM_SWAP の negative control）は物理改変の**検出手段**を前提にするが、本文には artifact 改変の検出（(j)・R4-02 fold 後）しか無い。channel-complete な ARM_SWAP も serial 照合を通る。
- **verdict / severity**: CONFIRMED / **MEDIUM**。
- **minimal fix（凍結 delta 0）**: (1) `HealthCheckKind` に `BASE_POSE_VERIFY`（fiducial / 外部計測による各 arm の base pose と layout の T_cell←base の等値・許容差 = baseline `diagnostic_items`）を追加し必須集合（06:287）へ。採らない場合は OPP に「physical base pose は profile 層で未検証（ISL + CAMERA_EXTRINSIC に依存）」と loud に書く。(2) `ToolPayloadSpec.tool_id` を「個体 serial」と定義し、TOOL_IDENTITY を「tool が報告する serial == arms[].tool_payload.tool_id」と定義文で置く。(3) 05:88 の monitor に入力源の最小集合（controller / ISL / tool の identity report の購読・layout / baseline registry の supersession 購読）を書き、identity 系を baseline `diagnostic_items` の周期項目として必須化。(4) CELL#ARM_SWAP / CELL#BASE_TRANSFORM_TAMPER の negative control に物理手順（channel-complete swap・artifact 不変の再取付）と期待検出（BASE_POSE_VERIFY fail）を明記。

### R4-15 — CONFIRMED / MEDIUM — wall-clock の健全性が検査されず `ENV#CLOCK_ANOMALY` に runtime 規則が無い

**sed 確認**: 05:183（wall→mono 変換は発行時に確定）／ 05:146（clock domain = manager 単調 clock・wall-clock の出所未定義）／ 06:291・06:293（`P_CALIBRATION_EXPIRED` / `P_EVIDENCE_EXPIRED` は permit 発行の wall-clock）／ 06:512 OPP-3（対応の「取り方」のみ）／ 05:627-641（clock 系 fault 無し）／ 06:382（ENV#CLOCK_ANOMALY 要求）／ 06:482 `diagnostic_items`（clock を項目に含めるとは書かれていない）。`grep -i wall 05 06` = 上記のみ。frozen: D0 `:354-355`。

- **text**: not refuted。wall-clock の健全性（逆行・drift）を検査する規則・記録義務・fault が無い。OPP-3 は対応の取り方を扱い健全性を扱わない。
- **logic**: not refuted。NTP 障害で wall-clock が 30 日戻る → 昨日失効の TCP calibration が「期限内」→ `validity_deadline_mono = mono_now + 29 日` → 失効 calibration で CLOSED_LOOP lease が成立し deadline も来ない。commissioning で ENV#CLOCK_ANOMALY の「拒否 / SafeHold 到達」を示す手段が無い（R4-03 (C) と同根）。
- **impact**: not refuted。Rs 裁定 OPP-11 ③「commissioning evidence に clock 異常を含める」は runtime 側の検出規則を前提とする — 実現不全。期限検査の fail-open。
- **verdict / severity**: CONFIRMED / **MEDIUM**。
- **minimal fix（凍結 delta 0）**: 05 §3.4 に (l)「permit 発行時の `wall_now` は ① 全 required record の `measured_at_iso8601` と acceptance `approvals[].t` の最大値以上、② baseline `diagnostic_items` の wall/mono drift 閾値内、③ 直前 PERMIT_ISSUED record の wall_now 以上（単調）— いずれか偽 = `R_CLOCK_ANOMALY`（runtime enum 追加）・permit 不発行」を追加し、PERMIT_ISSUED payload に `(wall_now, mono_now)` の記録を義務化。lease 中は monitor が同 drift を周期診断（baseline 項目）し超過 = TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED | SAFE_STOP)。R4-03 の写像表に CLOCK_ANOMALY → R_CLOCK_ANOMALY を入れる。

### R4-16 — CONFIRMED / LOW — acceptance state machine の細部が未定義

**sed 確認**: 06:459「REVOKED は終端。再使用 = 新 generation の新 record のみ」／ 06:485（SUSPENDED → ACCEPTED = 原因除去 evidence + two-key；REVOKED からの条件は無し）／ 06:486（全ての受理に P_* = 0 + two-key）／ 06:464「再受理ごとに +1」／ 06:471-472（monitor が書く SUSPENDED record の approvals / valid_until（None 不可）/ evidence_set_hash の意味未定義）／ 06:491「generation 非単調」／ 05:190 (i)・05:239（「最新 record」の順序未定義・条件 12 は record hash を比較しない）／ 06:472（valid_until 上限なし）。

- **text**: not refuted（一部 refuted）。REVOKED → 新 generation は 06:486 により two-key は要る（R4 の「条件が無い」は部分的に refuted）が「原因除去 evidence」は書かれていない。同 generation の ACCEPTED 再記録は 06:464 の定義に反するが、06:491 の「非単調」が等値を弾くかは曖昧。SUSPENDED record の field 意味・「最新」の順序・valid_until の上限は未定義。
- **logic**: not refuted。R4 の例（g=1 の ACCEPTED を 2 度）は「非単調」の読み次第で通る。追加の指摘: monitor が書く SUSPENDED record が「必須 field 欠落 = P_ACCEPTANCE_RECORD_INVALID」の対象なら、one-key fail-safe の suspension 自体が拒否され得る（fail-safe が塞がる）— 06:491 が受理 record のみを対象とするかが未定義。
- **impact**: not refuted。方向は概ね保守側だが、suspension の有効性に関わる曖昧さを含む。
- **verdict / severity**: CONFIRMED / **LOW**。
- **minimal fix（凍結 delta 0）**: 06 §8.3 に「新 ACCEPTED record は常に generation を +1（06:491 の検査 = 厳密増加）；REVOKED からの新 generation は SUSPENDED → ACCEPTED と同条件；SUSPENDED / REVOKED record は superseded record の hash field を複写し approvals = 失効者 1 名（MONITOR 可）・valid_until = superseded 値、06:491 の必須 field 検査は state ∈ {PROPOSED, ACCEPTED} にのみ適用；最新 = registry append seq 最大」を追記。05 条件 12 に `record_hash == permit.acceptance_record_hash` を追加。baseline に `max_acceptance_validity_s` を持たせ `P_ACCEPTANCE_RECORD_INVALID` で `valid_until − max(approvals.t) ≤` を検査。

### R4-17 — CONFIRMED / LOW — `min_separation_m` の測定対象が未定義・88 mm span との静的整合・base 配置の照合が無い

**sed 確認**: 06:104「`min_separation_m: CanonicalDecimal # > 0`」／ 06:302（`> 0 ∧ 参照 ≥ 1`）／ 06:308・06:327（`P_ENVELOPE_EMPTY` = 明らかな不能の例に本項なし）／ 06:82（layout comment「RS71 §0 の固定 base Y = ∓0.35 を含む」・検査 code なし）／ 06:311 `P_ROBOT_MODEL_MISMATCH`（robot model のみ）／ 05:457（separation の定義なし）。`grep "88\|grasp span\|∓0.35"` = 06:82 の comment のみ。frozen: RS71 `:24`。

- **text**: not refuted。separation の測定対象（TCP 点間 / link 間 / hull 間）が 05 / 06 のどこにも無い。RS71 grasp span との整合検査、base 配置の照合も無い。
- **logic**: not refuted。`min_separation_m = 0.10` は全 P_* = 0 で受理され、RS71 準拠の把持（両 TCP 間 0.088 m）は全て INTER_ARM で拒否される（fail-closed だが「明らかな不能」）。測定対象が未定義なら gateway は「評価不能 = FALSE」で全拒否 — 使えない仕様。base を Y = ∓0.50 で登録しても検査は無い（robot model は検査されるのに base は検査されない非対称）。
- **impact**: not refuted。方向は保守側（fail-closed）で fail-open ではない。定義の欠落は INTER_ARM 項の評価可能性そのものに関わる。
- **verdict / severity**: CONFIRMED / **LOW**。
- **minimal fix（凍結 delta 0）**: 06:104 と 05:457 に「separation = 両 TCP 点間距離（collision hull は ISL・OPP-14）」を明記；`P_ENVELOPE_EMPTY` の静的判定に「`min_separation_m ≥` RS71 grasp span（照合先 = RS71:24・値は本 doc に書かない）」を追加；`P_ROBOT_MODEL_MISMATCH` と対称に `P_BASE_LAYOUT_MISMATCH`（layout の T_cell←base の Y 成分が RS71 §0 #2 と許容差内・許容差 = baseline）を追加するか、OPP-7 に「base 配置の照合は未検査」と loud に書く。

### R4-18 — CONFIRMED / LOW — `effective_expires_at` の min に「項が存在する場合」の条件が無い

**sed 確認**: 05:187「= min(expires_at, ack.valid_until, decision.t_mono + decision_max_age_s, validity_deadline_mono)」／ 05:179（`authority_decision_ref`: CLOSED_LOOP ⇔ 非 null）・05:469（SHADOW = 任意）／ 05:227「前提条件（全て AND・不能判定 = FALSE）」・05:229 条件 2／ 05:190 (k)（発行時に導出して固定）／ 05:424（SHADOW lease では validity deadline は記録のみ）。

- **text**: not refuted。「存在する項の min」とは書かれていない。
- **logic**: not refuted。SHADOW permit（decision null）で第 3 項が評価不能 → 発行時の導出が未定義か、条件 2 が FALSE → slice（SHADOW）が一度も CAS できない。意図は明白だが本文は fail-closed を宣言している。
- **impact**: not refuted（LOW）。第 2 論点「SHADOW permit に validity deadline を混ぜつつ lease は無視」は **refuted**: permit 段での期限執行は (b) と同じ保守側で、lease 段が次 permit 評価で可とするのは裁定 OP-19 どおり — 矛盾ではなく強弱の差。
- **verdict / severity**: CONFIRMED / **LOW**。
- **minimal fix（凍結 delta 0）**: 05:187 を「存在する項の min（SHADOW で `authority_decision_ref` が null の場合 decision 項を除く）」と書き、「`validity_deadline_mono` は両 mode の permit に適用（保守側）・SHADOW lease では記録のみ（05:424）」と 1 文で明示。

### R4-19 — REFUTED / NOT_A_DEFECT — 「two-key」の語が frozen `two_key_state` と区別されない

**sed 確認**: 06:486・06:522（acceptance two-key）／ 05:784「`closed-loop authority` の conjoin 条件 = O0/S0/V0 two-key + 独立安全 gate + 上位 gate の全て」／ 06:495 §9 項 1「`CLOSED_LOOP_AUTHORITY` lease の必要条件（frozen `granted == True`・EP `usage_ceiling.closed_loop_authority` の conjuncts）に profile 由来の条件を**足す**だけで、外すことは無い」／ 05:815 OP-18（cell 固有 gate を frozen `AuthorityDecision` の入力に載せるかは O0/S0/V0 層の事項）。frozen: contracts_v2 `:349`（`two_key_state` は `evaluate_authority_grant` の入力・実体は O0/S0/V0 層）、EP `:156`。

- **text**: refuted。R4 の求める 1 文は無いが、§9 項 1（06:495）と 05 §11 項 1（05:784）が「profile / runtime は frozen の必要条件（two-key を含む EP conjuncts）に足すだけ・付与も代替もしない」を既に述べており、OP-18 が cell gate と `AuthorityDecision` 入力の関係を O0/S0/V0 へ明示的に carry している。概念は本文に存在する。
- **logic**: refuted。acceptance two-key を `two_key_state` の充足と読む経路は §9 / §11 / OP-18 の明示的境界を無視しないと成立しない。deployment doc は `two_key_state` record を生成するとどこにも言っていない。
- **impact**: refuted。機械検査・挙動に影響しない用語の明確化（様式）。
- **verdict / severity**: REFUTED / **NOT_A_DEFECT**（任意の改善: 06 §8.3 に「本 doc の acceptance two-key は deployment 層の受理権限であり frozen `two_key_state`（O0/S0/V0）を充足・代替しない（常に AND）」を 1 文足すことは害がない）。

## 3. 集計と主張しないこと

- confirmed 18（HIGH 4: R4-01 / 02 / 03 / 04；MEDIUM 10: R4-05 / 06 / 08 / 09 / 10 / 11 / 12 / 13 / 14 / 15；LOW 4: R4-07 / 16 / 17 / 18）／ refuted 1（R4-19 = NOT_A_DEFECT）／ merged 0。severity を R4 から変えたのは R4-07（MEDIUM → LOW・前例と同格）のみ。R4-13 は verdict 維持・修正案を差替え（R4 案は 2 code で循環）。R4-09 は範囲を縮小。
- 総合: HIGH が 4 件残るため v0.2.5 は **HOLD**（08 §5 の基準）。全 confirmed の最小修正は凍結 4 file を触らない（runtime / deployment 層の型・field・enum・規則のみ）。
- Rs 裁定 4 件の方向には異議を述べない。本 report は AI verifier の判定であり two-key / Rs 裁定 / gate を flip しない。impl / training / closed-loop authority は CLOSED のまま。open = 0 を宣言しない。
- 未読領域: 05 §13 の v0.2.1–v0.2.4 fold 行（11_ §3 / §7 で代替）、02 / 03 CSV。
