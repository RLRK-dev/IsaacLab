# 軸 B2（runtime safety・第 2 独立意見）— 05 RUNTIME SPEC v0.2.1 反証レビュー

- reviewer: REVIEWER B2（別 model・軸 B の 2 nd opinion）／ 実施 = 2026-09-04 02:0x–02:25 UTC（`date -u` 実測）
- 対象（primary）: `/home/user/IsaacLab/eval_runs/troot_optE_dapg_wholeroute_scope_20260701/WMSO_EXACT_BASELINE_REVIEW_v0.2_20260903/05_WMSO_RUNTIME_SPEC_v0.2_REVIEW_CANDIDATE_20260903.md`（792 行・本文 status = v0.2.1）
- 参照（結線先）: 同 dir `06_..._20260903.md`（456 行・本文 status = v0.2.1）／ 08 計画 §1・§3 checklist ／ 10 陽性対照記録
- 立場: 本 review は gate を flip しない。verdict 語彙は 08 §5。**注入欠陥（10 の PC-05-A/B/C/D・PC-06-E/F/G/H）は本 doc の実体に存在しないことを自分で確認した**（下記 §0(4)）ので報告しない。

## 0. 前提の自己実測（08 §1）

1. **pin 再測**: `bash $D/WMSO_EXACT_BASELINE_REVIEW_v0.2_20260903/verify_exact_baseline_pins.sh` を素の bash で実行 → **11/11 PASS**。
   `D1.1-A 00192d20ca00b654cf6cfdb9d04b03ca14adfd0b93f2105c0fea28c295ff8aff @ 54f90a7d` ／ `EP md c474acea7c…` ／ `EP json e63176af9b…`（definition hash `e7ca43093084c167a209b008533a66d26a1fd3223d2a3c11274d28306c3ff803`・`policy_semver=1.9.0`）／ `D1.1-B 5a1874d3be… @ 07250f4a` ／ blob `1353430228a90ad36dd190a9359cd81da52e8242` / `ebe8154abf2461a6b24db05728b20e5cee949fb2`。`HEAD=297c850bb31a61b56d5f2def9dd616a7c254f6d2 branch=claude/v0-2-design-candidates-snqbmy`。
2. **機械検査**: `python3 check_review_candidate.py 05_… --runtime` → `citations=139 FAIL=0 WARN=6`（WARN は全て C2 heuristic・§13 anchor 23 の説明どおり `:59` / `:151` を別規範の根拠として引く行）。FAIL=0 ゆえ読解に進んだ。
3. **引用は内容で引いた**: 下記の全 `frozen file:line` は `sed -n` で本文を読んで確認済（版 label に依存しない）。
4. **陽性対照の除外確認**: 旧 epoch 拒否は CAS の同一線形化点（`05:233`）で post-transfer ACK ではない／permit は one-shot（`05:182` + 条件 2 `05:220`）／CAS 事後条件に `∪ {consume_offer_id}` が在る（`05:231`）／SHADOW の actuation port は DISABLED（`05:419`・INV-09 `05:639`）。よって PC-05-A/B/C/D は実体に無い。06 側 PC-06-E/F/G/H も `06:229`（bounds field 無し）・`06:231`（等値）・`06:117`（True 必須）・`06:237`（緩和は型上不能）で不在を確認。

## 1. verdict

**HOLD**。CRITICAL 1 件・HIGH 6 件が未反証で残る。いずれも **frozen schema delta 0** の最小修正（散文規則の追加・runtime 層 enum の追加・条件節の追加）で閉じられ、設計の骨格（tuple CAS / one-shot permit / boundary-only）自体は保たれる。

反証の作法: 各 finding について「doc が既に処理している」と読める反論を 1 つ以上構成し、それでも残るものだけを載せた（反論と再反論は各 finding の *反証* 欄）。

## 2. Findings

### B2-01 (CRITICAL) IndependentSafetyLayer の heartbeat 欠落 / `health == failed` に runtime 処置が無い — 活性 lease が無監視で動き続ける

- doc: `05:227`（CAS 条件 9）・`05:261-276`（遷移表）・`05:429`（lease 無効化 trigger 列挙）・`05:714`（OP-9）
- frozen: `WMSO_D0_ARCHITECTURE_DRAFT_RSTECHLEAD2_20260718.md:318` 逐語「missed heartbeat ⇒ consumers must **not** assume safety alive; monitor `failed` ⇒ system fails to **safe-stop**」
- 主張: 05 は ISL の health を **CAS 前提条件（新 lease を作る瞬間）でしか読まない**。活性 lease の実行中に ISL の heartbeat が途切れる／`health == failed` になる事象に対する遷移行が §3.7 の 2 表いずれにも無く（`grep` 実測: `heartbeat` は `05:271` の **executor** heartbeat と `05:64/85/227/721` の参照のみ）、`§4.5` の lease 無効化 trigger 列挙（`05:429`）にも「ISL heartbeat 喪失」が無い。§5.2 の出力規則 (i) は「ISL の**未解除決定**があれば」で発火するが、**死んだ監視は何も発行しない**ため (i) は発火せず、(ii) が CLOSED_LOOP lease の command を admit し続ける。
- 再現（interleaving）:
  1. `S_LEASE_ACTIVE(CLOSED_LOOP_AUTHORITY)`、gateway 出力 = (ii)。
  2. t1 で ISL process が停止（SafetyDecision は 1 件も発行されない・heartbeat 停止）。
  3. AuthorityManager は CAS を試みないので条件 9 は評価されない。gateway の admission 条件（`05:233`・INV-03 `05:633`）は `(lease_id, control_epoch)` 等値と AcceptedEnvelope 連言のみで、ISL health を含まない。`SafetyRestrictionSet` は**静的** predicate（`06:114`「live 決定は IndependentSafetyLayer」）なので代替にならない。
  4. ⇒ skill 終端まで（TERMINAL boundary は数十秒〜分オーダー）**独立安全監視ゼロで closed-loop actuation が継続**する。D0:318 の consumer 義務に正面から反する。
- 反証（試みた反論）: (a)「profile の `health_checks` に `SAFETY_LAYER_HEARTBEAT` があるから覆われる」→ `06:68` の `HealthCheckStage` は `BEFORE_PERMIT | AT_HEALTH_CONFIRMATION | BOTH` の 3 値で **継続監視の stage が無い**。`06:248` も「stage は BOTH 推奨・BEFORE_PERMIT 必須」で lease 期間中の再評価を要求しない。(b)「OP-9 が defer している」→ OP-9（`05:714`）が defer するのは *HealthConfirmation と §F health が同一 channel か* であって、監視喪失時の処置ではない。defer は frozen 要求（D0:318）との矛盾を許諾しない。(c)「OP-17 が gateway 停止中の物理 failsafe を外部化している」→ それは gateway 側の停止であって ISL 側の停止ではない。⇒ 未反証。
- 最小修正（schema delta 0）: (1) `SafeHoldReason` に既存の `SAFETY` を再利用し、§3.7 遷移表に 1 行追加 —「`any (EXECUTOR)` | ISL heartbeat 欠落 or `health == failed` | `TRANSFER_TO_SAFEHOLD(SAFETY)` | `S_SAFEHOLD(SAFETY)` | `FAULT(R_SAFETY_LAYER_LOST)`, `DISPOSITION(SAFE_STOP)`」。`R_SAFETY_LAYER_LOST` は runtime 層 enum（frozen `E_*` 非接触）ゆえ追加可、INV-27 の行も同時に足す。(2) §5.2 に「gateway は ISL heartbeat の age が `safety_heartbeat_max_age_s` を超えたら (ii) を評価しない」を追記（INV-19 と同型の fail-closed）。(3) `06 RuntimeTimeouts` に `safety_heartbeat_max_age_s` を追加（deployment 層の新語ゆえ frozen 不変）。(4) §4.5 の無効化 trigger 列挙に同事象を追加。

### B2-02 (HIGH) `TRANSFER_TO_EXECUTOR` の前提条件に boundary guard が無い — mid-skill 切替が機械的に成立する

- doc: `05:216-228`（CAS 前提条件 1–10・「どれか 1 つでも満たされなければ状態は一切変わらない」）・`05:261`（「表外遷移は存在しない」）・`05:265`（from = `S_SAFEHOLD` / `S_BOUNDARY_WAIT`）・INV-21 `05:651`
- frozen: `WMSO_D11BC_SLICE_SCOPE_PREREG_RSTECHLEAD2_20260720.md:59`「boundary（本 slice の runtime 選択点）= skill 終端（TERMINAL outcome）のみ … checkpoint 途中切替の実行 = Phase G / 別 gate」／ `WMSO_D0_ARCHITECTURE_DRAFT_RSTECHLEAD2_20260718.md:287`「the manager may interrupt **only** at those」
- 主張: 条件 1–10 に `expected.owner.kind == SAFEHOLD ∨ expected.boundary_wait == True` が **無い**。条件 1 は「expected と current の tuple 等値」でしかなく、`expected` に `S_LEASE_ACTIVE` の tuple を入れても等値は成立する。offer は任意（`05:153` = SAFEHOLD 起動は null 可）なので offer 無し転送も可。⇒ 活性 lease の**実行中**に新 executor へ owner が移り、旧 lease は `LEASE_INVALIDATED(TRANSFER)` される。「mid-skill switching は無い」を保証しているのは散文（`05:261`）と INV-21 だけで、INV-21 は *Orchestrator の再選択開始時刻* の制約かつ違反時の欄が「設計違反（test）」であり、AuthorityManager の受理条件ではない。T-16 の model check 対象も INV-01/02/03/10/11 のみ（`05:678`）で本件を含まない。
- 再現: 悪意でなくてもよい。Orchestrator の instance A が boundary で B4 を実行中に、旧 outcome の再配信（at-least-once delivery）で instance B が同一候補の permit を要求 → 一方が MARK_BOUNDARY_WAIT 前の snapshot（`boundary_wait=False`, owner=EXECUTOR）を `expected` として permit を得る → CAS 成功 → 動作中の skill の途中で executor が差し替わる。ケーブルを把持したまま owner が変わる = §0 不変前提の物理危険側。
- 反証: (a)「Orchestrator が boundary でしか要求しない」→ 05 の責務分離は「Orchestrator は状態を持たない・authority を書かない」（`05:83`）で、enforcement は manager 側に置く設計。要求側の善行に依存する時点で B6 checklist の「経路が無いか」に対する答えは「有る」。(b)「遷移表に無い」→ §3.5 が normative な受理条件表であり、表 261 は事象→CAS の対応。両者が矛盾する場合、実装・検証は条件表に従う。⇒ 未反証。
- 最小修正: §3.5 TRANSFER_TO_EXECUTOR に **条件 11** を追加 —「`expected.owner.kind == SAFEHOLD ∨ (expected.owner.kind == EXECUTOR ∧ expected.boundary_wait == True)`。偽 = `REJECTED` / `R_MIDSKILL_TRANSFER_BLOCKED`」。同 code を §3.7 失敗表と INV（例 INV-28）と T-plan に 1 行ずつ追加。runtime 層 enum のみ・frozen 非接触。

### B2-03 (HIGH) permit / ReadinessAck / AuthorityCas の identity 束縛が欠落 — ACK していない executor に lease が渡る

- doc: `05:146-157`（`ReadinessAck` の field）・`05:221`（条件 3）・`05:191-203`（`AuthorityCas` の field）・`05:231`（事後条件）・INV-07 `05:637`
- frozen: `WMSO_D11A_CONTRACTS_V2_DESIGN_RSTECHLEAD2_20260719.md:382`（epoch/offer/owner の状態遷移責務 = authority manager）／ handoff 決定 C-2「次 executor の readiness ACK を owner 転送**より前**に取得」（`HANDOFF_DECISIONS_v0.2_20260903.md:20`）
- 主張: 条件 3 が照合するのは `ack.expected_control_epoch` / `now ≤ ack.valid_until` / `ack.invocation_id == permit.proposed_lease.invocation_id` / `ack.handoff_offer_id == permit.handoff_offer_id` / `ack.envelope_readback_ok` の 5 つだけ。**`ack.executor_id == proposed_lease.executor_id` が無い**。`ack.skill_action_id`・`ack.lease_proposal_id`（`05:151`,`05:154`）はどの条件・どの INV からも参照されない **dead field**。さらに `AuthorityCas.new_owner` / `new_lease_id` / `consume_offer_id`（`05:197-199`）を `permit.proposed_lease` / `permit.handoff_offer_id` に束縛する条件が無く、事後条件（`05:231`）はそれら **CAS 側**の値で状態を書く。§3.3 の散文（`05:160`「いずれかが変われば ACK は無効」）は条件表に落ちていない。
- 再現: 候補 c を選択 → executor instance `E1` が envelope readback を行い ACK（`ack_id=A1, executor_id=E1, lease_proposal_id=L1`）→ `E1` が crash → Orchestrator（無状態・`05:276`）が同じ invocation 候補で `E2` を起こす → permit の `proposed_lease.executor_id = E2`、`ack_id = A1`（`invocation_id` は一致・`valid_until` 未超過）→ 条件 1–10 全通過 → CAS 成功。**readback も readiness も確認していない `E2` が actuation authority を得る**。同型で、CAS が `new_lease_id = L'`（permit の proposed_lease.lease_id と別）を書けば、gateway が admit する `lease_id` に対応する lease 記録が存在せず、mode / control_mode / AcceptedEnvelope の解決先が消える。`consume_offer_id ≠ permit.handoff_offer_id` なら、検証していない offer が消費済に入り、検証済 offer は未消費のまま残る。
- 反証: (a)「`invocation_id` が一致するなら同じ executor のはず」→ `SkillInvocation.invocation_id`（frozen `:477-486`）は invocation の id であり executor process の id ではない。`ReadinessAck` が `executor_id` を別 field として持つ事実（`05:149`）が、両者が独立であることを doc 自身が認めている証拠。(b)「INV-07 が束縛を保証」→ INV-07（`05:637`）は epoch・valid_until・invocation_id の 3 つのみ。⇒ 未反証。
- 最小修正: 条件 3 に `∧ ack.executor_id == permit.proposed_lease.executor_id ∧ ack.skill_action_id == permit.proposed_lease.skill_action_id ∧ ack.lease_proposal_id == permit.proposed_lease.lease_id` を追加。条件 2 に `∧ cas.new_owner == OwnerRef(EXECUTOR, permit.proposed_lease.executor_id) ∧ cas.new_lease_id == permit.proposed_lease.lease_id ∧ cas.consume_offer_id == permit.handoff_offer_id` を追加。INV-07 を同内容へ拡張。ついでに `HealthConfirmation`（`05:205-213`）の受理条件に `hc.lease_id == state.active_lease_id ∧ hc.control_epoch == state.control_epoch` を明記（現在どこにも無い）。全て既存 field の等値追加ゆえ schema delta 0。

### B2-04 (HIGH) `TRANSFER_TO_SAFEHOLD` の再試行規則が `safehold_reason` を上書きする — SAFETY clearance を競合で外せる／INV-24 の出力主張が偽

- doc: `05:237`（再試行規則・前提条件 = 条件 1 のみ）・`05:228`（条件 10）・`05:464-470`（§5.4）・INV-24 `05:654`・`05:450`（(ii)）
- frozen: `WMSO_D0_ARCHITECTURE_DRAFT_RSTECHLEAD2_20260718.md:319-320`（preemption acts first / `stabilized_post_action_state`）
- 主張 (a): `TRANSFER_TO_SAFEHOLD` は前提条件が「条件 1 + reason 非 null」だけなので、**SAFEHOLD → SAFEHOLD（reason 差し替え）も成功する**。`05:237` は「CONFLICT したら current を再読して**即座に再試行**（… 必ず成功する）」と命じるため、後着の弱い reason が先着の `SAFETY` を上書きしうる。§5.4 と条件 10 は解除前提を **reason で keying** しているので、上書き後は clearance record 不要な行（第 1 行）へ落ちる。
- 再現:
  1. ISL が STOP を発行 → `TRANSFER_TO_SAFEHOLD(SAFETY)` 成功 → `(SAFEHOLD, e+1, reason=SAFETY, seq+1)`。
  2. 同時刻に gateway が旧 lease の envelope 違反を報告済で、`TRANSFER_TO_SAFEHOLD(ENVELOPE_VIOLATION)`（expected = 事前 tuple）が in-flight → CONFLICT。
  3. `05:237` の規則どおり再読 → 再試行 → 条件 1 成立 → SUCCESS → `(SAFEHOLD, e+2, reason=ENVELOPE_VIOLATION)`。
  4. 次の TRANSFER_TO_EXECUTOR で条件 10 は `reason ∈ {SAFETY, SAFE_STOP, MANAGER_RESTART}` を見る → `ENVELOPE_VIOLATION` なので **ISL の clearance record を要求しない**。条件 9 の「未処理の STOP/HOLD 決定が無い」は「未処理」の定義が doc に無く、D0:319 の「acts first」に従えば STOP は即時適用済（= 処理済）と読めるため防波堤にならない。⇒ 安全 HOLD の解除条件が競合で消える。
- 主張 (b): INV-24 は「CONFLICT 中の出力は (i) または (iii)」と述べるが、`05:237` の根拠文は safety の場合しか論じていない。`TRANSFER_TO_SAFEHOLD` は `EXECUTOR_LOST` / `DEADLINE_MISS` / `ENVELOPE_VIOLATION` / `TIMING_VIOLATION` / `HEALTH_FAIL` / `NO_CHAIN` / `GATEWAY_RESTART` でも使われる（`05:267,270,271,292,298,305,307`）。これらの CONFLICT 窓では SafetyDecision が無いので (i) は発火せず、状態はまだ `owner==EXECUTOR ∧ ¬boundary_wait ∧ CLOSED_LOOP` のままなので (ii) が成立し、**直近 admitted command が hold 地平内で出力され続ける**。INV-24 は反例を持つ。
- 反証: (a)「再試行は同じ reason で行われる」→ そう読める文言が無い。(b)「manager が reason 優先度を知っている」→ `SafeHoldReason` に順序も優先規則も定義が無い（`05:122`）。⇒ 未反証。
- 最小修正: §3.5 に「**reason 優先度**: `SAFETY` / `SAFE_STOP` > `MANAGER_RESTART` / `GATEWAY_RESTART` / `CHECKPOINT_DISABLED` > その他。current が既に SAFEHOLD の場合、再試行は *より強い reason へのみ* 状態を変え、弱い reason は状態を変えず `FAULT` + `AuditRecorder` 記録のみ（no-op success）」を追加。INV-24 を「(i)/(iii) に落ちるのは safety 起因の場合。非 safety 起因では gateway が検出時点で局所的に (iii) へ落ちる（pending-invalidate flag）」へ訂正し、§5.2 に当該 1 文を追加。

### B2-05 (HIGH) disposition `SAFE_STOP` が gateway 出力に到達しない — `SafeHoldReason.SAFE_STOP` を設定する遷移が存在しない

- doc: `05:451`（(iii) = `safehold_reason == SAFE_STOP` なら SafeStop）・`05:122`（enum）・`05:270`,`05:292`,`05:500`,`05:588`,`05:590`,`05:595`（disposition 側の SAFE_STOP）
- frozen/対文書: `06:174-179`（`EscalationPolicy.on_no_chain` / `on_health_fail` = `HOLD | SAFE_STOP`）・`06:237`（「**SAFE_STOP 側へのみ**（HOLD → SAFE_STOP 可・逆不可）」= profile に許された唯一の強化方向）・frozen `FailClosedAction = {RE_OBSERVE, SAFE_STOP, HANDBACK_TO_OWNER}`（`WMSO_D11A_CONTRACTS_V2_DESIGN_RSTECHLEAD2_20260719.md:151`）
- 主張: `grep -oE 'TRANSFER_TO_SAFEHOLD\([A-Z_| ]+\)' 05` の全 17 hit（`05:267,269,270,271,273,274,275,292,298,304,305,307,481,500,669,672,685`）に **`SAFE_STOP` は 1 件も無い**。よって `safehold_reason == SAFE_STOP` は到達不能で、(iii) は常に `SafeHold` を返す。結果として、profile が `on_no_chain = SAFE_STOP` / `on_health_fail = SAFE_STOP` と宣言しても gateway 出力は変わらず、`06` の単調強化は **無効（appearance-only）**。frozen `FailClosedAction.SAFE_STOP → disposition SAFE_STOP`（`05:595`）も同様に出力へ届かない。
- 再現: profile P に `on_health_fail = SAFE_STOP` を設定 → HealthConfirmation 失敗 → `TRANSFER_TO_SAFEHOLD(HEALTH_FAIL)`（`05:292` の状態欄が固定 reason）→ `(SAFEHOLD, reason=HEALTH_FAIL)` → (iii) は `SafeHold` を出力（`SafeStop` ではない）→ `06:237` が唯一許した強化が物理側に現れない。加えて §5.4 第 1 行は `HEALTH_FAIL` を「追加前提なし」に置くので、SAFE_STOP 相当の operator clearance も要らなくなる。
- 反証: (a)「disposition は audit 語彙であって出力語彙ではない」→ `05:451` が出力を reason で切り替える唯一の分岐であり、`05:443`（`SafeStop = SafeHold + stop-class 要求`）は物理的に異なる出力。(b)「manager が reason=SAFE_STOP で CAS すればよい」→ そう命じる行が無く、`05:261`「表外遷移は存在しない」に反する。⇒ 未反証。
- 最小修正: §3.5 / §3.7 に「**disposition が `SAFE_STOP` に解決される遷移（profile の `EscalationPolicy` 適用結果を含む）は `TRANSFER_TO_SAFEHOLD(SAFE_STOP)` を用い、原因 fault は `RuntimeAuditRecord.fault` に別途記録する**」を明記。§3.7 の該当 3 行（`05:270`, `05:292`, `05:500`）の状態欄を `TRANSFER_TO_SAFEHOLD(NO_CHAIN | SAFE_STOP per profile)` の形へ、`05:590`（Terminal INVALID_STATE 解消せず）にも対応行を与える。enum member は既存ゆえ schema delta 0。

### B2-06 (HIGH) `validate_outcome` が issues 非空のとき boundary_wait も SAFEHOLD 転送も起きない — B-05 fold の穴が「producer が壊れているとき」に開く

- doc: `05:239`（MARK_BOUNDARY_WAIT の前提 = 「`validate_outcome` の report ref を CAS に添える」）・`05:268`（遷移行の事象 = TerminalOutcome 受領）・`05:489`（B1: issues 非空 = `R_INVOCATION_INVALID` → **HOLD**）・`05:290`（失敗表: `R_INVOCATION_INVALID` = 「変化なし」「RESELECT / RE_OBSERVE」）・INV-23 `05:653`
- frozen: `WMSO_D11A_CONTRACTS_V2_DESIGN_RSTECHLEAD2_20260719.md:380`（`validate_outcome` = terminal ∈ declared_classes / interrupt checkpoint ∈ checkpoint_specs / duration・cost 有限非負 / invocation_id 一致）
- 主張: 同じ code `R_INVOCATION_INVALID` に **2 つの発生源と 2 つの異なる disposition**（B1 = HOLD、失敗表 = RESELECT/RE_OBSERVE・状態変化なし）が割り当てられており、**HOLD を実現する CAS が無い**（HOLD 出力は `TRANSFER_TO_SAFEHOLD` でしか作れない）。さらに `MARK_BOUNDARY_WAIT` は「report ref を添える」ことを要求するので、issues 非空の outcome では（i）添えて実行してよいのか（ii）実行しないのかが未定義。実行しない読みでは `boundary_wait` が False のままなので、**INV-23 / `R_POST_OUTCOME_COMMAND`（B-05 fold の本体）が発火しない**。
- 再現: executor が `TerminalOutcome{termination_class = declared_classes に無い値}` を発行（= `validate_outcome` が issues を返す典型ケース）しつつ、`action_rate_hz` どおりに command を出し続ける。→ B1 で `R_INVOCATION_INVALID`（doc 上の disposition は HOLD）だが状態遷移は無く、`owner == EXECUTOR ∧ ¬boundary_wait ∧ CLOSED_LOOP` のまま (ii) が admit する。`command_deadline_s`（送信が止まった場合のみ）も `R_TIMING_VIOLATION`（間隔逸脱した場合のみ）も発火しない。⇒ **「終了したと宣言した／不正状態を報告した producer が actuation を保持し続ける」**。
- 反証: (a)「executor は outcome 後 command を出さない」→ B-05 fold（`05:752`）が既にその仮定を捨てている。仮定を再導入するなら fold の意味が消える。(b)「§7 規則 2『fault は常に disposition を伴う』で HOLD になる」→ disposition と状態遷移の写像が無いのが本 finding。⇒ 未反証。
- 最小修正: (1) `MARK_BOUNDARY_WAIT` の前提を「`validate_outcome` の report ref を添える（issues の有無に関わらず）」と明示し、**outcome 受領の事実だけで `boundary_wait := True`** とする。(2) §3.7 失敗表に「`validate_outcome` issues 非空」の行を新設 —「AuthorityManager | `R_OUTCOME_INVALID`（新 runtime code）| `MARK_BOUNDARY_WAIT` 後に `TRANSFER_TO_SAFEHOLD(SAFE_STOP)` | `SAFE_STOP`」。(3) §6.2 B1 の disposition を同表と一致させる。frozen 非接触。

### B2-07 (HIGH) CAS 条件 10 が `CHECKPOINT_DISABLED` を含まない — checkpoint HOLD が clearance 無しで解除でき、「checkpoint 切替 = 別 gate」が実効で回避される

- doc: `05:228`（条件 10 = `{SAFETY, SAFE_STOP, MANAGER_RESTART}`）・`05:464`（§5.4 の列見出しが「追加前提（CAS **条件 10**）」）・`05:469`（CHECKPOINT_DISABLED 行 = 「profile が定義する operator clearance + 再観測」）・`05:481`・OP-11 `05:716`
- frozen: `WMSO_D11BC_SLICE_SCOPE_PREREG_RSTECHLEAD2_20260720.md:59`（checkpoint 途中切替の実行 = Phase G / 別 gate・「checkpoint-enabled slice に変える場合は chunk 改称 + scope 再審査」）
- 主張: §5.4 は自分の第 3 列を「CAS 条件 10」と名指すのに、条件 10 の集合には `CHECKPOINT_DISABLED` が無い。§9 の fail-closed 既定（`05:627`「評価不能 = FALSE」）は *評価される述語* にしか効かず、**列挙されない reason には述語自体が存在しない**ので、`S_SAFEHOLD(CHECKPOINT_DISABLED)` からの転送は追加前提 0 で通る。
- 再現: producer が `InterruptOutcome{PLANNED_SWITCH}` を発行 → `TRANSFER_TO_SAFEHOLD(CHECKPOINT_DISABLED)`（`05:269`）→ Orchestrator が §6.2 B0 から新規起動（§5.4 の解除経路は「完全経路」= 通常の ACK→permit→CAS）→ 条件 10 は該当しないので operator clearance も再観測も要求されない → 数百 ms 後に別 skill が起動。**「受理 + HOLD で切替を無効化した」はずの経路が、実質「HOLD を挟んだ checkpoint 切替」として動作する**。frozen `:59` は実行そのものを別 gate に置いているので、これは scope 逸脱。
- 反証: (a)「§5.4 が要求しているのだから実装はそれに従う」→ 条件 10 が normative な受理条件で、§5.4 は説明表。両者が矛盾するとき機械検査は条件 10 側を実装する。しかも §5.4 の列自身が「条件 10」を参照しており、参照先に行が無い時点で表が壊れている。(b)「OP-11 が defer」→ OP-11 は clearance の**内容**の defer であり、要求の有無ではない。⇒ 未反証。
- 最小修正: 条件 10 の集合を `{SAFETY, SAFE_STOP, MANAGER_RESTART, GATEWAY_RESTART, CHECKPOINT_DISABLED}` へ拡張し、§5.4 に GATEWAY_RESTART 行を追加（B2-08 と同時修正）。加えて「`CHECKPOINT_DISABLED` からの解除後の起動は新 `BeliefRef` での B0 起動であることを `InitializationRecord.previous_lease_id = null` で機械確認する」を追記。

### B2-08 (MEDIUM) §5.4 の解除表が `SafeHoldReason` を網羅しない（`GATEWAY_RESTART` / `TIMING_VIOLATION`）— 06 の `clearance_roles` と矛盾

- doc: `05:122`（enum 14 member）・`05:464-470`（表は 12 member 分のみ）
- 対文書: `06:179`「`clearance_roles`: (safehold_reason, role) — **SAFETY / SAFE_STOP / CHECKPOINT_DISABLED / MANAGER_RESTART / GATEWAY_RESTART** の解除に要する追加の role（05 §5.4）」
- 主張: 06 は `GATEWAY_RESTART` に clearance role を宣言できると書くが、05 §5.4 に行が無く条件 10 にも無いため、**profile が宣言した role は runtime のどこからも参照されない**。`TIMING_VIOLATION` は 05 内で行が無い（`05:305` で reason として使われるのみ）。
- 再現: profile に `clearance_roles = (("GATEWAY_RESTART", "cell_supervisor"))` を宣言 → gateway が再起動 → `S_SAFEHOLD(GATEWAY_RESTART)` → 次の TRANSFER_TO_EXECUTOR は条件 10 に該当しないため `cell_supervisor` の関与なしに成立。gateway 停止中の物理側は OP-17（`05:721`）で **本 doc の外**とされているので、「何が起きていたか不明な区間」の直後に無人で closed-loop へ戻れる。
- 最小修正: §5.4 に 2 行追加（`GATEWAY_RESTART` = gateway config match health check + profile clearance role ／ `TIMING_VIOLATION` = 追加前提なし（第 1 行へ併合））、条件 10 を B2-07 の修正と併せて拡張。

### B2-09 (MEDIUM) INV-27 が偽 — 5 つの `RuntimeFaultCode` に §3.7 の行が無い（§7 規則 2 も破れる）

- doc: INV-27 `05:657`（「全 `RuntimeFaultCode` member は §3.7 の失敗表に行を持つ」）・§7 規則 2 `05:578`（「fault は常に disposition を伴う」）・§3.7 の 2 表 `05:263-308`
- 実測（機械的に確認）: 33 member を §3.7 の transition 表（`05:261-277`）と失敗表（`05:278-308`）に照合 → **どちらにも行が無い = `R_EPOCH_STALE_COMMAND` / `R_DEADLINE_MISS` / `R_ENVELOPE_VIOLATION` / `R_ENVELOPE_EMPTY` / `R_LEASE_INVALIDATED`**（5 件）。厳密読み（「失敗表」に限る）では `R_CHECKPOINT_SWITCH_DISABLED` も欠（transition 表のみ）。
- 影響: `R_EPOCH_STALE_COMMAND` は 06 の `MIN_FAULT_INJECTION`（`06:323`）が CLOSED_LOOP 前に必ず発火させる要求 code の 1 つで、**期待される状態効果・disposition が仕様に無いまま fault injection の合否判定を行うことになる**（T-25 `05:687` は「失敗表の行どおり」を期待値としており、行が無いので判定不能）。`R_DEADLINE_MISS` / `R_ENVELOPE_VIOLATION` は §3.7 transition 表 `05:272` が事象としては覆うが fault 欄が総称「FAULT」で code 名を書いていない。
- 最小修正: 失敗表に 5 行追加（`R_EPOCH_STALE_COMMAND` = gateway・拒否のみ・状態不変・audit ／ `R_DEADLINE_MISS` = `TRANSFER_TO_SAFEHOLD(DEADLINE_MISS)`・HOLD ／ `R_ENVELOPE_VIOLATION` = 同 `(ENVELOPE_VIOLATION)`・HOLD ／ `R_ENVELOPE_EMPTY` = permit/CAS 前・不成立・RESELECT ／ `R_LEASE_INVALIDATED` = §4.5 の各 trigger に伴う記録・disposition は各 trigger 行に従う）。`05:272` の fault 欄に code 名を書く。INV-27 の文言を「§3.7 の**両表**」へ明確化。

### B2-10 (MEDIUM) 遷移表が `S_HEALTH_PENDING` での outcome 受領を覆わない（「表外遷移は存在しない」の反例）

- doc: `05:256-259`（状態定義）・`05:268`（outcome 行の from = `S_LEASE_ACTIVE` のみ）・`05:261`（表外遷移は存在しない）・`05:266`（health 確認で `S_LEASE_ACTIVE` へ）
- 主張: `HealthConfirmation` は `first_command_admitted` を含む（`05:211`）ので確認完了までに数 command 分の時間がかかる。短い skill（`WAIT` / 短い `SCRIPTED_SEQUENCE`）は **health 確認前に TERMINAL outcome を出し得る**。この事象の行が無い。
- 再現: `WAIT` skill を lease 化 → CAS 成功で `S_HEALTH_PENDING` → 100 ms 後に TERMINAL SUCCESS → 遷移表に行が無いので MARK_BOUNDARY_WAIT が定義されない → `boundary_wait` は False のまま（B2-06 と同じ穴）→ やがて `health_confirm_timeout_s` 超過で `TRANSFER_TO_SAFEHOLD(HEALTH_FAIL)` = **正常終了した skill が health 失敗として記録され chain が切れる**（`NO_CHAIN` ではなく HEALTH_FAIL の HOLD）。
- 最小修正: `05:268`/`05:269` の from 欄を `S_LEASE_ACTIVE / S_HEALTH_PENDING` に拡張し、`S_HEALTH_PENDING` で outcome を受けた場合は HealthConfirmation の待機を打ち切って `MARK_BOUNDARY_WAIT` を行う（health 未確認の事実は `RuntimeAssessmentRecord` に記録）と明記。

### B2-11 (MEDIUM) genesis record が `consumed_offer_ids` / `invalidated_lease_ids` を ∅ に戻し、epoch 起点の下限を課さない — P3 / P5 が genesis を跨いで破れる。`invalidated_lease_ids` には durable な home が無い

- doc: `05:141`（genesis record = `epoch 起点・consumed_offer_ids = ∅・invalidated_lease_ids = ∅`）・`05:129-137`（`AuthorityState` に `invalidated_lease_ids` field は**無い**）・INV-11 `05:641`・P3/P5 `05:113`,`05:115`・証明 C/E `05:316`,`05:320`
- 主張: (a) P3「同一 `handoff_offer_id` は 2 度消費されない」・P5「無効化された lease_id が再び活性になることは無い」は無条件の不変量として書かれているが、genesis 再発行で両集合が ∅ に戻り、`epoch 起点` に「過去の全 epoch より真に大きい」という要件が無いため、旧 `HandoffOffer`（`control_epoch == 新 e0`）が `validate_handoff` の**等値**検査（frozen `:381`）を通り、条件 4 も通る。(b) INV-11 が参照する `invalidated_lease_ids` は `AuthorityState`（唯一の durable 状態・`05:140`）に無く、どこに置かれ restart を跨ぐのかが未定義 ⇒ 証明 E の機械検査根拠が宙に浮く。
- 再現: durable store 消失 → operator が genesis 再発行（`epoch 起点 = 0`）→ 以前 epoch 0 で発行され未消費のまま残っていた offer `o`（producer 側の永続 queue / 再送）を投入 → `validate_handoff(…, snapshot=0)` PASS → 条件 4 PASS（∅）→ 消費される。旧 lease id と同じ id を manager が再発行しても INV-11 は ∅ 相手なので検出しない。
- 最小修正: `05:141` に「genesis の `epoch 起点` は **audit 記録に現れる全 `control_epoch` より真に大きい**値であること（genesis 世代 counter を epoch 上位ビットに置く等）」と「`invalidated_lease_ids` は `AuthorityState` と同じ durable store に置き、`consumed_offer_ids` と同様に genesis 以外で縮小しない」を追加。§3.2 の `AuthorityState` に field を足さずに済ませたい場合は「manager が保持する durable 付帯集合」として §3.2 に 1 段落で明記し INV-11 の参照先を pin する。frozen 非接触。

### B2-12 (MEDIUM) 跨 component の単調 clock domain が宣言されていない — `ReadinessAck.valid_until` の鮮度検査が無効化されうる

- doc: `05:156-157`（`t_ack` / `valid_until` = executor 側 stamp）・`05:221`（条件 3 で manager が `now ≤ ack.valid_until` を評価）・`05:213`（`t_confirm`）・`05:504`（「単調 clock … 単一 origin」）・OP-13 `05:718`（executor の process 境界は未定）
- frozen: `WMSO_D0_ARCHITECTURE_DRAFT_RSTECHLEAD2_20260718.md:354-355`「one declared monotonic source (e.g. `CLOCK_MONOTONIC`), **origin = process/episode start**」
- 主張: 05 は 5 つの component（manager / gateway / executor / ISL / orchestrator）を分離した上で、**ある component が stamp した単調時刻を別 component の `now` と比較**する。cited authority が定める origin は process/episode 起点であり、process が分かれた瞬間に比較は無意味になる。doc にも 06 にも clock domain（同一 host・同一 boot・同一 origin）を要求する deployment 条件が無い（`06 RuntimeTimeouts` は長さのみ）。
- 再現: (a) executor が新規 process（origin リセット）・manager は長時間稼働 → `now (259200) ≤ ack.valid_until (2.8)` = FALSE → **全 ACK が期限切れ扱い**で永久に転送不能（fail-closed だが可用性 0）。(b) 逆に manager が再起動直後（now=5.0）で executor が長時間稼働（`valid_until = 259202`）→ **数日前の ACK が新鮮と判定される** = B3 checklist の「stale ACK に束縛された permit」が成立。
- 反証: 「Linux の `CLOCK_MONOTONIC` は boot 起点で host 内共有」→ 同一 host 前提なら成立するが、その前提はどこにも書かれておらず、D0 の逐語は process/episode 起点。よって仕様としては未定義。⇒ 前提を書けば閉じる（= 修正可能）。
- 最小修正: §3.3 に「`valid_until` は **AuthorityManager が ACK 受領時に自分の clock で** `t_receive + ack_validity_s` として確定する（executor の stamp は advisory）」と明記、`HealthConfirmation` の timeout も manager 側計測とする。加えて §12 に OP を 1 件追加（「runtime component は単一 clock domain（同一 host・同一 origin）に置くことを deployment 前提とする。分散配置は本 doc の外」）。

### B2-13 (MEDIUM) `LINEAR_INTERPOLATE` の「hold 地平」が最終 command 後について未定義 — frozen の `hold` 意味論を超えて出力を定義している

- doc: `05:450`（(ii) の「hold 地平内の admitted command」）・`05:396`（「gateway がそのとおりに再生する」「次 command が `command_deadline_s` 内に届かなければ … `SafeHold`」）
- frozen: `WMSO_D11B_TENSOR_BINDING_DESIGN_RSTECHLEAD2_20260720.md:127`「`hold: ActionHold` # **policy step 間**の保持規約（ZOH / 線形補間）」・`:40`（enum）
- 主張: frozen は `hold` を **2 つの policy step の間**の規約として定義する。05 は「最後に admit された command から `command_deadline_s` までの区間」の出力を `hold` で定義しており、`LINEAR_INTERPOLATE` の場合、補間の終点（次 step）が存在しないため出力が未定義（あるいは外挿）になる。「hold 地平」の語も定義されていない。
- 再現: `hold = LINEAR_INTERPOLATE`・`action_rate_hz = 20`（50 ms）・`command_deadline_s = 60 ms` の lease で、executor が step k の後 55 ms 沈黙 → 50–55 ms 区間の gateway 出力が仕様上未定義。実装が直前 2 点の外挿を選べば、指令は境界外へ走る（AcceptedEnvelope は admit 済 command に対する検査であり、gateway が生成する補間値は検査対象として記述されていない）。
- 最小修正: §4.2 に 1 文 —「`hold` は **admit 済みの連続する 2 command の間**にのみ適用する。後続 command が未 admit の区間では `hold` の種別に関わらず直近 admitted setpoint を保持（ZOH 相当）し、**外挿は禁止**。`command_deadline_s` 超過で `SafeHold`」。「hold 地平」を「直近 admitted command から `command_deadline_s` まで」と定義。frozen 非接触。

### B2-14 (MEDIUM) `AuthorityDecision` の鮮度・失効規則が無い — 取り消された two-key / safety gate の下で CLOSED_LOOP lease が成立しうる

- doc: `05:224`（条件 6 = `granted == True` + inputs 束縛のみ）・`05:181`（permit 発行前提に profile 側の時刻依存検査はあるが decision は無い）・INV-08 `05:638`
- frozen: `WMSO_D11A_CONTRACTS_V2_DESIGN_RSTECHLEAD2_20260719.md:346-351`（`evaluate_authority_grant(eligibility_report, acceptance_state, two_key_state, safety_gate_state)`）・`:354`（「gate state の UNKNOWN/欠落 = fail-closed DENY」「granted == True を必要条件とする」）
- 主張: `AuthorityDecision` は 4 つの **時点依存な state record** から作られるのに、05 は「参照が非 null で `granted == True`」しか見ない。decision の作成時刻・有効期限・再評価点が無く、`06 DeploymentEvidencePolicy.validity_rule = ALL_REQUIRED_VALID_AT_PERMIT_ISSUE`（`06:316`）が覆うのは deployment evidence だけ。
- 再現: t0 に `granted == True` の decision D を得る → t1 に safety gate が取り下げ（`safety_gate_state = FAIL`）→ t2 に同じ D の ref を持つ permit を発行 → 条件 6 PASS → CLOSED_LOOP lease 成立。frozen `:354` の「UNKNOWN/欠落 = fail-closed DENY」の精神に反する（05 は decision を「読むだけ」と宣言する `05:391` ため、古い読みが通る）。
- 最小修正: §3.4 permit 発行前提に「(d) `authority_decision` は **permit 発行時刻**で再解決し、`granted == True` かつ profile の `decision_max_age_s` 内（`06 RuntimeTimeouts` に追加）であること。超過 = `R_AUTHORITY_DECISION_ABSENT`」を追加。frozen API は呼ぶだけなので schema delta 0。

### B2-15 (LOW) `R_COMMAND_KIND_MISMATCH` の escalation 閾値 `N` が profile に存在しない（かつ表外遷移）

- doc: `05:298`（「連続 N 回で `TRANSFER_TO_SAFEHOLD(ENVELOPE_VIOLATION)` — N は profile」）・INV-22 `05:652`・`05:261`（表外遷移は存在しない）
- 対文書: `06:144-151`（`RuntimeTimeouts` の 6 field に該当なし）・`06:349`（`P_*` 一覧にも該当検査なし）→ **参照先が存在しない profile parameter**。
- 影響: 当該 escalation は発火条件が未定義で、§3.7 transition 表にも行が無い（表外遷移）。実害は限定的（command は拒否される）だが、B8 の写像 totality を満たさない。
- 最小修正: `06 RuntimeTimeouts` に `command_kind_mismatch_limit: int`（> 0・`P_TIMEOUT_NONPOSITIVE` と同型検査）を追加し、05 の transition 表に 1 行追加。または escalation 句を削除して「拒否のみ」に統一。

### B2-16 (LOW) 到達不能な enum member（`RuntimeDisposition.ABORT` / `SafeHoldReason.BOUNDARY_WAIT` / `TRANSFER`）

- doc: `05:559`（`ABORT` は enum 定義行にのみ出現 = 全 file で 1 hit）・`05:122`（`BOUNDARY_WAIT` / `TRANSFER`）・`05:466`（§5.4 は両者を解除対象として列挙）
- 主張: `ABORT` を生成する規則が §7 の outcome×disposition 表にも §3.7 にも無い（D0 §E の `abort` は転送 protocol の語であって runtime disposition ではない）。`BOUNDARY_WAIT` は B-05 fold 後 `boundary_wait: bool` + owner=EXECUTOR で表現されるため `safehold_reason` としては到達不能。`TRANSFER` は lease 無効化 reason（`05:235`）であって safehold reason ではない。§5.4 が到達不能 reason を解除対象に並べているのは fold の伝播漏れ。
- 最小修正: `ABORT` を削除するか §7 表に生成規則を与える。§5.4 第 1 行から `BOUNDARY_WAIT` / `TRANSFER` を除去（または `SafeHoldReason` から削除）。

### B2-17 (LOW) audit の join 主張と INV-17 / T-17 の言明が実体と合わない

- doc: `05:606-619`（`RuntimeAuditRecord` に `profile_hash` **無し**）・INV-17 `05:647`（「ちょうど 1 件」）・T-17 `05:679`（「全事象 ↔ record の全単射」）・`05:265`（1 事象で `EPOCH_TRANSITION, LEASE_ACTIVATED, LEASE_INVALIDATED` の 3 record）
- 対文書: `06:336`「両者は `profile_hash` と `lease_id` で join する（05 §8）」
- 主張: (a) permit 発行時 fault（`R_PROFILE_MISBOUND` / `R_HEALTHCHECK_FAILED`）・`MANAGER_RESTART` / `GENESIS_RECORDED` の record は `lease_id = None` かつ `profile_hash` field が無いため、**deployment evidence との join が不能**。ちょうど deployment 起因の fault が join できない。(b) INV-17 の「ちょうど 1 件」と T-17 の「全単射」は、1 事象 3 record を書く自表（`05:265`）と矛盾する。
- 最小修正: `RuntimeAuditRecord` に `profile_hash: str | None` を追加（runtime 層の新語ゆえ frozen 非接触）、または `payload_ref` に profile_hash を必ず含める旨を §8 に明記。INV-17 を「§3.7 の各事象に対し、当該事象の記録義務 kind ごとに 1 件（多重度は行の記載に従う）」へ、T-17 を「全射（欠落 0）」へ訂正。

## 3. PASS 項目（08 §3 checklist id ごと・根拠行つき）

- **B1 二重所有 = PASS**（本レビューの範囲で反例を構成できず）。tuple 全体の CAS（`05:219` 条件 1・`05:130-137`）／ gateway admission の等値（`05:233`・INV-03 `05:633`）／ permit one-shot（`05:182`・条件 2 `05:220`・INV-04 `05:634`）／ 旧 epoch 拒否が **同一線形化点**で有効（`05:233`）／ 証明 A（`05:312`）は D0 `:270-272` の「exactly one owner at every instant」に対応。B2-02 / B2-03 は「誰が owner か」を誤らせるが、同時に 2 lease が admit される瞬間は作れなかった。
- **B2 所有不在 = PASS**。out(t) の (i)(ii)(iii) 全域性・排他性（`05:448-451`・INV-10 `05:640`）／ SAFEHOLD が「定義された owner」（`05:121`）／ CAS 事後条件で SafeHold 活性（`05:234`）／ gateway 起動時出力 = SafeHold（INV-25 `05:655`）／ manager 不達時も (iii)（`05:456`・INV-19 `05:649`）。⚠ 出力は常に**定義される**が、その内容が SAFE_STOP に強化されない点は B2-05。
- **B3 stale / replay = 部分 PASS**。offer replay は `consumed_offer_ids` を CAS tuple に入れる二重遮断（`05:222` 条件 4・`05:231` 事後条件・`05:245-247`・証明 C `05:316`・T-04 `05:666`）で成立、frozen `:381`（等値検査）/ `:382`（使用済み拒否 = manager 責務）/ `:498`（消費単位 = `handoff_offer_id`）に忠実。permit 二重使用・失効後使用も PASS（証明 D `05:318`）。⛔ stale ACK 束縛は B2-03 / B2-12 で FAIL。
- **B4 crash matrix = 部分 PASS**。新 executor の CAS 後 crash（`05:291`）／ manager 再起動（`05:274`,`05:293`・`epoch := persisted + 1`）／ gateway 再起動（`05:275`,`05:307`・INV-25）／ 転送窓内の safety override（`05:294`）／ permit 発行後の Orchestrator 喪失（`05:276`,`05:308`）はいずれも named disposition + SafeHold + 旧 owner 非復活（証明 E `05:320`）で覆われている。⛔ ISL 喪失（B2-01）・`S_HEALTH_PENDING` での outcome（B2-10）・gateway restart の clearance（B2-08）が欠。
- **B5 lease = 部分 PASS**。活性はちょうど 1（INV-01 `05:631`・`05:369`）／ profile_hash・authority_decision_ref・initialization・accepted_envelope・timing の完全束縛（`05:369-385`・handoff 決定 6）／ 変更 = 新 CAS・in-place 無し（`05:389`・`06:279`）／ 無効化 trigger 列挙（`05:429`）／ SHADOW は actuation を出さない（`05:419`・INV-09・T-11）／ CLOSED_LOOP は `granted == True` を**必要条件として検査するだけ**で authority を主張しない（`05:224`・`05:420`・`05:691` ↔ frozen `:354`・EP JSON `:213`）。⛔ decision の鮮度は B2-14。
- **B6 boundary-only = FAIL**（B2-02 / B2-07）。ただし再選択が新 `BeliefRef` + 明示 `now` から行われる点（`05:490`,`05:494`,`05:504` ↔ frozen `:372-373`）と、continuation / recovery 判断が **非 certified**（`05:507-528` ↔ frozen `:39`）である点は PASS。
- **B7 safety = FAIL**（B2-01 / B2-04）。(i) の最優先・ack 非待機（`05:449`,`05:454` ↔ D0 `:319`）と deadline miss の disposition（`05:396`,`05:272`）は PASS。
- **B8 disposition / fault = 部分 PASS**。`ProducerOutcome` 不変（`05:577`・INV-20・T-19）は frozen `:151` に忠実で、outcome×disposition 表（`05:583-593`）は frozen の `TerminationClass = {SUCCESS, FAILURE, TIMEOUT, INVALID_STATE}` / `InterruptReason = {PLANNED_SWITCH, EVENT, SAFETY_STABILIZED}`（いずれも `:151` 実測）に対し **total**。⛔ fault→行の写像は B2-09、outcome 検証失敗経路は B2-06。
- **B9 audit = 部分 PASS**。kind 網羅（`05:600-604`・v0.2.1 で `CAS_ATTEMPT` / `CLEARANCE_RECORDED` / `GATEWAY_RESTART` / `GENESIS_RECORDED` 追加）・record が `control_epoch` / `lease_id` を持つ（`05:612-613`）・記録義務（`05:622`）・hash 方式 = U14 defer を honest に宣言（`05:624` ↔ frozen `:537`）。⛔ B2-17。
- **B10 timing = 部分 PASS**。rate / hold は D1.1-B と**等値**（`05:333-336`・INV-12 `05:642`・T-14 `05:676` ↔ frozen `:96-99`,`:125-127`）、`max_obs_staleness_s` は記録のみで執行しない（`05:337`,`05:398` ↔ frozen `:99`）、鮮度は frozen `FreshnessPolicy` 起点で stricter-or-equal（`05:399`・None 規則を OP-16 で loud 化）。**数値定数の実測 = 0**（`grep -nE '[0-9]+(\.[0-9]+)?\s*(ms|s\b|Hz|秒)'` は header 以外 hit 無し）で INV-22（`05:652`）は成立。⛔ `hold` 地平は B2-13、`N` は B2-15。

## 4. 機械検査・陽性対照の再確認

- `check_review_candidate.py 05 --runtime` = `FAIL=0 / WARN=6`（再掲）。**本レビューの 17 finding のうち機械検査が捕捉したものは 0** — O3（`∪ {consume_offer_id}`）型の 1 トークン欠落は捕捉するが、「条件表に**在るべき条件が無い**」型（B2-02 / B2-03 / B2-07）は検出できない。計器の弁別力の穴として記録する（10 §2 の O3 追加と同じ論法で、`--runtime` に「条件表 ↔ §5.4 ↔ enum の相互被覆」assert を追加すれば B2-05 / B2-08 / B2-09 / B2-16 は機械化できる）。
- 陽性対照（10）の注入欠陥は本 doc に不在（§0(4) で自分で確認）。本 report は注入欠陥を 1 件も再報告していない。

## 5. 境界（本 review が主張しないこと）

1. gate flip・two-key・Rs 裁定・freeze を主張しない（08 §6）。
2. 凍結物に触れていない。全 finding の最小修正は **frozen 4 file への schema delta 0**（runtime 層 enum の member 追加と散文条件の追加、および 06 の deployment 層 field 追加のみ）。
3. 軸 A / 軸 C の finding は読んでいない（08 §1 項 6 の独立手続）。

-- 2026-09-04 11:25 JST（`date` 実測）
