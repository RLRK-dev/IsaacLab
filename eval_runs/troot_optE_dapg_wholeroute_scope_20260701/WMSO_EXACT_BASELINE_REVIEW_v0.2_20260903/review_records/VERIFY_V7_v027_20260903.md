# V7 verification — R7 findings against WMSO v0.2.7 REVIEW CANDIDATE（3 lens・adversarial・refute-first）

- verifier = V7（別 context・authority 無し・凍結物へ非接触・`eval_runs/` 未編集・書込み先 = `$OUT` のみ）; 実施 = 2026-09-06 00:03–00:1x UTC（`date -u` 実測: 開始 00:03:57 / pin 再測 00:04:02 / checker 00:04:17 / q2test 00:12:54–00:13:26 / JSON 書込 00:17:29 / 本報告書込 = 末尾）; 対象 commit = `fed354bd`（branch `claude/v0-2-design-candidates-snqbmy`。05 / 06 は `f1393050` と byte 同一 — `git diff --stat f1393050 fed354bd -- 05 06` = 空・exit 0）
- 対象 finding = `$OUT/R7_findings_v027.json`（19 件・R7-01..R7-19・7 MEDIUM / 12 LOW・全件読了）+ `$OUT/R7_review_v027.md`（238 行・全読）。重複注記のため `$OUT/R8_findings_v027.json`（25 件）の claim を skim（**merge はしない**）。対象 doc = `$PKG/05_…`（v0.2.7・1050 行・sha256 `007d753d…9fc` = SHA256SUMS 一致）／ `$PKG/06_…`（v0.2.7・733 行・sha256 `2d35d144…da4` = 一致）／ `$PKG/check_review_candidate.py`（sha256 `246b0644…7e4` = 一致・R7-16 の試験は COPY で実施）
- 略号: `$PKG` = `eval_runs/troot_optE_dapg_wholeroute_scope_20260701/WMSO_EXACT_BASELINE_REVIEW_v0.2_20260903`、`$D` = `eval_runs/troot_optE_dapg_wholeroute_scope_20260701`、`$OUT` = `/tmp/claude-0/-home-user-IsaacLab/f88a4426-3d65-5cc2-ba28-87f4976e6a66/scratchpad/review5`、A = `$D/WMSO_D11A_CONTRACTS_V2_DESIGN_RSTECHLEAD2_20260719.md`、D0 = `$D/WMSO_D0_ARCHITECTURE_DRAFT_RSTECHLEAD2_20260718.md`
- 拘束: 各 finding に text / logic / impact の 3 lens を独立に当て、**反証を試みる**（既定 `refuted = true`）。confirmed ⇔ 2 lens 以上が反証できず impact ≠ NOT_A_DEFECT。Rs 裁定（`11_` §8・`A_GPT56sol`: B+ / 外部 baseline / registry + acceptance record / OP-19 三層）の**設計方向は欠陥ではない**。`11_` §10.1 の起草既定（head-check (c)(d) = HOLD・`P_VALIDITY_UNBOUNDED` の kind 集合・`base_pose_tolerance_*` / `max_validity_s` の値・identity 系 diagnostic item 必須化・role registry head の発行者 / ACL・`COMMAND_ADMITTED` 同期 block）への単なる反対は impact で refute。陽性対照の注入 8 欠陥（`10_` §1）は実本文に無いことを前提とし、R7 も報告していない。

## 0. 手続記録（自己実測・`date -u` 順）

```text
$ date -u                                            → Sun Sep  6 00:03:57 UTC 2026
$ git log -1 --oneline                               → fed354bd WMSO: record the external review request for v0.2.7 (two-key input)
$ git branch --show-current                          → claude/v0-2-design-candidates-snqbmy
$ date -u; bash $PKG/verify_exact_baseline_pins.sh   → 00:04:02 UTC / 11/11 PASS
    PASS D1.1-A DESIGN v2.11.2 (worktree)  00192d20…8aff / PASS EP v1.9 md c474acea…27e7 / PASS EP v1.9 json e63176af…1c6e /
    PASS D1.1-B DESIGN v13 5a1874d3…49a6 / PASS blob 1353430… / PASS blob ebe8154… / PASS blob sha256 ×2 /
    PASS at frozen commit 54f90a7d / PASS at frozen commit 07250f4a / PASS evidence_policy_definition_hash e7ca4309…3803
    policy_semver=1.9.0  HEAD=fed354bdefba1fc346d3e2756145ce75c31b1036 branch=claude/v0-2-design-candidates-snqbmy
$ date -u; python3 $PKG/check_review_candidate.py $PKG/05_WMSO_RUNTIME_SPEC_v0.2_REVIEW_CANDIDATE_20260903.md --runtime
                                                     → 00:04:17 UTC / citations=153 FAIL=0 WARN=11
                                                        （WARN 行 = 88 / 98 / 98 / 146 / 234 / 554 / 556 / 822 / 832 / 1044 / 1044・全 C2 heuristic。原本 = $OUT/checker_05_V7.txt）
$ python3 $PKG/check_review_candidate.py $PKG/06_WMSO_INDUSTRIAL_PROFILE_v0.2_REVIEW_CANDIDATE_20260903.md --profile
                                                     → citations=65 FAIL=0 WARN=26（全 C2 heuristic。原本 = $OUT/checker_06_V7.txt）
$ sha256sum 05 06 check_review_candidate.py ↔ $PKG/SHA256SUMS.txt
                                                     → 05 007d753d4ece153d30168cc9b09631e758fdafed55116affe9a9dc1d75bfa9fc = 一致
                                                        06 2d35d144e00dafdf4b0d96737ca48a614d0bab1038c59a9dcd5563782bb20da4 = 一致
                                                        checker 246b0644f305b87dfc691a8f2db78fb1ab432790b1b6ad884d903a19baa727e4 = 一致
$ git diff --stat f1393050 fed354bd -- 05 06        → 空（exit 0）= byte 同一
$ cat -n 05（1-350 / 350-700 / 700-1050）／ cat -n 06（1-370 / 370-733） → 全行読了（chunk 読み・00:05–00:10 UTC）
$ grep -n ownership / role.registry / PROPOSED / 'sub = ' / genesis / head-check / 'lease 中' 05 06   → §2 各 finding に引用（00:10 UTC）
$ sed -n 270,274p / 317,320p D0；sed -n 380,382p / 496,506p / 151p A；sed -n 18,32p RS71 §0
$ sed -n 150,230p $PKG/check_review_candidate.py（Q2 = :186-195）
$ awk '/^## 8/…' / '/^### 10\.1/…' $PKG/11_THREE_AXIS_REVIEW_RECORD_20260903.md；grep 見出し・推奨行 $PKG/review_records/rs_consult/A_GPT56sol_20260904.md
$ python3 で $OUT/R8_findings_v027.json の id / severity / claim 先頭を skim（重複注記のみ）
$ date -u → 00:12:54 UTC；mkdir $OUT/q2test_V7；cp 05 06 check_review_candidate.py → COPY
$ [A] python3 COPY/check…py COPY/06 --profile（cwd = repo root）    → FAIL=0 WARN=26（Q2 PASS・baseline）
$ [B] COPY/05 の (j) 列挙から `predicate_sha256` を除き、除外文へ移す → FAIL=0 WARN=26（**Q2 依然 PASS** = R7-16 再現）
$ [C] 対照: COPY/05 の (j) から `predicate_sha256` を完全削除          → FAIL=1 「Q2 06 §2.1/2.2 hash field 'predicate_sha256' (SafetyRestrictionSet) is not named in 05 §3.4 (j)」
$ COPY を復元（sha256 先頭 007d753d4ece153d）；git status --short eval_runs/ → 空（実 file 無変更）；date -u → 00:13:26 UTC
$ date -u → 00:17:29 UTC；V7_results_v027.json 書込 → json.load OK / list / 19 entry / id 一意・順序 R7-01..19 / schema key 9 個一致 /
    confirmed 18・refuted 1・merged 0・severity {MEDIUM 7, LOW 11, NOT_A_DEFECT 1}
$ date -u → 00:17:41 UTC（本 file の Write 直前に実測）；本報告書込
```

## 1. 集計

| 区分 | 件数 | id |
|---|---|---|
| CONFIRMED / MEDIUM | 7 | R7-01 / R7-02 / R7-03 / R7-04 / R7-05 / R7-06 / R7-07 |
| CONFIRMED / LOW | 11 | R7-08 / R7-09 / R7-10 / R7-11 / R7-12 / R7-13 / R7-14 / R7-15 / R7-16 / R7-17 / R7-18 |
| REFUTED（NOT_A_DEFECT） | 1 | R7-19（impact lens で refute — 保守側・acceptance 失効による良性退役経路が既に在る・registry 意味論は Rs 専権） |
| MERGED（R7 内重複） | 0 | — |
| severity 変更 | 0 | reviewer 付値を全件維持（R7-05 は regime 注記付きで MEDIUM 維持、R7-12 は再現例を差し替えて LOW 維持） |
| reviewer の fix を置換 / 補正 | 5 | R7-02（head 再定義を主・副 case 追加案は不採用）、R7-03（供給側の原子 batch 義務を追加）、R7-05（違反 code = 既存 `R_LEASE_UNKNOWN_COMMAND` 行の拡張）、R7-06（`CalibrationRef.measured_at_iso8601` 追加を主）、R7-14（profile_hash は条件化せず provenance に降格） |

CRITICAL / HIGH = 0。全 confirmed の minimal fix は凍結 4 file への schema delta 0（runtime / deployment 層の型・散文・package helper のみ）。R8 との重複（注記のみ・merge せず）: R7-01 ↔ R8-20、R7-02 ↔ R8-05、R7-03 ↔ R8-15、R7-06 ↔ R8-07、R7-07 ↔ R8-09、R7-15 ↔ R8-01、R7-14 ↔ R8-22（部分）、R7-08 ↔ R8-14（部分）。

## 2. Finding 別 verdict

### R7-01（MEDIUM・CONFIRMED）— permit 前提条件 (m) が SAFEHOLD で評価不能（= FALSE）になり、genesis 直後・1 周期超の HOLD の後に permit を発行できない
- **sed 実測**: 05:191 (m)「DeploymentValidityMonitor の直近 heartbeat が … 内 ∧ baseline `diagnostic_items` の全項目が直近周期内に pass（評価不能 / 欠落 = FALSE）∧ 直近の head-check が周期内に成功。違反 = `R_REGISTRY_UNAVAILABLE`」／ 05:292 head-check 行 from 列 =「any (EXECUTOR)」・副場合 (a)「`head(registry, lease.profile_hash)` の `record_hash ≠ lease.acceptance_record_hash`」・(e)「≠ `record.timing_baseline_hash`」（lease / record を参照）／ 05:88「manager は monitor に依存しない head-check（§3.7）を周期実行する」（owner 条件なし）／ 05:143 初期状態 = `SAFEHOLD … INITIAL`／ 06:531「identity 系 4 項目 = `health_checks[]` の同 kind の check_id を **lease 中に**周期再実行（結果は (m) と head-check が参照）」／ 06:68「identity 系 check … は baseline diagnostic item として lease 中**も**周期再検証する」／ 05:707「fail-closed 既定: 評価不能 = FALSE」。
- **text lens（未反証）**: 引用は全て実在。head-check は遷移表の 1 行（from = EXECUTOR・副場合が lease field を参照）としてのみ定義され、SAFEHOLD での「head-check の成功」を定義する文は 05 に無い（grep head-check = 05:88 / 191 / 292 / 343 / 345 / 829 のみ）。05:88 の無条件文と from 列は矛盾。identity 系 item は 06:531 で「lease 中に」の再実行と定義される。
- **logic lens（未反証）**: genesis → SAFEHOLD(INITIAL)。最初の permit 要求で (m): 「直近の head-check」= SAFEHOLD では未定義 → 05:707 の既定で FALSE；identity item の「直近周期内の pass」= lease 中の再実行が無いので FALSE ⇒ `R_REGISTRY_UNAVAILABLE`・permit 不発行。以後 head-check は EXECUTOR でしか走らないので状態は変わらない（1 周期超のどの SAFEHOLD でも同型）。反証候補 = 05:88 の無条件文を採り、(a) BEFORE_PERMIT の同 check_id 実行を当該周期の結果と数える読み — 本文に書かれておらず、本文自身の既定が曖昧さを FALSE 側に解く。
- **impact lens（未反証）**: liveness 0（fail-closed 方向・凍結 / 裁定違反ではない）。R5-03 fold が本文どおりでは実装不能 — R5-03 と同 class の MEDIUM。R8-20 と重複（R8 は LOW）。
- **verdict**: CONFIRMED / MEDIUM。
- **minimal fix（frozen delta 0・散文のみ）**: (1) 05:292 from 列 →「any」＋「S_SAFEHOLD では副場合 (b)(c)(d) のみ評価（lease が無い (a)/(e) は N/A = True — 05:451 WAIT の N/A 規約と同型）。SAFEHOLD で副場合が発火した場合は INV-30 の理由強化 / 記録付き no-op」；(2) 05:191 (m) に「head-check と manager 実行項目（VALIDITY_MONITOR_LIVENESS / AUDIT_RECORDER_LIVENESS / CLOCK_DRIFT / REGISTRY_HEAD_CHECK）は SAFEHOLD 中も周期実行し、identity 系 item は (a) の BEFORE_PERMIT 実行（同 check_id）を当該周期の結果として扱う」；(3) 06:531「lease 中に」→「lease 中および SAFEHOLD 中（BEFORE_PERMIT 実行を含む）」・06:68 を同期；(4) T に「INITIAL からの最初の permit が monitor 生存下で (m) を通る」対照 case を追加（T-40 は陰性側）。

### R7-02（MEDIUM・CONFIRMED）— head-check (a)・(i)・条件 12 が registry の `PROPOSED` head を列挙せず、06 が許す gen+1 の PROPOSED 登録が活性 lease を「表に無い = SAFE_STOP」で止める
- **sed 実測**: 06:530「**state ∈ {PROPOSED, ACCEPTED} の record について**: generation が同一 profile_hash の直前 ACCEPTED / PROPOSED record + 1 でない」（= PROPOSED(gen+1) を ACCEPTED(gen) の後に append 可）／ 06:428 規則 (2)「第 1 評価点 = 登録時 = `ProfileAcceptanceRecord(state = PROPOSED, …)` の書込みの下で評価」／ 06:522「ACCEPTED → ACCEPTED(generation +1)（evidence 追加 / 再受理・受理条件は同じ …）」／ 06:532「head = registry append seq 最大」／ 05:292 (a)「`record_hash ≠ … ∨ state ≠ ACCEPTED`」→ disposition「head.state ∈ {SUSPENDED, REVOKED} = … 表で（REVOKED / 不読 = 表に無い = SAFE_STOP）、head が新 generation の ACCEPTED … = HOLD、兄弟 …」／ grep PROPOSED 05 = 0 hit。
- **text lens（未反証）**: 05 は PROPOSED を一切扱わない。(a) の発火条件は PROPOSED head で真になるが disposition 列挙に無い。
- **logic lens（未反証）**: P gen1 ACCEPTED・CLOSED_LOOP lease L 活性。commissioning が PROPOSED(gen2) を append（06:530 で合法・再受理は PROPOSED を経る 06:428）。次の head-check: head = PROPOSED(gen2)・`record_hash ≠ ∧ state ≠ ACCEPTED` → (a) 発火 → 列挙外 → SAFE_STOP・sub 語彙外。PROPOSED 窓の間 (i)/条件 12 も `state == ACCEPTED` 偽で新 permit を拒否。R5-13 の HOLD(GENERATION_SUPERSEDED) には到達しない。
- **impact lens（未反証）**: 良性の管理操作が活性 lease を SAFE_STOP（operator clearance 要）で止め、再 lease も ACCEPTED(gen2) まで不能。fold の totality 欠落・保守側 → MEDIUM。R8-05 と重複。
- **verdict**: CONFIRMED / MEDIUM。
- **minimal fix（frozen delta 0）**: (i) 05:191・条件 12 05:240・head-check (a) 05:292 の「head」を **authority head** =「state ∈ {ACCEPTED, SUSPENDED, REVOKED} の record のうち append seq 最大（PROPOSED は authority を持たず head 判定から除外）」と定義し、06:532 の seq 最大 head は fork 検出 / `supersedes_record_hash` 用に残す。06:522/532 に「PROPOSED(gen+1) は authority head を変えない。活性 lease は ACCEPTED(gen+1) が append されるまで ACCEPTED(gen) に束縛され、append 後に head-check (a) → HOLD(GENERATION_SUPERSEDED)」を 1 文。⚠ reviewer 代替案（副場合「PROPOSED = HOLD」）は良性登録で lease を止める点が残るため不採用。

### R7-03（MEDIUM・CONFIRMED）— v0.2.7 の role registry の head 変更に OP-19 三層のうち event 層の義務者も周期層（head-check）の副場合も無い
- **sed 実測**: 05:88 monitor 最小入力 =「controller / ISL / tool の identity 報告・layout と baseline registry（…）の supersession・registry の state 変化（R4-14）」（role registry 無し）／ 05:292 副場合 (a)-(e) に role registry head の比較なし／ 06:529「head の変更 = supersession: **認可された一者または monitor が** bound する全 ACCEPTED record に `SUSPENDED(ROLE_REGISTRY_SUPERSEDED)` を append」／ 対比 06:528（baseline registry）「monitor は event（BASELINE_SUPERSEDED）を出し、manager は head-check で … monitor 非依存に検出する」／ 05:191 (i)・05:240 条件 12 は permit / CAS 時のみ。
- **text lens（未反証）**: 06:529 は許容文で義務者が無く、05:88 の入力にも 05:292 の副場合にも無い。05:88「registry の state 変化」は ProfileRegistry の状態変化（SUSPENDED が誰かにより append された後の検出）であって role registry の head 変化ではない。
- **logic lens（未反証）**: lease L（record A・`role_registry_hash = h1`）活性。role registry head → h2。monitor は入力に無く event を出さない；append 義務者なし；(a)-(e) 全 pass。L は `lease_max_duration_s` / validity deadline まで走る。次 permit は (i) で拒否（新 lease のみ止まる）。
- **impact lens（未反証）**: 本文自身が宣言した trigger（`SuspensionReason.ROLE_REGISTRY_SUPERSEDED` → HOLD・06:346）に対し OP-19 の (2) event・(3) 周期層が結線されていない = R6-05 fold の未完（裁定の設計方向に反対しているのではなく、その構造が新型に適用されていない）。曝露は HOLD disposition・`lease_max_duration_s`・新 lease は (i) で拒否 ゆえ有界 → HIGH ではなく MEDIUM。R8-15 と重複（R8 は LOW）。
- **verdict**: CONFIRMED / MEDIUM。
- **minimal fix（frozen delta 0）**: (1) 06:529 を「role registry head を変更する custodian は、bound する全 ACCEPTED record への `SUSPENDED(ROLE_REGISTRY_SUPERSEDED)` を **同一 append batch（原子・R5-12 の 06:524 と同型）**で行う」と義務化（供給側で閉じる）；(2) 05:88 最小入力に「role registry（06 §8.3・1 head）の supersession」を追加；(3) 05:292 に副場合 (f)「`head(role_registry) ≠ record.role_registry_hash` → FAULT `R_PROFILE_SUSPENDED`（trigger = HEAD_CHECK・sub = ROLE_REGISTRY_SUPERSEDED）→ 06 §5 (b) ROLE_REGISTRY_SUPERSEDED 行（HOLD）」を (e) と同型で追加；(4) R7-09 採用時は sub 語彙に ROLE_REGISTRY_SUPERSEDED。

### R7-04（MEDIUM・CONFIRMED）— manager 不読（`R_MANAGER_UNAVAILABLE`）の間、SAFE_STOP disposition の SAFEHOLD が SafeStop から SafeHold へ緩む
- **sed 実測**: 05:520「(iii) else SafeHold（`state.safehold_disposition == SAFE_STOP` なら SafeStop — 線形化読み…）」／ 05:525「読みが `manager_liveness_s` 内に完了しない場合、(ii) を評価せず (iii) に落ちる（INV-19）」／ 05:729 INV-19 同文（SafeStop 分岐の扱い無し）／ 05:327「`R_MANAGER_UNAVAILABLE` … 出力 = (iii) **SafeHold**・lease は形式上継続」／ 05:82 gateway 状態 =「線形化読み（cache を持たない）+ 直近 admitted command + `restart_pending` flag」／ 05:249「(iii) は … 当該 SAFEHOLD 遷移の記録 disposition が SAFE_STOP なら §5.4 SAFETY 行の clearance record が記録されるまで SafeStop を出す」／ 05:498「`SafeStop` = `SafeHold` + profile `ControllerEnvelope` が定義する stop-class 要求」。
- **text lens（未反証）**: (iii) の SafeStop 分岐は線形化読みにしか依拠せず、読み不能時の扱いは 05:327 が字義で SafeHold と書く。gateway に disposition の局所 copy は無い。
- **logic lens（未反証）**: TCP calibration 期限 → TRANSFER_TO_SAFEHOLD(SAFE_STOP)（06:346 TCP → SAFE_STOP）・出力 SafeStop。manager ↔ gateway link 断（manager 生存）→ 読みが `manager_liveness_s` 超過 → 述語評価不能 → 05:707 既定 FALSE かつ 05:327 字義 → SafeHold。ISL 決定は無いので (i)/(i′) は効かない。link 復帰まで stop-class 要求が外れる。
- **impact lens（未反証）**: 05:249 の規範「clearance record まで SafeStop」が manager 不読で破れ、評価不能が弱い出力側に倒れる。stop-class の物理効果は OPP-4 の外だが、仕様層の安全出力規則の内部不整合 → MEDIUM。D0:318 は類推であって凍結違反ではない。
- **verdict**: CONFIRMED / MEDIUM。
- **minimal fix（frozen delta 0）**: 05:82 の gateway 局所状態に `last_disposition`（直近に完了した線形化読みで観測した `safehold_disposition`・`restart_pending` / `pending_invalidate` と同格の単調 flag）を追加；05:520 (iii)「線形化読みが `manager_liveness_s` 内に完了しない場合: `last_disposition == SAFE_STOP` なら SafeStop、未観測なら SafeHold（INV-25）」；05:327 効果列 →「(iii)・直近観測 disposition を保持」；INV-19 に同文；T（SAFE_STOP 中に manager link 断 → 出力は SafeStop のまま）を追加。同 delta のより単純な代替 =「読み未完了 → 常に SafeStop」。

### R7-05（MEDIUM・CONFIRMED）— CommandGateway の admission が `arm_targets[].resource_id ⊆ lease.ownership.control` を検査せず、lease の `ownership` が actuation 境界で不活性
- **sed 実測**: 05:82 admission =「`(lease_id, control_epoch)` 等値 ∧ `cmd.executor_id == lease.executor_id` ∧ `¬boundary_wait` ∧ `AcceptedEnvelope` 連言 ∧ command 種別 = lease.control_mode ∧ `cmd_seq` 単調」／ grep ownership 05 = 15 / 44 / 53 / 63 / 82（語 no-double-ownership のみ）/ 112 / 191 (h) / 353 / 426 / 441 / 528 / 530 / 724 / 855 / 940 / 1032 — §5（05:486-546）に 0 hit／ 05:508 `arm_targets: (resource_id ∈ {ee_left, ee_right}, setpoint_ref)`／ 06:328「CONTROLLER(profile.cell.arms[r].controller for r in cmd.arm_targets[].resource_id)」／ 05:724 INV-14「`definition.required_control_resources ⊆ lease.ownership.control`」／ 05:438「**完全に** lease へ束縛」／ 凍結 A:151 `ControlResourceSpec = {ee_left, ee_right, gripper_left, gripper_right: bool}`・A:506 `HandoffOffer.ownership: Ownership`・A:380「required ⊆ offered」= `validate_invocation_start` の検査。
- **text lens（未反証）**: admission 連言に ownership 項は無く、CONTROLLER 項は profile 側で index する。frozen の包含検査は invocation 開始時のみ。
- **logic lens（未反証）**: 部分 offer `{ee_left: T, ee_right: F, gripper_left: T, gripper_right: F}`（frozen 型上合法）・required = {ee_left, gripper_left} → `validate_invocation_start` pass → lease L（(h) 05:191 は `ownership ⊆ resource_availability` の上界のみ）。command `arm_targets = ((ee_left, s1), (ee_right, s2))` → 全連言が評価可能かつ pass → ee_right が動く。§11 項 5 の OUT（2 executor 同時保持）とは別 case（1 executor）。
- **impact lens（未反証）**: 唯一の actuation 経路で lease の ownership 境界が執行されない（未所有資源に対する fail-open）。RS71 §0 #1 の every-motion 両腕 regime では offer が両腕 True になるのが通常で THREAD 内の実曝露は小さく、INTER_ARM / CONTROLLER / WORKSPACE 項が運動自体は拘束する → HIGH ではなく MEDIUM。
- **verdict**: CONFIRMED / MEDIUM（regime 注記付き）。
- **minimal fix（frozen delta 0・新 code 不要）**: 05:82 / INV-03 に conjunct「`∀ (r, _) ∈ cmd.arm_targets: lease.ownership.control[r] == True`」を追加。違反 = 既存 05:322 行（`R_LEASE_UNKNOWN_COMMAND`・拒否のみ・audit）を「∨ cmd が `lease.ownership.control` に無い resource を提案」へ拡張（executor_id 不一致と同じ束縛不一致 class・R1-12 / R3-19 の先例）。reviewer 案（CONTROLLER 項の評価不能 = FALSE → `R_ENVELOPE_VIOLATION`）も delta 0 で可だが ownership を envelope に混ぜるため置換。05:508 に「gripper 動作の command は本 record の外（v0.2）」を明記し新 OP として記録（`gripper_targets` 追加は設計拡張・起草者 / Rs）。

### R7-06（MEDIUM・CONFIRMED）— `P_VALIDITY_UNBOUNDED` の上限項が `CalibrationRef` に無い `measured_at` を要し、第 1 評価点で評価不能 = 違反 → 全 profile 登録不能
- **sed 実測**: 06:298「`CalibrationRef.valid_until_iso8601`、および required record のうち kind ∈ {…} の `valid_until_iso8601` が None（無期限）、**または** `valid_until − measured_at > baseline.max_validity_s[kind]`（上限・… v0.2.7 R6-11）（… 評価点 = §8.1 (2) の両評価点（R5-04））」／ 06:202-206 `CalibrationRef = {calibration_id, kind, artifact_sha256, valid_until_iso8601}`（`measured_at` 無し）／ 06:368「§3 `P_VALIDITY_UNBOUNDED` が挙げる kind の required record と CalibrationRef は None 不可・上限 = baseline `max_validity_s[kind]`」／ 06:512 `max_validity_s: dict # CalibrationKind / DeploymentEvidenceKind → 上限`／ 06:428 規則 (1)「全検査は fail-closed（評価不能 = 違反）」・第 1 評価点 list「`P_VALIDITY_UNBOUNDED`（`CalibrationRef` 側 + MIN_SHADOW の SAFETY_LAYER_ACCEPTANCE・v0.2.7 R5-04）」／ 06:388 MIN_SHADOW に CALIBRATION_RECORD 無し。
- **text lens（未反証）**: 「または」節は同じ主語（CalibrationRef を含む）に掛かり、06:368 / 06:512 も calibration に上限を掛ける意図を示す。一方 CalibrationRef 型に `measured_at` は無く、06:428 の第 1 評価点 list に「None 検査のみ」の限定も無い。
- **logic lens（未反証）**: 任意 profile の登録（第 1 評価点・MIN_SHADOW には CALIBRATION_RECORD 無し）で上限項を CalibrationRef に評価 → `measured_at` が型に無い → 評価不能 → 規則 (1) で違反 → 登録拒否。calibration artifact の内容から取る読みは本文に無く、規則 (1) は推測を許さない。PT-43 は第 2 評価点の record 側のみ。
- **impact lens（未反証）**: 登録 block（deployment 層の liveness 0・保守側）。R6-11 fold が導入した型 / 規則の不整合であって裁定事項ではない → MEDIUM（R7-01 と同 class）。R8-07 と重複。
- **verdict**: CONFIRMED / MEDIUM。
- **minimal fix（frozen delta 0・deployment 層の型のみ）**: `CalibrationRef` に `measured_at_iso8601: str` を追加（06:202-206）し、R6-11 の上限を第 1 評価点で評価可能にする（06:512 が CalibrationKind を key にしている意図と整合）；06:298「CalibrationRef 側 = None 検査 + `valid_until − measured_at` 上限」・06:368・06:428 第 1 評価点の括弧を同期；PT-32 に「`valid_until − measured_at > max_validity_s[TCP]`」case を追加。代替（型変更なし）= reviewer 案: CalibrationRef 側の第 1 評価点は None 検査のみ、上限は第 2 評価点で subject = `calibration_id` の CALIBRATION_RECORD の `measured_at` で評価（record 不在 = `P_EVIDENCE_UNBOUND`）。

### R7-07（MEDIUM・CONFIRMED）— MIN_FAULT_INJECTION が `R_SAFETY_LAYER_LOST` / `R_BOUNDARY_DWELL_EXCEEDED` を bare code のまま置き、`trigger = None` で timeout 経路の要求を回避できる
- **sed 実測**: 06:391「∪ {R_DEADLINE_MISS, **R_SAFETY_LAYER_LOST**, …, **R_BOUNDARY_DWELL_EXCEEDED**, …}」（bare）／ 06:392「∪ {R_CALIBRATION_EXPIRED_ACTIVE#DEADLINE, R_EVIDENCE_EXPIRED_ACTIVE#DEADLINE, R_PROFILE_SUSPENDED#EVENT, …}」／ 06:387「規範最小集合（… comment ではなく本文の規則）」／ 06:395「timeout 駆動 code の FAULT_INJECTION_RESULT は subject_ref = '<code>#<timeout_trigger>'（例 R_SAFETY_LAYER_LOST#HEARTBEAT_TIMEOUT・R_BOUNDARY_DWELL_EXCEEDED#DWELL_TIMEOUT）で timeout 経路そのものを要求する（… subject = trigger が None なら code・あれば code#trigger … health==failed / attempts 尽きの別経路で代替できない …）」／ 06:374「None = trigger を問わない」／ 06:320 `P_EVIDENCE_POLICY_TOO_WEAK` = MIN を「含む」か／ 06:296 `P_EVIDENCE_UNBOUND`: `subject_ref == runtime_fault_code[#trigger]`／ 05:702 `R_SAFETY_LAYER_LOST: HEARTBEAT_TIMEOUT | HEALTH_FAILED`・`R_BOUNDARY_DWELL_EXCEEDED: DWELL_TIMEOUT | ATTEMPTS_EXHAUSTED`。
- **text lens（未反証）**: 規範文が名指す 2 例が集合定義では bare。MIN 包含検査は code 単位としか読めない。
- **logic lens（未反証）**: `FaultInjectionRequirement(R_BOUNDARY_DWELL_EXCEEDED, CLOSED_LOOP, trigger = None)` → MIN 包含 pass → 要求 subject = bare code → `P_FAULT_EVIDENCE_UNVERIFIED` の `<code>` 枝（trigger を問わない）が `payload.trigger = ATTEMPTS_EXHAUSTED` の FAULT で pass → dwell timeout 未発火で CLOSED_LOOP 受理。`R_SAFETY_LAYER_LOST` は `P_SAFETY_BUDGET_EXCEEDED`（06:289）が `#HEARTBEAT_TIMEOUT` record を別途要求するため間接的に止まるが、`R_BOUNDARY_DWELL_EXCEEDED` は止まらない。
- **impact lens（未反証）**: R2-01 → R5-10 / R6-03 の fold が閉じるはずの迂回が MIN code に残る（evidence gate の fail-open）→ MEDIUM。R8-09 と重複（R8 は更に 2 code を挙げる — merge せず注記のみ）。
- **verdict**: CONFIRMED / MEDIUM。
- **minimal fix（frozen delta 0）**: 06:391 の 2 entry を `R_SAFETY_LAYER_LOST#HEARTBEAT_TIMEOUT`・`R_BOUNDARY_DWELL_EXCEEDED#DWELL_TIMEOUT` に（06:392 と同形式）；06:320 / 06:379 に「`P_EVIDENCE_POLICY_TOO_WEAK` は `code#trigger` 単位で包含を判定（`#trigger` 付き MIN entry は `trigger = None` の要件では満たされない）」；06:374「None = trigger を問わない」に同旨の限定；PT に「MIN の timeout code を `trigger = None` で宣言 → `P_EVIDENCE_POLICY_TOO_WEAK`」を追加。

### R7-08（LOW・CONFIRMED）— R5-07 の厳密境界化が INV-35 / INV-07 に及んでいない
- **sed 実測**: 05:745 INV-35「decision age **≤** decision_max_age_s（条件 6・12）」↔ 05:234 条件 6「`now − decision.t_mono < decision_max_age_s`」・05:191 (e)「`age < decision_max_age_s`」／ 05:717 INV-07「`ack.valid_until ≥ permit.issued_at`」↔ 05:188 `effective_expires_at = min(expires_at, ack.valid_until, …)`・05:230 条件 2「`now < permit.effective_expires_at`」。
- **text / logic lens（未反証）**: age == max は INV-35 充足・条件 6 REJECTED（機械検査と CAS が食い違う）。`ack.valid_until == issued_at` の permit は INV-07 充足で発行されるが `effective_expires_at = issued_at` ゆえ条件 2 が発行直後から偽（dead-on-arrival・条件 2 優先規則で `R_ACK_MISBOUND`）。
- **impact lens（未反証）**: 境界の不一致・保守側 → LOW。
- **verdict**: CONFIRMED / LOW。
- **minimal fix**: INV-35 `≤` → `<`；INV-07 → `ack.valid_until > permit.issued_at`；05:191 (k) に「`effective_expires_at > issued_at` でなければ permit 不発行（条件 2 の優先規則の fault）」を 1 文。

### R7-09（LOW・CONFIRMED）— head-check 行の FAULT payload `sub` 語彙・(e) の `（BASELINE_SUPERSEDED）`・`DIAGNOSTIC:<item>` の `<item>` が §8 SSOT に束縛されない
- **sed 実測**: 05:292「(a) R_PROFILE_SUSPENDED（payload trigger = HEAD_CHECK・sub = SUSPENDED \| REVOKED \| GENERATION_SUPERSEDED \| SIBLING_SUSPENDED）… (e) R_PROFILE_SUSPENDED（BASELINE_SUPERSEDED）」（grep 'sub = ' = 本行のみ）／ 05:702「trigger 語彙の SSOT = 本列挙」・`R_PROFILE_SUSPENDED: EVENT | HEAD_CHECK | CAS_COND12 | PERMIT | DEADLINE`（`sub` / BASELINE_SUPERSEDED 無し）／ 06:300 `P_FAULT_CODE_UNKNOWN` = `trigger ∉ 05 §8 … 語彙`（`<item>` の語彙参照なし）／ 兄弟 REVOKED の sub 値なし。
- **text / logic lens（未反証）**: (e) を trigger = BASELINE_SUPERSEDED と実装すると 06 の FI 要件は宣言不能・`<code>#<trigger>` 枝も不一致。`sub` は typed payload に無い field。
- **impact lens（未反証）**: SSOT 未列挙の新語彙（round 3/4 と同 pattern）・FI evidence の宣言可能性に影響 → LOW。
- **verdict**: CONFIRMED / LOW。
- **minimal fix**: 05:702 に「`sub` 語彙（R_PROFILE_SUSPENDED のみ）= SUSPENDED | REVOKED | GENERATION_SUPERSEDED | SIBLING_SUSPENDED | SIBLING_REVOKED | BASELINE_SUPERSEDED（| ROLE_REGISTRY_SUPERSEDED — R7-03 採用時）」；(e) を「trigger = HEAD_CHECK・sub = BASELINE_SUPERSEDED」に；「`<item>` ∈ REQUIRED_DIAGNOSTIC_ITEMS ∪ `baseline.diagnostic_items` 名」と明記し 06:300 で未知 `<item>` を拒否。

### R7-10（LOW・CONFIRMED）— 同一条件（role_registry_hash ≠ 現 head）に 2 code が当たり得る／「registry 不読 = CAS 拒否」が TRANSFER_TO_SAFEHOLD まで含むと読める
- **sed 実測**: 05:191 (b)「doc 06 §8.1 (2) が第 2 評価点として列挙する**全 code** … 失敗 = `R_PROFILE_MISBOUND`」／ 06:428 第 2 評価点 list に `P_ACCEPTANCE_RECORD_INVALID`（head record 再評価）／ 06:530「検査 `P_ACCEPTANCE_RECORD_INVALID`（record 書込時 + 05 §3.4 (i) / 条件 12 で head record に再評価 …）… `role_registry_hash ≠ role registry の現 head`（書込時・R6-05）」／ 05:191 (i)「`P_ACCEPTANCE_RECORD_INVALID == 0 を head で再評価（role 充足・operator 相異）` … `record.role_registry_hash` == … 現 head（不一致 = `R_PROFILE_SUSPENDED`）」／ 05:343「起動時 / permit 発行 / CAS では拒否」・06:526「permit 発行・CAS・actuation は全面拒否」・T-36 ↔ 05:249「TRANSFER_TO_SAFEHOLD の前提条件 = 条件 1 のみ」。
- **text lens（未反証・ただし縮小）**: (i) の「（role 充足・operator 相異）」は再評価の範囲を部分的に限定するが、06:428 / 06:530 の再評価文は限定しない — 「2 code」ではなく「曖昧」が正確。第 2 部: 05:249 が明示するので TRANSFER_TO_SAFEHOLD は registry 非依存だが 06:526 / T-36 の字義は過大。
- **logic lens（未反証）**: 広い読みでは評価順で `R_PROFILE_MISBOUND` / `R_PROFILE_SUSPENDED` が分岐 → FI subject（`R_PROFILE_SUSPENDED#PERMIT`）照合・T-25 決定論が実装依存。06:526 字義では registry 不読中の ISL STOP → TRANSFER_TO_SAFEHOLD(SAFETY) が拒否されると読める（05:249 と衝突）。
- **impact lens（未反証）**: 決定論 / 明確性の欠陥・両読みとも fail-closed → LOW。
- **verdict**: CONFIRMED / LOW。
- **minimal fix**: 05:191 (i) に「head 再評価の `P_ACCEPTANCE_RECORD_INVALID` は role_registry_hash 節を除いて評価する（同節は本 (i) の `R_PROFILE_SUSPENDED`）」・06:530 の同節を「書込時のみ」と明記；05:343 / 06:526 / T-36 の「CAS」を「TRANSFER_TO_EXECUTOR の CAS（条件 12）」に限定し「TRANSFER_TO_SAFEHOLD は registry 非依存（05:249）」を 1 文。

### R7-11（LOW・CONFIRMED）— profile-scoped event の再試行述語が `profile_hash` だけを比較し、運ばれる `acceptance_record_hash` を使わない
- **sed 実測**: 05:249「event は `profile_hash` + `acceptance_record_hash` を運び、再試行は `current.active_lease.profile_hash == trigger.profile_hash` の間続ける」／ 06:522 ACCEPTED → ACCEPTED(gen+1)（profile_hash 同一）。
- **text / logic lens（未反証）**: gen1 SUSPENDED(OPERATOR) event が遅延 / 再送。その間に gen2 ACCEPTED（two-key）・lease L2（gen2）。遅延 event 到着 → 述語真 → TRANSFER_TO_SAFEHOLD(VALIDITY_EXPIRED) → L2 無効（head は ACCEPTED(gen2) なのに）。manager は CAS 前に registry head を再読しない。再受理 1 周期を越えて生き残る event が要るため narrow。
- **impact lens（未反証）**: spurious SAFEHOLD（保守側・availability）。必要な datum が既に運ばれているのに述語が使わない imprecision → LOW。
- **verdict**: CONFIRMED / LOW。
- **minimal fix**: 述語 → `current.active_lease.acceptance_record_hash == trigger.acceptance_record_hash`（`trigger.acceptance_record_hash` = 停止された ACCEPTED record の hash = SUSPENDED record の `supersedes_record_hash` と定義し、lease 束縛と等値が成立するようにする）；または manager が CAS 前に head-check (a) と同じ線形化読みで head 状態を確認。

### R7-12（LOW・CONFIRMED）— 同 tier / 同 reason no-op で記録する「clearance の和集合」を条件 10 が消費しない（再現例は差し替え）
- **sed 実測**: 05:249「同 tier / 同 reason（v0.2.4・R1-03）: reason は保持・epoch 不変の記録付き no-op とし、要求される clearance の**和集合**を CAS_ATTEMPT payload に記録する」／ 05:238 条件 10・05:543「該当する clearance record = `record.safehold_reason == current.safehold_reason ∧ …`」／ 05:249 lease-scoped 理由 = 「EXECUTOR_LOST / HEALTH_FAIL / DEADLINE_MISS / TIMING_VIOLATION / ENVELOPE_VIOLATION / **CHECKPOINT_DISABLED** / NO_CHAIN / VALIDITY_EXPIRED の deadline」・再試行は `current.active_lease_id == trigger.lease_id` のときのみ；non-lease-scoped = SAFETY / SAFE_STOP / MANAGER_RESTART / GATEWAY_RESTART。
- **text lens（未反証）**: 和集合は記録されるだけで、条件 10 も 05:543 も current reason の record しか要求しない。
- **logic lens（未反証・例を差替）**: reviewer の例（SAFEHOLD(GATEWAY_RESTART) 中の InterruptOutcome → CHECKPOINT_DISABLED）は不成立 — CHECKPOINT_DISABLED は lease-scoped で、current が SAFEHOLD（`active_lease_id = None`）なら CAS は捨てられる。成立する変種: current = SAFEHOLD(CHECKPOINT_DISABLED)（lease L の interrupt）→ gateway 再起動 → TRANSFER_TO_SAFEHOLD(GATEWAY_RESTART)（non-lease-scoped・無条件再試行）→ 同 tier → no-op・reason は CHECKPOINT_DISABLED 保持・和集合 {CHECKPOINT_DISABLED, GATEWAY_RESTART} 記録。条件 10 は CHECKPOINT_DISABLED の record のみ要求し、GATEWAY_RESTART 行の clearance role（05:541）は要求されない（GATEWAY_CONFIG_MATCH 自体は必須 BEFORE_PERMIT kind・06:291 ゆえ (a) で担保）。
- **impact lens（未反証）**: R1-03 fold の tier 2 内組合せでの未完・(a) / INV-31 で部分緩和 → LOW。
- **verdict**: CONFIRMED / LOW。
- **minimal fix**: 条件 10（05:238）・05:543 を「当該 SAFEHOLD 期間に記録された和集合の各 reason について該当 record が存在（和集合は AuthorityState 外の manager 台帳で durable 保持・TRANSFER_TO_EXECUTOR で reset）」に。fold 記録の例は CHECKPOINT_DISABLED + GATEWAY_RESTART の順とする。

### R7-13（LOW・CONFIRMED）— (l) の「直前の PERMIT_ISSUED の wall より単調」が audit 記録にしか無い値を参照し、recorder 不能時に `R_CLOCK_ANOMALY` へ誤帰属する
- **sed 実測**: 05:191 (l)「`wall_now` が直前の PERMIT_ISSUED の wall より単調。いずれか偽 = `R_CLOCK_ANOMALY`・permit 不発行。PERMIT_ISSUED payload に `(wall_now, mono_now)` を記録」／ 05:139（R1-03: (iii) は「audit 読出しに依存しない」）／ 05:345 `R_AUDIT_UNAVAILABLE` 行。
- **text lens（未反証）**: 直前 permit の wall の保存先として本文が定めるのは PERMIT_ISSUED audit record の payload のみ（CommitPermit.issued_at は mono）。
- **logic lens（未反証）**: recorder 停止中に permit 要求 → 直前 wall を読めない → (l) 第 3 項 評価不能 → FALSE → `R_CLOCK_ANOMALY`（PERMIT）。結果（permit 不発行）は「記録が先」と (m) の AUDIT_RECORDER_LIVENESS でも同じなので、誤るのは帰属 / clearance class のみ。
- **impact lens（未反証）**: 帰属欠陥・いずれも fail-closed → LOW。
- **verdict**: CONFIRMED / LOW。
- **minimal fix**: 05:191 (l)「比較対象 = manager が durable store（permit 台帳と同じ store・CAS tuple 外）に保持する `last_permit_wall`」；recorder 不能は (l) でなく (m)（DIAGNOSTIC:AUDIT_RECORDER_LIVENESS → `R_REGISTRY_UNAVAILABLE`）/ `R_AUDIT_UNAVAILABLE` で扱う。

### R7-14（LOW・CONFIRMED）— genesis record が束縛する `profile_hash` と `role = clearance_roles[GENESIS]` はどこでも検査されない（GENESIS に R6-06 の自己申告 pattern が残る）
- **sed 実測**: 05:143「genesis record（epoch 起点・… `profile_hash`（この genesis が権限づける profile・v0.2.4 R2-15）・role = doc 06 `clearance_roles[GENESIS]`）」／ grep genesis 05 = 143 / 145 / 696 / 736 / 894 のみ（05:191 (a)-(m)・05:229-240 に無し）／ 05:543 の資格照合（R6-06）は clearance record のみ／ 06:220 GENESIS の追加 role。
- **text / logic lens（未反証）**: 任意 operator_ref が role 文字列 GENESIS を自己申告して genesis を書ける；permit は genesis.profile_hash と比較されない。
- **impact lens（未反証）**: authority の起点を作る record に R6-06 が閉じた pattern が残る；profile_hash 束縛は装飾。operator の物理アクセスと GENESIS_RECORDED audit で有界 → LOW。R8-22 と部分重複。
- **verdict**: CONFIRMED / LOW。
- **minimal fix（reviewer 案 (i) を置換）**: GENESIS_RECORDED payload に `role_registry_hash == 現 head ∧ (operator_ref, GENESIS) ∈ registry`（05:543 と同文）を要求。`profile_hash` は provenance（「この genesis を書いた clearance 手続の profile」）に**降格**し permit 条件にはしない — 1 cell に複数 profile（06 OPP-8・PT-14 A→B 切替）があり得るため genesis 束縛の `cell_identity_hash` 条件化は正当な切替を block する。

### R7-15（LOW・CONFIRMED）— (i)・条件 12 は role / profile registry の head を見るが baseline registry の head 等値を見ない（(e) のみ・from = EXECUTOR）
- **sed 実測**: 05:191 (i)「`timing_baseline_hash` の artifact を解決して再検証」（content のみ）；`record.role_registry_hash == … 現 head`；baseline head 節なし／ 05:240 条件 12 に baseline 項なし／ 05:292 (e)「cell_id の baseline registry head ≠ `record.timing_baseline_hash`（R6-10）」（from = EXECUTOR）／ 06:524 / 528 baseline supersession は monitor の SUSPENDED append + head-check (e)。
- **text / logic lens（未反証）**: baseline head → h2・monitor 生存だが append 遅延。SAFEHOLD からの permit: (i) は h1 の artifact を解決できるので pass → 条件 12 pass → lease 活性 → 次の head-check (e) で HOLD。曝露 = append 着地 / 1 head-check 周期。monitor 停止なら (m) が既に permit を止める。
- **impact lens（未反証）**: R6-05 の role registry 扱いとの非対称・小さな保守側の窓 → LOW。R8-01 と重複（R8 は MEDIUM）。
- **verdict**: CONFIRMED / LOW。
- **minimal fix**: (i)・条件 12 に「`head(baseline_registry, profile.cell.cell_id) == record.timing_baseline_hash`（不一致 = `R_PROFILE_SUSPENDED`・trigger = PERMIT | CAS_COND12・sub = BASELINE_SUPERSEDED）」（R6-05 節と同形）。

### R7-16（LOW・CONFIRMED）— checker Q2 は (j) 区間内の**除外文**に現れた hash 名も「名指しあり」と数える（COPY で再現）
- **sed 実測**: `check_review_candidate.py:186-195`: `a = jl[0].find("(j) profile が宣言する"); b = jl[0].find("が解決先の内容と一致", a); seg = jl[0][a:b]`；count = 区間全体への regex／ 05:191 の「(j) の対象外 = `binding_expectations[].tensor_binding_hash`（…）」は [a, b) の内側。
- **logic lens（未反証・再現）**: `$OUT/q2test_V7/` の COPY で [A] 無改変 → FAIL=0；[B] `predicate_sha256` を列挙から除き除外文へ移す → **FAIL=0（Q2 PASS のまま）**；[C] 対照: 完全削除 → FAIL=1「Q2 … 'predicate_sha256' (SafetyRestrictionSet) is not named in 05 §3.4 (j)」。実 file は無変更（`git status --short eval_runs/` = 空）。
- **impact lens（未反証）**: R6-13 の強化が docstring より弱い（除外宣言で充足）。計器のみ・設計変更なし → LOW。
- **verdict**: CONFIRMED / LOW。
- **minimal fix（package helper のみ）**: Q2 で「(j) の対象外 = 」から次の「。」までの sub-span を count 前に除き、除外名は明示 allowlist `EXCLUDED = {"tensor_binding_hash"}` でのみ許す（allowlist 外の名が除外 span にしか無ければ FAIL）。再実行で [B] 型の変異が FAIL し、無改変の 05/06 が FAIL=0 のままであることを確認。

### R7-17（LOW・CONFIRMED）— 06 header に v0.2.7 の時刻が無く、05 anchor 23 の C2 WARN 件数（6）が実測（11）と一致しない
- **sed 実測**: 06:3 の版時刻 = v0.2.1 / 2.6 / 2.5 / 2.4 / 2.3 / 2.2（grep）— v0.2.7 無し；06:692「v0.2.7 fold（2026-09-05 23:32 UTC）」／ 05:1050「残る C2 WARN（6 件）… slice prereg `:59` … frozen `:151`」↔ 本 run の checker WARN=11（88 / 98 ×2 / 146 / 234 / 554 / 556 / 822 / 832 / 1044 ×2）— 88 / 98 / 98 / 146 / 234 は anchor の特徴づけ外。
- **impact lens（未反証）**: 鮮度残差（records-must-match-fact）→ LOW。
- **verdict**: CONFIRMED / LOW。
- **minimal fix**: 06:3 に「v0.2.7 = 2026-09-05 23:32 UTC（`date -u` 実測）」；05:1050 を「11 件」に更新し 88 / 98 / 146 / 234 の性格（D0 `:318` / `:354`・contracts_v2 `:323` / `:570`・D11B `:255` の散文引用で、同じ文の識別子は本 doc の runtime 新語）を 1 文で足す。

### R7-18（LOW・CONFIRMED）— `R_MANAGER_UNAVAILABLE` 行の disposition「manager 復帰時に TRANSFER_TO_SAFEHOLD(MANAGER_RESTART) と同じ経路で HOLD」は link 断のみでは実現されない
- **sed 実測**: 05:327「gateway が `manager_liveness_s` 内に AuthorityState を読めない … 出力 = (iii) SafeHold・lease は形式上継続 | manager 復帰時に TRANSFER_TO_SAFEHOLD(MANAGER_RESTART) と同じ経路で `HOLD`」／ 05:294 MANAGER_RESTART 行 = manager 自身の再起動が trigger／ 05:525 / INV-19 は読み未完了中に (ii) を評価しないとだけ言う／ 05:449 `command_deadline_s` の計時。
- **text / logic lens（未反証）**: link 断のみ（manager 非再起動）: 読み復帰時に行の CAS を発行する主体が無い（manager は gateway の不読を知らない・報告義務は書かれていない）。本文どおりの実挙動 = 断 < `command_deadline_s` なら (ii) 再開、以上なら `pending_invalidate` 経由の `R_DEADLINE_MISS` 経路。
- **impact lens（未反証）**: 約束された HOLD に actor が無い「未実現行」・いずれも fail-closed → LOW。
- **verdict**: CONFIRMED / LOW。
- **minimal fix**: 05:327 を機構どおりに書き換える「読み復帰が `command_deadline_s` 内なら (ii) 再開（新 CAS 不要）、超えれば `R_DEADLINE_MISS` 行；gateway は `COMMAND_REJECTED`（R_MANAGER_UNAVAILABLE）を記録し復帰時に不読を manager へ報告」。Rs が「manager 不読の後は必ず HOLD」を望むなら gateway 報告 → manager CAS の強化が要る（選択として記録・既定 = 機構一致の書換え・R7-04 の `last_disposition` と連動）。

### R7-19（NOT_A_DEFECT・REFUTED）— 兄弟節が REVOKED（終端）を含み、良性理由で REVOKED した旧 profile が同一 cell の全 profile を恒久 lock する
- **sed 実測**: 05:191 (i)「同一 `cell_identity_hash` を持つ他 profile の head record が `state ∈ {SUSPENDED, REVOKED}`（新 generation の ACCEPTED で置換されていない）なら本 profile も `R_PROFILE_SUSPENDED`」・05:240・05:292 (a) 同節／ 06:486「REVOKED は終端。再使用 = 新 generation の新 record のみ」／ 06:532「REVOKED → 新 generation は SUSPENDED → ACCEPTED と同条件（原因除去 evidence + two-key）」／ 06:503 `valid_until_iso8601: str # 受理の有効期限（None 不可）`。
- **text lens（未反証）**: 引用どおり。RETIRED 状態は無い。
- **logic lens（未反証）**: P1 → P2 移行で P1 を REVOKED（置換が理由）にすると P2 の permit は (i) 兄弟節で拒否され、解除は P1 の two-key 再受理のみ — シナリオは成立。
- **impact lens（**反証**）**: (1) 方向は保守側で、凍結 / 裁定 / fail-closed bar のいずれにも反しない；(2) REVOKED を使わない良性退役経路が既に在る — P1 を ACCEPTED のまま必須の `valid_until`（06:503）失効に任せれば、兄弟節は state のみを見るため P2 を block せず、P1 自身は (i)「有効期限内」で lease 不可；(3) REVOKED = 意図的な「使うな」= cell 単位 trigger と読むのは R3-09 / R5-12 の設計選択で、registry 意味論は Rs 専権（OPP-13）。reviewer 自身も text fix でなく open item 化を提案。⇒ 欠陥ではなく設計上の好み / 運用上の注記。
- **verdict**: REFUTED / NOT_A_DEFECT（confirmed = false）。
- **minimal fix**: 不要。任意（Rs 専権・text fix ではない）: OPP「profile の非安全起因の退役（RETIRED 状態、または兄弟節を SUSPENDED / cell 単位理由の REVOKED に限定）」を、acceptance 失効が既に良性退役経路を与える旨の注記付きで起票してもよい — 本 verification の要件ではない。

## 3. 反証 trace の要約（lens 別）

| lens | 反証できた finding | 反証できなかった主因 |
|---|---|---|
| text | 0（R7-10 の「2 code」は「曖昧」に縮小、R7-13 の「audit 読出し」は「保存先が audit 記録のみ」に精緻化 — いずれも反証には至らず） | 19 件とも引用行は実在し、概念の grep（ownership / role registry / PROPOSED / sub / genesis / head-check / lease 中）で「別所に規則あり」は見つからなかった |
| logic | 0（R7-12 の再現例は不成立だったが成立する変種を構成した） | 各 scenario は本 doc 自身の定義（fail-closed 既定 05:707・lease-scoped 再試行 05:249・head = seq 最大 06:532・MIN 包含 06:320）の下で step ごとに追跡できた |
| impact | 1（R7-19 = NOT_A_DEFECT: 保守側・既存の退役経路・Rs 専権の registry 意味論） | 残 18 件は凍結違反ではないが、fold の未完（R5-02 / R5-03 / R6-05 / R6-11 / R6-13 / R2-01→R6-03 / R1-03 / R5-07）または fail-closed bar への内部不整合（R7-04 / R7-05）として欠陥性が残る。裁定の設計方向・§10.1 起草既定への反対に当たる finding は無かった |

## 4. 主張しないこと・境界

- 本 verification は AI（別 context）の準備 verify であり、pS / pY / pN / Rs の two-key・gate PASS・Rs 裁定を代替しない。verdict は verifier 付値で、fold 採否は起草者と Rs の判断。
- 凍結 4 file・`eval_runs/` 配下・`check_review_candidate.py` は一切編集していない（R7-16 の試験は `$OUT/q2test_V7/` の COPY のみ・実 file の sha256 と `git status` を再確認）。書込み先 = `$OUT` のみ（`V7_verify_v027.md` / `V7_results_v027.json` / `checker_05_V7.txt` / `checker_06_V7.txt` / `q2test_V7/`）。
- R8 の finding は重複注記にのみ用い、merge も再判定もしていない（R8 は別 verifier の対象）。
- severity は本 doc 群の従来 scale（liveness 0 / fold 未完 = MEDIUM、境界・語彙・鮮度 = LOW）に合わせた reviewer 付値を維持した。R7-05 は RS71 §0 #1 regime 下の実曝露が小さい点、R7-03 は曝露が HOLD・`lease_max_duration_s` で有界な点を明記した上で MEDIUM を維持 — 起草者 / Rs が LOW と読み替える余地は残る。
- minimal fix は全て凍結 schema delta 0 を確認した提案であり、設計を確定するものではない。R7-05 の `gripper_targets`・R7-18 の「不読後は必ず HOLD」・R7-19 の RETIRED は起草者 / Rs の選択として区別した。
- open = 0 を宣言しない。本 verification が触れていない領域: 06 §6.1 evidence の物理内容・execution profile 軸・composition・EP v1.9 内部・`SafeHold` の物理実現（OP-4 / OPP-4）・R8 の 25 件。
