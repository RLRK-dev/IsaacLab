# V2_verify_v023 — R2 finding（R2-01〜R2-19・v0.2.3 対象）の 3 lens 反証検証（独立・adversarial）

- 検証者 = ADVERSARIAL VERIFIER V2（別 context・authority 無し・凍結物へ非接触・`eval_runs/` 配下は一切編集していない — `git status --short -- eval_runs/` = 空、2026-09-04 22:50:07 UTC `date -u` 実測）。
- 対象 = `$PKG/06_WMSO_INDUSTRIAL_PROFILE_v0.2_REVIEW_CANDIDATE_20260903.md`（v0.2.3・526 行・全行 `cat -n` 読了）／ `$PKG/05_WMSO_RUNTIME_SPEC_v0.2_REVIEW_CANDIDATE_20260903.md`（v0.2.3・928 行・全行読了）。`$PKG` = `eval_runs/troot_optE_dapg_wholeroute_scope_20260701/WMSO_EXACT_BASELINE_REVIEW_v0.2_20260903`、`$D` = `eval_runs/troot_optE_dapg_wholeroute_scope_20260701`。凍結略記: A = D1.1-A contracts_v2、B = D1.1-B tensor_binding、D0 = D0 architecture。finding 本体 = `scratchpad/review2/R2_findings_v023.json`（19 件・全件読了）+ `R2_review_v023.md`（全文読了）。
- 規則（`scratchpad/review/briefs/VERIFY.md`）: text / logic / impact の 3 lens を各 finding に独立適用し**反証を試みる**。既定 = refuted（文書どおりに欠陥を再現できなければ反証）。2 lens 以上が未反証のときのみ confirmed。severity は verifier が再判定。rate / hold / control_mode の**等値のみ**（R1）は binding premise であり欠陥にしない。
- 重複統合: 19 件を突合し**完全重複なし**（R2-01(c) と R2-12 は `lease_max_duration_s` を共有するが claim が異なる〔evidence 集合の欠落 vs INV-33 の字面〕。R2-09 / R2-16 は `SAFETY_LAYER_ACCEPTANCE.subject_ref` を共有するが claim が異なる。R2-03 / R2-17 は CD-04 残差を共有するが対象が異なる〔未照合の宣言 hash vs evaluator の再照合時点〕）。全件 `merged_into = ""`。R2-05 は**他 reviewer の R1-04 と同一欠陥**（cross-reviewer 収束）だが R2 集合内の重複ではないので独立に検証した（§2 R2-05 参照）。

## 0. 共通前提の実測

| 項 | 実測 |
|---|---|
| pin 再測 | `bash $PKG/verify_exact_baseline_pins.sh` = **PASS 11 / FAIL 0**（D1.1-A `00192d20…` @ `54f90a7d`・blob `135343…`／ D1.1-B `5a1874d3…` @ `07250f4a`・blob `ebe815…`／ EP md `c474acea…`／ EP JSON `e63176af…`・definition hash `e7ca4309…`・`policy_semver=1.9.0`）。HEAD = `ab52137f8ecf0c9016d78b99423c74816ce7d7a8` branch `claude/v0-2-design-candidates-snqbmy`（⚠ R2 report は HEAD `ddb37db1d3…` で再測している — branch が進んだが凍結 4 file の sha / blob は同一 = pin PASS） |
| 機械検査 | `python3 $PKG/check_review_candidate.py 06… --profile` = **citations=60 FAIL=0 WARN=24**（全 WARN = C2 heuristic・R2 report §0 と同一値） |
| 凍結行の実読 | D0:267-274（offer→accept→commit・fail-closed = producer retains）／ D0:310-320（ISL = 別 owner module・heartbeat/health 行 :318・`stabilized_post_action_state` :320）／ D0:354-355（monotonic clock）／ A:39（runtime val. は certificate を無効化しない）／ A:151（`ControlResourceSpec = {ee_left, ee_right, gripper_left, gripper_right: bool}`・`FreshnessPolicy`）／ A:380-382（validate_* の検査内容・責務分離）／ A:385（strict codec 5 拒否 = duplicate / NaN / unknown field / enum / lone surrogate — **missing field は列挙に無い**）／ A:537（U14 = runtime 記録の hash 方式 defer）／ B:23-24（反循環・B-declared 明示 null）／ B:40・:127（`ActionHold` = policy step 間の保持規約）／ B:255（D-4: 凍結側は content-addressed store を要求しない）／ RS71:23（DUAL-ARM 不変前提 = UR15 × 2 が全 motion で両腕）。全て `sed -n` で確認 |
| handoff 決定 | `scratchpad/handoff/HANDOFF_DECISIONS_v0.2_20260903.md` 全読（B: `AcceptedEnvelope` の 4 項／ C-6: lease への完全束縛／ C-7: monotone strengthening／ C-9: 合成は別 delta／ D: cell・controller・tool・payload = `IndustrialDeploymentProfile`／ F: deployment evidence = 別 policy） |

## 1. 判定一覧

| id | text | logic | impact | verdict | severity（V2） | severity（R2） |
|---|---|---|---|---|---|---|
| R2-01 | 未反証（(d) の 2/3 は誤り） | 未反証（反例 profile は修正版で再現） | 未反証 | **CONFIRMED** | **MEDIUM（降格）** | HIGH |
| R2-02 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | MEDIUM | MEDIUM |
| R2-03 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | HIGH | HIGH |
| R2-04 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | HIGH | HIGH |
| R2-05 | 未反証 | 未反証 | 未反証 | **CONFIRMED**（= R1-04） | MEDIUM | MEDIUM |
| R2-06 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | MEDIUM | MEDIUM |
| R2-07 | 未反証 | **反証**（「永久に不能」は過大） | 未反証 | **CONFIRMED（範囲縮小）** | **LOW（降格）** | MEDIUM |
| R2-08 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | MEDIUM | MEDIUM |
| R2-09 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | MEDIUM | MEDIUM |
| R2-10 | **反証** | **反証** | **反証** | **REFUTED** | NOT_A_DEFECT | LOW |
| R2-11 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | LOW | LOW |
| R2-12 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | LOW | LOW |
| R2-13 | 未反証 | 未反証 | **反証**（fail-closed に落ちる） | **CONFIRMED** | LOW | LOW |
| R2-14 | 未反証（(a) のみ） | 未反証（(a)）／ (b) は反証 | **反証**（fail-closed） | **CONFIRMED（(a) のみ）** | LOW | LOW |
| R2-15 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | LOW | LOW |
| R2-16 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | LOW | LOW |
| R2-17 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | LOW | LOW |
| R2-18 | 未反証 | 未反証 | **反証**（two-key 下の author 選択） | **CONFIRMED（再定式化: None 意味論未定義）** | LOW | LOW |
| R2-19 | 未反証 | 未反証 | 未反証 | **CONFIRMED** | LOW | LOW |

集計: confirmed **18** / refuted **1**（R2-10）/ merged **0**。severity（V2）: HIGH **2**（R2-03 / R2-04）・MEDIUM **6**（R2-01 / R2-02 / R2-05 / R2-06 / R2-08 / R2-09）・LOW **10**・NOT_A_DEFECT **1**。降格 2 件（R2-01 HIGH→MEDIUM・R2-07 MEDIUM→LOW）、範囲縮小 3 件（R2-01(d)・R2-07・R2-14(b)）、再定式化 1 件（R2-18）。HIGH が残るため 08 §5 の語彙では **HOLD**（HIGH 2 件の fold 案はいずれも zero frozen-schema delta、R2-04 は Rs 裁定 2 択）。

## 2. finding 別の検証

### R2-01（HIGH → **MEDIUM**）CD-01 fold の「timeout 駆動 fault は commissioning で必ず発火」が bind しない

- **sed 実測**: 06:258 `P_TIMEOUT_ORDER` = `safety_heartbeat_timeout_s ≤ command_deadline_s`／`inter_command_jitter_s < 1/action_rate_hz`／`decision_max_age_s ≤ permit_ttl_s ≤ boundary_dwell_s`／`ack_validity_s ≤ boundary_dwell_s`／`lease_max_duration_s ≥ boundary_dwell_s`（5 式・`manager_liveness_s` / `ack_timeout_s` / `health_confirm_timeout_s` は不在）。06:344-345 MIN_FAULT_INJECTION = 8 code ∪ timeout 駆動 7 code {R_DEADLINE_MISS, R_SAFETY_LAYER_LOST, R_TIMING_VIOLATION, R_PERMIT_EXPIRED, **R_ACK_TIMEOUT**, **R_HEALTH_CONFIRM_TIMEOUT**, R_BOUNDARY_DWELL_EXCEEDED}・comment「timeout 駆動の fault は同一 profile_hash の下で commissioning 時に必ず発火させる」。`R_LEASE_DURATION_EXCEEDED`（05:325・05:620）は集合に無い。05:279「heartbeat が `safety_heartbeat_timeout_s` を超えて欠落 **/ health == failed**」→ 同一 FAULT(R_SAFETY_LAYER_LOST)（05:324 も同型）。05:275「候補なし / `boundary_dwell_s` 超過 / `max_reselect_attempts` 超過」→「超過時 FAULT(R_BOUNDARY_DWELL_EXCEEDED)」（05:327「boundary 滞留 / 再選択回数の超過」も同一 code）。05:290 `R_ACK_TIMEOUT` = `ack_timeout_s` 超過、05:271 `R_HEALTH_CONFIRM_TIMEOUT` = `health_confirm_timeout_s` 超過。05:82・05:364・05:498 `manager_liveness_s` = 線形化読みの待ち上限、05:425「後続 command が未 admit の間、gateway は直近 admitted setpoint を保持」、05:423 `command_deadline_s` 超過 = SafeHold。06:280 (x)「絶対上限 = OPP-11」、06:433 OPP-11「絶対上限は … 相対順序しか検査しない」。D0:318「missed heartbeat ⇒ consumers must not assume safety alive」。
- **text**: 未反証（部分反証あり）。(a)(b)(c) は正確。**(d) は 3 field 中 2 field で誤り** — `ack_timeout_s` は `R_ACK_TIMEOUT`、`health_confirm_timeout_s` は `R_HEALTH_CONFIRM_TIMEOUT` を駆動し、両 code は MIN_FAULT_INJECTION（06:345）に**在る**。順序式にも集合にも無いのは `manager_liveness_s` のみ。
- **logic**: 未反証（反例は修正が要る）。R2 の反例 profile は `ack_timeout_s = health_confirm_timeout_s = 1e9` ゆえ、`R_ACK_TIMEOUT` / `R_HEALTH_CONFIRM_TIMEOUT` の FAULT_INJECTION_RESULT を commissioning で作れず（両者に timeout 以外の trigger は 05 に無い — 05:299 の executor crash も health confirmation が来ないまま timeout を待つ）、**字面どおりの反例は `P_EVIDENCE_UNBOUND` で CLOSED_LOOP に到達しない**。しかし `ack_timeout_s = health_confirm_timeout_s = 1` に直した反例は全 `P_*` を通る: `safety_heartbeat_timeout_s = command_deadline_s = 3600`（`R_DEADLINE_MISS` は 1 h 待てば出る）、`R_SAFETY_LAYER_LOST` は health == failed 注入で取得（05:279）、`boundary_dwell_s = lease_max_duration_s = manager_liveness_s = 1e9`、`R_BOUNDARY_DWELL_EXCEEDED` は `max_reselect_attempts = 1` で取得（05:275）、`R_LEASE_DURATION_EXCEEDED` は不要。効果 (a) は成立（CLOSED_LOOP 中に ISL heartbeat が途絶しても (i′) は 1 h 発火しない）。効果 (b)（manager crash 時の setpoint 保持）は `command_deadline_s` が gateway 局所で bound する（05:423・INV-24 の局所 flag）ため「manager_liveness_s まで」ではなく「command_deadline_s まで」— 反例では同じ 3600 s。効果 (c) は成立（`lease_max_duration_s` は `≥ boundary_dwell_s` 以外の拘束無し）。
- **impact**: 未反証・ただし **MEDIUM**。06:280 (x) と OPP-11 は「絶対上限は open」を既に loud に宣言しており、本 doc は絶対有界性を主張していない。残差は (1) 06:345 comment「必ず発火させる」が同一 code 別 trigger（health==failed / attempts）と record 内容の未照合（R2-02）により**執行不能な文**であること、(2) CD-10 で足した timeout 駆動 code `R_LEASE_DURATION_EXCEEDED` が同 comment の対象から漏れ、CD-10 の「lease を有限にする」が 1e9 で空文化すること、(3) `manager_liveness_s` にいかなる検査も無いこと、(4) safety window が「commissioning の忍耐」でしか bound されない事実が OPP-11 の文より弱く読めること。(1)(2)(3) は本文から機械的に再現できる内部不整合であり、CD-01 verdict（VERIFY_C:49「1e9 秒は commissioning で発火させられない ⇒ 自然に排除」）の根拠を部分的に崩す。fail-closed 違反ではなく「fold の bound が自らの comment より弱い」欠陥ゆえ HIGH は維持しない。
- **verdict**: CONFIRMED・MEDIUM。
- **最小修正（zero frozen delta）**: (1) 06 §6.1 MIN_FAULT_INJECTION += `R_LEASE_DURATION_EXCEEDED`, `R_MANAGER_UNAVAILABLE`。(2) 06 §3 `P_TIMEOUT_ORDER` += `manager_liveness_s ≤ command_deadline_s`（LEARNED / SCRIPTED）（`ack_timeout_s ≤ boundary_dwell_s` / `health_confirm_timeout_s ≤ lease_max_duration_s` は順序に物理的意味があるため Rs 確認付きの候補）。(3) 05 §8 FAULT record payload に trigger 副因（`HEARTBEAT_TIMEOUT | HEALTH_FAILED`、`DWELL_TIMEOUT | ATTEMPTS_EXHAUSTED`）を持たせ、06 §6.1 の timeout 駆動 code は `subject_ref = "<code>#<timeout_trigger>"` を要求（R2-02 の内容照合と対）。(4) OPP-11 に 1 文追加:「絶対上限が無い間、timeout 群は commissioning で当該 fault を実際に待てる時間でしか bound されない」。(5) R2 の (d) を「`manager_liveness_s` のみ」に訂正して fold 記録に残す。

### R2-02（MEDIUM）FAULT_INJECTION_RESULT / HEALTH_CHECK_RUN の内容が参照先 audit range と照合されない

- **sed 実測**: 06:264 `P_EVIDENCE_UNBOUND` = required kinds が (kind × subject) 単位で **存在**（`subject_ref == runtime_fault_code`）。06:320 `artifact_ref`「kind ∈ {HEALTH_CHECK_RUN, FAULT_INJECTION_RESULT} では 05 RuntimeAuditRecord の (lease_id, seq 範囲) に**解決する**」。06:353「deployment evidence は『存在・有効期限・束縛』だけを持ち」、06:358「artifact_ref が指す (lease_id, seq 範囲) で 05 §8 の record に束縛される」。06:414 §9 項 7「本 doc は『存在・束縛・fail-closed』を要求するのみ」。05:657-670 `RuntimeAuditRecord` = {seq, kind, lease_id, profile_hash, fault, …}。A:537 U14 = hash **方式**の defer。`grep -n "range\|範囲"` で 06 内に range 内容を読む `P_*` は無し。
- **text**: 未反証。引用は正確。「解決する」は解決可能性のみで、range 内に当該 fault / check の record があることを求める文は 05 / 06 のどこにも無い。
- **logic**: 未反証。`DeploymentEvidenceRecord(kind=FAULT_INJECTION_RESULT, subject_ref="R_DEADLINE_MISS", profile_hash=H, artifact_ref=(L1, 10..20))` で seq 10..20 に FAULT record が無くても 06:264 は真。HEALTH_CHECK_RUN も同型（HEALTH_CONFIRMED / HEALTH_FAILED の payload に check_id が無くてよい）。
- **impact**: 未反証。CD-16 fold は「audit trail への束縛」を目的に `artifact_ref` を導入した（06:320・06:505）が、照合の無い束縛は CD-04 が HIGH と判定したのと同じ「名前参照のみ」の型である。CD-01 fold（R2-01）が唯一の bound として依拠する record が装飾になる。照合は 05 の既存 field（kind / fault / profile_hash / lease_id / seq）だけで機械的に可能で U14 の hash 方式 defer に抵触しない。MEDIUM。
- **verdict**: CONFIRMED・MEDIUM。
- **最小修正（zero frozen delta）**: 06 §3 `P_EVIDENCE_UNBOUND` の第 2 評価点定義に 1 文:「HEALTH_CHECK_RUN / FAULT_INJECTION_RESULT は、解決した (lease_id, seq 範囲) に `record.profile_hash == 本 record.profile_hash` かつ〔FAULT_INJECTION_RESULT: `kind == FAULT ∧ fault == subject_ref`〕〔HEALTH_CHECK_RUN: `kind ∈ {HEALTH_CONFIRMED, HEALTH_FAILED, PERMIT_ISSUED, FAULT(R_HEALTHCHECK_FAILED)} ∧ payload に check_id`〕の record が ≥ 1 無ければ違反」（または別 code `P_EVIDENCE_CONTENT_UNVERIFIED`・§8.1 一覧に追加）。

### R2-03（HIGH）content hash の verify-on-resolve が CD-04 の 3 ref に限られ、zone 幾何・calibration・gateway config・evidence policy・evidence artifact は名前解決のまま

- **sed 実測**: 06:106 `ZoneRef.geometry_sha256`、06:176 `CalibrationRef.artifact_sha256`、06:182 `GatewayConfigRef.artifact_sha256`、06:224 `deployment_evidence_policy_hash`、06:321 `DeploymentEvidenceRecord.artifact_sha256` — `grep -n artifact_sha256` は 06:101（comment）/176/182/321 の型定義のみで照合規則の行は無い。06:271 `P_PREDICATE_HASH_MISMATCH` = `predicate_refs` / `evaluator_ref` のみ。05:184 (j) = `predicate_refs` / `evaluator_ref` / `tcp_offset_ref` の 3 つ。05:435 DEPLOYMENT_WORKSPACE 行「評価対象 = … 線分 / 点。tool 形状は評価しない」— 再検証の文**無し**。05:437 SAFETY_RESTRICTION 行「gateway は評価前に predicate の解決内容 sha256 を … 再検証し、不一致 = FALSE」。05:378 `EnvelopeTerm.predicate_ref`「内容は source 側が持つ」（評価時解決）。B:255（凍結側は content-addressed store を要求しない）。
- **text**: 未反証。CD-04 の照合対象は明示的に 3 ref に限られ、zone 幾何・gateway config・evidence policy・evidence artifact の hash は宣言のみ。`GATEWAY_CONFIG_MATCH` health check（06:67・06:260）は「稼働 gateway の config と宣言の一致」を意図すると読めるが評価内容は evaluator 任せで、`GatewayConfigRef.artifact_sha256` との照合とは書かれていない。
- **logic**: 未反証。`workspace.zones = (ZoneRef("ko_1", KEEP_OUT, geometry_sha256=X, base),)` を登録・lease 活性後、resolver が `ko_1` に返す幾何を空多角形にする ⇒ `profile_hash` 不変・lease 不変・05:184 (j) 対象外・05:435 は評価前照合無し ⇒ 旧 keep-out 内の setpoint を admit。`DeploymentEvidencePolicy` の差替も同型（`deployment_evidence_policy_hash` は「解決でき」（06:264）だけで、`H_WCJ(解決した policy) == 宣言値` の照合文が無い）。
- **impact**: 未反証。keep-out は deployment 層の安全制約であり、CD-04（同じ「名前参照のみ」の型）は verifier C で HIGH 確定。宣言 hash は解決時に照合して初めて束縛になる（B:255 の凍結側前提）。HIGH 維持。
- **verdict**: CONFIRMED・HIGH。
- **最小修正（zero frozen delta）**: 05 §3.4 (j) と 06 §3 `P_PREDICATE_HASH_MISMATCH` を「profile が宣言する**全** content hash（`geometry_sha256` / `CalibrationRef.artifact_sha256` / `GatewayConfigRef.artifact_sha256` / `deployment_evidence_policy_hash == H_WCJ(解決 policy)` / required record の `artifact_sha256`）」に一般化（code 名は据置か `P_CONTENT_HASH_MISMATCH` に改称）；05 §4.3 DEPLOYMENT_WORKSPACE 行に SAFETY_RESTRICTION 行と同文「gateway は評価前に zone 幾何の sha256 を `geometry_sha256` と再検証・不一致 = 評価不能 = FALSE」を追記。

### R2-04（HIGH）profile schema は robot を 1 台しか識別・envelope・tool 束縛・calibration できないのに両腕 cell の宣言を許す

- **sed 実測**: 06:76-83 `CellIdentity` = `robot_model_ref` / `robot_serial_ref` / `controller_firmware_ref` 各 1（単数）。06:86-93 `ControllerEnvelope` 1 つ・joint tuple「関節順は robot_model_ref の規約」。06:96-101 `ToolPayloadSpec` / `tcp_offset_ref` 1 つ。06:267 `P_ENVELOPE_SHAPE` = tuple 長 = robot_model_ref の関節数。06:68 CONTROLLER_IDENTITY =「controller が報告する serial / firmware と CellIdentity.{robot_serial_ref, controller_firmware_ref} の等値」（単数）。06:365「`ResourceAvailability.control` が両腕の資源を True にすることは『cell に両腕がある』事実の宣言であり…合成を許す意味ではない」。05:405 `ownership: Ownership`、05:417「SAFEHOLD からの初回起動では … offered 集合は profile が宣言する cell の資源可用性から構成する」、05:759 §11 項 5「lease は常に 1 executor」。A:151 `ControlResourceSpec = {ee_left, ee_right, gripper_left, gripper_right: bool}`。RS71:23「DUAL-ARM — the cable is held + manipulated by BOTH arms (UR15 × 2) in EVERY motion」。`grep -n "両腕\|腕\|arm\|robot"` in 06 = 上記の行のみ（per-arm の型・規則は無い）。
- **text**: 未反証。引用は正確。06 に「schema 1.0 = 単 robot cell」の宣言も、腕ごとの controller / envelope / tool / calibration の型も無い。
- **logic**: 未反証。`control = {ee_left: T, ee_right: T, …}`・`robot_serial_ref = "UR15-A"`・6-tuple envelope ⇒ 全 `P_*` = 0。右腕 controller B の firmware 更新: CONTROLLER_IDENTITY は A の報告のみ照合 ⇒ pass ⇒ permit ⇒ 両腕 ownership の CLOSED_LOOP lease（1 executor・dual-arm skill = THREAD の標準形）。右腕 command の CONTROLLER 項: envelope 1 つをどの robot に適用するか 05 / 06 に規定無し —「評価不能 = FALSE」（05:439）なら右腕は運転不能、そうでなければ A の envelope が黙って B に適用（fail-open）。反証候補「合成は OUT（§7）」は 06:365 自身が両腕宣言 ≠ 合成と述べるため成立せず；「1 腕 1 profile」は lease が `profile_hash` を 1 つしか束縛しない（05:406）ため成立せず。
- **impact**: 未反証。handoff D「cell・controller・tool・payload 固有条件 = `IndustrialDeploymentProfile`」の home に対し、対象 cell（RS71 §0 不変前提 = UR15 × 2）を表現できない。CD-09 の identity 保証が片腕にしか及ばない = fail-open か cell 使用不能かが未定義。HIGH。⚠ R2 の修正案 (A)「schema 1.0 = 単 robot cell」は THREAD の対象 cell に対して profile を使用不能にするため、**DUAL-ARM 不変前提との整合は Rs 裁定事項**（CLAUDE.md §0 L3 keyword）— verifier は (A)/(B) を選ばない。
- **verdict**: CONFIRMED・HIGH。
- **最小修正（Rs 2 択・いずれも zero frozen delta）**: (A) 暫定 = §2.2 に「profile_schema_version 1.0 は単 robot cell」を明記し `P_MULTI_ROBOT_UNSUPPORTED`（`resource_availability.control.ee_left` / `ee_right` のちょうど 1 つが True）を追加・dual-arm profile を OPP に登録（対象 cell では使えない旨を loud に）；(B) 06 型変更（deployment 層）= `CellIdentity.robots: tuple[RobotIdentity, ...]`（serial / firmware / model）、`controllers` / `tool_payload` / TCP calibration を frozen 資源 id（`ee_left` / `ee_right`）で key、`P_ENVELOPE_SHAPE` を robot ごと、CONTROLLER_IDENTITY / TOOL_IDENTITY / ENVELOPE_READBACK / REQUIRED_CALIBRATION_KINDS を True な全腕に要求、05 §4.3 CONTROLLER 項を腕ごとに評価。

### R2-05（MEDIUM）05:516 は SAFEHOLD(SAFETY) 解除に profile role を必須化し、06:261 は SAFETY role の宣言を任意にする（= R1-04）

- **sed 実測**: 05:516「『該当する clearance record』= `record.safehold_reason == current.safehold_reason ∧ record.role ∈ profile.escalation.clearance_roles[reason] ∧ …`。SAFETY 行は加えて IndependentSafetyLayer の clearance record（AND）。profile が当該 reason の role を 1 つも宣言しない場合は登録時に doc 06 `P_CLEARANCE_ROLE_MISSING`（定義なし = 解除不能 HOLD にも『任意の record で解除』にもしない）」。05:518 条件 10 の集合 = {SAFETY, SAFE_STOP, CHECKPOINT_DISABLED, MANAGER_RESTART, GATEWAY_RESTART}。06:190「{SAFE_STOP, CHECKPOINT_DISABLED, MANAGER_RESTART, GATEWAY_RESTART} の各 reason に ≥ 1 role が必須」、06:261 同集合 +「SAFETY は ISL clearance record が主・role は AND」。06:400 PT-22 = `clearance_roles = ()` のみ。05:678 fail-closed 既定。D0:273-274・D0:320。
- **text**: 未反証。05 は role 項を条件 10 の全 reason（SAFETY 含む）に課し、かつ「当該 reason の role 未宣言 = 登録時に `P_CLEARANCE_ROLE_MISSING`」と 06 の検査に委ねるが、06 の検査集合は SAFETY を含まない。cross-doc 不整合が本文から再現できる。
- **logic**: 未反証。`clearance_roles` = 4 reason のみ ⇒ 06:261 通過 ⇒ 登録。ISL STOP ⇒ S_SAFEHOLD(SAFETY)。ISL が clearance record を書いても条件 10 の role 項は `∈ ∅` で FALSE ⇒ 当該 profile_hash では TRANSFER_TO_EXECUTOR 不能 ⇒ 05:516 が自ら禁じた「解除不能 HOLD」。
- **impact**: 未反証。安全側（permanent hold）だが availability 欠陥 + 仕様不整合。**R1-04（他 reviewer）と同一欠陥**であり、独立に同じ再現に至った（cross-reviewer 収束）。MEDIUM。
- **verdict**: CONFIRMED・MEDIUM（`merged_into = ""` — R2 集合内の重複ではない）。
- **最小修正（text-only・両 doc 同時）**: 06:190 / 06:261 の必須集合に SAFETY を追加（05:518 と同一集合にする）— R1-04 も同じ選択を推奨。代替は 05:516 に「`reason == SAFETY`: role 項は profile が SAFETY の role を ≥ 1 宣言する場合のみ AND」を足す案だが、集合の同一定義（05:518「両者を同一定義から導く」）を保つ前者を推奨。

### R2-06（MEDIUM）`HealthCheckKind` は label であり evaluator の意味論が attest されない

- **sed 実測**: 06:164-170 `HealthCheckSpec` = {check_id, kind, evaluator_ref, evaluator_sha256, stage, fail_closed}（意味は evaluator が持つ）。06:260 `P_HEALTHCHECK_MISSING` = 6 kind が各 ≥ 1・stage ∈ {BEFORE_PERMIT, BOTH}。06:399 PT-21「firmware 更新・tool 交換 … CONTROLLER_IDENTITY / TOOL_IDENTITY が fail」。06:431 OPP-9「evaluator_ref の登録・版管理 … 内容は impl 解錠後」。06:280「契約層で検証できるのは形・単調性・宣言と evidence の束縛まで」— 物理事実の列挙に evaluator の正しさは無い。D0:318。`grep -n "always_true\|negative\|陰性"` = 06 / 05 とも 0。
- **text**: 未反証。kind は label で、evaluator の弁別能力を要求・検査・attest する文は無い。
- **logic**: 未反証。6 kind 全ての `evaluator_ref → "always_true_v1"`（`evaluator_sha256` 一致）⇒ 全 `P_*` = 0・HEALTH_CHECK_RUN ∀ check_id 存在（走って pass した記録）⇒ firmware 更新 + tool 交換でも permit。PT-21 の期待は evaluator が弁別する前提でしか成立しない。
- **impact**: 未反証。失敗し得ない check は情報を持たない（D0:318 の原理）。CD-09 の identity 保証が evaluator 意味論に全面依存し、06:280 の honesty 文はその依存を列挙していない。negative-control（誘発した不一致で fail する記録）の要求は FAULT_INJECTION_RESULT と同型で zero delta。MEDIUM。
- **verdict**: CONFIRMED・MEDIUM。
- **最小修正（zero frozen delta）**: 06 §6.1 に「必須 kind の各 check_id について、誘発した不一致の下で `R_HEALTHCHECK_FAILED` / HEALTH_FAILED を含む audit range を指す **negative-control** HEALTH_CHECK_RUN record（`subject_ref = "<check_id>#NEGATIVE"`）を CLOSED_LOOP lease 前に要求」；06:280 / OPP-9 に「kind 認証済 evaluator registry が無い間、`P_HEALTHCHECK_MISSING` は label 検査である」を明記。

### R2-07（MEDIUM → **LOW**）`artifact_ref → (lease_id, seq 範囲)` は lease 無しの audit record を解決できない

- **sed 実測**: 06:320「(lease_id, seq 範囲) に解決する」。06:264 HEALTH_CHECK_RUN ∀ `health_checks[].check_id`・FAULT_INJECTION_RESULT ∀ `fault_injection[]`。06:260 必須 kind の stage ∈ {BEFORE_PERMIT, BOTH}。05:184 (a) BEFORE_PERMIT check は permit 発行前提条件（lease 未活性）。05:663 `lease_id: str | None`。05:667 `profile_hash`「deployment 起因 record（permit 発行時 fault・MANAGER_RESTART・GENESIS_RECORDED）の join key（lease 無しでも profile と結線）」。05:157 `lease_proposal_id` = 最終 lease_id（proposal 時に確定）。05:262 S_BOUNDARY_WAIT = lease 活性。05:269 permit → CAS は `S_SAFEHOLD / S_BOUNDARY_WAIT` から。
- **text**: 未反証。06:320 は `lease_id` を要求し、05:667 は permit 発行時 record を lease 無しの join と定める。両 doc の join key が食い違う。
- **logic**: **反証（過大主張）**。「適法 profile は HEALTH_CHECK_RUN を永久に満たせない」は成立しない: HEALTH_CHECK_RUN が要るのは CLOSED_LOOP（MIN_CLOSED_LOOP）であり、その時点までに SHADOW lease は存在し得る。S_BOUNDARY_WAIT（lease 活性・05:262）からの permit 発行時に走る BEFORE_PERMIT check、および S_LEASE_ACTIVE 中に発火させる `R_GATEWAY_RESTART` / boundary で発火させる `R_ACK_TIMEOUT` / `R_PERMIT_EXPIRED` は lease_id を持つ audit range を作れる。ただし (i) 最初の SAFEHOLD(INITIAL) からの permit で走った check、(ii) 05:667 が permit 発行時 record を「lease 無し」と定める読みの下では、recorder が当該 record にどの lease_id を書くかが未定義であり、06:320 の字面では整形不能 — 「常に不能」ではなく「規定が食い違い、実装依存で不能になり得る」。
- **impact**: 未反証・LOW。fail-closed 側（`P_EVIDENCE_UNBOUND`）に落ちる仕様不整合で、修正は 1 文。
- **verdict**: CONFIRMED（範囲縮小）・LOW。
- **最小修正（text-only）**: 06:320 を「`(lease_id | None, seq 範囲)` かつ range 内 record の `profile_hash == 本 record.profile_hash`（05:667 の join key を写す）」に；05:667 に「permit 発行時の health check 実行 record は `lease_id = None`・`profile_hash` 必須」を明記。

### R2-08（MEDIUM）受理済 profile の撤回（revocation / retirement）経路が無い

- **sed 実測**: 06:373 規則 (2)「受理 = `accepted_profiles` への追加」・(3)「受理後の事実変化 … は profile を変えない」。06:300「profile を『更新』する操作は存在しない」。06:435 OPP-13「registry（所在・受理権限 … 受理済集合 `accepted_profiles` の永続化）… 受理の権限は Rs 専権側（本 doc は集合の存在だけを要求）」。05:184 (i)「`permit.profile_hash ∈ accepted_profiles`」のみ。`grep -n -i "revok\|撤回\|退役\|retire"` = 05 / 06 とも 0。06:67 HealthCheckKind 7 種に layout を見るものは無い。A:39（certificate は runtime 事象で無効化されない）。
- **text**: 未反証。受理の逆操作・supersession・撤回のいずれも両 doc に無い。
- **logic**: 未反証。profile A（`layout_revision_ref = "r1"`・keep-out Z1）受理 → layout 変更 → B（"r2"・Z2）受理。A は `accepted_profiles` に残り、A の CELL_COMMISSIONING / SAFETY_LAYER_ACCEPTANCE が `valid_until` 内なら 05:184 (i)(b) を通過、CONTROLLER_IDENTITY / TOOL_IDENTITY は不変 ⇒ 旧 keep-out Z1 の下で CLOSED_LOOP lease。反証候補「evidence の valid_until を短くすれば良い」は撤回ではなく失効であり、incident 起因の即時退役を表現できない。
- **impact**: 未反証。profile の不変性（06:300）は正しいが不変 ≠ 永久適格。layout / tooling / incident 後に旧 profile が permit 適格のまま残るのは deployment 層の fail-open。修正は registry 状態 1 つと (i) の 1 項で zero delta。MEDIUM。
- **verdict**: CONFIRMED・MEDIUM。
- **最小修正（zero frozen delta）**: 06 §8.1 に registry 状態 {ACCEPTED, REVOKED}（撤回 = `RuntimeAuditRecord(kind = CLEARANCE_RECORDED, payload = {revoked_profile_hash, role ∈ clearance_roles, operator_ref})`・REVOKED → ACCEPTED の復帰無し）；05 §3.4 (i) を「`∈ accepted_profiles ∧ ∉ revoked_profiles`」に；OPP-13 に撤回権限を追記。

### R2-09（MEDIUM）IndependentSafetyLayer 自身の identity / 版が profile に無く、ISL 更新が検出不能

- **sed 実測**: 06:76-83 `CellIdentity` に ISL 識別子無し（`grep -n safety_layer_ref` = 0）。06:67 `SAFETY_LAYER_HEARTBEAT` のみ（liveness）。06:311-312 `SAFETY_LAYER_ACCEPTANCE`、06:319 `subject_ref` 語彙 = cell_id / controller_firmware_ref / tool_id / calibration_id / config_id / check_id / fault code — ISL 用の語が無い。06:354「`SAFETY_LAYER_ACCEPTANCE` の内容は本 doc の外 … record の存在と束縛だけを要求」。D0:310-318（ISL = 別 owner module・独自 health）。
- **text**: 未反証。ISL の版・識別子を持つ field・check・subject は無い。
- **logic**: 未反証。ISL v1 受入（valid_until = +1y）→ ISL v2（STOP 閾値変更）→ profile 不変（変える field が無い）・SAFETY_LAYER_HEARTBEAT pass・`P_EVIDENCE_UNBOUND` / `P_EVIDENCE_EXPIRED` pass ⇒ 未受入の安全層の下で CLOSED_LOOP permit。
- **impact**: 未反証。06:354 は受入**内容**を外部 court に置くが、受入 record が**何を**受け入れたかの束縛は本 doc の「束縛」責務内。CD-09 は controller / tool の identity を同じ理由で必須化した（MEDIUM）— ISL は第 3 の identity 保持 component で漏れ。MEDIUM。
- **verdict**: CONFIRMED・MEDIUM。
- **最小修正（06 型変更・zero frozen delta）**: `CellIdentity.safety_layer_ref: str`；`HealthCheckKind += SAFETY_LAYER_IDENTITY`（ISL が報告する版 == `safety_layer_ref`）を 06:260 の必須集合に；06:264 / 06:319 に `SAFETY_LAYER_ACCEPTANCE.subject_ref == cell.safety_layer_ref`（R2-16 と同時に処置）。

### R2-10（LOW → **NOT_A_DEFECT**）(xi)「predicate を空虚にする」に対する check が空文

- **sed 実測**: 06:280 (xi)「envelope / zone / predicate を空虚にする — `P_ENVELOPE_NONPOSITIVE` / `P_WORKSPACE_EMPTY` / `P_ENVELOPE_EXCEEDS_MEASURED` / `P_PREDICATE_HASH_MISMATCH`」。06:117-119 `predicate_refs: tuple[str, ...]`（空を型上許す）。06:403 PT-25「`keepout_pred_v1` の内容を常時許可に差し替え → `R_PROFILE_MISBOUND`・gateway 評価前の再検証で FALSE」。06:404 PT-26「`predicate_refs = ()` … → `P_WORKSPACE_EMPTY`・`P_ENVELOPE_EXCEEDS_MEASURED`」。
- **text**: **反証**。(xi) の「predicate を空虚にする」は PT-25 が示すとおり「predicate の**内容**を空虚（常時許可）にする」経路であり、それは `P_PREDICATE_HASH_MISMATCH` + 評価前再検証で捕捉される。(xi) は「predicate 集合を空にする」経路を名指ししていない。
- **logic**: **反証**。`predicate_refs = ()` が全 `P_*` を通ることは事実だが、PT-26 が「`predicate_refs = ()` に対し predicate 系 code を期待しない」と明示しており、空集合の受理は**設計意図として記録済**（静的 restriction 無し = zone + ISL のみ；`requires_safety_layer_health = True` 必須・REACH_LIMIT 必須は維持）。「意図が未記載」という前提が成立しない。
- **impact**: **反証**。空集合は fail-open ではなく「追加 restriction 無し」であり、frozen / handoff / fail-closed bar のいずれにも抵触しない。残るのは (xi) の語の明確化という文体上の好み。
- **verdict**: REFUTED・NOT_A_DEFECT。
- **任意の明確化（欠陥ではない）**: (xi) 末尾に「（predicate 集合そのものは空を許す — PT-26）」を添える。

### R2-11（LOW）`P_ENVELOPE_EXCEEDS_MEASURED` を登録時に評価すると MIN_SHADOW だけの profile は規則 (1) で登録不能

- **sed 実測**: 06:270「各上限 ≤ `CONTROLLER_ENVELOPE_MEASUREMENT` record の測定値（… **登録時に照合**）」。06:264「登録時 = MIN_SHADOW」。06:342 MIN_SHADOW = {CELL_COMMISSIONING, GATEWAY_CONFIG_ATTESTATION, SAFETY_LAYER_ACCEPTANCE}（測定 record を含まない）。06:373 規則 (1)「評価不能 = 違反」、規則 (2) の第 1 / 第 2 評価点の列挙に `P_ENVELOPE_EXCEEDS_MEASURED` は**どちらにも無い**。
- **text**: 未反証。06:270 の「登録時」と 06:264 / 06:342 の登録時 evidence 集合が矛盾し、規則 (2) の列挙からも漏れている。
- **logic**: 未反証。evidence = MIN_SHADOW のみの profile ⇒ 測定 record 無し ⇒ 評価不能 ⇒ 規則 (1) で違反 ⇒ 登録拒否。Rs C3 の slice mode（SHADOW）で最初に使う形が字面上登録できない。
- **impact**: 未反証・LOW。fail-closed 側の不整合で 1 文で解消。
- **verdict**: CONFIRMED・LOW。
- **最小修正（text-only）**: 06:270 と 06:373 規則 (2) に「`P_ENVELOPE_EXCEEDS_MEASURED` は第 2 評価点・`proposed_lease.mode == CLOSED_LOOP_AUTHORITY` のときに評価（SHADOW では record 不在を違反にしない）」を明記（または MIN_SHADOW に kind を追加 — 前者を推奨）。

### R2-12（LOW）INV-33「全 lease」と執行行（S_LEASE_ACTIVE / S_HEALTH_PENDING のみ）の不一致

- **sed 実測**: 05:714 INV-33「全 lease の活性時間は `lease_max_duration_s` 以下 … WAIT を含む」。05:281 from 列 = `S_LEASE_ACTIVE / S_HEALTH_PENDING`（`any (EXECUTOR)` ではない）。05:262 S_BOUNDARY_WAIT「lease は活性・gateway = SafeHold」。05:275 S_BOUNDARY_WAIT は `boundary_dwell_s` で bound。06:258 `lease_max_duration_s ≥ boundary_dwell_s`（和は bound しない）。
- **text**: 未反証。INV の字面と遷移表の適用範囲が一致しない。
- **logic**: 未反証。`t0` 活性 → `t0 + lease_max − ε` で TERMINAL → S_BOUNDARY_WAIT ≤ `boundary_dwell_s` ⇒ 活性時間 ≤ `lease_max + boundary_dwell` だが `R_LEASE_DURATION_EXCEEDED` は発火しない。
- **impact**: 未反証・LOW。超過区間の gateway 出力は SafeHold（安全側）。字面の是正のみ。
- **verdict**: CONFIRMED・LOW。
- **最小修正（text-only）**: INV-33 を「S_LEASE_ACTIVE / S_HEALTH_PENDING の活性時間」に限定し「S_BOUNDARY_WAIT は `boundary_dwell_s` で別途 bound」を添える（05:281 の from 列に S_BOUNDARY_WAIT を足す案は boundary 中の SafeHold を TIMING_VIOLATION に変える副作用があるため非推奨）。

### R2-13（LOW）`WorkspaceRestriction.frame_ref: str = ""` に default があり no-default 規律と矛盾

- **sed 実測**: 06:113 `frame_ref: str = ""`（06:108 `ZoneRef.frame_ref: str` は default 無し — 06 内でも不揃い）。06:120 / 06:170「省略時 default を持たせない … 欠落は codec 拒否」。06:230「D1.1-B の追加規約（`Optional None` は明示 `null` …）を継承 … 新 canonicalization 規則は足さない」。B:24 B-declared ①「全域表現」。A:385 strict codec の 5 拒否に missing field は**無い**。06:277 `P_FRAME_MISMATCH`。
- **text**: 未反証。1 field だけ default を持ち、06 自身の規律（06:120 / 06:170）と不揃い。
- **logic**: 未反証。`frame_ref` 欠落の payload は dataclass default で `""` に decode され（凍結 5 拒否は missing field を拒否しない）、後段 `P_FRAME_MISMATCH` で拒否される。
- **impact**: **反証**。終着は fail-closed（`""` ≠ `base_frame_ref`）で hash は WCJ 全 field 直列化ゆえ分岐しない。規律の不揃いは文体上の欠陥。
- **verdict**: CONFIRMED（2 lens）・LOW。
- **最小修正（text-only）**: 06:113 の default を外す（`frame_ref: str`）。

### R2-14（LOW・(a) のみ）policy 型が許す循環 / 非単調

- **sed 実測**: 06:329 `must_be_exercised_before: LeaseMode`（SHADOW_NON_AUTHORITY 可）。06:334-335 `required_before_shadow_lease ⊇ MIN_SHADOW`・`required_before_closed_loop_lease`「上記 + …」（包含規則の文は無い）。06:310-312 `DeploymentEvidenceKind` = **8 member**、06:343 MIN_CLOSED_LOOP = MIN_SHADOW ∪ 5 kind = **8 kind 全て**。06:264 HEALTH_CHECK_RUN / FAULT_INJECTION_RESULT は audit range（lease）を要する。06:278 `P_EVIDENCE_POLICY_TOO_WEAK`。
- **text**: 未反証（(a) のみ）。(a) の policy 形は型上表現可能で拒否する検査は無い。(b) は下記 logic で反証。
- **logic**: (a) 未反証 — `required_before_shadow_lease = MIN_SHADOW ∪ {HEALTH_CHECK_RUN}` は `P_EVIDENCE_POLICY_TOO_WEAK` を通り、最初の SHADOW permit は lease 由来 record を要して不成立（R2-07 の修正後も INITIAL からの最初の permit では range が無い）。(b) **反証** — `required_before_closed_loop_lease ⊇ MIN_CLOSED_LOOP` = 全 8 kind ゆえ、schema 1.0 では CLOSED_LOOP 集合 ⊉ SHADOW 集合は**表現不能**（R2 自身「MIN_CLOSED_LOOP happens to contain it」と認める）。
- **impact**: **反証**。(a) は fail-closed（`P_EVIDENCE_UNBOUND`）で永久不成立になるだけで、frozen / handoff / fail-closed bar に抵触しない。静的 lint の有無は好み。
- **verdict**: CONFIRMED（text + logic の (a)）・LOW（範囲 = (a) のみ）。
- **最小修正（zero frozen delta）**: 06 §3 に `P_EVIDENCE_POLICY_CIRCULAR`（HEALTH_CHECK_RUN / FAULT_INJECTION_RESULT ∉ `required_before_shadow_lease` ∧ 全 `must_be_exercised_before == CLOSED_LOOP_AUTHORITY`〔schema 1.0〕）。`P_EVIDENCE_POLICY_NONMONOTONE` は 1.0 では到達不能ゆえ**追加しない**（到達不能 code は B2-16 の反例）。

### R2-15（LOW）genesis record を書く role が `clearance_roles` に key されていない

- **sed 実測**: 05:141「初回だけは operator が profile の clearance 手続（doc 06 `EscalationPolicy.clearance_roles`）で **genesis record** … を書き」。05:143 genesis の下限。05:122 `SafeHoldReason` に INITIAL。05:509 INITIAL 行の追加前提 = なし。06:190 `clearance_roles` の key = SAFETY / SAFE_STOP / CHECKPOINT_DISABLED / MANAGER_RESTART / GATEWAY_RESTART（`tuple[tuple[str, str], ...]`）。05:516「該当」= `record.safehold_reason == current.safehold_reason ∧ … ∧ record.profile_hash == permit.profile_hash`（genesis 時に permit は無い）。
- **text**: 未反証。genesis に対応する key も、genesis record が持つ profile_hash の規定も無い。
- **logic**: 未反証。厳密読み = 有効な genesis を書ける (reason, role) が無く manager が起動できない／緩い読み = 任意 operator。どちらかを本文が選んでいない。
- **impact**: 未反証・LOW。起動手続の未定義で text-only。
- **verdict**: CONFIRMED・LOW。
- **最小修正（text-only）**: 06:190 / 06:261 の必須 key に `GENESIS`（文字列 key・enum 変更なし）を追加し、05:141 に「genesis record（GENESIS_RECORDED）は `profile_hash` と `role ∈ clearance_roles[GENESIS]` を持つ；05:516 の permit 照合は genesis に適用しない」を明記。

### R2-16（LOW）singleton 5 kind の subject 規則が未定義

- **sed 実測**: 06:264 subject 規則 = CALIBRATION_RECORD ∀ calibration_id・HEALTH_CHECK_RUN ∀ check_id・FAULT_INJECTION_RESULT ∀ fault code のみ。06:319 `subject_ref` の語彙は **comment**（cell_id / controller_firmware_ref / tool_id / calibration_id / config_id / check_id / fault code — SAFETY_LAYER_ACCEPTANCE 用の語は無い）。06:247 `P_RESOURCE_UNATTESTED` は kind + profile_hash のみで subject を見ない。06:373 規則 (1)。
- **text**: 未反証。comment の語彙は 4 kind の対応を暗示するが規範ではなく、SAFETY_LAYER_ACCEPTANCE には語彙自体が無い。
- **logic**: 未反証。「(kind × subject) 単位で存在」を singleton kind に適用するとき subject が未定義 ⇒ 規則 (1) の厳密読みでは評価不能 = 違反（全 profile 拒否）、緩い読みでは subject 無視（06:264 の文より弱い）。
- **impact**: 未反証・LOW。text-only。
- **verdict**: CONFIRMED・LOW。
- **最小修正（text-only）**: 06:264 に kind 別 subject を規範化: CELL_COMMISSIONING = `cell.cell_id`、CONTROLLER_ENVELOPE_MEASUREMENT = `cell.controller_firmware_ref`、TOOL_PAYLOAD_IDENTIFICATION = `tool_payload.tool_id`、GATEWAY_CONFIG_ATTESTATION = `gateway_config.config_id`、SAFETY_LAYER_ACCEPTANCE = `cell.safety_layer_ref`（R2-09 と同時）。06:319 の comment を同文に揃える。

### R2-17（LOW）evaluator の content sha は permit 発行時のみ照合され、AT_HEALTH_CONFIRMATION / BOTH の実行時に再照合されない

- **sed 実測**: 06:168「第 2 評価点で照合・不一致 = R_PROFILE_MISBOUND」。06:271「登録時；第 2 評価点の再照合は 05 `R_PROFILE_MISBOUND`」。05:184 (j) = permit 発行時。06:69 stage AT_HEALTH_CONFIRMATION / BOTH は CAS 後（05:301 `HealthConfirmation.profile_health_checks` ← stage ∈ {AT_HEALTH_CONFIRMATION, BOTH}）。05:437 predicate には「評価前に再検証」があるが evaluator には無い。窓 = `permit_ttl_s + health_confirm_timeout_s`。
- **text**: 未反証。evaluator の実行直前再照合の文は 05 / 06 に無い。
- **logic**: 未反証。permit 発行（sha 一致）→ resolver の evaluator 内容差替 → CAS → HealthConfirmation は差替後の evaluator を実行。
- **impact**: 未反証・LOW。窓は有界（`permit_ttl_s ≤ boundary_dwell_s`）だが CD-04 の原則（解決時照合）の適用漏れで、text-only で閉じる。
- **verdict**: CONFIRMED・LOW。
- **最小修正（text-only）**: 06:168 / 05 §3.5 HealthConfirmation に「各 check の実行直前に evaluator 内容の sha256 を `evaluator_sha256` と再照合・不一致 = 当該 check FAILED（fail_closed）→ `R_HEALTH_CONFIRM_FAILED`」。

### R2-18（LOW・再定式化）calibration / 受入 evidence の `valid_until = None` が表現可能

- **sed 実測**: 06:177 `CalibrationRef.valid_until_iso8601: str | None`（comment は wall-clock と OPP-3 のみ・None の意味無し）。06:324 record 同型。06:263「`valid_until` が評価時刻より後」→ `P_CALIBRATION_EXPIRED`。06:265「required record の `valid_until_iso8601` が permit 発行の wall-clock より前」→ `P_EVIDENCE_EXPIRED`。06:373 規則 (1)「評価不能 = 違反」。
- **text**: 未反証。None は表現可能で、None の意味は本文に無い。⚠ ただし R2 の読み「None なら失効検査が到達不能」は唯一の読みではない — 06:263 の「評価時刻より後」は None に対し評価不能ゆえ規則 (1) の下では**常時違反**（None = 実質禁止）とも読める。欠陥の実体は「None の意味論が未定義」。
- **logic**: 未反証。None = 無期限と読む実装では失効検査が当該 profile で永久に発火せず、規則 (1) を適用する実装では None の calibration が全て拒否される — 実装が分岐する。
- **impact**: **反証**（R2 の定式化に対し）。無期限を許すか否かは two-key + Rs 下の author 選択で、frozen / handoff の要求ではない。ただし意味論未定義は仕様欠陥（LOW）。
- **verdict**: CONFIRMED（text + logic）・LOW（再定式化 = None 意味論の未定義）。
- **最小修正（zero frozen delta）**: 06:177 / 06:324 に None の意味を 1 文で確定 — 推奨 =「`CalibrationRef` と required record のうち kind ∈ {CALIBRATION_RECORD, SAFETY_LAYER_ACCEPTANCE} は非 None 必須（`P_VALIDITY_UNBOUNDED`）；他 kind の None = 無期限（失効検査の対象外）」。代替 = 無期限を許す旨を OPP に理由付きで登録。

### R2-19（LOW）ZERO_ORDER_HOLD の点評価は物理経路を見ない

- **sed 実測**: 06:295「hold = LINEAR_INTERPOLATE のとき直前 admitted setpoint → 新 setpoint の線分、ZERO_ORDER_HOLD のとき点」。05:435 同文。05:425「後続 command が未 admit の間、gateway は直近 admitted setpoint を保持」。05:468 SafeHold(DIFF_IK) =「計測現在姿勢で凍結」（robot が setpoint 間で動いている前提）。B:127 `hold` =「policy step 間の保持規約」。D0:314 ISL の `SafetyObservation` = F/T・EE velocity・cable tension・near-contact/penetration・NaN・grip-loss（keep-out zone の監視は列挙に無い）。06:436 OPP-14 = tool 形状のみ ISL 監視。
- **text**: 未反証。引用は正確。ZOH = 点評価は CD-18 fold の明文。
- **logic**: 未反証。hold = ZOH の LEARNED skill、`p_prev`（外）と `p_new`（外）の間に薄い keep-out 板 ⇒ DEPLOYMENT_WORKSPACE は `p_new` のみ評価 ⇒ admit ⇒ controller は板を通過して移動。同じ物理経路が LINEAR_INTERPOLATE では線分評価で拒否される。反証候補「ZOH では gateway が線分を command しない（controller の経路は OP-4）」は内部整合的だが、keep-out が物理制約である以上、gateway が command する点だけの評価は非保守的。
- **impact**: 未反証・LOW。1 step の変位は D1.1-B `bounds`（INTRINSIC 項）で bound され 50 Hz では短い；線分評価は保守側への強化で text-only・zero delta。
- **verdict**: CONFIRMED・LOW。
- **最小修正（text-only）**: 06 §4 / 05 §4.3 DEPLOYMENT_WORKSPACE を「hold 種別にかかわらず（直前 admitted setpoint〔無ければ計測 TCP〕→ 新 setpoint）の線分を評価。tool hull は OPP-14 のまま」に（線分は controller 経路の近似である旨を OPP-14 に併記）。

## 3. 集計と verdict

- confirmed **18** / refuted **1**（R2-10）/ merged **0**（完全重複なし）。
- severity（V2 再判定）: **HIGH 2**（R2-03 zone 幾何等の未照合 hash・R2-04 dual-arm cell の非表現）／ **MEDIUM 6**（R2-01〔HIGH→降格〕・R2-02・R2-05〔= R1-04〕・R2-06・R2-08・R2-09）／ **LOW 10**（R2-07〔MEDIUM→降格・範囲縮小〕・R2-11・R2-12・R2-13・R2-14〔(a) のみ〕・R2-15・R2-16・R2-17・R2-18〔再定式化〕・R2-19）／ **NOT_A_DEFECT 1**（R2-10）。
- 08 §5 の語彙: HIGH が 2 件残るため **HOLD**。両 HIGH の fold 案は zero frozen-schema delta（R2-03 = 05 §3.4 (j) / 06 §3 / 05 §4.3 の一般化、R2-04 = Rs 2 択・(A) は DUAL-ARM 不変前提との整合を要する）。fold 後は PASS-WITH-CONDITIONS へ移行可能。
- R2 の修正案のうち verifier が**採らない**もの: R2-01 (d) の `ack_timeout_s` / `health_confirm_timeout_s`（既に evidence 集合で bound）、R2-12 の from 列追加案（副作用）、R2-14 `P_EVIDENCE_POLICY_NONMONOTONE`（1.0 では到達不能 code）。

## 4. 主張しないこと

- gate PASS / two-key / Rs 裁定を主張しない。凍結 4 file への変更を要求しない（全 fix は 05 / 06 の新語層。R2-04 (B) と R2-09 は 06 自身の deployment 層型の変更）。
- impl / training / closed-loop authority は CLOSED のまま。`eval_runs/` 配下は編集していない（`git status --short -- eval_runs/` = 空・§0）。本 file と `V2_results_v023.json` のみを `scratchpad/review2/` に書いた。
- rate / hold / control_mode の等値のみ（R1）は premise として扱い、finding には含めていない（R2 側も同様 — R2 §0）。

— V2・2026-09-04 22:50 UTC（`date -u` 実測）
