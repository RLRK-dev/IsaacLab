# B_EVALUATOR_PRECHECK — pre-implementation design verification (adversarial)

**Date:** 2026-07-02. **Verifier:** pre-check sub-agent (skeptical mode; assume-flawed).
**Object:** `B_BC_BUILD_SPEC.md` v2 — converter (§2) + trainer reuse (§3) + evaluator `policy_route_runner.py` (§4) + B0/B1 pass bars (§1).
**Method:** every §4.1/§4.4 anchor re-derived against the CURRENT `test_newton_clip_routing.py` (post-batch-fix, sha `ac0e3f53…`); `newton_skill_env_base.py` signatures read; `bc_pretrain.py` + env7 `rsl-rl-lib 5.0.1` API read; the REAL `route_demo_raw.npz`/meta numerically probed (env7 python). Debate-R1-folded items (28) NOT re-reported.

---

## A. Load-bearing spec claims VERIFIED TRUE (no action; evidence for the builder)

| Claim (spec §) | Verified evidence |
|---|---|
| 37 per-arm grip transitions; `grip_cmd[0]=[0,0]` (§2.4) | npz: 37 exact (20 L + 17 R); init `[0. 0.]` |
| `ee_tgt_quat_effective_{l,r}` == default Rx(−90) xyzw ALL frames; exactly ONE override window (§2.4) | npz: maxdev **0.0** both arms; active frames 5000..6180, contiguous windows = 1 |
| pin event ≈ frame 2544, `pinned_body`, `pin_eqid` (§2.4) | npz: first-active frame **2544**, body **55**, eqid **27**; == `cuda_leg_route_c2_pin.json` freeze_scope (seat_k 27, pin_body 55, eq 27) |
| seated seg 27 ≠ phase-entry argmin 28 (CC2-2 trap) (§2.3) | npz: argmin-to-C1 at C1_SEAT entry (frame 2084) = **28**; at pin-fire = **27**. Trap is real; `pinned_body − cable_body_start = 55 − 28 = 27` ✓ |
| max ‖Δ‖ R=8.53 / L=11.39mm, per-comp 8.8mm, max\|a\|=0.59, no clipping (§2.2) | npz: 8.53 / 11.39 / 8.82mm → max\|a\| 0.588 |
| T=7707; frame 7700 last ctrl frame; 6-frame tail zero-motion; phase −1×10 + 13 phases (§0/§2.1) | npz: T 7707; tail max motion 0.0; `ph[:10]`=−1; unique −1,0..12 |
| Trainer reuse viable on env7 (§3) | `bc_pretrain.py` anchors :28/:49/:67-68/:138 EXACT; rsl-rl-lib **5.0.1** `ActorCritic.__init__(num_actor_obs, num_critic_obs, num_actions, actor_hidden_dims, critic_hidden_dims, activation, init_noise_std, …)` matches the call; **`act_inference` exists (actor_critic.py:127)** = deterministic mean; `policy.actor` attr used by the MSE loop exists |
| `make_solver`/`_wire_s6_grasp_solref` importable from base (§4.1-2/7) | `newton_skill_env_base.py:1301` `make_solver(model, backend=SOLVER_BACKEND, use_mujoco_cpu=USE_MUJOCO_CPU, enable_cable_contacts=False)`; `:1343` `_wire_s6_grasp_solref(solver, scene_info=None)`; main() call `test:6615` `make_solver(model, backend=solver_backend, enable_cable_contacts=(mujoco and use_cable))` — spec step-7 call is arg-equivalent |
| `build_scene` sig has NO cable-pos param; `cable_start` hardcoded (§5.1) | `test:1031-1032` (EXACT, undrifted) params `(use_cable, fk_model, fk_state, solver_backend, grasp_actuation, grasp_y)`; `cable_start` `test:1365` (EXACT) |
| PERCLIP_PIN pre-alloc gate read at build (§4.1-1) | `test:1386-1398` (≈exact) |
| env_gates CLIP2 omission workaround valid (§4.1-1, CC5-7b) | route HAS `CLIP2_X/Y` gates (`test:1222-1223`) defaulting to `CLIP_POSITIONS[1]`; meta resolved `[0.4, 0.075]` == defaults; task_config as-run sha == current → evaluator-unset reproduces C2, resolved-assert passes |
| AC env anchors (§2.1/2.2) | `newton_approach_cable_mujoco_env.py:230` `POS_ACTION_SCALE=0.015`, `:238` `PHYSICS_STEPS_PER_RL=10` (EXACT); `task_config:101` `SIM_SUBSTEPS=10`, `:264` `CABLE_XY_DR_AMPLITUDE=(0.020,0.020)` (EXACT) |
| Substrate faithfulness (framework 6) | Canonical = SolverMuJoCo(`use_mujoco_cpu=True` via `task_config:116` default, warp/Newton on cuda:0), `nconmax=MUJOCO_NCONMAX`, `solver="newton"`, `integrator="implicitfast"`; evaluator importing the same base + same task_config (sha `1a0851db…` == meta as-run) reproduces it implicitly. Landing control = the canonical run itself ✓ same substrate. eq-pin pokes on CPU `mj_model/mj_data` are the proven-faithful config (base :1343 docstring) — consistent with pin working in the demo |
| Import structure sound (framework 2) | No deadlock: `newton_skill_env_base.py:75` imports the route module at import-time; route imports base only inside main() (`test:6613`); gates set in step 1 precede EVERYTHING → either import order works. All step-2 symbols module-level (`build_scene:1031, physics_step:1759, solve_ik_dual:1823, build_fk_model:1689, _set_gripper_target:2986`; `__main__` guard :6912). `_physics_state_buffer` is lazily created inside physics_step (:1760-1762) — no main() dependency |

---

## B. ISSUES

### ISSUE 1: §4.1-2 route-file sha assert is UNSATISFIABLE against the current file — B0 cannot start as specced
**SEVERITY: CRITICAL**
**EVIDENCE:** Spec §4.1-2 (BINDING): "Assert route-file sha256 == meta `as_run_sha256` at start". Meta `as_run_sha256.test_newton_clip_routing.py` = `9bdf8f63c192…`; CURRENT file = `ac0e3f5311ea…` (recorder batch-fix, +65/−3, landed 09:06 — AFTER the demo 07:46 and AFTER spec v2 08:32; `P3_RECORDER_BATCHFIX_REPORT.md`). The assert fires at startup step 2 → evaluator STOPs → B0 unreachable. The as-run file IS reconstructable — verified: `git show 8a3c265ad7:…/test_newton_clip_routing.py` + `p3_dod_evidence/wt_hook_diff_8a3c265ad7.patch` (patch -p3) ⇒ sha `9bdf8f63…` EXACT — and the batch-fix carries static byte-identity evidence for `DEMO_RECORD=0` paths (report DoD row: `_demo_rec=None` default + 9 guards + no-op `_ph`).
**FIX:** Rs/spec-owner decision REQUIRED before the build charter (binding contract: deviation = STOP → %12). Options: (i) amend §4.1-2 to a 2-entry allowlist {as-run `9bdf8f63…`, current `ac0e3f53…`} citing the batch-fix byte-identity evidence + record which sha ran in `runner_verdict.json`; (ii) import a pinned as-run reconstruction (hazardous: path-shadowing must also win inside `newton_skill_env_base.py:75`'s route import, and `__file__`-relative asset/URDF resolution must be re-verified); (iii) re-record the demo with the current file (~40min cuda:0) — **but then ISSUE 3 must be fixed first** (15-phase schema). (i) is cheapest and evidence-backed.

### ISSUE 2: §4.5 "40 settle frames" mirror = frame-ledger double-step trap; event pre/post-step convention unpinned
**SEVERITY: HIGH**
**EVIDENCE:** The route's pin block (`test:4111-4145`) runs `for _ in range(40): state = physics_step(…)` AFTER `eq_active=1`; the recorder samples INSIDE physics_step (`test:1815-1816`) — so those 40 frames are ALREADY part of the recorded 7707 and are spanned by the converter's 770×10 ledger as near-zero-Δ control steps. Spec §4.5 "…→ `eq_active=1` → 40 settle frames; :4105-4126 mirror" invites the builder to ADD 40 extra physics_steps → total 7740 ≠ 7700 → every later scheduled event (grip ramps ~20 L-events, override window 5000..6180, landmarks) fires against a time-shifted physics state, and the §9-B0 per-step series is bogus after frame 2544. Same class of ambiguity: npz frame f is a POST-step sample; `note_pin`/`note_grip` fire at command time, so the first stamped frame is produced by the NEXT step — replay must apply an event scheduled at frame f BEFORE executing the step that produces frame f (off-by-one otherwise).
**FIX:** Spec wording: "the poke is instantaneous at its recorded frame index; the settle frames are already in the action stream — the evaluator NEVER steps outside the 770×10 loop"; pin the pre-step application convention for pin + grip + override events; add a hard assert `total_physics_frames == 7700` at end.

### ISSUE 3: Any NEW recording (incl. the ISSUE-1 re-record remedy and ALL B2 recordings) now emits 15 phases — §2.3/§2.4 schema breaks
**SEVERITY: HIGH**
**EVIDENCE:** Current file has 15 `_ph` labels — the 13 in the canonical npz + `GUIDE_PRELIFT` (`test:4232`) + `C2_TRANSPORT` (`test:4535`), added "for future re-records" (batch-fix CC2-C1). Spec §2.3 hard-pins "phase one-hot 13" → obs [770,**25**]; §2.3's per-phase seg table and §2.4 `phase_transitions` key on the 13-name set. Any re-record → meta `phase_names` = 15 → converter one-hot/table/obs-dim (27) all diverge from the pinned layout; §5 (B2) is silent on this. This directly couples to ISSUE 1 remedy (iii).
**FIX:** Make the converter schema meta-driven: one-hot dim = `len(meta.phase_names)`; per-phase seg-rule table generated over the actual name set (the 2 new labels are sub-segments of GUIDE_C2/C2_REGRASP windows — inherit their parents' seg rules); pre-declare the B0/B1(25-dim)-vs-B2(27-dim) policy-input discontinuity (B2 trains fresh policies, but the spec must say so) — or freeze a canonical 15-name superset mapping NOW and emit 15-dim one-hot from THIS npz too (13 observed + 2 always-zero).

### ISSUE 4: `cable_body_start` is NOT in the npz or meta — §2.3's "ground truth IN the npz" is only half-true, and the converter is numpy-only
**SEVERITY: MEDIUM**
**EVIDENCE:** npz keys + meta (read in full) contain `pinned_body` (55) but NO `cable_body_start` / body-index table; the converter may not import env/scene code (§2 "numpy only"). Un-guided builders will either hardcode 28 (magic) or reach for the phase-entry argmin the spec itself bans (= 28 → seg 27 becomes 27−… wrong). Verified derivation from the file alone: at the pin-fire frame, `argmin_s ‖cable_xyz[f*,s,:2] − meta.resolved_clip_c1_xy‖` = 27 (the pin activates on the EXACT runtime seat body, `test:4123-4131`) ⇒ `cable_body_start := pinned_body − 27 = 28`.
**FIX:** Pin THIS derivation into §2.3 (fire-frame argmin, NOT phase-entry) + echo `cable_body_start` into dataset meta; add it to the recorder-meta follow-ups (§0 list) for future records; assert derived value == 28 for THIS npz in §2.6.

### ISSUE 5: ~15 binding line anchors in §4.1/§4.4/§0 are stale after the batch-fix (+3..+14 drift)
**SEVERITY: MEDIUM**
**EVIDENCE (re-derived, current file):** main() wiring :6480-6615 → **:6494-6640** (def :6494); FK init :6531-6548 → **:6547-6566**; `scene_info["fk_model"/"fk_state"]` :6577-6578 → **:6591-6592**; `vbd_control` :6584-6585 → **:6598-6599**; NJMAX/contacts :6588-6589 → **:6602-6603**; make_solver import/call + solref wire :6607-6608 → **:6613/:6615/:6623**; `__main__` :6898 → **:6912**; physics_step :1756 → **:1759** (gripper_dynamic consumer :1779-1799 → **:1782-1802**); ik gripper-preserve :1988 → **:1992-2005**; `gripper_dynamic=True` :3525 → **:3528**; initial-open mirror :3526 → **:3524-3529** (see ISSUE 6); `_arm_split` :3677-3682 → **def :3689**, partition-freeze call :4085 → **:4097**; grip normals :4021-4033 → **:4028-4070** (`mj_contactForce` :4041/:4055/:4067); pin poke :4105-4126 → **:4107-4144**; 4-way verdict :4495-4502 → **:4506-4515** (the old range now lands on the ANTI-REVERT comment block); `_c2_regrasp_rec` :4505-4518 → **:4523-4531**. §0's "(CPU)" banner cite `test:3816` is dead (the banner fix landed; labels now `({DEVICE})`, report "Banner" row). Undrifted: :1031-1032, :1365, :1386-1397(≈), :230/:238/:101/:264, bc_pretrain :28/:49/:67-68/:138.
**FIX:** Refresh the anchor table in a spec v2.1 errata (or switch binding anchors to grep-strings, e.g. `"_wire_s6_grasp_solref(solver, scene_info)"`), so the builder mirrors the right blocks; note the recorder-side cites (`recorder:217`) also moved (227→315-line rewrite).

### ISSUE 6: grip_cmd column order and the servo `control` object provenance are unpinned — both are silent-catastrophe wiring points
**SEVERITY: MEDIUM**
**EVIDENCE:** (a) Recorder `note_grip` (`route_demo_recorder.py:160-165`): `_grip[0]` = L (drivers {6,10}), `_grip[1]` = R ({20,24}) — **L-then-R, the OPPOSITE of §2.2's R-then-L action order**. npz-verified: during L_HALF_UNCLAMP col0 ramps 0.7407→0.6900, col1 pinned at 0.7407. §0 says only "`grip_cmd [T,2]`"; §2.3's `grip_cmd[10t,L]` and §2.4's per-arm events never state the mapping — an order-assuming builder swaps the arms' servo schedules (L opens instead of R closing). (b) §4.1-8's `_set_gripper_target(control, …)` never says where `control` comes from: it MUST be the SAME object physics_step consumes (`scene_info["vbd_control"]`, read at `test:1780`; route fn does `control = scene_info["vbd_control"]` `test:3525`). A fresh `model.control()` compiles, runs, and leaves the servo inert — the exact CC3-2.1 failure class, one level down.
**FIX:** Spec: "grip_cmd col0=L, col1=R (recorder :160-165)"; §4.1-8: "`control = scene_info["vbd_control"]` — never construct a second control object". (Threshold sanity: grip-predicate 0.6 is sound — OPEN 0.0 / HALF 0.69 / CLOSE 0.7407, `task_config:289/:291/:313`.)

### ISSUE 7: The B0 pass-bar's REAL knife-edge is the C2-seat category (≈0.9mm / 1.4mm margins), not the declared reach-wall / close-miss cliffs
**SEVERITY: MEDIUM**
**EVIDENCE:** Canonical margins per category (code `test:4496-4499`, `:4588`; canonical `cuda_leg_route_c2_pin.json`): reach `_at_88` = 0.9mm vs ≤20mm bar → **19.1mm margin**; R-grip 104.51N vs >0.1N → huge; BUT `settled_in_notch` := `cable↔C2_released ≤ 0.5mm` AND `|z − groove| ≤ 3.0mm`, canonical = **−0.392mm (0.892mm from the bar)** and **830.6 vs 829.0 (1.4mm from the ±3 bar)**. A ~1mm open-loop replay drift (§4.2 sub-interp is declared NOT exact, CC3-3) plausibly flips `C2 seat` → a §1 category-FAIL of a healthy pipeline. Also `l_grip_N=0.0` sits 0.1N from relabeling SUCCESS_R_GRIP_L_CAGE→SUCCESS_DUAL_LOADED — harmless ONLY because §1's bar is set-based (`verdict ∈ SUCCESS_*`); §4.4's "equality" phrasing should not tighten this.
**FIX:** Pre-declare C2-seat as the measured knife-edge category: on a C2-seat-only category flip, B0 routes to the §6-row-1 attribution protocol (drift magnitude vs margin reported) instead of a bare FAIL; keep verdict-string comparison set-based, never string-equal.

### ISSUE 8: Evaluator video-leg mechanism + control-loop failure policy unspecified (scope/LOC risk, B1 taxonomy hole)
**SEVERITY: MEDIUM**
**EVIDENCE:** §4.7 mandates a video leg on every VERDICT rollout, but the route's capture runs INSIDE `ik_move_both` (`scene_info.get("recorder")`, `test:2009+`) and `_cap` — paths the evaluator bypasses (§4.2 calls `solve_ik_dual`+`physics_step` directly). No Newton post-hoc replay renderer exists (debate CC6 verified `replay_for_video` etc. wrong-fit). So the builder must add per-control-step offscreen capture (route ViewerGL pattern) — unbudgeted in ~250-400 LOC and unstated in the 8-step checklist. Separately, the ×770 loop defines no behavior for a `solve_ik_dual` failure / exception mid-rollout (route's ik_move_both returns `(state, False)`; plausible under B1 policy actions near limits): a crash produces NO `runner_verdict.json` → the §9-B1 per-rollout taxonomy table can't be filled.
**FIX:** Specify the capture mechanism (mirror the route's offscreen render call at each control step behind `--record-video`, reusing scene camera setup) and a loop-level try/finally that always emits a verdict json with `failure_mode` (add `ik_failure` to the taxonomy enum).

### ISSUE 9: §4.1 checklist gaps + env-gate replay hygiene
**SEVERITY: MEDIUM**
**EVIDENCE:** (a) The BINDING 8 steps never create the physics state: `state = model.state()` (main `test:6595`; route fn `test:3849`) — physics_step's `state` arg has no other source. (b) "Gate VALUES from meta `env_gates`" applied naively is unsafe: meta contains `DEMO_RECORD: "1"` (must be EXCLUDED/forced 0 — if the builder later wires `RouteDemoRecorder` for capture, the global `_demo_rec` hooks re-arm demo-record semantics inside the evaluator) and `C2_TILT_SIGN: null` (`os.environ` can't hold None; a `str(None)`="None" write poisons `float(os.environ.get("C2_TILT_SIGN","0"))` `test:4404` for any consumer). (c) The sha gate covers only the route file, but the substrate is co-determined by `task_config.py` (SIM_SUBSTEPS/NJMAX/USE_MUJOCO_CPU/solref/gripper rads) — meta already carries `as_run_sha256.task_config.py` (currently matching, `1a0851db…`); an un-asserted task_config edit before B0 silently changes physics under a "passing" route-sha assert.
**FIX:** Add step 5b "create the physics `state`"; gate-setting rule = "set only non-null gates, force `DEMO_RECORD=0`"; extend §4.1-2 to assert `task_config.py` sha against meta as well (and record `NEWTON_DEVICE`/`CUDA_VISIBLE_DEVICES` used, device-fragility is a carried P1 risk).

---

## C. Framework mapping

1. **REACHABILITY** — BLOCKED at startup step 2 by ISSUE 1 (sha assert); otherwise the apply path traverses: imports resolve (module-level symbols verified), wiring complete modulo ISSUE 9a, events replayable modulo ISSUE 2, scripted gripper sound (ISSUE 6 mappings once pinned).
2. **DEADLOCKS** — none structural: base↔route import cycle resolves (base:75 vs route-main-lazy); gates-before-import is satisfiable; `_physics_state_buffer` self-initializes.
3. **SPARSE SIGNALS** — §9-B0 series is dense and computable; the only 1-bit-dead surface is a crashed rollout with no verdict json (ISSUE 8).
4. **THRESHOLD GAPS** — C2 `settled_in_notch` at 0.892mm/1.4mm is the true cliff (ISSUE 7); reach/grip/regrasp margins are wide (19.1mm / 3 orders of magnitude).
5. **CAUSAL BREAKS** — settle-frame double-step + pre/post-step event convention (ISSUE 2); grip column swap (ISSUE 6); sub-interp-vs-per-frame-IK gap already declared (CC3-3, measured by B0 — not re-reported); obs live-vs-recorded EE source consistent (`recorder.sample` `bq[ee,:3]` ≡ `get_ee_positions` `test:1914-1921`).
6. **SUBSTRATE-RESOLVABILITY** — verified faithful: same `make_solver` args incl. implicit `use_mujoco_cpu=True` (`task_config:116`), same solref poke, CPU mj eq-pin = the proven config, landing control = the canonical run; faithfulness is contingent on task_config immutability → assert it (ISSUE 9c).

## SUMMARY: 9 issues (1 critical, 2 high, 6 medium)
## VERDICT: **BLOCK** — ISSUE 1 makes B0 unreachable under the spec's own binding startup checklist (fix is cheap and evidence-backed, but it is a locked-artifact/records decision = Rs/%12, not a builder call). ISSUES 2-3 must land in the same spec errata before the build charter; 4-9 are spec-patch items the builder can absorb once written.
