# pZ (IMPL-VERIFIER) — verdict on tip `6c0b76d500`

**Author** pZ / IMPL-VERIFIER (`w2:pZ`) — role brief `VERIFIER_ROLE_BRIEF_pZ_20260721.md` @ `4714493e64`, output form per `:33`–`:35`
**Filed** 2026-08-09 (measurements taken 06:51:45 – 06:52:30 JST)
**Status of this file** written by pZ; **not committed by pZ** — see §6.

---

## 0. Artifact

| | |
|---|---|
| commit | `6c0b76d500b409e40a1720d068c7e930dc247033` |
| parent | `3b4ddfd7ff` — the tip verified in PZ-144 |
| scope | **1 file, 1/0**, measured against **its own parent**, not a moving tip — a pure insertion |
| `ur15_steps_wired.py` | `6ca7247513ca117c03352b20c799deba7db86d3c9965226e530c05eb2b14fe50` — **unchanged** |
| `render_cell_overview.py` | `e2aa041c9777f1e2ccdf5d1be80739f95b0afd4bdf0f16f7bb6b4133a7216760` |

The whole commit:

```
+    out.parent.mkdir(parents=True, exist_ok=True)
```

Position at that commit: `:102 out = _GEN / "UR15_CELL_OVERVIEW_20260729.png"` → `:103 out.parent.mkdir(...)` → `:104 Image.fromarray(grid).save(out)`.
It binds to `out.parent`, so it follows the destination if the destination moves again.

## 1. The pre-registered discriminating test

Registered in PZ-146 **before this commit existed**: a single successful run **cannot** discriminate this change, because `:71 AS_BUILT.parent.mkdir(parents=True, exist_ok=True)` already creates `_gen` on its way to `_gen/meshpool`. The ordinary path succeeds with or without the new line, so a green run would carry no information about it.

The one runtime failure mode available: a `mkdir` that raises when the directory already exists. Therefore **run the render twice**.

| | rc | raster sha256 |
|---|---|---|
| run 1 | 0 | `91a106d16b22e955666c4003b24205c6ff95d6629489e86730fde5287f380b4b` |
| run 2 | 0 | identical |

Traceback / `FileExistsError` lines in run 2: **0**.

⇒ `exist_ok` is **effective at runtime**, not merely present in the source. Reading the flag in the diff shows it was written; only the second run shows it works.

## 2. Conditions

1. raster at `_gen/UR15_CELL_OVERVIEW_20260729.png` == `91a106d16b22e955666c4003b24205c6ff95d6629489e86730fde5287f380b4b` — **met, twice**
2. tracked PNG at the old path **not dirty** — `git status --porcelain` **empty before and after both runs** — **met**
3. (2) is meaningful because the same check returned `" M "` on the pre-relocation code (PZ-143), demonstrated while that was still possible

`auth-2` (explicit mkdir) flips **ABSENT → PRESENT**. Per p4, the flip closes on **landed**, not on implemented.

**Outcome form of the condition** (per p6's law — a condition naming an implementation cannot survive a better implementation):

> the directory the image is written into exists before the write, and a repeated run does not raise

satisfied by `out.parent.mkdir`, by `_GEN.mkdir(exist_ok=True)`, by `os.makedirs`, or by any future rewrite. ⛔ The form sentence in PZ-146 §D named `_GEN.mkdir()` and **must not be quoted as a condition** — it was a guess at how the line would look.

## 3. Item 14

Transfers **on content**: `ur15_steps_wired.py` is byte-identical to the artifact where violations were measured **0** over the complete 25-line target set. Re-hashed, not assumed.

## 4. What this verdict does not claim

- **Nothing about physical validity** — Rs's court, role brief `:28`.
- The raster is evidence of **repair**, not of **result**, and **is not the C-2 cell**.
- ⛔ **The line does not change behaviour** and is not supposed to; `:71` already creates `_gen`. These runs are consistent with a world in which the new line is never exercised. **Insurance cannot be tested by the absence of the accident.**
- Not machine-independent: the seeded XML still carries 7 mesh references under `/home/rlrk/src/ur15-line-render/`, outside any repository.
- **Completeness of intent is out of scope.** This verdict answers whether the artifact satisfies its acceptance items; it does **not** answer whether everything authorized has been implemented.

## 5. Disposition

All three conditions met; scope is one inserted line; `wired` untouched; the one runtime failure mode this change could have had is measured absent. **Nothing refutes it.**

**Landing condition** — landed content sha256 must equal **both** figures in §0. ⚠ The render sha has moved twice within one hour: `4a37a966d4…` covers the relocation (PZ-144), `e2aa041c97…` covers this. A comparison run against the wrong figure reports a false failure on a correct landing.

Post-landing, pZ computes landed-vs-verified from the lane unprompted. That comparison is **confirmation, not a gate** — a check that runs after landing cannot change the landed state.

## 6. Provenance and authority

- Every figure above was computed by pZ from the commit, in pZ's own fresh detached worktree, removed afterwards; `git worktree` registrations returned to their prior count; **zero changes by pZ in the shared working tree**.
- Env pin: `/home/rlrk/env_isaaclab7/bin/python` — newton 1.4.0 / mujoco 3.10.0 / warp 1.15.0.
- ⛔ **pZ has no measured grant to commit.** `Vault Write Permissions.md` (63 lines) keys on **directories** with a generic *CC (Agent)* column and names no pane; a query for `pZ|IMPL-VERIFIER` returns 0 **and so does the control for `p4|RS-TECH-LEAD`**, so that zero is **not discriminating**. `eval_runs/` is not covered by that matrix at all. The role brief assigns landing to p4 (`:39`) and lists pZ's output as the verdict itself (`:31`–`:35`), with no repo surface named.
- ⇒ This file is **written, not banked**. Banking it is requested of the chunk owner or a custodian. Until it is committed it is an untracked file: readable by path, invisible to `git grep <rev>`, and perishable.
