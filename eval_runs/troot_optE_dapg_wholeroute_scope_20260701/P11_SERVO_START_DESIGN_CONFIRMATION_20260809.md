# p11 — servo-start の設計確認（Rs 裁定①「腕を姿勢へ書き込むことは不可」への control-method 側の答）

**依頼** = p4 → p11「servo-start の形の設計確認」（p18 経由 m-p18-224 §4）。**期日** = 「p0 の branch が判ってから」。
⇒ ⭐ **本書は branch に依存しない部分を先に出す**（p0 は今から fix を書くため）。**branch 依存部は §4 に分離し、未確定と明示する**。
⛔ **実装しない・実行しない・run 認可を要求しない。** ⛔ **`:2388` に触れない**（fix chain が (iii) で届くまで誰も触らない）。

---

## 1. 裁定と不変条件（自分で読んだもののみ）

**Rs 逐語**（自分で blob を読んだ。p4 bank `20a4d2620d`・2026-08-09 16:07:24・diff `:36`）:
> 「**腕を姿勢へ書き込むことは不可　すべてコントローラの司令で実現できるはず。**」

**no-detour 不変条件（自分で測った baseline）**: 禁じられる向き `d.qpos[...] = <scratch>` は **HEAD で 0**（positive control: 合成行 `d.qpos[:] = sc.qpos` は 1 で当たる）。⇒ ⭐ **保つべきは「`d.qpos ← scratch` が 0 のまま」**。⚠ **逆向き（`scratch.qpos[:] = d.qpos`）は現に在り（6+ 箇所）、これは live 状態を評価用に読み出しているだけで、腕への書込ではない** — **2 つの向きを同じ語で呼ばない**。

---

## 2. ⛔ 「1 行消せば済む」ではない — 消すと **IK の種が黙って変わる**（本書の中心）

build tip `2fba2dfd67` の `ur15_steps_wired.py` を直読（⚠ HEAD では行番号が違う。⭐ 数には測定 rev を同じ行に置く）:
| 行 | 内容 | 役割 |
|---|---|---|
| `:1653` | `d.qpos[_a4] = HOME_POSE[_k4]` | **裁定が禁じた書込** |
| （直後） | `d.ctrl[_i4] = HOME_POSE[_k4]` ＋ `mujoco.mj_forward(m, d)` | servo 目標は**既に**与えている |
| **`:1658`** | `START = {t: np.array([d.qpos[a] for a in QADR[t]]) for t in SIDES}` | ⭐ **書き込んだ姿勢をそのまま読み戻す** |
| **`:1661`** | `START[t] = solve_ik(t, GRASP1[t], tries=24, near=START[t], …)` | ⭐ **START が IK の種（`near`）になる** |

⇒ ⭐⭐ **書込だけ消すと、`:1658` は「HOME」ではなく「model を build した姿勢」を読む** ⇒ **`:1661` 以降の IK の種が全部変わる**。⛔ **失敗として現れず、別の解に収束するだけ**なので、**削除だけの fix は静かに別の機械を作る**。
✅ **構造的に settle は可能**: 同 file で `mj_step(m, d)` は**書込点の前に 4 回・後に 5 回**（実測）⇒ **その場で live model を進める術は既に在る**。

---

## 3. ✅ 設計確認（branch に依存しない部分・本節が私の court の答）

**servo-start の形 = 3 つの要件**:
1. **腕へは servo 目標のみを与える**（`d.ctrl[arm] = HOME_POSE`）。⛔ `d.qpos[arm]` への代入を置かない。⛔ **scratch で作って `d` へ状態を写す迂回も不可**（§1 の不変条件）。
2. **目標を与えたあと、PD の実移動で HOME へ到達させる**（`mj_step(m, d)` を収束まで）。⭐ **収束判定は既存定数を使う**（同 file が `SETTLE_S` / `SETTLE_TOL` を import 済み）— **新しい閾値を発明しない**。
3. ⭐⭐ **`START` の読み出しを settle の後ろへ移す**。⇒ **`START` は「命じた姿勢」ではなく「実現した姿勢」でなければならない**。

**根拠（先例・`probe/pd1-arm-pd` `armpd_probe.py` を自分で読んだ）**: 同 probe は **腕の qpos 書込 = 0** で、`:197-205` が原理を逐語で述べている — 「the servo target seeded at build = the build pose; reset moved the arms … **Sync ctrl := realized q once so the route starts from a matched target (no stale-target step at frame 0)**」。
⇒ ⭐ **原理 = 「命じた目標」と「実現した q」が frame 0 で一致していること**。先例は *目標 := 実現 q* で合わせ、本件は *目標 := HOME・実現 q を PD で HOME へ* と**向きが逆なだけで同じ要件**。
⛔ **私の測定の訂正**: 先例で `.ctrl[` の出現を数えて 0 を得たが、**これは無意味な 0** — 同 file は raw mujoco ではなく env API（`env._control.joint_target_pos`）で駆動している。**面が違えば 0 は何も言わない**。

⚠ **本節が言わないこと**: HOME_POSE の値の妥当性／到達所要時間／PD gain の適否。**どれも未測**であり、本節は**形**だけを確認する。

---

## 4. ⏸ branch 依存部（p0 の zero-pose 診断が決めるまで未確定）  **[RESOLVED-BY: §9 — 非衝突枝で確定]**

| 診断結果 | 設計上の帰結 | 私の立場 |
|---|---|---|
| **非衝突** | §3 の 3 要件で足りる（目標 → settle → 読み戻し） | ✅ **そのまま確認できる** |
| **衝突** | 開始条件そのものが物理的に無効 ⇒ **制御の問題ではない** | ⛔ **私は設計しない** — 裁定の map どおり **数値を添えて Rs へ戻す**。⚠ 「asset 宣言の home（keyframe / joint ref）を使うか」は **Rs が引く境界**（keyframe reset が「書込」に当たるかは私の court でない） |

⇒ ⛔ **どちらの枝でも、§3 の 3 要件（servo 目標のみ／PD 実移動／実現 q を読む）は変わらない**。変わるのは **HOME へ至る経路**だけ。

---

## 5. 私が確認しない／触れないもの

- ⛔ 実装（p0 の file）・検証（pZ）・run 認可（Rs）。⛔ `:2388`（tip `:1653`）に触れない。
- ⛔ 退役 route と video 3 兄弟の offline-replay 分類（**OPEN のまま**）・shadow-rollout class（**未裁定**）。
- ⚠ 本書は **§14.27（kinematic 上書き 6 行の remediation 裁定）の bank 可否とは別件**。⭐ ただし**同じ不変条件に接する**ので、§14.27 を bank する際は §1 の no-detour 不変条件を同じ形で持たせる。

⚠ **等級**: 依頼は p4（誘発）。§2 の「削除だけでは IK の種が変わる」・§1 の no-detour の向きの分離・§3 の先例読みと自分の wrong-surface 0 の訂正は、私が測って出した。

---

## 6. 追記 2026-08-09 16:1x — ⛔ **ctrl は「増える」必要がない。servo 目標は既に同じ場所に在る**

pre-registered な受入基準に「**live `d.ctrl` 7 → 7 以上でなければならない。qpos が 0 に落ちて ctrl が増えないなら、腕は何にも命じられていない**」が入った。⛔ **この理由づけは本件では成り立たない**。

**実測（build tip `2fba2dfd67`・自分で blob を読んだ）**:
```
:1653        d.qpos[_a4] = HOME_POSE[_k4]      ← 裁定が禁じた書込
:1654    for _k4, _i4 in enumerate(AIDX[_t4]):
:1655        d.ctrl[_i4] = HOME_POSE[_k4]      ← ⭐ servo 目標は既に在る（2 行後・同じ block）
:1656    mujoco.mj_forward(m, d)
:1658    START = {t: np.array([d.qpos[a] for a in QADR[t]]) for t in SIDES}
```
⇒ ⭐⭐ **HOME への servo 目標は既に書かれている** ⇒ **ctrl が増えなくても腕は命じられている**。⛔ **「ctrl が増えないなら命じられていない」を根拠に、p0 が冗長な ctrl 行を足す必要はない**（閾値「7 以上」自体は据え置きで通る）。

⚠ **数の食い違いは食い違いでない**: 私の実測は **tip で `d.ctrl[...] =` の行一致 = 3**（`:1655` `:1687` `:2560`・positive control 1）、相手は **HEAD で AST node = 7**。⇒ **版が違い、数える単位も違う**（行一致 対 node）。⛔ **どちらが誤りとも言わない** — ⭐ **数を並べる前に、版と単位を同じにする**。

⭐ **本当に識別する検査は ctrl の本数ではない**: 要件 (3) が満たされたかは「**目標を与えてから `START` を読むまでの間に settle が走ったか**」で決まる。⇒ **ctrl の個数はそれを見ない**（目標は前からあるので、fix の前後で変わらない）。⇒ ✅ **受入表に置くべき述語 = 「`d.ctrl[arm]` の代入と `START` の読み出しの間に、live `d` を進める `mj_step` が在る」**（＋ 収束判定が既存 `SETTLE_S`/`SETTLE_TOL` であること）。⛔ これが無ければ、qpos 書込が消えて ctrl が 7 のままでも、**START は build 姿勢を読み続ける**（§2 の欠陥がそのまま残る）。
⚠ **等級**: 契機は他卓の受入表（誘発）。ctrl が既在であること・「ctrl 本数は識別しない」・版と単位の分離は私が測って出した。

## 7. 追記 2026-08-09 16:2x — ⛔ **§6 の「3」は低かった。私の正規表現も同じ壊れ方をしていた**

§6 で「私 3 / 相手 7 の差は **版と単位**の違い」と書き、**そこで止めた**。⛔ **三つ目の原因があった** — 私の述語 `\bd\.ctrl\[[^]]*\] *=` の `[^]]*` は **入れ子の添字で切れる**。他卓が自分の 4 について名指した defect と**同じ物**。

**自分で AST walk を走らせた実測**:
| rev | AST node | 私の正規表現 | ⛔ 取りこぼし |
|---|---|---|---|
| tip `2fba2dfd67` | **6**（`991, 1655, 1678, 1687, 2556, 2560`） | **3**（`1655, 1687, 2560`） | **3** = `991, 1678, 2556` |
| `HEAD` | **7**（`1031, 2390, 2517, 2568, 2576, 3475, 3479`） | 4 | **3** = `1031, 2517, 3475` |

取りこぼした 3 本はいずれも **`d.ctrl[GIDX[t]] = …`**（入れ子の添字）。
⇒ ⛔ **版でも単位でもなく、私の述語が壊れていた** — **私の言う単位（行一致）で数えても 6 が正**で、3 ではない。
⇒ ⭐ **差の内訳も訂正**: 実際は **tip 6 対 HEAD 7**（版差 = 1 本）＋ **私の取りこぼし 3 本**。§6 は「版と単位」で全部を説明したが、**版が説明するのは 4 のうち 1 だけ**だった。

⚠ **結論への影響**: 取りこぼした 3 本は **gripper の ctrl（`GIDX`）で腕ではない** ⇒ ⭐ **§6 の主張（腕の servo 目標は `:1655` に既在・ctrl 本数は要件 (3) を識別しない）は変わらない**。⛔ **ただし公表した数は誤りで、照合しに来た者は追いかける**。

⇒ ⭐⭐ **一般形（今夜の族に足す）**: **「版が違う・単位が違う」は座りの良い説明で、三つ目の原因（述語が壊れている）を退場させてしまう**。⛔ 他卓が「無害そうな帰属は、説明されていない事象を retire させる」と名付けたのと**同じ形**を、私は**数の食い違いに対して**やった。⇒ **食い違いを説明で閉じる前に、自分の述語を別の機構（AST）で測り直す**。
⚠ **等級**: 契機は他卓の「3 卓・3 つの正規表現の仮定」の指摘（誘発）。自分の分を AST で測り直し、内訳を割り直したのは私。

## 8. 追記 2026-08-09 16:2x — **§14.27（任務2）の第 1 パス** ＋ ⛔⛔ **私は自分で publish した族の法を、20 分後に自分で破った**

依頼が無く他項目が塞がっていたので、**role brief `:20-24` の任務2（§14.27 の bank 可否）**に自分で着手した。⛔ **本節は bank しない** — 第 1 パスの実測と、bank に何が要るかの提示のみ。

### (8)-1 ⛔⛔ 先に自己捕捉 — **同じ壊れた文字クラスを、法を書いた 20 分後に使った**
§7 で「`[^]]*` は入れ子添字で切れる／説明で閉じる前に別機構で測り直せ」と書いた。⇒ ⛔ **その直後、§14.27 の対象を数えるのに `phys_jq\[[^]]*\] *=` を使った**（合成 `phys_jq[IDX[t]] = x` に **0**）。
**AST で測り直した実測（HEAD・6 file）**:
| file | AST node | 私の flat regex |
|---|---|---|
| `route_executor.py` | **18** | 4 |
| `newton_route_env.py` | **14** | 2 |
| `newton_approach_cable_mujoco_env.py` | **12** | 2 |
| `test_newton_clip_routing.py` | 6 | 2 |
| `test_routeexec_writesite.py` | 5 | 3 |
| `newton_skill_env_base.py` | 4 | 1 |
| **合計** | **59** | **14** |
⇒ ⛔ **45 本取りこぼした**（§7 の ctrl は 3 本だった。**同じ欠陥で、桁が違う**）。⭐ **法を知っていることと、次に手を動かすとき使わないことは別物** — 書いた直後でも既定の書き方に戻った。
⚠ **自分の 3 つの数も単位で割れる**: (1) `phys_jq[..]=` **または** `phys_jq =` の行一致 = **30**／(2) 添字形のみ = **14**／(3) AST node = **59**。⇒ ⭐ **同じ問いに自分の中で 3 つの数**。⛔ 他所の数（DDR#35 の「19」）は**読んでいないので裁定しない**。

### (8)-2 ✅ Layer 8（機械 gate）の実測 — **私の手製述語より堅い**
`scripts/validations/check_control_method.sh` は **tracked**（⚠ Layer 6 の delegate は untracked だった。**同じ形ではない**）。その述語 `:48` = `grep -rnE '(phys_jq|joint_q|qpos)\[[^=]*\][[:space:]]*=[^=]'` ⇒ ⭐ **クラスが `[^=]*` なので入れ子添字を跨げる** ＝ **今夜の族の欠陥に該当しない**。⚠ ただし **逆向きの性質**（同一行の後方の `=` まで走り得る＝過剰一致）を持つ。
⚠ **母集団**: `:30` が `thread_isaac_lab/envs/*.py` の **maxdepth 1** に限定 ⇒ **envs/ のみ**。私の 59 は `scripts/` を含む 6 file。⇒ **数を比べる前に母集団が違う**。

### (8)-3 ⏸ §14.27 の bank 可否 — **第 1 パスの所見（結論は出さない）**
- ✅ **Rs 裁定①が、§14.27 が両論併記していた remediation の向きを決めた**: §14.27 の義務は「**delete / actuator 化**」の 2 択だったが、裁定は「**すべてコントローラの司令で実現できるはず**」⇒ **actuator/servo 側が既定**。⭐ **かつ本書 §2 が示したとおり、delete 単独は不十分**（読み戻しが残る）。
- ⛔ **残る未測**: §14.27 `:78` が自ら「**live であるとは主張しない**」（`_PIN["active"]` gate ＋ exercise 未測 ＋ 中間 module の backend 未確定）と書いている。⇒ **dead/live が未確定のまま bank すると、「効いていない機構への裁定」を効いている物として置くことになる**。
- ⛔ **私の側の前提が崩れた**: 対象規模は **19 でも 30 でも 14 でもなく、私の AST では 59**。⇒ **remediation の範囲が確定していない状態で bank しない**。
⇒ ⭐ **bank に要る 3 つ（提示のみ・実行は私の court でない）**: ①**単一の機械で数えた対象一覧**（AST か Layer 8 の述語を 1 つ選ぶ）②**dead/live の判定**（`_PIN["active"]` の実効値と exercise）③**裁定①との突合**（各 site が servo 化で置換可能か、delete で足りるか）。⛔ ①②は測定で、**私の court は③**。
⚠ **等級**: ⭐ 着手は self-scheduled（依頼なし・任務2）。⛔ (8)-1 の自己捕捉は、他卓が publish した族の法を自分に当てて出た。

## 9. 追記 2026-08-09 16:3x — **§4 の branch が決まった（非衝突）⇒ 設計確認は完成** ＋ ⭐ **端点が空いていることは、経路が空いていることではない**

### (9)-1 ✅ branch = 非衝突 ⇒ §3 の 3 要件がそのまま答
診断値（**⚠ 他卓の計器の出力を回付で受け取ったもので、私は走らせていない＝ relayed / 未自験**）: `qpos=0` で **arm↔arm +491.3 mm**（`L_base#2 ↔ R_base#41`）・**arm↔table +19.8 mm**（`R_forearm#44 ↔ table_top`）＝ CLEAR／HOME_POSE 対照で **+194.2 / +80.1** ＝ CLEAR。
⇒ ✅ **§4 の 2 枝のうち「非衝突」で確定** ⇒ **§3 の 3 要件（servo 目標のみ／PD 実移動で HOME へ／`START` は実現 q）が、そのまま完成した設計確認**。⛔ **keyframe の境界問題は発生しない**・⛔ **初期条件を Rs へ戻す必要もない**。

### (9)-2 ⭐⭐ ただし要件 (2) に条件を 1 つ足す — **settle は端点でなく経路で評価する**
診断が測ったのは **2 つの端点**（`qpos=0` と `HOME`）であって、**その間を PD が掃く経路ではない**。⇒ ⭐ **両端が空いている ≠ 経路が空いている**。
⚠ そして **margin は薄い側にある**: `qpos=0` の **arm↔table = +19.8 mm**（arm↔arm の +491.3 mm ではない）。⇒ **settle の間にテーブルへ 19.8 mm 以内まで寄る動きがあれば、端点だけ見ている限り見えない**。
✅ **要件 (2) の完成形（本節が正）**: 「目標を与え、**PD の実移動**で HOME へ到達させる（収束判定は既存 `SETTLE_S`/`SETTLE_TOL`）。⭐ **かつ settle の経路上でも arm↔table / arm↔arm の clearance を評価する** — **端点 2 点の CLEAR では足りない**。」
⇒ ⭐ **検査手段は既に在る**: 受入表に along-path 列（連続 STEP 間の関節空間線形補間・`mj_forward` ＋ `mj_geomDistance`）が折り込まれている。⇒ **同じ計器を settle 区間（現在姿勢 → HOME）にも当てればよい**。⛔ **新しい機構を作る必要はない**。
⚠ **等級**: branch の解決は他卓の診断（誘発・**数値は relayed で未自験**）。「端点 ≠ 経路」と「薄い側は 19.8 mm」の接続は私。⭐ 同じ形を私は以前 memory に持っていた（**回復機構は終点だけでなく経路にも同じ制約を課す**）— **今回は使えた**。

### (9)-3 これで私の設計確認は完結（未確定を残さない）
✅ **3 要件 ＋ (9)-2 の経路条件** = p0 が実装できる完成形。⛔ 実装・検証・run 認可は私の court でない。⛔ 私はこの先、**p0 の実装に対する疑義が routed されたときのみ**動く。

## 10. 追記 2026-08-09 16:4x — ⛔ **(9)-2 の「既存の計器を当てればよい」は誤り**（懸念は正しく、私が挙げた手当てが的を外していた）

(9)-2 で「along-path 列を settle 区間に当てればよい・**新しい機構は要らない**」と書いた。⛔ **これは誤り**。

**実測（tip `2fba2dfd67`・自分で読んだ）**: along-path の距離ヘルパ周辺（`:1340-1345` の最小値・geom 対の記録、`:1348` `arm_sets_disjoint`）の範囲に **`mj_step` は 0**（対照: 同 file 全体では **9**）。⇒ ⭐ **この計器は live model を進めない** ⇒ **測っているのは「命じた経路」（関節空間の線形補間）であって、PD が実際に描く軌跡ではない**。
⇒ ⛔ **PD の settle は動的で、行き過ぎ・振れがあり得る** ⇒ **補間標本のどれよりも近くを通り得る**。⇒ **私が挙げた手当ては、私が挙げた懸念を測らない**。

✅ **正しい状態（本節が正）**:
- **懸念は正しい**（端点 2 点の CLEAR は経路の CLEAR ではない・薄い側は arm↔table の +19.8 mm）。
- ⛔ **静的な計器では測れない** — 命じた経路の標本は、実現した traverse の代理にならない。⇒ **現状の正直な等級は「未測・gate 済」**。
- ✅ **測れる形は 1 つ**: **settle の loop 自身に、arm↔env clearance の走行中最小値を geom 対つきで記録させる**（＝ settle が自分で生む副産物。新しい計器も新しい認可も要らない）。⇒ **run が認可された日に、traverse が副産物として測られている**。⛔ **これは受入条件ではない**（実装者の選択）。
⇒ ⭐ **私の 3 要件 ＋ 経路の懸念は残り、経路の「手当て」だけが差し替わった**。⛔ **「懸念が正しい」ことを「手当ても正しい」に持ち越さない** — 今夜私が他所で書いた形（測った量に、それが測っていない結論をぶら下げない）の、自分の版。
⚠ **等級**: 誤りの指摘は他卓（誘発）。⛔ 私は「既存の計器で足りる」と書いて、**その計器が step しないことを確かめずに**書いた。

## 11. 追記 2026-08-09 16:4x — **`bc0bfe5b88` の設計確認**（3 要件は満たされている）＋ ⛔ **「commanded settle span」という読みを 1 語訂正する**

### (11)-1 ✅ 3 要件は実装されている（diff を自分で読んだ・`bc0bfe5b88` 2026-08-09 16:39:15・16/5・1 file）
| 要件 | 実装 | 判定 |
|---|---|---|
| (1) 腕へは servo 目標のみ | `d.qpos[_a4] = HOME_POSE[_k4]` の 2 行が削除され、**既存の servo block はそのまま**（冗長な ctrl 行の追加なし） | ✅ |
| (2) PD の実移動で HOME へ（既存定数） | `for _s4 in range(int(SETTLE_S / m.opt.timestep)): mujoco.mj_step(m, d)` ＋ `max abs(qpos − HOME) < SETTLE_TOL` で break | ✅ |
| (3) `START` は実現 q | 読み出しが settle の後ろへ移り、**理由がコード内のコメントに書かれた**（「every IK seed below inherits it」） | ✅ |
⭐ 旧コメントの**理由**（「zero is arms crossed」）も、測定値に置き換わっている。⇒ **正しく見える行の下の腐った理由**が、register だけでなく**コードから**消えた。

### (11)-2 ⛔ 訂正 — **「commanded settle span の補間」は、命じた経路を測らない**
回付された読みは「(2) の経路条件 = **命じた settle 区間**の補間を計器側の 1 行で測る」。⛔ **この呼び方が事実と合わない**。
**実測（diff より）**: 目標は **`HOME_POSE` 1 点を一度置くだけ**（既存 ctrl block）で、以後は `mj_step` を回すのみ。⇒ ⭐ **command は step 入力であって、軌道ではない** ⇒ **「命じた経路」は存在しない**。⇒ `qpos=0 → HOME` の直線補間は、**command も physics も生まない、誰も辿らない直線**。
⚠ **無価値ではない — 非対称**: **当たれば情報**（もっともらしい traverse 族が危ういと分かる）。**空いていても何も言わない**（PD の弧については無情報）。⇒ ⛔ **「空いた」を「settle は安全」と読ませない label が要る**。
⚠ **私の (9)-2 の「新しい機構は要らない」は §10 で撤回済** — 回付文はその撤回前の版を引いている。⇒ **along-path 列は step しないので traverse を見ない**（§10 実測）。

### (11)-3 ✅ 私の一語
- **置き場所の読みは正しい**: 追加の評価は **計器側の 1 行**であって **wired の中のコードではない**（wired 側に足すのは実装者の選択で、受入条件ではない）。
- ⛔ **呼び方を訂正**: 「commanded settle span」ではなく **「settle 区間の直線補間（誰も辿らない参照線）」**。**当たりのみ情報・空きは無情報**と label する。
- ✅ **第 3 節（followed traverse は run まで未測・gate）はそのまま成立**。⭐ **commanded と followed は別物**であり、settle については **commanded が存在しない**ぶん、両者の隔たりは route 区間より**大きい**。
⇒ ⭐ **設計確認 = `bc0bfe5b88` は 3 要件を満たす。経路の懸念は未測・gate 済のまま**（測れる形は settle 自身が走行中最小値を記録すること、run が認可された日に）。
⚠ **等級**: 依頼は routing（誘発）。diff の直読・step 入力に commanded path が無いという指摘・非対称性の label は私。

## 12. 追記 2026-08-09 16:5x — **確認の対象が動いた（`bc0bfe5b88` → ＋`d14ffac4e2`）** ＋ ⛔ **私が「薄い側」と名指した対が、記録器の見ていない対だった**  **[RESOLVED-BY: §13 — (12)-2 の穴は閉じた]**

⚠ §11 は **`bc0bfe5b88` についての確認**。⛔ **その後 object が動いた**（`d14ffac4e2` 2026-08-09 16:41:08・17/0・同 file／wired content sha256 `6ca7247513ca117c` → **`12f9d034a6568ac7`**・自分で計算）。⇒ **§11 の射程を明示する**（claim は object についてのもので、object が動けば射程を言い直す）。

### (12)-1 ✅ 3 要件は現 tip でも満たされている
`d14ffac4e2` は **17/0（追加のみ）** で、`bc0bfe5b88` の 3 要件部分を**書き換えていない** ⇒ ✅ **§11 の確認は現 tip でも成立**。追加分は **settle 中の走行最小 clearance の記録器**（＝ 私が §10 で「測れる形は 1 つ」と言った副産物）。

### (12)-2 ⛔ ただし記録器は、**私が薄いと名指した対を見ていない**（自分で測った）
settle 区間（現 tip `:2395-2425`）の呼び出し: `arm_pair_min` **1**／`column_gap` **1**／**`furniture_gap` 0**（file 全体では 17 / 13 / **5**・positive control 発火）。
⇒ ⭐⭐ **記録されるのは arm↔arm（+491.3 mm）と arm↔column の 2 つ＝広い方の対**。⛔ **診断で最も薄かった arm↔table（+19.8 mm）は、settle 区間で一度も呼ばれない**。
⇒ ⛔ **私が §9-2 で「薄い側は +19.8」と名指した、まさにその対が記録されない** — **記録器は私の懸念の理由の側を見ていない**。⚠ label 自体は正直（arm↔column と述べている）ので、**誤りではなく被覆の穴**。⇒ ⚠ **危険は読み手側**: 「traverse worst」を **the worst** と読むと、**最も薄い対を含まない数を最悪値として受け取る**。
⇒ ✅ **私の側の言い方（本節が正）**: 記録器の出力を引くときは「**arm↔arm と arm↔column の走行最小。arm↔table は未記録**」と書く。⛔ **「traverse は N mm まで寄った」と裸で書かない**。
⛔ **1 行足すかは実装者の court**（受入条件ではない）。私は**述語と label を出すだけ**。

### (12)-3 私の確認の最終形
✅ **3 要件 = `bc0bfe5b88` で満たされ、`d14ffac4e2` でも保たれる（追加のみ）**。⛔ **経路（followed traverse）は未測・gate 済**。⚠ **記録器が付いた後も、薄い対については未記録のまま**。⇒ ⭐ **「記録器が付いた」を「経路が測られた」と読ませない**。
⚠ **等級**: object が動いた事実は他卓の訂正（誘発）。⭐ **記録されない対が、私が薄いと名指した対だった**という接続は、私が呼び出しを数えて出した。

**⛔ (12)-4 即時訂正（同 turn・landing 直後に自分で気づいた）**: (12)-2 の等級を「**記録されない対が、私が薄いと名指した対だという接続は私**」と書いた。⛔ **over-claim**。**記録器のコメント自身が、既に同じことを逐語で書いている**（現 tip settle 区間内・自分で読んだ）:
> 「worst over the two ROOMY pairs while the tightest margin at this mounting is the **TABLE (+19.8 mm at the endpoints)** — a "worst" that excludes the tight pair reads as the worst and is not.」

⇒ ⭐ **接続は実装者が先に書いており、私はそれを測って再現しただけ**。✅ 私が独立に出したのは **呼び出し数の実測**（`arm_pair_min` 1 / `column_gap` 1 / `furniture_gap` **0**）であって、**「薄い対が抜けている」という発見ではない**。
⇒ ⭐ **教訓（自分用）**: **測った対象が、自分の発見を既に書いていることがある**。⛔ **数えた事実と、その意味の発見者を混同しない** — 今夜の私の等級規律の、最後の抜け。

## 13. 追記 2026-08-09 16:5x — **(12)-2 の穴は閉じた**（自分で確認）＋ ⛔ **今回は「追加のみ」で確認を継がなかった — 中身で確かめた**

**実測（現 tip・wired content sha256 `4981b37a8cc65d41`・自分で計算）**:
- ✅ **`furniture_gap` が記録器 block 内で呼ばれている = 1**（file 全体 **5 → 6**）⇒ ⭐ **(12)-2 で私が測った「薄い対（arm↔table）が未記録」は解消**。⇒ **(12)-3 の注意書き「記録器が付いた後も薄い対は未記録」は失効**（本節が正）。
- ✅ **live `d.qpos` への書込 = 0**（positive control 発火）⇒ 要件 (1) 保持。
- ✅ settle の既存定数（`SETTLE_S / m.opt.timestep`・`SETTLE_TOL`）が在る ⇒ 要件 (2) 保持。読み出し順の要件 (3) も同 block 内で不変。
⇒ ✅ **§11 の 3 要件確認は、現 tip でも成立する**。

⛔ **ただし今回は §12-1 の論法を使えない**: `01b2149ad3` は **9/4**、`d35d3e8973` は **2/1** ＝ **追加のみではない**（記録器を書き換えている）。⇒ ⭐ **「追加のみだから確認は継がれる」は使えず、要件そのものを測り直した**（qpos 0 / 定数の存在 / 読み出し順）。
⇒ ⭐ **教訓**: **diff の形（追加のみ）で確認を継ぐのは、形が保たれている間だけ有効な近道**。⛔ **形が崩れたら、近道ではなく述語に戻る** — 私は §12-1 で近道を使い、本節では戻った。

⇒ **私の確認の現在形**: **3 要件は tip `4981b37a8cc65d41` で満たされる**／**followed traverse は依然 未測・gate 済**（記録器は run が認可された日に 3 対すべてを答える）。⛔ **「記録器が 3 対を見るようになった」を「経路が測られた」と読ませない** — 走っていない。
⚠ **等級**: 対の追補は実装者（私は測って確認しただけ）。⭐ **近道を使った/戻ったの区別**は私。

## 14. 追記 2026-08-09 16:5x — **「hash は関数名を伴って初めて pin」を自分の 4 file に当てた ⇒ 該当インスタンス 0**（限界つき）  **[SUPERSEDED-BY: §16 — 「私設の綴り」の部分のみ撤回]**

他卓で起きた衝突は **同一 file の content-sha256 と git blob OID（SHA-1）** の取り違えだった。⇒ **自分の面に同型が在るか**を測った。
**実測（4 file・`` `[0-9a-f]{10,64}` `` を hash token とする。positive control 1・negative control 0）**: hash を含む行 = **49**／うち関数名（`sha256`/`SHA-1`/`blob`/`commit`/`OID`）を同じ行に持つ = **15**。
⇒ ⛔ 一見すると 34 行が無名。⇒ **全部を読んで分類した**: 無名のものは **すべて git の revision 参照**（文脈語 = `tip` 5・`bank` 1・`@ <rev>` 形・「初出 = `<commit>`」形）。⇒ ✅ **私が file の内容を hash した箇所は、例外なく `sha256` / `content sha256` / `blob` と明記されている**。
⇒ ⭐ **結論: 他卓を噛んだ衝突（同じ file の 2 関数）は、私の面には 1 件も無い**。理由は運ではなく、**file を hash するときだけ関数名を書く**という書き方を通していたから。
⚠ **限界（先に書く）**: これは **文脈語による分類**であって証明ではない。`tip`/`@` を伴わない file hash が在れば数え落とす。⇒ **母集団が小さい（無名 34 行）ので全部読んだ**が、**機械的には保証していない**。
✅ **以後の規則（本節が正）**: **hash が「file の内容」のときは関数名を必ず同じ行に書く**（sha256 か blob OID か）。**revision 参照は `tip` / `@ <rev>` / `commit` の文脈語を伴わせる** — これも別の namespace の明示であって、無印にしない。
⚠ **等級**: 法は他卓（誘発）。自分の面に当てて分類したのは私。⭐ **null 結果だが、対照が発火しているので「当たらなかった」ではなく「無い」**。

## 15. 追記 2026-08-09 17:0x — **KEEP 確定 ⇒ 引用規則を完成させる**（私の規則は「どの対か」までで、「読みが不在のとき何を意味するか」を欠いていた）  **[AMENDED-BY: §17 — 第 3 節に「どの query の半径か」を足す]**

KEEP ⇒ **記録器の数はこれから引用される**。⇒ ⛔ **私が §12/§13 で置いた引用規則は 2 節しかない**（どの対が記録されるか／記録器の存在は経路の測定ではない）。**3 つ目が要る**。

**実測（HEAD・自分で読んだ）**: 記録器の print は `_traverse_say(v, who, what, radius_mm)`（`:2429-2432`）で、**`v > 1e8` のとき `f"{what}: nothing within the {radius_mm:.0f} mm search radius"`** を返す。半径は `ARM_PAIR_CUTOFF * 1000`（arm↔arm）と `ARM_DECIDE_CUTOFF * 1000`（arm↔column/furniture）。
⭐ **そして半径は定数ではない**（spec 直読・`2fba2dfd67`）: **`ARM_PAIR_CUTOFF = 4.0 * GRIP_HALF_SPAN`**（`:645`）／**`ARM_DECIDE_CUTOFF = 2.0 * ARM_CLEARANCE`**（`:810`）。⇒ **176 mm は `GRIP_HALF_SPAN` が動けば動く**（4.0 × 44 mm）。

✅ **引用規則 完成形（本節が正・3 節）**:
1. **どの対か**を書く（arm↔arm ／ arm↔column ／ arm↔furniture の 3 対を記録）。
2. ⛔ **「記録器が在る」を「経路が測られた」と読ませない** — followed traverse は run 認可の日まで未測。
3. ⭐ **新設**: **「nothing within the N mm search radius」は「一度も範囲内に入らなかった」であって「非常に空いていた」ではない**。⛔ **数の代わりに安全を読み取らせない**。⚠ **かつ N は導出値**（`4.0 × GRIP_HALF_SPAN` / `2.0 × ARM_CLEARANCE`）⇒ **引用するときは値と導出の両方を書く** — でないと、行番号と同じように**上流が動いた日に静かに腐る**。
⇒ ⭐ **既知の 2 つの configuration は両方とも半径の外**（491.3 と 194.2 に対し 176）⇒ **「範囲内に入らなかった」は例外でなく既定の出力**。⛔ それを「clear」と読む余地を残さない。
⚠ **等級**: KEEP は決定者（誘発）。**半径が導出値であること**と**引用規則に 3 節目が要ること**は、私が読んで出した。

## 16. 追記 2026-08-09 17:1x — ⛔ **§14 の観測は「不在」を 2 種混同していた**（他卓の kind 区別を受けて自分で確認）

§14 で「**file は absence の綴りを 1 か所（`gap_mm`）に集めており、新しい print は 2 つ目の私設の綴りを足した**」と書いた。⛔ **この観測が不正確**。

**確認（HEAD・自分で読んだ）**:
- `gap_mm` の受け取るもの = **`None`**（`:1883` 逐語「arm_pair_min returns None when no pair is inside the search radius」）＝ **「読みが返ってこなかった」**
- `_traverse_say` の分岐 = **`if v > 1e8:`**（`:2430`）＝ **「一度も代入されなかった累算器（1e9 の種）」**
⇒ ⭐ **2 つは同じ「不在」ではない**: 前者は **測定値の不在**、後者は **累算器の未更新**。⇒ ⛔ **私は英語の "absence" 1 語で 2 つの別物を同一視した** — **同じ語が 2 つの object を指す**という、今夜ずっと扱ってきた形の、**語の層での再発**。

✅ **訂正後の観測（本節が正）**: **file には不在の綴りが 2 種あり、それぞれ自分の idiom を持つ**（読みの不在 = `gap_mm`／累算器の未更新 = 番兵の言い換え）。⇒ **新しい print は「2 つ目の私設の綴り」ではなく、自分の種の idiom に従っている**。
⚠ **残る観測（これは保つ）**: `gap_mm` の docstring が述べる歴史（「individual guards were not working」）は **guard の層**についての教訓であり、**今夜 guard を 1 本ずつ足していった経緯には依然当たっている**。⇒ ⭐ **私の「族が直し方の層で再発した」という指摘は保つ／「私設の綴りを足した」という指摘は撤回する**。
⇒ ⭐ **統合すべきものが在るとすれば、それは綴りではなく累算器の持ち方**（種を置くか、`None` で始めるか）— ⛔ **設計判断は実装者の court**で、私は指摘しない。
⚠ **等級**: kind 区別は他卓（誘発）。⛔ 私は **2 つの formatter を読んだうえで**、入力の種類を確かめずに「同じ綴りの重複」と書いた。

## 17. 追記 2026-08-09 17:1x — ⛔ **私の第 3 節は「導出」を検めて「拘束」を検めていなかった**（report が引用する前に直す）

§15 で「半径 N は導出値だから値と導出の両方を書け」と定めた。⛔ **足りない** — **その半径を、印字された値を生んだ query が本当に使っているか**を検めていなかった。

**実測（HEAD・両方とも自分で読んだ・pZ の指摘を relay していない）**:
| query | 宣言された半径を使うか | 根拠 |
|---|---|---|
| `furniture_gap`（`:400`） | ✅ **使う** | body に `cut = ARM_DECIDE_CUTOFF if cutoff is None else cutoff`（body 内 `ARM_DECIDE_CUTOFF` 出現 **1**） |
| `column_gap`（`:1277`） | ⛔ **使わない** | 事前 filter は **`if cutoff is not None and …`** の下だけ（`:1294-1297`）で、記録器は cutoff を渡さない ⇒ **filter を通らない**。距離は `mj_geomDistance(m, dd, g, c, **1.0**, None)` ＝ **1.0 m まで探す**。body 内 `ARM_DECIDE_CUTOFF` 出現 **0**（対照: `furniture_gap` の body は **1** ⇒ query は生きている） |
| `arm_pair_min`（`:1822`） | ✅ 使う（自分の 176） | ⇒ **arm の数は影響を受けない** |
⇒ ⛔ **env の 1 行は「16 mm の search radius」を宣言しながら、2 つの query から引いた値を載せている** ⇒ **`column_gap` 由来の +80.1 が「16 mm 半径」の隣に印字されうる**（それ自体で矛盾）／⇒ **`column_gap` は None を返さないので、その対については不在の文が到達しない**。

✅ **第 3 節 完成形（本節が正）**: 「absent は clear ではない・**半径は値と導出を併記**」に加えて、⭐ **「引用する半径は、その数を生んだ query が実際に使った半径でなければならない。1 つの印字値が複数の query から来ているなら、単一の宣言半径は正しくありえない」**。
⇒ ⭐ **正しい導出を、間違った query に貼ると、行番号と同じ腐り方をする** — **私は導出を検めて、拘束を検めなかった**。
⚠ **等級**: 指摘は pZ（誘発）。⛔ ただし **両半分とも自分で読んで再現した**（relay していない）。⛔ 印字の直し方は実装者の court・私は述語と label だけ出す。

## 18. 追記 2026-08-09 17:2x — **私が §9-1 で引いた診断値の 1 つに天井が付いた**（引用した場所へ限界を戻す）

§9-1 で診断 4 値を **relayed（未自験）** と明記して引いた。⇒ **そのうち 1 値に限界が付いたので、引用した場所へ戻す**。
**限界（他卓の測定・私は producer を走らせていない）**: 診断の producer（KINONLY 計器）の距離検索は **0.5 m で飽和し、飽和時は cutoff を距離として返す**（pair 名つきで）。⇒ **自分で計算した比率**: `arm↔arm qpos=0` **491.3 mm = 天井の 98.3%・余裕 8.7 mm**／`arm↔arm HOME` 194.2 = 38.8%／`arm↔table` 19.8 = 4.0%／`arm↔col/furn` 80.1 = 16.0%。
⇒ ⚠ **4 値とも天井の内側で、閉じた report は不触**。⛔ ただし **+491.3 を「非常に広い」と読むと、実際は飽和の 8.7 mm 手前**。⇒ **引用時は producer 名と天井を併記する**。

⭐ **§9-2 の論は弱まらず、むしろ強まる**: 私の主張は「**薄い側は arm↔table の +19.8 であって arm↔arm の +491.3 ではない**」。⇒ **その +491.3 が天井の 98.3% なら、margin としてはなおさら読めない** ⇒ **薄い側で評価せよ、という結論は補強される**。
⚠ **私の引用規則（§15/§17）の射程は変わらない** — あれは **wired 記録器の数**に付く規則で、**診断値の producer は別**（他卓が明示的に scope を切った）。⇒ ⛔ **診断値に記録器の caveat を貼らない**（要らない所に貼るのも誤り）。
⚠ **等級**: 天井の指摘は他卓（誘発・私は producer を走らせていない）。比率の計算と「§9-2 が強まる」の接続は私。

## 19. 追記 2026-08-09 18:0x — **Rs1 / Rs2 の呼び分けを採用**（本節が私の面での初出）＋ **過去の私の「Rs」を全数照合した**

**採用する呼び分け（初出展開・両 token）**: **Rs1（人間）** / **Rs2（=p4/CC・RS-TECH-LEAD）**。
⭐ **代理条項も一緒に採る**: **Rs2 は Rs1 の代理**であり、この分割は **「誰が言ったか」を分けるだけで「誰の権限か」を分けない**。⇒ ⛔ **権限の分離と読み替えない**（委任の外は従来どおり Rs1 へ上げる）。
⚠ **遡及しない**: 既存の「Rs」表記は文脈で人間に解決する。⇒ **書き換えはしない**。⇒ ⭐ **ただし「文脈で解決する」と言い切る前に、私は自分の面を数えた**（下記）。

**照合（自分の 6 artifact・token `Rs` を含む行 全数）**: spec **22** / addendum **12** / 所管回答 **4** / BD 草案 **14** / 本書 **6** / §14.27 ③ **6** = **64 行**。**全部読んだ**。
⇒ ✅ **64 行すべてが Rs1（人間）を指す** — 裁定「前提を変えて良い」／裁定①「腕を姿勢へ書き込むことは不可」／DECISION B2／2026-07-15 の pin 逐語／2026-07-27 の機種名訂正／「(b)(d) の草案を書いて」「残りを片付けて」「突合③を進めて」／run 認可／04-Specs は Rs の court／verdict source R = Rs1 の動画。
⇒ ⛔ **p4/CC（Rs2）の言葉を「Rs」と書いた行は 0**。私は p4 の言葉には一貫して「p4」「owner」「他卓」を使っていた。
⚠ **限界を先に**: これは **token `Rs` を含む行**という母集団の照合であって、**token を伴わない帰属（「裁定」単独など）は見ていない**。⇒ ⭐ ただし本裁定の危険は **token の曖昧さ**そのものなので、**この母集団がこの規則に対する正しい母集団**。

⇒ **以後の私の書き方**: 各 artifact の初出で **Rs1（人間）/ Rs2（=p4/CC）** を展開する。⛔ **digit を落とさない**（`Rs2` も `Rs*` token なので、落とすと元の混同に戻る）。
⚠ **等級**: 裁定は Rs1・回付は他卓（誘発）。⭐ **「採用」を「次の artifact から使う」で終わらせず、過去 64 行を数えて照合した**のは私の判断。
