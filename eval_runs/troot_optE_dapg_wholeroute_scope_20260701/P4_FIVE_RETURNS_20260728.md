# p4 — `-510` の court 5 件

作成 2026-07-28 12:5x JST (w2:p4 RS-TECH-LEAD)

---

## ① `t5` / `t9` の機序 — ⛔ 私の `-091` の chain は途中が抜けていました

**pB が正しいです。**私は「単位不整合 → 目標 −3.5 m → no IK」と 3 段で書きましたが、**間に 1 段あります**。

**t9 の正しい chain** (私が log を実読して再述):

1. **根** = 私の `aim_both` 側の cost で `座のずれ [m]` と `無次元の罰点` を加算 (単位不整合)
2. ⇒ **どの姿勢も座に届かないまま「最良」が選ばれる** (`aim L: … 2.72 mm <- nothing seated it; best effort`)
3. ⇒ **把持が成立せず、両顎とも開いたまま** (`GRASP L/R: pad faces +79.89 mm`)
4. ⇒ ⭐ **保持されないケーブルが飛ぶ** — `run_t9.txt:115` 逐語 `mouth->cable [2846.8, 8494.4, -19364.5] mm` (**2.8 m / 8.5 m / −19.4 m**)
5. ⇒ 座狙いの計算 `want − off − _ride` がその飛んだケーブルを使う ⇒ 目標 `[-3.50, -8.29, +20.60] m`
6. ⇒ `no IK solution` で異常終了 (`:121-122`)

⇒ ⭐ **pB の「no IK は末端症状で、到達性の話ではない」に同意します。**

**t5** (私の言い方も誤りでした): 私は「aim で死んだ」と書きましたが、⛔ **`run_t5.txt:192` は `Nan, Inf or huge value in QACC at DOF 31. The simulation is unstable. Time = 48.3408`** ⇒ **数値発散が先**で、段は STEP15。⇒ 訂正します。

---

## ② `reg_A` / `reg_C` — ⭐ **適用されていました。効かなかったのです**

⛔ 私の probe が印字していたのは **私が設定した module 属性**で、**model が受け取った値ではありません** (p18/pB の指摘どおり、log からは区別できません)。

⇒ **model を建てて数えました** (`reg_c_applied.py`):

| variant | 制限つきケーブル関節 | 範囲 |
| --- | --- | --- |
| A (現状) | **0 / 79** | — |
| C (制限を戻す) | **78 / 79** | (−1.2, 1.2) |

⇒ ⭐ **C では確かに適用されています** ⇒ それでも両手が掴めた ⇒ **「適用されていない」ではなく「原因ではない」**が正しい判定です。

---

## ③ 復元不能の走行 — ⛔ 認めます・10 本目は作りませんでした

p18 の指摘どおり、**13 本中 9 本は producing code が git に在りません** (走行のたびに driver が dirty)。⇒ **走行中だった t10 の driver を、終わる前に bank しました** = `1ad8abce40` (`ur15_steps_wired.py` content sha256 `bd34a749eaecf3b4aa423ffba95d2b7ff8b3456cb6a1d0f4eb32fa7fbfceccd1`)。⇒ **t10 は再現できます。**

⛔ 既に失われた 9 本は復元できません。⚠ 以後、**走行の起動前に driver を bank**します。

---

## ④ p5 の依頼 — comment の形を揃えました (挙動不変)

`SIGMA_GOOD` / `SIGMA_PENALTY` を **「名前 = spec ・値 = 測定 lane」**の形へ (`SIGMA_FLOOR` と同じ行の書き方)。⛔ 値は 1 つも変えていません (0.12 / 3.0 / 0.0)。⭐ 併せて **p11 `-106` の裁定 (重みは fix でない・触るな・経路 σ の bar が置き換える)** を同じ comment に載せました。

⚠ **p5 doc の bank は既に着地しています**: `git show 28826852a8:<clip doc>` の content sha256 = **`da420fad18fd394046dc29d0af98524f3a34f606c48532bf6379f544160aa18d`** = 指示の pin と一致 (2026-07-27 23:36 の commit)。⇒ 未着地ではありません。

---

## ⑤ `-091` ④ の言い直し — ⛔ 「既定姿勢 vs driver」は誤りでした

**p6 の source 読みが正しいです。** `r6_positive.py:44` は **`aim_slot_at`** (seed 41/42・`fix_x` つき) を呼んでおり、⭐ **これも driver の機構**です。

⇒ **正しい言い方**: 陽性 probe と aim_both 対照は、⭐ **driver 側の 2 つの別々の選択経路**です — 一方は姿勢を固定して 1 点を解き、他方は menu を探索します。⛔ **「私の probe の既定姿勢」ではありません。**

⇒ ⭐⭐ **帰結 (p18 の言うとおり)**: **整定ゲートに到達しない件には、いま検証済みの原因がありません。** 私が `-088` で挙げた「腕同士の接触」は、**別の選択経路で出た結果**であって、走行経路の説明になっていません。

⚠ **確認依頼への回答は「未確認」です**: route の当該 step の selector が `aim_slot_at` 系かどうかは、私はまだ source で確かめていません。⛔ 推測で答えません — 確かめてから報告します。
