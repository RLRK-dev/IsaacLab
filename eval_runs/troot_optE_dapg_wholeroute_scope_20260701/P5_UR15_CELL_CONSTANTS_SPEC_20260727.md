# P5 仕様 — UR15 cell 定数の単一出所 (SSOT)

**Owner** = w2:p5 SKILL-DETAIL-DESIGN ・**0-commit** (bank = w2:p4)
**起動** = Rs ・依頼 = w2:p18 `-201` (クリップ設計 `-195` の scope に追加) ・**根拠** = DDR #46 + w2:p6 実測
**兄弟文書** = `P5_UR15_CLIP_DETAIL_DESIGN_20260727.md` (sha256 `32360b20…`) — クリップの形はそちらが正
**作成** 2026-07-27 ・⛔ 本書の数はすべて私が on-disk を実読・機械抽出して出しました

---

## §0 規則 (これだけ)

> ⭐ **driver は定数を定義しない。1 つの module から読む。**
> ⭐ その module は、**権威ある出所が在る量は そこから取り**、**無い量だけ 本 spec の値を持つ**。

---

## §1 測定 — 「そのクリップ」だけでなく「その cell」が 1 個ではありませんでした

**方法**: `p4_ur15_sim_20260727/*.py` **12 file** を AST で解析し、module 直下の代入を全部抽出して突き合わせ (⭐ grep でなく構文木 ⇒ 取りこぼしなし)。

⇒ ⛔ **値が食い違う定数 = 21 件。** うち **物理を変えるもの = 10 件**:

| 定数 | `ur15_cell` / `ur15_route` | `ur15_steps*` 3 本 | 差の意味 |
| --- | --- | --- | --- |
| **`CABLE_R`** | **0.005** | **0.004** | ⛔⛔ **Ø10 対 Ø8** — 別のケーブル |
| **`CABLE_N`** | **40** | **32** | リンク数 |
| **`CLIP_H`** | **0.070** | **0.026** | 壁の高さ (w2:p6 発見) |
| `TABLE_HX` | 0.95 | 0.70 | 台の長さ |
| `TABLE_HY` | 0.30 | 0.20 | 台の幅 |
| **`YOKE_SPREAD`** | **0.22** | **0.40** | ⚠ 本日 私が 118 mm と誤った量 |
| **`TILT`** | **45°** | **20°** | ヨーク角 |
| `CLAMP` | — | 255 / **236** (`c1seat` のみ) | 閉じ指令 |
| `OPEN` | — | 0 / **18** | 開き指令 |
| `HALF` | — | 170 / **214** | 半保持 |

⭐ 残り 11 件は run 固有で正当な差 (`OUT` `W` `H` `HOLD_S` `WAY` `ARMATURE` ほか導出式) ⇒ §5。

⚠ **形そのものも違います** (定義の有無): `CLIP_RISER` `REST_TOP` `REST_Y` `Z_SEAT` `CLIP_Y_ODD` `GRIP_HALF_SPAN` は **`ur15_steps` 系にしか存在しません** ⇒ `ur15_cell` / `ur15_route` の cell には rest 台も riser も無く、⭐ **同じ名前の cell が 2 種類**あります。

---

## §2 ⛔⛔ 最重要 — **「正しい方の script」は存在しません**

| 定数 | 権威ある値 | `cell`/`route` | `steps*` |
| --- | --- | --- | --- |
| `CABLE_R` | **0.004** (`task_config.py:137`) | ⛔ 0.005 | ✅ 0.004 |
| `CABLE_N` | **40** (`task_config.py:135`) | ✅ 40 | ⛔ 32 |

⇒ ⭐⭐ **一方は半径で外し、他方はリンク数で外しています。** ⇒ ⛔ **どちらかを選んで写す、という直し方は成立しません。** ⇒ ⭐ **これが「1 つの出所から読む」以外に手が無い理由です** (本 spec の存在理由)。

⭐ **3 件目**: `task_config.py:135` 逐語 `CABLE_SEGMENTS = 40  # 40 segments × 15mm = 600mm` ⇒ **1 節 = 15 mm**。⛔ **p4 の全 driver は `CABLE_SEG = 0.030` (30 mm)。**
⇒ ⭐⭐ **帰結が本日の計器問題に直結します**: 最寄りリンク中心への吸着の量子化は **節長の半分** ⇒ 30 mm なら **±15 mm** ・**SSOT の 15 mm なら ±7.5 mm** ⇒ ⭐ **定数を SSOT に戻すだけで、計器の床が半分になります。**
⚠ ただしケーブル全長も変わります (40×15 = 600 mm 対 32×30 = 960 mm) ⇒ ⛔ **節長だけ直すのは不可**。cell の寸法と一緒に決める量です。

---

## §3 Tier A — 権威ある出所が在るもの ⇒ ⛔ **値を書かず、読む**

| 定数 | 権威ある出所 (逐語) | 値 | 現況 |
| --- | --- | --- | --- |
| `TABLE_TOP` | `task_config.py:20 TABLE_HEIGHT` | 0.80 | ✅ 全 driver 一致 |
| `CABLE_R` | `task_config.py:137 CABLE_RADIUS` | **0.004** | ⛔ `cell`/`route` が 0.005 |
| `CABLE_N` | ⭐ **導出量** = 必要な全長 ÷ `CABLE_SEG_LEN` (§9 裁定①) | **64** (960 mm の cell の場合) | ⛔ 現 32 ・⚠ `task_config.py:135` の 40 は **env の cell の全長**であって定数ではありません |
| `CABLE_SEG` | **`task_config.py:136 CABLE_SEG_LEN`** (⭐ 実定数 ・import 可) | **0.015** | ⛔ 全 driver が 0.030 |
| `GRIP_HALF_SPAN` | `task_config.py:235` | **0.044** | ✅ (`steps*` のみ定義) |
| クリップ 5 箱 | `newton_skill_env_base.py:1858-1864` | 兄弟文書 §3 の回転形 | ⛔ 全 driver が自作 |
| クリップ接触 | `:1918-1921` + `task_config.py:187` | `solref="-40000 -400"` `friction="1.0 0.005 0.005"` | ⛔ 未指定 (MuJoCo 既定) |
| クリップ衝突 | `newton_skill_env_base.py:1908` `:1925` | **ON** (`0x6`) | 兄弟文書 §6-A |
| **未浮上の着座** | `task_config.py:226 GROOVE_CENTER_Z` | `TABLE + 0.009` | ⛔ p4 は `+0.070` を定数保持 |
| **浮上量** | `newton_skill_env_base.py:1897` `:1923` (`target_clip_float_z`) | 未確定 (兄弟文書 §4) | ⛔ p4 は `CLIP_RISER` で別に持つ |

⚠⚠ **着座の 2 行は分けなければなりません (w2:p0 指摘 ・私の誤りでした)**: `GROOVE_CENTER_Z` の定義に **浮上項は入っていません** (`task_config.py` 全体で `float_z` の閉じた query = **0 件**)。浮上はクリップの**設置位置**側に掛かります (`newton_skill_env_base.py:1897` 逐語 `TABLE_HEIGHT + dz + target_clip_float_z`)。
⇒ ⭐ **浮上した後の着座 = `GROOVE_CENTER_Z + target_clip_float_z`** ・⛔ **`GROOVE_CENTER_Z` 単独はもう着座ではありません。**
⇒ ⭐⭐ **これが罠です**: 定数の**名前は「座」のまま**で、クリップだけ動きます。⇒ 浮かせた cell で `GROOVE_CENTER_Z` を挿入目標に使うコードは **黙って外します**。
| グリッパ幾何 | `_ur15_2f85_koshape_actuated.xml` | 資産が正 | ✅ 全 driver が同一絶対パス |

⛔ **Tier A を driver 内に literal で書くことを禁じます。** ⭐ 理由 = 本日 4 回、同じ量の複数コピーが判断を狂わせました (`YOKE_SPREAD` / 帯 8.00 対 10.00 / 旧 XML copy / `CLIP_H`)。

---

## §4 Tier B — cell 固有で権威ある出所が無いもの ⇒ ⭐ **本 spec が持つ**

| 定数 | 採る値 | 根拠 (⛔ 私が決めたのではなく、on-disk の実測記録) |
| --- | --- | --- |
| `YOKE_SPREAD` | **0.40** | `ur15_steps.py:37` 逐語 "measured: 0.22/45deg made the two arms interleave at an 88 mm span; 0.40/20deg clears the rest row and both clips" |
| `TILT` | **20°** | 同上 (対で測られている ⇒ 分離不可) |
| `TABLE_HX/HY` | **0.70 / 0.20** | `steps*` = rest 台とクリップ列を含む後発の cell。⚠ 弱い根拠 (記録コメント無し) ⇒ §7 |
| `SHOULDER_HEIGHT` | `0.37 + 0.58*2` | 全 5 file 一致 ⇒ 争いなし |
| `REST_Y` `REST_X` | `steps*` の値 | `:53` 逐語 "saddles kept clear of both 88 mm grasp spans" (⭐ 制約は在るが値の導出は無い) |
| **`REST_TOP`** | `steps*` の値 (+0.150) | ⛔⛔ **根拠なし** — 引用コメント `:66` は "at **+0.060** the open fingers press into the table" ⇒ ⭐ **これは「60 より上」という下限であって、150 の導出ではありません** (w2:p0 指摘 ・`-240`(2)③ ・私の「弱い」も過小でした) |
| `float_z` (旧 `CLIP_RISER`) | ⛔ **未確定** | 兄弟文書 §4 = p4 の 1 実測待ち |
| `KP_ARM` `KP_WRI` `KVR` `ARMATURE` `DAMP` | `steps*` の値 | ⛔ **arm-control の court** — 本 spec は名前と出所だけ持ち、値の可否を判定しません |
| `CLAMP` `OPEN` `HALF` | ⛔ **凍結しません** | ⭐ 測定 lane が現に動かしている量 (255/236 ・0/18 ・170/214) ⇒ **spec は「測定 lane の最新 banked 値を読む」と定めるだけ** |

---

## §5 Tier C — run 固有 ⇒ ⛔ **spec に入れない**

`OUT` (出力 path) ・`W` `H` (解像度) ・`HOLD_S` ・`WAY` / `STEPS` (振り付け) ・カメラ ・seed。
⇒ ⭐ **これらが script ごとに違うのは正常**です。単一化の対象に含めると、逆に振り付けの実験ができなくなります。

---

## §6 実装契約 (⭐ 作るのは w2:p4 ・本書は形だけ定めます)

1. driver dir に **module 1 個** (例 `ur15_cell_spec.py`)。全 driver は先頭で **そこからのみ** cell 定数を取る。
2. **Tier A** = 可能なら `thread_isaac_lab.configs.task_config` を import。⚠ import が重い/不可なら **生成した写し + 照合 guard**。
   ⛔ **写すだけで guard 無しは不可** (それが今の状態です)。
   ⛔⛔ **guard は行の sha で作らないでください** (⭐ w2:p0 指摘 ・私の初版が誤り): 行 sha が答えるのは「**バイトが動いたか**」だけで、⛔ コメント修正で**偽警報**を出し、⛔ 別の場所での再代入を**素通し**します。⇒ ⭐ **§6.4 の AST pass で SSOT 側の値を評価し、値どうしを比べる。** ⇒ byte-sha は **通知**へ降格 (失敗させない)。
3. **Tier B** = 本 spec の値を module に直書きし、各行に **本 spec の節番号**を付す。
4. **guard** (安い ・run 不要): ⭐ **名前集合は 1 つでなく 3 つ**。driver の module 直下の大文字代入を AST で数え上げ、**どれにも属さない名前が在れば失敗**する。

| 集合 | 中身 | guard の判定 |
| --- | --- | --- |
| **OWNED** | module が供給する量 (Tier A + Tier B) | ⛔ driver が **再定義**したら失敗 |
| **RETIRED** | ⭐ **退役した構成の定数** — `CLIP_H` `GROOVE_W` `CLIP_RISER` ほか p4 の自作クリップ由来 | ⛔ driver が **定義していたら**失敗 (値は問わない) |
| **TIER-C** | run 固有 (§5) | ✅ 自由 |
| *(上記のどれでもない)* | 新しく発明された定数 | ⛔ **失敗** ・spec への追加を促す (§6.5) |

⛔⛔ **なぜ 3 つ必要か (w2:p0 指摘 ・`-240`(1))**: module は物理 10 のうち **9 を所有し、`CLIP_H` を所有しません** (⭐ 私の設計でクリップが権威ある 5 箱に替わり、`CLIP_H` は**退役した形の定数**になったため)。
⇒ ⛔ **所有だけを見る guard は、driver が `CLIP_H = 0.026` を持ち続けても通します** ⇒ ⭐⭐ **発端の定数 (w2:p6 の発見) だけが射程外**になります。
⇒ ⭐ **一般形**: **所有の検査は「私の値を書き換えたか」しか答えられず、「捨てるべきものを持ち続けたか」を原理的に見られません。** ⇒ 退役集合が要ります。
⇒ ⭐⭐ そして **「どれにも属さない ⇒ 失敗」** が本契約の fail-closed 部分です — これが在れば **明日 発明される定数**も捕まります (列挙では追いつきません)。

#### 6.4a ⭐⭐ 適用範囲の決定 (w2:p0 `-248` の 1 決定依頼への回答)

**⛔ p0 の 3 択はどれも採りません。** 実測 (1 driver): 全 module-level 105 (漏れ 0) / UPPER 58 (漏れ 0) / UPPER∧literal 13 (**漏れ 10**)。

- ⛔ **UPPER∧literal は却下** — 漏れ 10 が `Z_SEAT` `CLIP_C1` `CLIP_C2` `CABLE_Z0` = ⭐ **式で書かれた cell 幾何**、つまり **guard が捕まえるべきもの そのもの**です。
- ⛔ **UPPER 限定も採りません** — 漏れ 0 は **今日の 1 driver での実測**であり、⭐ 「cell 定数は大文字」は**規約であって機構ではありません**。⇒ 明日 `clip_h = 0.026` と小文字で書かれたら素通しします。⇒ 私が終日 指摘してきた「想定しなかった場合を見られない述語」と同型です。

> ⭐⭐ **決定: 適用範囲 = module 直下の 全束縛 (105)。⭐ ただし分類は列挙でなく 1 つの規則で行う。**
>
> **判定規則** = 値の式に **所有名から導けない数値リテラル**が在るか。
> - **在る** ⇒ その束縛は **cell を定義している** ⇒ OWNED / RETIRED / TIER-C のいずれかに属さねばならない。⛔ どれでもなければ **失敗**。
> - **無い** (葉が 所有名 ・model/data オブジェクト ・整数添字 のみ) ⇒ ⭐ **DERIVED** (第 4 種) ⇒ **自由** ・spec に宣言させません。

⭐ **これで p0 の第 4 種 (計算束縛 45 = `CABG` `CLAWG` `GIDX` 等 model からの handle) は 規則で自動的に DERIVED に落ちます** — 名前を列挙しません。
⭐ そして `Z_SEAT = TABLE_TOP + CLIP_RISER + 0.030 - CLAW_OFFSET` は **リテラル 0.030 を持つ**ので捕まります (漏れ 10 のうちの 1 つ)。

⚠ **費用について正直に**: ⛔ 105 名を人が貼るのではありません — ⭐ **規則が大半を DERIVED に落とすので、人の判断が要るのは「リテラルを持つ束縛」だけ** (実測 13 + 漏れ 10 = **23 前後**と私は見ます)。⚠ ⛔ **この 23 は私の見積りで、測定ではありません** (p0 は中間案の費用を未測と明記) ⇒ 実装時に実数が出たら本節を差し替えてください。

#### 6.4b ⭐⭐ import 束縛の分類 (w2:p0 `-271` の 1 行決定依頼への回答)

⛔ **問題**: `from ur15_cell_spec import CABLE_R` は module 直下に `CABLE_R` を束縛するので、⛔ **素直な読み方が「再定義」に見えます** ⇒ spec に忠実な最初の driver が自分の guard に落ちます。

> ⭐⭐ **決定 = ①。ただし「guard の例外」ではなく 分類の完成として書きます。**
>
> ⭐ **`ImportFrom` で module が spec の場合、束縛される名前は 構成上 単一出所そのもの** ⇒ 新しい類 **SOURCED**。
> ⇒ ⭐ **SOURCED は所有の違反ではなく、所有を満たしている状態**です。
> ⛔ **spec 以外の module から** OWNED / RETIRED 名を `ImportFrom` したら ⇒ **失敗** (第 2 の出所ゆえ)。

⭐ **なぜ ① で漏れないか (私が形を数え上げました)**:

| 書き方 | 判定 | 理由 |
| --- | --- | --- |
| `from ur15_cell_spec import CABLE_R` | ✅ **SOURCED** | 構成上 出所は spec ・⭐ リテラルを運べません |
| `import ur15_cell_spec as s` ＋ `CABLE_R = s.CABLE_R` | ✅ **DERIVED** | `Assign` だが式にリテラルが無い (§6.4a の規則でそのまま通ります) |
| `from spec import CABLE_R as X` ＋ `CABLE_R = 0.005` | ⛔ **失敗** | 2 文目が リテラル付き `Assign` |
| `from other_module import CABLE_R` | ⛔ **失敗** | module が spec でない = 第 2 の出所 |

⇒ ⭐⭐ **これで guard は「書き換えたか」を否定的に見るだけでなく、「出所から取ったか」を肯定的に確認できます。**

⛔ **②(属性参照 `spec.CABLE_R` を義務化) は採りません**: 分類の穴に対して **全使用箇所を変える**代償が釣り合わず、⛔ ②自身にも穴が在ります (`X = spec.CABLE_R` の後で `X = 0.005` を防げません)。

⚠ **本決定で閉じる穴と、残る穴**:
- ⛔ **`from ur15_cell_spec import *` は禁止** — AST が束縛名を数え上げられないので **guard を失敗**させてください (安く閉じられる実在の穴)。
- ⚠ **関数内の遮蔽は射程外** — 私が範囲を module 直下に決めたためです (§6.4a)。⛔ 拡張は今しません ・限界として記録します。

#### 6.4c ⛔⛔ 反実仮想 A を **閉じます** (w2:p0 `-300`(2) の 1 決定依頼への回答)

⛔ **現況**: 述語は代入の **右辺しか見ません** ⇒ 反実仮想 **A** (文レベルの `if` で包む ・右辺は `0x6` のまま) が **True で素通り**。B/C/D (値変更・三項 gate) は発火します。

> ⭐⭐ **決定: A を閉じる。** guard は代入の **先祖鎖を走査**し、`If` / `IfExp` の中に在れば **失敗**させてください。

⛔ **「露出は狭い」を受容しない理由**:
- ⭐ 見逃す形 = **条件つきの衝突設定**。⇒ ⭐⭐ **それは この repo に実在する形そのもの**です (`test_newton_clip_routing.py:1168` `:1194` の `_clip_collide`)。⛔ 実在するのが三項で、見逃すのが文 `if` = **意味は同じ双子**です。
- ⇒ ⭐⭐⭐ **この guard が存在する理由は「env-base は無条件・producer は条件つき」を区別すること**です (§3 Tier A / 兄弟文書 §6-A) ⇒ ⛔ **条件つきを見られない guard は、自分が建てられた主張を検証できません。**
- ⭐ **一般形**: **「露出が狭い」は 可能な形の空間に対してでなく、その codebase が実際に使う形に対して測る。** ⇒ 本件で見逃す形は、⭐ **実在する形の意味的双子**です。

⚠ **偽陽性の扱い**: 正当な `if` (環境分岐等) も落ちます ⇒ ⭐ **それで良い**とします。直しは **1 行の巻き上げ (hoist)** で済み、⛔ fail-closed を緩めるより安いためです。

#### 6.4d ⭐ 49 名の配置 (私が driver の**式**を読んで分類 ・名前だけでは決まりません)

⛔ **`unclassified_49.txt` は名前と行番号だけ**なので、⭐ 私は `ur15_steps_c1seat.py` から **値の式**を抽出して分類しました (`LX1 = C1[0] − GRIP_HALF_SPAN` は DERIVED ・`LX1 = 0.106` なら OWNED — 式を見ないと決まりません)。

**OWNED (cell を定義するリテラルを持つ ⇒ spec へ移す)**
`CLIP_Y_ODD = 0.35` / `CLIP_Y_EVEN = 0.4` / `C1 = (0.15, …)` / `C2 = (0.04, …)` (⇒ Tier A `CLIP_POSITIONS`) ・`Z_HOME = TABLE_TOP + 0.2` / `Z_RISE_REST = +0.23` / `Z_RISE_ROUTE = +0.18` (⭐ **加算**リテラル = 単位つき) ・`SETTLE_TOL 0.002` / `FINGER_RAMP 0.75` / `RAMP 4000` / `SIGMA_FLOOR 0.0` (Tier B ・値は arm-control) ・`CLAMP 236` / `HALF 214` / `OPEN 18` (⭐ 名前のみ所有 ・値は測定 lane) ・`GRASP_ATTITUDES` (roll 表) ・`SIDES` `R_DES` (⭐ 符号・姿勢の**規約**だが cell の事実ゆえ OWNED)

⛔⛔ **2 件は「書写」でした (⭐ 単一出所の実例)**
- `CLAW_OFFSET = 0.27574726696 − 0.2548428289592266` ⇒ ⭐ **`task_config.py:320/:321` の 2 定数の差** ⇒ **Tier A で import すべき**もの。
- `EFFORT` / `LIMS` ⇒ ⭐ **ロボットの仕様** (トルク上限・関節範囲) がリテラル配列で写されています ⇒ **Tier A (URDF / robot spec 由来)**。

**DERIVED (自由なリテラルを持たない ⇒ 自由)**
`LX1` `LX2` `RX1` `RX2` (所有名の加減) ・`GL` `GR` `GRASP1` ・`gL` `gR` `iL` `iR` `SEAT1` `SEAT2` (model 由来) ・`cam` `cam2` ・`render_every`

⭐⭐ **群 (c) の判断 = 規則を 1 段 精密化して受理します**
`RX_MID = 0.5 * (C1[0] + C2[0])` ・`Y_GRASP_REST = 0.5 * (gL[1] + gR[1])` ・`kps = [KP_ARM]*3 + [KP_WRI]*3`
> ⭐ **リテラルが 乗算・除算・繰返し数・添字として現れ、他の葉が OWNED/DERIVED なら、そのリテラルは 無次元** ⇒ **DERIVED。**
> ⛔ **加算・減算で現れるリテラルは 同じ単位を持つ** ⇒ **cell 定義。** (⇒ `Z_HOME = TABLE_TOP + 0.2` は捕まり、`0.5 * (…)` は通ります。)
⚠ **残る穴 1 つ**: `X = OWNED * 1.0375` のように **比で長さを忍ばせる**形は通ります (⛔ 実在しませんが、規則の限界として記録)。

**TIER-C (run 固有 ⇒ 自由)** `frames` `log` `n` ・`claw_min` `col_min` `sig_min` (⭐ 1e9 は **番兵**で入力ではありません)

#### 6.4e ⛔⛔ 私の規則が **見られない場所**が在りました — `world` (最重要)

`world = f'<mujoco model="ur15_steps">…'` ⇒ ⭐⭐ **cell の幾何は f-string のテンプレートに書かれています。**
⇒ ⛔⛔ **AST のリテラル検査は 文字列の中の数字を見られません** ⇒ ⭐ driver が `size="0.016 0.024 0.02"` と直書きしても **全検査を通ります。**
⇒ ⛔ **これは狭い露出ではありません — 幾何が住んでいる場所そのものです。**

> ⛔ **初版 (幾何 4 属性の列挙) を撤回し、規則を反転します** — 理由は下記。
> ⭐⭐ **テンプレートの静的部分に現れる数値リテラルは すべて 失敗。** 例外は ①**描画・見た目の属性の allow-list** (`rgba` `material` ほか) ②**`0` と `1` そのもの** (原点・単位クォータニオン)。
> ⇒ ⭐ 幾何も接触も、**`{}` 置換 = 所有名経由**でしか書けなくなります。

⛔ **なぜ列挙をやめるか (w2:p0 `-316` の拡張提案を受けて)**: p0 実測で幾何 4 属性は **59 site 中 27** しか覆わず、⛔ **`friction` / `damping` / `stiffness` / `condim` / `mass` の焼き込みが 10 site 実在**します。⭐ 本 spec は `solref` / `friction` を **Tier A で所有済み** (§3) ⇒ ⭐ **焼き込みは軸違いの同型の失敗**です (p0 の言い方に同意)。
⇒ ⛔ **ここで接触群を足しても、次に 60 番目の属性が来ます** ⇒ ⭐⭐ **列挙は「UPPER 限定」と同じ形** = どの形が重要かを規則に埋め込むこと ⇒ 私が §6.4a で採った「**分類できないものは失敗**」と不整合です。
⇒ ⭐ **反転すれば、明日 追加される属性も自動で捕まります。** ⚠ 描画群 22 site が対象外という p0 の判断は そのまま採ります (⭐ allow-list の中身 = その 22 site の属性)。

⭐ **`-316`(2) 実装補完に異議ありません**: `UnaryOp(USub, Constant)` を裸のリテラルへ畳む ⇒ `REST_X` の `−0.300` が落ちるのを防ぎます (裁定の意図どおり)。

⭐⭐ **p0 の自己評価は そのまま採りません — 2 規則は 順位ではなく 補完です。**
p0 は「乗除/加減の次元解析は自分の経験的括りより原理的」とされましたが、⛔ **私の次元規則には穴が在り** (§6.4d の `X = OWNED * 1.0375`)、⭐ **p0 の経験的括り (素の係数の小集合) はその穴を捕まえます** (`1.0375` は素の係数でない)。
⇒ ⭐⭐ **結合形を採ります**: **乗除に現れるリテラルが通るのは、それが素の係数の小集合 (`0.5` `2` `3` `1000` 等) に在るときだけ。** それ以外の乗数は cell 定義として扱う。⇒ ⛔ どちらか一方では穴が残ります。

#### 6.4f ⛔⛔ **guard の一覧は「床」であって「所有すべきものの定義」ではありません**

⚠ `-308` により 49 名の**配置**は保留 (26 件は guard 実装が §6.4a の裁定と不一致なための産物 ⇒ 縮小後 ~23)。⭐ 保留は受けます。
⇒ ⛔ ただし **縮小後の一覧に対して 1 つ注意が要ります**: それは **「guard が捕まえたもの」**であって、**「spec が所有すべきもの」ではありません。**

⛔ **guard に永久に載らない cell の事実が 2 類 在ります**:
1. ⭐ **無次元リテラルの規約** — `SIDES = {'L': -1.0, 'R': +1.0}` ・`R_DES` (0.0/±1.0 の回転行列)。⇒ 添字整数を除外すれば **guard は通します**が、⭐ **どちらも cell の事実**です (左右の符号・基準姿勢) ⇒ **spec が所有すべき。**
2. ⛔⛔ **場面テンプレートの中身** — `world` の f-string (§6.4e)。⇒ ⭐ **どんな一覧にも現れません** (AST が文字列内の数字を見ないため)。

⇒ ⭐⭐ **ゆえに: guard が挙げた分だけを配置すると、cell の事実が単一出所の外に残り、⛔ guard は文句を言いません (言えません)。**
⇒ ⭐ **配置は 2 経路で行う**: ①guard の一覧 (機械) ②本 spec の §6.4d 分類 (判断) ⇒ ⛔ ①だけでは足りません。

⚠ **併せて記録 (`-308` の含意)**: 私の §6.4a 裁定は **bank されたが 効いていませんでした** (除外分岐が dead code) ⇒ ⭐⭐ **49 名の一覧は driver についての証拠ではなく、guard についての証拠でした。** ⇒ 「書いてある ≠ 効いている」の実例が、私の裁定自身に起きました。

#### 6.4g ⭐⭐ ケーブル自身の物理 — **6 属性すべてに Tier A の出所が在ります** (`-323`(2) への回答 ・私が実読)

| テンプレートで焼き込まれている属性 | 権威ある出所 (`task_config.py`) | 値 |
| --- | --- | --- |
| `mass` | `:138` MODEL-TRUTH (ρ=1100 由来) | **1.1243 g/seg** |
| `stiffness` (曲げ) | `:144 CABLE_BEND_STIFFNESS` | 0.005 N·m² |
| `damping` (曲げ) | `:152 CABLE_BEND_DAMPING` | 0.01 |
| 軸剛性 / 減衰 | `:153 CABLE_STRETCH_STIFFNESS` / `:154 CABLE_STRETCH_DAMPING` | 1.0e6 / 0.0 |
| `condim` | `:176 MUJOCO_CONTACT_CONDIM` | 6 |
| `friction` | `:180 MUJOCO_CABLE_TABLE_FRICTION` | (1.0, 0.005, 0.005) |

⇒ ⭐⭐ **新設の Tier B 行は不要です。6 件すべて Tier A** ⇒ 置換で解決します。

⛔⛔ **そして 1 件、物理の結果を変える食い違いが出ました。**
テンプレートの逐語 = `mass="0.004" … friction="1.1 0.03 0.002" condim="6"`

| 属性 | テンプレート | 権威 | 差 |
| --- | --- | --- | --- |
| **`mass`** | **4.0 g/seg** | **1.1243 g/seg** | ⛔⛔ **3.6 倍** |
| 線密度 (換算) | **133 g/m** (32×4.0/960 mm) | **75 g/m** (40×1.1243/600 mm) | ⛔ **1.78 倍** |
| `friction` | 1.1 / 0.03 / 0.002 | 1.0 / 0.005 / 0.005 | ⛔ 3 成分とも違う |
| `condim` | 6 | 6 | ✅ 一致 |

⇒ ⭐⭐⭐ **これは §10 の垂れの裁定に直接掛かります**: 実測 127.8 mm は、⭐ **権威ある線密度の 1.78 倍 重いケーブル**で出た値です。
⇒ ⭐ 質量を SSOT に戻すと (張力支配 `L²` で線密度に比例) 垂れは **同じスパンでも約 72 mm** ・スパン短縮と併せると **約 45 mm** と私は見ます。
⚠ ⛔ **これは私の外挿で、自由変数を明示します**: ①張力が変わらないこと ②新しい垂れでも `L²` 域に留まること ③⛔ 私は当該 log を読んでいません。⇒ ⭐ **落とせる検定 = 質量を戻して 1 run 測る** (スパンを変えずに)。
⇒ ⛔ **裁定は変わりません** (600 mm は依然 有利) が、⭐ **改善幅の内訳が変わります** — 私が「スパンの効果」と書いた分の一部は **質量の効果**です。

#### 6.4g-2 ⛔⛔ 私の裁定① の副作用は **質量だけではありません — 曲げ剛性が逆向きに壊れます**

⭐ w2:p0 が「刻み半減で質量誤差が倍 (3.56×)」を出されました ⇒ ⛔ **私の裁定① の副作用で、私は予見していませんでした。**
⇒ ⭐ そこで**同じ形の量が他に無いか**を私が探しました。**在ります。**

**権威 (`task_config.py:146-147` 逐語)**: 「`CABLE_BEND_STIFFNESS = EI here` … `CABLE_MUJOCO_BEND_K = EI/CABLE_SEG_LEN`, NOT a joint stiffness; active mujoco `add_revolute_cable` joint K = **0.005/0.015 = 0.333 N·m/rad**」
**テンプレート (`:156-157` 逐語)**: `<joint … damping="0.010" stiffness="0.12"/>` ⇒ ⛔ **リテラル**。

| | 権威 K = EI/SEG | テンプレート | 比 |
| --- | --- | --- | --- |
| SEG 0.030 (現行) | **0.1667** | 0.12 | ⛔ **0.72×** (28% 柔らかすぎ) |
| **SEG 0.015 (裁定①)** | **0.3333** | 0.12 | ⛔⛔ **0.36×** (64% 柔らかすぎ) |

⇒ ⭐⭐⭐ **同じ 1 変更で 2 つの誤差が 逆向きに 2 倍ずつ動きます**:
- `mass` は **1 節あたり ∝ SEG** ⇒ 刻み半減で リテラルは **重すぎ側へ 2 倍**
- `stiffness` は **1 関節あたり ∝ 1/SEG** ⇒ 刻み半減で リテラルは **柔らかすぎ側へ 2 倍**

⇒ ⛔ **ゆえに w2:p0 の blocker は「質量の着地まで run 不可」では不足です** ⇒ ⭐ **`mass` と `stiffness` の両方**が同一変更で着地する必要が在ります。
⚠ **`damping` は未確定**: `CABLE_BEND_DAMPING = 0.01` [N·m·s] も EI と同型の量ですが、⛔ **SSOT のコメントは剛性の関係式のみを明示**しており、減衰の per-joint 形は書かれていません ⇒ ⭐ **同じ扱いが要る可能性が高いが、私は断定しません** ⇒ 決めるのは `add_revolute_cable` の実装 1 読。

⭐ **一般形 (本日の型の 1 つ)**: ⭐⭐ **離散化を変えたら、1 要素あたりの量はすべて引き直す。** ⇒ ⛔ **向きは量ごとに違います** (質量は ∝SEG・剛性は ∝1/SEG) ⇒ 片方を直しても他方は直りません。

#### 6.4g-3 ⭐ 減衰の裁定 (`-333` が設計 court へ残した問い) + Tier A 行の caveat 1 件

**(a) ⭐⭐ 裁定 = 実装は正しい。⛔ 変更しません。決め手は SSOT 自身の単位表記です。**

| SSOT | 単位 | 意味 |
| --- | --- | --- |
| `:144 CABLE_BEND_STIFFNESS = 0.005` | **[N·m²]** | ⭐ **材料の量** (EI) ⇒ per-joint へは `EI/SEG` で割り下ろす ⇒ ∝1/SEG |
| `:152 CABLE_BEND_DAMPING = 0.01` | **[N·m·s]** | ⭐⭐ **関節の量** (モーメント / 角速度) ⇒ ⭐ **割り下ろす対象ではない** ⇒ SEG 非依存 |

⇒ ⭐⭐⭐ **単位が、2 つを別扱いする理由をそのまま書いています。** ⇒ ⭐ w2:p0 の実装 (`:1013` 素通し) は **宣言された単位に忠実**です ⇒ ⛔ 補正不要。

**(b) ⚠ ただし物理の帰結は実在します (⛔ 私の裁定① の 3 番目の副作用)。**
関節あたりの減衰が一定なら、⭐ **関節数が倍になれば 単位長あたりの散逸も倍**です ⇒ ⭐ **裁定① の後、ケーブルは より減衰します** (揺れが早く収まる)。
⇒ ⭐⭐ **落とせる検定は 既に在ります**: `task_config.py:147-149` 逐語「**Picked VISUALLY from the drape render (the sim drape IS the acceptance criterion**)」⇒ ⭐ **これら定数の受入基準は 垂れの見た目**です。
⇒ ⭐ **ゆえに裁定① の後、同じ基準 (drape render) で見直しが要ります。** ⛔ 数値では代替できません — 元の受入が視覚だったためです。⚠ 視覚判定は私の court ではありません ⇒ 実施は該当 court。

**(c) ⛔⛔ Tier A 行に caveat 1 件 (`-333`(2)) — そして これは guard の 3 番目の盲点です。**
`CABLE_BEND_STIFFNESS` は **runtime 可変** (`:928` の env `CABLE_BEND_STIFFNESS_OVERRIDE`) ⇒ ⛔ **純関数ではありません** (`CLIP_COLLIDE` と同型)。
⇒ ⭐⭐ **env override は「第 2 の出所」です** — 私の §6.4b は「spec 以外の module から OWNED 名を取れば失敗」と定めましたが、⛔ **env 変数は静的解析に一切映りません。**

⭐ **guard の既知の盲点 3 つ (ここで揃いました)**:
1. 場面テンプレート内の数字 (§6.4e) ⇒ **反転規則で対処済**
2. 規則が通す無次元の規約 (§6.4f) ⇒ **判断で配置**
3. ⭐ **runtime override (env 変数)** ⇒ ⛔ **どの静的検査にも映らない**

> ⭐⭐ **3 の処分 (私の裁定) = 禁止せず 宣言させる。** override は実験に必要 (`CLIP_COLLIDE` が実例) ⇒ ⛔ 禁止は高すぎる。
> ⇒ ⭐ **要件 = module が override を読み、有効値を起動時に loud に印字する。** ⇒ **見えない第 2 の出所を、見える第 2 の出所に変えます。**

#### 6.4h ⭐ 素の係数集合 — **一覧でなく規則で定めます** (`-323`(1) への裁定)

⛔ 集合を列挙すると保守が要り、⛔ `14.0` のような整数値の物理量が滑り込みます。
> ⭐⭐ **規則**: 乗除に現れるリテラルが自由になるのは、次の **3 類だけ**。
> ① **小数点なしの整数** (`3` `6` `12` = 個数・繰返し) ・② **単純有理数** (`0.5` `0.25` `0.75`) ・③ **10 の冪** (`1000.0` `0.001` `1e-3`)
> ⛔ **それ以外の乗数は cell 定義**として扱う。

⭐ **p0 の境界成員 4 件で検定しました**: `0.72` ⛔捕まる ・`0.15` ⛔捕まる ・**`14.0` ⛔捕まる** (⭐ 整数値でも **小数点つきは物理量の書き方**) ・`0.0007` ⛔捕まる。⇒ ⭐ **4/4 捕まり、`0.5` `*3` `*1000.0` は自由**。
⚠ ⛔ 私は 23 種の一覧を見ていません ⇒ ⭐ **新たに要分類となる実数は p0 の一覧で出ます** (私は規則を決めただけです)。

⚠ **既知の穴 1 つ**: 「整数リテラル」の扱い。`range(3)` や添字の `0`/`1` を cell 定義と誤判定しないための除外が要りますが、⛔ **除外を広げると `CABLE_N = 32` のような整数の cell 量を逃がします。**
⇒ ⭐ 私の線引き: **名前に直接束縛された数値リテラルは常に cell 定義**扱い (`CABLE_N = 32` は捕まる) ・**添字 / `range` / 比較の中の整数は除外**。⇒ ⛔ これが唯一の裁量部分なので、実装で困ったら戻してください。
5. ⛔ **driver が新しい cell 定数を必要としたとき、自分で定義しない** — 本 spec に足す (w2:p4 の brief 規則① と対)。

---

## §7 射程 (私が出していないもの)

- ⛔ **新しい値を 1 つも作っていません。** Tier A は repo SSOT、Tier B は p4 の実測コメントからの採用です。
- ⚠ **`TABLE_HX/HY` の採用根拠は弱い** — 記録コメントが無く、「後発の cell だから」という理由だけです。⇒ ⭐ p4 が根拠を持っているなら本節を差し替えてください。
- ⛔ **`CLAMP`/`OPEN`/`HALF` と PD gains の値を判定していません** (別 court)。
- ⛔ **`CABLE_SEG` を 0.015 に変えろ、とは言っていません** — §2 のとおり全長と対で決まる量です。⭐ 言えるのは「30 mm は SSOT と食い違い、計器の床を 2 倍にしている」まで。
- ⚠ 本書は **値の一覧であって、cell が正しく建つことの検証ではありません。** 単一化した後に 1 run 通すことが要ります。

---

## §9 裁定① — ケーブルを SSOT の 600 mm にするか (w2:p4 が §7 に従い停止 ・`-225`)

### 9-1 ⭐⭐ 2 つの定数は **別のもの**を符号化しています

| SSOT 定数 | 符号化しているもの | cell 固有か |
| --- | --- | --- |
| `CABLE_SEG_LEN = 0.015` (`:136`) | ⭐ **離散化の細かさ** (何 mm 刻みでケーブルを表すか) | ⛔ **cell 固有ではありません** — 0.030 に cell 側の理由が在りません |
| `CABLE_SEGMENTS = 40` (`:135`) | ⭐ **全長** (逐語コメント "5-clip span 300mm + 150mm margin each end") | ✅ **cell 固有です** — env の cell の寸法 |

⇒ ⛔ **「SSOT に合わせる」を 2 つまとめて適用したのが、この行き詰まりの原因です。**

### 9-2 ⛔ 600 mm にすると何が外れるか (私の実測)

p4 の cell: saddle `REST_X = (-340, -140, +240) mm` ・clip `C1 +150 / C2 +40`。

| 全長 | 範囲 | saddle |
| --- | --- | --- |
| 960 mm (現行) | ±480 | ✅ 3 本とも内 |
| **600 mm** (SSOT) | ±300 | ⛔ **−340 が外** ⇒ **その saddle は何も載せません** |

⇒ ⭐ 全長は cell の寸法と対 — 私の §7 のとおりでした。

### 9-3 ⭐⭐ 裁定 = **刻みだけ採り、本数は導出する**

> ⭐ **`CABLE_SEG_LEN = 0.015` を採る (SSOT)。⛔ `CABLE_SEGMENTS = 40` は採らない。**
> ⭐ **`CABLE_N` は定数でなく導出量**: `N = 必要な全長 / CABLE_SEG_LEN` ⇒ 960 mm の cell なら **N = 64**。

⇒ ⭐ cell 寸法は 1 mm も動かさずに、⭐⭐ **吸着の量子化床が SEG/2 = 15.0 → 7.5 mm に半減**します。

⚠⚠ **ただし無償ではありません (先に書きます)**: 全長を保ったまま刻みを半分にすると **関節が倍**になります (32 → 64) ⇒ ⭐ **同じ長さに対して曲がりやすくなります。** ⛔ 「同じケーブル」ではありません。
⇒ ⭐ ただし向きは **SSOT の離散化へ寄る**側です (banked run が記録された刻みは 15 mm) ⇒ 忠実度としては**改善方向**。
⇒ ⚠ 計算量も 2 倍 (body/joint 数)。

⚠⚠ **費用の 3 件目 — 私が書き落としていました (w2:p11 `-243` 指摘)**: 刻みを変えると**垂れと傾きが変わる** ⇒ ⛔ **帯 12.00 を決めた置き誤差 4 点 (aim L 1.64 / R 1.45 / STEP3 L 2.33 / R 4.89) は持ち越せません。** 置き誤差は「グリッパ + ケーブル + 狙い loop」という**系の性質**であって、ケーブルだけ差し替えても保たれません。⇒ ⭐ **裁定① を実行するなら、帯の 4 点は再測定が要ります** (⛔ 私は §9-3 で「忠実度は改善方向」とだけ書き、この費用を落としていました)。

### 9-5 ⛔⛔ 本節の主張を撤回します (w2:p11 `-250` ・p11 が正)

⛔ **私の 1.45 mm は「計器の誤差」で、帯を食う物理の項ではありませんでした。**
- 私の量 = 半ピッチ吸着 `15.0 × sin(5.55°)` = **1.45 mm** ⇒ ⭐ これは **ケーブル位置の測り方の誤差**です。
- 帯を食う物理の項 = **爪長由来** `11.0 × tan(5.55°)` = **1.07 mm** ⇒ ⭐ **刻みに依りません。**
⇒ ⛔ **ゆえに「裁定① が余裕の成立条件を買う」は偽です。** 刻みを半分にしても 1.07 は動きません。
⇒ ⛔ **私の型 = 測った量に、それが測っていない結論をぶら下げた** (計器の誤差を、物理の余裕の話に使った)。

⭐ **結論だけは理由を替えて残ります**: **6.00 − 4.89 − 1.07 = 0.04 mm** ⇒ ⭐⭐ **現行の傾きでは 余裕は事実上ゼロで、それは どの刻みでも同じ**です。⇒ **手当は刻みでなく 傾き側 (roll の第 2 の仕事) に在ります。**
⭐ **裁定① 自体は原根拠で不変**: ①SSOT の離散化への忠実度 ②計器の床 15.0 → 7.5。⛔ 「余裕の成立条件」は根拠から外します。

### 9-5b (旧 9-5 の見出し ・以下は上記のとおり誤りを含みます ・記録として残置)

私は `-064` で 1 つ falsifier を置きました: 「**傾き 4.23 度で 1.11 mm の余裕を使い切る**」(1.11 = 半帯 6.00 − 最悪残差 4.89)。
⭐ **w2:p11 が量を出しました** (判定 log ・`-243`): L z 0.9499 / R z 0.9586 ・span 89.5 mm ⇒ **垂れ 8.7 mm ⇒ 弦の傾き 5.55 度**。

| 刻み | 量子化 = SEG/2 | スロット軸へ漏れる量 = 量子化 × sin(5.55°) | 余裕 1.11 mm |
| --- | --- | --- | --- |
| **0.030 (現行)** | 15.0 mm | **1.45 mm** | ⛔ **超過** |
| **0.015 (裁定①)** | 7.5 mm | **0.73 mm** | ⭐ **内** |

⇒ ⭐⭐⭐ **falsifier は発火しました (5.55 > 4.23)** ⇒ ⛔ **現行の刻みでは、狙い点の選び方だけで帯の余裕を食い潰しています。**
⇒ ⭐⭐ **そして裁定① はそれを余裕の内へ戻します。** ⇒ 刻みの変更は「計器が綺麗になる」話ではなく、⭐ **帯 12.00 が依拠する余裕を成立させる条件**です。

⚠ **射程 (3 件)**: ①**弦の傾き 5.55 度は各爪位置の局所傾きの代理**です (局所値は log に未印字 ・p11 明示) ②刻みを変えれば垂れ自体も変わるので **0.73 は「今の傾きのままなら」の値** ③⛔ 私は当該 log を読んでいません (数は p11 経由)。
⇒ ⭐ それでも向きは一意です: **超過 → 内**。

### 9-4 ⭐ 別解 (私は採りませんが、成立します)

**cell を縮めて 600 mm を丸ごと採る** = `REST_X` の −340 を −300 の内へ移す。⇒ SSOT の**両方**に一致します。
⛔ 私が採らない理由 = `ur15_steps.py:53` 逐語 "saddles kept clear of both 88 mm grasp spans" ⇒ **saddle の位置には記録された制約が在り**、−280 等が今もその制約を満たすかを私は測っていません。
⇒ ⭐ **p4 が「−300 の内でも把持域を避けられる」と示せるなら、別解の方が上です** (SSOT と完全一致し、本数も 40 に落ちて計算量も減る)。⇒ ⛔ 私は測れないので選びません。

---

## §10 裁定②′ — **600 mm 別解を採ります** (w2:p4 の鞍実測 `-261` を入力に)

### 10-1 ⭐⭐ 垂れが決定打で、⛔ 私の想定と逆に **別解が有利**でした

p4 の窓 = `[−300, −54.6]` + `[+244.6, +300]` ・鞍 3 個。⇒ 最大無支持スパンを最小化する置き方 = **窓の端**。

| | 鞍位置 | 支点間 | 張出 | **最大無支持** |
| --- | --- | --- | --- | --- |
| 現行 (960 mm) | −340 / −140 / +240 | 200 / **380** | 左 140 ・右 240 | **380 mm** |
| **別解 (600 mm)** | **−300 / −54.6 / +244.6** | 245.4 / **299.2** | 左 0 ・右 55.4 | **299.2 mm** |

⇒ ⭐⭐⭐ **最大無支持が 380 → 299.2 mm (−80.8 mm ・21% 改善)** ⇒ ⛔ **別解は垂れを悪化させません。良くします。**
⇒ ⭐ **未回答だった「その窓で 600 mm を支えられるか」は、比較で答えが出ます**: ⭐ **今日の配置より支えられます。**

⛔ **【私の「どちらも破綻」は悲観過剰でした — 実測が在りました (w2:p11 `-300`(3))】**
私は「`L²` なら 157 / `L⁴` なら 2827 mm ⇒ どの scaling でも非現実的」と書き、⚠ 「テーブルに触って止まっているはず」と推測しました。⇒ ⛔ **両方 外れです。**
⭐ 判定 log の実測: 最下点は **台の 26.2 mm 上** (⇒ ⛔ **触っていません**) ・鞍 3 個のみで支持 ・**実測垂れ 127.8 mm**。
⇒ ⭐⭐ **`L²` (張力支配) が正しい model** です (私の `L²` 予測 157 に対し実測 128 = 23% 過大 ・`L⁴` の 2827 は桁違い) ⇒ ⛔ `L⁴` は棄却。
⇒ ⭐⭐⭐ **ゆえに別解が減らすのは「非現実的な数」ではなく 実在する 128 mm です。** 実測点から `L²` で外挿すると **127.8 × (299.2/380)² ≈ 79 mm** ⇒ ⭐ **約 49 mm (38%) の減少** ・台からの逃げは **26.2 → 約 71 mm** (⚠ 私の外挿 ・推定と明記します)。
⇒ ⭐ **600 裁定は強まります。** ⚠ 射程 = 1 run ・⛔ log は provenance 保留下 ・⛔ 私は当該 log を読んでいません。

### 10-2 ⭐ 裁定

> ⭐⭐ **600 mm 別解を採ります。** ⇒ `CABLE_SEG_LEN = 0.015` と `CABLE_SEGMENTS = 40` の **両方が SSOT と一致** ⇒ ⭐ §9-3 の「`CABLE_N` は導出量」という特例が **不要**になります (`N = 40` が SSOT の値そのもの)。
> ⭐ 計算量も 裁定① の `N = 64` より **減ります** (64 → 40)。

**鞍の移動先 (私の値)**: `REST_X = (−0.300, −0.055, +0.245)` — ⭐ **窓の端に置く**のが最大無支持を最小にします。
⚠ ⛔ **1 点だけ p4 に確認が要ります**: 窓 `[−300, −54.6]` は **鞍の中心**の許容域ですか、それとも**外形**ですか。外形なら中心は半幅 (14 mm) 内側 ⇒ `(−0.286, −0.0686, +0.2586)` になり、⛔ **最大無支持は 327.2 mm に悪化**します (それでも現行 380 より良い)。

### 10-3 ⚠ 射程

- ⛔ **`CABLE_R` の食い違い (0.005 / 0.004) は本裁定と独立**です。SSOT は **0.004** (§3) で不変。
- ⛔ 私は p4 の doc を読んでいません (窓・半幅 36.6 は `-261` 経由)。
- ⚠ 垂れの外挿は **2 つの scaling の両端**で、⛔ どちらが正しいかを私は知りません。⭐ 結論に使ったのは **比 (380 対 299.2)** だけで、⭐ 比はどちらの scaling でも同じ向きです。
- ⚠ **§9 の裁定① は取り下げません** — 刻み 0.015 を採る判断は本裁定に含まれています。変わったのは **本数を導出しなくてよくなった**ことだけです。

---

## §8 改訂 1 (w2:p0 の敵対的検証を受けて ・実装前 ・3 件とも私が on-disk で確認して採用)

| # | 私の初版 | 直し | 私の誤りの型 |
| --- | --- | --- | --- |
| ① | `CABLE_SEG` の出所を `:135` の**コメント** "40 segments × 15mm" とした | ⭐ **`:136 CABLE_SEG_LEN = 0.015`** = import 可能な**実定数**が 1 行下に在った | ⛔ **1 行読んで止めた** — 同じ block を数え上げなかった |
| ② | 着座の Tier A 行に `TABLE + float_z + 0.009` と書き `task_config.py:226` を根拠にした | ⭐ **2 行に分割** — `:226` に浮上項は無い (`float_z` の閉じた query = **0 件**)。浮上は `newton_skill_env_base.py:1897`。⇒ 浮上後の座 = `GROOVE_CENTER_Z + target_clip_float_z` | ⛔⛔ **引用先に存在しないものを引用した** — 私が終日 他者に指摘してきた型 |
| ③ | guard を「SSOT の当該行の sha」で作れとした | ⭐ **AST で値を評価して 値どうしを比較**。byte-sha は通知へ降格 | ⛔ **識別性の無い述語** — コメント修正で偽警報・別所再代入を素通し |

⭐ **①③ は結論を変えず、②は Tier A の行を 1 つ増やしました。** ⛔ どれも実装前に見つかったので、書き換えの費用はゼロです。

⭐ **併せて記録**: w2:p0 は自身の「権威 env は衝突 OFF」を撤回されました (2 つの build 経路を 1 語に畳んでいた) ⇒ ⭐ 本 spec の Tier A「クリップ衝突 = ON」(`newton_skill_env_base.py:1908` `:1925` ・committed で無条件) は敵対的検証を生き残りました。⚠ 私はこれを「私が正しかった」でなく「**2 つの経路が逆の既定を持つ**」という事実として持ちます (兄弟文書 §6-A)。
