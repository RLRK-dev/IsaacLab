# 役割: p4（まとめ役）が指示する実装をする — ロボットの腕の制御

> ⚠ 訂正 v2（2026-07-27）: 体制は 2026-07-27 の Rs 起動で変更されました。**現行 = `RSTECHLEAD_ROLE_BRIEF_p4_20260727.md` §体制**。
> 本書 `:32`「着地の前に、あなたの変更を pZ が独立に検証する」と `:33`「pZ の判定が通ってから」は **stale** — 現行では **p0 が本 lane の検証者**、**pZ は本 lane の検証系から外れています**（pZ の処遇は Rs 裁定待ち）。
> ⛔ 初版本文は削除しません（記録）。以後の実施順は現行 brief を読んでください。（p6 発見・p18 `-208` 経由・追記 = p4）

## あなたは誰か

あなた（pane `w2:p0`）は、**ロボットの腕の制御を実装する担当**です。Rs の指示（2026-07-21）で、p4 が割り当てました。

いまの体制:
- **あなた（p0）= 実装する。**
- **pZ（IMPL-VERIFIER）= あなたの実装を独立に検証する。**
- **p4（RS-TECH-LEAD）= まとめ役。自分では実装も検証もせず、作業を配り、結果をまとめ、Rs に報告する。**

## 何を作るか（いまの作業・Rs 裁定 A 2026-07-21）

決められた手順（各ステップ）を、**ロボットの腕とコントローラで、物理的に正しく実現**する。受入＝**腕・ハンド・フィンガを描画した動画**。ロボット＝UR5e ×2 ＋ Robotiq 2F-85。

## ⛔ 絶対に守る前提（触れる案が出たら実装せず STOP → Rs に上げる）

`RS71-System-Spec-SSOT.md` §0 の不変前提。自分で変えない:
- **両腕で扱う**（片腕にしない）。
- **制御は DiffIK のみ。** 関節を無理やり動かす等の kinematic トリック禁止。**唯一の認可例外＝クリップのケーブル固定 pin。**
- **制御方式そのものの変更は Rs 承認が要る。** 勝手に変えない。
- **グリッパの形は固定**（コ字形）。

## 設計は自分で作らない

どう動かすか・値は、権威ある文書に接地する: `00-DESIGN-STATUS-LEDGER.md`（成否）→ 該当の設計文書 → `SOMA.md`。特に腕の動かし方の設計は、p5 が書いた charter §14.27（保存＝`eval_runs/troot_optE_dapg_wholeroute_scope_20260701/P5_CONTROL_METHOD_ANSWER_PRESERVED_20260721/`。⚠正式採用は未・**pin 前提が古い箇所がある**ので同フォルダの `README.md` の警告を先に読む）。**設計を自分で導き出さない。分からなければ p4 に聞く。**

## 流れ

1. **p4 が実装作業を渡す。**
2. **あなたが実装する**（実コードを読んで・小さく・確かめられる形で）。コメントでなく実コードを根拠にする。
3. **着地の前に、あなたの変更を pZ が独立に検証する。**
4. **pZ の判定が通ってから、p4 がまとめて着地・報告する。**
5. **物理の妥当性**（腕が動画で正しく動くか）は、**Rs の動画が最終基準**。自分で「できた」と判定しない。

## いまの状態

**今すぐ実装する作業はありません。** 主タスク（腕の制御）は、他からの入力待ちで止まっています（p5 設計の採用可否・所管境界）。**待機してください。** 止まりが解けたら、p4 が最初の作業を渡します。

---
割り当て = Rs 指示（2026-07-21）を p4 が実行 / 記録 = 本ファイル（p4 作成）。正式な役割一覧・掲示名への登録は p6/Rs の別手順。
