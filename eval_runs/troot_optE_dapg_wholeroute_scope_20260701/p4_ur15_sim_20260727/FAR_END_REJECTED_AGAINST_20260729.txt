THE FAR END: centre -0.250, left hand on cab0 (-0.2924) -- p5 -172(3), to separate option 1 from option 3

[steps] ⚠ grasp centre moved off C1: x=-0.2500 m (C1 is at +0.1500); span unchanged at 88 mm commanded
[steps] measured grasp: L=cab0 [-0.2924  0.28    0.9538]  R=cab6 [-0.2024  0.28    0.9517]  drop across the span = 2.0 mm
[steps] start pose = the cell's home, both arms: [-3.1521 -0.2867  2.4674 -1.3953  1.5634 -1.5782]
[steps] ⛔ start-pose IK L: NOT ONE of 13 candidates cleared the clearance or its path, so all 13 were put back and the choice below is made among poses that were all rejected -- read the next line's 'collision-free' as 'none'
[steps] start-pose IK L: rejected against -- the other arm x13, g6 on L_forearm_link vs stem x9, g6 on L_forearm_link vs crown x3   (parts named; the crown and stem/foot are the mounting, 'the other arm' is not)
[steps] start-pose IK L: 13 solved / 0 collision-free (⛔ 0 -- all 13 put back) / floor 0.00 removed 0 of them (it ranks, it does not exclude), chosen pos  0.00 mm roll  0.0 deg sigma_min 0.1782 |q|max=2.34 rad
[steps] ⛔ start-pose IK R: NOT ONE of 15 candidates cleared the clearance or its path, so all 15 were put back and the choice below is made among poses that were all rejected -- read the next line's 'collision-free' as 'none'
[steps] start-pose IK R: rejected against -- the other arm x13, g44 on R_forearm_link vs stem x6, g43 on R_upper_arm_link vs stem x3, g44 on R_forearm_link vs crown x2, g43 on R_upper_arm_link vs crown at 6/9 along the move (on the way) x1, g43 on R_upper_arm_link vs crown at 5/8 along the move (on the way) x1, g44 on R_forearm_link vs crown at 7/9 along the move (on the way) x1, g46 on R_wrist_2_link vs crown at 7/11 along the move (on the way) x1   (parts named; the crown and stem/foot are the mounting, 'the other arm' is not)
[steps] start-pose IK R: 15 solved / 0 collision-free (⛔ 0 -- all 15 put back) / floor 0.00 removed 0 of them (it ranks, it does not exclude), chosen pos  0.00 mm roll  0.0 deg sigma_min 0.1088 |q|max=2.34 rad

READING (p4, after the run; the lines above are the measurement)

⚠ The counts do NOT partition the candidates.  Each candidate is put through the arm-to-arm
   test and the mast-at-pose test independently (the path test runs only if the pose test
   passed), so one pose can appear under two names.  13 + 9 + 3 = 25 against 13 candidates.

1. p5's question was posed as a binary -- still the mounting, or now the other arm.  The
   measurement is BOTH, and the two do not carry equal weight:
     - the other arm rejects 13 of 13, which is new; at centre +0.150 it rejected 0 of 7.
     - the mast rejects 12 of 13 (stem 9, crown 3), which is not new.

2. ⭐ The mast test does not consult the other arm, so its 12 stand whatever the other arm
   does.  Removing the other arm entirely would leave exactly ONE candidate clear of the
   mounting at this end.  At centre +0.150 the same subtraction leaves ZERO of 7.

3. ⇒ So the separator does not come out cleanly either way.  The mounting still blocks almost
   everything at the far end, which is the direction of option 3; but 'almost' is not 'all',
   and the one pose that clears the mast there is a thing that does not exist at the near end.
   ⛔ Whether one candidate is a window or a rounding error is a design judgement, not mine.

4. Both arms are fully rejected at this centre (R: 15 solved / 0 free), consistent with the
   sweep, where R's clear count reaches zero from centre 0.000 onward.

⛔ Not a verdict and not a recommendation.  Options 1 and 3 are p5's court and Rs's to settle.
