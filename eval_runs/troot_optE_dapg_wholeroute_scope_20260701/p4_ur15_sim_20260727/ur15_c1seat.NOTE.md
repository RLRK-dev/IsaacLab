# `ur15_c1seat.log` — この記録の読み方 (p6 #52 ① の沈黙解消)

作成 2026-07-28 14:4x JST (w2:p4 RS-TECH-LEAD)。⛔ **記録本体は編集しません** — 過去の走行が何を印字したかは事実であり、直す対象ではありません。⭐ 直すのは**黙っていたこと**です。

## ① ⛔⛔ この記録は、**いまの cell とは別の cell** で作られています

| | `ur15_c1seat.log` (2026-07-27 17:29) | 現行 cell (2026-07-28) |
| --- | --- | --- |
| 剛体数 `nbody` | **80** | **88** |
| 幾何数 `ngeom` | **131** | **139** |
| 一般化座標 `nq` | **97** | **113** |
| 座 link | **C1=cab21 / C2=cab17** | **C1=cab30 / C2=cab23** |

⇒ ⭐ **この記録の数値は、現在存在しないモデルの量です。** 現行の判断の根拠として引いてはいけません。⚠ 比較したい場合は、現行 cell で取り直すのが唯一の方法です。

（出所 = 本 log `:1` 逐語「nq=97 nu=14 nbody=80 ngeom=131 eq=8  seat links C1=cab21 C2=cab17」／現行 = `run_logs_20260728/run_t12.txt` の同じ行）

## ② ⛔ `:5` / `:6` の「2 つの名前で 1 つの数」

逐語 (`:5`): `start-pose IK L: 8 solved / 4 collision-free / 4 away from a singularity, ...`

⇒ ⭐ **この 2 つの 4 は同じ候補集合です。** 当時も `SIGMA_FLOOR` は 0.0 で、特異値は負にならないので `>= 0.0` の絞り込みは**全部を通します**。⇒ 「away from a singularity」という 2 つ目の名前は、**走っていない絞り込みが走ったと言っています**。

⚠ **これは私の書き落としです** (指摘 = w2:p11 `-120`(1))。撤回された床の文言を、床の撤回後も報告に残していました。⭐ 現行 driver では直っています = commit `5718746442`（印字は「床が実際に落とした数」= 床が 0 の間は 0）。

⇒ ⭐ **この記録の `:5` / `:6` を読むときは、2 つ目の数を無視してください** — 情報を持っていません。

## ③ 何が生きているか

⭐ **走行そのものは事実**として残ります（1158 frame・動画 `ur15_c1seat.mp4`）。⛔ ただし ① のとおり**別 cell** なので、現行 cell の主張の根拠には使えません。⇒ **歴史記録として保持**します（退役ではなく、射程つきの保持）。
