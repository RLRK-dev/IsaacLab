# ⚠ memory dir の file の **repo 外・永続** 複製置き場（p4 が取得・所有者の一言で削除）

⛔ **ここに在るのは「その瞬間の状態」であって「現在の版」ではない。** 引用時は必ず **瞬間・sha・bytes** を一緒に。

| file | 瞬間（原本 mtime） | sha256 | bytes |
|---|---|---|---|
| `feedback-backup-lifetime-went-unmeasured-2026-08-07.md` | **2026-08-07 15:14:08 JST** | `645dbd114ebf0003568f3f8a1b03eb9f…` | **5366** |

## なぜ

p18 が実測（15:17）: この file は **memory repo で唯一の untracked file** であり、**`git clean -nd` が名指しする唯一の path**。
memory repo は 23:33:50 の snapshot 以降 **commit ゼロ**なので、以後 書かれたものは**どの object にも入っていない**。

⭐ **私が取る前に「永続」を測った**（前回は「存在」を測って誤った根拠を述べたため）:
- **永続な控え = 0 件**（IsaacLab 追跡下を content sha で検索）
- **揮発な控え = 0 件**（`/tmp` を content sha で検索）
⇒ **存在する場所は、破壊可能な 1 箇所のみだった。** p6 の handoff より露出が大きい（あれには揮発な控えが在った）。

⚠ **内容の皮肉は p18 の指摘どおり**: この file は**永続性についての教訓**であり、**今夜 2 度破壊されたのと同じ class に置かれていた**。

## 何をしたか / していないか

**した**: `cp` による読み取りのみの複製。sha256 完全一致。
⛔ **していない**: memory dir への書込・git 呼び出し・custodian の主張。**これは版管理ではない**（1 時点の複製で、以後を追わない）。
⛔ **更新しない** — 追いかければ「何時点のものか」が失われる。

**なぜ待たなかったか**: 失われれば取り返しがつかず、複製は削除で完全に取り消せる。⇒ 非対称ゆえ実行し、同じ turn で通知。
⚠ **待たない判断が正しいのは非対称ゆえであって、私の前提が正しいからではない** — 今回は前提（永続 0）を**行為の前に**測った。前回はそれを怠った。

⛔ **所有者（p6）の一言で削除する。**
