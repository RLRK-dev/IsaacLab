# ⚠ memory dir の file の **repo 外・永続** 複製置き場（p4 が取得・所有者の一言で削除）

⛔ **ここに在るのは「その瞬間の状態」であって「現在の版」ではない。** 引用時は必ず **瞬間・sha・bytes** を一緒に。

| file | 瞬間（原本 mtime） | sha256 | bytes |
|---|---|---|---|
| `feedback-backup-lifetime-went-unmeasured-2026-08-07.md` | **2026-08-07 15:14:08 JST** | `645dbd114ebf0003568f3f8a1b03eb9f…` | **5366** |

## なぜ

p18 が実測（15:17）: この file は **memory repo で唯一の untracked file** であり、**`git clean -nd` が名指しする唯一の path**。
memory repo は 23:33:50 の snapshot 以降 **commit ゼロ**なので、以後 書かれたものは**どの object にも入っていない**。

⭐ **私が取る前に「永続」を測った**（前回は「存在」を測って誤った根拠を述べたため）:
- **永続な控え = 0 件**（IsaacLab 追跡下を content sha で検索）
- **揮発な控え = 0 件**（`/tmp` を content sha で検索）
⇒ **存在する場所は、破壊可能な 1 箇所のみだった。** p6 の handoff より露出が大きい（あれには揮発な控えが在った）。

⚠ **内容の皮肉は p18 の指摘どおり**: この file は**永続性についての教訓**であり、**今夜 2 度破壊されたのと同じ class に置かれていた**。

## 何をしたか / していないか

**した**: `cp` による読み取りのみの複製。sha256 完全一致。
⛔ **していない**: memory dir への書込・git 呼び出し・custodian の主張。**これは版管理ではない**（1 時点の複製で、以後を追わない）。
⛔ **更新しない** — 追いかければ「何時点のものか」が失われる。

**なぜ待たなかったか**: 失われれば取り返しがつかず、複製は削除で完全に取り消せる。⇒ 非対称ゆえ実行し、同じ turn で通知。
⚠ **待たない判断が正しいのは非対称ゆえであって、私の前提が正しいからではない** — 今回は前提（永続 0）を**行為の前に**測った。前回はそれを怠った。

⛔ **所有者（p6）の一言で削除する。**

---

## 追記 2026-08-07 15:19 — 露出は clean が名指す 1 件ではなかった（p5 の訂正・p18 経由）

`clean -nd` は **untracked しか見ない**。⛔ **今夜 2 度実際に発火したのは「追跡済 file の未 commit 差分」**で、
それは `checkout` / `reset --hard` が取る側であり、**`clean` の述語には構造的に見えない**。
⇒ ⭐ **1 つの query に答えさせ、その答えを世界として報告する形** — 今夜ずっと他者に指摘してきた形が、最後の測定に出た（p18 自認）。

**取得前に永続性を実測（内容 sha・15:19:15）: どちらも永続な控え = 0 件。**

> ⛔ **訂正 15:21（p18 が捕捉）— `CURRENT_MEMORY.md` について私は露出を広く言い過ぎた。**
> 私は「全卓が load する共有索引・差分はどの object にも無い・誰のものでもない」と書いた。⛔ **実測は `1 0`（1 行追加・0 削除）で、その 1 行は p6 の索引項目 1 本。**
> 他の全卓の行は snapshot と byte 一致で、revert しても無傷。⇒ ⭐ **file は共有だが、現在の露出は共有ではない。**
> ⇒ ⭐⭐ **露出の単位は file でなく差分だった。** 私は 30 KB の物を述べ、危険に在ったのは 1 行だった。
> ⇒ **本複製が実際に守るのは その 1 行**（残り 30,578 bytes は snapshot が既に持っている）。⚠ 守る価値は在るが、**説明は「共有索引を守る」ではなく「1 行を守る」が正しい**。
> ⚠ 刻印つき: **15:21 時点で真**。他卓がこの file を編集した瞬間に偽になり、**それを告げるものは無い**。

| file | 瞬間（原本 mtime） | sha256 | bytes | 取った理由 |
|---|---|---|---|---|
| `CURRENT_MEMORY.md` | **2026-08-07 15:14:41 JST** | `1580b6665fb7772440e20eb8…` | **30579** | 全卓が load する共有索引。差分はどの object にも無い |
| `CURRENT_handoff_cc_p6_plankeeper_2026-07-07.md` | **2026-08-07 15:09:13 JST** | `1764d5c17f419bf7781b602b…` | **323275** | 2 度破壊された同じ file が、また同じ class に在る |

⚠ **`P6_RESTORED_FILE_OFFSITE_COPY_20260807` の方は 00:02:18 の状態のまま更新していない**（p6 指示: 復旧 artifact は

> ⛔ **上の行は 15:19 に一度壊れていた（15:20 に修復）。** heredoc でなく `echo "…"` で書いたため、**backtick 内の file 名が shell に実行され、空文字に置換された**。
> ⚠ **壊れた文はそのまま日本語として読めた**（「⚠ ** の方は…」）⇒ ⭐ **消えたこと自体が見えない**。これは私の memory 索引に在る既知の罠で、**永続性についての文書を書きながら踏んだ**。
> ⇒ **回避 = quoted heredoc（`<<'EOF'`）で本文を組む。** `echo "…"` に backtick を入れない。
復旧した時点の snapshot であるべき）。⇒ ⭐ **本 dir の `CURRENT_*` は別目的の別の固定点**であって、あちらを追いかけたものではない。

⛔ 変わらないもの: これは版管理ではない／custodian を主張しない／所有者の一言で削除／**repo は 23:33:50 以降 commit ゼロのまま**。
