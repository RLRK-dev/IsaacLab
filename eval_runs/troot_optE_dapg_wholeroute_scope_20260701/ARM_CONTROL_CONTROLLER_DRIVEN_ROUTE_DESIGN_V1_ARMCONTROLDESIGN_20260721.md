# 腕を制御器で駆動して route を実現する設計 v1.0（p11 ARM-CONTROL-DESIGN, 2026-07-21）

**Author:** ARM-CONTROL-DESIGN (`w2:p11`)。**Status:** DESIGN v1.0 — **proposal**（landing = p4 → L3 + 設計 gate → p0 実装 → pZ 検証 → Rs 動画）。
**契機:** Rs 直接指示（emphatic・2 回連呼）「**kinematic は使用するな！完全削除！！**」（p4 relay 2026-07-21 21:16:41 JST）。p4 が現校正 route を実行し、**腕が kinematic 駆動である**ことを実証して Rs に提示した結果。
**任務:** **腕を制御器で駆動して route を成立させる**（kinematic 書込ゼロ）。**DoD = 腕・ハンド・フィンガを描画した動画で、route が制御器駆動で成立していること。**

⛔ **本書は構造の設計であり、ゲイン・bar・速度の数値を確定しない。** 数値は測定（既存 P-D1 実測 + 測定ハーネス）から取る。v0.2〜v0.4 の 3 連続失敗は「手で数値を導いた」ことに由来する。

### p4 からの確定回答（2026-07-21 21:27:42 JST）

| 論点 | 確定 |
|---|---|
| 削除範囲 | ✅ **A（制御ループ内 physics 書込）= 削除 / C（FK 書込）= 削除しない** で確定。**pin = ✅ 2026-07-26 Rs 裁定で決着（残す・§7）＋ extent 制約あり（§7.1）／B（reset 再配置 `:1096-1097`）= ⏸ 未決・照会中** |
| 測定 | ✅ **HOLD 解除 = YES**。p0 の H-2 結線は継続。**測定は制御器設計に直結**（`kd/ke` と task bar を H-2 / H-5 が供給する） |
| 重力補償 | ✅ **入れない**（力は足りているため必要性の根拠が無い）で現状維持 |
| pin | ✅ **決着 = (i) 残す**（Rs 直接裁定 2026-07-26・§7）＋ ⭐**extent = clip 着座の固定点 1 箇所のみ・ケーブル全体の固定は不可**（Rs 追加確認・§7.1）。⛔**可否と extent のみ・機構は未決**。⇒ **腕側は元から依存せず、本設計は変更不要** |
| 次段 | 数値が測定で埋まり次第 **L3 + 設計 gate**（`/diffik-trajectory` `/force-design` `/pre-check`） |

⭐ **測定 spec の被覆確認**（⛔ **行 pin を現行版へ更新 = pN **B33**。旧記載は **v1.7 時点の行**（`:131`/`:142`/`:143`/`:217`/`:183`/`:219`/`:139`）で **stale**）: 本書 §6 が要る 4 量は **spec が要求済** — **現 v1.9 の行** = `K_d` 生 28×28 dump（**`:135`**）／`τ_bias`（**`:146`**）と `a_max`（**`:147`**・`AC-4` = **`:236`**）／**phase 別 伝達比**（**`:201`**・`AC-6` = **`:238`**）／`ζ`（**`:143`**）。⚠ **私が現 spec 上で 7 行すべて読み直して確認した。** ⛔ **版をまたいだ行番号を混在させない**（旧 v1.7 行は本文の履歴としてのみ残す）。
📎 **HISTORICAL / SUPERSEDED（根拠に用いない）**: 旧記載は **spec v1.3** に接地し行 pin を `:130` / `:141-142` / `:154` / `:138` としていたが、**v1.7 で全て移動**した。併記していた「⇒ **spec を触らない**（churn を作らない）」も **SUPERSEDED** — その後 v1.4〜v1.7 で spec を実際に改訂した（H-4 3 点 / H-5.1 / H-3.1 / pad body 導出規則）。

---

## 1. 私が自分で確認したこと（relay を根拠にしない）

| # | 事実 | 実測（p11 が読んだ場所） |
|---|---|---|
| 1-a | **mujoco 枝は毎 substep、腕の関節座標を FK 値で上書きし速度を 0 にする** | `test_newton_clip_routing.py:1815-1838` 逐語「per-substep OVERWRITE joint_q=FK + zero joint_qd」+ `phys_jq[_ARM_OVERWRITE_IDX] = fk_state.joint_q…` / `phys_jqd[…] = 0.0` / `state_0.joint_q.assign(phys_jq)` |
| 1-b | **VBD 枝は FK の body 変換を physics へ複写する** | `:1752` `update_kinematic_bodies`（`phys_bq[:robot_body_count] = fk_bq[…]`） |
| 1-c | ⭐**指はすでに制御器駆動** — gripper 座標は DYNAMIC のまま servo が `control.joint_target_pos` で駆動 | 同 `:1826-1828` 逐語「leave the gripper coords … DYNAMIC so the servo drives them via `control.joint_target_pos`」 |
| 1-d | ⭐**RL env にも同じ kinematic 駆動が在る**（producer 限定ではない） | `newton_route_env.py:829-830`（`_broadcast_arm_jointq` = settle 中に毎フレーム）/ **`:1272-1273`（route 駆動の毎 physics サブフレーム）** |
| 1-e | 同 env の `:1096-1097` は **episode 開始時の re-pose**（reset） | `:1088-1097`（`env_ids` 単位の reset 再配置） |

⇒ **1-c が本設計の骨格**: 求められている機構は**同じ file の中に既に在り、指で動いている**。

## 2. ⭐ 削除範囲の分類（**これを誤ると IK が壊れる**）

| class | 内容 | 扱い |
|---|---|---|
| **A** | **制御ループ内**の physics state 書込（`_state_0.joint_q/qd.assign` で腕座標を上書き） | ⛔ **削除対象**（＝ Rs 指示の対象）。`route_env:829-830` / `:1272-1273`、`test:1815-1838`、`:1752` |
| **B** | **reset 直後**の初期化（episode 開始時 1 回） | ⚠ 現行規則では**許可**（`CLAUDE.md` の reset 例外）。⏸ **2026-07-26 の Rs 裁定でも未決**（§7 参照 — 逐語は pin への応答であり class B は列挙外）。⇒ **p4 へ照会中。⛔ どちらとも仮定しない。**（本設計は A を前提に組み、**B の可否で構造は変わらない**） |
| **C** | **FK state への書込**（`_fk_state.joint_q.assign(...)`） | ✅ **削除しない**。これは**目標を計算する側**であって physics の駆動ではない。⛔ ここまで消すと IK が目標を作れなくなる |

⚠ **class C を A と混同した「完全削除」は、指示の実現ではなく破壊になる。** 実装時の述語は「**physics state に書いているか**」であって「`assign` を含むか」ではない。

## 3. ⭐ 既存実装（**新規発明ではない** — §運用4 prior art）

`probe/pd1-arm-pd` に **腕の POSITION servo 経路が既に実装済**（環境変数 `ARM_PD_DRIVE=1` で切替）。p11 実測 = `git show probe/pd1-arm-pd:thread_isaac_lab/envs/newton_route_env.py`:

| 機構 | 実装場所（同 branch） |
|---|---|
| XML actuator の無力化（M-1 相当） | `:449` `ARM_XML_ACT_NEUTRALIZE` |
| 起動時の目標同期（M-4 相当） | `:452` `_arm_pd_ramp_q0`（活性時の実現 q を snapshot） |
| 立ち上げの傾斜（M-5 相当） | `:450` `ARM_PD_RAMP_FRAMES` / `:1390-1391` |
| POSITION mode + ke/kd 実配線 | `:301-304`（`joint_target_ke` / `JointTargetMode.POSITION`）/ `:833-834` |
| 毎フレームの目標書込 | `:1380-1391`（`control.joint_target_pos`・qd-indexed） |
| reset / hold も target 経路へ | `:909-914` / `:1183-1226` |

⇒ ⭐**設計課題は「機構を作る」ではなく「(a) 分岐を唯一の経路へ昇格し kinematic 側を削除する」「(b) 数値を決める」「(c) 削除で壊れる箇所を設計し直す」。**

## 4. ⭐ 実測が既に答えていること（`ARM_CONTROL_PD1_RESULT_RSTECHLEAD_20260719.md`・6 run）

| 問い | 実測の答え | 出典（逐語） |
|---|---|---|
| 制御器駆動でトルクは足りるか | ✅ **足りる** — 飽和 **0.0%**、最悪関節 **25.1 N·m < 28 cap** | §3「L-P3 saturation: PASS — 0.0% on every joint」 |
| では何が問題か | ⭐ **粘性の追従遅れ**（力不足ではない）。`err ≈ (kd/ke)·ω` | §3「the error is a **viscous velocity-tracking lag**, not force starvation」 |
| 遅れは残るのか | ⛔**残らない — 速度に比例し、遅い局面で消える**。実測 profile **0.45 → 0.51 → 0.20 → 0.02 → 0.005 → 0.001 rad** | §3 |
| どのレバーを引くべきか | **`kd/ke` の比**（`ke` の大きさではない）。effort cap は触らない | §5「suggests the **kd/ke ratio, not ke magnitude**, as the lever … effort caps stay untouched」 |
| 旧振付は清潔基盤で成立するか | ⛔**しない**（kinematic 駆動でも把持連鎖が形成されない） | §2「the clean substrate does not carry the banked chain even kinematically」 |

⚠ **私の過去の誤読を撤回済**: 「arm q 差 ≤1.1 mrad で連鎖が全滅」は誤り。原文は **両 run とも kinematic 駆動ゆえ腕姿勢は強制一致**し、**flip は cable 側**が担う、である。

## 5. 設計（構造のみ・数値は §6 の測定から）

### 5.1 制御路（唯一の経路）
- 腕関節 = **POSITION target mode** + `joint_target_ke` / `joint_target_kd` / `joint_effort_limit`。
- IK が出した関節目標を **毎 physics frame `control.joint_target_pos` に書く**（指と同一機構・§1-c）。
- ⛔ physics state（`joint_q` / `joint_qd` / `body_q`）への制御ループ内書込は**行わない**（class A 全廃）。
- 起動 = **M-4 同期**（活性時の実現 q を目標の初期値にする）→ **M-5 傾斜**（既存実装）。

### 5.2 ⭐ 振付は「制御器が追える形」で書く（W-b 再工事の中心）
`err ≈ T_lag·ω`（`T_lag = kd/ke`）ゆえ、**遅れは指令速度で決まり、止まれば消える**。⇒ 振付側に 2 つの要求:

1. **接触が効く瞬間の前に静定させる** — 目標を止めてから `t_dwell` 待つ。⇒ 把持・着座・押込みは **静定した状態で**起こす。
   - ⭐ **`t_dwell` の式は ζ で場合分けする**（**H-2 の ζ が要るのはここ**）。⛔ **どちらの場合に居るかは、宣言された free-arm 状態でのみ確かめている**（**B22 / B23**。把持中は `ζ` が UNVERIFIED ⇒ **枝の判定を流用しない**）: **ζ ≥ 1**（非振動）なら残差は概ね `exp(−t/T_lag)` ⇒ `t_dwell ≥ k·T_lag`（`k = ln(初期誤差/ε)`）。**ζ < 1**（振動）なら包絡は `exp(−ζ·ω_n·t)` で**行き過ぎと振動が残る** ⇒ `t_dwell ≥ k/(ζ·ω_n)` かつ **半周期の整数倍を待たない**（節で測ると静定して見える）。
   - ⚠ **`t_dwell` を「速く見えるまで」で決めない。** ⛔ **1 時点の速度が小さい ≠ 静定**（振動の折返しでも小さくなる）。判定は **窓で**（連続 N フレーム |q−target| ≤ ε かつ |q̇| ≤ ε̇）。
2. **連続精度が要る局面だけ速度を絞る** — その局面の `ω ≤ ε_task / T_lag`。自由移動区間は速くてよい（誤差は task に効かない）。

⚠ **旧 L-P1 の「連続 5 mrad」は task 要求ではなく、記録済み軌道への parity bar**。⇒ 本設計の bar は **task に効く量（着座時の cable 中心位置）× 効く瞬間**で立てる（測定 H-5 の伝達比が要る）。**⛔ 連続追従 bar を task bar に流用しない。**

### 5.3 削除で壊れる箇所（設計し直す対象）
| 箇所 | 現状 | 設計 |
|---|---|---|
| cable settle 中の腕保持（`route_env:829`） | 毎フレーム joint_q 書込 | **目標を home に固定して servo に保持させる**（外乱は物理として現れてよい） |
| route 駆動（`:1272`） | 毎サブフレーム joint_q 書込 | `control.joint_target_pos` へ（§5.1） |
| reset（`:1096`） | joint_q 再配置 | class B（§2）。**A の削除とは独立**に扱う |
| 監視 | — | **追従乖離ガード**（実現 q と目標の差が閾値を超えたら停止して報告）。⛔ 黙って続行しない |

### 5.4 ⭐ SKILL 側の到達閾値との結合（p5 が 7 SKILL 分を送付予定・2026-07-21 21:32 p4 relay）

SKILL は EE 到達閾値を持つ（例: CLAMP 2 mm）。⇒ **制御器の追従がそれを満たすように gain を決める**必要がある。⭐**ただし誤差は 2 種あり、効く対策が違う**:

| 誤差 | 式 | 静定で消えるか | 効くレバー |
|---|---|---|---|
| **動的な遅れ**（追従ラグ） | `≈ T_lag·ω = (kd/ke)·ω` | ✅ **消える**（目標を止めれば減衰・§5.2） | 指令速度 `ω` / `kd/ke` の比 |
| ⭐**静的なたわみ**（重力ドループ） | `≈ τ_bias(q)/ke`（純 PD） | ⛔ **消えない**（釣り合ってしまうため、いくら待っても残る） | **`ke` の大きさ**のみ（重力補償は §8 で除外済） |

⇒ ⭐**閾値が「静定した瞬間」に効くもの（CLAMP 等は到達してから掴む）なら、gain sizing を縛るのは遅れではなく たわみ**: **`ke ≥ τ_bias(q)/ε_joint`**（envelope 上の最大 `τ_bias` で）。⇒ **H-3 の `τ_bias` が gain の下限を決める。**
⚠ 逆に、閾値が**動いている最中**に効くもの（連続追従が要る局面）なら §5.2 の速度制限が効く。⇒ **どちらかは閾値ごとに違う。**

⭐ **閾値一覧の受領時に必ず添えて貰う 3 項**（無いと変換できない・v0.2/v0.3 の失敗の型そのもの）:
1. **どの点か** — 手首フランジ / 指先 / pad / **cable 中心**。⚠ `ee_pos_*` は**手首フランジ**であり指先ではない（掴んでいても物体まで 268 mm と出る）。
2. **どの瞬間か** — 静定後 / 動作中。⇒ 上表のどちらの誤差に当たるかが決まる。
3. **何に対する距離か** — 目標姿勢との差 / 物体との距離。

⇒ 変換経路: **cable 中心の量は H-5 の伝達比**で、**腕 EE の量は H-4 の Jacobian**で関節 bar に落とす。⛔ **cable の量を腕 Jacobian で変換しない**（cable は腕の剛体従属ではない）。

#### 5.4.1 p5 から受領した閾値表（`P5_EE_REACH_THRESHOLDS_for_p11_20260721.md` @ `49a6f66323`・sha256 `e93ee3d799f0bb0d…`・**p11 が code 側を独立確認**）

| skill | 閾値 | 点 | 瞬間 | routing |
|---|---|---|---|---|
| acquire-grasp | pos **2 mm**（`task_config.py:362` `T_DIST=0.002`）/ ori **10°**（`:364` `T_ALIGN=0.1745`） | 指先 | **保持 K=5**（静定） | **H-4** |
| approach | pos **12 mm**（`:363` `T_DIST_APPROACH=0.012`） | 指先 | — | **H-4 + H-5**（基準が cable 分節ゆえ分離） |
| insert 着座 | pos **3 mm**（`:368` `T_GROOVE=0.003`）/ ori `cos > 0.85`（`:369` `T_SEAT`） | ⭐**cable 分節 body**（腕でない） | 着座 | ⛔**H-5 のみ**。腕側の押込目標 `PUSH_Z=1.025` は H-4 |
| aerial | 落下不可 `min(cable z) > 0.82` | cable body | — | ⛔**H-5** |
| carry | 明示数値なし（下流継承 = grasp の 2 mm 予算内） | 指先 | — | H-4 |
| hold / wait | grasp の 2 mm 保持を継承 | 指先 | **静定** | H-4 |
| set_finger | `0.002 / 0.006 / 0.04` | **指 joint** | — | **H-4/H-5 とも N/A**（gripper servo・腕 EE 量ゼロ） |

⭐ **結論（sizing の核）**: **ほぼ全ての成功閾値が「保持・静定」で評価される** ⇒ §5.4 の表より **縛るのは重力たわみ = `ke` の大きさ**（`kd` ではない）。
⚠ **`T_SEAT = 0.85` は段階的引き締めの初期値**（`:369` 逐語「0.85→0.9→0.95」）⇒ **sizing は最終側 0.95 で見る**（保守側）。p5 の指摘どおり。

#### 5.4.2 ⛔⛔ 実測で見つけた測定面のずれ（**私は裁定しない**）

⚠⚠ **court の訂正（2026-07-26・p5 の correction `fe80839219b913518f3d2afa84323f9cbbd02df4` §3 を p11 が直読）**: 旧記載「**p5/Rs へ escalate**」は **stale** — **Rs は自らの court であることを撤回**（逐語「2　は私が判断することではない」）。⇒ **消費先ごとに court が分かれる**（1 個の「Rs 判断」に丸めない）:
| # | 対象 | court | gate |
|---|---|---|---|
| **(A)** | **success 述語の測定面**（2mm/12mm/10° を**どの点**で評価するか） | ⭐**p5** | **`/reward-design`** ＋ GPU 消費前に **`/pre-check`** |
| **(B)(C)** | **`GRASP_Z` / `PUSH_Z` / `EE_TO_FINGERTIP` 自体** | ⛔⛔ **UNCONFIRMED / HOLD** | `/geometric-design` ＋ `task_config.py` ゆえ **L3** |

⛔⛔ **(B)(C) の owner を捏造しない。** 複数 skill が共有する SSOT 定数であり、**候補 = p5 / p17 / p11（私）/ p16** のまま**未裁定**（p5 が pN 経由で境界照会中）。⇒ **私は自分を owner とも他者とも書かない。確定まで (B)(C) は進まない。**
⛔⛔ **撤回（2026-07-26・pN **B9**）**: 旧文「✅ **非 blocking**: H-4 を 3 参照点で出す設計ゆえ **どちらに決まっても再測定不要**」は **撤回する**。**3 点を出す設計であることは、出た量が十分であることを意味しない。** ⇒ **現行 H-4 は「各列の最大絶対成分」への縮約 ＋ 1 姿勢**であり、**方向つきの完全な Jacobian でも envelope の証拠でもない**。⇒ **参照点の選択で並進 J は変わり得るが、その方向・量・bar への帰結は UNVERIFIED。**
⭐ **今の時点で安全に言えること** = **pin された参照オフセットどうしが 34.8〜55.7 mm 違う**、それだけ。**権威ある参照点の選択 ＋ 認可された envelope 上の方向つき 3 成分 Jacobian が揃うまで、J と bar への帰結は UNVERIFIED。**
⭐⭐ **B15 の区別（両方とも真）**: ✅ **proxy は測定済** = 現行 H-4 の 3 点の値（`908ac4674576c3b936fe66866254d17691b6cc8e`・**各列を最大絶対成分に縮約 ＋ 1 姿勢**）。**「未測」ではない・消さない。** ⛔ **未測なのは evidence-grade** = **方向つきの完全な 3 成分 Jacobian ＋ 認可 envelope**。⇒ ⛔⛔ **proxy から bar への帰結を導かない。**

閾値が測る「指先」は `ee_pos + R(ee_q)·[0,0,**0.220**]`（`newton_skill_env_base.py:899-905`）。⚠ **その 0.220 は自身のコメントで Franka legacy と明記**（`task_config.py:78` / `:84`「Franka value; **re-derive S6**」/ `:324`）。⚠ **同 file に実測のコ字値が別に在る**: `EE_TO_PINCH_CLOSED = 0.2548`（`:320`）/ `EE_TO_PINCH_TIP_CLOSED = 0.2757`（`:321`）。

⇒ ⭐ **「指先」という同じ語が 2 つの点を指し、差は 34.8〜55.7 mm。**
⛔⛔ **訂正（2026-07-26・pN RETURN-004 **B6**）**: 本節でこの差を **「判定面と接触面のずれ」** と読ませていた箇所を撤回する。**実際の接触 geom / 接触面は UNMEASURED** — ①`task_config.py:37-38` は `GRIPPER_PAD_BODY_IDX` を **接触フィルタの logic 用**とし **pad の幾何は先送り**と明記 ②`route_executor.py:2436-2438` は保持を **f1ext(下爪)+f2ext(上爪) の sandwich** と定義し、**f1ext のみを見た旧 gate は「cable が上爪へ上がった」場面で false-FAIL した実績**がある ⇒ **触れる相手は 1 つに固定されない**。⇒ **J-a/J-b/J-c は「ラベルの付いた参照点」**である。
⛔⛔ **さらに訂正（pN **B8** / **B9**）**: ①**「2 mm 閾値の 17〜28 倍」という比は active な根拠から外す** — **2 mm は別の量（成功距離の閾値）**であり、**この比は接触誤差も bar の倍率も違反も確立しない**。以後この比を書く場合は **引き算だけ・誤差や bar を導かない**と明記する。②**旧「差が効くのは どの点で Jacobian を取るか（＝関節 bar が変わる）まで」も narrow**: 正しくは **「各参照点で **evidence-grade の Jacobian**（＝**方向つき 3 成分 ＋ 認可 envelope**）**を測る必要がある。差の量・方向は UNMEASURED**」（⭐**B15**: **proxy = 現行 H-4 の縮約 ＋ 1 姿勢の値は測定済**・⛔ そこから bar を導かない）**。
- **sizing への影響**: **参照点を変えると並進 Jacobian は変わり得る。⛔ ただしその方向・量・bar への帰結は UNVERIFIED**（**B15**: 現行 H-4 は **proxy として測定済**だが、**各列の最大絶対成分への縮約 ＋ 1 姿勢**であり、**方向つき 3 成分 Jacobian でも envelope の証拠でもない**）。⇒ **H-4 を 3 点で出させる**（spec §H-4.1: J-a 閾値の点 0.220 / J-b pad body / J-c 爪先 0.2757）。⭐ **sizing 点についての本 doc の立場（B12 に統一）**: **保持する原則 = sizing 点は、将来 authority が確定する成功評価の点と一致させる。** ⇒ **「J-a で sizing する」は conditional proposal**（＝ **authority が確定する点が J-a だった場合**）であり、⛔ **現行の採択ではない**。⛔ **点の選択・値・owner を本 doc は選ばない**（成功述語の測定面 = p5 court／定数そのものは UNCONFIRMED / HOLD）。
- **task への影響**: 目標も同じ 0.220 で作られていれば**腕と目標の一致には相殺**する。⛔⛔ **旧文「cable との接触は物理の世界で起きるので相殺しない」は撤回**（B6・**接触面が UNMEASURED** ゆえ、相殺しない根拠として接触を持ち出せない）。⇒ 言えるのは **「判定式が使う点」と「別ラベルの参照点」が 34.8〜55.7 mm 離れている**ことまで。⇒ **どの点で閾値を評価すべきかの court** は **`fe80839219` §3 で更新済**（**(A) success 述語の測定面 = p5** ／ **(B)(C) 定数自体 = UNCONFIRMED / HOLD**）。⛔ 旧「p5/Rs の court」は stale。
- ⛔⛔ **訂正（2026-07-26・pN `MSG-PN-P11-FINGERTIP-B6-LIVEWORD-20260726-005`）**: 旧文「**live** の acquire-grasp 成功判定が `compute_clamp_pos` を呼ぶ」は **p5 帰属**・「**p5 に live consumer の file:line を照会**」は **両方とも撤回**。**p5 が既にこの区別を閉じている。** ⇒ ⭐ **正しい書き方は 2 段**:
  - ✅ **呼び出しの連鎖は banked source に実在する**（immutable）: `newton_skill_env_base.py:899-905` `compute_clamp_pos` → `newton_grip_env.py:1158`/`:1164` → `:1167` `cable_pos` → `:1169` `find_nearest_cable_point` → `:1224` の閾値比較（`CLAMP_DIST_THRESH = T_DIST`・`:223`）。
  - ⛔ **runtime / production で到達するかは UNVERIFIED。** **source に在ること ≠ 実行時に効いていること。**
  - ⇒ ⛔ **「live consumer」を未決の事実としても、依頼としても残さない。p5 への新規照会は出さない。**
- ⛔⛔ **撤回（pN **B12**）**: 旧文「⭐ **本設計はこの未確認に依存しない** — H-4 を **3 点で出せば、どの点でも bar を立てられる**」。**現行 H-4 の 3 点の値は「各列の最大絶対成分」への縮約 ＋ 1 姿勢**であり、**これだけではどの点にも evidence-grade の bar を立てられない**。⇒ **方向つき 3 成分 Jacobian ＋ 認可された envelope が揃うまで HOLD。**
- ⭐ **保持する原則はこれだけ**: **sizing に使う点は、将来 authority が確定する「成功評価の点」と一致していなければならない。**（別の点で bar を立てると、満たしても判定は落ちる／その逆。）⛔ **どの点がそれかを本 doc は選ばない。**

## 5.5 ⭐ H-2 着地（p0 `c16858c666`・p4 検証済・**p11 が report json を自分で読んだ**）

⛔⛔ **本表の scope（pN **B22**・B16 の伝播是正）**: 以下の `ζ` / `T_lag` の結論（**過減衰・単調枝・行き過ぎ無し・節での誤判定無し・縮約が重要でない**）は、**宣言された free-arm / 暫定の測定状態にのみ scope する。** ⛔ **task 全体へ一般化しない** — **cable を掴んだたわむ状態＋接触の下では、effective `M` / `c` / `K` および `ζ` と根の方向・量は UNVERIFIED**（B16。掴んだ負荷が `M` に半正定値で加わり `λ_max(M)` が単調増加する、という仮定を私は接触下で検証していない）。

| 出力 | 実測値 | 設計への帰結（⚠ **すべて上記 scope 内**） |
|---|---|---|
| DOF 予測の照合 | `expected_by_p11 {arm 12, driver 4, passive 12, arm_side_total 28}` **== measured**（+ `cable_or_other 40`）・`mismatch {}`・`STOP_required false` | ✅ **§H-2.1 の宣言は実測と一致**。名前解決は `solver.mj_model + mj_id2name`（Newton Model に `joint_key` が無い build） |
| `ζ_min`（連成・(i) 固定 12×12） | **2.857**（⭐**過減衰 ζ > 1**）。3 縮約が **0.023% 一致**（⚠ p0/pZ の N1 指摘で 0.02%→**0.023%** に精度訂正・p11 が再計算 = 0.022527%） | ⭐**§5.2 の dwell は単調枝**（`t_dwell ≥ k·T_lag`・**行き過ぎ無し**・節での誤判定も起きない）。⭐**縮約の選択は非重要**と実測で確定（(i) が最小＝保守側で正しい） |
| 対角近似 `ζ_min` | **3.160**（連成 2.857 より **約 11% 楽観**） | ⛔ **対角近似を bar に使わない**（併記の理由がこれ） |
| ⭐`T_lag` の妥当性 | ζ > 1 ⇒ **過減衰**。過減衰系の遅い根は `≈ −ke/kd` | ⭐ **`T_lag = kd/ke` は便宜的近似ではなく、実測された過減衰系の支配根**。§5.2 の `err ≈ T_lag·ω` はこの上に立つ |
| `max|τ_bias|`（重力＋Coriolis） | **27.22 N·m** | ⭐ **gain 下限 `ke ≥ 27.22 / ε_joint`**。⛔ **訂正（B18）**: `ε_joint` が待っているのは **将来 authority が確定する success-evaluation point の evidence-grade Jacobian**（方向つき 3 成分 ＋ 認可 envelope）。**`J-a` は conditional** であって active に固定しない |
| `λ_max(M)` | 2.393 | `a_max` の分母 |
| ⛔⛔ `cap_used` | **1e6**（`cap_source = H-6a measured value`） | ⛔ **効力上限が実在しない（fail-open）**ことが**測定で確定**。⇒ `a_max = 417,881 rad/s²` は**算術は正しいが物理的に無意味**。⛔ **加速度余裕として使わない** |

### 5.5.A ⛔⛔ **上表の数値はすべて暫定** — 測った範囲が設計の動作範囲ではない（p11 実測・2026-07-26）

`h1_envelope` を読むと（**p0 が正直に flag 済**）:
| 項目 | 実測値 |
|---|---|
| `status` | **`PROVISIONAL`** |
| `reason` | 逐語「**W-b waypoints undetermined; re-run is required once they are fixed**」 |
| 掃引 | `lower_rad −0.5` / `upper_rad +0.5` / `step_rad 0.25` / `samples_per_joint 5` — **1 関節ずつ ±0.5 rad（±28.6°）** |
| `grasp_state_included` | **`False`**。⛔⛔ **fence（pN **B34**）**: 同 cell の逐語「inertia is therefore on the LOW side, which makes damping ratios **OPTIMISTIC**」は **producer（p0）の assertion であり、私の active な結論ではない**。⭐ **私の現 state = UNVERIFIED** — **掴んだたわむ cable ＋ 接触の下では effective `M` / `c` / `K` と `ζ` の方向・量は未確定**（B16 / B22 / B23 / B25）。⇒ **逐語は記録として残すが、根拠として引かない。** |
| `validity` | 逐語「valid **INSIDE it only**. Outside is **UNMEASURED (not safe)**」 |

⚠⚠ **訂正（landing commit `64021225038f61d088d52077a353bad19047da70` = 2026-07-26T15:17:18+0900・`908ac4674576c3b936fe66866254d17691b6cc8e` の report を実読）— 実際はもっと狭い。** 上表の箱は **宣言された掃引意図**であって、H-3/H-4 が覆った範囲ではない:
| 量 | 実際の被覆 | 出所 |
|---|---|---|
| `τ_bias 27.22 N·m` | ⛔ **単一姿勢**（`pose_coverage.measured_at` 逐語「the **single pose/velocity** the env is in after construction」・`envelope_max_implemented: false`・逐語「max over **JOINTS at this state**, NOT the max over the envelope」） | report `h3_torque_budget` |
| `J 649/224 mm·rad⁻¹` | ⛔ **単一姿勢**（逐語「the single pose … (**P0 lift-point**)」） | report `h4_grasp_jacobian` |
| `ζ 2.857` | ⛔ **単一姿勢と読む**（report に pose 欄は無いが、`raw_matrices_arm_side` の `M`/`K_e`/`K_d` が **28×28 が 1 組**であり姿勢の列を持たない ⇒ 構造からの推定。⚠ **report が明言していない点は明記する**） | report `h2_modal_damping` |

⇒ ⛔ **`27.22` も `ζ 2.857` も `J` も、route の動作範囲を覆っていない（1 姿勢の標本）。** ⛔ **設計確定値として使わない。** ⭐ p0 の逐語 = 「These numbers are **samples, not bounds**」。
⇒ ⭐ **数値の覆いが足りない要因が 3 つある**: ①**箱が狭い**（伸びた姿勢の重力トルク・慣性を見ていない）②**把持慣性を含まない**（§5.5.0）③**効力上限なし**（§5.5.1）。
⛔⛔ **訂正（pN **B25**）**: 旧文の **「楽観方向の要因が 3 つ重なっている（すべて同じ向き）」**、および続く **「厳しくなることはあっても緩くならない」** は **無条件の方向の断定ゆえ撤回**。**②は B16 の current fact（掴んだたわむ cable ＋ 接触の下では effective `M` / `c` / `K` と `ζ` の方向・量が UNVERIFIED）と競合する** ⇒ **②を「楽観側」と数えられない**。⇒ ⭐ **言えるのは「3 つとも覆いの欠落である」**まで。
⛔⛔ **整合訂正（pN **B35**）**: 旧文の「**①③については向きが言えても**」は、直後の「**向きについて何も結論しない**」と**隣接して矛盾**していた。⇒ ⭐ **1 つの整合した state に揃える**:
- **①（箱が狭い）**: ⭐ **これは方向でなく「境界の関係」として述べる** — **狭い箱で採った最大値は、それを含むより広い箱の最大値以下**（部分集合の最大の性質）。⇒ **報告値は広い箱の最大の下界**。⛔ **ただし「だから要求が厳しくなる」までは言わない**（要求がその最大に単調かは別の前提）。
- **②（把持慣性を含まない）**: ⛔ **方向・量とも UNVERIFIED**（B16）。
- **③（効力上限なし）**: ⛔ **方向は UNVERIFIED** — cap の意味論（下げれば必ず不利になるか）を私は検証していない。
- ⇒ ⛔⛔ **current state（本節で唯一の結論）: 3 要因を合成した task / requirement 上の全体方向は、結論しない。**
📎 **削除の記録（pN **B29**・⛔ ここに current state を重ねて書かない = **B35** 精密化）**: 撤回文の直後に置いていた **「⇒ 現状の数値は『この向きに厳しくなることはあっても、緩くなることはない』と読む」** を **削除した** — **撤回したはずの結論を、同じ行の末尾で active に再導入していた**ため。⇒ **current state は直上の 1 行のみ**（重複させない）。
⇒ **W-b の waypoint が決まり次第 再測定が要る**（p0 が既にそう書いている）。⚠ ただし **§5.5.B の構造的関係は envelope に依存しない**（下記）。

### 5.5.B ⭐⭐ 構造の発見 — **減衰は剛性に比例している**（p11 が生行列から実測・2026-07-26）

report の生 28×28 から実測（`raw_matrices_arm_side`）:
- `K_e`・`K_d` とも **完全な対角**（非対角の最大絶対値 = **0.0**）。腕 12 本の `ke` = size3 **2000** ×3/腕・size1 **500** ×3/腕、`kd` = **400** / **100**。
- ⇒ ⭐ **`K_d = 0.2 · K_e` が 12 本すべてで厳密に成立**（`|K_d − 0.2 K_e|` の最大 = **0.0**）＝ **比例減衰**。
- ⇒ **`ζ = (kd/2ke)·ω_n` が厳密式**（`K_e v = μ M v` の `ω_n = √μ`）。**検算**: 私が `(c/2)√μ_min` を独立に解くと **2.8571914109737504**、report の bar 値 **2.8571914109737273** ⇒ **13 桁一致**。`ω_n,min = 28.572 rad/s`。
- ⇒ **3 縮約が 0.023% で一致した理由がこれ**（比例減衰では modal 分解が素直になる）。

⭐ **設計に効く帰結 — ゲインの動かし方が 2 つあり、`ζ` への効き方が逆**（`ζ ∝ kd/√ke`・`T_lag = kd/ke`）:

| 動かし方 | たわみ | 遅れ `T_lag` | 減衰 `ζ` |
|---|---|---|---|
| **`ke` だけ β 倍**（§5.4 のたわみ対策） | **1/β** | **1/β（改善）** | ⛔ **1/√β（悪化）** |
| **`ke` と `kd` を同時に β 倍** | **1/β** | **不変（0.2 s のまま）** | ✅ **√β（改善）** |

⇒ ⭐⭐ **たわみ対策と「過減衰である」という前提は、同じ予算を食い合う**。
⛔⛔ **境界（B23・B16 / B22 と同一）**: **以下の枝条件は「仮定つきの条件付き注記」であり、active な task 判定ではない。** 仮定 = **①減衰と剛性が不変 ②把持による付加負荷が effective `M` に半正定値として加算される**（⇒ `λ_max` が単調増加）等。**私はこれらを たわむ cable ＋ 接触の下で検証していない。** ⇒ ⛔ **`8.16` を、仮定の検証なしに task の結論・枝の確定として引かない。**
📎 その仮定の下でなら、単調枝が保たれる条件は
> **β · α < ζ_free² = 8.16**  （`β` = `ke` の倍率〔`kd` 据え置きの場合〕、`α` = 把持による**最悪方向の**慣性増加率 = `1 + λ_max(M⁻¹ΔM)`）

⚠ **`α` の厳密な形**は最小固有値の min-max 評価から `ζ_new ≥ ζ_old/√α` として出る（一様増加なら単純に `α` 倍）。⇒ **§5.5.0 の 8.16 倍は「最悪方向の増加率」と読む**（この節で厳密化）。⚠ **これも上記の仮定の下でのみ成り立つ。**
⚠ **これは禁止条件ではなく枝の条件**。超えたら振動側へ移り、`t_dwell` の式が包絡形に変わる（§5.5.0）。⛔ ただし **接触が効く瞬間の行き過ぎ**は task に直に効くので、超えるなら意図して超える。
⛔⛔ **境界（B22 / B23）**: **どちらの枝に居るかは、宣言された free-arm 状態でしか確かめていない。** **掴んだたわむ cable ＋ 接触の下では `ζ`・根の方向と量が UNVERIFIED** ⇒ **把持 phase の `t_dwell` を自由腕の枝判定から決めない**（下記 §5.5.0 の (a) / (b) で押さえる）。

### 5.5.C ⚠ たわみの大きさ — **どちらの関節に載っても、旧 5 mrad は超える**（算術のみ）

`Δq = τ_bias/ke` に実測値を入れる（⚠ **どの関節が 27.22 を負うかは未測**ゆえ両方を出す）:
| 27.22 N·m が載る関節 | `ke` | 静的たわみ |
|---|---|---|
| size3（肩・肘） | 2000 | **13.6 mrad** |
| size1（手首） | 500 | **54.4 mrad** |

⇒ ⭐ **どちらでも、旧 L-P1 の連続 5 mrad bar を 2.7〜10.9 倍 超える。** ⚠ ただし旧 bar は task 要求ではない（§5.2）。**task に効くのは手先 [mm]** であり、それには **per-joint `τ_bias`（spec §H-3.1 で要求）× H-4 の evidence-grade Jacobian** が要る。⛔ **訂正（B18）**: 旧文は **`J-a` を active に固定していた**ので撤回 — 要るのは **将来 authority が確定する success-evaluation point の Jacobian**（方向つき 3 成分 ＋ 認可 envelope）であり、**`J-a` は conditional**。⛔ **手先 mm をここで見積もらない**（Jacobian 無しに mm を書くのは v0.3 の失敗そのもの。**proxy から合成するのも同じ失敗** = B15）。
⛔⛔ **RETRACTED / HISTORICAL — その場で fence（pN **B20**）。以下 1 段落は根拠を失っており、根拠として引かないこと。**
> 📎 旧記載: 「⇒ ⭐ **予測（反証可能・H-3.1 + H-4 で決まる）**: 2 mm 閾値に対し `ke` の引き上げが要る公算が高く、その倍率は **§5.5.B の 8.16 予算と同じ桁**になり得る。⇒ **衝突するなら「`ke` と `kd` を同時に上げる」枝**が候補。」
>
> ⛔ **撤回理由**: この予測は **手先 [mm] を 2 mm と比べられること**を前提にしていたが、**その合成量は未実装・未認可**であり、**現行 H-4 は proxy（各列を最大絶対成分に縮約 ＋ 1 姿勢）**にすぎない（B15）。⇒ **前提が無い。**
⭐ **現在の state**: **方向つき 3 成分 Jacobian による合成が揃うまで、2 mm との比較・必要な引き上げ倍率・どの枝を採るかは すべて UNVERIFIED / HOLD。** ⛔ **測る前に採らない。**

#### 5.5.C-2 ⭐ H-4 着地（`908ac4674576c3b936fe66866254d17691b6cc8e`）— **proxy 3 点が着地した**（⚠ 1 姿勢・pairing 未確定）

⛔ **見出しの訂正（pN **B27**）**: 旧見出し「**予測は当たった側**」は撤回。**直前 §5.5.A で予測そのものを `RETRACTED / HISTORICAL` に落としている**ため矛盾していた。⇒ **本節は「3 参照点の proxy 値が出た」という事実のみを述べる。⛔ 予測の的中は主張しない。**

p0 実測（`h4_grasp_jacobian`・SSOT index 解決・**名前探索でない**）:
| 参照点 | 最大並進 [mm/rad] arm0 / arm1 |
|---|---|
| **J-a**（閾値の点 `ee + 0.220·ẑ`） | **649.34 / 224.26** |
| **J-b**（pad body・**ラベルの付いた参照点**。⛔ 旧「＝実際に触れる所」は撤回 = B6） | 529.27 / 310.65 |
| **J-c**（実測 爪先 `0.2757`） | 705.09 / 275.75 |

⛔⛔ **撤回（pN **B19**・私の再計算で確認）**: 旧文「**J-a と J-b の上表の値は両腕とも約 127〜133 mm/rad 違う**」は **上表と一致しない**。**上表からの引き算 = arm0 `|649.34 − 529.27| = 120.07` ／ arm1 `|224.26 − 310.65| = 86.39` mm/rad**（⇒ **両腕で同程度ではない**）。⛔⛔ **さらに訂正（pN **B28**・私の実測で確認）**: 直前に書いた「**`127〜133` の出所を特定できなかった／`arm_control_measurement_h2_report.json` にも無い**」は **FALSE。撤回する。** ⚠ **原因 = 私の探索が `head -3` で切り詰められていた**（**切り詰めた出力から「無い」を主張した** ＝ 本 arc の B2 と同型の再発）。
⭐ **実在する（私が commit `7223219f3e3d11b9b16df209b6a5a347ff3185b9` 上で確認）**: `arm_control_measurement_h2_report.json` の **`.h4_grasp_jacobian.value.arms[0].J_a_minus_J_b_max_mm_per_rad = 126.61949725828234`（同 commit `:7020`）／`arms[1] = 133.08344289986846`（同 `:7497`）**。**生成源 = `arm_control_measurement_harness.py:969` 逐語 `float((np.abs(ja_p) - np.abs(jb_p)).max() * 1000.0)`**。
⇒ ⭐⭐ **2 つの量を混同しない**:
- **`126.619 / 133.083`** = **別の proxy**。**各成分の絶対値を取ってから引き、その最大**を取る（⛔ `|ja_p − jb_p|` ではない）⇒ **方向と符号を失う**。
- **`120.07 / 86.39`** = **上表の `max_translation_mm_per_rad` どうしの単純な引き算**。
⛔⛔ **旧文「上表の値の差が 127〜133」は、この 2 つを取り違えていたので撤回を維持する。** ⛔ **どちらからも bar や誤差の結論を出さない**（両方とも lossy proxy・B15）。⛔⛔ **訂正（2026-07-26・pN **B8**）**: 旧文「⇒ **どの点で閾値を評価するかで関節 bar が変わる**という §5.4.2 の懸念は**数値で確認された**」は **撤回**。**この表の量は bar の変化を確立しない** — `:234` のとおり `max_translation_mm_per_rad` は **各列を「最大絶対成分」1 個に潰した値で方向と符号を捨てている**ため、**感度を忠実に表していない**。⇒ **bar の変化量も方向も未確立。** 併せて **1 姿勢のみ・pairing 未確定・H-4 全体 HOLD**。⚠ **J-a/J-c は EE body 上ゆえ `gripper_dof_contribution` を含まない**（p0 逐語）。⛔⛔ **訂正（pN **B10**）**: 旧文の「**感度は過小側**」は **方向の断定ゆえ撤回** — **task に効く実際の点が UNMEASURED** で、**vector の方向・符号・pairing も未確定**である以上、**過小とも過大とも導けない**。⇒ **正しくは「gripper DOF の寄与を含まない。task に効く感度に対して過小か過大かは UNVERIFIED」**。⚠ **p0 の逐語が出所でも、私の active な根拠として再伝播しない。**
📎 **経緯（HISTORICAL・pN **B36** で決着済）**: 私は B8 の「別参照点の Jacobian は未測」という文言に対し、「**私の banked 記録では 3 点とも数値が出ている**（`908ac4674576c3b936fe66866254d17691b6cc8e`）」と surface した。
⛔⛔ **stale ask の撤回（B36）**: 旧文末尾の「⇒ **どちらの言い方を採るかは pN の裁定に従う**」は **撤回する** — **B15 で既に決着している**（未決の依頼として残さない）。
⭐ **決着した現 state（B15）**: ✅ **proxy は測定済** = 現行 H-4 の 3 点の値（**各列を最大絶対成分に縮約 ＋ 1 姿勢**）／⛔ **未測は evidence-grade** = **方向つき 3 成分 Jacobian ＋ 認可された envelope**。⇒ ⛔ **proxy の存在を消さない／proxy から bar への帰結を導かない。**

⛔⛔ **RETRACTED（landing commit `0f373bedbdfa25d361917e50036e11947b146bdf` = 2026-07-26T16:08:16+0900・pN の B2 / p0 の R9 を受理）— 本節の「手先たわみ [mm]」は撤回する。**
- **理由（p0 の自測・`P0_ROUTING_SUBMISSION_H4_H31_20260726.md` R9）**: 出力の `max_translation_mm_per_rad` は `_summarise`（`arm_control_measurement_harness.py:938`）が **各 Jacobian 列を「x/y/z のうち最大の絶対成分」1 個に潰した値**であり、**方向と符号を捨てている**。⇒ これに `|Δq_i|` を掛けた量は **手先変位ではない**。**上界でも下界でもない**（列ノルムより小さい成分を使い、かつ符号相殺を無視して和を取るため、過小と過大が混在する）。
- ⇒ **撤回する記述** = 「`13.6 mrad × 0.649 ≈ 8.8 mm`」「`54.4 mrad × 0.224 ≈ 12.2 mm`」「**2 mm 閾値の 4〜6 倍**」、および §5.5.C-2 の `14.594 / 4.074 / 8.838 / 2.000 mm` と「**両腕とも 2 mm 超過**」。
- ⚠⚠ **さらに私自身の規則違反**: 本節は 2 行上で「⛔ **手先 mm をここで見積もらない**（Jacobian 無しに mm を書くのは v0.3 の失敗）」と書きながら、**Jacobian を得た途端に、その Jacobian が何を表すか確かめずに mm を書いた**。⇒ 規則は「Jacobian が在るか」ではなく「**その量が手先変位を表しているか**」で立てるべきだった。
- ✅ **撤回しないもの**（Jacobian を経由しない量）: `τ_bias` の最大 **27.22 N·m が `shoulder_lift`（`ke = 2000`）に載る**こと、および **`Δq = 13.61 mrad`**（`h3_torque_budget.per_joint_H3_1`・関節空間の量）。
- ⇒ **正しい形（p0 の提案・⛔ 未実装・authorization 待ち）**: 3-vector を保って `Δx = Σ_i jacp[:,i]·Δq_i` を合成し `‖Δx‖` を取る。上界が要るなら `Σ_i ‖jacp[:,i]‖·|Δq_i|`。⇒ **これが出るまで、たわみが 2 mm 閾値に対してどうかは未確定。**
⛔⛔ **訂正（2026-07-26T16:08:16+0900 の `0f373bedbd` 以降・pN の C3 を受理）**: 旧記載「①**どの関節が 27.22 を負うか未測**（H-3.1 待ち）」は **stale かつ自己矛盾**だった — **同じ節の 2 行上で「27.22 は `shoulder_lift`（`ke`=2000）に載る」と実測済**と書いている。⇒ **①を撤回。**
⇒ ⭐ **現在ほんとうに未確定なのは 1 つだけ**: **合成された手先変位 `‖Δx‖ = ‖Σ_i jacp[:,i]·Δq_i‖`（3-vector で合成した値）と、それが 2 mm bar に対してどうか**。⚠ **符号相殺があるため、関節ごとの寄与を足しても決まらない**（§5.5.C-2 の RETRACTED を参照）。⛔ **この 1 点が出るまで、たわみが bar を超えるとも超えないとも言わない。**
⛔⛔ **RETRACTED（同上）— 「`ke` を 5 倍程度」「予算 8.16 と同じ桁で衝突する」も撤回する。** これらは**撤回した mm 値から導いた**ため、根拠を失った。⇒ **`ke` の必要倍率は現時点で未確定**であり、**衝突するか否かも未確定**。
- ✅ **撤回しない構造**（mm に依存しない）: §5.5.B の関係式（`ke` だけ上げると `ζ ∝ 1/√β`・`ke`+`kd` 同時なら `T_lag` 不変で `ζ ∝ √β`）。⛔⛔ **ただし scope 訂正（B25）**: 併記していた **単調枝の条件 `β·α < ζ_free²` は「撤回しない構造」ではない** — **`α`（把持による慣性増加率）が未検証仮定に依存する**（`c`/`K` 不変・`ΔM` が半正定値）。⇒ **枝条件は仮定つきの条件付きモデルとしてのみ保持**し、**active な task 判定に使わない**（B16 / B23）。⇒ **どちらの枝を採るかの判断材料は生きているが、「どれだけ上げる必要があるか」の入力が無い。**
- ⇒ **必要なもの** = 上記の **正しい合成（`‖Σ_i jacp[:,i]·Δq_i‖`）**。⛔ **それが出るまで枝を選ばない。**

### 5.5.D ⭐⭐ 掃引をやめて **上界で押さえる**（p0 の照会への設計回答・2026-07-26）

p0 逐語の照会: 「A full grid is not tractable — **5 samples on each of 12 arm joints is 5^12 poses** — and the choice of sweep changes the answer, so it is **p11's to make**」。⇒ **私の court。回答 = 格子で標本を採るのをやめ、①解析上界 ②方向づけ最大化 ③実 waypoint 評価 の 3 本立てにする。**

#### (1) ⭐ `ζ` は **掃引不要** — 1 個のスカラーに落ちる（p11 が実測で検証）

§5.5.B の比例減衰（`K_d = c·K_e`, `c = 0.2`）と一般化固有値の min-max から:
> `ζ_min = (c/2)·√μ_min` かつ `μ_min ≥ λ_min(K_e)/λ_max(M)` ⇒ **`ζ_min ≥ (c/2)·√(λ_min(K_e)/λ_max(M))`**
> ⇒ ⭐ **`ζ_min ≥ 1` の十分条件 = `λ_max(M) ≤ λ_min(K_e)·c²/4` = `500 × 0.01` = **5.0 kg·m²****

**検算（p11 が report の生行列で実行）**: `μ_min = 816.354` / 下界 `λ_min(K_e)/λ_max(M) = 208.946` ⇒ **不等式成立**。`ζ` 実値 2.857 / 下界 **1.445**（下界は約 2 倍ゆるい＝保守側）。`λ_max(M)` を私が独立に解くと **2.392958**（report 値 2.3929582 と一致）。
⇒ ⭐⭐ **「姿勢を掃く」問題が「`λ_max(M)` の最大値 1 個を求める」問題に変わる。**
- ⚠⚠ **訂正（landing commit `e7eaccbdcaddd88f80ae62333b8ddd6859a5dcbd` = 2026-07-26T15:48:15+0900・pN の R5 指摘を受理）**: 初版は「`λ_max(M)` は腕を伸ばした姿勢で最大（数点で確認可能）」と書いた。⛔ **これは自分の spec `…HARNESS_SPEC…:95` 逐語「手で選んだ数姿勢を『最大』と呼ばない。掃引は格子または最適化で行う」に反する** — 私が書いた規則を同じ息で破っていた（v0.3 の失敗の型）。⇒ **降格**: 「伸展姿勢が最大」は **characterization（見立て）**であって根拠ではない。
  ⇒ ⭐ **代わりに 2 段で `λ_max(M)` を押さえる**（どちらも姿勢の恣意選択を含まない）:
  - **(a) `q` に依存しない解析上界**: 剛体連鎖では `M(q) = Σ_j (m_j J_{v,j}ᵀJ_{v,j} + J_{ω,j}ᵀ I_j J_{ω,j})` ゆえ、**質量・慣性・最大モーメントアーム**から `λ_max(M) ≤ Σ_j (m_j r_{j,max}² + λ_max(I_j)·n_j)` の形で **`q` によらない真の上界**が計算できる（ゆるいが**掃引不要で保証になる**）。⇒ **これが 5.0 kg·m² を下回れば、それだけで `ζ ≥ 1` が全姿勢で保証される。**
  - **(b) 数値最大化**（多点初期値の局所最適化）で上界を締める。⚠ **最大化が返すのは真の最大の下界** ⇒ そう明記する。
  ⇒ **いずれにせよ `5^12` の格子は不要**（結論は変わらない）。⛔ **ただし「数点を見て最大と呼ぶ」経路は使わない。**
- ⭐ **把持慣性（§5.5.0 の `α`）も同じスカラーで扱える** — `M → M + ΔM` でも判定は `λ_max` 一つ。⛔⛔ **条件（B25）**: これは **`ΔM` が半正定値として effective `M` に加算され、`c` / `K` が不変**という**未検証の仮定**の上でのみ成り立つ。**たわむ cable ＋ 接触では私は検証していない** ⇒ **条件付きモデルとしてのみ扱う。**
- **現姿勢からの余裕**: 保証つき **×2.09**（`5.0 / 2.393`）／一様増加を仮定した場合 **×8.16**（= `ζ²`）。⛔⛔ **訂正（B25）**: 旧文 **「⇒ 真の余裕はこの間」は撤回** — **上端の `8.16` は上記の未検証仮定に依存**するため、**区間として「真の余裕」を主張できない**。⇒ ⭐ **言えるのは「`×2.09`」まで**。⛔⛔ **その scope（B30）**: **`×2.09` が保証なのは「宣言された free-arm / 現姿勢のスカラーモデルの中だけ」**（`λ_max(M) = 2.393` に対する `5.0` の余裕）。⛔ **掴んだ cable に対する保証や「真の余裕」としては使わない** — **grasped 側は `M` / `c` / `K` / `ζ` とも UNVERIFIED**（B16）。⛔ **`8.16` を保証値としても区間の上端としても引かない。**

#### (2) `τ_bias` は **最大化**（格子でなく）＋ 解析上界を併記
- **方向づけ最大化**: 関節ごとに `|τ_bias,i(q)|` を **多点初期値からの局所最大化**で求める（12 個の小問題・伸展/水平姿勢を種にする）。⚠ **最大化が返すのは真の最大の *下界***（見つけた中の最大）⇒ **そう明記する**。
- **解析上界**: 重力項は `|τ_grav,i| ≤ g·Σ_{下流 link j} m_j·r_max,ij`（質量とリンク長から計算・**真の上界**・ゆるい）。⇒ **上界と下界を併記**すれば、掃引の恣意性なしに範囲が閉じる。
- Coriolis 項は `q̇²` に比例 ⇒ **設計上の最大指令速度**で押さえる（§5.2 の速度制限と同じ量）。
- ⛔ **「格子で採った標本」を最大と呼ばない**（v0.3 の失敗）。

#### (3) W-b の waypoint が出たら、**その点集合で評価する**
⇒ 運用上意味があるのは **実際に通る姿勢**であり、点数も小さい。⇒ (1)(2) は **waypoint 確定前でも進められる保証**、(3) は**確定後の実値**。

⚠ **「W-b」= UNRESOLVED（⛔ UNDEFINED ではない・2026-07-26 訂正 / pN の R8・B5 を受理）**:
- ✅ **概念は定義済** = 「**振付の再工事**」（`ARM_CONTROL_PD1_PREREG_V14_WB_DRAFT_ARMCONTROLDESIGN_20260721.md:12` 逐語「その振付は Rs 裁定 B（振付＝再工事 W-b）で作り直す対象」）。
- ⛔ **未解決 = operative な waypoint 集合 と owner**（私はこれを定める artifact を特定できていない）。
⇒ **(3) は「waypoint 集合と owner が確定するまで着手不能」**。⛔ 私は W-b を定義しない（振付の owner 課題）。⚠ 旧記載「UNDEFINED TERM・定義 artifact が見当たらない」は**言い過ぎ**ゆえ撤回。

### 5.5.0 ⚠⚠ pZ caveat の反映（2026-07-21 21:57・**設計を条件付きにする**）

**N5（効く）**: `ζ = 2.857` は **`M` に把持中の cable 慣性を含まない「自由腕」の値**（cable は接触で結合し `M` には現れない — §H-2.3 の前提そのもの）。⛔ **§5.2 の「単調枝で確定」は自由腕限定**であり、把持中の phase には**そのままでは適用できない**。

⛔⛔ **訂正（pN **B23**・**B16 / B22 と同一境界へ narrow**）**: 旧文の **「⇒ 把持状態では実効慣性が増え ζ は下がる＝楽観側」** と、下の **「実効慣性増加 < 8.16 倍 なら単調枝は保たれる」** は、**いずれも無条件の断定だったので active な task 主張としては撤回する。**
⭐ **現在の state**: **掴んだたわむ cable ＋ 接触の下では、effective `M` / `c` / `K` と `ζ`・根の 方向と量は UNVERIFIED。** ⛔ **「増える」「下がる」「楽観側」を無条件に書かない。**

📎 **条件付き注記（仮定を明示した場合にのみ保持・⛔ active な判定に使わない）**: 下の関係が成り立つのは **①減衰と剛性が不変（`c` / `K` が変わらない） ②把持による付加負荷が effective `M` に半正定値として加算され `λ_max(M)` が単調増加する** 等の仮定の下だけであり、**私はこれらを たわむ cable ＋ 接触の下で検証していない**（接触を介した結合は `c` や `K` も変え得る）。
> その仮定の下でなら: `ζ ∝ M^(−1/2)` ゆえ、把持で ζ が 1 を下回るには **実効慣性が `ζ_free² = 2.857² ≈ 8.16 倍`** に増える必要がある ⇒ 「増加 < 8.16 倍なら単調枝」。
⇒ ⛔ **この 8.16 の判定を、仮定の検証なしに task の結論として引かない。** ⛔ 「たぶん小さい」でも済ませない。

⭐ **設計側の安全化（測定を待たずに効く）**: **静定の判定は常に窓で行う**（連続 N フレーム `|q−target| ≤ ε` かつ `|q̇| ≤ ε̇`・§5.2）。⭐ **窓判定は ζ の枝に依存せず正しい** — 振動していれば窓が埋まらないため。⇒ **枝が変わって壊れるのは `t_dwell` の見積り式だけで、静定の判定そのものは壊れない。**
⇒ 把持 phase の `t_dwell` は **(a) H-5 の把持 phase 測定** か **(b) 把持状態での小ステップ応答（行き過ぎ量から ζ を読む）** で押さえる。⛔ **自由腕の 2.857 を把持 phase の根拠に流用しない。**

**N2（表現）**: 対角近似 3.160 は bar（連成 2.857）より **+10.6% 非保守**。⇒ 既に bar から除外済（§5.5）。⚠ **引用する場合は必ず「非保守」と併記**する。

### 5.5.1 ⛔ fail-open な効力上限は **sim を非保守にする**（設計上の要処置）

実機 UR5e には関節トルク上限が在るが、**build 済モデルには無い**（1e6）。⇒ **sim の方が現実より容易**になり、**§15 の非保守方向**に当たる（保守方向の FAIL は確定的に扱えるが、非保守な PASS は転移前に追加確認が要る）。
⇒ **設計として実在の cap を課す**。⭐ **prior art をそのまま流用**: `task_config.py:316-318` 逐語「`GRIPPER_DRIVER_EFFORT_LIMIT_NM = 2.5` … **restores the force cap the tendon strip removed**」＋ 受入形「**one-frame post-clamp** `|qfrc_actuator| <= 2.5`」（rev7/rev8 で成立）。⇒ **腕にも同じ形**（定数の置き場所も受入形も既存に合わせる）。⚠ **腕の cap 値そのものは Rs / p4 court**（実機仕様の転記であり私が発明しない）。

### 5.5.2 ⏸ **HOLD → 解消**（landing commit `0f373bedbd` = 2026-07-26T16:08:16+0900・pN の B3 を受理）— H-4 の ABSENT は解消したが「仕様どおり」とは言えない

⭐ **実装は SSOT index で行われた**（p0 逐語 `resolution` = 「**SSOT index (stride 14, EE +5, pads +9/+13) — NOT name search**」）。

**⭐ 現在の状態（同期 landing commit `dda526b8a983b3e53654b71d56e2dd894992730e` = 2026-07-26T16:23:41+0900・pN の B2 を受理）**
| 項目 | 現況 |
|---|---|
| spec の pad body 導出規則 | ✅ **訂正済 = v1.7**（`…HARNESS_SPEC…:156` / `:175`）。**(i) SSOT 定数からの index 解決 ／ (ii) `shape_label` の親 body** の 2 規則・採った方を出力に明記。⛔ **`body_label` を "pad" で検索しない**と明記 |
| J-b の **設計/spec カテゴリ矛盾** | ✅ **解消** — 「`body_label` 検索 vs index 方式」という**方式の食い違いは無くなった**（v1.7 が index 方式を正規の (i) として定義） |
| J-b の **実装 conformance** | ⏸ ⛔**未確定 / HOLD**（⚠ 過剰主張を撤回）。**v1.7 の (i) は「定数を import して使う」ことを要求し literal 複製を禁じている**（spec `:158`）が、**現行 harness は literal `(9,13)`**（`:928`）。かつ **spec `:161` が現行実装の適合判定を未決と明記**。⇒ **方式が一致したことは、規則に適合したことを意味しない。** |
| harness の literal `(9,13)` | ⏸ **未認可のまま carry**（`arm_control_measurement_harness.py:928`・p0 R8 の自己申告）。⛔ **実装修正は私の court でなく、私は要求しない**（p4 court） |
| H-4 全体 | ⏸ **HOLD 継続**（pN 指示・私からの「仕様どおり／手戻り不要」判定は撤回したまま） |

📎 **HISTORICAL（2026-07-26T16:08:16 以前の状態・根拠には用いない）**: 当時 spec `:154`/`:168` は「`body_label` から発見すること」のままで、実装（index 方式）と矛盾していた。**cause-side = 私**（spec 未改訂）。⇒ **p4 の R8 裁定 `442f58678359bf85` が spec owner = 私へ RETURN したことで解錠され、`0f373bedbd` で訂正した。**
⚠ **裏取り（私の実測）**: `body_label` 70 件中 "pad" **0 件** ／ `shape_label` 107 件中 **16 件** ⇒ 旧指定は**現モデルで実行不能**だった。⇒ **p0 が `wrist_3` で代用せず ABSENT と報告した判断は正しい。**

### 5.5.2（原記録）⭐ H-4 が ABSENT だった件への指定（参照点の定義は設計側）

### 5.5.2（原文）⭐ H-4 が ABSENT の解消（**私の court** — 参照点の定義は設計側）

p0 報告 = 「`pad` を含む MuJoCo body 名が無い。pad は `shape_label` に付く（`newton_skill_env_base.py:1576-1583`）ので pad→body は Newton モデル側から供給が要る」。⛔ **`wrist_3_link` で代用しなかったのは正しい**（spec が禁止）。

⇒ **名前で探さず、SSOT の body index を使う**（⛔ 部分一致で探す実装にしない = §1.1.1 と同じ理由）:
| 参照点 | 解決方法 |
|---|---|
| **J-a**（閾値の点） | `ee_pos + R(ee_q)·[0,0,0.220]`。**EE body = 各腕の local index 5**（`task_config.py:30` `EE_BODY_IDX = 5`）。⇒ **名前解決 不要** |
| **J-b**（pad body） | ⭐ `task_config.py:37` **`GRIPPER_PAD_BODY_IDX = [9, 13]`**（**BODY space**・逐語「pad-carrying followers」）＋ 腕ストライド `:43` **`BODIES_PER_ARM = 14`**。⇒ world/arm ごとに `base + 9` / `base + 13`。⛔ 名前一致で探さない |
| **J-c**（爪先） | `ee_pos + R(ee_q)·[0,0,0.2757]`（`task_config.py:321` `EE_TO_PINCH_TIP_CLOSED`）。⇒ **名前解決 不要** |

⚠ **J-b は 2 body（左右の follower）**ゆえ、**どちらを取るか / 中点を取るか**を出力に明記すること（`task_config.py:320` の `EE_TO_PINCH_CLOSED` は **pinch_mid**＝中点基準の実測値ゆえ、**中点**が既存と整合）。

## 6. 数値（**測定待ち** — 決めない）

| 量 | 出所 |
|---|---|
| per-joint `T_lag = kd/ke`（as-built） | 測定ハーネス H-2（`K_e`/`K_d` 実測。⚠ 既知の vendor 値は size1 = 100/500・size3 = 400/2000 ＝ **どちらも 0.2 s** — as-built で照合する） |
| 到達可能な `ω`・加速度余裕 | H-3（`τ_bias` + `λ_max(M)`。cap は H-6a の実測値） |
| `ε_task`（着座許容） → 関節 bar | **H-5 の伝達比**（⛔ arm Jacobian で変換しない） |
| ζ（減衰） | H-2（§H-2.2 の (i) 固定縮約が bar） |
| `k`（静定待ち時間の係数） | 上記 `T_lag` と `ε_task` から決まる |

## 7. ✅ pin — **Rs 裁定で決着**（2026-07-26・**可否**＋**extent**。⛔機構は未決）

⭐⭐ **Rs 直接裁定（p4 が直接 witness・custody `P4_RS_RULINGS_20260726_PIN_AND_FINGERTIP.md` @ `5d87b3a1fbdf72bfb7718784c4b630b12c4a365f`・sha256 `66638471171e9a9e32de0e4a28ba45b2a976af6f3715c56b6cf535398dd98d72` = **p11 が独立照合し一致**）:**
> **逐語:「1 クリップのケーブル固定ではkinematicを残す。それ以外ではkinematic禁止」**

⇒ **disposition = clip 側のケーブル保持 pin は残る**（＝ **既存 §0#5 の再確認であって新規 rule ではない**）。⛔ **gripper の把持ではない。**
⇒ ⭐ **腕側の kinematic は禁止のまま** ⇒ **本設計（controller-driven）は変更不要・そのまま継続**。

### 7.1 ⭐⭐ 認可の **extent**（Rs 追加確認 2026-07-26・可否に続く 2 本目）

**custody = `P4_RS_PIN_EXTENT_CONSTRAINT_20260726.md` @ `8412ab652a76203505bb951e87ae409bd030377a`・sha256 `3662fcdc0cea86dc0b560996de4e65bd9bcd9dabd363e773230de0e3694ed1d4` = **p11 が独立照合し一致**。**受領文脈** = p4 が §7 の可否裁定を報告した直後の Rs 直接応答（「確認だが」＝ 理解の確認として発せられた）。**
> **逐語:「確認だがクリップでケーブルを固定しても固定点だけ動かなければいいだけで、ケーブル全体を固定かしないように」**

⇒ ⭐ **認可の範囲 = clip 着座の固定点 1 箇所のみ。⛔ ケーブル全体の固定は不可。** location = 既存の **clip-seat-only**。⛔ **機構は非導出**（下記と同じ）。
⇒ ⭐ **将来 pin を再実装・移植する際の受入条件**として本制約を持つ（＝ 可否だけでなく **extent** も満たすこと）。

⚠ **これは私が 2026-07-21 に §14.27 で報告した実測と一致する**: `_pp` は `ARM_Q = 28` 以降＝**ケーブル 40 節すべてを毎ステップ書き戻す**ため、**clip 側の保持より広い**。⇒ **extent 軸での私の読みは Rs 逐語で追認された。**
⛔ **ただし「だから equality 拘束が正しい」とは書かない** — 機構は依然として未決であり、本節から導出しない（下記）。

⛔⛔ **本裁定が定めたのは「可否」と「extent」だけであり、realize 機構は未決。** ⇒ **ここから equality 等の具体機構を導出しない**（2026-07-21 に私が casual な発話を機構規則へ昇格させ、Rs の disavow で全面 revert された件の再発防止）。⇒ **下記 P-a/P-b/P-c は「pin を消す場合の選択肢」であり、本裁定で pin が残る以上 moot。⛔ 機構の設計もしない。**

⚠ **本裁定が決めていないこと（私は決めない・要照会）**: **§2 の class B（reset 直後の再配置 `route_env:1096-1097`）**。逐語は p4 の列挙項目 **1 = pin** への応答であり、**class B は当該列挙に含まれていない**。現行規則は reset 時の初期化を明示的に許容している一方、逐語の「それ以外では kinematic 禁止」がそこまで及ぶかは**この文言だけでは決まらない**。⇒ **p4 へ照会中。⛔ どちらとも仮定しない。**

📎 **HISTORICAL（本裁定で SUPERSEDED・根拠に用いない）**: 2026-07-21 21:16 の p4 relay「Rs『**完全**』は pin を含む」を前提に、本節は以下の衝突を提起していた:

1. `RS71-System-Spec-SSOT.md:27` §0#5 = 「唯一の認可例外 = clip-retention pin」＋ **2026-07-15 Rs 決定**「クリップ**のみ** pin を RL env に恒久配線しろ」。
2. 同 §4 `:62`（Rs DECISION B2, 2026-06-25）= cable は **鉛直面のみ曲がる 1-DOF ベンダ**ゆえ**水平方向の配線曲率を表現できない** ⇒ **staggered clip 通しは kinematic**であり、⭐**pin の無い RL env は task を表現できない**。

📎 **（以下も HISTORICAL・pin が残る以上 moot）** ⇒ 当時の整理「pin を消すなら、次のどれかが必要」（いずれも私の court 外・**現在は採らない**）:
| | 案 | 影響 |
|---|---|---|
| P-a | 保持を**物理拘束**で実現（clip 形状・摩擦・接触で保持させる） | substrate/asset 側の設計。⚠ コ字 asset は human-LOCKED ⇒ 触れるなら Rs |
| P-b | **cable モデルを変更**（水平曲率を表現できる形へ） | §4:62 の前提そのものの変更 ＝ Rs 専権 |
| P-c | **task 表現を変更**（clip 通しの定義を変える） | SOMA/工程表側 ＝ Rs 専権 |

⭐ **本設計は pin の可否に依存しない**: §5 は**腕の駆動**のみを扱い、pin は保持機構として直交する。⇒ **pin 裁定を待たずに腕側を進められる**（p4 の「それまで削除方向で設計継続」と整合）。
⛔ **私は §0#5 を解釈しない。** 衝突の所在を示すのみ。

## 8. gate / 手続き

- **L 自己申告 = L3**（diff 内容キーワード `newton` / `ik` / `solver` 該当・制御方式の実装形変更）。⇒ `/rule-check stage1` は p4 court で確定。
- **設計 gate**（`/reward-design` は非該当・`/pre-check` は実装前に必須）＝ p4 が起動。
- ⚠ **重力補償は本設計に含めない**（制御則の変更 ＝ Rs 承認事項。未承認ゆえ入れない）。§4 のとおり**力は足りている**ので、現時点で必要性の根拠も無い。

## 9. 非主張

- ゲイン・速度・dwell の**数値を出していない**（測定待ち）。
- P-D1 の 6 run は **p4 の実測**であり、p11 は**再現していない**（doc を読んだ）。私が独立に読んだのは §1 の 5 点と §3 の branch 実装。
- 「制御器駆動で route が成立する」とは**まだ言えない**。実測が言うのは「**トルクは足り、遅れは速度依存で静定すれば消える**」まで。成立の判定は **Rs の動画**（DoD）。
