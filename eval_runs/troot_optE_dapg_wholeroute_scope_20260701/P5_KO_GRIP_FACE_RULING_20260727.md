# p5 裁定 — コ字爪でケーブルに摩擦を生む面 / `exclude(right_pad,left_pad)` の必要性

**stable ID:** `P5-KO-GRIP-FACE-RULING-20260727-001`
**著者:** SKILL-DETAIL-DESIGN (`w2:p5`)。**発行:** 2026-07-27 12:24:10 JST（`date` 実測）。⚠ **本書は uncommitted / 新規 untracked**（bank GO 未取得）。
**契機:** `MSG-P18-P5-KO-GRIP-FACE-DESIGN-QUERY-20260727-062`。**Rs 逐語（p18 経由）**「**SKILL-DETAIL-DESIGN に聞いてくれ**」。
**強制ゲート:** `/geometric-design` をロードし本書に protocol 出力を生成（CLAUDE.md スキル参照ガイド）。
**⛔ 非主張:** 実装・実行・gate 変更・LOCKED geometry の変更を求めない。⛔ 8.00 mm を「Rs 指定」として扱わない（`MSG-P18-8MM-IS-NOT-AN-RS-SPECIFIED-VALUE-20260727-063` の帰属訂正を受諾。8.00 mm は `task_config.py:137 CABLE_RADIUS = 0.004` からの算術）。

---

## 0. 結論（2 問への回答）

| # | 問い | 裁定 |
|---|---|---|
| **Q1** | ケーブルに当たって摩擦を生む面 | **(a) 背板（`pad_box1` の前面・pad-local y = +1.40 mm）**。⛔ 爪板（`f1ext`/`f2ext`）の先端対向面は **ケーブルに当たり得ない**（下記 §1 の z 非重複）。⭐ 爪板の役割は **y 摩擦ではなく z の形状拘束（cage）**。 |
| **Q2** | `exclude body1="right_pad" body2="left_pad"` はクランプ成立に必要か | **必要（クランプ成立の必要条件）**。⛔ ただし **十分条件ではない**（§5 の到達性）。 |

## 0-A. ⛔⛔ 最重要の訂正（原因側 = p5）— 本裁定は **banked 設計の再発見**であり新規判断ではない

**`w2:p18` の `MSG-P18-IT-WAS-ALREADY-IN-THE-BANKED-DESIGN-20260727-069` を受け、私が自分で逐語確認しました。**
**source pin:** `thread_isaac_lab/thread-vault/06-Knowledge/GD-KoShape-Finger.md` sha256 `d30b8195782edb8a49c62bef7597fd5aeba27938f60031718a9a3f718dfd8a80`（**WT == banked** @ `bcb7393ec8aecbedce951d1a6d3d019753c46580`・2026-07-01 21:45:03 +0900）。

- **`:95` 逐語** = 「flat-pad (`pad1`) pinch **[dominant, −1.06/−1.39]** + vertical claw (f1ext/f2ext) straddle [−0.7]」
  ⇒ ⭐ **Q1 の答え（背板が dominant、爪は またぎ）は banked 設計に貫入量つきで既に記録されていた。**
- **`:58-59` 逐語** = 「At the grasp pose: f1ext Z≈796.6 (**BELOW** cable), f2ext Z≈809 (**ABOVE** cable), cable Z≈800-808 **between them**」
  ⇒ ⭐ **§1 で私が「未検証」と留保した軸帰属はここで解決済**: world では **f1ext が下・f2ext が上** ⇒ **pad-local +z は world −z に対応**（pad-local では f1ext 38.2 が上）。⇒ ⛔ **asset header と `route_executor.py:2437` の「f1ext bottom + f2ext top」は world 基準で正しい。私が示唆した「注記が誤りの可能性」は撤回する。**

⛔ **私の失敗（§運用4 違反）**: 私は同 doc を **`:9-16` しか読まずに**「設計 doc を参照した」として asset 幾何から機構を導出しました。⇒ **部分読みを参照として扱った**。⭐ p18 の自己申告と同型で、**本裁定の内容は変わらないが、導出経路が不要だった**（doc を最後まで読めば §1 の測定は確認作業で足りた）。
⭐ **以後の規律**: 設計を導出する前に、**該当設計 doc を最後まで読む**（`grep` の当たった行だけで「参照」としない）。

⭐ **Q1 と Q2 が同じ問いである理由**（p18 の framing に同意・私も独立に検算）: 摩擦面が背板なら、背板を Ø8 まで詰めるために爪板どうしが **2.00 mm 相互貫入**する必要があり、その貫入を許すのが本 exclude 1 行。⇒ **面の同定がそのまま exclude の可否を決める。**

---

## 1. Step 1 実測（すべて banked asset からの直読・p18/p11/p0/p4 の数値は relay しない）

**source pin:** `thread_isaac_lab/assets/ur5e_robotiq/robotiq_2f85/2f85_koshape.xml`
sha256 `a3bef79ee9b4f4161dd6da20967e65e0da78f5706724fbf61e35fb43ba230ba3` / **WT == banked**（last commit touching path = `85315bbec6787a9cfcb3cb147c87fb78beb3b5ca`・2026-06-23 03:54:15 +0900・`git status` clean）。
⚠ MuJoCo `size` は **half-extent**。以下 pad-local・mm。

| geom | 定義元 | y 範囲 | z 範囲 | y 最前面 |
|---|---|---|---|---|
| `pad_box1`（背板・上） | `:58-60` `pos="0 -0.0026 0.028125" size="0.011 0.004 0.009375"` | **[−6.60, +1.40]** | **[18.750, 37.500]** | **+1.40** |
| `pad_box2`（背板・下） | `:62-64` `pos z=0.009375 size z=0.009375` | [−6.60, +1.40] | [0.000, 18.750] | +1.40 |
| `f1ext`（爪） | `:157` `pos="0 -0.0026 0.0382" size="0.011 0.009 0.0012"` | **[−11.60, +6.40]** | **[37.000, 39.400]** | **+6.40** |
| `f2ext`（爪） | `:158` `pos z=0.0258 size z=0.0012` | [−11.60, +6.40] | **[24.600, 27.000]** | +6.40 |

**導出（引き算のみ）:**
- **爪の突出 = 6.40 − 1.40 = 5.00 mm / 片側**（pad-local y での差・**単一 pad 内の静的量**）。
  ⇒ ⚠⚠ **「背板どうしの隙間 = 爪先どうしの隙間 + 10.00 mm」は恒等式ではなく、前提つきの関係**です。**前提 = 左右 pad が互いに平行（傾かない）**。⛔ **4-bar リンクは pad を傾ける**（`w2:p4` 実測・私は未再測）ゆえ、閉じ行程の途中でこの +10.00 は**保証されません**。⇒ ⭐ **私が主張する形は「全閉からさらに約 2 mm 閉じる行程が要り、その間 爪は互いを通り抜けていなければならない」まで（1 桁目のみ）。** 必要な重なり量は**測って求める量**であり、引き算では確定しません。
  〔⚠ **自己申告**: 初稿では上を「恒等式」と書き、**前提（平行）を明示せずに他 pane の差分値を採っていました**。⇒ `MSG-P18-TRAVEL-NUMBER-IS-NOT-A-DERIVED-QUANTITY-20260727-066`（出所 = `w2:p11` の限定）で気づいた**受動的な捕捉**であり、私が独立に検出したものではありません。⭐ 併せて `w2:pZ` の規律を採用します（`…-067` B）= **他 pane の限定を採る前に、その限定が成り立つ前提を書き出し、その前提を自分が測っているかを見る**。本件の前提は「ケーブルが爪の進路に在る」= **z の重なり**という測定可能量で、書き出せば §1 の表で即決まりました。〕
- **爪間 z 隙間 = 37.000 − 27.000 = 10.00 mm**、その中心 = **32.00 mm**（asset header `:9` 逐語「gap ~10 mm」と整合）。
- Ø8（= 2 × `CABLE_RADIUS 0.004`・`task_config.py:137`）を中心 32.00 に置くと **z = [28.00, 36.00]**、上下クリアランス **各 1.00 mm**。

**⭐ z 重複の判定（Q1 を決める測定）:**
| 対 | z 重複 | 帰結 |
|---|---|---|
| 背板 `pad_box1` [18.750, 37.500] × cable [28.00, 36.00] | **完全被覆** | ⇒ **背板前面は cable に y で対向する** |
| 爪 `f2ext` [24.600, 27.000] × cable [28.00, 36.00] | **0**（27.000 < 28.00・1.00 mm 離れ） | ⇒ 爪は cable の下を通過 |
| 爪 `f1ext` [37.000, 39.400] × cable [28.00, 36.00] | **0**（37.000 > 36.00・1.00 mm 離れ） | ⇒ 爪は cable の上を通過 |

⇒ ⛔ **爪先対向面は、ケーブルが完全含有帯の中心に在っても、y 方向でケーブルに接触できない。** ⇒ **Q1 = (a)**。

**⚠ 私が測っていないもの（他 pane の測定として引用し、私の裁定はこれに依存しない）:** 全閉時の背板対向 +9.98 mm / 爪先最小 −0.07 mm / 差の実測平均 10.04 mm（出所 = `w2:p4` / `w2:p0` / `w2:p18`）。⛔ 閉じ姿勢は code 側で 4-bar を再構築する設計（asset header `:7-8` 逐語「the 4-bar is rebuilt in code at build」）ゆえ、**静的 asset から閉じ角の到達限界は導けない**。

**⚠→✅ 軸帰属（解決済・§0-A 参照）:** pad-local z では f1ext(38.2) が f2ext(25.8) より上だが、**world では f1ext が下・f2ext が上**（`GD-KoShape-Finger.md:58-59` 逐語）⇒ **pad-local +z は world −z**。⇒ asset header と `route_executor.py:2437` の「f1ext bottom + f2ext top」は **world 基準で正しい**。⛔ 私が初稿で示唆した「注記が誤りの可能性」は **撤回**。⭐ **本裁定（Q1/Q2）は z の *重複の有無* だけに依存し、上下の呼び方には依存しない**ゆえ結論は不変。

---

## 2. Step 0 機構問い直し（BOUNDED・1 pass）

1. **その機構は必要か:** ケーブル保持は L0 目標「コ内にクランプ」の直接要件（Rs 指示）⇒ 必要。
2. **既存幾何の salvage:** ⭐ **あり = exclude 1 行の復帰**（geometry を変えずに背板の到達余地を開ける）。⇒ **LOCKED geometry（不変前提 #4）に触れずに済む唯一の安価な経路**。
3. **下流ステージが真のリスクか:** 半アンクランプでの誘導（工程表 `:1238` = 0.006）は **z cage を保ったまま x 軸滑りを許す**設計であり、y 摩擦の成立が前提。⇒ 本件は下流の前提。

---

## 3. Step 2 制約

**ハード制約（違反不可）**
- **H1（形状 LOCK）**: コ geometry は human-LOCK（RS71 §0 #4）⇒ box 寸法・位置を変えない。
- **H2（y 接触）**: 摩擦を生むには **背板どうしの隙間 ≤ 8.00 mm**（Ø8 に接触。8.00 mm = `task_config.py:137` からの算術・Rs 指定ではない）。
- **H3（爪の通過）**: H2 を満たすには **全閉からさらに約 2 mm の閉じ行程**が必要で、その間 **爪どうしは互いを通り抜けていなければならない** ⇒ **pad-pad 接触が有効なら H2 は達成不能**。⛔ **必要な重なり量の精密値は主張しない**（§1 の前提つき関係・4-bar の傾き未測）。
- **H4（cable 接触の保持）**: exclude は **pad-pad のみ**を除外し、cable(別 body) との接触を落とさない（asset `:172-174` 逐語「Cable contact is UNAFFECTED (the cable is a separate body)」）。
- **H5（kinematic 禁止）**: 物理無視の強制配置を用いない（§0 #5）。

**ソフト目標**
- S1: z クリアランス 1.00 mm/側 を維持（Rs 関連発言②「上下をきつくしすぎるとクランプしづらくなる」と同方向。⚠ この発言は p4 relay で私は Rs 本人と未照合ゆえ、**裁定の根拠にはしていない**）。
- S2: 半アンクランプで x 軸滑りを許す（工程表 `:1237` 0.002 / `:1238` 0.006 / `:1239` 0.04）。

**2a 有効設計空間:** H2 が要求する背板 8.00 mm に対し、**pad-pad 接触が有効な世界での下限は 10.00 mm**（= 爪先 0.00）。⇒ **有効幅 = 0 mm（空集合）** ⇒ 🔴 **危険域**。exclude 有効時の下限は **actuator/4-bar の travel 限界**で決まり、**私は未測定**。

**2b 矛盾検出:** S1（z を緩く）は H2/H3 と**独立**（別軸）ゆえ矛盾なし。⚠ ただし §1 の z 非重複は「S1 を緩くした結果、爪が cable の進路から完全に外れた」状態であり、**cage としては機能するが y 摩擦には寄与しない**という**役割分離**が生じている。⇒ 矛盾ではなく**設計の帰結**。

---

## 4. Step 3 断面図（pad-local・y-z 断面・mm・左右 pad を対向配置）

```
 pad-local z [mm]
   39.4 ┤   ▓▓f1ext▓▓                     ▓▓f1ext▓▓        爪(上): z 重複 0
   37.0 ┤   ▓▓▓▓▓▓▓▓                     ▓▓▓▓▓▓▓▓         ↑ cable 上端より +1.00
   37.5 ┤ ┌──背板上端                       背板上端──┐
   36.0 ┤ │   ╭─────────── Ø8 cable ───────────╮   │      cable z=[28.0,36.0]
   32.0 ┤ │   │        ● slot 中心 32.0        │   │      = 爪間 10.00 の中心
   28.0 ┤ │   ╰─────────────────────────────────╯   │
   27.0 ┤   ▓▓f2ext▓▓                     ▓▓f2ext▓▓        爪(下): z 重複 0
   24.6 ┤   ▓▓▓▓▓▓▓▓                     ▓▓▓▓▓▓▓▓         ↓ cable 下端より −1.00
   18.75┤ └──pad_box1/2 境界              境界──┘
    0.0 ┤ 背板下端                             背板下端
        └────────────────────────────────────────────────→ y
          背板前面 +1.40 ┆ 爪先 +6.40      爪先 +6.40 ┆ 背板前面 +1.40
          ←─ 爪突出 5.00 ─→               ←─ 5.00 ─→

 y 方向の閉じ（左右対向・gap は「対向面どうし」）
   全閉（他 pane 実測）  背板 ≈ 9.98 ・ 爪先 ≈ −0.07  ← pad-pad 接触が有効な世界での停止点 ❌ H2 未達
   H2 達成に要る状態      背板 ≤ 8.00 ・ 爪先 = 通り抜け中   ← ⭐ さらに「約 2 mm」の行程（1 桁のみ）
   ⚠ 上の 2 行を結ぶ +10.00 の関係は「両 pad が平行」前提。4-bar は pad を傾ける ⇒ 精密値を読まない。
```

⇒ ⭐ **cable は y で「背板 × 背板」、z で「f2ext × f1ext」に囲まれる。爪先対向面は cable ではなく相手の爪に当たる。**

---

## 5. Step 4 トレード表（H 違反行は候補から除外）

| # | 案 | H1 形状 LOCK | H2 y 接触 | H3 爪通過 | H4 cable 接触 | H5 | 判定 |
|---|---|---|---|---|---|---|---|
| 1 | **`exclude(right_pad,left_pad)` を有効化（banked LOCK の状態に戻す）** | ✅ 変えない | ⭕ **到達可能になる**（下限は travel 限界・未測） | ✅ 貫入可 | ✅ 別 body ゆえ不変 | ✅ | ⭐ **推奨（唯一 H を全て満たす）** |
| 2 | 爪の z 位置を cable 帯に寄せて爪で掴む | ❌ **LOCKED geometry 変更** | ⭕ | – | ✅ | ✅ | ⛔ 除外（#4 に触れる ⇒ Rs 専権・設計 lane で選べない） |
| 3 | 背板を y に伸ばして先に当てる | ❌ 同上 | ⭕ | – | ✅ | ✅ | ⛔ 除外（同上） |
| 4 | 現状維持（exclude なし） | ✅ | ❌ **爪どうしの接触で停止し Ø8 に約 2 mm 届かない**（他 pane 実測 = 背板 9.98） | ❌ | ✅ | ✅ | ⛔ 除外（H2/H3 違反 = クランプ不成立） |
| 5 | cable を kinematic に固定して「掴んだ」ことにする | ✅ | – | – | – | ❌ | ⛔ 除外（§0 #5 違反） |

⭐ **案 1 は「設計変更」ではなく「banked LOCK asset が既に持っている状態への復帰」**（§6 参照）。

---

## 6. ⭐ 私の独立発見 — exclude が「落ちている」面の同定

| asset | `exclude` 総数 | `right_pad`/`left_pad` | pin |
|---|---|---|---|
| **banked LOCK** `thread_isaac_lab/assets/.../2f85_koshape.xml` | **7** | ⭐ **在り（`:176`）** | sha256 `a3bef79ee9b4f4161dd6da20967e65e0da78f5706724fbf61e35fb43ba230ba3`・WT==banked @ `85315bbec6787a9cfcb3cb147c87fb78beb3b5ca` |
| `2f85_koshape_scratch.xml` | – | **在り** | （untracked） |
| `2f85_koshape_scratch_wide.xml` | – | **在り** | （untracked） |
| **`eval_runs/.../p4_ur15_sim_20260727/_ur15_2f85_koshape_actuated.xml`** | **6** | ⛔ **無し** | sha256 `c2d65167d32b413bcbf2985a153025e5455a3d5e8051cd89733cb67e11751ffe` |

⇒ ⭐⭐ **本 1 行は「設計に無い」のではなく、生成された `_ur15_..._actuated.xml` で欠落している。** 他 6 本（base×driver 等）は残っているので、**欠落はこの 1 対に限る**。
⇒ ⛔ **私が測っていないこと**: (i) その生成物が p0/p4 の実測に使われた model か（**測定した側の court**）(ii) 生成手順のどこで落ちたか。⚠ 併せて確認: `thread_isaac_lab/` の Python で `2f85_koshape` を参照する行 = **0 件**、`envs/`・`scripts/` で contact exclude を操作する行 = **0 件**（⇒ code は exclude を落としていない）。
⇒ **要求（私の依頼）**: 生成物を作った側が、欠落が **生成 step の回帰**か **意図的な除去**かを 1 行で確定してください。回帰であれば **案 1 は「復帰」であって設計変更ではありません**。

---

## 7. Step 5 物理妥当性 / 5a 感度 / 5b 連動 / 5c 因果連鎖 / 5d 軸分解保持

**5 実機妥当性:** 実機 2F-85 では左右 pad が独立部品ゆえ、**爪どうしが機械的に干渉する形状は実機で組めない**。⇒ ⚠ **exclude は sim 上の必要条件だが、実機では「爪が相互貫入する閉じ姿勢」は成立しない。** ⇒ **転移時に non-conservative になり得る**（sim だけが閉じられる）。⛔ これは §運用15 の conservatism 方向として **転移前に高 fidelity / 実機確認が必要**な項目。**本裁定は sim 内でのクランプ成立の必要条件を述べるのみで、実機成立を主張しない。**

**5a 感度（H2 = 背板 8.00 mm への到達）**
| 変動要因 | 幅 | 影響 | 判定 |
|---|---|---|---|
| cable 実径（Ø8 名目） | ±0.2 mm | H2 の目標 gap が 7.8–8.2 mm へ | ⭕ 線形・符号不変 |
| cable の z 位置（slot 中心 32.0 から） | ±1.0 mm | 片側クリアランス 0→2.0 mm。**±1.0 mm を超えると cable が爪の z 帯に入る** ⇒ **爪が cable に当たり得る = Q1 の答えが変わる** | ⚠ **境界。誘導（半アンクランプ）中は現実的に起こり得る** |
| 4-bar travel 限界 | 未測 | 到達可否そのもの | 🔴 **未測 = 案 1 の十分性は未確定** |
| **pad の傾き（4-bar 由来）** | 未測（`w2:p4` が傾きを実測） | 背板 gap と爪先 gap の**縮み率が同じでない** ⇒ 必要な重なり量が動く | 🔴 **精密値を読めない**（1 桁のみ主張） |

**5b 連動パラメータ:** `FINGER_CLOSE_POS = 0.002`（`:277` 逐語「gap=4mm < cable 8mm → 2mm/side compression」）⇒ **この定数は既に「背板が Ø8 を 2 mm/側 圧縮する」前提**で書かれている ⇒ ⭐ **設計意図は Q1=(a) と整合**。／`T_FINGER = 0.012`（`:365`）／工程表 `:1237`/`:1238`/`:1239`。⛔ 数値変更は本裁定の対象外。

**5c 因果連鎖（案 1 = exclude 有効時）**
| Step | 状態 | Event | 物理応答 | 問題 |
|---|---|---|---|---|
| 0 | 全閉（背板 ≈ 9.98・爪先 ≈ −0.07・他 pane 実測） | close 継続 | 爪が相互貫入（pad-pad 除外） | ⭕ |
| 1 | 背板 ≤ 8.00（さらに約 2 mm 閉じた後） | 背板が Ø8 に接触 | y 圧縮 → 摩擦発生 | ⭕ H2 達成 |
| 2 | 圧縮継続 | さらに close | cable が y に潰れる / z へ逃げる | ⚠ 過圧縮の上限は未設計 |
| 3 | 保持 | 半アンクランプ 0.006 | y 圧を抜き z cage で保持 → x 滑り | ⭕ 誘導の設計と整合 |
| 4 | 誘導中 | cable が z にずれる | ±1.0 mm 超で爪帯に進入 ⇒ 爪が cable を叩く | ⚠ 5a の境界と同一 |

**5d 軸分解保持（μ0 falsifier）**
| cable DOF | enclosed / carried | 根拠 |
|---|---|---|
| z 上方脱出 | **enclosed** | `f1ext` z 下端 37.000 が cable 上端 36.00 を塞ぐ（μ0 で成立・形状拘束） |
| z 下方脱出 | **enclosed** | `f2ext` z 上端 27.000 が cable 下端 28.00 を塞ぐ（同） |
| y（左右） | **enclosed（gap ≤ 8.00 のとき）** | 背板 2 面で挟む。⛔ gap > 8.00 では **未拘束** |
| x（軸方向滑り） | ⛔ **carried（摩擦依存）** | 幾何が塞がない。⇒ **μ0 では保持されない = residue として明示 carry**。⭐ 半アンクランプ誘導はこの DOF を**意図的に開ける**運用 |
| roll-out（コの開口方向） | **enclosed（閉じ時）** | 左右の爪が中央で重なり y-z 断面を閉じる（案 1 の貫入前提） |

⇒ ⭐ **「左右で摩擦」は y DOF についての要求であり、z は形状拘束・x は摩擦 carried。**⛔ 「掴めている」を y 摩擦の測定なしに主張しない。

---

## 7-A. ⭐ 事後の実測による corroboration（Q2・私の裁定より後に landed）

`w2:p4` が **exclude 復帰の比較を実行**（⚠ **Rs 本人の直接指示**による実行であり一般解錠ではない。p18 `…-069` D）。artifact = `P4_EXCLUDE_RESTORED_PINCH_MEASUREMENT_20260727.md` @ `53217d96fb1d4496cf4b58be401cabae2445ee59` / sha256 `1a0fe6a7c48e8ff92476f03cea958f2b72ba594c65a98419ecc299d2aff9e771`（**pin は p18 が独立照合。⛔ 私は artifact 本文を未読ゆえ、以下は p18 の要約の引用**）。
- exclude 復帰 ⇒ `nexclude` 6 → 7。**復帰前は全指令で背板接触 0 件**。復帰後 **HALF で L/R 両方の `pad1` が cable に接触（−0.08 mm）**。
⇒ ⭐ **Q2「exclude は必要」と同方向**。⛔ ただし **p4 の限定 4 件を必ず併記**: ①CLAMP 行は実挙動の代表でない ②`pad1↔pad1` の列は面間隔として読めない（傾きで最近接点が移る）③**exclude が唯一の原因までは示していない** ④受入判定ではない。
⇒ ⛔ **因果は依然として未確立**。⇒ 私の Q2 は「必要条件」の主張のままで、**十分性も因果も主張しない**（§5 の到達性・§8）。

## 8. 非主張 / 残（私は決めない）

- ⛔ **exclude を戻す実行・比較 run を求めない**（解錠は p11 court・gate CLOSED）。⛔ 実装もしない。
- ⛔ **LOCKED geometry の変更を提案しない**（案 2/3 は除外）。触る必要が生じたら **Rs 専権 + STOP**。
- ⛔ **8.00 mm を Rs 指定として扱わない**（`task_config.py:137` からの算術）。
- ⛔ **全閉 9.98 / 爪先 −0.07 は他 pane の測定**であり私は未再測。⛔ **exclude 復帰後に 8.00 mm へ到達するかは未測定**（4-bar travel 限界・案 1 の**十分性は未確定**）。
- ⚠ **pZ への含意**（p18 `-064` F と同方向・私の court では述語の面のみ）: 受入述語 CLAMP-1 の C レグが要求すべき接触 geom は **`pad_box1`（背板）× cable** です。⛔ `f1ext`/`f2ext` × cable を y 接触の要件にしない（§1 の z 非重複）。⭐ ただし **z cage の検証**には f1ext/f2ext を使う（別レグ・μ0 で確認可能）。
- ⚠ **Rs 関連発言①②③は p4 relay** で私は Rs 本人と未照合ゆえ、**本裁定の根拠に用いていない**（整合はしている）。
