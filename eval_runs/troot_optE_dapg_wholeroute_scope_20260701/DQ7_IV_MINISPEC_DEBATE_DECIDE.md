# CC1 REBUT_OR_ACCEPT + DECIDE — DQ7 stage (iv) mini-spec pre-implementation debate
CC1 = %12 RS-TECH-LEAD. Panel: CC2 premise/provenance, CC3 rule/SSOT, CC4 numerical/physics, CC5 side-effects/regression, CC6 NHA. 2026-07-03 06:3x JST.
Subject: dq7_iv_mini_spec.md (COORD CP-(iv)-1) + CC1 modifications M1 (V1=ON), M2 (aug-null incl. V1), M3 (final_L=L2), M4 ([2,20]mm all legs).

## REBUT_OR_ACCEPT (deduplicated ledger)

U1. M1 V1=ON [CC6 HOLD; CC3 HIGH; CC2/CC4/CC5 conditional items] — **ACCEPT (withdraw M1)**.
CC6's staging analysis supersedes CC1's rationale: (ii) physically cannot supervise {2,3} ({2}=skip, {3}=avoid per doc §3 map) → the 2-leg (iv) matches (ii)'s supervision pattern; keeping {2,3} generalization-only preserves the load-bearing question ((ii)'s fate) at 0 GPU; V1-ON would over-predict (ii). CC3: authority channel — a third leg = charter amendment requiring %9 consult under the delegation protocol; marginal cost of the compliant path ≈ 4 min retrain. V1 remains a pre-declared post-Outcome-B diagnostic toggle (requires attributable diagnosis + one-message %9 consult + spec/prediction re-issue). CC5's wording challenge also ACCEPTED: V1 is gate-MODEL-consistent (L-coupling unmodeled at dual-hold), not "physically consistent" — recorded.

U2. M3 final_L=L2 [CC3 HIGH; CC5 HIGH (E15 retraction precedent B_BC_BUILD_SPEC.md:159); CC2 HIGH; CC4 LOW — 4 bodies] — **ACCEPT (withdraw M3)**.
final_L = **L3** (§0 Phase-keyword auto-escalation stands; context-scoping license exists only for the セッション継続性 class; demotion = Rs-initiated per rule-check Step 6「L 降格は基本不可」; direct precedent: E15 v1 L2 label RETRACTED with「§0 keyword に new-file 免除なし」). Factual substrate (converter offline, no sim import, task_config lazy leaf read :91 — CC2 exactness fix adopted) is recorded as a demotion RECOMMENDATION to Rs, loud, non-blocking. Default = run the L3 chain: this pre-debate (done) + 層3 mechanical + post-build 層2 debate + 層5 3-view (all offline). %9 O-1 carve-out (「本 consult はそれを免除しない」) honored.

U3. M4 [2,20]mm blanket [CC4 HIGH×2 (DR-lattice 10.0mm conflict + beat-geometry compression; gate reads 2mm only, og:406 CC1-verified); CC5 HIGH (E15-(a) span-stretch at tail + m3-coverage gap); CC3 MED; CC6 narrowing; CC2 LOW truncation; +%11 box-floor measurement §運用28-reconciled with CC4's MC (point-keep vs bin-average; binding axis = Rx per real label positions) — 5 bodies + owner] — **ACCEPT (revise M4 → per-leg pre-registered magnitude table)**:
- ee-leg {0,1}: log-U[2,20]mm (offset adaptation rides on never-perturbed seg — CC4-verified no conflict; guard2 18.77mm tail rationale lives here)
- ee-leg {2,3} (L-dims): log-U[2,8]mm (< 10.0mm lattice spacing; span-stretch physics; 2mm gate)
- ee-leg {11} (R, pre-contact): log-U[2,20]mm
- seg-leg {11}: log-U[2,10]mm (union-box truncation; beat geometry; (ii) ≤10mm mirror)
Plus: tangent-orthogonal projection on label-PRESERVED ee-legs (CC5 tangent-aliasing MED ACCEPTED; sample in leg subspace → project out local path tangent [og_b's dwp construction] → renormalize → 25°-axis rejection); per-(leg × magnitude-bin) accepted/rejected counts in meta + report (CC2/CC4).

U4. seg-leg label rule wrong vs expert [CC2 HIGH — 1 body, CC1 code-verified before accepting: test:4377 `_La` frozen; test:4405 `_R_ty` HOLD Y=88mm INVARIANT#2; og:252-265 pair probe = per-axis R-response, mean over 3 axes] — **ACCEPT**.
Faithful per-axis labels: co-move R_x/R_z with d_x/d_z; PRESERVE R_y and L[3:6]. The specced all-axis rule would teach L self-following (integrator-class) at the phase whose closed-loop probe already diverges (og_bprime C2_REGRASP contracted=false) and fabricate R_y-following the expert holds at 0. Derived finding (CC2, CC1-verified): faithful-expert pair seg-follow ≈ mean(1,0,1) ≈ 0.67 < band-lo 0.8 → **the B2 §3.3 [0.8,1.2] pair band may be unreachable by faithful behavior** — no (iv) boundary impact (seg-follow = INFORMATIVE), but a **band-recalibration question for the (ii) GO gate = Rs 専権, carried loud** into the (ii) charter + Rs notify. Prediction: seg-follow 0.282 → toward ~0.67 ceiling. Analysis script to report per-axis pair gains (og returns the mean only).

U5. (c) beat attribution failure directions [CC2 MED (aug-null ignore-obs collapse → (c) structurally unreachable → instrument-artifact Outcome B); CC4 HIGH-2 partially mitigated by U3 cap; CC5 LOW volume confound] — **ACCEPT (reporting mitigations; boundaries frozen)**.
Mini-spec v1.1 predicts BOTH (c)-failure directions; report includes aug-null full per-cell table + own-slice vs off-slice split + per-leg aug counts for BOTH arms (volume note if >10% mismatch); pre-committed diagnostic cells: "(c)-fail ∧ (a)(b)-pass = attribution inconclusive (aug-null collapse | null-coverage geometry)" distinct from mechanism failure. §3 sentence "null probed on rows it never fit" corrected (its (+10,0) path = 1/9 of probe rows — CC4-verified FALSE as written).

U6. v2 aggregation unpinned [CC5 MED; CC4 MED — CC1's own charter gap] — **ACCEPT (pin now, pre-train)**:
v2 = (OG-a mean rmse over all cells ≤ 0.550mm = 1.10× baseline mean 0.500mm) AND (every VERDICT_CRITICAL cell rmse ≤ 1.10× its baseline cell in b2_cpE_og/bc og_gate.json). Recorded as charter §3 v1.1 amendment (validity-precondition sharpening; A/B outcome numbers untouched — %9 条件1 letter preserved).

U7. Basename collision + meta contract [CC5 MED×2] — **ACCEPT**: aug outputs = bc_dataset_abs_aug.npz (+ `_aug` schema tag); meta = copy base + truthful overrides (n_rows_clean/n_rows_aug) + {V1=OFF, ladder step, K, per-leg magnitude ranges, rejection cfg 0.906/25° + tangent-projection flag, sampler seed, source npz sha, converter git sha, per-leg×bin accept/reject}. Report quotes og json per-phase n vs clean baseline {466,360,191,540,1063} as basis check (kills the wrong-basis og V5 scenario).

U8. Tie rule [CC5 non-blocking] — **ACCEPT (pre-registered pre-train)**: any (a)/(b) cell within ±0.02 of its bar → one same-seed re-train; if the side flips → unfavorable side.

U9. Cite/wording fixes [CC3 LOW; CC2 MED/LOW] — **ACCEPT**: og:24-31 → :33-38; band-scope cite → og:34/:37/:407-413; import-audit sentence += task_config lazy leaf read (:91); "until CC1 review" → 「commit 判断 = %12」; re-grep remaining og cites (CC2 verified :97-140/:268-277/:425-441 correct).

U10. Gate-set mapping [CC3 LOW] — **ACCEPT**: DoD = charter §3 A/B + validity (v1/v2 pinned); pre-mortem = §5 predictions + retry ladder + panel failure scenarios; handoff = §運用25 judgment (not triggered); 層5 = fires post-build (L3 + expected 3+ files); 層2 post-debate = fires (L3).

U11. Prediction-table currency [CC3 MED; CC2 MED] — **PARTIAL**: with V1 OFF the §5 {2,3} row and +7366 row estimate are correct AS WRITTEN (staleness was conditional on M1, now withdrawn); still amended: seg-follow row (→~0.67 ceiling + band flag), beat row (both failure directions), §2 magnitude table + tangent projection.

REBUTTED (none silent): no challenge is dismissed; CC1's original M1/M3/M4 are the items REBUTTED-BY-PANEL and withdrawn/revised. CC1 positions surviving intact: M2 (= charter §3(c) restatement; kept with U5 mitigations), A/B boundary set (all-bodies recompute-verified), m1 25° design (CC4: not self-defeating), leg↔probe dim/coupling architecture (CC2/CC4 line-verified), regression/sha anchors, video-leg N/A-loud, rollout prohibition.

## NO_ACTION_EVALUATION
- No change (don't build (iv)): rejected — D-1 staged decision (Rs-delegated, %9 CONCUR×8) stands; CC6 NHA itself endorses build-now-as-chartered.
- Already solved by KNOWN_ALTERNATIVES: YES in the direction CC6 argued — alternative (a) "2-leg charter-as-written" IS the adopted skeleton; the debate's net effect = revert CC1's M1/M3, keep the charter path with sharpened pre-registration.
- CC6 NHA judgment: HOLD (as-chartered) → honored: M1/M2-as-modification rejected, M4 narrowed, M3 reverted to L3.

## DECIDE
**Verdict on CC1's proposed package (mini-spec + M1-M4): FAIL** (HIGH challenges accepted against M1/M3/M4) → fixes specified above.
**Verdict on the amended plan (V1 OFF; final_L=L3; per-leg magnitude table U3; faithful seg-leg labels U4; U5-U9 mitigations): APPROVED for CP-(iv)-2** conditional on %11 issuing mini-spec v1.1 incorporating U3-U9/U11 (CC1 conformance check against this ledger before build — cycle-2 targeted re-check, not a fresh panel; the amendments originate from the panel itself).
Consensus stats: L3 = 4/5 bodies; magnitude cap = 5/5 + owner measurement; seg-leg label = 1 body but CC1 code-verified (test:4377/:4405, og:252-265, og:406).
Carried loud to Rs / (ii) charter: (a) L3 auto-escalation + demotion recommendation (Rs may downgrade; default L3 chain runs), (b) pair-band [0.8,1.2] vs faithful-expert ≈0.67 recalibration question (Rs 専権, decide before (ii) gate runs), (c) {2,3} = model-circular, no m3 coverage by design (non-conservative for transfer; bankable claims exclude them or route to (iii)/rollout).

## Consensus triage (machine section — buckets by challenger consensus; dispositions above)

### CONFIRMED (5/5)

#### M4 blanket [2,20]mm magnitude envelope must be replaced by a per-leg table
All five bodies (+ owner %11's box measurement) converge: DR-lattice conflict at {2,3} (CC4), E15-(a) span-stretch + m3-coverage gap (CC5), (ii)-mirror narrowing (CC6, CC3), union-box truncation (CC2, CC4 MC). Disposition U3: ee{0,1}=[2,20], ee{2,3}=[2,8], ee{11}=[2,20], seg{11}=[2,10] + tangent-orthogonal projection on label-preserved legs + per-(leg×bin) accept/reject reporting. ACCEPTED.

### LIKELY (3/5)

#### final_L=L2 self-demotion invalid — auto-L3 stands, demotion is Rs-initiated
4/5 bodies (CC2, CC3, CC4, CC5; E15 retraction precedent). Disposition U2: final_L=L3, demotion recommendation to Rs loud, L3 chain runs by default. ACCEPTED (M3 withdrawn).

#### V1=ON (third leg) rejected — representativeness + authority channel
CC6 HOLD + CC3 HIGH + conditional staleness items (CC2/CC4/CC5). Disposition U1: V1 stays OFF; post-Outcome-B diagnostic toggle via %9 consult. ACCEPTED (M1 withdrawn).

#### Beat (c) attribution failure directions under-predicted
CC2 (aug-null ignore-obs collapse), CC4 (null-coverage geometry), CC5 (volume confound). Disposition U5: predict both directions, aug-null full table + own/off-slice split + per-arm counts, pre-committed diagnostic cells; §3 "never fit" sentence corrected. ACCEPTED.

### POSSIBLE (1-2/5)

#### seg-leg label rule unfaithful to the scripted expert (L frozen, R_y held)
CC2 only — CC1 code-verified (test:4377, :4405, og:252-265) before accepting. Disposition U4: per-axis faithful labels (co-move R_x/R_z, preserve R_y + L); band-vs-expert ≈0.67 question carried to Rs/(ii). ACCEPTED.

#### v2 validity aggregation unpinned (post-hoc adjudicable)
CC4 + CC5. Disposition U6: pinned = mean ≤0.550mm AND every VERDICT_CRITICAL cell ≤1.10× baseline cell. ACCEPTED.

#### Aug npz basename collision with og provenance + meta contract incomplete
CC5. Disposition U7: bc_dataset_abs_aug.npz + truthful meta overrides + per-phase n basis check in report. ACCEPTED.

#### Boundary-adjacent nondeterminism tie rule
CC5 (non-blocking). Disposition U8: ±0.02 → same-seed re-train, flip → unfavorable side. ACCEPTED (pre-registered).

#### Stale/wrong-line citations in mini-spec grounding
CC2 + CC3. Disposition U9: og:33-38 constants, og:34/:37/:407-413 band scope, import-audit exactness (:91), wording nit. ACCEPTED.

#### L2/L3 gate-set mapping not enumerated
CC3. Disposition U10: DoD/pre-mortem/handoff/層5/層2 mapping enumerated. ACCEPTED.

#### Prediction-table currency under adopted modifications
CC2 + CC3 (conditional on M1 — mostly mooted by U1). Disposition U11 PARTIAL: {2,3} row + row estimate correct as written under V1-OFF; seg-follow row (~0.67 ceiling + band flag) and beat row (both directions) amended.
