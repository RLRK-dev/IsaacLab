# p4 実測 — 顎が閉じ切らない面はどこか（`mj_geomDistance`）＋ 私の旧説明の撤回

**測定者:** RS-TECH-LEAD (`w2:p4`)。**発行:** 2026-07-27 11:53:39 JST（date-THEN-write）。
**契機:** p18 `MSG-P18-B5-ORTHOGONAL-AXIS-CORRECTION-20260727-044`（24.0 mm と 10.00 mm は直交軸・並置は無効）。同じ検査を自分の主張に当てた結果、**私の説明が軸ごと誤っていた**ことが判明した。

⛔ **本書は測定のみ。** 因果も採否も判定しない（p11 / pZ の court、最終基準は Rs の動画）。
⛔ **一度限りの静的測定。** harness 変更なし・probe / 動画の再実行なし。

---

## 1. ⛔ 撤回する私の説明（3 件）

| # | 私が書いたもの | なぜ誤りか |
|---|---|---|
| 1 | 「全閉のパッド間隔 **24.2 mm** に対しケーブルは 10 mm ⇒ **パッドでは挟めない**」 | ⛔ 24.2 mm は **body 原点間の距離**であって面と面の隙間ではない。面の隙間として使ったのが誤り |
| 2 | Rs 宛「パッド間隔が 24.0 mm で停止する／爪の開口は 10.0 mm」の**並置** | ⛔ p18 `-044` のとおり**直交する別軸**。私の実測でも**なす角 89.24°**（顎軸 ≈ world x ／ 爪の上下軸 ≈ world z）。並べた提示を撤回する |
| 3 | 「パッドが 24.0 mm より閉じない」を**閉じ切らない面**として提示 | ⛔ 下記 §3 のとおり、**パッド面は全閉でも 9.98 mm 空いている**。閉を止めている面はパッドではない |

⚠ 途中で私は `geom_size[0]`（11.00 mm）を顎方向の半幅として引き算し「面が 3.77 mm 重なる」と算出しかけた。**これも誤り**で、`geom_size[0]` は**ケーブル方向の半長**である。⇒ 軸を混ぜた 3 度目。当該中間値は使用していない。

## 2. 使った計器

`mujoco.mj_geomDistance`（本 project が既に cross-check に用いている関数）。model = `_ur15_2f85_koshape_actuated.xml`（私の移植版・untracked / sha256 `c2d65167d32b413bcbf2985a153025e5455a3d5e8051cd89733cb67e11751ffe`）。
各指令で `mj_forward` 後 4000 step 静定させてから測定。**負値 = 重なり。**

## 3. ⭐⭐ 実測結果（単位 mm）

| 指令 | パッド面 対向<br>`left_pad1`↔`right_pad1` | 爪 f1 対向<br>`left_pad_f1ext`↔`right_pad_f1ext` | 爪 f2 対向 | 同一 pad の爪の上下<br>`f1ext`↔`f2ext` |
|---|---|---|---|---|
| OPEN (0) | +85.19 | +75.20 | +75.19 | +10.00 |
| HALF (170) | +28.35 | +18.27 | +18.80 | +10.00 |
| **CLAMP (255)** | **+9.98** | ⛔ **−0.07** | **+0.26** | +10.00 |

## 4. ⭐ 読み取れること（⛔ 因果は主張しない）

1. **全閉で先に当たるのは爪板どうしである。** 爪 f1 対向が **−0.07 mm**（接触・わずかに重なり）に達する一方、**パッド面は +9.98 mm 空いたまま**。⇒ **閉を止めている面はパッドではなく爪板。**
2. ⭐ **これは banked LOCK 設計のコメントが事前に警告していた現象と一致する** — 逐語「the protruding コ claws `f1ext`/`f2ext` can overlap at `GRIPPER_CLOSE_QPOS`」（`2f85_koshape.xml:173-175`）。その対策行 `<exclude body1="right_pad" body2="left_pad"/>`（`:176`）を、**私は移植版で落としている**（file 面・runtime 面とも確定済み・`nexclude=6`）。⛔ **因果は未確認。**
3. **Ø8 のケーブルを爪の間に保持するには、対向する爪の隙間が 8 mm 以上で止まる必要がある。** 実測では HALF で **18.3 mm**、CLAMP で **−0.07 mm**。⇒ **その間のどこかで止める閉じ量が要る。** ⛔ 値の決定は p11 の court。
4. **同一 pad の爪の上下（slot）は指令によらず +10.00 mm 一定**（爪は同じ pad に固定なので当然）。⇒ Ø8 に対し clearance 2.0 mm。⭐ この量は §1 の撤回の影響を受けない。

## 5. 非主張

⛔ 設計しない ／ 実装しない ／ 検証しない ／ 因果を主張しない ／ 閉じ量を決めない ／ 値を採用しない ／ GO を出さない ／ gate flip なし ／ probe・動画の再実行なし ／ harness を変更していない ／ 他 pane の record を代理編集しない ／ 物理妥当性を判定しない。

---
**p4 measurement = 2026-07-27 11:53:39 JST / RS-TECH-LEAD (`w2:p4`)**
