# p4 実測 — ケーブルはクリップの台座を貫通していた。当たり判定は効いており、サーボが押し勝っている

**測定者:** RS-TECH-LEAD (`w2:p4`)。**発行:** 2026-07-27 17:50 JST（date-THEN-write）。
**契機:** Rs 逐語「'/home/rlrk/Downloads/ur15_c1.mp4' クリップの底面を貫通しているのは認識できないのか？」
**対照 (Rs 提供):** Rs 逐語「'/home/rlrk/Downloads/ur15_ko_clamp7.mp4' これでは貫通していない（失敗しているが）」

⛔ **私の自己申告:** 認識できていませんでした。私の C1 SEAT 印字は **目標点からの距離**であって **実体との干渉**ではありません。
⇒ ⭐ **数値は出ていたのに、貫通は一度も検出していない。** 本日 柱（Y 字ヨーク下部）で踏んだのと同じ形で、**いずれも Rs の目だけが見つけています。**

---

## 1. 作った検出器

⭐ **各クリップ箱の座標系での厳密な点-箱判定**（ケーブル中心線を各リンク 5 点で標本化・箱をケーブル半径だけ膨らませる）。
⛔ **距離クエリを使わない理由 = 深い重なりで飽和する**（実測済: 完全に包まれた対で `+0.00`・爪は板厚 1.2 mm×2 の −2.6 で頭打ち。`P4_CLAW_FLOOR_RECONCILES_10MM16_20260727.md` §2）。⇒ **貫通検査が要るのは、まさにクエリが諦める領域。**
⭐ 併せて **クリップ×ケーブルの接触数**を数える（実体の中に在るのに接触 0 なら、当たり判定自体が効いていない ＝ 柱と同じ class）。

## 2. ⭐⭐ 発火の記録（`ur15_penetration_detector_validation.log`）

| STEP | 検出 | 接触数 |
|---|---|---|
| 2–6 | clear | 0 |
| **7 / 8 / 9** | **cab19 が `C1_riser` の中に 18.2 / 13.6 / 8.3 mm** | **3** |
| 10 | clear | 0 |
| 11–14 | 0.2 / 0.5 / 1.2 / 1.1 mm（`C1_base` `C1_wa`） | 5 / 3 |
| **15 / 16 / 17** | **cab16 が `C2_riser` の中に 11.3 / 8.9 / 9.8 mm** | 4 |
| 18 | clear | 0 |

⭐ **識別性:** 貫通していない段では発火せず、押し込み段でだけ発火し、離すと消える。⇒ **常時真の述語ではない。**
⚠ **0.2–1.2 mm は通常の接触ソルバの食い込み**であり欠陥として読まないでください。⭐ 欠陥は **8–18 mm の段**です。
⭐ **C2 でも同じことが起きています。** Rs は C1 のみご指摘でしたが、原因は同じです。

## 3. ⭐⭐⭐ 機構 — 当たり判定は効いている

⛔ **「クリップに当たり判定が無い」ではありません。** 貫通している間、**接触は 3–4 件 存在します**。
⇒ ⭐⭐ **効いている接触を、腕の位置サーボが押し切っています。**

**Rs の対照がこれを裏づけます** — `ur15_ko_clamp7.mp4` = `ur15_ko_clamp7_control.log`:

| | コでのクランプ | C1 固定 | 貫通 |
|---|---|---|---|
| `ko_clamp7`（14:33） | ⛔ 失敗（`gates.grasp = False`） | ⭐⭐ **発火** `pinC1: t=23.00s seat=[0.1437 0.3499 0.87]` | ⭐ **無し**（Rs 判定） |
| `ur15_c1` = `ur15_wide14`（Rs「両方成功」） | ⭐ 成功 | ⛔ 未発火 | ⛔ **有り** |

⇒ ⭐⭐ **掴んでいないときは、クリップの床がケーブルを 0.870 ちょうどで受け止めた**（＝接触は正しく働く）。
⇒ ⭐⭐ **掴んでいるときだけ貫通する** — 剛体把持したケーブルを位置サーボが目標へ運ぶため、接触力より指令が勝つ。
⇒ ⭐ **把持と C1 の座りは、本日 逆相関しています。**

## 4. 上流の原因（別途 実測済）

押し込み段は **ピンチ**を溝座標へ送り、口との差を手書き定数 `CLAW_OFFSET = +20.9 mm` で補正していました。
⭐ **実測のオフセットは L −44.4 / R −39.8 mm** ⇒ **符号も大きさも誤りで、合計 60 mm 低い**ところへ押し込んでいた。
⇒ 把持段は同じ量を **実測**（`seat_offset`）しており、**押し込み段だけが定数を使っていた**のが食い違いです。

## 5. ⛔ もう 1 件の欠陥（本 run で顕在化）

固定条件 `seated()` は **固定のリンク番号**を見張ります。本 run ではそのリンクが **x +0.198**、クリップは **x 0.150** ⇒ **48 mm 離れた場所**を見ていました。
⇒ ⭐ ケーブルが自軸方向に滑ると、**座っているのに座っていないと読む**（配置の誤りでなく同一性の誤り）。
⚠ 本 run では「幾何だけなら適格なリンク」も 0 件なので、**この欠陥が成功を隠してはいません**。⛔ ただし述語としては生きた欠陥です。

## 6. 非主張

⛔ 設計しない ／ 実機の値を主張しない ／ Rs の動画判定を再評価しない ／ 「両方成功」の判定を取り消さない（**同一映像の別場面**であり、両立します — `ur15_c1.mp4` と `ur15_wide14.mp4` は sha256 `8b499aa4f38a…` で **バイト同一**）
／ 修正後の run の結果を主張しない（走行中）。

---
**p4 measurement = 2026-07-27 17:50 JST / RS-TECH-LEAD (`w2:p4`)**
