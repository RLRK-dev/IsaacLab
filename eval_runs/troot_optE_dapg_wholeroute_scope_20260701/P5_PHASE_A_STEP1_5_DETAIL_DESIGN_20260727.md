# Phase A (STEP 1-5) 実行の詳細設計 — SKILL-DETAIL-DESIGN (w2:p5) 2026-07-27

⚠ **状態 = as-read / 未 bank。** 本 file は p5 が書いた working tree の内容であり、commit されていない。bank は landing owner の court。

## §0 接地 (§運用4)

| 参照 | 逐語 / 要点 | 状態 |
| --- | --- | --- |
| `RL-Routing-Design.md:1288-1292` | Phase A の 5 行。STEP 3 / 4 とも左右ハンド = **下降点(ケーブル)**、STEP 4 で フィンガ = **クランプ** | **STEP 系列は生きている** |
| `00-DESIGN-STATUS-LEDGER.md` (RL-Routing-Design 行の caveat) | 「43-step 表の *幾何* は stale (UR5e 前提) … **p5 の再導出まで本表の幾何を current として引用しない**」(Rs 裁定 / DDR #40) | ⇒ **本書がその再導出** |
| `2f85_koshape.xml` sha256 `a3bef79ee9b4f4161dd6da20967e65e0da78f5706724fbf61e35fb43ba230ba3` | 幾何の実測元 (tracked / clean) | LOCK (RS71 §0#4) |
| `task_config.py:137` / `:274` / `:276` / `:277` | `CABLE_RADIUS=0.004` / `FINGER_OPEN_POS=0.04` / `FINGER_HALF_OPEN_POS=0.006` / `FINGER_CLOSE_POS=0.002 # gap=4mm … 2mm/side compression` | 数値 SSOT |

⚠ **枠の宣言**: 本書の z はすべて **pad-local**。**pad-local +z は world の下**を向く (`GD-KoShape-Finger.md:58-59` / w2:p11 と w2:p0 の訂正を取り込み済)。

## §1 実測 (banked asset の直読)

| 量 | 値 [mm] | 出所 |
| --- | --- | --- |
| 爪 f1ext の z 帯 | [37.00, 39.40] | `:116` `:157` |
| 爪 f2ext の z 帯 | [24.60, 27.00] | `:117` `:158` |
| **コの口 (爪の内面間)** | **[27.00, 37.00]** ・高さ **10.00** ・中心 **32.00** | 上 2 行より |
| 背板 pad1 の z 帯 | **[18.75, 37.50]** (高さ 18.75) | `:59` |
| pad2 の z 帯 | [0.00, 18.75] | `:63` |
| 爪の突出 (顎方向 y) | **5.00 / 側** (背板前面 +1.40 / 爪前面 +6.40) | `:59` `:116` |
| Ø8 が口に完全収容される **ケーブル中心の帯** | **[31.00, 33.00]** = 32.00 ± **1.00** | 上より |
| 実機の顎の停止点 (爪が当たる・pad1 面間) | **10.16** (実測 / p4 sweep) | Ø8 まで **2.16 足りない** |

⭐ **支配関係 (本設計の要)**: 口 [27.00, 37.00] は **背板 pad1 の z 帯 [18.75, 37.50] に完全に含まれる**。
⇒ **ケーブルが口に入っていれば、必ず背板の面と向かい合っている。** 逆は成り立たない。

## §2 制約

**ハード (違反不可)**
- H1 幾何 LOCK: 爪の突出・口の高さ・背板寸法を変えない (RS71 §0#4 ⇒ 変更は Rs 専権)。
- H2 kinematic トリック禁止 (§0#5)。腕の姿勢は PD の実移動で到達する。
- H3 制御方式は IK のみ・**位置指令**。位置↔力の切替は **制御方式変更 = Rs 承認必須** (`prohibited.md`)。
- H4 収容: 把持の瞬間、ケーブル中心が **32.00 ± 1.00**。

**ソフト (目標)**
- S1 下降でケーブルを動かさない。
- S2 指を閉じる間、腕を動かさない。
- S3 狙いは 1 回で決める (反復ループに依存しない)。

## §3 Q1 — 「下降点(ケーブル)」の定義

### 結論
**ハンド側の狙い点 = コの口の中心 (pad-local z = 32.00)。ピンチ点ではない。**
**ケーブル側の狙い点 = 最寄りケーブルリンクの中心** (収容判定がケーブル**中心**の帯だから)。

### 根拠
1. §1 の支配関係より、**口を狙えば背板も自動的に満たされる**。ピンチ点を狙っても口は満たされない (口 10.00 に対し背板は 18.75 と広く、狙いの情報量が足りない)。
2. 許容は **±1.00 mm** で、Phase A で最も厳しい。⇒ 最も厳しい脚に狙いを合わせる。
3. driver 設計文 `:88-91` 逐語 = "the cable must arrive in the **slot BETWEEN the claws**" と一致。

### 構成 (⚠ ここを外すと 20 mm 級で外れる)
- **x, y = 顎の中心線**から取る。⛔ 爪 4 個の位置平均は使わない (四節リンクが左右非対称 ⇒ p4 実測で最大 **21 mm** 中心線から外れる)。
- **z = 爪 2 枚の中点**から取る (口の中心)。
- ⭐ **評価する姿勢 = 捕捉時に顎が居る姿勢**。現在の開いた姿勢で評価しない (四節リンクで口が約 **13 mm** 動く・p4 実測)。⇒ **1 回の決定的な評価**で足り、反復は要らない。
- ⇒ driver の `seat_point()` の構成 (x,y = 中心線 / z = 口の高さ) は **正しい**。`slot_centre()` の単純平均は **狙いには使わない**。

## §4 Q2 — STEP 3 と STEP 4 の間に腕は動くか

### 結論
**動く。ただし「接触の前・standoff で x,y を直す」だけ。接触後・下降中には動かさない。**

### 構成
| 段 | 中身 |
| --- | --- |
| STEP 2 | **STEP 3 の真上**に置く (p4 の発明 ③ を **採用**)。以後の下降は **鉛直のみ**。 |
| STEP 3 前半 | ケーブル上 standoff まで鉛直下降 (接触しない高さ)。 |
| STEP 3 後半 | **standoff で 1 回だけ狙い直す**。⛔ **x は狙い直さない** — 下降点の x は `GRIP_HALF_SPAN = 0.044` (= §0#2 の 88 mm span) で **設計定数として固定**され、ケーブルからは取らない (driver `:533-536` 実読)。⇒ **再評価するのは y と z だけ**。 |
| STEP 3 終端 | 残りを **鉛直**に下降。 |
| STEP 4 | **腕を保持**し、指のみ閉じる。⛔ 下降中・閉じ中の狙い直しをしない。 |

### 根拠
- 下降中にケーブルが **14-26 mm** 動いたのは、下降が斜めで **爪がケーブルを叩いた**ため (p4 実測: 接触 geom = f1ext ・狙いの x,y が上空 waypoint と最大 18 mm ずれ)。⇒ **原因は下降経路であって狙いの精度ではない。** 経路を直す。
- ⛔ p4 の発明 ⑤ (STEP 4 中の live 補正ループ・4 往復で発散) は **廃止**。動いている対象を閉ループで追うと、接触が始まった後は収束しない。
- ⛔ p4 の発明 ① (顎を閉じて誤差を測る 7 往復) も **狙いの手段としては廃止**。⚠ ただしその**着想 (口は閉じ姿勢で評価しないと合わない) は正しく**、§3 の「捕捉時姿勢で 1 回評価」に吸収した。

## §5 Q3 — クランプ指令の量 ⇒ ⛔ **ESCALATION (Rs 専権)**

### 事実
- 表の「クランプ」= `task_config.py:277` の 0.002 = **pad1 面間 4.0 mm**。
- ⛔ 実測: 実機の停止点は **pad1 面間 10.16 mm** (爪が先に当たる) ⇒ **4.0 mm は実機で到達不能**。
- sim で 4.0 mm に到達できるのは、asset が **claw-claw の contact を exclude している**ため (`:176`・資産注記 `:173-175`)。⇒ **sim だけの配置**。
- ⇒ ⭐ **現行の LOCK 幾何のもとでは、「ケーブルを圧縮し、かつ実機で到達できる」指令値は存在しない。**

### ⇒ 2 つの枝 (⛔ 私は選ばない)
- **(A) 捕捉枝**: 爪の当たり手前で止める (指令 pad1 面間 ≈ **11.0 mm** ・停止点 10.16 に 0.8 mm の余裕)。圧縮 0・保持は **荷重下の形状拘束**。⇒ 表の「クランプ」= **捕捉**の意味になる。
- **(B) 挟み枝**: 4.0 mm を保つ。⇒ Phase A の全結果が **sim 限定**であり、§運用15 により転移前に高 fidelity か実機確認が要る。

⛔ **力で締める (ctrl 255) は第 3 の選択肢にできない** — 位置指令から力での締め付けへの変更は **制御方式の変更**であり Rs 承認必須 (H3)。⚠ 「ctrl 255 で 1 度掴めた」は **爪のすり抜けを機構とする sim 限定の結果**。

## §6 Q4 — クランプ速度と順序

### 結論
**速度は Rs 指示のまま 1/2 を維持する。直すのは順序。**
**「腕が静定していること」を、指を閉じ始める前提条件 (gate) にする。**

### 根拠
- 速度を 149.2 → 74.9 mm/s に落として掴めなくなった原因は **閉じている間に腕が動いた**こと (p4 実測)。⇒ **速度の問題ではない。**
- ⇒ 速度と腕の運動は別の軸。片方を戻して他方を償わない。
- ⇒ p4 の発明 ⑥ (指令のなまし + 腕を先に静定させてから閉じる分離) を **採用**し、**heuristic ではなく gate** に格上げする: 腕の指令残差が閾値以下になるまで指の ramp を開始しない。⚠ 閾値の値は本書では決めない (計測して決める量)。

## §7 p4 の 6 件の disposition

| # | 発明 | 判定 |
| --- | --- | --- |
| ① | 顎を閉じて誤差を測る閉ループ (7 往復) | ⛔ **廃止** (着想は §3 に吸収) |
| ② | tool 姿勢メニューの固定と腕ごとの選択 | ⚠ **未決** — 姿勢は工程表の項目であり、本書では決めない (要 Rs / 表の再導出) |
| ③ | STEP 2 を STEP 3 の真上に置く鉛直下降 | ⭐ **採用** (§4) |
| ④ | STEP 4 で狙い直す / 姿勢を保持する | ⭐ **採用。ただし狙い直しは接触前の standoff へ移す** (§4) |
| ⑤ | STEP 4 中の live 補正ループ (発散) | ⛔ **廃止** (§4) |
| ⑥ | 指令のなまし + 腕の静定と指の閉じの分離 | ⭐ **採用・gate へ格上げ** (§6) |

## §8 感度 (H4 = ±1.00 mm に対して)

| 変動要因 | 実測された大きさ | H4 への影響 |
| --- | --- | --- |
| 下降中のケーブル移動 | 14-26 mm | ⛔ 14-26 倍。**支配的** ⇒ §4 で原因を消す |
| 爪 4 個平均による中心線からのずれ | 最大 21 mm | ⛔ 21 倍 ⇒ §3 で構成を変える |
| 四節リンクによる口の移動 (開↔閉) | 約 13 mm | ⛔ 13 倍 ⇒ §3 で捕捉時姿勢で評価 |
| servo の到達残差 | 0.4-1.0 mm | ⚠ 同オーダー ⇒ §6 の gate で抑える |

⇒ ⭐ **上位 3 つは設計で消せる (経路・構成・評価姿勢)。残るのは servo 残差だけで、それは ±1.00 と同オーダー。** ⇒ 本設計の狙いは「精度を上げる」ことではなく **外乱を作らないこと**。

## §9 本書が決めていないこと (依存・他 court)

- ⛔ **Q3 の枝 (A/B)** = Rs。⇒ 決まるまで STEP 4 の「クランプ」の意味は未確定。
- ⛔ 受入述語の owner 席 (p18 -110 (4))。⚠ 本書は述語を定義しない。
- ⛔ log の L/R label と実際の腕の対応 (p18 -131 (6))。⇒ ⭐ **本設計は左右対称ゆえ、この未決に依存しない。**
- ⛔ Rs が判定した動画の同定 (p18 -131 (5))。
- ⚠ tool 姿勢 (②)。


---

## §10 追補 (2026-07-27・w2:p11 の供給と、私自身の code 実読による訂正)

### 10-1 ⛔ §5 の枠組みを訂正する — 「位置か力か」は偽の二者択一だった
w2:p11 の指摘 (私も実読で確認): 現行は **位置目標 + effort 上限** = **既に力制限つきの圧縮**。
`task_config.py:129 FINGER_EFFORT_LIMIT = 60.0` / `:316 GRIPPER_DRIVER_EFFORT_LIMIT_NM = 2.5`。
⇒ ⛔ 私が §5 で「位置指令から力での締め付けへの変更 = 制御方式の変更」と書いたのは **枠組みが誤り**。方式は 1 つで、`ctrl 255` は **同じ方式のまま位置基準を捨てること**。

⭐ **結論は保持し、根拠を差し替える**: `ctrl 255` を採らない。理由 =
- 到達端は pad1 面間 **-1.3 mm** ⇒ ⭐ **正しく口に入った Ø8 は排除される** ⇒ 「255 で 1 度掴めた」は **正しく狙えていなかった run とだけ両立**する ⇒ **方式の証拠にならない** (p11)。
- ⭐ **実測: L は ctrl 255 でも 6.81 mm で停止している** (log `:56` `:59` `ctrl=255`) ⇒ **ケーブルが在れば、sim でも指令 4.0 mm には到達しない**。⇒ 到達点は **ケーブルの抵抗と effort 上限の釣り合い**が決めている。

⇒ ⭐⭐ **⇒ 設計すべき量は「指令 gap」ではなく「effort 上限」である。** 指令は表どおり `:277` の 4.0 mm を目標にし、到達は自己制限に委ねる = **現行実装がすでにその形**。
⇒ ⛔ **Rs へ上げる論点は 1 つに絞られる (§5 の A/B は substrate の問題として残る)**: **実機では爪が 10.16 mm で全てを止めるので圧縮が 0 になる。** sim 内の指令値の議論はこの点を解決しない。

### 10-2 ⚠ SSOT の齟齬 (p11 発見・私は編集しない)
`task_config.py:278` の注記 逐語 = `One-shot target + effort_limit=20N cap` ⇒ ⛔ 実値は `:129` の **60.0**。⇒ 注記が古い。⛔ `task_config.py` は L3 ⇒ owner の判断。

### 10-3 ⭐ Q1 の向き — 私は p11 と逆に読む (⛔ 決めるのは私、と p18 が明示)
p11 の仮説 = 閉じるとスロットが **13.4 mm** 動く (p4 banked 実測) ⇒ 狙いが逃げる。
⇒ ⭐ **私の読み: だからこそ「閉じた姿勢で評価した口の位置」を狙う** (§3 のまま)。開いた姿勢で評価して閉じると、13.4 mm 逃げる。
⇒ ⚠ p11 の文 (「閉じた状態の点で狙わない」) が **ピンチの定数 (`:320` `EE_TO_PINCH_CLOSED` / `:321` `EE_TO_PINCH_TIP_CLOSED`・差 20.9 mm) を狙いに使うな**の意味なら、⭐ **§3 と一致する** (私も「ピンチ点を狙わない」と書いた)。
⇒ ⭐ **符号を決める試験 (p11 提案・新規測定 0)**: 腕を固定したまま、**指を閉じる前と後**の `slot vs cable` を比べる。腕が静止しているのに誤差が約 13 mm 増えるなら **動いているのはスロット**。

### 10-4 ⛔ 自分の「第 3 の証人」を格下げする (私の code 実読による)
私は -027 で `slot vs cable` の大きさ (L 25.0 / R 17.4) を **枠に依存しない証人**として出した。⇒ ⛔ **無条件では使えない。**
driver 実読 (`:842` `:523-530` `:533-536`):
- `sc_err = seat_point(t) - cable_at(GL[0] または GR[0])`
- `cable_at(x)` は **live 位置**から **x が最も近い link** を選ぶ。⚠ その x は **`C1[0] ± GRIP_HALF_SPAN` = 設計定数** (ケーブルから取らない)。
⇒ ⭐ **参照点 (seat_point = 口) は正しく、位置も live。⛔ しかし link の選び方が「固定 x に最も近いもの」**ゆえ、**顎に在る link と別の link を指し得る**。
⇒ ⭐ **格下げ後の主張**: 「x で選ばれた link が顎に在る link と同じなら」という条件つき。⚠ L では `cable moved 25.1 mm since the aim` と `sc_err 25.0` がほぼ一致 ⇒ **同じ link を追っていて、それが実際に動いた**方を示唆する。⛔ ただし本 log だけでは決まらない (`cable_at` が返す index は捨てられており印字されない)。
⇒ ⭐ **同 log には「ケーブル」の基準が 3 つある**: ①口 (`seat_point`) × 固定 x で選んだ link ②**ピンチ点**に 3 次元で最も近い link (`:822-824`) ③実際に接触している geom。⚠ ② のピンチ点は口から **26-31 mm** 離れている (p4 実測) ⇒ ⭐ **「nearest cable link at 33.5 mm from the pinch」は、正しく口に入っていても出る値**であり、非把持の証拠にならない。


---

## §11 追補 2 (2026-07-27 14:39 以降・w2:p4 の code 実読を受けた撤回と、そこから出る設計上の帰結)

### 11-1 ⛔ §4 の根拠のうち「下降でケーブルが 14-26 mm 動く」を撤回する
w2:p4 が自 code を実読: `cable_at()` に渡す x は **STEP 1 で決めた固定値**で以後更新されない。
⇒ ケーブルが x 方向に滑ると **比較対象の link 自体が入れ替わる** ⇒ ⛔ **「移動量」と「同一性の変化」が混ざっている** ⇒ **量として使えない。**
⇒ ⭐ **§8 の感度表の第 1 行 (下降中のケーブル移動 14-26 mm) は無効。** 取り直しが要る。
⭐ **生き残る根拠 (独立)**: 接触 geom が **f1ext** = **爪がケーブルを叩いている** (p4 の接触同定)。⇒ ⭐ **§4 の結論 (下降は鉛直にし、経路で外乱を作らない) は再導出できるが、量は未測**とする。

### 11-2 ⛔ p11 の「スロットが動く」説は落ちた (向きが逆)
説の予測 = ケーブルはスロットの **下** に残る / R の実測 = ケーブルはスロットの **上**。⇒ **根本原因として運ばない。**
⚠ ただし **13.4 mm という量そのもの**は p4 の相対実測として残る (⛔ 向き = フレーム未実測)。

### 11-3 ⭐⭐ 経路の制約 — 終点だけ合わせても足りない (w2:p18 (6) を設計側で受ける)
実装の `slot_after_close` は **閉じ終えた後の位置**を予測して腕を合わせる ⇒ ⭐ **終点は扱うが、閉じる間にスロットが掃く区間は無防備。**
⇒ ⭐ **設計要件を 1 つ足す**: ケーブルは **閉じ動作の全区間**でスロットの内側に居なければならない。終点だけの一致では、途中で爪が掃いて叩く。
⇒ ⚠ **条件つきの帰結 (量が未確定ゆえ conditional)**: **もし**開↔閉でスロットが **13.4 mm** 動き、スロットの高さが **10.00 mm** なら、⇒ ⛔ **開いた姿勢の帯と閉じた姿勢の帯は重ならない** ⇒ ⭐ **全区間で内側に保てる狙い点は存在しない。**
⇒ ⇒ その場合、捕捉は必然的に「閉じる途中でケーブルが相対的にスロットへ入る」形になり、**閉じる間の爪とケーブルの接触は避けられない** ⇒ 設計は「接触を避ける」ではなく **「接触してもケーブルを弾き出さない」**方向で組む必要がある。
⇒ ⭐ **反証の仕方 (新規 run 1 本で足りる)**: 腕を固定して指だけ閉じ、**スロット中心の変位の大きさと向き**を測る。⇒ 変位 < 10.00 mm なら重なる帯が存在し、狙い点は 1 点で足りる。
⚠ ⛔ 私はこの条件文の帰結を確定として運ばない (13.4 mm は向き未実測・p11 の限定を継承)。

### 11-4 ⭐ §6 の gate の意味が 1 段強くなる
「腕が静定してから閉じる」は外乱を減らすためだけでなく、⭐ **11-3 の掃引の間にケーブルが動かないようにするため**でもある。⇒ gate は保持。
