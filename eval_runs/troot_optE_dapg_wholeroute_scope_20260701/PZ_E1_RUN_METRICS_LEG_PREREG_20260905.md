# pZ — pre-registered leg for E1 (RUN_METRICS.json written by the driver at exit), written **while the edit window is open and before the edit lands**

**Author** pZ / IMPL-VERIFIER (`w2:pZ`) · **Written** 2026-09-05 07:55:35 JST on m-p18-289 — ⚠ **14 seconds AFTER the edit landed** (`b19c4c5f5d` 07:55:21): written blind to the landing (every measurement below was taken 07:49–07:55 at the parent blob), but unlike `79b53c797a`/`9d118cbf93` this judgment does **not** precede the judged in git history, and I say so rather than call it pre-registered (Rs1 (the human) 「push 1:開く」 §1410 and, minutes later, 「2：推奨で良い」 §1411 — p18's flagged inference: L1 may run as a static-class instrument; window scope = §8.45 @ `5930ebf411`, sha256 `494937923f…f077`). Naming per m-p18-256: **Rs1 = the human; Rs2 = p4/CC**.
**Object (when it exists)**: p0's E1 commit on `ur15_steps_wired.py`, judged **against its own parent**. ⚠ At 07:53 JST the worktree was already **moving** (` M`, sha `18355d40…` ≠ HEAD blob `0c023a0ae7…`): every line number below is **at the HEAD blob `c737f6974e`**, the parent-to-be — never the worktree, which is not a population.
⛔ **Not an authorization**: this leg runs nothing wired. L1 (`P4_CLIP_DUMP=1`) is **not** covered by Rs1's word (§1410: "1:開く" only) and I do not run it; ② stays conditional and unmet.

## 0. Pre-edit baselines (HEAD blob `c737f6974e`, measured 07:49–07:55 JST)

- 3902 lines · `json.dump|RUN_METRICS` hits **0** · `RUN_METRICS.json` under `_gen/` **0** · `run.log.sha256` sidecars **0**.
- `import atexit` `:19` · `atexit.register(_depth_audit_report)` `:1656` · `OUT` `:40`.
- **Early exits — TWO, not one**: `raise SystemExit(0)` at `:1103` (`P4_CLIP_DUMP`, branch `:1050`) **and at `:2928`**, the last line of the module-level `if _os.environ.get("P4_RELEASE_ONLY") == "1":` block (`:2916–:2928`, which calls `release_ctrl()`, `def` `:2725`); that path steps **scratch** physics (`mj_step(m, _sc)`). ⚠ **Correction 08:07 JST, measured at the parent blob**: the `:1103` path is **not** physics-free either — `:1044–:1049` settle the cable on the **live** `d` (`for _ in range(2000): mujoco.mj_step(m, d)`) *before* the `P4_CLIP_DUMP` check at `:1050`. My first draft inherited §8.45's "no physics — the stills' class" without measuring what precedes `:1103`; p0 retracted the same clause in §8.46 (`a5b3924efa`) and the chain court reclassified L1 as a small run (§1413). Both early-exit paths step physics — one live, one scratch. §8.45 §2-M2 says the :1103 path is "the only such path"; **it is not** (F-a below).
- Control-line inventory (my AST + ordered-grep walk, `pz_e1_ctrl.py`): live state writes **0**; `d.ctrl` writes **7** at `[1046, 2407, 2580, 2631, 2639, 3538, 3542]`; `mj_step(` call sites **11**; control-class line **sequence 57 lines, sha256 `426aa229deb2cb2b`** — the reorder control (one line swapped in a synthetic sequence) changes the sha, so the predicate discriminates ordering, not just count.
- Mechanisms E1 leans on, **proven on my own stand-in under env7 Python 3.12.3 (exit=1)**: atexit handlers run **LIFO** (first-registered ran last); `sys.last_type/last_value` are set during atexit after an uncaught `RuntimeError` (message byte-equal); `os.readlink("/proc/self/fd/1")` resolves to the **regular file** under shell redirect and to `pipe:[…]` under a pipe — A1's fallback branch is reachable and distinguishable.
- Existing contract (`data/test_newton_1ep_zcheck_cycle7_20260329/RUN_METRICS.json`): **8** top-level keys — the 7 §8.45 names **plus `goal_context`**; `/log-analyzer` reads `$RUN_DIR/RUN_METRICS.json` (SKILL.md `:39`) and the log via `artifacts.logs.path` / `run.log_path` (`:52-63`), fallback `$RUN_DIR/run.log`.
- Real logs on disk (untracked → content pins at read): `_gen/reshoot_speccell_20260810/run.log` 397 lines sha256 `a1ad9a2a8b0a2326…`, `_gen/dod_c2_20260810/run.log` 393 lines `04599b84e34be51e…` — **both end with the DEPTH AUDIT block (atexit was the last writer) and carry exactly one `RuntimeError:` line** (L2(b)'s target shape exists). `config_stills_20260810/` has 8 PNGs and **no run.log** — no on-disk precedent for L1's shape.
- Env-switch population, **derived independently** (not from the edit's list): **23 names** (17 driver + 6 spec), file `env_names_23.txt` sha256 `1b08da6905232425…`; shape control: variable-form `environ.get(VAR)` = **0** in both files, so the literal query covers the population.

## 1. Rows (parent-relative on the E1 commit)

| # | row | requirement |
|---|---|---|
| 1 | scope | exactly **1 file**; `numstat` **deletions = 0**; added lines **≤ 120**; untouched: kinonly `120746a49b`, `compare_24_vs_240.py`, every spec file |
| 2 | control invariance — **mechanical** | ordered control-class sequence sha == parent's **`426aa229deb2cb2b`** (57 lines); live state writes **stay 0**; `d.ctrl` writes **7, same order** (judged by sequence — line numbers will shift, order may not) |
| 3 | handler placement | one handler registered with `atexit` **before** `_depth_audit_report` (`:1656`) so it runs **last**; armed before **both** early exits (`:1103` and `:2928`), i.e. registered at or near `:40` |
| 4 | M2 semantics | `run.end_reason ∈ {completed, raised, exited_early}`; `exception{type,message}` from `sys.last_type/last_value`; `exit_code` 0 / 1 / null respectively; `exited_early` must cover **both** paths of §0 |
| 5 | M1 / A1 | `run.log_path` = `readlink(/proc/self/fd/1)` when regular, else `OUT.parent`; `log_bytes_at_write` / `log_sha256_at_write` taken **after** flush and **after** the announce line; the launcher's sidecar `run.log.sha256` in `sha256sum` format; equality ⇒ nothing followed the write |
| 6 | A2 shape | top-level ⊇ {`schema_version` = "run_metrics.v1", `generated_at`, `final`, `run`, `artifacts`, `progress`, `judgement`}; `judgement.verdict` = "PENDING", `decided_by` = null (**the driver never grades** — CLAUDE.md `:275` 三者一致); driver content under `ur15_steps`; `config.env_switches_set` keys == **my** 23-name population, not the edit's own literal list |
| 7 | identity/provenance | LEFT/RIGHT `arm_xml`/`grip_xml` + sha256, driver path + sha256, cell dump + sha256 — each re-hashed by me against the commit's blobs |
| 8 | loud failure | handler body in `try/except` printing `[steps] RUN_METRICS.json NOT written: <exc>`; an atexit exception leaves the exit code unchanged (stand-in at leg time) |
| 9 | transport check (L2 only) | every per-step JSON number re-formats with the print's own spec (row f-string `:3680` at the parent blob: `t={…:5.1f}s L={le:6.1f}mm R={re_:6.1f}mm …`; my first draft cited `:3781`, a number read off the **moving worktree** — corrected at the named blob) to the digits of the matching `[steps] STEPn` line — detects a **lost** value, not a wrong one |
| 10 | analyzer pickup (L2(e), pB's court) | `/log-analyzer <run_dir>` finds `$RUN_DIR/RUN_METRICS.json` and the log path through the keys in §0 |
| 11 | no run from this desk | my probe of the **writer's own unit** (raise → "raised", message byte-equal) runs only on a stand-in of mine; the driver's `P4_CLIP_DUMP=1` (L1) is **p0's** to run under Rs1's 「2：推奨で良い」 (§1411, static class only, p18's reading flagged as inference) — I verify its outputs (row 13) and do not run it; L2 waits for Rs1's ② |
| 13 | L1 outputs, when p0 lands them | `RUN_METRICS.json` **beside** `run.log`; `run.end_reason` = "exited_early", `exit_code` null, `exception` null; `config` + `identity` filled, `steps` = []; `run.log_path` == the redirect target; `log_sha256_at_write` == sidecar `run.log.sha256` (nothing followed the write) and `log_bytes_at_write` == the file's size; the log's **last line is the writer's announce line** (LIFO: after the DEPTH AUDIT block); ⛔ the run must be the `:1103` path — a log showing `[rel]` lines is the `:2928` path; and ⚠ **L1 exercises only the `SystemExit` exit — see the verdict's F-d for why an L1 PASS cannot vouch for the writer on the raised/completed paths** |
| 12 | pins | commit + content sha256 named; all evidence `file:line` **at a named blob** |

## 2. Findings registered in advance (so they are not discovered after and read as afterthoughts)

- **F-a**: §8.45 §2-M2 "the only such path is `P4_CLIP_DUMP` :1103" is incomplete — `:2928` (the `P4_RELEASE_ONLY` block `:2916–:2928`) is a second `SystemExit(0)`; it steps scratch physics, so it is **not** the stills' class. Consequence for the edit: nothing (the handler at `:40` is armed before both); consequence for wording and for L1: L1 (`:1103`) and `P4_RELEASE_ONLY` (`:2928`) both step physics; whether either is inside 「2：推奨で良い」 is the chain court's question to Rs1 (§1412–§1413), not this leg's.
- **F-b**: the live contract carries `goal_context` as an 8th top-level key; §8.45 A2 lists 7. Analyzer-neutral (its lookups need `run`/`artifacts` only) — named so the omission is a choice on record, not an oversight.

## 3. Provenance

`git show HEAD:` blobs for every driver number; `sha256sum`; own AST/grep walk (`pz_e1_ctrl.py`); own stand-in `pz_e1_mech.py` (exit 1, output captured); env population by grep with a shape control. Zero tracked-content modifications by pZ. **Written, not banked; banking requested of a custodian — ideally before the landing, as with `79b53c797a` and `9d118cbf93`.**
