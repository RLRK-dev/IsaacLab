# Rs 裁定 (b) — 採択 D-1 を候補へ格下げ（custody）

- 記録者 = `w2:pQ` RS-TECH-LEAD2（node `T-WMSO`）／記録 = **2026-07-21 19:34 JST**（shell 実測）
- 受領経路 = 本 pane `w2:pQ` の user turn（**relay 0 hop**）／routing = `WMSO_D11C_RS_ROUTING_20260721.md` の **(b)**

## 1. 逐語

> **落選した案を却下した理由に誤りがあったとき、採択された案は再度候補にかくさげ**

## 2. 直前文脈

私が (b) を説明し、選択肢 **B-1（D-1 有効のまま）／B-2（有効だが落選案の foreclose を決定脚だけで再確認）／B-3（B1 を再 open）** を提示し、**B-2 を推奨**した直後の応答。⇒ Rs は 3 択のいずれも選ばず、**格下げという一般規則**を述べた。

## 3. 私の読み（⚠ 解釈であり、Rs の代弁ではない）

- **規則**: 落選案の**却下理由に誤りがあった**場合、**採択案は「採択」の地位を失い、候補に戻る**。
- **本件への適用**: **D-1（`EvidenceRecord.source_ref` を locator に束縛）は 採択 → 候補**。⇒ **B1 は CLOSED → OPEN**。
- **理由（規則の含意）**: 選定は**候補間の比較**で行われる。1 つの候補の却下理由が誤っていれば、**比較そのものが誤った条件下で行われた**ことになり、勝者の地位は比較の健全性に依存しているため維持できない。⇒ 結論が同じでも、**その結論に至った比較をやり直す**。

## 4. 事実（本裁定の適用対象）

- **誤りの所在**: 凍結 v13 `:256` の落選行「rank 2 への proof kind 追加 = ⛔FORECLOSED」の理由に **`E_PROOF_KIND_FOREIGN`** が併記されていたが、同 code は凍結 EP md `:114`/`:129(iii)` で **component 意味論**と定義されており、却下の根拠にならない（訂正 note `00cf0c0bf86681f8…` @ `20cd755ac9`・Rs 裁定 (a)=B で履行）。
- **格下げされる案** = **D-1**（凍結 v13 `:252`・Rs ratify **2026-07-20 21:44**）。
- **同表の他候補** = D-2 / D-3 / D-4（`:252`〜`:256`）＋ 落選していた「rank 2 への proof kind 追加」。

## 5. ⛔ 本 custody が決めていないこと（Rs 確認が要る 3 点）

1. **却下されていた案（rank 2 への proof kind 追加）も候補に戻るか。** ⚠ 同行の**決定脚は「D-3 に合流」**（= required set の拡張は frozen schema delta を要する）で、これは誤った併記と**独立に成立**する（pQ 実測）。⇒ 誤りが除かれても foreclose が維持される可能性があるが、**判定はしていない**。
2. **凍結 D1.1-B（FROZEN / CUSTODY-CLOSED）の status をどうするか。** v13 は B1 = D-1 採択を本文に含む。**(a)=B の方針により凍結物は編集しない**ので、格下げは**別 artifact ＋ LEDGER** で述べる形になる。⇒ **凍結物の「B1 = 採択」記述は現況と一致しなくなる**（freeze record = 時点 snapshot / 現況 = LEDGER の convention と同型）。
3. **B1 を誰が・いつ選び直すか。** 前回の経路 = CC1 提案 → pS 設計軸 → pN exact-pin → **Rs ratify**。⚠ 私は**再選定を自己起動しない**。

## 6. 影響範囲（実測）

- **D1.1-C v2.5 は D-1 に依存していない** — 同 doc 内の D-1 言及 5 箇所は**すべて §1.1（経緯記録）**であり、機構としては使っていない（v2.5 の中身 = U-5 と `substrate_id`）。⇒ **本裁定は v2.5 を止めない**。
- 依存が生じるのは**後続版**（prereg IN-1 manifest 型 / IN-3 grade 別 proof obligation 供給写像）。⇒ **後続版は B1 の再選定が着地するまで locator を前提にできない**。

## 7. 誠実な範囲

- 本記録は **CC1 の transcription**。Rs channel の一次ログは持たない。**逐語の byte 忠実性は独立検証されていない**。
- §3 の「読み」は**私の解釈**であり、Rs が述べたのは §1 の 1 文である。**解釈が違えば訂正する**。
