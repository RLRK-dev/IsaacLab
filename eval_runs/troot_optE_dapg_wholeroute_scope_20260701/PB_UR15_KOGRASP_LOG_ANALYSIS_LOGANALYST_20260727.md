# pB 数値状態の記録と診断 — UR15 コ形把持 log (`ur15_steps` 2026-07-27)

**作成:** LOG-ANALYST (`w2:pB`)。**発行:** 2026-07-27 11:02:33 JST (date-THEN-write)。
**依頼:** `MSG-P18-PB-RELAY-UR15-KOGRASP-20260727T1050JST-001` (原便 = `w2:p4` RS-TECH-LEAD / 依頼元 = Rs 直接指示「LOG-ANARIST VIDEO-ANARIST を使用せよ」)。
**bank 認可:** `MSG-P18-PB-VERDICT-RECEIPT-AND-BANK-AUTH-20260727T1105JST-010` (`w2:p18`)。
**返信元:** `MSG-PB-P18-UR15-KOGRASP-VERDICT-20260727T1112JST-001` PART 1/2・2/2 ＋ 時刻訂正 `…-CORR-20260727T1101JST-001` (ID 埋め込み時刻 1112 は誤り。実測送信は 10:58 / 10:59 JST。ID は custody 維持のため据え置き)。

**範囲:** 数値状態の**記録と診断のみ**。
⛔ 成立判定 (把持できている / 着座している) を出さない — **物理妥当性の最終判定は Rs の動画確認**、視覚レグは `w2:pC` VIDEO-ANALYST。
⛔ rerun していない / gate flip なし / GO なし / 設計変更なし / 実行認可の要求なし。
⛔ 本書の数値はすべて自分で on-disk 実測した。**他 pane の message の数値を根拠にしていない。**
⛔ `./isaaclab.sh -p` の終了コードは根拠に使っていない (`MSG-P18-ADVISORY-WRAPPER-RC-MASKING-…-009`: wrapper が非ゼロ終了を飲む)。

---

## 0. 対象 artifact と自己照合

| path (repo-relative) | sha256 | 照合 |
|---|---|---|
| `eval_runs/troot_optE_dapg_wholeroute_scope_20260701/p4_ur15_sim_20260727/ur15_steps_20260727_0850_run.log` | `5f3d36588157638a6e7827f6829171ed541d57fc8ff95da3b09be805bec47c9c` | 一致 |
| `eval_runs/troot_optE_dapg_wholeroute_scope_20260701/p4_ur15_sim_20260727/ur15_steps.py` | `5b52fb99476e576e58889888b5971373a41d869592a3cea54363c93e46271723` | on-disk と commit `bfb517862c809943042074bae6eb6e51e4f70875` 上の blob が一致 |

**付随の実測 (依頼外・記録):** 同ディレクトリの `ur15_steps_20260727_0838.log` と `ur15_steps_20260727_0850_run.log` は **byte 同一** (両者とも sha256 `5f3d3658…c9c`、`diff` 差分なし)。`_run.log` の mtime は `2026-07-27 10:32`、`0838.log` は `08:51` ⇒ **`_run.log` は後刻に作られた写し**。⇒ 名前が示す 08:50 の**別 run である証拠は log からは出ない**。

以下、行番号は上記 2 file のもの。`log:N` = log の N 行目、`py:N` = `ur15_steps.py` の N 行目。

---

## 1. `grip=LR` / `grip=-R` は何を根拠に出ているか

**生成箇所:** `py:634`。各 STEP 行を印字する**その瞬間**に `grasped('L')` と `grasped('R')` を評価して文字を組み立てている。

**述語の実体:** `grasped(t)` = `py:326-335`。内容は
1. 現在の接触 `d.contact` を走査し、
2. 一方の geom がその腕の geom 集合 `PADG[t]` に属し、他方が cable geom 集合 `CABG` に属する接触を拾い、
3. 拾った geom の**名前に `left` が含まれれば脚 L、`right` が含まれれば脚 R** と記録し、
4. **L と R の両方が揃えば真**。

**述語が見ていないもの (すべて code 実測):**

| # | 見ていないもの | 根拠 |
|---|---|---|
| 1 | **位置・包含** — ケーブルがコの内側にあるかの判定が無い (包含・深さ・接触法線のいずれも不参照) | `py:326-335` に位置項が存在しない |
| 2 | **geom の種別** — docstring は "on BOTH pads" と書くが、`PADG[t]` は接頭辞 `Lg_` / `Rg_` の**全 geom** | `py:210-211`。XML 実測ではこの集合 = `left/right` 各 4 個 (`*_pad1` `*_pad2` `*_pad_f1ext` `*_pad_f2ext`、`_ur15_2f85_koshape_actuated.xml` の `<geom>` 8 件) ⇒ **爪の外面・先端の接触でも真** |
| 3 | **リンクの同一性** — 左側 geom が `cab18`、右側 geom が `cab21` に触れていても真 | `py:328-334` は側の文字だけを集合に入れる |
| 4 | **接触の力** — 接触の存在のみ。`efc_force` を参照しない | `py:329-332` |

**⚠ 語の衝突:** 表示の `L`/`R` は**腕**、述語内の `L`/`R` は**パッドの左右**。`grip=LR` = 腕 2 本それぞれで、左側 geom と右側 geom が何かに触れている、という意味。

**同じ log 内にある反証 (把持ステップの実測):**

| 実測 | 値 | 根拠 |
|---|---|---|
| L: 指を阻んでいる物 | `cab18` | `log:22` |
| L: ピンチ点から最近傍リンクまで | **5.5 mm** (`cab19`)、触れている geom は **`Lg_left_pad2` の 1 個のみ** ⇒ 脚 R が立たず `grasped('L')` は偽 | `log:23` |
| R: ピンチ点から最近傍リンクまで | **26.7 mm** (`cab22`)、**pad-cable 接触 0** | `log:25` |
| 両腕の pad 間隔 | **24.0 mm** (指令 `ctrl=255` = `CLAMP`、`py:68`) | `log:23` `log:25` |
| grasp gate | **`gates['grasp'] = False`** | `log:41`、算出は `py:605` |

**⇒ 記録される事実:** `STEP 5` (`log:27`)、`STEP 6` (`log:28`)、`STEP14-16` (`log:36-38`) に現れる `grip=LR` / `grip=-R` は、**把持が成立したことを意味しない**。grasp gate が偽を記録した後の、**別の時刻・別の述語**の値であり、搬送中にケーブルが gripper の外面に触れれば真になる。
Rs の視認 (「はじめからケーブルをコ内にクランプできていない」) と **log の数値は矛盾していない** — log は最初から `gates['grasp'] = False` と書いている。

---

## 2. `pin=C1` の発火は着座を意味するか

**表示:** `py:633` — `d.eq_active[EQ['C1']]` が 1 なら `C1`、0 なら `--`。
**発火:** `py:585-589` — `seated()` を 1 度通過した時に `d.eq_active` を 1 にし、`gates['pinC1']` に時刻と座標を記録する。

**`seated()` の中身 (`py:338-349`):**
1. seat link の **body 原点**が箱の中 — `|x−cx| < 0.022` ∧ `|y−cy| < GROOVE_W/2` (= 0.008、`py:54`) ∧ `|z − (TABLE_TOP + CLIP_RISER + 0.030)| < 0.006` (= 0.870 ± 0.006、`py:45` `py:63`)。`py:341-342`
2. **その clip の geom と cable geom の接触が 1 つ以上** — `py:345-348`

**述語の弱い点 (code 実測):**
- 2 は **seat link 自身を要求しない**。別のリンクが clip の壁に触れていても成立する (`py:347` は `CLIPG[clip]` と `CABG` の任意の対)。
- docstring (`py:339`) は「the seated link touching the **groove floor**」と書くが、code は**床を指定しておらず壁でも成立**する ⇒ **記述が code より強い**。
- **latch である。** `d.eq_active` を 0 に戻す code が file 内に無い ⇒ `STEP10`-`STEP18` の `pin=C1` は**再測定ではない**。

**⛔ log 自身が「pin 後に保持されていない」ことを示している:**

| 時刻 | seat link (`cab21`) の実測 | 溝の窓との関係 | 根拠 |
|---|---|---|---|
| t=16.00 s (発火) | `[0.1607, 0.3483, 0.8686]` | 箱の中 (x/y/z とも) | `log:41` |
| t=17.6 s (`STEP 9` 行) | `y +0.250` / `z-table +0.044` | **y は窓 (0.350±0.008) の外へ 100 mm**、z は窓 (64–76 mm) の **20 mm 下** | `log:31` |
| `STEP10`–`STEP18` (9 行) | `y 0.253 … 0.344` / `z-table 0.004 … 0.176` | **y と z を同時に満たす行が 1 つも無い** | `log:32-40` |

**窓の照合を機械で行った結果 (`STEP 2`–`STEP18` の全行):** y の窓 (0.342–0.358) を満たすのは `STEP14` のみ (0.344)、z の窓 (0.064–0.076) を満たすのは `STEP15` のみ (0.070)、**両方を同時に満たす行は 0 行**。
⚠ **限界:** STEP 行は y と z しか出力しない (`py:632`) ため、**箱の x の脚は行からは検証できない**。上表は y と z の 2 脚についてのみの判定。

行の `c1[...]` と `seated()` が記録する座標は**同一の body** (`py:630` と `py:340` がともに `d.xpos[CAB[SEAT1]]`、`SEAT1` = `cab21`、`log:1`)。

**⇒ 記録される事実:** `pin=C1` は「eq を有効化した」以上の意味を持たない。**着座の証拠にならない。** 有効化の 1.6 秒後には、pin した当のリンクが溝の窓の外にある。

### 2.1 🔶 推測 (未確認・私は決めない)

`py:165` の `<connect name="C1_pin" body1="C1" body2="cab{SEAT1}" anchor="0 0 0.030" active="false"/>` について、**相手側 (body2) の anchor は model の compile 時、すなわち初期姿勢 `qpos0` で決まる**という仮説。
初期姿勢では cable は `y = REST_Y = 0.28` / `z = REST_TOP + CABLE_R = 0.955` に置かれ (`py:48` `py:110-111`)、C1 の anchor は `y = 0.35` (`py:49-50`) / `z = 0.870` ⇒ **両者は約 110 mm 離れている**。
仮説が正しければ、途中で有効化する操作は「今の位置で固定する」ではなく「**初期姿勢の相対配置へ引き戻す**」動作になり、§2 で観測した pin 後の移動方向と整合する。

🔶 **これは推測であり、確認していない。** 決め手は model の `eq_data` の相手側 anchor を読むこと (1 行) だが、**実行は CLOSED のため行っていない** (`MSG-P18-PB-…-010` の条件)。判断は本書の court ではない。

---

## 3. log から支持されない数値 (依頼 (c))

| # | 主張 | log/code の実測 | 状態 |
|---|---|---|---|
| 1 | 「両手で把持」 | `gates['grasp'] = False` (`log:41`)、R の pad-cable 接触 0 (`log:25`) | **不支持**。⭐ ただし **p4 が既に自分で撤回済** — `P4_UR15_HANDOVER_TO_IMPL_CHAIN_20260727.md:32` `:84` `:102` ⇒ 原因側の訂正は済んでいると読む |
| 2 | ケーブル径 **Ø8** (`P4_UR15_HANDOVER_TO_IMPL_CHAIN_20260727.md:70`、出所を `task_config.py:137` とするもの) | producing script は `py:47` で `CABLE_N, CABLE_SEG, CABLE_R = 32, 0.030, 0.005` ⇒ **直径 10 mm / 節 30 mm / 32 節 (全長 960 mm)**。script 自身の注記 `py:70` も「cable is 10 mm」。SSOT は Ø8 / 節 15 mm (`thread_isaac_lab/configs/task_config.py:136-137`) | **この run には当てはまらない**。⇒ 同 doc `:75` の「全閉パッド間隔 24.2 mm に対しケーブル Ø8」は **run の値と SSOT の値を混ぜている**。log のケーブルは Ø10 |
| 3 | 「全閉パッド間隔 **24.2 mm**」(同 doc `:75`) | log の実測は **24.0 mm** (両腕・`ctrl=255`、`log:23` `log:25`)。24.2 は script の注記 `py:70` の値で log の測定値ではない | 差 **0.2 mm**。measurement でなく注記 |
| 4 | 「C2 未着座」(同 doc `:85`) | `gates` に `pinC2` の key が無い (`log:41`)、全 STEP 行の C2 側が `--` (`log:20-40`) | **支持される** |
| 5 | 「保持するのはパッドではなく爪」(同 doc の実測メモ) | script の設計注記 `py:69-72` と整合 (全閉でパッドは離れたまま、爪はピンチ点の 25.8 mm 上、`py:64-65` `py:72`) | **整合**。⚠ ただし `grasped()` はその**爪の中の保持を判定していない** (§1) ⇒ **設計意図と判定述語が一致していない** |

---

## 4. 本書が主張しないこと (範囲の明示)

- **把持や着座の成否を判定していない。** 数値は「述語が何を見ているか」と「log に何が書かれているか」に限る。物理妥当性の最終判定は **Rs の動画確認**であり、視覚レグは `w2:pC`。数値単独の合否は出さない。
- **述語を直すべきか、どう直すかの設計判断をしていない。** 設計は別の court (`w2:p11` ARM-CONTROL-DESIGN / `w2:p5` SKILL-DETAIL-DESIGN)。
- **§2.1 の仮説を確認していない。** 実行は CLOSED。
- **rerun していない。** 本書のすべては既存 artifact の読み取り。

---

**custody:** 本書の内容は `MSG-PB-P18-UR15-KOGRASP-VERDICT-20260727T1112JST-001` PART 1/2・2/2 (送信実測 2026-07-27 10:58 / 10:59 JST) と同一。差分は本書に加えた出典の明示のみで、数値の追加・変更はない。

---

## 5. 射程注記 — 解放・着座に触れる記述 (追記 2026-07-27 18:55:46 JST ・依頼 = `MSG-P18-PB-SCOPE-DUTY-SEATED-OBSERVATIONS-WERE-STILL-HELD-20260727-258`)

**追記のみ。上の判定・数値は一切変更していない。** 対象の有無は本 doc への閉じた query で確認 (着座 5 件 / 解放 0 件 / grip 3 件 / seat 7 件)。

**(a) 新事実 (p18 中継・出所 `P4_RELEASE_SWEEP_20260727.md` @ `c0b3e36cdc` + p5 裁定):** `grasped()` は**両方向に誤る** — True は貫通経由でも立ち、**False は「囲まれたまま」でも立つ** ⇒ ⛔ **`grip=--` は解放の証拠にならない。**
⇒ **本 doc への影響 = なし。** §1 は `grip=LR` が把持成立を意味しないという **True 方向のみ**を述べており、`grip=--` を解放と読む記述は本 doc に存在しない (閉じた query で確認)。⇒ ⚠ **本 doc の §1 を「grip=-- なら離した」へ拡張して読まないこと。**

**(b) ⚠ p18 の「本日の全 run で STEP 17 の解放は未発生」は、本 doc の run を覆っていない (私の実測):**

| | 本 doc の run (`ur15_steps.py` @ `bfb517862c`) | p18 の掃引対象 (`ur15_steps_reaim.py` @ `e9f93a7556` / `ur15_steps_c1seat.py` @ `439470e6db`) |
|---|---|---|
| 誘導手の指令 | `HALF = 170` (`:68`) | `HALF = 214` |
| 開の指令 | `OPEN = 0` | `OPEN = 18` |
| ケーブル径 | **Ø10** (`CABLE_R = 0.005`, `:47`) | **Ø8** |

⇒ p18 の数値 (爪先間 1.82 mm で Ø8 不通) は **HALF=214 / Ø8 で測られたもの**。本 doc の run は **HALF=170 / Ø10** ⇒ ⛔ **本 doc の run が STEP 17 で解放したか否かは UNKNOWN**。「解放しなかった」とも「した」とも読まないこと。`log:39` の `STEP17 解放 … grip=--` は (a) により**解放の証拠にならない**。

**(c) §2 の pin 後の観察に付く射程:** §2 の表 (`STEP10`–`STEP18` の seat link の位置) は、**その間に手がケーブルを保持していたか否かを確認せずに**取られている。(a)(b) より手の状態は本 run では UNKNOWN ⇒ ⛔ **リンクが動いたことを「pin が保持に失敗した」根拠として読まないこと** (手が運んだ可能性を排除していない)。
⭐ **§2 の結論は不変**: 「`pin=C1` は eq を有効化した以上の意味を持たず、着座の証拠にならない」は **表示が何を意味するか**についての主張であり、リンクが動いた原因の帰属に依存しない。

**(d) §3 の行 4 (「C2 未着座」= 支持される) に付く射程:** 支持の根拠は `gates` に `pinC2` の key が無いこと = **pin が一度も発火しなかったという code 状態の事実**であって、**物理的な着座状態の観察ではない**。

### 5.1 (b) の UNKNOWN は解けた — 解放は幾何的に可能だった (追記 2026-07-27 19:08:33 JST)

**契機:** `MSG-P18-PB-YOUR-UNKNOWN-IS-RESOLVED-RELEASED-20260727-277`。**⚠ ただし以下は中継された数値を使わず、掃引 artifact の公開表だけで私が導いた。**

**出典:** `eval_runs/troot_optE_dapg_wholeroute_scope_20260701/P4_RELEASE_SWEEP_20260727.md` (内容 sha256 `c8a49e0efcab0513e3995b0eb94ba1101cb36e3c8416e5be9c46c19ae5571ee2` ・on-disk と commit `c0b3e36cdc` の blob が一致)。⚠ **同 artifact の公開表に `ctrl 170` の行は無い** (grep 0 件) ⇒ 中継された「背板 28.35 / 爪先 18.19」は本 artifact では確かめられないので**使わない**。

**私の導出 (公開表 8 点のみ):**
- 爪先間は ctrl に対し**単調減少** (機械確認)。
- `ctrl 170` は表の `160` (爪先 **21.86**) と `190` (爪先 **10.79**) に**挟まれる** ⇒ 爪先間 ∈ [10.79, 21.86] mm。
- 本 doc の run のケーブルは **Ø10** ⇒ ⭐ **最悪側の 10.79 mm でも 10.00 mm を上回る。**

⇒ ⭐ **`HALF = 170` では Ø10 が爪先を通過できる** ⇒ **本 doc の run の STEP 17 は、幾何としては解放できる側**。⇒ §5 (b) の **UNKNOWN を解消**する。
⛔ **限定:** これは**通過できる幾何**の判定であって、「実際に離れた」の観察ではない。ケーブル在中の停止点は本掃引の対象外 (artifact `:61` が明記)。実際の離脱は視覚レグと Rs の判定による。
⚠ **前提 1 件 (明示):** 掃引 model は開口 **14.00 mm** 版、本 doc の run は **10.00 mm** 版。⭐ 開口の変更は爪を **z 方向にのみ** 動かしたもの (f1ext 0.0382→0.0402 / f2ext 0.0258→0.0238 ・**y と size は不変**・私の実測) で、爪先間は **y** の量ゆえ ctrl→爪先間の対応は移ると見なした。⛔ この移送自体は測っていない。

⭐ **全体像 (p18 中継・記録のみ):** 解放能力は `170` → `214` の版更新で失われ、STEP 17 の再実装 (`ctrl 189.3`・p5 承認待ち) が回復させる見込み。
