# pZ — formal verify leg on the nominated instrument revision `120746a49b` (kinonly_step_solve.py)

**Author** pZ / IMPL-VERIFIER (`w2:pZ`), role brief `VERIFIER_ROLE_BRIEF_pZ_20260721.md` @ `4714493e64`
**Written** 2026-08-10 00:19–00:50 JST, on the trigger m-p18-263. Naming per the ruling relayed in m-p18-256: **Rs1 = the human; Rs2 = p4/CC** — first use in this artifact, adopted throughout.
**Judged against** the nominated commit's **own parent** `4f3385968f` — never a tip, never a fixed integer.
**Nothing here is a physical-validity claim** (Rs1's court, role brief `:28`); nothing wired ran; the only executions were the frozen instrument's read-only functions, my own stand-ins, and one isolated regeneration copy (§10).

## 0. Nomination, freeze, chain — verified at this desk

- Parent of `120746a49b` = `4f3385968f` ✓. Commits touching the file after the nomination: **0** ✓. Worktree == nominated blob (empty `git diff`, empty `git status`) ✓.
- The one-commit-per-run chain, re-read: `bbc500b636` 22:44:58 → `d8badd92d3` 23:01:49 → `f531b019b2` 23:16:52 → `4f3385968f` 23:34:02 → `120746a49b` 23:54:47, each touching the file ✓.
- §8.44 @ `912820d0e1`: blob sha256 full form **`290f8978f80d07b6f9b760db1f970325013c2cddb10b2553eb6e04225cb46dc1`** (relay's `290f8978f8…` completed), heading at `:2052` ✓. Ledger §1368 @ `18d74ca6e6` present ✓.

## 1. Handover objects — third-desk content pins (2026-08-10 00:19:43 JST)

| object | sha256 at my read | matches |
|---|---|---|
| `_gen/shakedown10.log` (108 lines) | `2d82e0af1b10342b64bb0b8bec9a6387514841f2ab8aff9200414f91285eb866` | p0 read-time, p18 00:16, me |
| `_gen/kinonly_solutions.json` (36 rows) | `58af7ebe0f60e557d466d906d3ba2bad87ffd98038d2a289c7712e674a9bdb1c` | same three desks |

Both untracked (`git ls-files` 0) — consumed by content-sha per my R4; the SD7-era bank I pinned at 22:49 (`788fe874…`) is a different object and stays my record of that world.

## 2. The parent-relative diff (+34/−8, one file) — matches its own subject, nothing else

- **S1 graze fix**: `REST_XY` x from `0.0` → widest saddle-free window midpoint. Arithmetic re-derived by me from the spec constants the comment names: posts at REST_X (−0.300, −0.055, +0.245) → windows 245/300 mm → wider midpoint **+0.095**; jaw edges at ±GRIP_HALF_SPAN(0.044) → clearance to both neighbouring post edges (−0.041, +0.231) = **92 mm, symmetric** ✓. The bank's STEP-2 targets carry it: `target_L x = 0.051 = 0.095−0.044`, `target_R x = 0.139 = 0.095+0.044` ✓.
- **Per-family chains**: `prev` becomes `{"v1", "v2"}`; two-z rows advance each family from its own solved instance; single-z stations advance both. The SD9 defect (a v1 path starting from a v2 prev 213 mm below its datum) is structurally removed; the along-path column now sweeps a row's own motion.
- No unrelated hunks: the diff touches REST_XY derivation, the referent message, the chain logic, and the adjacent `[budget]` print context only.

## 3. Static leg on the blob — (4)v2 and (5)v2

Mechanisms re-proven first (selftest re-run this session): 9/9 predicates fire on their own violations, stay silent on the compliant fixture that NAMES a driver in comment/string (the D-2(c) case), and survive the three hostile fixtures (nested parens ×2, multi-line). Then applied to `git show 120746a49b:…` (994 lines, 431 Call nodes parsed as the walk-liveness control):

- **Imports, complete list (12)**: `__future__`, sys, os, json, math, pathlib, re, numpy, mujoco, `scipy.spatial.transform.Rotation`, **`ur15_cell_spec`**, collections. None of the five driver-family names (`ur15_steps_wired`, `ur15_route`, `ur15_steps`, `ur15_steps_reaim`, `ur15_steps_c1seat`). **Driver-family imports = 0.**
- **Execution-capable calls, complete list (1)**: `cell.compile()` at `:312` — `MjSpec.compile()`, a MuJoCo model build on the spec object, not Python code execution. Published, disposed.
- **(5)v2**: `mj_step`/`mj_step1`/`mj_step2` = **0**. `mj_forward` 2 (`:536` axfix on throwaway MjData, `:594`), `mj_kinematics` 3 (`:679, :700, :716`), `mj_comPos` 1 (`:680`) — comPos is a position-stage sub-computation of the allowed `mj_forward` (it advances no time); named here rather than silently classified. The `:679→:680` pairing is the SD4 Jacobian fix (fresh `cdof` before `mj_jacSite`).

## 4. Runtime leg — the counter, the audit record, the in-process control

- **Stand-in liveness (the control the instrument assigns to my leg at `:106-107`)**: on my own two-body XML, the instrument's exact form (`RAW = mujoco.mj_step` → rebind to a counting wrapper) counts **2 after 2 real steps**, the audit hook sees **0** mujoco events during those steps (why the wrapper must exist), and qpos moved (the steps were real).
- **The run's audit block (in the log)**: `mj_step calls: 0` · suspect readings 466,830 → bound-substituted 438,190 / kept-as-contact 28,640 = **6.134%** (the declared 6.1% residue, recomputed) · import/exec/compile counts with per-source attribution · **Popen: 2, both argv-published** as `env_isaaclab7 python -c "import ctypes… get_version…"` from `subprocess.py:1823` — library version probes, self-identifying, not drivers · `system: 0` · `sys.modules at exit: 589` published (the hook cannot see pre-install) · **DRIVER-FAMILY modules present: 0**.
- **In-process control at my desk**: `K._MJ_STEP_CALLS == 0` after every placement/re-measurement I ran through the frozen instrument's own `closest()`.

## 5. The m-p18-262 scope clauses, measured

- **(i)** `geom_groups` at the nominated revision: L=38, R=38, env=25 (== log). `base_link_inertia` in L/R: **NONE**; `crown` ∈ env; both bases ∈ env **as structure** — and STEP 1's env reading (`+51.5`, `L_upper_arm_link#6 <-> L_base_link_inertia#4`) shows moving links measured against the mounts. The exclusion took the commanded form: bases left the clearance sets, the crown never left env.
- **(ii)** residual negatives are real signal: the −0.6 mm pad↔table reading carries a witness of exactly 0.647 mm for a −0.647 mm distance (consistency check TRUE) — geometry, not the discharged class.

## 6. Re-measurement at a second desk — the bank's contract holds

Placed banked winners on the frozen instrument (read-only kinematics; `mj_step` counter 0 throughout):

| sample | table says | my re-measure | pair reproduced |
|---|---|---|---|
| STEP 2 single Z_RISE_REST | arms +16.9 / env +21.3 | **+16.9 / +21.3** (bank floats 16.9332/21.2610) | ✓ both |
| STEP 3 single 0.812 (v2) | arms −0.9 / env −0.6 | **−0.9 / −0.6** (−0.8548/−0.6472) | ✓ both |
| STEP 16 design(+0.075) GROOVE_CENTER_Z | arms −0.2 / env −3.5 | **−0.2 / −3.5** | ✓ both |
| STEP 15 design(+0.075) verbatim | arms +5.7 `>=bound` / env +50.8 | **+5.7 `>=bound` / +50.8** | ✓ incl. the tag |

- **Path cell re-derived independently**: my own 19-interior-sample straight joint interpolation from the bank's `path_from` to the winner q reproduces row 2's **−115.2 mm at 6/20, same pair (`Lg_right_coupler#17 <-> stem`)** — the table's deepest negative is the declared path model, correctly computed.
- **Anchor control, independent**: `grep -c '^| STEP | Ph |'` on the canonical table = **1** (instrument published `anchors matched: 1`); termination `:61` is indeed the first non-`| <int> |` line (`| 19-26 |` fails the row pattern). The canonical table is tracked, worktree-clean, blob `5515bbafcd` — the run consumed committed content; its `:229` z-generation sentence read with my own eyes, arithmetic checked (1.0668 − 0.2548 = 0.8120 = the [z] line's runtime cross-check).
- **SSOT reconciliation (§運用2 [VERIFY])**: `task_config.py:20` TABLE_HEIGHT = 0.80; `:93` GRASP_Z = TABLE + CLIP_BASE + EE_TO_FINGERTIP = 1.025 — the canonical `:229` Franka-generation formula sits **verbatim in the SSOT**; CLIP_POSITIONS C1 matches spec.C1 under the declared axis transposition (control printed 0.0, expect 0.0), C2 differs by exactly the known DEV-C2X **35.0 mm** (expect 35.0) — which the `cell(0.040)/design(+0.075)` both-values candidates carry through every C2 row.
- **Table controls**: every numeric cell names its pair (incl. `>=bound` tags that round-trip through the bank); expects printed as numbers (`C1 = 0.0 (expect 0.0), C2 = 35.0 (expect 35.0)`); budget per-row (`it/pool/env-clear/clear-pairs/sel`) + global `[budget]`; the maxmin fallback is declared per-row in words, twice in the stronger env-capped form.

## 7. D-2(c) — fifteen citation targets, thirteen exact, two drifts, one wrong revision label

Verified exact against the named blobs (`2fba2dfd67`): spec `:352` crown-"none" removal · spec `:670` TABLE_Y centreline · spec `:421-424` crown-reaches-mounts · driver `:244-247` mast trio · `:186-194` saddles · `:148-161` CLIP_PARTS · `:182-183` crown capsule · `:426-448` `_measure_axfix` (throwaway MjData wording verbatim) · `:1448-1462` 6D form (0.6 rot weight, 0.05² damping, half-step at `:1457-1459`) · `:1414` sign-on-both (p5 −167) · `:1376-1378` nearest-prev quote · spec §6.4d GRASP_ATTITUDES (65 at runtime import).

- **Drift 1 (c9)**: `re_max=0.02` sits at driver `:732` (sole occurrence); cited `:733` = the closing line of the same call. Claim true, one line off inside one statement.
- **Drift 2 (c13)**: the quote "holds each arm on one IK branch…" sits at `:751-752`; cited `:753-756`. Claim true, range off by two.
- **F1 (c14) — right lines, wrong revision label**: the instrument's counter cites its form's precedent as "`ur15_steps_wired.py:1644` raw save / `:1819` rebind **@ 2fba2dfd67**". Closed queries at `2fba2dfd67`: module-attribute rebinds **0**, bare attribute saves **0** — the precedent does not exist at that revision. It exists **exactly at `:1644` (`_MJ_GEOM_DISTANCE_RAW = mujoco.mj_geomDistance`) and `:1819` (`mujoco.mj_geomDistance = _mj_geom_distance_audited`) in the current driver** (worktree == HEAD `0d2b91c496`). The line pair was read from the live file; the rev label was carried by habit from the neighbouring citations. The counter itself is self-contained and proven live (§4) — the mechanism is unaffected; the fix is a one-token revision correction, p0's to make.

## 8. Scopes the table carries and my leg passes through (not holes in the instrument — bounds on the object)

- **Cable absent** — declared on the object (`[cell] ⚠ the CABLE IS ABSENT by design … row 48 is open`); the sub-3-mm work-row standoffs are exactly where an 8 mm cable changes the answer.
- **Straight-joint-path model** — declared in `[budget]`, now independently re-derived for the deepest cell (§6).
- **Kept-contact residue** — the 28,640 kept zeros surface in the table as **17 kept-zero cells** (16 path `+0.0 at i/20`, 1 endpoint `+0.0 (Lg_base_mount#11 <-> Rg_base_mount#50)`), all pair-named, none `>=bound`-tagged. Under my R1/R2 these read **attribution-unknown, conservatively gated as touching** — never as proven contacts, never as clear.
- **§8.44's headline numbers are all on the object**: v1 endpoint ranges (+5.7…+23.9 / +20.3…+50.8), the v2 negatives row by row, `clear-pairs 0, sel=maxmin` with env-clear pools L1-4/R0-3 on every design-height work row, TOUCHING 33/36 (3 CLEAR rows counted: 4-v1, 8-v1, 9-v1).

## 9. What this leg does NOT establish

Physical validity of any pose or path (Rs1's court). Design-worthiness of the design-height negatives (Rs2 (=p4)'s consumption read, next in the chain). Whether the four mounting C-2 edits should unlock (p4's derivation from the table, after their read). The wired driver stayed un-imported and un-run at this desk throughout.

## 10. Determinism

`main()` re-run in an isolated directory copy (same interpreter `/home/rlrk/env_isaaclab7/bin/python`, stack newton 1.4.0 / mujoco 3.10.0 / mujoco-warp 3.10.0.3 / warp-lang 1.15.0, seed 20260809 from the code), completed 00:47 JST, rc = 0:

- **The regenerated bank is byte-identical**: sha256 **`58af7ebe0f60e557d466d906d3ba2bad87ffd98038d2a289c7712e674a9bdb1c`** reproduced in full (36 row-instances; the audit block's 466,830 suspect resolutions re-run).
- Captured stdout matches the banked `shakedown10.log` up to exactly four benign classes: the six header lines interleaving later in my capture (stdout block-buffering under redirect vs unbuffered stderr — same lines, same content, at `:13-18`); the two absolute paths that name the copy (UserWarning `:312`, `[bank]` destination); and `[audit] compile: 342` vs `341` — one extra compile event, explained by the copy's cold bytecode cache (`cp -r` resets source mtimes, invalidating the copied `__pycache__`). Every table row, referent line, and other audit line is identical.

⇒ **Determinism CONFIRMED at a second desk: both handover objects are a pure function of (code @ `120746a49b`, seed 20260809).**

## 11. Verdict (four clauses, no bare word)

1. **Structure**: at `120746a49b`, judged against `4f3385968f`, the instrument is driver-family-0 on every surface measured (imports, execution-capable calls, runtime modules, spawn argv) and step-0 by three independent instruments (AST walk, the run's own live counter, my stand-in-proven wrapper form).
2. **Controls**: the table's declared controls exist and discriminate — pair-per-row with `>=bound` tags that survive the bank round-trip, expects as numbers, per-row budgets and declared fallbacks, an anchor count I reproduced independently.
3. **Reproduction**: a second desk reproduces the object — 4/4 sampled endpoint cells exact to the bank's floats, the deepest path cell re-derived exactly from `path_from`, witness-consistent negatives, and the full regeneration reproduces the bank **byte-identically** (§10).
4. **Findings that ride along**: F1 — one citation names a revision where its content is absent (the content sits at exactly the cited lines in the current driver; one-token fix, p0's); two 1–2-line citation drifts (c9, c13); and the object's own scopes (cable absent, path model, 17 kept-zero cells = 6.134% residue class read under R1/R2). None of these moves a number on the table; all of them ride with it into Rs2 (=p4)'s read.

## 12. Provenance

All measurements by pZ from `git show`, `sha256sum`, `ast.parse`, and read-only placements via the frozen instrument's own functions; leg scripts in this session's scratchpad (`pz_leg_selftest.py`, `pz_leg_apply_120746a49b.py`, `pz_leg_remeasure.py`, inline stand-ins), perishable, their outputs transcribed here. Zero modifications to tracked content by pZ; this file is a new untracked path. ⛔ pZ holds no measured grant to commit (re-measured 2026-08-09: `Vault Write Permissions.md` @ `55dae1a15a`, `pZ|IMPL-VERIFIER` → 0, control non-discriminating) — **written, not banked; banking requested of a custodian.**

---

## ADDENDUM 2026-08-10 01:01 JST (ruling A: appended, nothing above rewritten)

### A1. F1 and the drifts are closed at `99b2d7d672` — comment-only, verified hunk by hunk at this desk

Four sites fixed (+7/−5, no behaviour): the counter's precedent now cites **`@ 93adb43eb7`** — verified here as the driver's own last commit (17:32:06, "Give the recorder one absence and one radius"; worktree clean against it, so my §7 measurement of the save/rebind **at exactly `:1644`/`:1819`** transfers to that blob) · `re_max` `:732` · branch-pin quote `:751-752` in **both** of its homes — the code comment AND the printed `[budget]` f-string. That second home is the nuance: the banked `shakedown10.log` carries the old token `:753-756`, so a rerun at the new tip differs from the banked stdout by **exactly one printed token**; the solutions bank carries no citation text, and the byte-identity claim of §10 is untouched. The nominated blob `120746a49b` is immutable and unmoved — this leg's object did not change.

### A2. The fourth site was outside my §7 population — the gap is mine, with its mechanism

`99b2d7d672` also fixes a **fourth** drift my leg never reported: the warm-start citation, `:1434-1436` → `:1431-1432`. Verified here against `2fba2dfd67`: the warm-start comment ("warm-start EVERY tool pose from the previous waypoint…") sits at `:1431-1432`; the cited `:1434-1436` is the pinned-menu comment three lines below. **Why my leg missed it**: my §7 inventory matched lines carrying `@ <rev>` or `.md` **on the same line**; this citation inherits its revision from the block's "@ 2fba2dfd67" two lines up and escaped the pattern. So §7's "fifteen citation targets" carried an unstated scope — *lines matching my two patterns*, not *citations in the file* — and the drift count my leg reported (3) was one short of the file's truth (4). p0's class sweep over every citation token ("never build the recheck from the named strings alone") is the complete form — the same corrective shape as the AST walk that replaced my regex family. Verdict clauses unchanged; clause 4's finding list gains this site by this addendum.
