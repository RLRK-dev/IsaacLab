# pB — t42 trace の読み（p18 `-775`・送信者 = p4・per-run gate）

**測定者:** LOG-ANALYST (`w2:pB`)。**発行:** 2026-07-29 00:31 JST（date-THEN-write）。
**依頼:** p4 の実測 4 点を「**壊しに行く**」形で検証 + **左腕 stall の機構仮説**（行番号根拠つき）。
⛔ **numeric 単独 PASS は出しません。** 視覚レグは別計器で並行中・verdict は p4（私は読みのみ）。

---

## 0. 読んだもの（pin）

| 対象 | pin |
|---|---|
| t42 trace | `p4_ur15_sim_20260727/T42_RUN_TRACE_20260729.txt`・内容 sha256 `178479bf8336d810ed3e4c7e2062f009f6505cbf7ba2aaff13483d4da397c6cf`（p18 申告と一致・**161 行**） |
| ⚠ **取得の状態** | ⛔ **この trace は commit されていません**（`git log --all -- <path>` = 空、`git status` = `??`）。⇒ **作業ツリーの読み**であって banked artifact ではありません。mtime `2026-07-29T00:14:07`・私が 00:26 に同 sha で凍結写しを取得。⇒ ⚠ **後から同じ bytes を取り出す保証が在りません**（p4 が上書きし得ます） |
| producing driver | `ur15_steps_wired.py` @ **`576cb8f029`**（「Start from the cell's home pose」07-28 23:51）・sha256 `4934d38af91ca989cb0d7152bb6fd1df20322bd53a6772f95bace9db1d68a6bb` |
| 参照 | `00-DESIGN-STATUS-LEDGER.md:157`（DDR #53）・on-disk as-read 2026-07-29 00:28 |

---

## 1. ⭐⭐⭐ 左腕 stall の機構（依頼の中心・行番号根拠つき）

producing driver `576cb8f029` `:2358-2363` 逐語:

```python
_room = 1.0 - max(float(np.abs(np.array([d.qpos[a] for a in QADR[t2]]) - qcmd[t2]).max())
                  / TRACK_TOL[t2] for t2 in SIDES)
if _room <= 0.0:
    held_ticks += 1
prog = min(1.0, prog + dprog * max(0.0, _room))
```

⭐ 読み取れること:
1. **`prog` は 1 本しか在りません**（`:2334` で初期化・step ごとに 1 個）⇒ ⭐⭐ **左右で共有された 1 本の指令ランプ**です。
2. `_room` は **両腕にわたる `max`** の 1 − 倍率。⇒ ⛔ **どちらか一方が自分の許容の 1 倍以上遅れていれば `_room ≤ 0` になり、指令は *両腕とも* 止まります。**
3. 印字の「5.2 mrad」は `:2546` の `min(TRACK_TOL.values())` ⇒ **両腕の許容の最小値**。⇒ ⛔ **この数は片側を指しません。**
4. `_stalled` は `:2497-2499` の `if s_ - _last_gain >= steps` ⇒ **1 step 分の tick で `prog` が 1 度も増えなければ stall。**
5. ⚠ `_settled`（`:2492-2493`）も **両腕の `max`** ⇒ **整定判定も結合**しています。

### ⭐ 数値で閉じます（trace の STEP1 から）

| 量 | 値 | 出所 |
|---|---|---|
| 許容 `TRACK_TOL` | **5.175 mrad** = 8.0 mm / 1546 mm | `:21`／driver `:384` |
| 左腕 STEP1 の関節誤差 | **[−20.7, −123.3, −233.5, −0, −0, −0] mrad** | trace `:35` |
| 最悪 / 許容 | ⭐ **233.5 / 5.175 = 45.1 倍** | 私の計算 |
| ⇒ `_room` | ⭐⭐ **1 − 45.1 = −44.1** | 同上 |
| 左腕の駆動力 | `[206.9, **433.**, **204.**, 0, 0, 0]` N·m（上限 433/433/204） | `:36` |
| 飽和 | ⭐ **j1 と j2 が True** | `:38` |
| 左腕の接触 | ⭐⭐ **`['column (via g6 on L_forearm_link)']`** | `:31` |

⇒ ⭐⭐⭐ **機構仮説（証拠つき）: 左前腕は STEP1 の時点で柱に接触し、j1 と j2 が上限（433 / 204 N·m）に張り付いており、そのため関節誤差が許容の 45 倍に達している。共有ランプの `_room` は初手から −44 で、以後 1 度も正にならない。したがって `prog` は 0.0 のまま動かず、STEP2-6 の全 tick が held になる。**

⇒ ⭐ trace の実測とも一致: STEP2-6 の held は **10560/10560・21600/21600・21600/21600・9600/9600・13440/13440**（`:51 :80 :118 :130 :142`）＝ **1 tick も進んでいません。**

---

## 2. 読み (i)「STEP2-6 全段で左腕 STALLED」 → ⭐ **半分は成立、半分は壊れます**

### ⭐ 成立する部分
STEP2-6 の 5 本すべてに `reached 0.0% ... held back on N of N ticks ... <- STALLED ... <- THE MOVE DID NOT FINISH`（`:51 :80 :118 :130 :142`）。⇒ **stall は実在し、全段です。**

### ⛔ 壊れる部分 — **この行は「左腕」と言っていません**
- ⛔ **5 本の `COMMAND` 行はいずれも側を持ちません。**逐語は「**the arm** was more than 5.2 mrad behind」で、`prog`・`held_ticks`・`_stalled` は §1 のとおり **左右で共有された 1 個の量**です。
- ⛔ **「5.2 mrad」も側を指しません** — `:21` の実測は **L も R も 5.18 mrad で同値**、印字は両者の `min`。
- ⭐⭐ **右腕も許容を超えています**: `:112` `GRASP R: joints vs commanded [-43.7 -63.9 46.7 **97.2** -52.4 -74.] mrad -> servo did not arrive` ⇒ **97.2 / 5.175 = 18.8 倍**。
⇒ ⛔ **「左腕が STALLED」は、trace が支持する形ではありません。**支持されるのは「**共有ランプが凍り、両腕とも指令が進まなかった**」で、⭐ **その原因側は左腕**（§1 の 45 倍 + 柱接触 + 2 関節飽和）です。

### ⭐⭐⭐ 壊した結果として出てくる、より強い読み
右腕は `:113` で **`touching nothing`・飽和なし・駆動力すべて 0** です。⇒ ⭐⭐ **右腕は塞がれてもおらず、力も出していない — 指令が来ていないだけ**です。
⇒ ⛔ **t42 における右腕の未到達は、右腕自身の問題ではなく、共有ゲート経由で左腕の jam に従属しています。**

---

## 3. 読み (ii)「mast L=−0.8 mm INSIDE THE MAST が STEP2-6 持続・R=+60.1 mm」 → ⭐ **数値は成立、意味づけが 3 点壊れます**

⭐ **数値の確認**: `:52 :81 :119 :131 :143` の 5 本すべてが `mast L=-0.8 mm (g6 on L_forearm_link vs crown) R=+60.1 mm (g42 on R_shoulder_link vs crown)   <- INSIDE THE MAST`。⇒ **両値・両 geom とも 5 段で完全同一。**

### ⛔ 壊れ 1 — 「持続」は 5 回の観測ではなく **1 つの状態の 5 回のサンプル**です
§1 のとおり **指令は 1 tick も進んでいません**（`prog` = 0.0）。⇒ ⭐ **腕が動いていないのだから、同じ値が 5 回出るのは当然**で、**独立な 5 件の証拠にはなりません。**

### ⛔⛔ 壊れ 2 — −0.8 mm は **run が作ったものではなく、開始姿勢に既に在ります**
`:31` 逐語: **`STEP1 L arm touching: ['column (via g6 on L_forearm_link)']`**（＝ STEP2 より前）。
`:26` 逐語: **`start pose = the cell's home, both arms: [-3.1521 -0.2867 2.4674 -1.3953 1.5634 -1.5782]`**
⇒ ⭐⭐⭐ **左前腕は「cell の home 姿勢」の時点で柱に接触しています。**⇒ ⛔ **「STEP2-6 で持続」より「開始姿勢が既に内側」の方が、trace の言う所です。**

### ⛔ 壊れ 3 — R の +60.1 mm は **右腕の余裕ではなく、取付の固定スタンドオフ**です
- `:52` 他: R 側の勝者は **`g42 on R_shoulder_link`** = ⭐ **柱に直付けされた body**。
- ⭐⭐ 同じ **+60.1 mm** が **左でも出ます**: `:94/:96/:98` 逐語「`mast at the pose +60.1 mm (g4 on **L_shoulder_link** vs crown)`」。
⇒ ⭐⭐⭐ **+60.1 mm は左右で同一の取付スタンドオフ**であり、**その腕で他に近いものが無いときに現れる床**です。⇒ ⛔ **「R=+60.1 mm だから右腕はマストから 60 mm 離れている」とは読めません。**

---

## 4. 読み (iii)「grip 全編未成立・STEP2 aim L seat error 117.96 mm / R 1.46 mm」 → ⭐ **数値は成立、切り分けが 2 点壊れます**

⭐ **数値の確認**: `:49` `aim L: … seat error **117.96 mm**   <- nothing seated it; best effort`／`:50` `aim R: … seat error **1.46 mm**`。grip は `:62 :91 :129 :141 :153` の全 step で `grip=--`。⇒ **どちらも成立。**

### ⛔ 壊れ 1 — 117.96 mm は **STEP2 限りの数**で、**STEP3 で 1.93 mm に回復**しています
`:63` 逐語: **`STEP3 L: standoff re-aim (y,z only), seat error  1.93 mm`**
⇒ ⭐⭐ **狙いは回復しました。回復しなかったのは「狙いに到達する能力」の方**です（L の tool 誤差は STEP3-6 で **254.7 / 256.1 / 277.5 / 299.3 mm**・`:91 :129 :141 :153`）。
⇒ ⛔ **117.96 mm を run の狙い誤差として引くと、実際より悪く見えます。**⭐ **分けるべきは「狙い（1.93 mm まで回復）」と「到達（一度もせず）」**です。

### ⛔ 壊れ 2 — L の狙いの失敗は「118 mm ずれた」ではなく「**座らせる解が無く、最善努力に落ちた**」です
`:49` 末尾の逐語: **`<- nothing seated it; best effort`**。⇒ ⭐ **解が 118 mm 外れたのではなく、解が見つからなかった。**⇒ 別種の失敗で、対処も別です。

### ⭐ 併せて読める補強（grip 側）
`:106/:115` `clamped=False`・`:108/:117` **`pad separation 93.3 mm, ctrl=18`** ⇒ ⭐⭐ **GRASP の印字時、両手とも全開（ctrl 18 = OPEN）**で、`:107/:116` **`fingers blocked by nothing`**。最近ケーブルは L **142.6 mm** / R **76.9 mm** 先。
⇒ ⛔ **「掴めなかった」のではなく、その瞬間には閉じてすらいません。**

---

## 5. 読み (iv)「停止面 = fingers 41.5° vs 5.73°（DDR #53 と同型か）」 → ⛔ **同型ではありません**

⭐ **数値の確認**: `:157` `STEP7 R: fingers **41.5 deg** off straight down …, allowance **5.73 deg** -- margin −35.80`／`:161` の `RuntimeError`。⇒ **成立。**

### ⛔ DDR #53 との照合 — **41.5° は 2 つの閾値の *どちらの外側* にも在ります**
DDR #53（`LEDGER:157`）の逐語要旨: **解き側** `re_max=0.30` rad = **17.19°** を受理／**検査側**の許容 **5.73°** ⇒ **3 倍の食い違い**。
⇒ ⭐⭐⭐ **#53 の形が予言するのは「5.73° < θ ≤ 17.19° の姿勢が、解かれてから検査に落ちる」ことです。t42 の 41.5° は 17.19° の 2.4 倍で、解き側の受理域の外**です。
⇒ ⛔ **どちらの閾値を採っても 41.5° は通りません。**⇒ **#53 の「同じ量に 2 閾値」では説明できない別の事象**です。

### ⭐ t42 の内部では、その 2 数は **一致**しています
`:44` 逐語: `vertical check: allowance **5.73** deg, cap **5.73** deg` ⇒ ⭐ **本 run の垂直検査の中に食い違いは在りません。**

### ⭐⭐ では 41.5° はどこから来たか — trace から言える範囲
`:161` の例外文自身が逐語で言います:「the pose can also arrive by **inheritance, which does not pass through the solve**」。
そして §1 のとおり **STEP2-6 で指令は 1 度も進んでいません**。⇒ ⭐⭐⭐ **STEP7 で検査に掛かった姿勢は、STEP2-6 の解きが届けた姿勢ではありません**（解きは候補を出していますが、指令がそちらへ動いていません）。
⇒ ⭐ **「解かれて落ちた姿勢」でも「検査を素通りした姿勢」でもなく、*凍った指令が腕を置いていった姿勢*が検査に来た** — これが #53 と違う 3 つ目の経路です。
⚠ ⛔ **STEP7 R の姿勢が解き由来か相続由来かは、trace からは決まりません**（STEP7 には `inherited aim pose` の印字が無く、垂直検査が先に例外を投げています）。

---

## 6. p4 自身の付記（「(i) が真なら (ii)(iii) は整定前の腕の測定・DDR #49 同型」） → ⭐⭐ **成立、しかも p4 が言うより強いです**

DDR #49 は「**整定ゲートに到達しない ⇒ 以後の数値は動いている腕の測定**」。t42 はそれより厳しい状態です:
- ⛔ **整定以前に、指令が 1 度も進んでいません**（`prog` = 0.0・全 tick held）。
- ⇒ ⭐⭐⭐ **`AS REALISED`（`:54 :83 :121 :133 :145`）が測っているのは「動いている腕」ですらなく、「指令が動かなかったので開始姿勢付近に留まった腕」です。**
- ⇒ ⭐ したがって **(ii) の mast 値も (iii) の grip 状態も、run が到達した状態ではなく、開始条件の測定**として読むのが正確です。

---

## 7. ⚠ 私が立てて、自分で壊した候補（送る前に潰したもの）

**候補: 鏡映 build の右グリッパが左と非対称ではないか。**
`:156` L `pads **+7.48 mm** apart at the COMMANDED opening 236`／`:157` R `pads **+0.80 mm** apart at the COMMANDED opening 236` ⇒ **同じ指令で 6.68 mm 違う**。
⛔ **しかし同 run に反証が在ります**: `:133`/`:145` の `AS REALISED` は **L も R も `pads +3.79 mm`** で**完全一致**。
⇒ ⛔ **固定的な左右非対称は支持されません。**⇒ STEP7 の差は指の実現状態の差であって、資産の非対称の証拠にはなりません。**候補は取り下げます。**

---

## 8. ⭐ 依頼外だが同じ 2 行から出る 1 件（判定はしません）

`:156` L: `lowest point **geom6** at **−84.2 mm** vs the table`／`:157` R: `lowest point geom72 at **−27.5 mm** vs the table`
⇒ ⭐⭐ **STEP7 の時点で、検査に掛かっている姿勢は両腕ともテーブル面より下に在ります**（左 84 mm・右 28 mm）。⚠ `geom6` は `:52` より **`L_forearm_link`** ⇒ **左前腕はマストの内側に在りながら、同時にテーブル面の 84 mm 下**。
⛔ **run を止めたのは指の角度で、この 2 つを止めた検査は在りません。**（⚠ これが欠陥かは設計判断であり、私は判定しません。）

---

## 9. ⛔ この trace で決められないこと

1. **STEP7 R の姿勢の由来**（解き / 相続 / 凍結の残り）— §5 のとおり印字が無い。
2. **左前腕が home 姿勢で柱に触れる理由**（cell = 0.22 / 45° の取付と home 姿勢の組合せか、鏡映 build 側か）— trace に姿勢の分解が無い。
3. **`prog` が 0 のまま腕が少し動いた分**（R の tool 誤差は 10.4→86.8 mm と変化）が、重力・接触・指の動作のどれによるか。
4. ⚠ **本 trace の後日再取得**（§0 のとおり未 commit）。
5. **PASS / FAIL**。本書に verdict は在りません。

---

## 10. ⛔ 私が主張しないこと

- 視覚判定（別計器が並行中）。
- 設計の可否（共有ランプを分けるべきか等は p4 の court）。
- t42 の結論を他 run へ持ち込むこと。
- §1 の機構仮説を「確定」と呼ぶこと — ⭐ **code と数値は閉じています**が、⚠ **私は run を再現していません**。

---
**pB LOG-ANALYST measurement / 2026-07-29 00:31 JST**
