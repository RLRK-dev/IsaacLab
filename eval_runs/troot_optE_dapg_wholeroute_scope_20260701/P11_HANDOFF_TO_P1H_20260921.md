# ARM-CONTROL-DESIGN 引継ぎ（旧 `w2:p11` Claude → `w2:p1H` Codex）

**作成** 2026-09-21 02:00:56 JST（`date` 実測）・**旧担当** Claude `w2:p11`・**依頼** m-p18-515（利用者逐語「herdrにおけるp11の役割を君に移管させたい」・⚠ 文型は意図形）
⛔ 本書は**記録の引継ぎ**であって、判断・受入・run 認可の移譲ではない（それらは p4 と Rs1 = `w2:p19`）。⛔ 当卓は役割 label を動かさない。

## 1. 正本と最終 commit

- **設計書（正本）** = `eval_runs/troot_optE_dapg_wholeroute_scope_20260701/P11_UR15B_CONTROLLER_DESIGN_20260913.md`
  **最終 commit `5c6a608ae014aada052c529aaefd57cf8daf832d`**・blob `36ecff5d25f22f465231db3db6d857bafe030422`・sha256 `e31c4af8ea25455789f328e0d09eb3f93f708d31c7a62c1c464ea8248de88d19`・**552 行**・HEAD で clean。
- **§17 は append-only**（唯一の非 append は `:1` の印の更新 = §17.5 の規則による・§17.23）。§10 = leg の表（R0/R1/R1′/R2/R3/R4）。
- **cross-session 記録** = `thread_isaac_lab/thread-vault/02-Workflow/HANDOFF_p11_armcontrol.md`（最終 `7b3a76e1f9`・guard 前後 PASS）。
- 当卓の per-pane memory = `~/.claude/projects/-home-rlrk-IsaacLab/memory/handoff_cc_p11_armcontrol_2026-08-08.md`（Claude 側の面・参考）。

## 2. 今の状態（2026-09-21 02:00:56 JST 時点）

1. **#69 再撮影 run = p4 受理済**（kickoff item 95 @ `70e6417e46`）。⛔ 射程は**形の履行と 5 記録の一致のみ** — 工程の成否・把持・route・物理妥当性・行 68 閉鎖・node COMPLETE・training-ready を**含まない**（§17.31）。
2. **利用者（人間）の目視は #69 に未適用**。Rs1 役は 2026-09-21 00:21:28 JST 以後 `w2:p19`（Codex）。**委任は述語を移さない**（§17.31・p4 item 95 B）。
3. **次の 1 run = 形 v3（N1-N11）で文言確定・未認可**。発火には **Rs1 の新しい一語**が要る（§17.39・§17.41）。
4. **R0 targets の再導出条件 = 確定形 ①-⑧**（当卓の ⑥⑦⑧ が採用・item 97 @ `e47cc510a3`・§17.31）。推奨 3 knob は ①-⑧ を触らない（§17.37・当卓が blob 自読）。
5. **#69 の因果の頭** = 左の開始姿勢が **13 候補すべて非 clear のまま採用された**（run.log `:51`/`:54`/`:57`/`:60`）。rest 接触・飽和・stall は結果（§17.43）。**#69 の位置づけ = C かつ loud 行あり**（bar 未達・§17.45 で当卓が自己訂正）。

## 3. 残件（⛔ いずれも self-start しない・word 待ち）

- **(a) driver の `GRIP_XML` が共有 tree の絶対 path**（item 78 B・object は無傷・以後の形では `grip_xml` sha == blob sha の照合行を p0 §8 に必須）。**p11 court**。
- **(b) 近接カメラの構図設計**。要件は **(i) 注視点（両 pinch の中点をやめる）と (iii) 方位**の 2 本に絞られた（**(ii) distance は現行 0.40 m で充足・p4 が候補から除外**・§17.34-§17.36・§17.39）。方位を変えるなら**映像卓 `p1D` の左右読み替え規約も同時に更新**。実装は依頼の後・`/geometric-design` の 6 ステップで出す。
- **(c) 推奨 run が走ったときの読み** = **(bar, loud 行) の対**で A/B/C（§17.43・p4 が (N11)(c) に採用）。p0 の §8 記録 3 点（両側の loud 有無・逐語（候補数 N）・直後の collision-free 値）。
- **(d) 未解決の差 1 件** = 左の棄却内訳で **p4 は「柱 11」・当卓の python 集計は「柱 12」**（計 16・相手腕 3・経路 1）。質的結論は不変（§17.45）。
- **(e) 未確認の前提 1 件** = `SPREAD_TILT_SWEEP_TRIES240_KINONLY.txt` `:54`（C-2 既定で L free 5）の**把持中心**が #69 と同じ 0.150 か（p4 の別便に「08-02 は中心 −0.200」）。⇒ 「左に clear な開始姿勢が存在する」と断じない（§17.45）。

## 3b. p4 から設計 court へ開いている 5 件（p4 m-p4-319 → m-p18-516・kickoff item 117 @ `3cee58767e`・**依頼ではなく一覧**）

1. **カメラ要件 (i)(iii) の実装** — 注視点を把持点か片腕の pinch へ／stem・crown と交差しない方位。**(ii) distance 0.40 m は充足済**。⚠ 方位変更時は映像卓の左右読み替え規約も同時。**実装の依頼は Rs1 の語の後**・p4 は source を触らない。（納品物 `38e404cf81` の §4 と追記 2）
2. **§17.4 の 4 札化** — p4 は採らず **Rs1 へ持つ提案として保持**（現行 = 3 分類 ＋「その他」の本文に機構を名指す）。
3. **§17.37 (a) の撤回は受領済・(N11) は p4 が発行**（形の文言 = p4・採否 = Rs1）。
4. **#69 の札の原因節の refinement**（上流 = 13 候補が 1 本も通らず棄却姿勢を採用／rest 接触は結果）は p4 が発行済。**設計面の異論は後継の語で**。
5. **p4 からの未応答依頼は 0 件。**

## 3c. 引継ぎ直前に当卓が閉じた 1 件（§17.46）

- **左の棄却内訳の「柱」は 12**（p4 の追記 3 は表に `5+4+2+1` を挙げながら「= 11」と書いている = 足し算のずれ・総計 16 = 12＋相手腕 3＋along 1 は 12 でしか閉じない）。**質的結論は不変**。
- ⚠ **この内訳は棄却理由の数であって候補の数ではない**（左 = 13 候補に対し理由 16／右 = 12 棄却に対し理由 21）。「16 件中 12」を候補数と読まない。

## 4. 当卓が**自分で照合していない** pin（未照合・後継が blob で実測すること）

- **pB の verdict `ff396f98e6` / `77ba6e0b25`**・**pC の `ffd3c5200c`**・**Rs1 (p19) の目視記録 `cf44999454` 内 `RS1_RUN69_REVIEW_20260921.md`** = **未読**（当卓は hub/p4 の relay で状態のみ把握）。⚠ ただし `cf44999454` の **RS71 §0 差分と新 spec `UR15-B-Controller.md`（50 行）は当卓が読んだ**（§17.31）。
- ~~p4 の納品物 追記 3/4 `38e404cf81` = 未読~~ → **2026-09-21 02:02:18 JST に当卓が追記 3 を読了**（§17.46 の決着はその読みから）。追記 4 は relay の逐語のみ = **未照合**。
- **p0 の §8 各 addendum・kickoff の各 item 本文**は、当卓が cite した item（22/24/35/37/39/49/51/56/60/63/65/67/69/73/75/78/86/91/93/95/97/104/105/110/111/114 のうち本文を直読したもの）以外は**relay 経由**。
- **LEDGER / state.md / DDR の行**は p6 の commit を stat で見たのみ（行 69 の head は読了）。

## 5. 当卓を縛っていた規則（後継も同じ枠で）

- ⛔ **self-start しない**: cycle 3・v4・build・run・driver / `pose_menu` の編集・04-Specs の編集（**CC read-only**）。
- **§0 不変前提に触れる案は STOP → Rs 確認**（`RS71-System-Spec-SSOT.md` §0・当卓は `cf44999454` で #2-#5 が byte 同一なことを確認済）。
- **commit** = explicit pathspec ＋ `--no-verify`（共有 tree で `isaaclab.sh -f` を走らせない）＋ trailers。**数値は出力からコピー**（§17.36）・**cite に述語を付けるときは行の種類を `grep -n` で確認**（§17.41）・**行数は `--numstat`**（§17.40）。
- **pin は p18 へ**（§27 の 3 行形式・⛔ backtick と行頭 `EOF` を本文に入れない・artifact の path + 完全 SHA を根拠欄に）。⚠ **hub の tool は Codex 卓を拒否する** ⇒ Rs1・映像・ログ・検証・計画への配達は**検証できない**（§17.38・§17.40）。⛔ **映像卓へ数値を送らない**。
- **呼称**: 2026-09-21 00:21:28 JST より前の記録の「Rs1」= 利用者（人間）／以後 = `p19`。旧 `pB`/`pC`/`pZ`/`p6` は退役し **`p1E`/`p1D`/`p1F`/`p1G`**。⛔ 遡って読み替えない。

## 6. 受付終了

- **当卓は 2026-09-21 02:00:56 JST をもって新規受付を終了する**（この時刻が hub の宛先切替の成立点）。以後の ARM-CONTROL-DESIGN 宛は `w2:p1H`。
- **未処理の便 = 0 件**（最後の受領 = m-p18-514 = p4 m-p4-318 → §17.45 で自記録を訂正済／m-p18-515 = 本移管依頼 → 本書と宣言で応答）。
- 履歴と pane は保持・⛔ 旧卓へ重複送信しない。
