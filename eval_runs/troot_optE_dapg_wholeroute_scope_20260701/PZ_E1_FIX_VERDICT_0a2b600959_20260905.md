# pZ — verdict on the F-d/F-c fix `0a2b600959`, judged against the driver's previous state `b19c4c5f5d`

**Author** pZ / IMPL-VERIFIER (`w2:pZ`) · **Written** 2026-09-05 08:18 JST on m-p18-295. Naming per m-p18-256: **Rs1 = the human; Rs2 = p4/CC**.
**Object**: `0a2b600959` (08:13:24, "Capture the driver path at import so RUN_METRICS.json is written on every exit path"), **1 file, +10/−9**, all three code hunks inside the E1 block. Parent commit `39f19fcc31` (a ledger bank); the driver's previous blob is `b19c4c5f5d`'s. Content sha256 **`307868a9721d2896e3f4849fe18a6297d2034fefe2c276b9f3bd4d7325fb90a5`** (== p18's pin). ⛔ Nothing wired ran; the driver was never imported.

## Rows

| # | row | verdict | evidence (my measurement) |
|---|---|---|---|
| 1 | scope | **holds** | 1 file; hunks at `:38-56` (comment reflow, the capture, `_RM_ENV`), `:77-84` (`here = _RM_FILE.parent`), `:103-110` (`"driver": xml(_RM_FILE)`) — nothing outside the E1 block |
| 2 | window budget (m-p18-294) | **holds — exactly at the budget** | cumulative since the window opened, `git diff --numstat b19c4c5f5d^..0a2b600959` on the driver = **120 added / 0 removed / net 120 ≤ 120** (E1 119 + F-d 1; F-c folded at **net 0** by re-wrapping the tuple, as Rs2 (=p4) required) |
| 3 | control invariance | **holds, mechanical** | ordered control-class sequence sha **`426aa229deb2cb2b`**, 57 lines, identical to the parent; live state writes 0; `d.ctrl` 7 same order |
| 4 | the fix itself | **holds** | `_RM_FILE = Path(__file__).resolve()` captured **at import** (`:48`, beside `_RM`); the handler's two former global reads now use it (`:81` `_RM_FILE.parent`, `:107` `xml(_RM_FILE)`); global `__file__` reads inside the handler: **0** (the block's single remaining `__file__` is the capture line) |
| 5 | three exit paths, real `__main__` | **holds — the defect is gone** | E1 block extracted **verbatim** from the landed blob and run as `python file.py OUT > run.log 2>&1` (env7 3.12.3), sidecar by `sha256sum` after exit: **raised** → `end_reason` "raised", `exit_code` 1, `exception.message` **byte-equal** to the log's `RuntimeError: ` line, at-write sha == sha(prefix) == sidecar, tail 0 B; **completed** → "completed", 0, same integrity; **exited_early** → as before. `driver.sha256` populated on all three. (The same leg fired on `b19c4c5f5d`: raised/completed → NO JSON — the control was taken before the fix, on the unfixed blob.) |
| 6 | F-c | **closed** | `_RM_ENV` now **24 names**, **set-equal** to my corrected population (23 by literal-double-quote grep + `P4_OLD_SEAT_AIM`, read at driver `:84` with single quotes): `CABLE_BEND_STIFFNESS_OVERRIDE` added; nothing spurious, nothing missing |
| 7 | no run | **holds** | stand-ins only; L1 remains Rs1's word (§1413); route run (2) unmet |
| 8 | pins | **holds** | commit, content sha256, parent blob named; §8.47 @ `2add15b8a7` exists ("…with the negative control that my own fixture lacked") |

## Notes that ride (none blocks)

- The comment reflow at `:41-42` dropped the sentences "armed on the P4_CLIP_DUMP early exit … Reads globals defensively" — the **code** still does both (`globals().get` throughout; registration at `:139` precedes both `SystemExit` paths), verified by reading; wording only.
- p0 added the missing negative control to their own fixture (§8.47) — the fixture-shape lesson of F-d is closed on their side too.
- What an L1 run (if Rs1 authorizes it) will and will not show: it exercises the `SystemExit` path, which worked before and after; the two paths the fix repaired are proven here by stand-in, and will be exercised for real only by the next authorized route run (L2).

## Verdict

**All eight rows hold. The writer now writes on all three exit paths, with byte-equal exception text and a hashed log prefix that equals the launcher's sidecar; the control surface is untouched; the window closes exactly at its 120-line budget with F-c inside it at net 0.** The "do not read an L1 PASS as a writer PASS" hold from PZ-209 is **lifted** for this revision.

## Provenance

`git show`/`diff --numstat`; `sha256sum`; own `pz_e1_ctrl.py` (parent and commit); own `pz_e1_fixleg.sh` (numstat, budget, block extraction, three-path stand-in, field/byte checks) — self-tested to **fire** on `b19c4c5f5d` before being trusted on `0a2b600959`; `ast.literal_eval` of `_RM_ENV` against `env_names_23.txt` + the single-quoted name. Zero tracked-content modifications by pZ. **Written, not banked; banking requested of a custodian.**
