# DQ7 DAgger build — 5体 pre-debate DECIDE (CC1=%12) → FAIL(v1) → Rs "A" = minimal v2 budget-test

**Author:** RS-TECH-LEAD (%12, CC1 lead). **Written:** 2026-07-04 00:23 JST.
**Object:** `DQ7_DAGGER_BUILD_SPEC_COORD2.md` (v1). **Debate:** CC2-5 challengers + CC6 NHA (all 5 returned, `general-purpose` subagents, independent context, code-verified).
**Verdict:** **FAIL (v1 spec)** — 3 CRITICAL (independently found) + many HIGH; the DAgger DIRECTION is UPHELD (mechanism sound), the v1 BUILD is re-architected. **Rs decision 2026-07-04 00:2x = "A"** = build a MINIMAL single-offset 1-rollout DAgger iteration first (using the approved rollout) to cheaply falsify the on-path/off-path BUDGET question, before any full 8-proc m×k build.

---

## §1. REBUT_OR_ACCEPT (per challenge; no silent dismissal)

### CRITICAL (all ACCEPTED — code-verified, load-bearing)
- **C1 relabel "per-phase constant" is EMPIRICALLY FALSE (CC2+CC5, independent) → ACCEPT.** Within one offset's episode the abs target RAMPS: Rz span phase0 **392mm** / phase1 97mm / phase3 48mm (only phase2 ~0.6mm const) — measured from `b2_dataset_v2/bc_dataset_abs_val.npz` (CC2). The centre = `GRASP_YC`=mean(settled-cable-Y) (`test:3941`) + `x_grasp`=mean(settled-cable-X) for dx≠0 (`:3953`) = per-episode PHYSICS/settle outcome (CC5). ⇒ the "trivial constant lookup = cheap" premise is void. **FIX: relabel per CONTROL-STEP** `T*(o,t)` = demo-o's abs target at the same schedule-clock step t (rollout shares the clock, `runner:675`), reconstructed from THIS rollout's own settled centre; assert 88mm span/row (INV#2). Supersedes %12-R2 (offset-specific) → step-AND-settle-specific.
- **C2 runner SystemExits on the live tree (CC5) → ACCEPT.** `ROUTE_SHA_ACCEPT` (`runner:35-39`) lacks the live route sha `7f5bc711` → `_assert_shas:138` STOP. "Reuse a working runner" needs a **route-sha re-sync + marker-integrity gate** (grep ⛔ANTI-REVERT still at 4477/4493/4593 before blessing the sha, so re-sync cannot launder a reverted marker).
- **C3 rollout-free boundary is procedural-only; "rollout PROHIBITED" is in NO rule file, HIGH-COST-GATE misses a 5-6min rollout (CC3) → ACCEPT.** `grep rollout prohibited.md CLAUDE.md`=0 hits (it's a this-session convention). **FIX: mechanical `--rs-go <token>` guard** (SystemExit unless present, mirrors `_opt_i_prime_check:356`) on the runner `--policy` path + `dagger_loop.py`, converting the convention into an airtight gate; state the convention explicitly.
- **C4 §9 {2,3} co_seg is a THIRD confound; UNCHANGED og has NO co_seg-free {2,3} path (CC4+CC5) → ACCEPT.** `movable_all_go:453`/`null_beat:455` include raw {2,3} co_seg γ⊥ (`og:97` co_seg for p≥2); "read {2,3} co_seg-free" contradicts "og UNCHANGED". γ⊥≤0.5 is WRONG-SIGNED for {2,3} (co-moving tracking → γ⊥≈1 correct). **FIX: §9 gate = clean {0,1} ONLY** (rename goal "movable {0-3}"→"clean {0,1}"); {2,3} NOT γ⊥≤0.5-gated (co_seg-free probe = a separate code add if needed later, not for the minimal).

### HIGH (accepted; those applicable to the minimal are v2-required, multi-iteration ones deferred)
- **H1 relabel data sources don't exist (CC2) → ACCEPT (minimal-required).** No per-row offset field in base npz; no 15-phase rollout dir (only b0/b1p = 13-phase/25-D). **FIX: per-offset 15-phase dataset-dir via `route_demo_to_bc.py` conversion** (for the minimal = the ONE chosen offset), sha-pinned.
- **H2 feasibility-filter needs in-scene IK, "reuse fix-⑤ reach-screen" callable doesn't exist, cheap analytic has false-negatives on the <1mm-fragile route (CC2+CC3) → ACCEPT (minimal-required).** **FIX: compute `(s,T*_p)` IK residual DURING the rollout** (one extra `solve_ik_dual(T*_p)`/step, dump it); relabeler filters on that ground-truth; analytic screen = cheap pre-filter only.
- **H3 feasibility-filter can drop the off-path restoring states DAgger needs → reproduces (iv) tension mislabeled as representation-limit (CC4) → ACCEPT (minimal-required).** **FIX: instrument the off-path distribution of KEPT states (‖ee−on-path‖), not just counts**; require off-path-but-feasible states survive before attributing a plateau.
- **H4 §9 OG γ⊥ evaluated at ON-MANIFOLD demo rows, blind to the off-path states DAgger adds (CC2) → ACCEPT (minimal-required).** **FIX: for the minimal, ALSO evaluate γ⊥ at the policy's visited off-path rows** (the budget question is about off-path restoring) + keep the fixed-eval {0,1} as the anchor; strengthen the rollout-SR-confirm into a hard gate before any GO.
- **H5 device-parity single-offset ≠ batch-parity + {11}-scoped fragility (CC5) → ACCEPT (DEFERRED to full build).** Minimal = single-offset single-GPU → moot; for full, per-offset parity OR unconditional single-GPU×4.
- **H6 R1(mock-only) too strict — demo-replay (`policy is None`:691-693) is NOT a policy rollout, is permitted at build (CC3) → ACCEPT, supersedes %12-R1.** Build byte-identity/truncation via demo-replay (permitted); POLICY rollout (`policy is not None`) stays token-gated. Also: R1 lived only in the scratchpad — the minimal-v2 spec is the authoritative doc (no stale text).

### MED/LOW (accepted; folded into v2 where applicable)
- truncation trips `_finalize_verdict` INVALID path → reuse `smoke`/`smoke_partial` semantics + write dump BEFORE `_finalize_verdict` (CC3); frame-assert re-scope is tautological → derive expected truncation step from schedule + assert (CC2); seed-pin not cudnn-deterministic → record retrain γ⊥ noise-floor for the collapse test (CC2, matters for full multi-iter); dead-band(0.5,0.7]+K_max, projected-iters-on-flat-slope → slope≥0⇒ABORT (CC4, full multi-iter); DESIGN-GATE-SKIP hides §9 bar reachability → ground-truth the {0,1} bar (CC5); β-mix needs per-step target (CC2, ties to C1); parity/EGL/GPU-cap unvalidated till the leg (CC3-LOW); eval-set pin by sha + aggregate obs_dim assert (CC2-LOW).
- **The one claim CC2 tried to break and could NOT: the β-mix target-space ≡ action-space equivalence is EXACT** (`_abs_decode` affine `:189`) — confirmed sound.

## §2. NO_ACTION_EVALUATION (CC6 NHA)
CC6 = **HOLD** (build premature: capacity PASS removed the on-path/off-path BUDGET constraint that (iv) Outcome-B found conservative-definite-fatal; DAgger re-introduces it via `D_base∪relabel`; the build produces no evidence testing it; {11} exclusion rests on the same-day-REFUTED obs-fix premise). CC6's cheaper offline discriminator (relabel existing dumps) is **NOT turnkey** — %12 verified all on-disk policy-rollout dumps are 25-D/13-phase (≠ current 27-D/15-phase). **Disposition:** CC6's budget concern is ACCEPTED as the central strategic risk; pure NO-ACTION is NOT chosen (the question is answerable cheaply). **Rs "A" = the synthesis:** a MINIMAL single-offset 1-rollout iteration IS the low-cost budget-falsification (feasible via the approved rollout, since the offline-dump route is 25-D-blocked), run BEFORE the full build. {11} stale premise → reconcile in v2 (frame {11} as "same BC-copycat root, deferred", not "obs-fix").

## §3. DECIDE = FAIL(v1) → v2 MINIMAL (Rs "A")
**Scope (minimal budget-test):** ONE DR offset, ONE β=0 pure-π rollout of the current BC-(ii) policy, truncated {0-3}, → per-step relabel (C1) + in-scene IK feasibility (H2/H3) → aggregate into D_base under budget → retrain → OG γ⊥ on clean {0,1} (C4) + off-path-row γ⊥ (H4). **KEEP (v2-required):** C1 per-step relabel / C2 runner sha re-sync+marker gate / C3 mechanical `--rs-go` guard / C4 §9={0,1} / H1 per-offset 15-phase conversion / H2 in-scene IK filter / H3 off-path coverage log / H4 off-path γ⊥ + hard rollout-confirm / H6 demo-replay build-test / frame-assert derive-expected / smoke-semantics truncation. **DEFER to full build (iff minimal passes):** 8-proc/2-GPU + device-parity (H5) / multi-iteration β-schedule + β-confound (CC4) / dead-band+K_max / cudnn-determinism noise-floor.
**Read:** γ⊥ moves toward ≤0.5 UNDER budget (clean {0,1} + off-path rows) → budget was not fatal → scale to full 8-proc m×k DAgger. Plateau like (iv) (conservative-definite) → budget tension confirmed → route to (i)-RL (P2-CRITICAL, Rs re-decide). **Conservatism:** minimal single-offset = NECESSARY-but-non-conservative for the full multi-offset DAgger; a minimal PASS justifies the full build, a minimal plateau is conservative-definite (1 real on-policy iteration under budget failing ⇒ more won't rescue the budget tension).
**Gates:** minimal build = L3 (runner touch + new scripts) → v2 spec → %9 re-validate (conformance to §1 fixes, CP-(ii)-0→v2 precedent, not a full new 5体 debate) → %11 build → the 1 rollout runs under Rs "ロールアウト承認" + the `--rs-go` token (single truncated {0-3} ≈5-6min, single-proc = NOT HIGH-COST-GATE). 0-commit; INVARIANTS untouched; {11} OUT.

---
*RS-TECH-LEAD %12 (CC1) — 2026-07-04 00:23 JST. Debate did its job: caught a false core premise (relabel) + a non-starting runner + a §9 confound + a mechanical-gate gap + the strategic budget risk, all BEFORE any build/rollout spend.*
