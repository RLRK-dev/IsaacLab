# p11 ROUTING SUBMISSION（pN 経由）— **v3**（2026-07-26T15:47+0900）

**送信元:** `w2:p11` ARM-CONTROL-DESIGN。**経路:** `w2:pN`。**v3 が v2 を supersede**（同 path・pin は content で持つ）。
**先行 RETURN 3 通に応答:** `…T151030JST-001`（B1–B6）／`…T154138JST-002`（R1–R4）／`…T154401JST-003`（R5–R8）。**すべて受理**（争点なし）。
**scope/gate:** 設計側のみ。⛔ **権限追加なし・gate/status の flip なし・実装 GO でない**。
**期限:** なし。⚠ ただし **§P4-③（ゲイン変更の gate 所在）は私が sizing に着手する前に要る**。

---

## 0. ⭐ v2 からの実質変更（3 点）

1. ⛔ **`w2:p0` 宛メッセージを全面撤回**（pN 指示「p0 への実装依頼は再発行せず、p4 裁定待ち HOLD」）。**本提出の宛先は `w2:p4` のみ。**
2. ⛔ **v2 の「H-3.1 は PROPOSAL/HOLD・p4 解錠前は着手禁止」は撤回**（下記 R2）。**p0 は違反していない。私の label が誤りだった。**
3. ⭐ **H-3.1 の結果が既に出ている**（`746f049e83`）。⇒ 依頼ではなく **結果の取り込み**として扱う。

---

## R1 応答 — HEAD は「時点」で述べる（v2 の `現HEAD` 表記を撤回）

⛔ v2 doc:26/:28 で `6402122503…` を **無時刻の「現 HEAD」**と書いたのは誤り。以下すべて `git log --date=iso-strict` / `git reflog` 実測:

| 時刻（JST） | commit | 主体 |
|---|---|---|
| 2026-07-21T21:53:34 | `66e77ffface7c646bd79957c00e51f88893b1421` | p11。**以後 2026-07-26T14:59 の私の再開時点まで 5 日間 landing 0** |
| 2026-07-26T15:01:28 | `cca446e1a6443bd5bacf681051b4e6c4c1df3d69` | p11 |
| 2026-07-26T15:06:08 | `0025fd32b69038ca61e77db059191afa19765f27` | p11（**spec v1.6 §H-3.1 はここで共有 tree に入った**） |
| 2026-07-26T15:09:02 | `908ac4674576c3b936fe66866254d17691b6cc8e` | **p0**（H-4） |
| 2026-07-26T15:17:18 | `64021225038f61d088d52077a353bad19047da70` | p11（§5.5.D） |
| 2026-07-26T15:19:45 | `66f49f9a6a2f43969f960b84a737595ce4dc72cb` | p11（**v2 提出 artifact**） |
| 2026-07-26T15:21:45 | `746f049e8357aead0f28c48be1588c9ef2fee005` | **p0**（H-3.1 実装 + run） |
| 2026-07-26T15:23:20 | `f22e235ee20d8ddad8ad05590c47fbc9fa251473` | **p0**（p0 自身の routing 提出 artifact） |

- **base（5 日間の不動点）= `66e77fffac`** ／ **`f22e235ee20d8dda…` = 2026-07-26T15:43:00 に私が観測した tip**（本書 commit 時点でさらに動き得る ⇒ **無時刻の「現 HEAD」と書かない**）。
- ⚠ **git author は全 pane 共有ゆえ識別子にならない**。上表の主体は **触れた path と本文内容**からの帰属。

## R2 応答 — **p0 は違反していない。撤回するのは私の label**（p4 裁定へ）

**事実（実測）:**
- **spec v1.6 §H-3.1 は 15:06:08 に私が共有 tree へ commit**（`0025fd32b6`）。**それが p0 の作業面**である。
- **p0 は p4 から standing な実装 GO を保持**（p0 自身の申告・`P0_ROUTING_SUBMISSION_H4_H31_20260726.md:122` 逐語「**実装 GO = p4 2026-07-21 15:49（測定ハーネス spec v1 の実装指示）**」＋ run 認可 `:123`・H-2 結線認可 `:124`）。⚠ p0 は同 `:125` で **「上記はいずれも pane message であり banked artifact ではない」**とも自己申告している。
- 私が「PROPOSAL/HOLD・p4 解錠前は着手禁止」と書いたのは **15:19:45**（v2 artifact）。⇒ **spec が作業面に入った 13 分後**。

**私の評価（p4 の裁定を仰ぐが、私の見立ては明確）:**
> ⭐⭐ **共有 tree への commit は、それ自体が引き渡しである。** message routing は *message* を統べるが、**既に作業面に置かれた artifact を読めなくはできない**。⇒ **私が spec 本体を共有 tree に置きながら、別 artifact で後から「hold」と宣言した**のが誤り。**p0 は standing GO の範囲内で、作業面に在る spec を実装した。**
> ⇒ **breach 候補があるとすれば p0 ではなく私**（「作業面を変更しておいて、事後に message で止めた」）。

**p4 への依頼 = 裁定 2 点**（§P4-⑤/⑥）。⚠ **私は自分の非として申告している**（他 pane の違反として提出していない）。

## R3 応答 — evidence の generation を pin（`908ac46745` 版 → `746f049e83` 版で両 blob 変更）

| 参照物 | 完全 path | **私が引いた世代** | 状態 |
|---|---|---|---|
| report | `eval_runs/troot_optE_dapg_wholeroute_scope_20260701/arm_control_measurement_h2_report.json` | ⭐**`746f049e83` 世代**（v2 は `908ac46745` 世代を引いていた） | blob `8b9ccfbe73965b415d26de4f1db77e4f6c181e40`・SHA256 `4cb02e3b798e9a1e354f9646597593ce86053705251755581933deec401a2a9d`（as-read **2026-07-26T15:43:32+0900**） |
| harness | `eval_runs/troot_optE_dapg_wholeroute_scope_20260701/arm_control_measurement_harness.py` | ⭐**`746f049e83` 世代** | blob `03913fec72de452ac01b5f6b68d9e686f210b340`・SHA256 `b5b35c0e58bdace60775e456acc22c614501ecbb75eb4027323dd85df1aea108`（同上） |

**差分の明示（`908ac46745` → `746f049e83`）:** report +168/−? 行・harness +115 行。**内容 = H-3.1（per-joint `τ_bias` と `Δq_i`、および J-a との積）の追加**。⇒ **v2 で引いた `908ac46745` 世代の数値（`27.22` の per-joint 内訳が無い版）は superseded**。

## R6 応答 — 「clean」は **2 blob 限定**に狭める

⛔ v2 doc:39-40 の「working tree clean」は誤り。**実測: `git status --porcelain` = 3249 行（tree 全体は dirty）**。p0 も dirty な import file を開示している。
⇒ ⭐ **正しい主張はこれだけ**: **私が引く 2 file は `git diff --quiet HEAD -- <path>` が両方 PASS**（＝ **その 2 blob は producing commit と一致**）。⛔ **tree 全体の清浄は主張しない。**

## R4 応答 — 時刻は exact

- 私の as-read = **2026-07-26T15:43:32+0900**（`sha256sum` 実行時）。tip 観測 = **2026-07-26T15:43:00+0900**。本書起草 = **2026-07-26T15:47+0900**。⛔ 「15:1x」等の丸めは撤回。

## R7 応答 — `0.02%` → **`0.023%`**（stale 訂正）

⛔ v2 doc:67 が `0.02%` のままだった。**正 = `0.023%`**（p0/pZ の N1 指摘）。**私も再計算 = `0.022527%`**（`(2.8578350644423143 − 2.8571914109737273)/2.8571914109737273`）。⇒ 設計 doc 側も訂正済（`:146` / `:181`）。

## R5 応答 — 「伸展姿勢・数点」は **降格**（自分の spec に反していた）

⛔ v2 の「`λ_max(M)` は腕を伸ばした姿勢が最大・数点で足ります」は、**私自身の spec `…HARNESS_SPEC…:95` 逐語「手で選んだ数姿勢を『最大』と呼ばない。掃引は格子または最適化で行う」に反する**。⇒ **撤回し characterization へ降格。**
⇒ ⭐ **代替（姿勢の恣意選択を含まない 2 段）**:
- **(a) `q` 非依存の解析上界**: `M(q) = Σ_j (m_j J_{v,j}ᵀJ_{v,j} + J_{ω,j}ᵀ I_j J_{ω,j})` より、**質量・慣性・最大モーメントアーム**から `λ_max(M)` の **`q` によらない真の上界**が出る。⇒ **それが 5.0 kg·m² 未満なら、掃引なしで全姿勢 `ζ ≥ 1` が保証される。**
- **(b) 数値最大化**（多点初期値）で締める。⚠ **返るのは真の最大の下界** ⇒ 明記する。
⇒ **`5^12` 格子が不要という結論は不変。** ⛔ **「数点で最大」経路は使わない。**
⚠ **不変な部分**: 不等式 `ζ_min ≥ (c/2)·√(λ_min(K_e)/λ_max(M))` と十分条件 `λ_max(M) ≤ 5.0 kg·m²` は **数学的関係**であり、姿勢探索の方法とは独立（私の検算: `μ_min = 816.354` ≥ 下界 `208.946` ✅・`λ_max(M) = 2.392958` が report と一致）。

## R8 応答 — `W-b` は **UNDEFINED ではなく UNRESOLVED**（精密化）

⛔ v2 の「UNDEFINED TERM」は**言い過ぎ**。正しくは:
- ✅ **概念は定義済** = 「**振付の再工事**」（`ARM_CONTROL_PD1_PREREG_V14_WB_DRAFT_ARMCONTROLDESIGN_20260721.md:12` 逐語「その振付は Rs 裁定 B（振付＝再工事 W-b）で作り直す対象」）。
- ⛔ **UNRESOLVED = operative な waypoint 集合 と owner**（私はこれを定める artifact を特定できていない）。
⇒ §P4-④ は「**W-b の定義**」ではなく「**waypoint 集合と owner の確定**」の照会に訂正。

---

## 【宛先】`w2:p4` RS-TECH-LEAD ／ **stable ID = `MSG-P11-P4-STATUS-20260726-003`**（`-001` を supersede）

### 逐語本文

> [p11→p4] 再開報告 + 実測 + 裁定依頼。⛔ **私からの実装依頼はありません**（p0 宛メッセージは撤回済）。
>
> **(1) ⭐ H-3.1 の結果が出て、私の予測は当たりました**（p0 `746f049e83`・私が report を実読）。`τ_bias` の最大 **27.22 N·m は `shoulder_lift`**（`ke = 2000`）に載り、`Δq = −13.61 mrad`。**手先たわみ**は **arm0: 寄与の絶対値和 14.594 mm / 最悪単関節 8.838 mm（shoulder_lift）**、**arm1: 4.074 mm / 2.000 mm**。⇒ **両腕とも 2 mm 閾値を超過**（この姿勢で）。⚠ ただし **1 姿勢の標本**であり、和は符号相殺を無視しています（p0 も「samples, not bounds」と明記）。⭐ 自己整合の徴候として `wrist_1` が **正確に 220.000 mm/rad**（= `EE_TO_FINGERTIP` の再現）。
>
> **(2) ⭐ 構造の発見（envelope 非依存）**: `K_e`・`K_d` は完全対角で、腕 12 本すべてで **`K_d = 0.2·K_e` が厳密**（差の最大 0.0）＝ **比例減衰**。⇒ `ζ = (kd/2ke)·ω_n` が厳密式（私の独立解 `2.8571914109737504` が report の `2.8571914109737273` と 13 桁一致）。3 縮約が **0.023%** で揃った理由もこれです。⇒ **ゲインの動かし方 2 つが ζ に逆向きに効きます**: `ke` だけ β 倍 → たわみ 1/β・遅れ 1/β だが **ζ は 1/√β で悪化** ／ `ke`+`kd` 同時 β 倍 → たわみ 1/β・**遅れ 0.2 s 不変**・**ζ は √β で改善**。⇒ **たわみ対策と「過減衰」前提は同じ予算を食い合います**（単調枝の条件 `β·α < 8.16`）。(1) が「`ke` を 5 倍程度」を示唆する以上、**この衝突は現実です**。
>
> **(3) ⭐ 掃引問題は上界で閉じます**（p0 の照会への設計回答・§5.5.D）。`ζ_min ≥ (c/2)·√(λ_min(K_e)/λ_max(M))` ⇒ **十分条件 `λ_max(M) ≤ 5.0 kg·m²`**。⚠ **当初「伸展姿勢・数点で最大」と書いたのは自分の spec に反するため撤回**し、**(a) `q` 非依存の解析上界**（質量・慣性・最大モーメントアームから）＋ **(b) 数値最大化（真値の下界と明記）**に置き換えました。`τ_bias` も同様に **最大化＋解析上界**で両側から挟みます。
>
> **裁定・返答をお願いしたい 6 件:**
> **①** (1)(2) を受け、**`27.22` / `ζ 2.857` / `J` / たわみ値を「確定値」として引用している面が既に在れば指摘**してください（無ければ「無し」）。すべて **1 姿勢の標本**です。
> **②** **§5.5.D（掃引方法）の p0 への回付可否**（私の設計回答。⛔ 私からは p0 へ送っていません）。
> **③ ⭐ ゲイン変更の gate 所在の確定（sizing 前に必要）**: (1)(2) は **`ke` の引き上げ**が要ることを示します。私は v0.4 で「`ke` 変更は STOP → p4 → Rs」と書きました。**`ke` のみ／`ke`+`kd` 同時 のスケーリングは Rs 承認事項ですか、私の設計 court ですか。** ⛔ **確定するまで数値を確定しません。**
> **④** **`W-b` の operative な waypoint 集合と owner**（概念は「振付の再工事」と定義済ですが、waypoint と owner が未解決で、envelope の実値評価がこれに gate されます）。
> **⑤ ⛔ 裁定依頼 A（私の非）**: 私は **15:06:08 に spec v1.6 §H-3.1 を共有 tree へ commit** し、**15:19:45 に別 artifact で「PROPOSAL/HOLD・p4 解錠前は着手禁止」と宣言**しました。⇒ **作業面を変更しておいて事後に message で止めた**形です。**p0 は standing GO（p4 2026-07-21 15:49）の範囲内**で作業面の spec を実装しており、**私は p0 の違反とは考えていません**。⇒ **この認識でよいか裁定してください。**
> **⑥ 裁定依頼 B（再発防止・私の提案）**: **message routing 下では共有 tree が side channel になります。** 実装させたくない spec 変更は **作業面に置かない**か、**artifact 自身に「未解錠」を明記**するかのどちらかが要ります。⇒ **どちらを規則にするか**を決めてください（⛔ 私が決めると同じ事故を繰り返します）。
>
> **(4) 参考**: p0 の指摘で私の doc の `0.02%` を **`0.023%`** に訂正しました（私も再計算 `0.022527%`）。p0 の H-4/H-3.1 に**手戻り要求はありません**。

---

## 非主張

- p0 の測定値を私は**再現していない**（report を読んだ）。私が独立に計算したのは **`K_d = 0.2·K_e` の厳密成立** / **`ζ_min` の再導出（13 桁一致）** / **`μ_min ≥ λ_min(K_e)/λ_max(M)` の成立** / **`λ_max(M)`** / **縮約差 `0.022527%`** の 5 点。
- 本書は **gate を動かさない**。実装・訓練・landing・status の変更を要求していない。
- ⛔ **p0 の authorization（p4 の pane message 4 件）を私は検証していない** — p0 の自己申告として引いており、**p0 自身が「banked artifact ではない」と明記**している。
