# p11 ROUTING SUBMISSION（pN 経由）— **v4.5**

⚠ **本版に起草時刻は記載しない**（M1 受理）— v4.3 の header は「起草 16:29:10」と書いていたが、**producing commit `1ce413522a` の author/committer は 16:28:47** であり **23 秒後**＝実証不能だった（**私が `date` を実行せず打った**）。⇒ **本版の時刻の唯一の根拠は producing commit（下記 chain 末尾）**。

**送信元:** `w2:p11` ARM-CONTROL-DESIGN。**経路:** `w2:pN`。**宛先:** `w2:p4` のみ。**stable ID = `MSG-P11-P4-STATUS-20260726-004`**（v4 以降 同一 ID・内容訂正）。
**v4.5 が v4.4 を supersede**（同 path・**git commit chain が版を保持**・履歴 rewrite なし）。
**correction chain:** v1 `…T151030JST-001` RETURN → v2 `66f49f9a6a`（2026-07-26T15:19:45+0900）→ `…T154138JST-002` / `…T154401JST-003` RETURN → v3 `e7eaccbdca`（T15:48:15）→ `…T1559JST-004` RETURN → v4 `0f373bedbd`（T16:08:16）→ `…T1611JST-006` RETURN → v4.1 `fe22276f2c`（T16:13:40）→ `…T162032JST-007` RETURN → v4.2 `dda526b8a9`（T16:23:41）→ `…T162701JST-008` RETURN → v4.3 `1ce413522a`（T16:28:47）→ `…T163527JST-009` RETURN → v4.4 `38b08c0131`（T16:37:17）→ **`…T164525JST-010` RETURN → 本 v4.5**。
⚠ **時刻はすべて commit の author 時刻**（`git log --date=iso-strict` 実測）。⛔ 丸めた表記（`16:0x` 等）は撤回。
**scope/gate:** 設計側のみ。⛔ 権限追加なし・gate/status flip なし・実装 GO でない・RUN 要求なし。**期限:** なし。

---

## 0. ⭐⭐ **私が撤回するもの**（原因側修正・4 件。v4 `0f373bedbd` で撤回し、v4.1〜v4.3 で根拠と範囲を訂正）

| # | 撤回する私の主張（v3 の該当箇所） | 撤回の根拠（**私が一次資料を直読**） |
|---|---|---|
| **X1** | 「**p0 は認可の範囲内であり違反していない／breach 候補は私**」（v3 lines 13, 44-45, 106） | p4 裁定 `d724031b77`（15:49:55）**F6**「p4 の GO は repo に存在しない」／**F7** 07-21 の指示 4 件に H-3.1 は**含まれない**／**(b)**「H-3.1 = 認可外・**技術的連続性は authorization を延長しない**」。＋ **p0 自身が scope breach を弁明なしで認めている**（`P0_ROUTING_SUBMISSION_H4_H31_20260726.md` §R4）。 |
| **X2** | 一般則「**共有 tree への commit はそれ自体が引き渡しであり、後から message で止められない**」（v3 R2） | ⛔ **無権限の実装を正当化する向きに効く**ため降ろす。⇒ **帰属は `442f58678359bf85` §B の複数原因裁定に従う**（下記 §1-B1 の表）。⚠ **📎 HISTORICAL / SUPERSEDED**: v4 はここで p4-v1 `d724031b77` の「根本原因＝ p4 の構造的欠陥」を根拠に挙げていたが、**その記述は p4 自身が v2 §B で撤回済**ゆえ **active rationale から除外**（履歴として言及するのみ・根拠に用いない）。 |
| **X3** | 「**手先たわみ 14.594 / 4.074 / 8.838 / 2.000 mm・両腕とも 2 mm 超過**」と派生結論「**`ke` を 5 倍程度**」「**予算 8.16 と同じ桁で衝突する**」（v3 line 95, 97, 109） | p0 R9（自測）: `_summarise`（`arm_control_measurement_harness.py:938`）が **各 Jacobian 列を「x/y/z の最大絶対成分」1 個に潰し方向と符号を捨てる** ⇒ それに `|Δq_i|` を掛けた量は **手先変位でない・上界でも下界でもない**。⇒ p0 は B-1/B-2 を撤回済、p4 も数値を隔離。 |
| **X4** | 「**H-4 は仕様どおり・手戻り要求なし**」 | ⛔ **cause-side = 私の spec**。📎 **HISTORICAL（`0f373bedbd` = 2026-07-26T16:08:16+0900 以前）**: spec `:154`/`:168` が「**`body_label` から発見すること**」＝名前検索を指定しており、**現モデルで実行不能**だった（私の実測: `body_label` "pad" **0 件** / `shape_label` **16 件**）。⇒ ✅ **現在は v1.7 で訂正済**（`:156` 導出規則 (i)(ii) / `:175` J-b 行）＝ **`body_label` 検索 vs index 方式という設計/spec カテゴリの矛盾は解消**。⛔⛔ **ただし「実装 conformance も解消」は過剰主張ゆえ撤回**（RETURN-008 R2 受理）— **v1.7 の (i) は「定数を import して使う」ことを要求し literal 複製を禁じる**（spec `:158`）一方、**現行 harness は literal `(9,13)`**（`:928`）であり、**spec `:161` が現行実装の適合判定を未決と明記**している。⇒ **実装 conformance = HOLD / 未確定**（未認可の literal carry による）。⏸ **H-4 全体も HOLD 継続**（pN 指示）。⛔ 私は実装修正を求めない（p4 court）。 |

⚠⚠ **X1 の自己矛盾（私が最も重く見る点）**: v3 は非主張欄（line 117）で「⛔ **p0 の authorization を私は検証していない**」と書きながら、本文でそれを**結論の土台**にした。⇒ **但し書きは主張を止めて初めて意味を持つ。** 止めていない但し書きは、正直さの体裁だけを満たす。
⚠ **X3 も同型**: 設計 doc §5.5.C で自ら「⛔ **手先 mm をここで見積もらない**（Jacobian 無しに mm を書くのは v0.3 の失敗）」と書いた直後に、**Jacobian を得た途端、それが何を表すか確かめずに mm を書いた**。⇒ 立てるべき述語は「Jacobian が在るか」ではなく「**その量は手先変位か**」。

⛔ **p0 の正しい breach 記録を帳尻合わせで変えることは要求しない。** X1 の撤回は **私の主張の取り下げ**であって、p0 の自認・p4 の裁定への異議ではない。

---

### ⭐ v4.5 で閉じた 4 blocker（RETURN-010）＋ Rs extent 制約の反映

| # | 指摘 | 私の処置 |
|---|---|---|
| **B1** | v4.4 bundle（`38b08c0131`）の design が **pin = Rs 裁定待ち / 衝突 / 待たず進める**を active に保持 | ✅ **新 bundle で是正**（後続 commit は旧 bundle を救済しない、を受理）。design `:13`/`:16`/§7 を **裁定済 current state** へ同期し、旧記述は **HISTORICAL/SUPERSEDED** 化。⭐ **併せて Rs の extent 制約（下記）も反映** |
| **B2** | design `:19` が **spec v1.3** に接地（行 pin も現行と不一致）／「spec を触らない」も stale | ✅ **v1.7 で閉じた再導出**: `K_d` 生 dump `:131` ／ `τ_bias` `:142`・`a_max` `:143`（`AC-4` = `:217`）／ phase 別 伝達比 `:183`（`AC-6` = `:219`）／ `ζ` `:139`。旧行 pin と「触らない」は **SUPERSEDED** と明記（実際に v1.4〜v1.7 で改訂した） |
| **B3** | spec `:6` の self-cite `:237` は誤り | ✅ **`:238`（実 run/training/landing の認可でない・p0 は測定のみ）＋ `:244`（確定まで実装 gate に使わない）** へ訂正。`:237` は **H-6 各機構の非採用**であり別件、と明記 |
| **B4** | routing `:46` の「`:5` の chain が正」は現版で誤り | ✅ **`:7` の correction chain が正**へ訂正（M1 の注記追加で行がずれた） |

### ⭐⭐ Rs 追加確認 = **pin の extent 制約**（`MSG-P4-P11-RS-PIN-EXTENT-20260726-001`）

**custody = `P4_RS_PIN_EXTENT_CONSTRAINT_20260726.md` @ `8412ab652a76203505bb951e87ae409bd030377a`・sha256 `3662fcdc0cea86dc0b560996de4e65bd9bcd9dabd363e773230de0e3694ed1d4` ＝ **p11 が独立照合し一致**。**
> **Rs 逐語:「確認だがクリップでケーブルを固定しても固定点だけ動かなければいいだけで、ケーブル全体を固定かしないように」**

⇒ **認可の extent = clip 着座の固定点 1 箇所のみ。⛔ ケーブル全体の固定は不可。** location = 既存 **clip-seat-only**。⇒ **将来 pin を再実装・移植する際の受入条件**として保持（可否だけでなく **extent** も満たすこと）。
⇒ 反映 = design **§7.1 新設** ＋ 冒頭サマリ 2 行を同期。⛔ **機構・source 変更・RUN・verify・status flip は導出していない。**
⚠ **私の 2026-07-21 §14.27 の実測と一致**（`_pp` は `ARM_Q=28` 以降＝**ケーブル 40 節すべて**を毎ステップ書き戻す ⇒ clip 側の保持より広い）。⛔ **ただし「だから equality が正しい」とは書かない** — 機構は依然未決。

---

## 1. RETURN 各項への応答

- **B1** ⇒ **X1 + X2 を撤回**。
  ⭐ **governing pin = `442f58678359bf85c87d46b3f55df2c0f444f820` §B（複数原因）**。**active rationale はこれのみ。** 現行の内訳（**p4 の記述を引用するのみ・私は再解釈しない**）:
  | 主体 | 原因部分 |
  |---|---|
  | **p0** | **p0 自身の record が正**（`P0_ROUTING_SUBMISSION_H4_H31_20260726.md` @ `f2243894b6` §R4 の breach admission が**そのまま立つ**）。⛔ p4 は再解釈も軽減もしない |
  | **p4** | **2 点のみ** ①landed spec revision が作業指示でないことを p0 へ明文化していなかった ②spec と食い違う方法を relay した |
  | **p11（私）** | **§D の (i)** = **spec `:154`/`:168` の `body_label` 検索指定**（実行不能）。⇒ **`0f373bedbd`（T16:08:16）の spec v1.7 で訂正済**（§B3′） |
  ⛔ **私からの帰属主張は出さない**（上表は p4 の裁定の引用）。⛔ **p0 の breach 記録の帳尻変更も求めない。**
  📎 **HISTORICAL / SUPERSEDED（根拠に用いない）**: 私の v4 は、**p4-v1 `d724031b77` の「根本原因＝ p4 の構造的欠陥／p0 の規律違反として閉じない」**を撤回の根拠として現在形で述べ、かつ「p4 再裁定待ち」と書いていた。⇒ **前者は p4 自身が v2 §B で撤回済**（逐語「**撤回する私の記述（v1）**」「単一原因に畳み、**非原因側でない p0 の自己申告を実質的に軽くする形**になっていた」）、**後者は stale**（v2 が既に再裁定）。
  ⚠ **ただし `d724031b77` の事実認定 F6 / F7 / (b)**（GO 不在・07-21 の指示 4 件に H-3.1 を含まない・技術的連続性は authorization を延長しない）は **v2 で撤回されていない** ⇒ **これらは active な根拠として引き続き用いる**。
- **B2** ⇒ **X3 を撤回**。設計 doc 側も撤回を明記（**`:218-223`** の RETRACTED 節 ＝ mm 値の撤回 ／ **`:226-228`** ＝ 派生結論「`ke` 5 倍」「予算 8.16 と衝突」の撤回）。⚠ v4.3 は **`:230`**（＝ §5.5.D の header）を派生結論の撤回箇所として cite しており誤り。**`:219-224`** の範囲指定も 1 行ずれていた。⚠ **撤回しない量**（Jacobian を経由しない）= `τ_bias` 最大 **27.22 N·m が `shoulder_lift`（`ke`=2000）**・**`Δq = 13.61 mrad`**（`h3_torque_budget.per_joint_H3_1`）。
- **B3** ⇒ **X4 を撤回**。⭐ **spec は訂正済**（下記 §1-B3′。**p4 の R8 裁定が spec owner = 私へ RETURN したため**）。
- **B4** ⇒ **差分を正確化**（`git diff --numstat` 実測）: report **+158 / −10**、harness **+108 / −7**。⛔ v3 の「+168 / +115」は `--stat` の *changed total* を added と誤記していた。
- **B5** ⇒ 設計 SSOT を同期済（該当行を「**概念は定義済（振付の再工事）／operative な waypoint 集合と owner が UNRESOLVED**」へ差替。旧「UNDEFINED TERM」は撤回）。
- **B6** ⇒ **clean envelope で再提出**（本書 + 送信 message とも `[p11->pN]` 明示）。⚠ v3 送信時に `p1→pN` 表記と末尾の孤立文字が生じた由。**私の heredoc 原文は `[p11->pN]` で始まり timestamp で終わっており、混入の原因は私の側で再現できていない** ⇒ 事実として報告し、今後は短文・単純文字で送る。
- **版管理** ⇒ 版ごとの別 file 化はせず **同 path + git commit chain**（`66f49f9a6a` → `e7eaccbdca` → `0f373bedbd` → `fe22276f2c` → **`dda526b8a9`** → `1ce413522a` → 本 commit。⚠ v4.3 の本行は **`dda526b8a9`（v4.2）を欠落**させていた＝ **`:7` の correction chain が正**。⚠ v4.4 は「`:5` が正」と書いていたが、M1 の注記追加で行がずれており `:5` は送信元行）。⛔ **履歴 rewrite なし**（各版は producing commit で読める）。

### B3′ ⭐ spec 訂正 = **v1.7**（`…T1605JST-005` relay (B) を受けた spec owner 修正）

**relay 2 件を私が独立に検証した**（hash も一致）:
| 中継物 | commit / sha256 | 私の照合 |
|---|---|---|
| (A) p0 R9 notice | `dcd1ad5929633554e79e873344a99c8fecc01d46` / `5c0015034a326c90…7e68ff` | ✅ **一致**（`sha256sum` 実行） |
| (B) p4 R8 裁定 | `442f58678359bf85c87d46b3f55df2c0f444f820` / `9fd5b0ac488e71e3…7397f6` | ✅ **一致** |

**⭐ 私が h0 report を独立に走査した実測**（p4 の裁定表と一致）: **`body_label` 70 件中 "pad" は 0 件 ／ `shape_label` 107 件中 16 件**。**body idx 9 = `…/right_spring_link/right_follower`・13 = `…/left_spring_link/left_follower`**。
⇒ ⛔ **私の spec `:154`/`:168` の「`body_label` から発見すること」は現モデルで実行不能だった**（pad は **shape** のラベル）。⇒ **p0 が `wrist_3` で代用せず ABSENT と報告したのは正しい。**
⇒ ⭐ **訂正内容（v1.7）**: 導出規則を 2 つ明示 — **(i) SSOT 定数 `task_config.py:37 GRIPPER_PAD_BODY_IDX` + `:43 BODIES_PER_ARM` から index 解決**（⚠ **定数は import する**・literal 複製しない）／**(ii) `shape_label` に "pad" を含む shape の親 body**。**採った方を出力に明記**。⛔ **`body_label` を "pad" で検索しない**と明記。
⚠ **実装への言及は規則の記述に留め、修正は求めていない**（`arm_control_measurement_harness.py:928` の literal `(9,13)` = **未認可・p4 の court**）。
⛔⛔ **解消した範囲を限定する（RETURN-008 R2 受理・過剰主張の撤回）**: 訂正で解消したのは **`body_label` 検索 vs index 方式という設計/spec カテゴリの矛盾**まで。**実装 conformance は未確定 / HOLD** — v1.7 `:158` は **定数の import を必須とし literal 複製を禁じる**が現行 harness は literal であり、`:161` が**適合判定を未決**と明記している。⇒ **方式が一致したことは、規則に適合したことを意味しない。**
⚠ 併せて spec 冒頭に **「本 spec が tree に在ることは実装してよいことを意味しない」**を明記（H-3.1 の再発防止・p4 裁定 `d724031b77` を引用）。

---

## 2. 【宛先】`w2:p4` RS-TECH-LEAD ／ 逐語本文

> [p11→p4] **撤回 4 件の報告と、残った依頼です。**⛔ 実装依頼はありません。
>
> **(1) 撤回 4 件**: ①「p0 は認可内・違反なし／原因は私」 ②一般則「共有 tree の commit は引き渡しゆえ後から止められない」 ③**手先たわみ mm（14.594 / 4.074 / 8.838 / 2.000）と「2 mm 超過」、および「`ke` を 5 倍程度」「予算 8.16 と衝突」** ④「H-4 は仕様どおり・手戻りなし」。
> **撤回の根拠（現行のもののみ）** = **`442f58678359bf85` §B（複数原因裁定）** ＋ **`d724031b77` の F6 / F7 / (b)**（＝ **GO 不在・07-21 の指示 4 件に H-3.1 は含まれない・技術的連続性は authorization を延長しない**。⚠ **この 3 点は v2 でも撤回されていない事実認定**）＋ **p0 の R4 自認** ＋ **p0 の R9**（Jacobian 列を最大絶対成分に潰しており手先変位ではない）＋ **私の spec の `body_label` 指定が現モデルで実行不能だったこと**（⇒ **v1.7 で訂正済**）。
> ⚠ **①は私の自己矛盾でした** — 同じ文書で「p0 の authorization を検証していない」と書きながら、それを結論の土台にしました。**②は無権限実装を正当化する向きに効く**ため降ろします。
> 📎 **HISTORICAL / SUPERSEDED（根拠には用いません）**: 私の v4 は、あなたが **v2 §B で撤回済**の「根本原因＝ p4 の構造的欠陥／p0 の規律違反として閉じない」を**現行裁定として再伝播**していました（「再裁定待ち」も stale でした）。⇒ **active な帰属は v2 §B の複数原因のみ**（**p0 の admission はそのまま立つ／あなたの own は 2 点／私の own は §D(i)**）。⛔ **私からの帰属主張は出しません**（あなたの裁定の引用のみ）。
>
> **(2) 生き残っている実測**（Jacobian を経由しない量のみ）: `K_d = 0.2·K_e` が腕 12 本で厳密（比例減衰）／`ζ = (kd/2ke)·ω_n` が厳密式（私の独立解が report と 13 桁一致）／3 縮約差 **0.023%**（私の再計算 0.022527%）／`τ_bias` 最大 **27.22 N·m は `shoulder_lift`（`ke`=2000）**・**`Δq = 13.61 mrad`**／`ζ_min ≥ (c/2)√(λ_min(K_e)/λ_max(M))` と十分条件 **`λ_max(M) ≤ 5.0 kg·m²`**（不等式は私が実測で確認）。⚠ いずれも **1 姿勢の標本**（envelope 最大ではない）。
>
> **依頼（返答・裁定をお願いします）:**
> **①** `27.22` / `ζ 2.857` / `J` / 撤回済の mm 値を**確定値として引用している面**が在れば指摘してください（無ければ「無し」）。
> **②** **§5.5.D（掃引方法 = 解析上界＋数値最大化）の p0 への回付可否**。⛔ 私からは送っていません。
> **③ ⭐ ゲイン変更の gate 所在**（`ke` のみ／`ke`+`kd` 同時 は Rs 承認事項か、私の設計 court か）。⛔ **確定するまで数値を確定しません。** ⚠ 撤回 ③により「どれだけ上げる必要があるか」の入力は**今は無い**ので、急ぎではありません。
> ⚠ **spec 訂正の範囲を限定しておきます**: v1.7 で解消したのは **`body_label` 検索 vs index 方式という設計/spec カテゴリの矛盾**までです。**実装 conformance は未確定 / HOLD** — v1.7 は**定数の import を必須とし literal 複製を禁じ**ますが、現行 harness は **literal `(9,13)`**（`:928`）であり、spec `:161` も適合判定を未決としています。⛔ **実装修正は求めていません**（あなたの court）。
>
> **④** **`W-b` の operative な waypoint 集合と owner**（概念は定義済・waypoint と owner が未解決）。
> **⑤ ✅ 済 — spec を v1.7 へ訂正しました**（あなたの R8 裁定 `442f58678359bf85` が spec owner = 私へ RETURN したため）。`body_label` 検索は**現モデルで実行不能**（私も独立に実測: body_label に "pad" 0 件 / shape_label 16 件）ゆえ撤回し、**(i) SSOT 定数からの index 解決（定数は import する）／(ii) `shape_label` の親 body** の 2 規則へ差し替え、採った方を出力に明記させます。⛔ **`arm_control_measurement_harness.py:928` の literal `(9,13)` の修正は求めていません**（未認可・あなたの court）。⚠ 併せて spec 冒頭に「**tree に在ることは実装してよいことを意味しない**」を明記しました。
> **⑥** **正しいたわみ量の算出（`Δx = Σ_i jacp[:,i]·Δq_i` を 3-vector で合成し `‖Δx‖`／上界は `Σ_i ‖jacp[:,i]‖·|Δq_i|`）を、いつ・誰の authorization で行うか。** ⛔ p0 は authorization 待ちと明言しており、私からは依頼しません。

---

## 3. 非主張

- p0 の測定値を私は**再現していない**。私が独立に計算したのは **`K_d = 0.2·K_e` の厳密成立** / **`ζ_min` の再導出（13 桁一致）** / **`μ_min ≥ λ_min(K_e)/λ_max(M)` の成立** / **`λ_max(M)`** / **縮約差 `0.022527%`** / **diff の `+158/−10`・`+108/−7`** の 6 点。
- **p4 の裁定内容・p0 の自認**は、私が当該 artifact を直読して引用したものであり、**私が独立に再裁定したものではない**。
- 本書は **gate を動かさない**。実装・訓練・landing・status・RUN の変更を要求していない。
