# p4 実測 — 背板が Ø8 に届く前に爪どうしが当たる（クランプの窓は無い）

**測定者:** RS-TECH-LEAD (`w2:p4`)。**発行:** 2026-07-27 13:14:39 JST（date-THEN-write）。
**依頼:** p18 `MSG-P18-THE-ORDER-OF-CONTACTS-DECIDES-EVERYTHING-20260727-090` 項 B(3)（p5 発）＋ `-091` 項 C（p11 も同旨で独立に要求）。
**実行根拠:** Rs 直接指示「はやくコでケーブルクランプして動画を！！」の範囲内。⛔ 新規 gate の解錠を主張しません。

⛔ **私は設計を判定しません。** 本書は測定のみ。分岐の裁定は p5 / p11、幾何 LOCK は Rs 専権。

---

## 0. 計器と対象（exact pin）

| 項目 | 値 |
|---|---|
| model | `thread_isaac_lab/assets/ur5e_robotiq/robotiq_2f85/_ur15_2f85_koshape_actuated.xml` |
| commit | `a3fbd7d7e4700f7b40224b8c5fc3c2cc3fa894a6`（**tracked かつ clean**） |
| sha256 | `1966631e61496f980c398bee17d46bfda3eafd38b497c35d584cf403a03015c0` |
| env | newton 1.2.1 / mujoco 3.8.1 / mujoco_warp 3.8.1 / warp-lang 1.13.0（**更新前**） |
| 計器 | `mujoco.mj_geomDistance`（符号付き 3 次元面間距離・負 = 重なり） |
| 手順 | ctrl を 205–240 の **1 刻み**・各点 2500 step 静定・**ケーブル無し** |

⭐ 爪 geom（`*_pad_f1ext` / `*_pad_f2ext`）と `pad1` は banked LOCK `2f85_koshape.xml` と **byte 一致**（`P4_KOSHAPE_LOCK_DELTA_MEASUREMENT_20260727.md` §1）。⇒ **幾何の結論は LOCK 設計に対して読めます。** ⚠ 駆動（tendon 復活）は異なります。

## 1. ⭐⭐ 答え — 爪が先

| 事象 | 起きる ctrl | その時の背板間 |
|---|---|---|
| **対向爪が 0.00 mm を横切る**（爪どうしが接触） | **219.16** | **約 10.16 mm** |
| **背板が 8.00 mm に達する**（Ø8 に初めて触れる） | **224.78** | 8.00 mm |

⇒ ⭐⭐ **爪が 5.6 counts 先に当たります。** ⇒ **窓は存在しません。**
⇒ 爪が接触した時点で、背板は Ø8 まで **あと 2.16 mm** 足りません。

**根拠となる生データ（`f1ext × f1ext` は単調・補間は隣接 2 点のみ）:**

| ctrl | 217 | 219 | 221 | 223 | 225 |
|---|---|---|---|---|---|
| `pad1 × pad1` | 10.91 | 10.16 | 9.41 | 8.67 | 7.92 |
| `f1ext × f1ext` | +0.81 | **+0.06** | **−0.69** | −1.44 | −2.19 |
| `f2ext × f2ext` | +1.48 | +0.74 | −0.00 | −0.74 | −1.49 |

## 2. ⭐ 依頼 B(1)(2) への回答

1. **`−2.57 mm` の geom 対 = `left_pad_f1ext` × `right_pad_f1ext`**（同じ段どうし）。全 5 対の実測は上表・交差ペア（`f1ext × f2ext`）は同 ctrl 域で **約 +9.8 mm** で当たりません。⇒ 最初に当たるのは **同じ段の対向爪**。
2. **ケーブル無し**の測定です。**achieved == commanded**（無負荷・静定平衡ゆえ actuator は指令位置に収束）。⇒ 上表は **無負荷の幾何**であり、⛔ **ケーブルが在る状態の停止点ではありません**（p11 の警告どおり、実際にはケーブルで先に止まります）。

## 3. ⚠ 私の probe の無効な半分（先に潰します）

⛔ **「ケーブル在中」の sweep は無効です。** 私はケーブルを **静止姿勢の爪位置**から求めた slot 中心に置きましたが、当該 ctrl 域では 4 節リンクが pad を振っており、全距離が約 44 mm のまま動きません（＝顎はケーブルに一度も近づいていない）。⇒ **その列を根拠に使わないでください。** 有効なのは §1 のケーブル無しの列だけです。
⚠ 併せて `f1ext × f2ext` の ctrl 223 の 1 点だけ `0.00` が出ています（前後は 9.98 / 9.94）。⇒ **外れ値**として扱い、§1 の判定には使っていません（判定は `f1ext × f1ext` の単調な 2 点補間）。

## 4. ⛔ 読んでよい範囲

- ⭐ **成立するもの:** この幾何では、**対向爪が互いを通り抜けない限り、背板は Ø8 に到達しません。**
- ⭐ **実機の停止点（この測定が示唆する量）= 背板間 約 10.16 mm** ⇒ Ø8 に対しクリアランス **約 2.16 mm**。⇒ **圧縮ではなく捕捉**。
- ⛔ **私は「実機で到達不能」と結論しません。** 私が測ったのは sim の 1 model の無負荷幾何です。実機の値は測っていません。
- ⛔ **`FINGER_CLOSE_POS = 0.002` の可否を判定しません。** p5 / p11 の court、幾何 LOCK は Rs 専権（RS71 §0 #4）。
- ⚠ **単調性の確認:** p5 が仮定と明記していた「閉じるほど爪間が減る」は、205–240 の全 36 点で **単調減少**を実測（上表）。⇒ 仮定ではなく観測になりました（当該区間に限る）。

## 5. 非主張

⛔ 設計しない ／ 受入判定しない ／ 値を採用しない ／ GO を出さない ／ gate flip なし ／ LOCK 面を編集していない ／ 他 pane の record を代理編集しない ／ 実機の量を主張しない。

---
**p4 measurement = 2026-07-27 13:14:39 JST / RS-TECH-LEAD (`w2:p4`)**
