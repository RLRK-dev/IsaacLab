# P3 demo RECORDER build + DoD — INDEPENDENT GT cross-PV (L3 verification leg)

**Verifier:** OPS-SUPERVISOR (%9). **Requested by:** RS-TECH-LEAD (%12), Rs「連携して進めて」. **Date:** 2026-07-02 ~08:25 JST.
**Decider≠verifier:** %11 built + self-reported FULL DoD PASS; I recomputed everything below MYSELF from on-disk artifacts (`$SP` = `/tmp/claude-1000/-home-rlrk-IsaacLab/06ddfabd-27b3-4b11-89ac-443b0f28a0a5/scratchpad`). NOT a rubber-stamp — I found real (non-overturning) deviations, listed in §2.
**HEAD** `8a3c265ad7` (verified: `git diff bcb7393ec8..8a3c265ad7 -- test_newton_clip_routing.py task_config.py` = **0 lines**; the move is 8 vault/docs files only → the %11 claim is TRUE). Build = 0-commit working-tree (`route_demo_recorder.py` untracked-added + test-file modified). INVARIANTS #1-5 untouched (audited: hooks are pure observers).

## §1. Per-charter-item adjudication (all recomputed by me)

| # | Item | Verdict | My independent evidence |
|---|---|---|---|
| 1 | READ-ONLY equality proof (cpu leg0 vs leg1) | **CONCUR — PASS-in-substance; PARTIAL-vs-spec (state-hash leg not executed)** | My `cmp`: `route_c2_pin.json` **byte-identical** (md5 `70627fae…` both). My RAW stdout diff (NOT their scrub): **23 differing lines, ALL 3 non-physics classes** — warp module-load timings (×20), output-dir path (×2), `[DEMO_REC]` (×1); **zero physics-numeric diffs** across ~1240-line stdouts (IK residuals / contact-N / z-track lines all equal). ⚠ Spec §6.1's **final-state hash (`body_q`+`joint_q` byte-compare) was NOT implemented** (grep of scripts/logs = absent) → formal DoD deviation, severity LOW: the full-run stdout numeric equality ≈ a running physics fingerprint (any perturbation cascades through chaotic contact dynamics into later prints), and the verdict-JSON embeds final-state-derived numerics. ⚠ %11's own first scrub printed `STDOUT_DIFF_CLEAN=NO` (leg log) — the report's "clean modulo 3 classes" is TRUE in substance (my classification settles it) but the DoD log line itself reads NO. **cpu-as-substrate sufficiency (honest assessment):** hooks are device-independent Python; the recorder module imports NO sim lib (structurally cannot call the solver); the only device-varying behavior (`.numpy()` GPU→host sync per frame) cannot write state. PLUS an on-device empirical leg exists: the cuda DEMO_RECORD=1 run reproduced the **no-recorder** authoritative baseline fingerprint EXACTLY (item 2) → read-only holds on cuda at fingerprint precision. Sufficient. |
| 2 | Canonical leg (cuda:0) fingerprint | **CONCUR — EXACT** | My own JSON extraction, cuda leg vs authoritative baseline (`canonical_run_records/integrated_route/run_canonical/route_c2_pin.json`): regrasp_ok=True / `SUCCESS_R_GRIP_L_CAGE_AT_88` / R 104.51N / reach 0.9mm / r_target (0.362,0.119,0.836) / Y-span 88.0 / 3D 100.3 / tilt 0.0 / L 0.0N + config echo (x_clip 0.35, y_clip 0.15, float 20mm, spacer, c2 0.40/0.075) — **field-by-field drift = NONE**. (Fingerprint fields are 1-2dp rounded by construction; equality is at that defined precision.) meta.verdict carries the same. |
| 3 | NPZ structural | **CONCUR — every number reproduced** | My own numpy recompute on the cuda npz: T=7707; **22 keys** (report said "20 keys" — count nit, superset OK); sim_time≡frame_idx/480; 13 phases + pre(-1), ALL non-empty (histogram sums to T); anti-alias first-vs-last 129.0mm + moves across boundaries; drivers move 0.703/0.697 rad in GRASP_CLOSE; per-10-frame pose-delta max 8.53mm < 15mm; all 5 quat arrays |q|∈[0.999999,1.000001]; `ee_tgt_quat_effective_r[0]` == Rx(−90°) xyzw exact; pin ON at f2544 in C1_PIN, pinned_body=55. **Grip 26==26 basis = HONEST, not fitted:** my transition decode (23 npz transitions) reconciles exactly — 13 close (cage 0.6666 + 12-step 0.6728→0.7407) + 5 L-half (→0.69) + 1 R-open(0.0) + 1 L-full + 2 R-cage/full + 1 both-open; 26 events − 23 = 3 no-op/same-frame merges (R-open when already 0.0 [OPEN_RAD=0.0=recorder default → the uncounted pre-construction setup-OPEN is info-lossless as claimed], L-full re-assert, 2 settle events in 1 frame). Loop multiplicities (×12 close ramp, ×5 L-half) are VISIBLE in the npz value sequence = real. |
| 4 | Sufficiency spot-derive | **DONE (mine) — superset proven on the real artifact** | Derived `held_cable_z = cable_xyz[t, held_seg_l[t], 2]`: LIFT-end − GRASP mean = **+44.8mm** (matches the banked コ ~45mm lift datum). Derived cable tangent at R's seg (final frame) ≈ (−0.57, 0.82, −0.02) — plausible seated diagonal. Derived |ee_tgt−ee| err stream: median 2.2mm — well-behaved **except GRASP_HOVER max 394mm (R) / 499mm (L)** = the first big approach command held against a far EE (all later phases ≤12.5mm). Confirms the spec's own zero/spike-train caveat ON the real artifact and localizes it → converter MUST re-chunk/drop the HOVER prefix. |
| 5 | Code read-only audit | **CONCUR — PASS** | `route_demo_recorder.py` imports ONLY atexit/hashlib/json/os/subprocess/numpy — **no mujoco/newton/warp import → solver calls structurally impossible**; every stored slice `.astype(np.float32)`+`.copy()` (double-copy, aliasing defeated); `sample()` reads `state.body_q/joint_q.numpy()` only; all 10 hook sites are `if _demo_rec is not None:` observers; module import gated inside `DEMO_RECORD=="1"`; `note_grip` fires AFTER the servo assign (records what was commanded); `note_pin` AFTER `eq_active=1`; finalize before BOTH `sys.exit` sites (+4753/+5930) + atexit, idempotent `_done`. **ANTI-REVERT markers: my `git show bcb7393ec8` line-compare — 4372==old4335, 4388==old4350, 4487==old4447, byte-IDENTICAL.** Latent defects found → §2. |
| 6 | L3 accounting + §8 demotion grounds | **CONCUR — L3 correct** | My counts: module 226 lines + test-file `--numstat` **48 added / 0 deleted** (purely additive) = 274 > 200 → auto-L3 per spec §6.5; no DoD item trimmed (all 22 keys / 13 phases / full meta present). **§8 demotion-grounds VERIFIED TRUE** (designated-verifier duty): every `phase`/`ik` keyword hit in the diff is an observer line or label (`_ph(...)`, `note_targets` at ik_move_both entry) — no control/phase/physics LOGIC change. Practically MOOT: L3 stands via the size trigger anyway → 層2 post-debate + 層5 fire. |
| 7 | Device-fragility finding | **CONCUR — REAL, not a config artifact** | My leg0 extraction: `BLOCKED_REACH_WALL`, R **0.0N**, reach **83.2mm**, 3D-span 139.7 (vs cuda 104.51N / 0.9mm / 100.3); the leg scripts' env strings are IDENTICAL apart from `NEWTON_DEVICE` (+`CUDA_VISIBLE_DEVICES`), and both JSONs echo the SAME config (x_clip/y_clip/float/spacer/c2). Independently corroborated by a 2nd artifact: the cpu npz asserts = 16/17 with exactly `regrasp_ok==TRUE` failing. → The canonical demo is **cuda:0-specific**; the R re-grasp sits on the LEDGER reach-wall knife-edge. Conservatism: concur with the report (cpu-FAIL conservative on the knife-edge; cuda-SUCCESS non-conservative for real/other-device transfer; sharpens P1 risk④). |
| 8 | §運用14 my own video check | **CONCUR — PHYSICALLY VALID in-window (spot)** | My own frames (6 native `_route_frames` + 8 ffmpeg-extracted from `~/Downloads/p3_demo_recorder_canonical.mp4`, self-selected): coherent capture → LIFT (cable rises WITH the grippers, catenary sag both sides = genuinely held, "LIFT 11/12") → diagonal transport → C1 seat → GUIDE slide ("slip=0.44 (PARTIAL) cradle=True JAM=False C1IN", cable bent around C1 = pin+seat holding through the drag) → R re-grasp → dual-seat ("7/12 Lgrip↔C2 +20.6") → settle "**cable↔C2 = −0.39mm seated=True**", grippers retract, cable stays. No teleport / no explosion / no gross penetration in my sampled frames; strand continuous throughout. Sub-cm interpenetration NOT judgeable in this render — concur that `mj_geomDistance` is the authority there. HONESTY: my check is independent but **not strictly blind** (the charter+report preceded my viewing) and is a spot-check (14/69 frames). **NEW finding: the baked banner "(CPU)" is a HARDCODED stale label (`test:3816`, pre-existing, NOT %11's hooks) — the cuda:0 canonical video carries a false "(CPU)" title.** Device is proven by meta+logs; flag a 1-line device-aware-label follow-up. |
| 9 | Records-vs-fact | **CONCUR with deltas** | (i) HEAD-move 0-diff claim TRUE (verified). (ii) **as_run_sha256 all 3 files MATCH the current working tree** (my rehash) — the run used the shipped build. (iii) `joint_names=[]` confirmed (env7 `fk_model.joint_key` empty); tolerable ONLY because `arm_q` is the full 74-dim physics `joint_q` + driver indices recorded + sha-pinned build allows model-rebuild at convert time → the **converter spec MUST include a joint-layout map**. Also a misnomer: `arm_q` contains the WHOLE joint_q incl. ~46 cable coords (74 = 2×14 arm + cable) — document it (extra data = superset-friendly, but unlabeled). (iv) meta device echo cpu/cuda per leg ✓. (v) cpu-leg asserts **16/17** (regrasp_ok=False) appear in the leg log but are UNREPORTED in the build report — fully explained by item 7, but should have been surfaced (transparency nit). |

## §2. MISSED items (not in %11's report; none overturn the DoD substance)

1. **⛔ ARTIFACT DURABILITY (must-fix before 層2/bank):** the shipped demo npz + meta + equality legs + stdout logs + native frames live under %11's **session-scoped /tmp scratchpad** (`$SP/dod/...`) — volatile (session cleanup / reboot destroys them). Spec §2's default `DEMO_OUT` was `eval_runs/.../demo_raw/`; the DoD override left the durable tree EMPTY (only the video copy reached `~/Downloads`). Same failure class the spec itself guarded against ("preserved from the volatile %2 scratchpad"). → **Copy `$SP/dod/` (or at minimum `cuda/demo_raw/*, cuda/run/route_c2_pin.json, leg0/leg1 JSONs + stdouts + the two logs) into `eval_runs/troot_optE_dapg_wholeroute_scope_20260701/` before anything banks.**
2. **Latent meta defects (fix before ANY non-canonical-config use; harmless for THIS npz):** `resolved_clip_c2_xy` is **HARDCODED** `[0.40, 0.075]` (`route_demo_recorder.py:217`) and `env_gates` omits `CLIP2_X`/`CLIP2_Y` → a future run with a different C2 would write a LYING sidecar. Also `finalize(out_path=…)` arg is dead (cosmetic), and `note_grip` increments the event counter even if the driver set matches neither arm (latent, moot on this route).
3. **Converter contract notes (record now, cheap):** (a) `nearest_seg_r/l` = EE-origin XY-argmin and `held_seg_l` = Y-nearest — convenience indices ≠ the env's `clamp_pos_ko` 3D window/span-guard seg selection → the converter must recompute target segs from `cable_xyz`; (b) phase-label semantics: L's re-grasp move + full-clamp (f4970) is labeled `GUIDE_C2` (the `C2_REGRASP` label starts at the R leg); (c) the GRASP_HOVER 394/499mm `ee_tgt` spike (item 4) → re-chunk or drop the pre-grasp prefix.
4. **Spec-vs-execution deviation:** the §6.1 final-state hash leg (see §1 item 1) — recommend either a 1-line stdout hash print (future runs) or an explicit spec note that stdout-numeric equality subsumes it.
5. **"(CPU)" hardcoded video banner** (`test:3816`, also `:3334`) — pre-existing; misleads any future device-split evidence reading. 1-line f-string fix (use `DEVICE`).

## §3. Conservatism directions (every fidelity-bound claim)
| claim | direction | note |
|---|---|---|
| cpu equality ⇒ recorder read-only on cuda | logic + empirical (cuda fingerprint match at rounded precision) | residual risk negligible (no sim-lib import); a bit-exact cuda claim is NOT made |
| canonical demo trajectory | **NON-CONSERVATIVE for transfer** (device-fragile, reach knife-edge; cpu flips to BLOCKED) | BC/DAPG consumers must pin cuda:0 + treat reach-margin as a robustness gap (P1 risk④ sharpened) |
| demo at SIM_SUBSTEPS=10 vs env RL_SIM_SUBSTEPS=4 | **NON-CONSERVATIVE** (spec §3) | convert/env-time re-validation gate stands |
| pin-ON dynamics embedded | **NON-CONSERVATIVE vs pinless deploy** (spec §4 LOUD FLAG) | Rs Q — carried, not silently closed |
| video leg (sub-cm) | **NON-CONSERVATIVE for interpenetration** | mj_geomDistance = authority; gross kinematics reliable |
| npz asserts / equality on cpu route (BLOCKED trajectory) | hook-coverage COMPLETE anyway | all 13 phases + 26 events + pin + monkeypatch fired on the cpu legs too (verified in the cpu npz/log) |

## §4. OVERALL VERDICT — **CONCUR-PASS with QUALIFICATIONS (gate: 層2/層5 may proceed)**

The build is faithful, genuinely read-only, and the DoD's SUBSTANCE holds on my independent recompute: equality proven (my own cmp + raw-diff classification), canonical fingerprint EXACT vs baseline, npz structurally sound + superset-sufficient (my own derivations), code audit clean, L3 accounting correct, ANTI-REVERT intact, device-fragility REAL and correctly escalated. No finding overturns a DoD item.

**Conditions/follow-ups (map):**
- **BEFORE 層2/bank (blocking):** §2.1 durability copy into `eval_runs/`.
- **≤1-line fixes (with the already-flagged meta gaps):** §2.2 meta C2 hardcode + env_gates CLIP2; ANTI-REVERT new-line-numbers into meta (%11's own flag); "(CPU)" banner (§2.5).
- **Record in the converter spec (P3 follow-on):** §2.3 a-c + joint-layout map (§1 item 9-iii).
- **For Rs (carried, unchanged):** pin-ON demo embedding (spec §4 Q1/Q2), single-demo insufficiency (CRIT2 partial), device-fragility as a first-class DAPG/BC robustness gap.

0-commit maintained; no %11 file edited; my writes = this MD (+ my own scratchpad frame extracts).
