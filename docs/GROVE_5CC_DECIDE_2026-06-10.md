# GROVE v2 — 5-CC debate DECIDE + v3 fold plan (%12 lead; PAUSED before execution per human-Rs 一旦停止)

**Date:** 2026-06-10 19:0X JST · **Subject:** `docs/grove_methodology_2026-06-05.html` (v2, the full restructure) · **Launch:** human-Rs 「必要であれば5CCでGROVEを強化して」 · **Bodies:** CC2 SSOT-consistency / CC3 portability / CC4 autonomy-model / CC5 operating-rules / CC6 NHA — all completed. **Verdicts:** 4× PASS-with-fixes + NHA CHANGE_JUSTIFIED (scoped). **Status: dispositions DECIDED below; the v3 rewrite + SSOT landing are HELD by the human-Rs pause.** Full agent outputs: the %12 session transcript (2026-06-10).

## DECIDE summary
**v2 = structurally right (core/binding split, case-study quarantine, honest boundaries) but NOT operable as governance and not yet portable.** All findings ACCEPTED except none — zero rebuts were warranted (every challenge was disk-grounded). Two CRITICALs:
- **CC2-1: the 権限モデル note silently amends CLAUDE.md [HIGH-COST-GATE]** (it shrinks human gates to "production 級" while the SSOT holds GPU≥10h / production-scale / multi-skill at default NO_GO + Rs approval; the grant record itself kept multi-skill human-side and scoped autonomy to GPU smoke). → v3: scope the autonomy claim to sub-threshold runs + name the procedure (/production-launch-gate, GO_CANDIDATE distinction) + a PENDING marker: 「正規発効は CLAUDE.md 系への反映時(rs 指示)」.
- **CC4-1: boiling-frog drift is ungated** — the lead owns BOTH drift-detector knobs (re-baseline + intentional-exemption naming), "milestone" is undefined, no trajectory auditor. → v3: original-anchor preserved forward-only + rule-4 compares vs BOTH anchors; re-baseline needs supervisor concurrence + log; milestone defined with hard bounds (time/cost/run-count); supervisor gets an explicit per-milestone drift review.

## Accepted fixes by lane (v3 fold list)
**CC2 (SSOT):** scope gate ③ to the binding's HIGH-COST-GATE thresholds; 「5 種のみ」→「定常開発サイクル内の ask は 5 種」+ carve-out (プロセス kill / sudo 級 / 制御方式変更 / スコープ外新規物 / NEST node 起動承認は不変); inherit the 3-strike hard-stop into core; note rule-files stay rs-origin; insert [TASK] (NEST) into the workflow figure.
**CC3 (portability):** de-domain residue (§28 → 「実装後 on-disk state と承認済み diff の機械的一致検証」+ THREAD pointer in binding; 物理ゲート/テレポート → labeled examples); checklist 6 → ~10 (＋cost model / 実行・検証ハーネス / 知識 SSOT 配線 / production・外向き定義 / blind-judge channel / 再現性 tolerance policy); subtitle drops "Claude Code 群" (platform = binding ③; the 2026-06-06 platform swap = the agnosticism evidence); §7 gains the single-human-authority + software-dev scope limits; ordered bootstrap path; design-gate trigger defined abstractly (目的関数・成功定義・環境契約を変える変更); 「一次モダリティ→副次計測(ログ等)→照合」; rule 3 allows property tables where numbers don't exist; baseline numbers labeled (THREAD/L0 実測例); **a standalone portable GROVE-CORE spec = the missing artifact (post-pause task)**.
**CC4 (autonomy):** ≤2 gates/milestone DEMOTED from target to observed indicator + counter-metrics (human-overturn rate, surprise rate, drift-envelope breaches) — also fixes the internal contradiction with 「数値目標は設けず」; gate-⑤ classification gets supervisor concurrence + a forward-only fail-class taxonomy + disagreement⇒gate fires; veto gets a MECHANISM (stop-flag channel checked at chain-step boundaries + heartbeat reports; THREAD binds signal-file/BLOCKED_FOR_USER); core rule 9 = runaway bounds (same-fail-class 3-strike → escalate; 行き詰まり defined: 3-strike OR stall>T OR chain cost>declared budget); baselines labeled n=1-day + human-in-loop conditionality (the mutual-catch evidence INCLUDED the human layer — re-measure at each gating-reduction step).
**CC5 (rules):** add the dropped lessons as core rule(s): 「gate anchor・期待値は operating regime で MEASURE する(fine sweep; 文脈変化で再較正); 期待値に provenance tag (measured/derived/authored)」; rule 7 gains the improvisation-vs-disposition discriminator (実行者は run 中編集禁止 / リードは runs 間・記録付き / 監督発行物は監督 concurrence); 実例2-② の「規則 4」= stale lessons numbering → fix; rule 1 scoped (gate/binding runs + 判定点; 内部反復除外; 正式判定は zero-exposure 盲検); rule 6 ex-ante 宣言必須(事後の縮小は gate 編集 = rule 7 経路); rule 8 debate は §3 の L 階層に従う; rule 4 = identity OR per-quantity declared tolerance(決定論基板は identity 既定); rule 3 handover = 数表+一次モダリティ成果物; rule 2 真陽性 = 計器構成ごと; rule 5 「binding にする」→「正式化する」+ 依存集合の粒度定義.
**CC6 (NHA, scoped):** version-control the doc BEFORE v3 (it is untracked; v1 unrecoverable; the sole pre-restructure copy = ~/Downloads 18:46 accidental survivor) — the doc violated its own forward-only principle; restore ~10 provenance lines INTO the case studies (「yoi」/ TGS 32→4 / v15_lowiter / ±56-84 / S5=Option-E SolverMuJoCo+Robotiq / period-2 step-multiplier / memory-file pointer / T-Meta node ID / usage-limit cause / 受動 120 step + QUARANTINE / AGENTS.md in footer); rules 2-6 relabeled **candidate core (初観測 2026-06-10; 第2 binding で確証後に昇格)**; restore the 「vault LL は P1 完了時」 qualifier (v2 turned it into an overclaim); **SSOT landing plan: the 8(+) rules + gate economy land in the CLAUDE.md family (rs-gated L3) + vault LL; the doc shrinks to summaries + pointers** (currently second-SSOT by construction: 0 hits for the rules in all governing files).

## Post-pause execution order (HELD)
1. Version-control the doc (git add or vault sidecar hash) — BEFORE any rewrite.
2. v3 fold (all of the above) + the milestone reframe from %3's rev5 PV (engagement, not grasp).
3. Portable GROVE-CORE spec (standalone artifact the 10 binding points plug into).
4. rs-gated CLAUDE.md landing of the gate economy + core rules (human-Rs 指示起点; until then the economy stays PENDING and CLAUDE.md governs).
5. %3 re-Tier-A of v3 (debate-informed, the S4/S5 pattern).
