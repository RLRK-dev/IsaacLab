---
title: R-S7.1 / Option-E System Spec — SINGLE SSOT INDEX
created: '2026-06-19'
owner: human-Rs (decisions); RS-TECH-LEAD (%5) records
status: ACTIVE — consolidation per human directive 2026-06-19 「アーム、クリップ、テーブル、その他の仕様も1箇所に」
authoritative: true
tags: [spec, SSOT, R-S7.1, option-E, arm, clip, table, cable, gripper, pipeline]
---

# R-S7.1 / Option-E System Spec — SINGLE SSOT INDEX

**Purpose (human directive 2026-06-19):** every component spec recorded in ONE findable place to stop drift.
This doc is the **single entry point** for the component specs; it records the design/geometry spec + points to
the impl. **Numeric param SSOT = `thread_isaac_lab/configs/task_config.py`. Goal/phase SSOT = `04-Specs/SOMA.md`.**
Env: env7 Newton 1.2.1 / mujoco 3.8.1 SolverMuJoCo, UR5e×2 + Robotiq 2F-85. HEAD `7afa84b463`.

---

## 0. FOUNDATIONAL INVARIANTS (⛔ 変更 = PREMISE CHANGE = Rs専権 = STOP-and-flag, NEVER build-first)

These are the project's INVIOLABLE premises. A design OPTION that would change ANY of them is NOT a design tradeoff — it is a premise change reserved to human-Rs (`prohibited.md:18`「指示された方針・手法を独自判断で変更しない…報告のみ…rs判断を待つ」). On touching one: **STOP → BLOCKED_FOR_USER (Rs確認) → run the §運用2 [VERIFY] 5体 debate BEFORE any build/probe.** Generative-design produces OPTIONS + ESCALATIONS, never a silent method-swap. (Landed 2026-06-21 after the single-arm 重大事故; `feedback-vault-check-on-handoff` 6th instance; surfaced every session by `.claude/hooks/handoff_grounding_gate.sh` + auto-L3 per `CLAUDE.md` §0.)

1. **DUAL-ARM** — the cable is held + manipulated by BOTH arms (UR5e × 2) in EVERY motion; neither arm is dropped/parked to make it single-arm. "asymmetric route/hook OK" = both arms doing DIFFERENT motions, NOT single-arm. [`CLAUDE.md:396` project identity; §1; §6 `:52`]
2. **GRASP SPAN / FIXED BASES** — 88 mm two-EE grasp span on the cable; bases fixed at Y = ∓0.35. [§1; `task_config.py:21-22` / `:235`]
3. **CONTROL = DiffIK only** — DifferentialIKController; JT-IK abandoned; no kinematic teleport / forced placement. [`CLAUDE.md` DiffIK §; `prohibited.md`]
4. **GRIPPER GEOMETRY LOCKED** — the active finger DESIGN is **コ-shape** (human-DECIDED 2026-06-20; BANKED 2026-06-21 CPU building-block, Rs source-GO); its geometry is human-LOCKED. The former **◇ V-groove (gripper pad `vgu`/`vgl` diamond) is DISCARDED as a design reference** (human 2026-06-21, wording 2026-06-22) — no longer a retained/locked reference. ✅ records-vs-code RESOLVED 2026-06-23 (commit `85315bbec6`; committed asset = `2f85_koshape.xml` = コ). ⚠ cable pos/pose-random = DEPLOY requirement (Rs 2026-06-24, **SOMA L38 SUPERSEDED**). 検証履歴 + robustness 現況 (GPU cg-screening / held-rate / IC-robust / yaw-tol / sag / re-grasp gap / pending) = **§0-A (verbatim 移設 2026-07-12)**. (Scope = the *gripper* ◇ only; the *clip* V-groove `GROOVE_CENTER_Z`/`cable_in_groove` is a separate active object, untouched.) [§5; `06-Knowledge/GD-KoShape-Finger.md`]
5. **NO KINEMATIC TRICK** — physics-faithful only; the ONLY authorized exception is the clip-retention pin (`log.md:6534`). [`prohibited.md`; §2]

---

## 0-A. INVARIANT #4 (GRIPPER) 検証履歴 / robustness 現況 — §0 #4 から verbatim 移設 (2026-07-12)

Premise (正) = §0 #4。本節はその検証履歴・robustness 現況の詳細。内容は 2026-07-12 に §0 #4 行から verbatim 移設 (削除ではない; Rs 承認「改善案をすべて反映」— anchor-set gate P0 対策: §0 肥大で hook echo 12.7KB が persist 閾値を超え context 外へ退避されていた)。premise が再肥大したら履歴は本節へ足す。

✅ **records-vs-code RESOLVED 2026-06-23** (commit `85315bbec6`): the committed asset is now `2f85_koshape.xml` (コ); the ◇→コ asset swap LANDED (CPU build-safe — R1 IK-motion / R2 K2 / S6_GRASP finite / S8 cable surf_min 0.8000 / RL-base build all PASS). GPU R-S6.6-on-コ STATIC-SCREENING DONE 2026-06-23 (solver=cg = the #1415 NaN-fix on the contact-rich コ grasp: newton 0/30 vs cg 30/30 finite, all 3 validity conditions [nacon engage / solref-on-GPU / no nefc overflow]; cg penetration-faithful via %2 mj_geomDistance cross-PV -0.661mm ≤ newton-ref -0.995mm, no crush; SCREENING bar, CPU = proven-finite safe fallback; `eval_runs/troot_rs66_gpu_stability_20260619/R_S66_VERDICT_cg_fixes_1415_on_ko_20260623.md`). --lift DYNAMIC done 2026-06-23 (cg fixes #1415 through close+lift, 30/30 finite); retention with the WORKING recipe (z_grasp=1.0668 cradle + gradual 12-substep lift) = GPU/cg HELD existence proof (N=1: cable cradled in コ claws [f1ext vertZ form-closure], rise +45.7mm, solver-independent [CPU-newton concurs +44.6mm], §運用14 video + %2 mj_geomDistance cross-PV, reproduces the 6/21 CPU «成功»; the `r_s66_grasp_gpu_settle` --lift SLIP 30/30 was the non-banked 1.070-flat-pad+single-move choreography, NOT an inability — retention is set by the choreography, not the solver). HELD-RATE (N≥30, GPU #562 non-det): ✅ **30/30 ESTABLISHED 2026-06-24 at FIXED-IC/screening** (WR cradle recipe z_grasp=1.0668 + gradual-12-substep lift, cg-GPU; %2 cross-PV CONCUR — real claw f1ext vertZ form-closure, deepest −0.39..−0.51mm, 0 soft-sink; = GPU-non-det REPRODUCIBILITY at ONE fixed seed/IC, **NOT IC-robustness**; `eval_runs/troot_rs66_gpu_stability_20260619/r_s66_retention_wr_result.json` + `log.md` 2026-06-24); ✅ IC-robustness (grasp-depth ±2mm + IK-seed jitter) = **IC-ROBUST 30/30 2026-06-24** (real claw form-closure: %2 mj_geomDistance sweep all-30, 0 soft-sink, deepest −0.89..−0.49mm; §運用28 recompute exact) = a **DEPTH+seed MECHANISM-PRECISION FLOOR, NOT deploy-robust**. ✅ **yaw-Z orientation tolerance ≥12°** (Stage A' grasp-orientation sweep, %9 + %2 cross-PV CONFIRM zero-mismatch 2026-06-25: achieved==commanded EXACT [world-Z yaw = free DOF q1, IK zero-residual; %2 axis_z=1.000], the held non-monotonicity = GPU#562-stochastic NOT a yaw threshold; conservative scripted/no-sensing FLOOR, ONE axis [yaw-Z only — roll/pitch UNTESTED, position fixed], 14/15 HELD) = another MECHANISM-PRECISION FLOOR, NOT full-pose / NOT deploy-robust [`STAGE_AP_ACHIEVED_YAW_VERDICT.md`, `log.md:6638`]. ⚠ NEW (Rs 2026-06-24, **SOMA L38 SUPERSEDED**): cable **POSITION/POSE-RANDOM is now a DEPLOY requirement** (`task_config.py:255-256` framing STALE → SUPERSEDED-annotated at `task_config.py:257-262` 2026-06-24, reflection DONE) — pos/pose-random + **SHAPE (bending)** [⚠ vertical-sag part CHARACTERIZED 2026-06-25 Stage B (%2 §運用28 cross-PV CONCUR): the representable vertical sag is conservatively COVERED for the both-arms-lift pipeline (grasp-local pitch ≤~0.8° ≪ yaw-Z 12° tol; clamp is LOCAL → sag absorbed by the free arm beyond the grasp; baseline held-30/30 already holds the 50mm-lift sag); the deeper Stage-D mid-air re-grasp +9..12° sag = SEPARATE gap [⚠ tilt-follow **N>1 sweep DONE 2026-06-25** (%2 §運用28 cross-PV CONCUR): HOLDS the narrow spec-50mm CENTERED re-grasp (8.6°, N=5) / SLIPS beyond ~9-11° (z_lift≥0.07) / worst-representable-tilt UNSWEPT → mechanism validated at the LOW end only, **NOT a robust solve**; robust-bank pending a re-grasp-Y-position sweep — ⚠ a non-88mm-span re-grasp = INVARIANT #2 Rs design Q; `06-Knowledge/GD-KoShape-Finger.md:126-128` + `log.md` 2026-06-25 15:26]; horizontal-routing curvature = kinematic per §4 B2 → so the residual "shape" pending = the deeper Stage-D sag + real-fidelity + RL-pilot, NOT the both-arms-lift vertical sag; `log.md` 2026-06-25 12:43] + grip-magnitude-under-load + real-fidelity + RL-pilot remain pending (non-conservative). The *built* gripper is now コ. 

---

## 1. ARM (UR5e × 2)
- **Bases:** `ROBOT_LEFT_BASE` (0.0, −0.35, 0.80) / `ROBOT_RIGHT_BASE` (0.0, +0.35, 0.80) [`task_config.py:21-22`] — 0.70 m apart, on the table plane (z=TABLE_HEIGHT).
- **IK:** DifferentialIK, `IK_ITERATIONS = 100`, `ik_move_both` = ONE `solve_ik_dual` [`newton_routing_utils.py:74`].
- **Collision-avoidance (dual-arm):** EE-EE safety spheres `COLLISION_SPHERE_RADII` (0.035 + 0.035 = 70 mm + 10 mm margin), `COLLISION_WEIGHT = 5.0` [`newton_routing_utils.py:147/165`] → achieved arm-to-arm floored ~80.5 mm (**measured/empirical**; mechanism = 70mm sphere + 10mm margin) **by design** (NOT under-convergence / NOT a real pad collision).
- **Converge gate (production):** 5 mm/EE (`moves_ok`).
- **Grasp spacing:** `GRIP_HALF_SPAN 0.044` → arm-to-arm **88 mm** at grasp (`WIDE_LEFT_Y 0.106 / WIDE_RIGHT_Y 0.194`) [`task_config.py:235/262-263`]. ⚠ The grasp-episode probes hardcode ±0.20 (400 mm) = a REGRESSION to fix → use `WIDE_*_Y` (`test_newton_clip_routing.py:2606/2729/2317`).
- **Impl:** `task_config.py`, `scripts/newton_routing_utils.py`, `assets/ur5e_robotiq/ur5e/ur5e.xml`.

## 2. CLIP (× 5)
- **Positions (staggered 千鳥, Z = TABLE_HEIGHT 0.80):** C1 (0.35, +0.150) / C2 (0.40, +0.075) / C3 (0.35, 0.0) / C4 (0.40, −0.075) / C5 (0.35, −0.150) [`task_config.py:211-216`]. X stagger 0.35/0.40 (`CLIP_X_ODD/EVEN`).
- **Geometry:** `CLIP_BASE_HEIGHT 0.005` (5 mm base plate) / `GROOVE_CENTER_Z 0.809` (cable-seated Z = TABLE + base + CABLE_RADIUS) / `CLIP_GROOVE_INNER_RADIUS 0.006` [`task_config.py:91/226/233`].
- **Collision (⚠ ABSENT-IN-CODE):** the clip boxes are `shape_flags=1` VISIBLE-only — collision is OFF (`test_newton_clip_routing.py:958` intent comment / `:971` flag; %3-audit-confirmed ROBUST — not re-enabled downstream [clip body=-1 skipped by `bi<0` continue :1194]). The clip-as-catcher cannot retain until collision-wiring is added (R-S7.1.1 / F-1; GD-S2A). **⚠ UPDATE 2026-06-21: R-S7.1.1 (B) DEMONSTRATED the collision wiring (`CLIP_COLLISION=1`, `test_newton_clip_routing.py:1006`) — the clip self-seats @ `GROOVE_CENTER_Z` 0.809 + RETAINS the cable even WITHOUT the pin (CPU, human-confirmed «A», `GD-KoShape-Finger.md:124-147`). → F-1 = DEMONSTRATED-on-CPU; BUT `CLIP_COLLISION` is WORKING-TREE/uncommitted + depth-hand-tuned (0.820) + no perturbation/tension test → retention-robustness NON-conservative / un-banked; the COMMITTED build is still collision-OFF. NOT a flat "resolved" until committed + robustness-verified.** **⚠ GEOMETRIC CONSTRAINT (human 2026-06-21 「明らかに設置できない所に設置した」, %4-confirmed): the clip MUST sit on SOLID table, SEPARATED from the grasp slot/void — co-locating the clip on the grasp void (the first B→A test, `CLIP_X=0.30,Y=0.0`) is a COUPLED-CONSTRAINT (void EMPTY for the lower-claw grasp vs clip SOLID for the seat) that TILTS the grasp finger (the "fix clip↔break finger" back-and-forth root). ✅ VALIDATED B→A (`r_s71_clip_dropin_72.py`, CPU, human-confirmed «成功»): grasp center 0.30 over the void (clean/VERTICAL) → ROUTE +0.10X to a clip on solid table (X≥0.40, off the void) → drop-in seat 809 → pin → retains @809 (gripper away). Real clips: a 2D-footprint check (clip base ±20×±15mm `test:1008` vs the grasp swept-void X[0.234,0.366]∧Y[−0.060,+0.060] `test:978`/`:968-969`) shows **ONLY C3 (0.35,0.0)** overlaps BOTH axes (C1/C5 X-overlap but **Y-clear 75mm**; C2/C4 **X-clear 14mm**) — the earlier "C1/C3/C5" was an X-only check. ✅ **RESOLVED 2026-06-21 (human option A): C3 → (0.40,0.0)** off-void (= validated `r_s71_clip_dropin_72.py` pos; a COMPLETE void-fix, not partial). ⚠ **`task_config.py:214` numeric commit DEFERRED** to the table-slot/multi-clip landing (5体 Debate NHA: the edit is inert until the void table lands + the probe uses the `CLIP_X` env-override, not `CLIP_POSITIONS[2]`) — **COMMITTED code still C3=0.35**; decision recorded `GD-KoShape-Finger.md` "Clip placement resolution" §. The off-center routing (LEFT-arm reach, Y=±0.150/±0.075) remains a SEPARATE problem. Detail: `GD-KoShape-Finger.md` clip-insertion §, `log.md` 2026-06-21 18:53 + 19:37.**
- **Retention:** AUTHORIZED kinematic PIN — clip-only exception (human 2026-06-16, `log.md:6534`); the ONLY kinematic trick in the system. The grip SEATS the cable; the pin RETAINS it. **⚠ UPDATE 2026-06-21: with (B) collision wired, the real clip floor retains vertically WITHOUT the pin → the pin is DOWNGRADED to Y-slide insurance, NOT the primary vertical retainer (`GD-KoShape-Finger.md:144-147`; CPU/working-tree).**
- **Impl:** `task_config.py`, `create_clip` / clip asset, `log.md:6534`.

## 3. TABLE
- **Surface:** `TABLE_HEIGHT = 0.80` [`task_config.py:20`]. Single SOLID box in the scene build (`test:936` half (0.35,0.35,0.005) @ (0.3,−0.05) → X[−0.05,0.65] **Y[−0.40,+0.30]**). No slot/recess in the committed build.
- **Cable must NOT overhang the table (human directive 2026-06-19):** the cable was +Y-shifted — `test:1064` centered it on `CLIP1_Y` (+0.15), not the clip-array center → cable Y[−0.15,+0.45], +Y end overhung the table edge (+0.30) by 150mm. **FIXED (%4 working-tree 2026-06-20; %5-verified via git diff + real `build_scene`):** `test:1064` now `cable_y_start = clip_y_center − cable_half_len` with `clip_y_center = (CLIP_POSITIONS[0][1]+CLIP_POSITIONS[-1][1])/2 = 0.0` → cable Y[−0.30,+0.30] (bodies [−0.30,+0.285], +Y axis tip at the edge +0.30). NO_OVERHANG=True, COVERS_CLIPS=True (clip margin −Y150/+Y135mm), ALL_GRASPS_ON_CABLE=True (`R_S71_CABLE_CENTER_24.md`; working tree, NOT committed). ⚠ caveat: the table is offset −0.05 from the clip center → the +Y tip sits EXACTLY at the table edge (zero +Y margin; −Y has 100mm) — re-centering the table on the clips would give symmetric margin (separate optional fix). **⚠ 5-CC DEBATE 2026-06-20 (post-fix verification) FINDING:** the body/axis +Y tip is at the edge (+0.30) but the capsule END-CAP (`CABLE_RADIUS`=4mm) protrudes **~4mm PAST the +Y table edge** → the "no overhang" directive is **NOT fully met** by the body-only measure; **+Y-margin follow-up REQUIRED** (CC2 MEDIUM + CC6 NHA-HOLD + %2 cross-PV concur; fix = shift cable −Y ~15mm OR fix table geometry in the slot redesign). **HUMAN CHOSE (b) 2026-06-20: DEFER the +Y-margin fix to the upcoming table-slot redesign** — the cable-centering stays as-is; the ~4mm cap residual is accepted until the slot redesign reworks the table geometry (where the under-grip slot + the +Y margin are designed together). ALSO pending per CC3: verify the uncommitted ×2.8 vgl interaction (`ncon` cable-vs-pad == 0) before relying on free-air probes. Debate verdict = PASS-with-required-followup (the 150mm-overhang fix is a real improvement and stays; the ~4mm cap residual + the vgl-interaction are the open items). ⚠ The earlier "use the +Y overhang free-space for the grasp" idea is **RETRACTED** — the overhang was a bug being removed, NOT a usable space (RS-TECH-LEAD %5 error, corrected per `log.md:6556` + the human directive).
- **TABLE-SPACE slot (REQUIRED for aerial grasp+lift; NOT yet landed):** with the cable centered (no overhang) on the SOLID table, the gripper's lower walls cannot reach UNDER the cable → a real SLOT (void within the table, under the grasp point) is required so the lower walls reach under (solves the under-grip block). NOT a kinematic trick (real slotted fixture). **Landing = multi-box table build (separate gate).** Prior probe validation: `R_S71_FAITHFUL_SLOT_15.md` / `R_S71_TABLE_SPACE_14.md` (⚠ those probes' narrow-slot PINS the centered cable → no lift; the wide slot lifts but sagged — open trade-off, `R_S71_BIGV_LIFT_22.md`). **⚠ 5-CC DEBATE 2026-06-20 VERDICT — a STATIC medium-slot sweet-spot is REFUTED (CC2/CC3/CC6 convergent CRITICAL):** the same support that prevents off-centering ALSO pins against lift = contradictory at one line; the proven 48mm lift (`FAITHFUL_SLOT_15`) used a **2-BOX VOID** (cable unsupported under the grasp ≈ wide), NOT a medium slot; lift-onset force ∝ **1/W³** → feasible only at W≳75-80mm where the cable is off-center; the conflict resolves in **TIME (a mechanism), not WIDTH**. → PATH FORK (human architectural call): **(A) ACCEPT the PROVEN 48mm lift** (wide void; cable hangs at the grasp but lifts+routes; spec bar = "operating at all, even rough/imperfect" `SOMA.md:16-17`; the LOCKED ◇ is UNCHANGED — CC5: the ◇-lock is geometry, NOT the cable-span/sag) **OR (B) a MECHANISM** = dual-arm controlled-delivery (clamp→elevate off the supports→◇ holds the centered cable→lift = the PARKED `GD-S2A-ControlledDelivery.md`; needs a prior-art/blocker re-check — F-1 history). BOTH require: land the faithful coupling (by-label, `neq==6`, run WITHOUT `-O` — CC4) + solve the LEFT-arm REACH lag (separate wall, no slot fixes it — CC3); bank "single-arm lift confirmed", NOT "dual-arm" (CC4). **✅ HUMAN CHOSE (A) 2026-06-20:** accept the proven 48mm lift — wide-VOID multi-box table + land the faithful coupling; the cable hangs at the grasp (the LOCKED ◇ is UNCHANGED; spec bar "operating at all"). **LANDING ORDER (%4, L3):** (1) faithful coupling (connect solref→[0.001,1]+rigid; follower mirror BY-LABEL right/left_driver polycoef[0,1,0,0,0]→neq 4→6; guard `neq==4`→`6`, keep `len(grasp_connects)==4`; run WITHOUT `-O`); (2) multi-box wide-void table (`table_shape_idx`→LIST, fix consumers :581/:1031/:1292, test box-box-cable contact on the mujoco branch); (3) grasp choreography (reproduce the 48mm RIGHT-arm lift); (4) verify + §運用14, bank single-arm. **NEXT sub-problem (separate): LEFT-arm REACH lag** (no slot fixes it). **✅ (A) LANDED + BANKED 2026-06-20** (full verification chain: %5 §運用14 multi-frame + %2 x-ray cross-PV + %3 final audit, ALL QUALIFIED PASS): **single-arm RIGHT CPU lift CONFIRMED 49.9mm/100%** (slip 0.1mm), neq=6 faithful coupling ACTIVE (not visual-only), real wide-void, **NO kinematic trick** (grep-clean), pad penetration ≤~1.1mm (STIFF-contact compliance, committed `MUJOCO_PAD_SOLREF=(-65789,-2105.3)` task_config:187, NOT the D3e soft-dodge artifact), under-grip 179N peak/19.8N settle (within the Robotiq 2F-85 ≤235N envelope; the lift needs <1N so does NOT rely on the over-squeeze). **AXIS-RESOLVED conservatism (%3): lift FEASIBILITY = CONSERVATIVE (holds on CPU → holds stiffer); grip MAGNITUDE = NON-CONSERVATIVE ×3 (CPU-vs-GPU / ~1mm-adds-grip / 179N over-squeeze) → GPU + grip-tune required before PRODUCTION (NOT production-ready).** Caveats: CPU-only (GPU mjw R-S6.6 = production HARD gate); LEFT-arm 20mm reach-lag SEPARATE (NOT banked); NOT dual-arm. Working tree, NOT committed (commit = separate human-GO + `./isaaclab.sh -f`). Non-blocking recs: tune grip DOWN (shrinks penetration), GPU is the hard gate. **⛔ OUTSTANDING (human spec 「左右とも同じ動作」): symmetric dual-arm = solve the LEFT-arm reach (off-center-clip stretch) = the NEXT task. 5-CC DEBATE 2026-06-20 (symmetric task) VERDICT:** **(A) tune-the-stretched-LEFT-pose = REFUTED** (CC2/CC4/CC6: the off-center stretch is a GEOMETRIC INVARIANT of fixed bases [Y=∓0.35] + off-center clip [LEFT→C1 = 0.46m lateral vs RIGHT 0.16m]; `solve_ik_dual` has NO manipulability/per-arm knob → A = 対処療法 on a near-reach-limit pose). **(B) centered grasp (Y~0) makes the LIFT symmetric but DEFERS the asymmetry to the off-center HOOK** (CC3 CRITICAL — the arms still go to the clip Y to hook). **(C) tilt + (D) per-clip-assignment violate the LITERAL "same action"** (CC5). **The "symmetric" requirement is UNDER-SPECIFIED → human clarification REQUIRED before design** (CC5 CRITICAL + CC6 mandatory HOLD). Deeper (CC6): the asymmetry is a SYMPTOM of the both-arms-grasp-the-88mm-cable architecture; the symmetric push may be PREMATURE vs the real blockers (GPU R-S6.6 + clip-collision ABSENT-IN-CODE §2). → HOLD for human: pin "symmetric" interpretation + priority.** **✅ HUMAN PINNED 2026-06-20: symmetric = (B) CENTERED symmetric grasp** — both arms grasp+lift the cable at its CENTER (Y~0, EEs at ±`GRIP_HALF_SPAN`=±0.044, symmetric ~0.31m reach each = both strong) → symmetric lift → route. The off-center **HOOK asymmetry is ACCEPTED as a SEPARATE next problem** (per the human's choice: B is the symmetric LIFT; hook = separate). Implementation (%4, L3, per CC4 zero-regression): **PARAMETERIZE the grasp_y + the void on a `grasp_y` arg** (default = C1 → byte-identical, preserves the banked single-arm RIGHT case as a regression guard; do NOT redefine `WIDE_*_Y`); grasp_y=0 for the centered symmetric lift; **verify BOTH arms lift symmetrically** (the key test: does the LEFT now lift at the symmetric pose vs 20mm at off-center C1?) + the full chain (%5 §運用14 → %2 x-ray cross-PV → %3 audit). **SCOPE (human clarification 2026-06-20 「今はクランプ、lift動作において同じ動作をしろ。全ての動作で同じ動作をしろと言っているわけではない」): the symmetric requirement applies to the CLAMP + LIFT motions ONLY, NOT all motions — the route/HOOK are EXPLICITLY NOT required symmetric (asymmetric OK).** → B (centered symmetric clamp+lift) FULLY satisfies it; the off-center hook-asymmetry is ACCEPTABLE (a functional placement sub-problem, NOT a symmetry concern → the CC3 hook-defer caveat is RESOLVED by the human's scope). Docs: R_S71_FAITHFUL_LAND_STEP{1,2,3}_*.md + R_S71_XRAY_CROSSPV_P2.md + 3 COORD2 audit docs. **⚠ CLAMP NON-CONFORMANCE (caught by human 「VAULTのDesignステップ表の動作が大前提」 2026-06-20): the (A) lift used `RAD_FULL` (0.7407 = INSERTION clamp, `r_s71_faithful_land_lift_27.py:55`), but the step-table (SOMA:80) + the V-groove design require the LIFT+ROUTE = GUIDANCE = HALF-clamp (`GRIPPER_DRIVER_HALF_OPEN_RAD`≈0.667 = the ◇-FORMING clamp, "half clamp = load-bearing" `Gripper-VGroove-Design.md:106`); FULL = INSERTION ONLY. → the (A) lift did NOT form the LOCKED ◇ (which forms at HALF-clamp) — it was a full-clamp PINCH, not the ◇ form-closure. → the lift MECHANISM must be REDONE with the HALF-clamp ◇ (land `GRIPPER_DRIVER_HALF_OPEN_RAD`=0.667, PROPOSED); the B symmetric clamp+lift likewise uses the half-clamp ◇. %5 banked (A) without checking the clamp vs the step-table = a miss, corrected per the human's grounding directive. The 49.9mm lift datum holds as "a lift is achievable" but NOT as the step-table-conformant ◇-GUIDANCE lift.** **⚠ CORRECTED-B (half-clamp ◇) RE-CONFIRMED the DIAMOND-BOTTOM obstacle (2026-06-19), %5 §運用14-verified (`r_s71_halfclamp_conflict.png`): the LOCKED ◇ FORMS (encloses, EE+16, no lift) XOR LIFTS (◇-BELOW, Z_ENGAGE−6, doesn't enclose) — MUTUALLY EXCLUSIVE (the open bottom). The SYMMETRIC clamp+lift (human spec) IS ACHIEVED via ◇-BELOW (both arms 30mm airborne, |L-R|=0.0, LEFT now lifts at Y~0 vs 20mm at C1) — NOT the ◇ form-closure. **%5 OVER-READ (own it):** my half-clamp "correction" assumed the LIFT uses the ◇ form-closure; but the LIFT is ◇-BELOW regardless of clamp (the form-closure CAN'T lift — open bottom); the clamp affects only lift strength (full 49.9 / half 30mm). The figure-#52 ◇ form-closure = HORIZONTAL drag-guidance + up-cap (prior %2 drag 0.90), NOT the aerial VERTICAL lift. → DECISION (human, = the recurring diamond-bottom): **(1) ACCEPT the ◇-below lift [%5 recommends — the symmetric spec IS met; closing the ◇ bottom was REFUTED 2026-06-19 as flat-floor/penetrating]** / (2) close the ◇ bottom [◇ geometry change = source-GO] / (3) re-scope. Doc `R_S71_HALFCLAMP_B_CONFLICT_28.md`.** **✅ HUMAN CHOSE (1) 2026-06-20 10:14 JST: ACCEPT the ◇-below lift as the OFFICIAL lift mechanism** (closing the ◇ bottom was REFUTED 2026-06-19 flat-floor/penetration; the ◇ geometry stays human-LOCKED, UNCHANGED). OFFICIAL symmetric clamp+lift = **◇-BELOW at HALF-clamp** (step-table-conformant: half = load-bearing GUIDANCE `Gripper-VGroove-Design.md:106` / `SOMA.md:80`; the cable RIDES on the lower walls from below, NOT enclosed — the open bottom precludes FORM+LIFT together). Geometry: both arms centered Y~0 (EEs ±`GRIP_HALF_SPAN`=±44mm), both lift ~30mm airborne, |L−R|=0.0 (LEFT now lifts; was 20mm short at off-center C1). **VERIFICATION STATUS (honest, do NOT over-claim):** single-arm RIGHT FULL-clamp 49.9mm = FULLY BANKED (full chain %5+%2+%3); the symmetric HALF-clamp 30mm ◇-below = %4-run + %5 §運用14 schematic-verified, **FULL chain (%2 x-ray cross-PV + %3 audit) PENDING** before banking at (A)-confidence. **NEXT (downstream, separate tasks):** (i) run the full verification chain on the symmetric half-clamp ◇-below (bank "symmetric clamp+lift confirmed"); (ii) land `GRIPPER_DRIVER_HALF_OPEN_RAD`=0.667 (PROPOSED; `task_config.py` = L3 gate); (iii) route/HOOK asymmetry = separate problem (human-scoped OUT of the symmetric requirement, 2026-06-20). Conservatism unchanged: lift FEASIBILITY conservative / grip MAGNITUDE non-conservative ×3 → GPU (R-S6.6) + grip-tune before PRODUCTION. **⚠ UPDATE 2026-06-20 10:33 (%4 human-directed video analysis + %5 §運用14 + §運用28 numeric cross-check): (a) POSITIVE — the slot DESCENT / under-tuck prerequisite is INDEPENDENTLY VERIFIED at the centered grasp:** vgl fingertips reach Z≈**785mm (JSON `r_s71_slot_descend_check_30.json` tipL 785.3 / tipR 785.2, depth 14.7-14.8mm) ≈ 781mm (rendered geom-bottom, fig `~/Downloads/r_s71_slot_yz_section.png`)** = **15-19mm BELOW the table top (800)** AND **below the cable bottom (799.9, below_cable_bottom=true)**, tipY_in_void=true, IDENTICAL centered(±44) vs WIDE, EE→1052.8 IK converged (%5 §運用14 figure self-checked; the ~4mm 785-vs-781 = geom-extent-vs-reference, qualitative under-tuck holds). **(b) ⚠ CORRECTION — "symmetric achieved" was PREMATURE / %5 OVER-CLAIM:** the symmetric HALF-clamp ◇-below LIFT is **NOT video-verified** — the ONLY symmetric VIDEO so far (`r_s71_symmetric_video_28.py:134` = **RAD_FULL**, full-clamp) shows **NO lift** (cable LEFT on the table, grippers rise EMPTY — visually decisive); the earlier full-clamp **riseR=38mm was a measurement ARTIFACT (empty-gripper rise), video-REFUTED**. → the 30mm half-clamp "|L−R|=0 LEFT-lifts" datum is so far **%4-run numeric + %5 SCHEMATIC only** (a hand-drawn schematic does NOT satisfy §運用14 video-first for a motion-bearing lift), and it carries **elevated suspicion of the same artifact class** until a real video confirms the CABLE (not the empty gripper) lifts. The human's instinct ("最初に table slot に下降していないのがおかしい") was right to probe — the no-descent was a camera-angle artifact (camC elev −6° hides the 19mm descent), but it surfaced that the symmetric video is full-clamp/no-lift. **STATUS HONEST:** descent/under-tuck ✓ verified; single-arm WIDE full-clamp 49.9mm ✓ banked; **symmetric half-clamp CABLE-lift = NOT confirmed (video PENDING, NOT "achieved")**. NEXT (pending human task-GO): a half-clamp symmetric lift VIDEO with a cross-section / high-angle camera that SHOWS the slot descent + the cable rising (= the %2 x-ray cross-PV) BEFORE any "symmetric confirmed" bank. Doc: `r_s71_slot_descend_check_30.py` + %4 SYNC 10:31. **📷 CAMERA + RENDER SETTINGS for §運用14 slot-descent / V-groove-seating verification (recorded per human 2026-06-20 11:23 「カメラ設定もvaultに記録せよ」; FINALIZED after %4's camera/render overhaul — NOT the old broken ones):** **TWO failure modes that produced the false "no slot-descent" read:** (1) **NEAR-HORIZONTAL free cameras** — old `r_s71_symmetric_video_28.py:101-104` (= `r_s71_faithful_video_15.py:104-107`): camW lookat[0.30,0.0,0.92] dist1.15 **az52/elev−16**; camC lookat=gripped-seg dist0.34 **az38/elev−6**; at elev −6° the gripper bodies occlude + foreshorten the ~19mm vertical descent into the Y-axis slot. (2) **COSMETIC OUTER-MESH rendering** (the deeper cause, %4 phys-render 33): the gripper's cosmetic shell made it appear to SIT ON the tabletop — the human's "テーブル面に下降していない/おかしい" was a CORRECT observation OF THE COSMETIC RENDER, while the underlying PHYSICS V-groove (vgu/vgl collision geoms) DID descend into the slot. cosmetic mesh ≠ physics collision geometry. **CORRECTED (%4): render the PHYSICS model (solver.mj_model collision geoms: pad/vgu/vgl), NOT the cosmetic mesh; cameras that SHOW the descent (GX=GRASP_X=0.30):** **Y-Z section** (sees slot gap + descent) az 0 / elev −12, lookat[GX,0.0,0.80] dist0.30 (`r_s71_phys_render_33.py:64-65` camYZ); variant az0/elev−16 dist0.42 (`r_s71_diag_cam_video_32.py:70-71` camD). **DIAGONAL 3/4** (steep, shows vertical structure) az 52 / elev −30, lookat[GX,0.0,0.84] dist0.55 (`r_s71_diag_cam_video_32.py:72-73` camS). **CLOSE-UP R-gripper V-groove** az 12 / elev −8, lookat[GX,0.044,0.79] dist0.13 (`r_s71_phys_render_33.py:66-67` camD). **GENERAL RULE (§運用14, BANK): for vertical-motion-into-slot / V-groove-seating verification — (a) render the PHYSICS collision geometry, NOT the cosmetic mesh (cosmetic can show the gripper riding the tabletop while the real geom descends); (b) use a Y-Z cross-section (az 0) AND/OR a steeply-elevated diagonal (elev ≤ −30), NOT near-horizontal (elev −6/−16) cameras (they occlude + foreshorten vertical descent → false "no-motion").** **⚠ MAJOR FINDING the corrected (physics) render REVEALED (%4 11:19, grounded in physics geoms; bears DIRECTLY on the official ◇-below):** the descent is REAL but **TOO DEEP** — at the current descent the cable rides **ON TOP** of the V-groove (≈ gripper top ≈ table level), NOT cradled between vgu/vgl → on lift it ROLLS OFF → **EMPTY GRASP** (= the no-lift failure). full-clamp sinks a further 13mm (vgl 785→**772mm = 28mm below table-top**). **PROPOSED FIX (UNVERIFIED): descend ~25mm SHALLOWER** so the cable seats between green vgu / blue vgl. → this is a ◇-below DEPTH-DESIGN decision (the official mechanism needs the cable IN the lower-V cradle, not overshot). **%4 is awaiting the human's A/B (A=test shallower descent / B=sync to %5); the ~25mm is PROPOSED, NOT verified.** Figs `~/Downloads/r_s71_phys_descend.png` + video `r_s71_diag_descend.mp4`; scripts `r_s71_phys_render_33.py` / `r_s71_diag_cam_video_32.py`. **✅ INDEPENDENTLY CONFIRMED by the human's arm+physics-finger video `r_s71_arm_physfinger.mp4` + %5 §運用14 (9-frame, 12:15): the V-GROOVE is NOT clamping the cable — the cable is pinched at the MID-FINGER (the flat pad ABOVE the V-notch); the V-groove (green vgu / blue vgl) sits at the finger TIP, BELOW the cable, hanging in the slot, NOT engaging.** cable z 803.9 (rest/grasp) → 815.5 (+12) → 823.9 (+20mm, END): it DOES rise ~20mm but via a MID-FINGER FLAT PINCH (gripper +30 vs cable +20 = ~10mm SLIP), **NOT the locked V-groove cradle = an OFF-DESIGN pinch** (cf. the banked V-groove/cage design; a flat-pad pinch is a 先祖返り — `feedback-vault-check-on-handoff`). Root = the same too-deep overshoot (the descent drives the V-notch/tips below the cable into the slot → the cable lands on the mid-finger pad). **This is the FORM-vs-LIFT / diamond-bottom tension in a new guise:** for the V-GROOVE to clamp the cable the notch must be at the cable height (~804 ≈ table top), but then the tips are NOT under the cable in the slot (the below-lift needs them under) — a single descent depth cannot do both → a DEPTH/DESIGN decision is required (= %4's option A, ~25mm shallower, UNVERIFIED; may not resolve the FORM-vs-LIFT contradiction). Frames `/tmp/physfinger_frames/`. **📸 CURRENT render-config SNAPSHOT (recorded per human 2026-06-20 12:19 「現状のカメラ設定、メッシュ設定等を記録」) — source `r_s71_arm_physfinger_video_35.py` (the `~/Downloads/r_s71_arm_physfinger.mp4` video); GX=GRASP_X=0.30:** **CAMERAS (3, all MjvCamera FREE; camCU finger CLOSE-UP added 12:22 per human 「フィンガ拡大図も追加」):** (1) camD FRONT-oblique — lookat[GX,0.0,0.92], dist **1.0**, **az 232 / elev −22** ("moved back→front oblique") `:108-109`; (2) camYZ Y-Z (slot + V-groove) — lookat[GX,0.0,0.86], dist **0.6**, **az 0 / elev −12** `:110-111`; (3) camCU FINGER CLOSE-UP — lookat[GX,0.0,0.80], dist **0.20**, **az 232 / elev −22** (same front-oblique as camD, zoomed onto the finger/V-groove) `:112-113`. Render res per-panel **640×560** (EV.WI×HI, `r_s71_engagement_video_full.py:43`), 3-panel output ~1670×460 (§運用14 12:28: 3-panel layout + finger CLOSE-UP confirmed, `/tmp/physfinger2/`). **MESH / RENDER MODEL (HYBRID, per human 「フィンガは物理モデルで統一」):** ARM = exterior cosmetic Robotiq mesh (EV.one_arm_spec attached visual `:56`) rendered **SEMI-TRANSPARENT alpha=0.14** so the physics V-groove reads through `:71-79`; FINGER / gripper PADS = **PHYSICS collision boxes** (the 32 pad geoms, mocap-driven from solver.mj_model, OPAQUE — NOT cosmetic) `:62-69`; note solver.mj_model has NO arm geoms → the arm can ONLY come from the visual spec `:4`. **geom rgba:** cable(CAP)=ORANGE[0.95,0.55,0.18] `:61`; **vgu=GREEN[0.13,0.62,0.16] / vgl=BLUE[0.10,0.42,0.90] / other-pad=light-gray[0.85,0.85,0.88]** `:66`; table-halves=TAN[0.80,0.73,0.60] `:53`; ground-plane=dark-gray[0.26,0.28,0.32] `:48`. OPAQUE = physics pad-boxes + table + cable + arm-links; TRANSPARENT = only the cosmetic Robotiq shell (alpha 0.14). **This is the exact config that produced the human-verified MID-FINGER-pinch / V-groove-NOT-engaging observation above.** **🧪 DECISIVE — descend-depth sweep (CABLE-rise-corrected) CONFIRMS the FORM-vs-LIFT conflict is FUNDAMENTAL (%5 RS-TECH-LEAD 2026-06-20 12:41, on %4's handover「%5にわたして」).** I caught + fixed a measurement flaw in %4's `r_s71_depth_sweep_37.py` (its `airborne`/`riseR` keyed on the GRIPPER z → would score a fallen-cable run as CLAMP+LIFT); corrected `r_s71_depth_sweep_cablerise_38.py` measures the GRIPPED SEGMENT (bR) cable rise. **5-depth sweep (centered grasp_y=0, FULL-clamp, lift 50mm):** EE−6 cable_in_V=False / CABLE_rise **38.0mm**; EE−0 False / **41.9mm**; EE+8 False / **45.0mm**; EE+16 in_V=**True** / **0.8mm**; EE+22 in_V=**True** / **4.7mm**. → **NO depth gives V-CLAMP + CABLE-LIFT.** DEEP (≤+8): cable sits 15-25mm ABOVE vgu (on the pad top = mid-finger) → lifts vertically (38-45mm) but NOT in the V; SHALLOW (≥+16): cable enters the V (at vgl) but does NOT lift (0.8-4.7mm — the open ◇ bottom gives no upward support → cable left behind). Sharp flip between +8↔+16 (mechanism: the V-notch can be AT the cable [clamp] XOR the tips UNDER the cable [lift], never both). **UNTESTED gap EE+10..+14** (offered; monotonic flip makes a robust sweet-spot unlikely). **⚠ §運用28 caveat: this DEEP lift = 38-45mm slip~0 CONTRADICTS %4's earlier "+20mm slipping / symmetric_lift left-behind ~804" → the deep pinch is RUN-VARYING = NOT a reliable grasp; the deep "lift" may be cable-RIDING-ON-TOP (would fail on routing) — security UNVERIFIED.** §運用14: motion-bearing sweep; deep regime already video-confirmed (`r_s71_arm_physfinger.mp4`); shallow clamp-no-lift is numeric (CABLE_rise<5mm), video would only re-confirm (offered). **CONCLUSION (RS-TECH-LEAD technical call): depth-tuning CANNOT solve it — the LOCKED V-groove + the on-table grasp are geometrically incompatible with clamp+lift. Resolution = a DESIGN change (human source-GO, ◇ is LOCKED):** (A) accept the deep pinch-lift [off-design, run-varying, security unverified]; (B) change the V-groove geometry [notch higher / supporting profile — source-GO]; (C) two-stage controlled-delivery [GD-S2A parked: shallow V-clamp → scoop/elevate support → lift — untested, may break the conflict]; (D) re-anchor the grasp. → **ESCALATED to human for the design call.** Data `r_s71_depth_sweep_cablerise_38.{py,json}` + log `/tmp/depth_sweep_38.log`. **✅ RESOLVED 2026-06-20: human chose コ-shape** (removes the V-groove ◇ open-bottom; active finger = コ, see §5 + `06-Knowledge/GD-KoShape-Finger.md`). This §3 FORM-vs-LIFT saga is the PRE-decision narrative; the コ choice CLOSES it (コ now **BANKED 2026-06-21**, Rs source-GO, CPU building-block; GPU-production-pending).
- **Impl:** `build_scene` table box (`test:936`), cable position (`test:1064`).

## 4. CABLE
- **Model:** 40 segments × 15 mm = **600 mm**; `CABLE_RADIUS 0.004` (**Ø8 mm**) [`task_config.py:135-137`]. Rigid-capsule CABLE-joint chain (**NOT a Cosserat rod** — `validate.sh` Layer 6 guard).
- **⚠ FIDELITY BOUNDARY (Rs DECISION B2, 2026-06-25):** the cable is a **1-DOF-per-joint PLANAR bender** built with the bend plane **VERTICAL (sag)** — 39 inter-segment joints each a single revolute about local-X→world-X (`test_newton_clip_routing.py:990` + seg_q `:942-951`, direction=(0,1,0) `build_scene:1271`). → it dynamically represents **vertical SAG + position (free root) + free-root pose**, but **NOT horizontal routing curvature** (the 5-clip 千鳥 X-Y curvature would need a 2nd bend DOF/joint). **Horizontal routing through the staggered clips is therefore KINEMATIC** (grasp-drag + the AUTHORIZED clip-retention pin, §2 / `log.md:6534`), NOT a dynamically-curved cable. This is a **banked sim2real fidelity limitation** (Rs-accepted, NOT a defect) that EXPLAINS the AR routing fidelity-QUARANTINE (LEDGER `RL-Routing-Design.md` MIXED). Cable-SHAPE robustness is trainable/validatable in-sim only for **vertical sag**, not horizontal routing curvature. (Escalation + decision: `log.md` 2026-06-25 02:27 / 10:57 / 11:05; B1 substrate-upgrade [add world-Z DOF, re-validates all cable results] + B3 VBD declined.) **✅ Stage B RESULT (2026-06-25, %2 §運用28 cross-PV CONCUR):** the REPRESENTABLE vertical sag at the grasp-local segment is SMALL — REST (on-table gravity settle) ±0.18° / 50mm-lift-held 0.78° (2-seg secant; %2 quaternion-orientation 0.45° all-30 worst) ≪ yaw-Z 12° tol → the clamp is LOCAL (sag absorbed by the free arm BEYOND the grasp) → fixed-cable banking is CONSERVATIVELY COVERED for the both-arms-lift vertical sag (**NO new held-rate needed**); the deeper Stage-D mid-air re-grasp +9..12° sag (`06-Knowledge/GD-KoShape-Finger.md:126-128`) = SEPARATE gap (tilt-follow N>1 sweep 2026-06-25, %2 cross-PV CONCUR → HOLDS narrow spec-50mm 8.6° N=5 / SLIPS >~9-11° / worst-tilt UNSWEPT = mechanism-validated low-end, robust-bank pending Y-sweep [⚠ 88mm INVARIANT #2 Rs-Q]; see §0 INVARIANT 4 + `log.md` 15:26). BONUS: horiz out-of-plane tangent ≤0.173° across all 30 held configs RE-CONFIRMS the vertical-sag-only premise on REAL data (not just the construction grep). Evidence: `eval_runs/troot_clamp_cable_state_space_20260624/stage_b_vertsag_measure.{py,json}` + `stage_b_vertsag_crosspv_opssup.*` (%2); `log.md` 2026-06-25 12:43.
- **Stiffness:** `CABLE_BEND_STIFFNESS 0.005` (**EI [N·m²]**, power-cable-floppy, human VISUAL pick 2026-06-19 + %3 AUDIT-PASS) / `CABLE_BEND_DAMPING 0.01` [`task_config.py:144/152`]. (EI here, NOT joint-K; mujoco joint K = EI/CABLE_SEG_LEN, `test:827`.)
- **Impl:** `task_config.py`, cable build (`add_revolute_cable`, `test:830/1066`).

## 5. GRIPPER / FINGER → see the dedicated LOCKED SSOT
- **🔒 LOCKED finger spec SSOT = `07-Design/Gripper-VGroove-Design.md` «🔒 LOCKED FINGER SPEC».** Human-FROZEN 2026-06-19 (figure `R_S71_DIAMOND_LOCKED_SPEC.png`). ◇ V-groove (committed `7afa84b463`) + S1 cage (`65c056f63a`) + faithful 1-DOF coupling (REQUIRED, unlanded) + roles. Do NOT change the ◇ geometry without human source-GO.
- **⚠ FINGER-SHAPE SUPERSEDED→コ 2026-06-20 (human decision):** the ACTIVE finger is now **コ-shape (C-bracket)** — see `06-Knowledge/GD-KoShape-Finger.md` + the `07-Design/Gripper-VGroove-Design.md` SUPERSEDED banner. The V-groove ◇ is **DISCARDED as a design reference** (human 2026-06-21; see §0 INVARIANT 4) — no longer retained/locked as a reference. ✅ records-vs-code RESOLVED 2026-06-23 (commit `85315bbec6`): the committed asset is now コ (`2f85_koshape.xml`); ◇→コ asset swap LANDED (CPU build-safe; GPU R-S6.6-on-コ validation + grip-magnitude pending). コ = the ACTIVE finger, **BANKED 2026-06-21 (Rs source-GO)** as a CPU-verified building-block (penetration/crush + retention + grip-down clamp **0.69**, %2 cross-PV CONVERGENT; the 2026-06-17 5体 crush CRITICAL is RETIRED). **PRODUCTION-pending: GPU R-S6.6 + grip-magnitude + full-131mm-route/snag retention + R2/R3 §運用14 visual** (same caveat structure as banked-(A)). (Banked per human Rs source-GO 2026-06-21; evidence `eval_runs/troot_optE_rs71_kinematic_retention_20260616/R_S71_KO_GRASP_VERIFY_FIRST_VERDICT_73_74.md` + 75/76/77.)

## 6. PIPELINE (SSOT = SOMA)
- **Sequence:** Grasp → **Lift 50 mm** → Route 131 mm → Hook placement [`SOMA.md:31`] — **AERIAL** (on-table guide collides with the table-mounted clips).
- **Clamp roles:** **full-clamp = clip INSERTION / half-clamp = next-clip GUIDANCE** [`SOMA.md:80`].
- **Grasp mechanism (validated CPU):** dual-arm (88 mm) + table-space slot + faithful gripper → under-grip → near-full lift; cradle ~2 mm (◇ inherent). GPU mjw = R-S6.6-deferred (non-det NaN; CPU is the current bar).
- **⚠ LIFT DATUM (2026-06-21):** with the finger now コ (§5), the **LIVE lift datum = コ ~45 mm level** (**BANKED CPU building-block 2026-06-21, Rs source-GO**; GPU + full-route/snag-retention + R2/R3 = production-pending; `06-Knowledge/GD-KoShape-Finger.md:44`). The V-groove lift numbers — 49.9 mm (banked single-arm RIGHT, full-clamp PINCH) / 30 mm (symmetric ◇-below, video-PENDING) / 38 mm (REFUTED empty-gripper artifact), all in `:37` — are **REFERENCE-ONLY for the superseded V-groove shape**; do NOT cite them as the current lift. None is step-table-conformant. ✅ For the コ finger the half-clamp GUIDANCE clamp `GRIPPER_DRIVER_HALF_OPEN_RAD`=**0.69** is **LANDED in `task_config.py` 2026-06-25** (human-confirmed 2026-06-21 + %2 cross-PV; supersedes the PROPOSED 0.667; SSOT param — **no committed consumer yet** [the committed S6 grasp is FREE-AIR], GPU R-S6.6 verify pending; force 66.6N CLOSED vs 153N at full, CPU non-conservative ×3).

---

## LOCK / STATUS
- **§5 finger geometry = human-LOCKED**: the active finger is **コ-shape** (human-DECIDED; コ geometry human-LOCKED, source-GO required to change). **⚠ finger-SHAPE SUPERSEDED→コ (human decision; §5 + `GD-KoShape-Finger.md`); the ◇ V-groove is DISCARDED as a design reference** (human 2026-06-21; see §0 INVARIANT 4) — no longer retained/locked as a reference. コ BANKED 2026-06-21 (Rs source-GO, CPU building-block; GPU R-S6.6 + full-route/snag retention + R2/R3 = production-pending). ✅ records-vs-code RESOLVED 2026-06-23 (commit `85315bbec6`): committed asset now コ (`2f85_koshape.xml`); ◇→コ asset swap LANDED (CPU build-safe; GPU R-S6.6-on-コ validation + grip-magnitude pending).
- §1-4, §6 = current spec; numeric SSOT is `task_config.py`, goal SSOT is `SOMA.md` — this doc indexes + records the design/geometry + impl pointers. Update HERE when a component spec changes (keep it the single index).
- **Unlanded requirements** (tracked): faithful gripper coupling (§5), table-space multi-box (§3), clip collision-wiring (§2), grasp-episode ±0.20→WIDE_*_Y regression (§1).
