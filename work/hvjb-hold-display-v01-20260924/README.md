# 模式動画の保持対象と手先表示の照合

2026-09-24。v05c全体動画で、明示的に搬送する表示物と手先の相対変位を読取る。
既存のH01〜H08・12用途・20仕事の担当表に対する補助観察であり、実機の把持判定ではない。

対象は保存済みの1785標本・304列。出力は新規JSONと確認用図だけから開始する。
工程・工具・部品の未確定仕様を追加せず、原本bankを書き換えない。
ケーブルは変形する群記号であり、剛体と同じ相対行列一定の条件を当てない。

既存の `legacy_model.py` の部品群・手先原点、`render_process.py` の場面と役割、
`probe_saved.py` の表示時計変換を再利用する。区間は既存コードの搬送キー時刻から読む。
距離は無寸法の図解座標。数値的一致を把持力・公差・製造上の受入基準に読み替えない。

prior-art: `HVJB 保持対応 保存行列 受渡し`、exit 0、findings=0、blockers=0。
前段のXYZ補正とA/B/C表示補正を読んで、既に修正された区間を基準として含める。
次の対象はヘッダー搬送と、背景で始める次一式のA/B搬送。変更は観測後に限定して記録する。

## 最初の読取

`audit/relative_holds.json` はヘッダーの相対Yに約0.05980、相対Zに約0.00997、
背景の次下板の相対Zに0.10000の変動を記録した。いずれも無寸法。
背景の次ヒューズ板は相対位置一定だが、先行する板の90度姿勢と異なり0度で保持する表示だった。
数値は保存サンプルの範囲であり、連続時間の最大値や実機接触の測定ではない。

初回probeは書式検査の長行警告を残したまま実行された。測定に使ったソースは
`audit/probe_holds_executed.py.txt` にそのまま保存した。JSON内のscript SHAはこの写しと一致する。
現行 `probe_holds.py` は説明文字列を括弧で改行しただけで、計算式・文字列値・閾値は変更していない。
値は再計算して上書きせず、初回の読取記録を保持する。

## 次の表示修正の範囲

- C外側ヘッダー：部品の既存移動と初期の手の相対位置を維持して手を追従させる。
  その後の離脱では、元の退避へ接続する。製品位置は変更しない。
- Aの次下板：先行Aユニットの既存姿勢・開き幅を再利用し、運搬中の相対位置を保つ。
- Bの次ヒューズ板：先行板の側縁位置・90度姿勢・開き幅を再利用する。
- 接近と離脱は既存のキー時刻・smoothstepを使う。工具・配線・製品・パレット・カメラは不変。

ロボットの実把持面や軌道を採用する変更ではない。局所H06/H05の実寸比較モデルとも別の模式表示。

## 実行後の記録

`build_bank.py` は493標本・27列だけを変更し、保存読戻しが一致した。
初回修正bankのSHA256は `72779ccf3e7a15087b45f2eeaa6cb5ccbb0ca1bef3cf7f06ac79a00c379d45b9`。
`verify_holds.py` は元bankに対して相対表示の不一致を再現し、修正後の11区間と
12か所の分岐連続性を確認した。部品・工具・配線・パレット・カメラ・時計は変更していない。

比較PNG9枚を描画して観察した。Aの次下板は既存の先行ユニットと同じ広い手先表示になり、
C外側ヘッダーの相対位置ずれも修正された。一方で、B次板の指と白い箱部品の重なりが残った。
相対位置一定という数値確認だけでは、この表示上の問題を検出できなかった。
この段階を完成版として納品せず、`../hvjb-panel-display-v02-20260924/` で追加修正した。
9枚は修正途中の履歴として保持する。
