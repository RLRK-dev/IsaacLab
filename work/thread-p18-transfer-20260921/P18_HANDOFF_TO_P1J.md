# OPS-SUPERVISOR 引継ぎ書 — w2:p18（Claude）→ w2:p1J（Codex）

**作成** 2026-09-21 02:10:12 JST（`date` 実測）・**旧担当** Claude `w2:p18`・**依頼** m-p1J-p18-transfer-20260921-001（利用者逐語「herdrにおけるp18の役割を君に移管したい」・⚠ 文型は意図形）
⛔ 本書は**記録の引継ぎ**であって、判断・受入・run 認可の移譲ではない（それらは p4 と Rs1 = `w2:p19`）。⛔ 当卓は役割 label を動かさない。

## 0. 受付終了

- **当卓は 2026-09-21 02:10:12 JST をもって OPS-SUPERVISOR の新規受付を終了する。** この時刻が宛先切替の成立点。以後の hub 宛は `w2:p1J`。
- 旧 pane・履歴・台帳は保持する。⛔ 旧記録を書き換えない。

## 1. ⭐ 最優先の生きた債務（1 件・本日発生）

**Rs1 が新規 run 1 回を認可した**（裁定 `work/thread-rs1-20260921/PROPOSAL_A_DISPOSITION.md`・66 行・sha256 `9228e03bccfb54c0c0f33344e8b7a3083235fcd851f1af1961c7d5c643908fbe`・**untracked = as-read**・**全読必須**）。当卓は全文を読了し p0/p4 へ `m-p18-519`（02:07:34）で配付した。

⭐ **Rs1 が当卓に課した債務** = **p0 の実行識別子・開始/終了・artifact 所在・停止札を `work/thread-rs1-20260921/P18_TO_RS1.md` へ「一度ずつ」追記する**。到達の 1 回目は P18-TO-RS1-013 で済み、**実行結果の追記は未了**。⇒ **貴卓が引き継ぐ。**
⛔ 当卓も貴卓も**実行しない・裁定しない・再走を認可しない**（追加 run は新しい Rs1 裁定）。

## 2. 便の状態（ID 別・当卓の `pending` 実測 2026-09-21 02:10:12 JST）

- **合計 245 leg が未完了**。⚠ **ほとんどは過去 session の残骸**で、宛先の多くが**既に存在しない卓**（w2:p6 が 61・w2:pZ が 55・w2:p11 が 52・w2:pB が 3・w2:pC が 2）。⇒ **これは生きた待ち行列ではなく、完了させられなかった leg の記録**である。⛔ 一括で完了扱いにしない・⛔ 盲目的に再送しない。
- **注意を要する最近の leg**:
  - `m-p18-519`（**Rs1 の裁定**・02:07:34）= p0 **DELIVERED**／p4 **QUEUED**。
  - `m-p18-518`（当卓の訂正）= p4 **ABSORBED(unacked)**・`verify` 後も同状態。**配達未確定**。⛔ 当卓は盲目再送しなかった。
  - `m-p18-516`（p4 の未了 5 件・01:59:50）= 旧 p11 で**処理されたか不明**。⭐ **内容の欠落は無い**（同じ 5 件を `work/thread-p11-transfer-20260921/P18_TO_P1H.md` P18-TO-P1H-002 に置き、p1H が p4 item 117 を Git 原文から読了済と報告）。
  - `m-p18-499`/`500`/`501` = 旧 pB・旧 pZ の入力列。**処理は当卓には決まらない**（両卓とも agent 不在を実測してから移管）。
- **返答待ち = 1 件のみ** = **p0 の実行報告**（上記 §1）。

## 3. 台帳と道具

- **台帳（当卓の SSOT）** = `eval_runs/troot_optE_dapg_wholeroute_scope_20260701/P18_CLAMP_COURT_EVIDENCE_LEDGER_20260727.md`・**49,712 行**・最終節 **§1653**。⛔ 追記のみ・既存節を書き換えない・訂正は後続節で行う（本日の訂正 6 件はすべてこの形）。
- **送信 tool** = `eval_runs/.../p18_desk_tools_20260905/hub_send.py`（凍結 pin `5b3c93002d`）・本文 = `bodies/`（198 file）・記録 = `sent_records.jsonl`・**採番 = `bodies/.floor` の増分**（現在 **519**）。
  - ⭐ **ID は floor の増分で取る**（送信前に floor を読み、送信後に増えたことを確認する）。⛔ `.floor` を読み直して ID を決めない — 送信失敗時に**他の便の ID を自分の便に付けてしまう**（当卓の実績あり・台帳 §1633）。
  - ⛔⛔ **`check_kind` の Codex 拒否 guard を解除しない**（凍結）。⇒ **Codex 卓へは tool で届かない**。現在 tool で届くのは **`p4` と `p0` の 2 卓のみ**で、**Rs1・映像・ログ・検証・計画・設計の各卓は共有 file 経由のみ**・**配達も読取りも検証できない**。⛔ これらについて「届いた」と書かない（書けるのは「file に置いた」まで）。
- **watcher 常駐プロセス = 無し**（当卓が ${CUT} に `ps` で実測）。
- **共有 file の経路**（Codex 卓向け）: Rs1 = `work/thread-rs1-20260921/P18_TO_RS1.md`／映像 = `work/thread-pc-transfer-20260921/P18_TO_P1D.md`（⛔**数値を送らない**）／ログ = `work/thread-pb-transfer-20260921/P18_TO_P1E.md`／検証 = `work/thread-pz-transfer-20260921/P18_TO_P1F.md`／計画 = `work/thread-p6-transfer-20260921/P18_TO_P1G.md`／設計 = `work/thread-p11-transfer-20260921/P18_TO_P1H.md`。

## 4. commit 規律

- **全 commit `--no-verify` ＋ 明示 pathspec**（`git add -- <paths>` → `git commit --no-verify --only -q -F <msgfile> -- <paths>`）。ref lock 失敗時は 2 秒後に再試行。
- ⛔ **共有ツリーで `./isaaclab.sh -f` を走らせない**（1000 file 規模を書き換える）。
- ⛔ **push は Rs1 の一言があるときだけ**。⭐ **2026-09-21 02:13 に利用者の逐語「push」で実行済** = `cf44999454..72b8666729`・**104 commit（全卓分）**・rc=0・fetch 後 **0 ahead / 0 behind**・remote tip `72b86667292a36e632180ec6fe6e2d8138783f6b`。事前に秘密混入 0 を実測（file 名と追加行の両方）。⇒ **本日の記録は公開済**で、以後の commit から再び差分が溜まる。**本日の当卓の commit = 21 件**（session trailer で数えた。⚠ git user は全卓共有なので author では数えられない — 本日の全卓合計は 104 件）。
- ⛔ **他卓の file を commit しない。**

## 5. ⭐ 本日の当卓の誤り 6 件（後継が同じ穴に落ちないために）

1. **untracked の hash を pin と書いた** → 3 本中 2 本で hash が動いた。⇒ **untracked は as-read**。
2. **便の宛先を取り違えた**（500 を旧 pB 宛と書いた）。⇒ 自分の送信記録で確かめる。
3. **numstat が 7 を出している画面で「+5」と書いた**。
4. **自分が 1 節前に banked した数値と矛盾する行を、Rs1 の判断面まで通した**（#69 を B と C の両方に置いた）。
5. **区間から順序を主張した**（label 移動の時刻が決められないのに「宣言より前」と書いた）。
6. **自分の出力を `cut -c1-230` で切り、6 番目の項目が落ちたまま、総計だけ他卓の便から取った**（自分の並べた数を足せば合わないのに気づかなかった）。
- ⭐ **3〜6 は同じ族** = **手元の測定が答えを決めていないのに、本文が答えを書いた**。⭐ **対処** = ①数は `--numstat` の 1 行をそのまま貼る ②cite 行に述語（停止・書込み・既定値）を付けるなら行の**種類**を見る ③**他卓の主張を通す前に、同じ量について自分が何を記録したかを見る** ④**行を切って表示したなら、切ったと書く**。
- ⭐ **本日の構造的発見**: 同じ誤りを **3 卓が通し、捕らえたのは 1 卓**。3 卓とも矛盾する数値を自分の記録に持っていた。⇒ **便の本文は、受け手の記録を経由せずに面から面へ移る。**

## 6. 未照合のまま渡すもの

- 各共有 file（すべて **untracked = as-read**・hash は観測時刻つき）。
- 245 の未完了 leg の**個別の処理状況**（当卓には決まらない）。
- `m-p18-518` の p4 への配達。
- 旧 pB・旧 pZ の入力列に残った `499`/`500`/`501`。

## 7. 引き継がないもの

⛔ 判断・受入・run 認可・物理妥当性の判定（p4 と Rs1）。⛔ 役割 label を動かす行為。⛔ 他卓の file の commit。⛔ `--no-verify` を包括的例外として読むこと（記録のみの commit に限る運用）。
